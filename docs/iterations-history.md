# Iterations history

Per-iteration changelog of the zcrypto project. New entries are appended at the bottom by Claude Code as the final task of each iteration's implementation plan; each entry is a `## <YYYY-MM-DD> — <heading>` section with a bullet list (one bullet per feature/change/fix). CLAUDE.md's "Project state notes" section points here.

## 2026-07-07 — iter-001: trial registry (Phase 0 · P0-1)

First iteration of the autonomous research loop, working Phase 0 of `docs/research/00.master-plan.md`.

- **Added `cli/registry/`** — the append-only, integrity-checked JSONL trial registry (`TrialRegistry`, `TrialRecord`, `RegistryError`/`RegistryCorruptionError`, plus `canonical_json`/`compute_hash`/`VERDICTS`/`SCHEMA_VERSION`), stdlib-only, mirroring `cli/logging/`. This is the master plan's first-class "integrity by construction" deliverable (§9).
- **Encodes the PoC NaN-DSR failure on both paths** so it cannot recur: `json.dumps(allow_nan=False)` on write and `json.loads(parse_constant=…)` on read, plus a recursive finiteness walk over nested dicts/lists of metrics (rejecting `bool`/numpy leaves via `type(x) is` checks).
- **Integrity by construction:** monotonic-contiguous `trial_id`; per-record `record_hash` self-check (accidental-edit detection — the cross-record hash chain is deferred to Phase 2); `n_trials_in_family >= recorded-family-count` floor (anti-gaming the DSR deflation denominator); `fcntl.flock` + `os.fsync` append that re-derives the next id from disk under lock; and a torn-trailing-line self-heal so a crashed append never bricks the autonomous loop.
- **33 unit tests** incl. planted-corruption: bare-`NaN` token at load, NaN buried in a nested list, `record_hash` mismatch, contiguity gap/dup/reorder, torn-tail heal vs. interior raise, unknown schema, family-count floor, concurrent-unique-ids.
- **Design/plan:** `docs/specs/00000-trial-registry-design.md` (from a 3-proposal + adversarial-critic design panel), `docs/plans/00000-trial-registry.md`. **Deferred to Phase 2:** the cross-record hash chain, the corrupt-a-copy CI test, and SPA/DSR computation. **Known minor:** an external-hand-edited complete last line lacking a trailing newline would make the *next* append concatenate — it fails *loudly* on the following load (never a silent fake winner), left as Phase-2 hardening.
- Phase 0 human-gated items (D3(i) account actions + live fee/AoP confirmation) parked in open-topic **T0000**.
- Reviewed by an independent whole-branch reviewer (verdict: approved, zero Critical/Important defects, integrity core verified under live probes).

## 2026-07-07 — iter-002: Kraken reference-data snapshot register (Phase 0 · P0-2)

- **Added `cli/snapshot/`** — a stdlib-only fetcher/deriver for Kraken's **public** reference endpoints: `fetch.py` GETs `AssetPairs`/`Assets` and raises `SnapshotError` on Kraken's in-body `error` array; `assetpairs.py` derives per-symbol margin/leverage/order-minimum and resolves symbol aliases; `register.py` builds a content-hashed snapshot and renders the register markdown; `errors.py`. No API key, no third-party HTTP client.
- **Committed the live register** `docs/research/01.1.kraken-snapshot-register.md` (added to the mdformat allowlist): the candidate basket's ground-truth margin/leverage table — all 12 §3 symbols online & margin-enabled (EUR majors 2–10×, DOT 2–5×, ETH/BTC 2–5×, SOL/BTC 2–4×) — the Kraken symbol-alias ledger (XBT=BTC, XDG=DOGE), order minimums, and provenance (endpoint + UTC + raw-snapshot sha256). This is the Phase-1 universe-selection ground truth §3 requires. The raw snapshot JSON lands under `data/snapshots/` (gitignored); the doc records its hash.
- **11 unit tests** on saved trimmed fixtures with monkeypatched `urlopen` (no live-network tests): error-array raise, symbol resolution + alias, non-margin/absent flagging, deterministic sha256.
- **Deferred / parked:** live fee-tier, the July-9 AoP rule, and rollover bands remain account-gated (open-topic **T0000**); the L2 capture daemon is Phase 1. **Known minor:** `fetch_public` on a malformed-but-`error`-free response (missing `result` — not a shape Kraken's API actually returns) raises `KeyError` rather than `SnapshotError`; a defensive guard is left as future hardening.
- Independent whole-branch review: approved, zero defects — the reviewer independently recomputed the register's raw-snapshot hash from disk and confirmed the fixtures are genuine trimmed live samples.

## 2026-07-07 — iter-003: NautilusTrader Kraken adapter smoke test (Phase 0 · P0-3)

- **Verified the NautilusTrader Kraken adapter's public data path** — the autonomous, no-trade-key half of the master-plan §7 Phase-0 adapter verification (the Phase-6 execution-engine adoption criterion). Run in a **throwaway venv**; `nautilus_trader` is **not** added to project deps (that is a Phase-6 step). `nautilus_trader==1.230.0` installs cleanly on Python 3.14.6 (cp314 wheel); connected to Kraken's real public Spot WS (`wss://ws.kraken.com/v2`, **no API key**), loaded 1829 instruments, streamed 152 QuoteTicks / 12 TradeTicks / 12 Bars for BTC/USD over a hard-bounded 45s, clean SIGINT shutdown, zero errors.
- **Memo:** `docs/research/01.2.nautilus-kraken-adapter-memo.md` (added to the mdformat allowlist) — the data-client API surface, the confirmed keyless public-data config, two documented rough edges (`InstrumentProviderConfig(load_ids=[...])` crashes the factory's `lru_cache` → use `load_all=True`; `node.run()` sync vs `run_async()` event-loop constraint), and a measured verdict (data path viable; caveats: short single-pair Spot-only run — no L3/Futures/reconnect/soak).
- **Parked (Phase 6 / T0000):** the execution side — spot-margin/leverage/short/post-only order semantics + reconciliation — needs a trade key.
- **This completes Phase 0's autonomous work** (trial registry, snapshot register, adapter memo). The remaining Phase-0 exit-bar items (ratifications already dated 2026-07-06; the D3(i) account actions) are human-gated in T0000. The loop advances to **Phase 1 (Data Foundation)** next.

## 2026-07-07 — iter-004: OHLC ingestion → canonical Parquet (Phase 1 · v0)

- **Added `cli/ohlc/`** — a public-REST OHLC → canonical **polars** Parquet ingestion pipeline (the project's first research-core data dep, `polars>=1.42.1`): `fetch.py` (public `OHLC` GET, raises `OHLCError` at every failure boundary incl. a missing/`last`-only result and unparseable string decimals), `dataset.py` (`to_frame` — typed schema: UTC `ts`, Float64 OHLCV/vwap, Int64 count, sorted+deduped, raises on NaN/non-monotonic; `write/read_parquet`; deterministic `dataset_hash`), `ingest.py` (`ingest_basket` — writes `data/ohlc/{symbol}/{interval}.parquet` + `manifest.json`, returns a content-hashed manifest), `errors.py`.
- **Generated the v0 dataset** — 12 candidate symbols (resolved live via `cli.snapshot`) × 3 decision-cadence intervals (1d/4h/1h) = 36 series × 721 rows (Kraken's REST cap ≈ 2y daily / 120d 4h / 30d 1h), under gitignored `data/ohlc/`. Cataloged in `docs/data-catalog.md` (symbol×interval → rows/span/sha256 + fetched_at).
- **18 unit tests** on a saved trimmed `OHLC` fixture + monkeypatched opener (no live-network tests): error-array + boundary raises, decimal-parse, sort/dedupe, NaN raise, Parquet round-trip, `dataset_hash` determinism + sensitivity, manifest persistence.
- **Parked (→ open-topic T0001):** full-history backfill from Kraken's downloadable OHLCVT ZIP archive — the ~2y REST window is insufficient for §9's 2019→2026 walk-forward regimes; this v0 dataset is what the backfill later extends. **Known minor:** `dataset_hash` stability depends on the polars version's float formatting — a polars bump may require regenerating the catalog hashes.
- Independent whole-branch review (Sonnet): initial verdict CHANGES REQUIRED (the catalog documented a `manifest.json` the pipeline didn't write; two error boundaries leaked non-`OHLCError` exceptions) — all fixed and re-review APPROVED, verified by execution.

## 2026-07-07 — iter-005: rule-driven universe finalization (Phase 1)

- **Added `cli/universe/`** — the §3 mechanical universe rule, the first iteration to *consume* two prior iterations' outputs (iter-002's margin/leverage snapshot + iter-004's daily OHLC): `volume.py` (`median_quote_volume` — 30d median of volume×vwap), `rules.py` (`finalize_universe` — margin ∧ leverage≥2× ∧ median-quote-volume≥floor; BTC/ETH-EUR mandatory; `escalate` on <8/>15 names), `build.py` (`build_universe_file`/`render_markdown` — the point-in-time file with per-symbol criteria + provenance + `spread_cap: pending-capture`), `errors.py`.
- **Generated the point-in-time universe file** `docs/universe/point-in-time-universe.md` — and its first real run **fired the §3 escalation**: at the (loop-chosen, tunable) €1M/day EUR-volume floor only **6 names** pass (BTC/ETH/SOL/XRP/ADA/LTC-EUR); LINK/DOGE/DOT/AVAX have genuinely thin Kraken-EUR liquidity (<€1M/day median), and the BTC-quoted RV legs (ETH/BTC, SOL/BTC) fail on a quote-currency unit mismatch (BTC-denominated volume vs a EUR floor). This divergence from §3's recommended 10–12 basket is a human decision → parked in **open-topic T0002** (calibrate the floor / route thin alts via deeper USD pairs / accept a smaller basket / make the volume floor quote-currency-aware).
- **13 unit tests** (synthetic inputs, no live network): median-volume + too-few-rows raise, leverage/volume-floor drops, mandatory-kept-though-failing + flagged, escalate boundaries, reasons populated, build determinism.
- **Deferred (non-blocking, → T0002):** relabel cross-quote volume reasons as "quote-currency mismatch — deferred"; quote-currency-aware / FX-normalized volume; the spread criterion still awaits the L2 capture daemon. A `MAX_NAMES`-boundary test is a noted test-coverage gap.
- Independent whole-branch review (Sonnet): APPROVED — rule engine correct + tested; the BTC-leg mismatch adjudicated as a narrow, non-silent limitation legitimately deferred to T0002 (escalate=True guarantees human review before production).

## 2026-07-07 — iter-006: OHLC dataset QA report (Phase 1)

- **Added `cli/ohlc/qa.py`** — read-only QA over the canonical OHLC Parquet dataset: `detect_gaps` (missing candles on the interval grid), `wick_outliers` ((high−low)/close > 0.20), `qa_series` (rows, coverage%, gaps, wick outliers, monotonic-ts, nonneg-volume), `qa_dataset` (walks the nested `{base}/{quote}/{interval}.parquet` tree — symbols contain `/`), `render_markdown`. Pure (`as_of` injected; no clock/network); stdlib + polars.
- **Generated the QA report** `docs/ohlc-qa-report.md` over the v0 dataset — **clean: 100% grid coverage, 0 gaps across all 36 series** (12 symbols × 1d/4h/1h); 87 wick-outlier flags, all on daily altcoin candles (a benign heuristic flag, not a failure).
- **10 unit tests** on synthetic frames (no live-dataset reads): planted gap → `missing==1`, planted extreme candle flagged, coverage math, monotonic/nonneg flags, whole-dataset walk + summary, `as_of` present/omitted.
- **Deferred (follow-up Phase-1 iterations):** empty-interval reconstruction (fill no-trade candles — the gaps this surfaces), Binance-Vision cross-venue reconciliation, the symbol & corporate-action ledger.
- Independent whole-branch review (Sonnet): APPROVED — verified by regenerating the report from the live dataset (byte-identical), confirming the nested-layout walk matches the `manifest.json` symbol convention, and checking every correctness detail (missing-candle count, coverage math, div-by-zero→inf-not-crash, empty-frame guards).

## 2026-07-07 — iter-007: universe floor calibration + quote-currency-aware volume (Phase 1 · T0002)

- **Resolved open-topic T0002** — iter-005's §3 escalation (only 6 of 12 names cleared the €1M/day EUR floor). Grounded in a live **EUR-vs-USD-vs-USDT** depth + margin-parity + FX/tax analysis (multi-agent workflow, adversarially verified): USD Kraken books are 2–8.5× deeper than EUR for all 10 bases, but that depth does **not** bind at ~$10k (a full per-name position is <1% of even the thinnest EUR book) and EUR gives up **zero** margin capability (EUR=USD exact `leverage_buy`/`leverage_sell` parity on all 10 bases), while USD adds conversion + continuous cash-leg EUR/USD risk + per-fill §23-EStG tax joins + cross-margin FX double-counting. USDT ruled out entirely (thinnest venue, MiCA-delisted for EEA, AVAX/USDT non-marginable, LTC/DOT-USDT capped at 3×).
- **`cli/universe/` changes** (commit `9495e2d`): lowered `DEFAULT_MIN_MEDIAN_QUOTE_VOLUME` €1M → **€150k**/day (footprint-based — a full max-size position ≈1% of median daily EUR volume, our microstructure-impact floor); added `quote_volume_in_eur(daily, *, fx_daily=None, window=30)`, which FX-normalizes a BTC-quoted leg's `volume × vwap` to EUR via the BTC/EUR daily close (inner-join on `ts`, then **sort before `.tail`** so the latest window is always selected — a join-ordering guard added in review, since polars does not guarantee join output order). 6 new tests (144 total).
- **Regenerated `docs/universe/point-in-time-universe.md`** — now **12 of 12** selected, `escalate=False`: the four previously-thin EUR alts (LINK/DOGE/DOT/AVAX) clear €150k, and the two BTC-quoted RV legs FX-normalize to €579,964 (ETH/BTC) and €233,595 (SOL/BTC), ending the unit mismatch — the full §3 target (10 EUR + 2 BTC RV). `data/universe/point-in-time-universe.json` regenerated (gitignored; new sha256 recorded in the doc's provenance).
- **Master-plan §3** — inserted a "Quote leg, liquidity depth, and the floor — measured (Phase 1)" subsection + a floor-decision paragraph: the measured EUR/USD/USDT depth, the margin parity, the friction rationale for EUR, the size-dependence (depth doesn't bind at our footprint), and USD-routing deferred as a pre-registered **scaling trigger**. Supersedes the earlier unquantified EUR-preference note.
- Independent review of the code commit (Sonnet): APPROVED — the FX-normalization correctness and the `.sort("ts")` join-ordering guard both verified (the reviewer reproduced the failure mode empirically).
- **Decisions logged** to `.tmp/decisions.md` (iter-007). **Open-topic sweep:** T0002 → resolved + archived; T0001 (OHLCVT backfill) and T0000 (residual Phase-2 cost-model fold-in) remain open.

## 2026-07-07 — iter-008: full-history OHLCVT backfill (Phase 1 · T0001)

- **Added `cli/backfill/`** — reconstructs canonical OHLC bars from Kraken's downloadable **OHLCVT 1-minute dumps** (on the NAS): `read.py` (`read_minute_rows` — merges the base dump + quarterly-update zips), `aggregate.py` (`aggregate_minutes` — 1-minute → 1h/4h/1d: OHLC first/max/min/last, volume+trades summed, **vwap = Σ close·vol / Σ vol**, a proxy since the dumps carry no vwap), `backfill.py` (`backfill_basket` — canonical Parquet + manifest, reusing `cli.ohlc.dataset`), `reconcile.py` (vs the v0 REST), `errors.py`; `ohlcvt_source_dir` added to `cli.config` / `zcrypto.toml`. **27 tests** on synthetic ZIP fixtures.
- **Generated the full-history dataset** — `data/ohlc-full/` (gitignored): the 12-name universe × 1h/4h/1d, **BTC/EUR daily 2013-09-10 → 2026-03-31** (4581 rows). The 1-minute bars are full-history (only *native* 4h is short, 2024+), so **all cadences are reconstructed from 1-minute**, giving full-history 1h/4h/1d + a real vwap. Cataloged in `docs/data-catalog-full.md`.
- **Validated** (`docs/ohlcvt-backfill-reconciliation.md`) — reconstructed OHLC is **bit-identical to the v0 REST** (100% exact match over 623 daily + 137 4h overlap rows per pair); vwap proxy within ~0.05% of REST's true vwap. Honest caveats: daily volume within ~7% max on the worst bar (aggregation/revision artifact; 4h volume exact); 1h not independently reconciled (v0's REST 1h window post-dates the 2026-03-31 dump end). QA: 7807 no-trade gaps, 90.5% min coverage (expected for thin markets / early history).
- **Merge policy** — found by an instrument-check smoke test on the real archive: the base dump and 2023-2025 quarterly updates overlap and disagree on volume/trades (the base is more complete) → the base dump is authoritative for its range and quarterlies extend only past its last ts (Q1-2026, exactly contiguous).
- **Design/plan:** `docs/specs/00005-ohlcvt-backfill-design.md`, `docs/plans/00005-ohlcvt-backfill.md`. **Resolves open-topic T0001** (→ archived). **Deferred:** finer cadences (1m/5m/15m), full-exchange breadth, empty-interval reconstruction, Binance-Vision cross-check, the corporate-action ledger, and pointing the universe/backtests at the new dataset hash (a Phase-2 decision).
- Independent whole-branch review (Sonnet): APPROVED — after two review-driven hardening fixes (numeric de-dup + `BackfillError` at every boundary; the base-authoritative merge policy), both verified live; the 100% OHLC reconciliation vs REST corroborates the reconstruction.

## 2026-07-07 — iter-009: full-history dataset QA report (Phase 1)

- **Generated `docs/ohlc-full-qa-report.md`** — ran `cli.ohlc.qa` (iter-006) over the new full-history dataset `data/ohlc-full/` (iter-008): 36 series (12 pairs × 1h/4h/1d), integrity clean (monotonic ts + non-negative volume on every series). Coverage is high — the 7807 gaps are **no-trade intervals** (Kraken omits them; reported, not filled), concentrated in 1h for thinner/older pairs (worst: LTC/EUR/60 at 90.5%; most series 97–100%).
- **Gap characterization for §9** — per-year bar density: BTC/EUR 1h is **100% dense 2014–2025** (2013:18% partial Sept listing; 2026:25% data-to-March); DOT/EUR 1d 100% 2021–2025 (2020:37% partial listing). So the full-history intraday carries reliable density across the major walk-forward regimes (2014+); thin-alt early 1h is sparser — flagged for regime-aware validation.
- Manufactured package (the R&D open-topics backlog was empty after T0001/T0002 resolved) — "deepen data QA" per the loop's empty-backlog rule. No code change (reuses iter-006's `cli.ohlc.qa`); the report + gap characterization are the deliverable. **Deferred follow-ups** (empty-interval reconstruction, Binance-Vision cross-check, the symbol & corporate-action ledger) unchanged.

## 2026-07-07 — iter-010: symbol & corporate-action ledger + discontinuity audit (Phase 1)

- **Added `cli.ohlc.qa.price_discontinuities`** — flags bar-over-bar close moves beyond 3× (or below 1/3×): candidate corporate actions (splits, redenominations) or data errors. 5 tests (176 total).
- **Committed `docs/symbol-corporate-action-ledger.md`** — the §3-required ledger for the 12-name universe: Kraken aliases (XBT=BTC, XDG=DOGE + the dump altnames `ETHXBT`/`SOLXBT`/…), the corporate-action check, and quote-book migrations (the USDT EEA delisting — not applicable to our EUR/BTC legs).
- **Discontinuity audit** over the full-history dataset (`data/ohlc-full/`, daily): **only 2 flags across all 12 symbols, both genuine market events, not corporate actions** — ETH/EUR 2015-08-08 (ratio 0.25, ETH's chaotic launch weeks) and DOGE/EUR 2021-01-28 (ratio 4.89, the Jan-2021 WSB/Elon pump). **DOT/EUR shows no discontinuity** — confirming DOT's Aug-2020 1:100 redenomination is *not* in our history (Kraken listed DOT post-redenomination). Conclusion: the full-history dataset is **clean of corporate-action price artifacts** for the universe; no price adjustments needed for backtesting.
- Manufactured Phase-1 package (§3 hard-dependency; R&D open-topics backlog empty). **Deferred data-foundation follow-ups** remaining: empty-interval reconstruction, Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED — the function + tests verified; the ledger's audit findings independently re-run (2 flags, DOT clean) and the alias table cross-checked against `cli.backfill` / `cli.snapshot`.

## 2026-07-08 — iter-011: empty-interval reconstruction — fill_gaps (Phase 1)

- **Added `cli.ohlc.reconstruct.fill_gaps`** — empty-interval reconstruction as an **opt-in on-read transform**: inserts a synthetic bar per missing grid point (OHLC = vwap = the prior forward-filled close; volume = 0, count = 0), returning a contiguous frame (`detect_gaps` → `[]` after). 6 tests (182 total).
- **Design (distrust-the-instrument):** the fill is a reusable utility, **not** baked into a canonical dataset — a synthetic zero-volume flat candle looks like a real bar and can bias features, so the fabrication stays explicit + opt-in at use time. A filled dataset variant is generatable on-demand if a strategy needs contiguous bars.
- Resolves the "empty-interval reconstruction" deferred data-foundation follow-up (iter-004/006). **Remaining Phase-1 follow-up:** the Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED.

## 2026-07-08 — iter-012: Binance cross-venue cross-check (Phase 1)

- **Added `cli/xcheck/`** — an independent-venue sanity check on the full-history dataset: `binance.py` (`binance_pair_name` maps the canonical `BASE/QUOTE` to Binance's concatenated symbol; `fetch_binance_klines` GETs daily klines from `data-api.binance.vision` via stdlib `urllib`; `binance_daily_closes` → `{ts, close}`; `crosscheck_series` reports overlap rows + close/return Pearson correlation + max abs relative diff; `crosscheck_dataset` walks the dataset root), `errors.py` (`XCheckError`). **19 tests** (201 total) on synthetic frames + mocked `urllib` (no live network in the suite).
- **Generated `docs/binance-crosscheck-report.md`** — Kraken vs Binance daily closes over the recent overlap window (902 rows/pair): **close correlation 1.0000 on every one of the 12 universe series**, return correlation ≥ 0.998, max abs relative diff ≤ 4.8% on the worst single bar (DOT/EUR 2025-10-10 — a venue-specific daily-close timing artifact, not a data error). The two venues corroborate each other; the full-history dataset is venue-consistent.
- **Scope (disclosed in the report + docstrings):** the cross-check covers the most recent **≤ 1000 daily candles** per pair — Binance's single-page kline limit. Binance EUR history reaches only ~2020 anyway; a full-overlap check via `startTime` pagination is a deferred follow-up.
- **Distrust-the-instrument:** the review independently verified the correlation metric is genuine — it returns −1.0 on anti-correlated inputs and ≈0.60 on independent random walks, so corr≈1.0 on the real data is a real finding, not a formula that always returns ~1.
- Resolves the last deferred Phase-1 data-foundation follow-up (the Binance-Vision cross-check). Manufactured Phase-1 package (R&D open-topics backlog empty).
- Independent whole-branch review (Sonnet): APPROVED — after review-driven fixes (mocked fetch-error-path tests, the recent-window scope caveat in report + docstrings, and skipping malformed symbols in `crosscheck_dataset`), all verified.

## 2026-07-08 — iter-013: CPCV splitter — validation harness opens (Phase 2)

- **Opened `cli/validation/`** (the §9 "harness is the product" package) with the **CPCV (combinatorial purged cross-validation) splitter** — the first Phase-2 component, built on synthetic index positions before any strategy exists. `cpcv.py`: `make_groups` (contiguous half-open `[start, stop)` blocks, first `n%N` one larger), `n_backtest_paths` (`C(N-1, k-1)`), `cpcv_splits` (every `C(N, k)` combination of `k` test groups → `{test_groups, train, test}` with purge + embargo); `errors.py` (`ValidationError`). Stdlib-only (`itertools`, `math`); pure functions, no data/strategy/logging.
- **Purge + embargo (the leakage defense, §9.1), per contiguous test block `[a, b]`:** purge drops training indices in `[a - label_horizon, a)` (their forward label window reaches into the test); embargo drops indices in `(b, b + embargo]` (serial-correlation buffer; caller sets `embargo ≥ label horizon + feature lookback`). Non-adjacent test groups form multiple blocks, each purged/embargoed independently. **26 tests** (full suite 227) — property-style: coverage `= C(N-1, k-1)` per group, train ∩ test = ∅, and no train index leaks into any test block.
- **Distrust-the-instrument:** because this splitter judges every future strategy, the whole-branch review **mutation-tested** the property tests — planting under-purge / under-embargo / forgot-per-block bugs and confirming each turns a purge/embargo test red — proving the tests catch real train→test leakage, not tautologies. The leakage math was independently re-derived + executed against hand calculations; no off-by-one.
- **Deferred (harness-precision follow-up):** the property tests verify the purge/embargo windows are *sufficient* (catch all leakage) but not *tight* (leakage-safe over-trimming isn't caught) — a two-sided-bound assertion belongs in the Phase-2 acceptance-suite iteration, per the review.
- **Design/plan:** `docs/specs/00006-cpcv-splitter-design.md`, `docs/plans/00006-cpcv-splitter.md`. First Phase-2 iteration (kickoff decision logged in the running `.tmp/decisions.md`).
- Independent whole-branch review (Sonnet): APPROVED — leakage math, path-count formula, guards, and spec compliance all verified; tests confirmed non-tautological via mutation testing.

## 2026-07-08 — iter-014: deflated & probabilistic Sharpe ratio (Phase 2)

- **Added `cli/validation/dsr.py`** — the §9.4 multiple-testing control: `probabilistic_sharpe_ratio` (PSR = Φ of the Sharpe estimator's studentized excess over a benchmark, with the non-normal skew/kurtosis SE correction), `expected_max_sharpe` (Bailey & López de Prado's expected maximum of `N` i.i.d. null trial Sharpes), and `deflated_sharpe_ratio` = PSR deflated by that expected-max benchmark (more trials ⇒ higher bar). Stdlib `statistics.NormalDist` (Φ, Φ⁻¹); pure scalar functions consuming the trial-Sharpe distribution CPCV (iter-013) produces. **31 tests** (full suite 258).
- **NaN-refusal is the deliverable (§7 PoC lesson).** The PoC minted a fake winner from a NaN deflated-Sharpe; this module raises `ValidationError` on any degenerate input rather than returning NaN — the integrity the trial registry asserts on read.
- **Distrust-the-instrument — the review caught the cardinal bug live.** The first whole-branch review (adversarial NaN-hunt) found that the integer count params `n_trials`/`n_obs` were range-checked only (`< 1`, `< 2`), never `isfinite`-checked — and `NaN < 1` is `False`, so `deflated_sharpe_ratio(1.5, float("nan"), 100, 1.0)` **returned NaN**, the exact PoC failure. It also found `expected_max_sharpe(inf)` / `(1e16)` raising a bare `StatisticsError` instead of `ValidationError`. Fixed (commit `743b01b`): `isfinite` guards on both counts + an inverse-CDF domain guard (`0 < 1 - 1/N < 1`) for extreme finite `n_trials`; regression tests for `nan`/`inf`/`1e16` and the `denom == 0` boundary. Re-review verified the boundary (1e15 finite → 9e15/1e16 raise) and found no remaining leak.
- **Design/plan:** `docs/specs/00007-deflated-sharpe-design.md`, `docs/plans/00007-deflated-sharpe.md`. Formulas independently re-derived in review (`expected_max_sharpe(10, 1.0) = 1.5746`; PSR SE denominator `1 + sr²/2` for normal returns; `√(n_obs−1)`; full — not excess — kurtosis).
- Independent whole-branch review (Sonnet): APPROVED after the NaN-leak fix — formulas correct, every degenerate input now raises `ValidationError`, no non-`ValidationError` crash path remains.

## 2026-07-08 — iter-015: probability of backtest overfitting (Phase 2)

- **Added `cli/validation/pbo.py`** — the §9.4 companion to DSR: **PBO via CSCV** (Bailey/Borwein/López de Prado/Zhu). Over **every** `C(S, S/2)` symmetric split of `S` time blocks into in-sample/out-of-sample, it takes the IS-best config and measures its **average OOS rank** `r = #{oos<v} + (#{oos==v}+1)/2`; the split is overfit iff `logit(r/(N+1)) < 0` (IS-best below the OOS median); PBO = the overfit fraction. Reuses `make_groups` for the block partition; `metric` defaults to `statistics.mean`, callable-overridable. Returns `{"pbo", "n_combinations"}`. **18 tests** (full suite 276).
- **Bounded ⇒ never NaN.** `r ∈ [1, N]` (the equality count includes the config itself), so `w = r/(N+1) ∈ (0, 1)` strictly and the logit is always finite; degenerate *inputs* raise `ValidationError` — non-rectangular / non-finite matrix, odd or non-int `n_splits`, `T < n_splits`, `N < 2`, a `metric` that returns non-finite **or raises**.
- **Distrust-the-instrument — review + mutation testing again earned its keep.** The whole-branch review hand-derived the CSCV math (matched all exact cases) and **mutation-tested** the suite: flipping the overfit sign or the rank comparator turns the exact-value tests red (good), but last-argmax tie-breaking and a `<=` boundary initially slipped through. It also found two non-`ValidationError` crash paths — a `metric` that *raises* (e.g. `statistics.stdev` on a size-1 block → `StatisticsError`) and an even-*float* `n_splits` (`2.0 % 2 == 0` → later `TypeError` in `make_groups`). Fixed (commit `0857ff5`): wrap the metric calls, add an `isinstance(n_splits, int)` guard, and regression-test both crash paths + the first-argmax (`pbo=0.5`) and `w==0.5` strict-`<` (`pbo=0.0`) boundaries. Re-review re-ran the mutations — all now killed.
- **Design/plan:** `docs/specs/00008-pbo-design.md`, `docs/plans/00008-pbo.md`. Completes the DSR + PBO overfitting-detection core of §9.4.
- Independent whole-branch review (Sonnet): APPROVED after the crash-path fix — CSCV math correct, every degenerate input raises `ValidationError`, tie/boundary conventions test-pinned.

## 2026-07-08 — iter-016: stationary block bootstrap CIs (Phase 2)

- **Added `cli/validation/bootstrap.py`** — the §9.5 uncertainty layer: `stationary_bootstrap_indices` (Politis & Romano resampling — geometric blocks via restart prob `1/mean_block`, wrap-around advance) and `bootstrap_ci` (seeded/reproducible percentile CI of a caller-supplied `statistic`, linear-interpolation percentile). Stdlib `random`; returns `{point, lower, upper, n_resamples, mean_block}`. **20 tests** (full suite 296).
- **Reproducible + never crash weirdly.** `random.Random(seed)` is function-local (verified independent of global RNG state), so output is byte-stable given the seed; degenerate inputs raise `ValidationError` — non-finite series, `mean_block < 1`/`inf`, bad `alpha`/`n_resamples`, non-`int` seed, and a `statistic` that returns non-finite or **raises**.
- **Distrust-the-instrument — review + empirical checks.** The whole-branch review empirically validated the resampling (avg run length ≈ `mean_block`: 5.00 for mean_block=5; 1.00 for mean_block=1 = i.i.d.) and the percentile math, then found two non-`ValidationError` crash paths — a NaN/non-int-float `n_obs`/`n_resamples` slipping the range-only guard into `range()`/`randrange()` (`TypeError`), and `_stat`'s finiteness check sitting *outside* its try so a `statistic` returning `str`/`None` crashed. Fixed (commit `72079da`): `isinstance(int)` guards on the count params + move the finite check inside `_stat`'s try; regression-tested both.
- **Follow-up opened — T0006.** The review also flagged a systematic (pre-existing) pattern: non-numeric-*type* args to the float params (`mean_block`/`alpha` here; `sr`/`var_trials`/`skew`/`kurtosis` in `dsr.py`; `perf_matrix` cells in `pbo.py`) raise `TypeError` not `ValidationError`. None can return NaN — it's discipline/consistency, not a validity hole — so it's parked as one coherent harness-hardening pass (T0006) rather than fixed piecemeal.
- **Design/plan:** `docs/specs/00009-stationary-bootstrap-design.md`, `docs/plans/00009-stationary-bootstrap.md`. Fourth Phase-2 harness component (§9.5).
- Independent whole-branch review (Sonnet): APPROVED after the crash-path fix — resampling + percentile math correct, RNG isolation verified, every degenerate input raises `ValidationError`.

## 2026-07-08 — iter-017: Kraken cost model — fees + margin (Phase 2)

- **Opened `cli/costs/`** — the §7/§8/§12 explicit cost model, first two legs: `fees.py` (`SPOT_FEE_TIERS` — the confirmed **July-9-2026** 17-tier ladder; `spot_fee_rates(30d_volume)` → 1-based tier + maker/taker; `round_trip_fee`) and `margin.py` (`MARGIN_RATES` per base; `margin_rate(base, band)`; `margin_carry` = `notional·rate·(1 + floor(hold_hours/4))` — opening + one rollover per completed 4h). Values transcribed verbatim from `docs/kraken-fee-schedule.md` (T0000): base **Tier 1 0.40% maker / 0.80% taker** (a taker round trip = 1.60%, superseding the master-plan §1/§4/§14 0.25/0.40 snapshot); BTC margin 0.01–0.02%/4h, alts 0.02–0.04%/4h. **29 tests** (full suite 325). `CostModelError` guards; stdlib-only.
- **Deferred (scoped):** the **spread** term (T0003-gated — captured L2), a combined total-trade-cost function (needs spread), and the **AoP** qualification path (moot below $20k held — YAGNI) are explicit non-goals, not stubbed.
- **TDD discipline caught a spec bug (mine).** The plan's/spec's test asserted `spot_fee_rates(9_999) → Tier 1`, but $9,999 ≥ $2,500 is **Tier 2** — the implementer refused to loosen the assertion and reported BLOCKED; I corrected the spec + plan (Tier 2, plus a real just-below-Tier-2 probe at $2,499 → Tier 1) rather than the code, which was right. The whole-branch review then cross-checked all 17 tiers + margin rates line-by-line against the source doc (no mismatch), verified the tier boundaries and the `floor` rollover convention (24h → 6 rollovers, matching §4), and confirmed the `"BTC"`-not-`"XBT"` base-key convention gives a loud `CostModelError` on the dump altname (not a silent mis-cost).
- **Design/plan:** `docs/specs/00010-cost-model-design.md`, `docs/plans/00010-cost-model.md`. Fifth Phase-2 component; on the §12 exit bar ("cost model validated…", whose captured-spread calibration remains T0003-gated).
- Independent whole-branch review (Sonnet): APPROVED, no findings — fee/margin values line-by-line correct, boundaries + rollover convention verified, guards clean.

## 2026-07-08 — iter-018: performance statistics (Phase 2)

- **Added `cli/validation/metrics.py`** — the statistics a strategy is scored on: `sharpe` (`(mean − risk_free)/sample-stdev`, optional `× sqrt(periods_per_year)`), `volatility` (sample stdev, optional annualization), `annualized_return` (geometric: `prod(1+r)**(ppy/n) − 1`), `max_drawdown` (worst peak-to-trough on the equity curve, as a non-negative fraction). These are the `statistic` callables the bootstrap CIs and the (later) acceptance-suite planted-signal recovery consume — the harness previously had no way to compute a headline number. **25 tests** (full suite 350). Stdlib-only.
- **Never NaN.** The two traps are guarded: zero-variance returns (Sharpe `x/0` → `ValidationError`) and any `1 + r <= 0` (a ≤ −100% period return breaks geometric compounding / the equity curve → `ValidationError`). Plus `len<2` (Sharpe/vol), empty (return/maxDD), non-finite inputs, and non-positive-int `periods_per_year`.
- **Distrust-the-instrument — mutation testing caught a test gap.** The whole-branch review hand-derived + executed all four formulas (Sharpe uses *sample* stdev not population; annualize by `sqrt(ppy)` not `ppy`; `annualized_return` exponent is `ppy/n`; maxDD non-negative — all correct), then found the `annualized_return` exact tests both had `n == periods_per_year` (or `cumulative == 1`), so a `ppy/n → n/ppy` exponent inversion would ship green. Fixed (commit `e857f49`): added a case with `n≠ppy` and `cumulative≠1` (`[0.01]*10, ppy=252 → 1.01**252 − 1`) that diverges sharply under the inversion; re-review confirmed the mutant now fails.
- **Design/plan:** `docs/specs/00011-perf-metrics-design.md`, `docs/plans/00011-perf-metrics.md`. Sixth Phase-2 harness component. (`metrics.py`'s `risk_free` float param falls under the T0006 non-numeric-type-guard sweep.) Deferred: sortino/Calmar, returns-from-prices, benchmark-relative deltas.
- Independent whole-branch review (Sonnet): APPROVED after the test-gap fix — formulas correct + NaN-safe under every adversarial input, exact-value tests load-bearing (verified via mutation).

## 2026-07-08 — iter-019: trial-registry hash chain (Phase 2)

- **Added a `prev_hash` chain to `cli/registry/`** (§9.7 tamper-evidence) — each record now commits to its predecessor's `record_hash` (`GENESIS_HASH = "0"*64` for the first; `SCHEMA_VERSION` 1→2). Because `prev_hash` lives inside the hashed body, the existing self-hash already protects it; the one genuinely new check is the cross-record link (`_assert_cross_record`: `prev_hash == prior.record_hash`). This closes exactly the gap the Phase-0 registry's own docstring named: the self-hash *"is NOT tamper-evidence against a re-hashing writer."* 6 new corruption tests + 3 existing updated for the new field (all other integrity tests unaffected); full suite 356.
- **The new capability, proven live in review.** A metric tamper on a middle record **with a correctly recomputed `record_hash`** — invisible to the Phase-0 self-hash — now breaks the *next* record's `prev_hash` link → `RegistryCorruptionError`. The reviewer reproduced this in a throwaway dir, confirmed it's the *chain* (not the self-hash) catching it, verified the documented residual gap (a writer re-hashing the *entire trailing suffix* can still forge a consistent chain — a non-goal needing an external anchor), and confirmed the torn-tail self-heal still heals cleanly before the chain check runs.
- **No existing integrity weakened.** The review audited all 3 updated tests (strictly additive: schema bump + `prev_hash` field) and confirmed contiguity / monotone-family / self-hash-mismatch / NaN-rejection / self-heal / concurrency all still meaningfully fire under the new schema; `SCHEMA_VERSION=2` rejects a v1 record unconditionally. Review-requested fix applied: the stale `TrialRegistry` docstring was refreshed to describe the chain (it had still said the chain didn't exist).
- **Design/plan:** `docs/specs/00012-registry-hash-chain-design.md`, `docs/plans/00012-registry-hash-chain.md` (supersedes the Phase-0 `00000` v1 schema to v2). Seventh Phase-2 component; the corruption-detection test is the §9.7 CI acceptance the acceptance suite will reuse.
- Independent whole-branch review (Sonnet): APPROVED — tamper-evidence proven live, no existing integrity check weakened, no NaN/corruption path slips through.

## 2026-07-08 — iter-020: acceptance suite — recovery + null (Phase 2)

- **Added `cli/validation/synthetic.py`** (`linear_signal` — deterministic `r = beta·x + noise·ε` generator; `sign_strategy_returns` — a toy `sign(x)·r` strategy) + **`tests/test_acceptance.py`** — the harness's **first end-to-end composition** of CPCV + metrics + PSR on synthetic data. **15 new tests** (full suite 371). Stdlib-only; `ValidationError` guards; deterministic (function-local `random.Random(seed)`).
- **The §9 acceptance claims, measured (not fudged).** Planted signal (beta=0.5): per-period **Sharpe = 0.3769** (matching the spec's derived ≈0.382), **PSR = 1.0**, and **median OOS path-Sharpe = 0.3768** over CPCV's 45 splits — signal recovered. Null (beta=0): **1/20** seeds flag `PSR > 0.95` (≈ the nominal 5% false-positive rate; `P(≥5)≈0.003`), null-median PSR **0.487** — noise rejected.
- **Distrust-the-instrument — the acceptance test itself was proven meaningful.** The whole-branch review independently re-derived the Sharpe math, spot-checked 8 seeds (0.34–0.42, so seed-42 isn't cherry-picked), and — crucially — **ran the null through the planted-recovery assertions and confirmed they FAIL** (`sr=-0.003, psr=0.448`), proving the suite genuinely discriminates signal from noise rather than passing unconditionally. It also proved `test_signal_beats_null_median` is logically implied by the other two (kept as a documented defense-in-depth contrast) and that the null test is a one-sided bound meaningful only *paired* with the recovery test (now commented so a future edit can't silently create the gap).
- **Scope (precise):** the CPCV call exercises **fold-structure** correctness (grouping + `C(10,2)=45` enumeration, zero train/test overlap), not purge/embargo temporal-leak prevention — the toy rule isn't fit to data, so there's nothing to leak. The **injected-leak** end-to-end check (embargo demonstrably removes a serial-correlation leak) + folding the iter-019 registry-corruption test into a single named §12 acceptance suite is **iter-021** (its own careful design).
- **Design/plan:** `docs/specs/00013-acceptance-recovery-null-design.md`, `docs/plans/00013-acceptance-recovery-null.md`. Eighth Phase-2 component; a §12 exit-bar step (the full "acceptance suite green in CI" + captured-spread cost validation remain — the latter T0003-gated).
- Independent whole-branch review (Sonnet): APPROVED, no Critical findings — checks proven meaningful (fail on a broken harness), statistics re-derived + matched, fully deterministic.

## 2026-07-08 — iter-021: validation numeric-param type guards — T0006 (Phase 2)

- **Hardened `cli/validation/` numeric params (resolves open-topic T0006).** Added `isinstance(x, (int, float))` guards **before** the existing `math.isfinite`/range checks for the float params — `bootstrap.py` (`mean_block`, `alpha`), `dsr.py` (`sr`, `var_trials`, `benchmark_sr`, `skew`, `kurtosis`), `pbo.py` (`perf_matrix` cells), `metrics.py` (`risk_free`), `synthetic.py` (`beta`, `noise_sd`) — so a non-numeric **type** (`str`/`None`) now raises `ValidationError` instead of a bare `TypeError`. Strictly additive; **11 regression tests** (full suite 382). Uniform never-crash discipline across the harness.
- **Closed per defined scope.** `dsr.py`'s `n_trials`/`n_obs` remain `isfinite`-only, exactly as T0006's own text scoped them (they feed arithmetic, not `range()`; a non-numeric type there gives a clear `TypeError`, never NaN) — the review verified the closure is honest ("closed per defined scope," not "pattern eliminated everywhere"). **T0006 → resolved + archived**, index synced.
- Mechanical single-pattern hardening (no committed spec/plan, per the trivial-change ceremony carve-out); the topic itself was the design.
- Independent whole-branch review (Sonnet): APPROVED — every guard placed before its `isfinite` check (none dead), no valid input rejected (`bool` still passes), scope clean, live-verified across all 5 modules.

## 2026-07-08 — iter-022: acceptance suite — injected-leak detection (Phase 2)

- **Added `overlapping_label_series` + `nn_leak_metric` to `cli/validation/synthetic.py`** + **`tests/test_acceptance_leak.py`** — the §9 "a look-ahead bug must be caught" acceptance check. Labels `y_t = Σ_{k<H} e_{t+k}` (H-sum of i.i.d. noise → `Cov(y_i,y_j)=H-|i-j|`), feature `x_t=t` (no real signal), and a 1-NN-by-index predictor whose OOS "skill" is **pure leakage**. **13 new tests** (full suite 395; leak test ~0.3s). Stdlib-only.
- **The leak is caught — measured + analytically grounded.** Without purge, the predictor picks an overlapping-label boundary neighbor → fake OOS skill **mean ŷ·y ≈ 26.7**; with CPCV `label_horizon=30` **and** `embargo=30` (purge before + after) the H boundary indices are removed on both sides → **≈ −0.3**. The harness removes the injected leak.
- **Distrust-the-instrument — removal proven attributable to embargo, not coincidence.** The whole-branch review empirically confirmed `Cov(y_i,y_{i+1})≈H-1` and `corr(x,y)≈0.03` (no learnable signal), then swept **6 purge settings**: the both-sided-purge leak tracks the closed-form `H-(d+1)` prediction at every window, while **neither one-sided purge collapses to zero** (label-horizon-only ≈15, embargo-only ≈23) — ruling out an artifact. It confirmed a fully- **or half-**broken purge/embargo would fail the `abs(purged) < 2.0` assertion.
- **§9 acceptance triad complete on synthetic data:** recovery + null (iter-020) + injected-leak (this) + registry-corruption (iter-019). A Phase-2 exit-bar milestone — the remaining exit-bar piece (cost model validated against ≥2 weeks of captured spreads) is T0003-gated (the capture pipeline).
- **Design/plan:** `docs/specs/00014-acceptance-leak-design.md`, `docs/plans/00014-acceptance-leak.md`. Ninth Phase-2 component.
- Independent whole-branch review (Sonnet): APPROVED, no Critical/Important findings — leak analytically real, removal demonstrably the purge/embargo mechanism (matches `H-(d+1)` across 6 settings), metric correct, deterministic.

## 2026-07-08 — iter-023: SPA / White reality check (Phase 2)

- **Added `cli/validation/spa.py` — `reality_check_pvalue`** (White 2000): the §9.4 data-snooping-corrected test of whether the best of a family of strategies beats the benchmark. Statistic = `max_k d̄_k` (best strategy's mean per-period outperformance); the stationary bootstrap (iter-016) resamples the **time rows**, recentered `V* = max_k(d̄_k* − d̄_k)` imposes the least-favorable null, `p = (1+count)/(B+1) ∈ (0,1]` (never NaN, no `/std`). + `outperformance_matrix` scaffold in `synthetic.py`. **15 new tests** (full suite 410; SPA acceptance ~0.4s). Stdlib-only; reuses the bootstrap, not a reimplementation.
- **Calibrated + detects — measured.** Acceptance: a planted-superior family (edge 0.3, 5 strategies) → **p = 0.005** (the bootstrap floor — zero resamples beat the observed statistic), `best_strategy` correctly = the edge column; an all-null family → **0/20** false positives (p-values spread 0.08–0.93). The below-nominal null FP is White's RC's **known conservativeness** (controls FWER ≤ nominal via least-favorable-null recentering), not under-calibration.
- **Distrust-the-instrument — calibration verified, not assumed.** The whole-branch review re-derived the statistic against White (2000), then empirically probed calibration (weak edge 0.05 → p=0.10 — *not* auto-significant; a monotone edge→p sweep; null p spread 0.016–0.924) and **mutation-tested the recentering**: omitting the `−d̄_k` recentering flips a strong-edge p from 0.001 → 0.485, turning the planted-detection test red — confirming the acceptance suite is a real tripwire for a broken instrument (the paired planted+null tests, per the iter-020 pattern).
- **§9.4 complete:** DSR (iter-014) + PBO (iter-015) + SPA (this) — the multiple-testing / benchmark-superiority core. The benchmark family SPA runs against is fixed in Phase 3. Spec/plan `00015`. Tenth Phase-2 component.
- Independent whole-branch review (Sonnet): APPROVED, no Critical/Important findings — RC construction + recentering + calibration all verified analytically and empirically; deterministic; never NaN.

## 2026-07-08 — iter-024: explicit-cost backtester engine (Phase 3)

- **Opened `cli/backtest/`** (Phase 3 kickoff) — `run_backtest(asset_returns, positions, *, fee_rate, periods_per_year)`: turns a strategy's target positions into a **net-return series** after explicit per-turnover fees, then scores it. Reuses `cli.validation.{sharpe, max_drawdown, annualized_return}` (not reimplemented) → `{net_returns, total_return, sharpe, max_drawdown, annualized_return, n_periods}`. **15 tests** (full suite 425). Stdlib-only; the engine the benchmark family B0–B4 is built on.
- **Timing convention (fixed, look-ahead-critical):** `positions[t]` is held during period `t` and earns `asset_returns[t]` (no index shift); the caller must set `positions[t]` from information ≤ `t−1` — documented as the strategy's obligation (the engine can't enforce it; the CPCV/validation harness is what catches a leaky strategy). `turnover[t] = |positions[t] − positions[t−1]|` with `positions[−1]=0`, so the initial entry from flat is charged once; a full sign flip costs turnover 2 (trade through flat).
- **Never NaN.** A **flat** strategy (zero-variance net → Sharpe undefined) and a **blow-up** (`1 + net[t] ≤ 0`) both surface as `BacktestError` (wrapping the metrics' `ValidationError`), and `total_return` is computed only after the metrics validate — no NaN, no leaked `ValidationError`.
- **Distrust-the-instrument.** The whole-branch review hand-worked **and** live-ran the buy-and-hold (bit-for-bit `net=[0.09,−0.05,0.20]`, entry fee once) + sign-flip (turnover 2) cases, confirmed the metrics are genuinely reused (compared to the harness directly), and that the timing test genuinely pins index alignment (distinct returns ⇒ an off-by-one shift would fail). No look-ahead, no cost double-count.
- **Deferred (v1 scope):** margin carry (`cli.costs.margin_carry`) + spread (T0003-gated) + borrow as parameters; multi-asset/portfolio (the basket benchmark); cost-stress multipliers (§9.6, applied at eval time); the benchmark strategies B0–B4 (next iterations).
- **Design/plan:** `docs/specs/00016-backtester-engine-design.md`, `docs/plans/00016-backtester-engine.md`. First Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED, no Critical/Important findings — timing convention verified free of look-ahead, cost accounting correct, metrics reused not reimplemented, never NaN.

## 2026-07-08 — iter-025: benchmark strategies B0 + B1 (Phase 3)

- **Opened `cli/benchmark/`** — the first two benchmark strategies (position generators fed to the backtester): **B0 `buy_and_hold`** (constant full long) and **B1 `vol_target`** — `position[t] = min(target_vol / stdev(returns[t-lookback:t]), max_leverage)`, `0` for the warm-up (`t < lookback`) or a zero-vol window. **16 tests** (full suite 441). Stdlib-only (`statistics.stdev`, sample — consistent with the metrics module); `BenchmarkError` guards.
- **Look-ahead-free (the crux).** The realized-vol window `returns[t-lookback:t]` **excludes `t`**, so `position[t]` never uses `return[t]`; the backtester then applies `position[t]` to `return[t]`, so the composed B1 strategy is look-ahead-free end to end. A test asserts `position[t]` is invariant to a perturbation of `return[t]` while `position[t+1]` (whose window includes `t`) does change.
- **Distrust-the-instrument.** The whole-branch review live-perturbed `return[t]` (confirmed `position[t]` unchanged, `position[t+1]` changed) and **constructed the off-by-one inclusive-window bug** (`[t-lookback:t+1]`) to prove the test would catch it; verified the vol estimator matches `cli.validation`, the cap/zero-vol/warm-up paths, guards, and a real `run_backtest` integration.
- **Deferred:** B2 (inverse-vol majors basket), B3 (B2 + 200-day long/flat gate), B4 (B3 + short), and the real-data bar-to-beat dossier (all of B0–B4 through the backtester on the Phase-1 dataset, with DSR/PBO/SPA comparison — §9's deployment rule).
- **Design/plan:** `docs/specs/00017-benchmark-b0-b1-design.md`, `docs/plans/00017-benchmark-b0-b1.md`. Second Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED, no Critical/Important findings — no look-ahead (verified live + via off-by-one mutation), vol math correct + codebase-consistent, guards clean.

## 2026-07-08 — iter-026: B0/B1 bar-to-beat report on real BTC (Phase 3)

- **Recorded the first bar-to-beat.** Added `returns_from_prices` (close-to-close, reusable by B2–B4) and ran **B0 buy-and-hold + B1 vol-targeted BTC** on real `data/ohlc-full/BTC/EUR/1440.parquet` daily (2013-09-10 → 2026-03-31, 4580 returns) through the backtester, at zero fees. `docs/benchmark-b0-b1-report.md`: **B0 — 607× / 66.7%/yr / Sharpe 1.075 / maxDD 82.5%** (the high-return, brutal-drawdown floor); **B1 (10%/yr per §9, 30d, ≤1.0×) — 3.76× / 13.2%/yr / Sharpe 1.111 / maxDD 22.0%** (the low-risk floor: ~0.17× median exposure, the 1.0× cap never binds). 448 tests.
- **Validates the whole stack on real data.** The full chain (dataset → `returns_from_prices` → `buy_and_hold`/`vol_target` → `run_backtest` → metrics) runs end-to-end and gives sane numbers — BTC's ~82% max drawdown is a known historical fact (2018/2022 crashes), confirming the backtester + maxDD against reality; the vol-targeted ~0.17× exposure matches the master plan's §10 "0.2–0.5×" expectation.
- **Distrust-the-instrument — the review caught a spec deviation.** The first draft parameterized B1 at **60%/yr** (my arbitrary smoke-test value), but master-plan §9 specifies **B1 vol-targeted BTC at 10–12%/yr**; the review flagged it (a wrong "B1 bar" would mislead Phase-4). Corrected to 10%/yr (§9). Bonus finding: because vol-targeting scales the position linearly, **Sharpe is target-invariant** (1.111 across 10/11/12%); the 10–12% band is a single risk/return ray (maxDD 22→26%, annret 13→16%), not a Sharpe choice. Every reported number re-verified reproducible on real data.
- **Deferred:** B2 (inverse-vol basket), B3 (200-day gate), B4 (short); the full B0–B4 panel + the DSR/PBO/SPA significance comparison (the §9 deployment rule); the fee model on the rebalancing benchmarks.
- **Plan:** `docs/plans/00018-benchmark-bar-to-beat.md` (light report iteration; no separate spec). Third Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED after the B1 target-vol correction — helper correct, every report number reproduced bit-for-bit from real data (no fabrication), B1 now matches the master-plan canonical definition.

## 2026-07-08 — iter-027: regime gate (200-day SMA long/flat) (Phase 3)

- **Added `cli/benchmark/sma_gate`** — the §5 **prior survivor** (the best-performing prior-phase rule): a long/flat regime signal `signal[k] = 1.0 if prices[k] > SMA(prices[k-window+1:k+1]) else 0.0`, aligned with the return series (length `len(prices)-1`), warm-up 0. Composes multiplicatively: gated buy-and-hold = `sma_gate` itself; gated vol-target = `sma_gate × vol_target`. **11 new tests** (full suite 459). Stdlib-only; `BenchmarkError` guards.
- **Look-ahead-free (the crux).** `signal[k]` uses `prices[k-window+1 .. k]` — through `prices[k]`, the price at the **start** of return-period `k` (which is the move `prices[k]→prices[k+1]`) — and never `prices[k+1]`. `prices[k]` legitimately appears in both the gate decision and the period's return without look-ahead (decide at the start, earn the period), matching the backtester's own timing contract.
- **Distrust-the-instrument.** The whole-branch review live-perturbed `prices[k+1]` (confirmed `signal[k]` unchanged, `signal[k+1]` changed) and **constructed both off-by-one variants** — a forward shift (real look-ahead) turns the no-look-ahead test red, a backward shift crashes the value test — so both alignment directions are pinned by the suite. SMA math, warm-up boundary, composition, and guards all verified.
- **Deferred:** B2 (inverse-vol basket), B3-proper (gate × basket, needs B2), B4 (short); an EMA variant; the real-data gated-BTC run (folds into the full B0–B4 panel dossier).
- **Design/plan:** `docs/specs/00019-regime-gate-design.md`, `docs/plans/00019-regime-gate.md`. Fourth Phase-3 component (the B3 gate mechanism).
- Independent whole-branch review (Sonnet): APPROVED, no Critical/Important findings — alignment look-ahead-free (verified live + via both off-by-one mutations), SMA math + composition + guards correct.

## 2026-07-08 — iter-028: gated-BTC benchmark panel (Phase 3)

- **Extended `docs/benchmark-b0-b1-report.md` to the four-strategy BTC panel** — added the 200-day-gated variants (gated-B0 = the §5 prior survivor; gated-B1 = gate × vol-target) run on real BTC/EUR daily (2013–2026) through the backtester, zero-fee. No new code (composes the existing `sma_gate`/`vol_target`/`run_backtest`).
- **The §1/§5-thesis result, on real data.** The 200-day gate cuts B0's max drawdown **82.5% → 62.8%** while *raising* Sharpe **1.075 → 1.102** (long only ~56% of the time), and **gated-B1 (gate × vol-target) is the panel's best risk-adjusted result — Sharpe 1.247, maxDD 12.3%** (total 2.75×, 11.1%/yr). This *supports* the master plan's thesis that a disciplined vol-targeted trend/regime rule — not raw buy-and-hold, not vol-targeting alone — is the realistic best case / deployable target family.
- **Distrust-the-instrument.** The implementer caught a rounding slip in *my plan* (gated-B1 total 2.77× vs the real **2.75×**), back-solved it from the annualized return, and used the real value (no fabrication). The whole-branch review reproduced all four rows bit-for-bit + cross-checked the annualized↔total-return consistency, and flagged that "validates" over-claimed for a single zero-fee non-significance-tested run — softened to "supports / directionally consistent with" (the DSR/PBO/SPA significance comparison across the family is a later iteration).
- **Deferred:** the full B0–B4 panel (adding the inverse-vol basket B2 + the short B4) and the DSR/PBO/SPA significance comparison (the §9 deployment rule) — plus the fee model on the rebalancing strategies.
- **Plan:** `docs/plans/00020-benchmark-gated-panel.md` (light doc iteration; no spec). Fifth Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED — every panel number reproduced bit-for-bit from real data, interpretation honest (over-claim softened), scope clean.

## 2026-07-08 — iter-029: benchmark cost-stress panel (§9.6, Phase 3)

- **Added the `## Cost stress (§9.6)` section to `docs/benchmark-b0-b1-report.md`** — re-ran the four BTC strategies (B0, gated-B0, B1, gated-B1) through the backtester's per-turnover `fee_rate` at the confirmed **Tier-1 Kraken fee ladder** (0 / 0.40% maker base / 0.60% = 1.5× / 0.80% = 2× = taker; base from `cli.costs.spot_fee_rates`). No new code (composes the existing `sma_gate`/`vol_target`/`run_backtest` with the fee model).
- **§9.6 verdict — gated-B1 does not die at 1.5×.** Its Sharpe erodes gently across the ladder (1.247 → 1.117 → 1.052 → 0.986) because turnover is low (~0.008/day); at 0.60% (1.5×) it is still 1.052 > 1.0, clearing the deployment gate. B0 is fee-immune (turnover ~0.0002/day — it only buys once), unchanged at ~1.075.
- **The honest crossover the stress surfaces (distrust-the-instrument).** At the realistic base maker fee (0.40%), gated-B1 (1.117) is still the panel's best Sharpe, above B0 (1.075) — but because B0 pays no fees while gated-B1 pays to rebalance, **B0 overtakes gated-B1 on Sharpe at 1.5×+** (1.074 vs 1.052 at 0.60%). So the gated family's *Sharpe edge over buy-and-hold* is fee-sensitive; its deployment case rests on the robust **drawdown** advantage instead — gated-B1's maxDD stays **15–18%** across the ladder vs B0's flat **82.5%** (~4–5× smaller). Also named the subtle correct effect: **fees slightly raise the gated strategies' maxDD** (gated-B1 12.3% → 18.0%) — fee drag deepens drawdown troughs.
- **Scope caveat (honest).** The ladder stresses **exchange fees only**; the bid-ask spread is not yet modeled (gated on the spread-capture pipeline, T0003), and margin rollover/carry is N/A here since all four strategies are long/flat spot (≤ 1.0×, never short).
- **Deferred:** the full B0–B4 panel (adding the inverse-vol basket B2 + the short B4) and the DSR/PBO/SPA significance comparison; a spread-inclusive cost model once T0003 lands.
- **Plan:** `docs/plans/00021-benchmark-cost-stress.md` (light doc iteration; no spec). Sixth Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED — every cost-stress cell reproduced bit-for-bit from real data, the Sharpe-crossover claim verified against the table it presents (no over-claim), scope clean.

## 2026-07-08 — iter-030: inverse-vol majors basket generator (B2, Phase 3)

- **Added `inverse_vol_basket(prices_by_asset, *, lookback)` to `cli/benchmark/strategies.py`** — the B2 generator: turns a dict of pre-aligned equal-length price series into the inverse-vol-weighted basket's daily **net portfolio return series** (length `L-1`). Each weighted day, asset `i` gets weight ∝ `1 / stdev(returns_i[t-lookback:t])` normalized over the positive-vol assets, applied to `returns_i[t]`. Reuses `returns_from_prices` + stdlib `statistics.stdev`; exported from `cli/benchmark/__init__.py`. No real-data run, no I/O, no CLI/README change.
- **Look-ahead-free + never-NaN.** `weight_i[t]` uses the window strictly before `t` (`[t-lookback:t]`, excludes `t`); zero-vol assets (which would take infinite inverse-vol weight) are **excluded** from that day and the rest renormalized; warm-up (`t < lookback`) and all-excluded days are exactly `0.0`; degenerate input (empty dict, unequal lengths, `L < lookback+2`, non-finite/non-positive prices, `lookback < 2` or `bool`) raises `BenchmarkError`.
- **Composition decision** (`.tmp/decisions.md` [iter-030], Decision 2): the pure generator is the **fixed 10-asset intersection basket** — the caller supplies pre-aligned series (data loading + calendar alignment live in the later real-data run). The richer **dynamic-composition full-history variant** (basket grows 2→10 assets over BTC's 2013→2026 calendar) is parked as open topic **T0007**.
- **TDD:** 10 tests (hand-computed value 0.75/0.25 → 0.025; equal-vol → 0.5/0.5; length+warm-up; no-look-ahead via last-price-perturbation prefix-equality; window-is-real via first-price perturbation; zero-vol exclusion; all-flat day; single-asset; guards; backtester integration). 459 → **469 passed**.
- **Deferred:** the real-data B2 bar-to-beat vs BTC over the matched common window (its own iteration, mirroring iter-025 generator → iter-026 report); B2 rebalancing/basket-turnover cost model + §9.6 stress; B3-proper (gate × basket) + B4 (short); the T0007 dynamic variant.
- **Pre-existing nit noted (not fixed — surgical scope):** the older `vol_target`'s `lookback` guard lacks the `isinstance(bool)` exclusion this new function adds; `vol_target` is untouched here.
- **Spec/plan:** `docs/specs/00022-inverse-vol-basket-design.md`, `docs/plans/00022-inverse-vol-basket.md`. Seventh Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED — reproduced all value cases by hand + ran an independent adversarial look-ahead probe (no backward leak; exact window boundaries), verified zero-vol exclusion/never-NaN and all guards, all 10 tests meaningful, scope clean.

## 2026-07-08 — iter-031: B2 inverse-vol basket bar-to-beat (Phase 3)

- **New report `docs/benchmark-b2-basket-report.md`** — ran the reviewed `inverse_vol_basket` on the 10 EUR majors aligned to their common window (2021-12-21 → 2026-03-31, 1562 closes → 1561 returns; AVAX is the newest listing) and compared it to single-asset BTC B0/B1 over the *same* window, zero-fee. No new code (composes `inverse_vol_basket`/`vol_target`/`run_backtest`).
- **Honest, counter-intuitive finding: the raw inverse-vol basket UNDERPERFORMED single-asset BTC.** B2 lost money — total **0.68×** (−8.6%/yr), Sharpe **0.194**, maxDD **69.4%** — a *deeper* drawdown than BTC buy-and-hold (1.36×, Sharpe 0.397, maxDD 65.8%). Naive diversification across the majors was no free lunch.
- **Mechanism, corroborated per-asset:** the window opens near the Nov-2021 top; the 2022 bear devastated the alts and most never recovered — **8 of 10 majors ended below their window-start price** (AVAX −93%, DOT −95%, ADA −82%; only BTC 1.36× and XRP 1.38× gained). An inverse-vol basket still *holds* the losers at risk-normalized weights, so the two winners can't carry it.
- **Vol-targeting is the edge, not asset selection.** Both vol-targeted variants land at Sharpe ~0.45 / maxDD ~15–18% (B1 BTC-vol-target 1.21×/0.456/17.5%; B2+vol-target 1.21×/0.453/14.6%) — far better than either raw strategy — reinforcing the §1/§5 thesis that disciplined risk control delivers the risk-adjusted profile.
- **Window caveat + B2's place:** a single BTC-unfavorable window (BTC Sharpe 0.40 here vs **1.075** over the full 2013–2026 history); the full-history dynamic-composition basket (**T0007**) would test whether the underperformance is structural or window-specific. B2 does **not** raise the bar — the deployable target stays the vol-targeted / gated family; any Phase-4 alpha still clears B0/B1/gated-B1, not the raw basket.
- **Plan:** `docs/plans/00023-benchmark-b2-basket-report.md` (light doc iteration; no spec). Eighth Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED — reproduced every table cell from real data (all within tolerance), confirmed the basket's deeper drawdown / loss vs BTC's gain holds against its own table, verified the 8/10-majors-down arithmetic + the full-history-Sharpe reference + the T0007 pointer, scope clean.

## 2026-07-08 — iter-032: basket gate + short (B3/B4), Phase 3

- **Extended `docs/benchmark-b2-basket-report.md` with B3 (200d gate) + B4 (short)** over the common window, completing the B0–B4 family on the multi-asset side. The gate is applied to the basket's **own equity index** (self-referential regime, 200d SMA); B4 replaces "flat" with "short" below the SMA. No new code (composes `inverse_vol_basket`/`sma_gate`/`vol_target`).
- **B3 (gate) rescues the raw basket from a loss but doesn't lift it.** Long only ~41.8% of the time, it turns raw B2's −8.6%/yr into +1.4%/yr (0.68× → **1.06×**) and roughly halves the drawdown (69.4% → **48.4%**) by sitting out much of the alt bear — but Sharpe (**0.237**) stays far below the vol-targeted basket's 0.453.
- **The gate HURTS the vol-targeted basket.** gated-B2 + vol-target (Sharpe **0.270**) is *worse* than plain B2 + vol-target (0.453): once vol-targeting controls risk, the gate only subtracts participation. On the basket the gate and vol-target are **substitutes, not complements** — the *opposite* of single-asset BTC, where `gate × vol-target` (gated-B1) was the family's **best** (Sharpe 1.247).
- **B4 (short) is disastrous.** Adding the short takes Sharpe *negative* (**−0.136**) and the drawdown to **83.0%** — the worst line in the panel. The 200-day gate is a *lagging* signal, so B4 shorts *after* the crash and gets whipsawed by the violent 2022–2023 bear-market counter-rallies. (Idealized zero-cost/always-shortable short — real short-borrow + per-alt shortability would make it even worse.)
- **Family conclusion:** no overlay lifts the basket to the single-asset BTC bar — vol-targeting is the only overlay that helps, gating rescues-but-doesn't-lift (and is redundant with vol-targeting), shorting backfires. The deployable target remains **gated-B1** (vol-targeted BTC with the regime gate).
- **Plan:** `docs/plans/00024-benchmark-basket-gate-short.md` (light doc iteration; no spec). Ninth Phase-3 component; the **B0–B4 benchmark family (single-asset BTC + multi-asset basket) is now complete**, pending only the DSR/PBO/SPA significance layer + a basket-turnover cost model.
- Independent whole-branch review (Sonnet): APPROVED — reproduced every table cell from real data (all within tolerance), verified the load-bearing cross-references (gated-B2+vt 0.270 < plain 0.453; gated-B1 1.247; B4 Sharpe genuinely negative), placement + caveats update + honest framing all correct, scope clean.

## 2026-07-08 — iter-033: benchmark bootstrap CIs (Phase-3 exit bar)

- **Added `## Statistical significance (bootstrap CIs)` sections to both benchmark reports** (`docs/benchmark-b0-b1-report.md`, `docs/benchmark-b2-basket-report.md`) — 95% percentile CIs on each strategy's annualized Sharpe under the stationary block bootstrap (`cli.validation.bootstrap_ci`, Politis-Romano), block ℓ≈n^(1/3) (16 for the 4580-day BTC history, 12 for the 1561-day common window), 2000 resamples, seed=42. No new code (applies the Phase-2 harness).
- **Full-history BTC family: all four Sharpes are significantly positive** (95% lower bounds ~0.43–0.61 > 0) **but their CIs overlap heavily — the differences are not statistically distinguishable** from a single ~12-year path. gated-B1 has both the highest point (1.247) and the highest, robustly-positive lower bound (~0.61, stable across ℓ∈{8,16,24} and seed∈{1,42,99}).
- **Common-window family: every CI straddles zero** — over the ~4.3-year window not one strategy has a significantly-positive Sharpe, and B4's −0.136 is not significantly negative ([−1.05, 0.80]). The basket underperformance and B4 disaster are directional point estimates, not significant results — a statistical-power problem that reinforces the case for the full-history basket (**T0007**).
- **Honest meta-finding:** the benchmark comparisons are dominated by estimation uncertainty. The deployment bar is chosen on point estimate + highest/robustly-positive lower bound + drawdown control (12.3% vs 82.5%), **not** on a significant Sharpe edge over B0/B1 (which the CIs show does not exist).
- **Deployment bar FROZEN** (`.tmp/decisions.md` [iter-033], per the §12 exit bar) = **gated-B1** — vol-targeted BTC (10%/yr, 30d) × the 200-day long/flat regime gate; full-history Sharpe 1.247 (95% CI ~[0.61, 1.88]), maxDD 12.3%, ~1.12 after Tier-1 fees (survives §9.6 to 2×). B4's acceptability as the fallback deployable stays the open human decision point for the phase close-out.
- **Plan:** `docs/plans/00025-benchmark-bootstrap-cis.md` (light doc iteration; no spec). Delivers the §12 Phase-3 exit-bar "B3/B4 numbers with bootstrap CIs" + "deployment bar frozen in the decision log" requirements. Tenth Phase-3 component.
- Independent whole-branch review (Sonnet): APPROVED — reproduced every CI cell from real data (points ≤0.001, bounds ≤0.005), verified both load-bearing claims (full-history lower bounds >0 with overlapping CIs; all six common-window CIs straddle zero) and that neither report overclaims a significant Sharpe edge, scope clean.

## 2026-07-08 — iter-034: benchmark regime slices (Phase-3 exit bar)

- **Added `## Regime slices (calendar-year)` to `docs/benchmark-b0-b1-report.md`** — annual net return of B0/B1/gated-B1 for each year of BTC history, bucketing each return by the year it is realized (`ts[k+1].year`), compounded, zero-fee. No new code (existing components + a year partition).
- **gated-B1 turns catastrophic bear years into near-flat ones.** In BTC's three big bear years — 2014 (**−50.5%**), 2018 (**−73.0%**), 2022 (**−62.1%**) — gated-B1 lost only **−5.5%, −5.2%, −0.5%** by going flat below the 200-day line. Its **worst year in the whole 2013–2026 sample is −5.5%**; B0 has four double-digit-loss years.
- **The gate's marginal value over vol-targeting is concentrated in the bear years** (2018: B1 −17.9% → gated-B1 −5.2%; 2022: −17.1% → −0.5%); in calm uptrends (2016, 2017) the gate is long all year and gated-B1 ≡ B1.
- **The cost is the bull-year cap:** gated-B1 gives up the explosive years — 2017 **+46.9%** vs B0's **+1211%**, 2020 +27.5% vs +269.6% — so over the full history it dramatically underperforms buy-and-hold in raw cumulative return (2.75× vs 607×). It trades return for the elimination of catastrophic years.
- **Decomposes the whole dossier:** the bootstrap CIs showed gated-B1's Sharpe edge over B0/B1 is not statistically significant; the year-by-year view shows what *is* robust and economically real — never losing more than ~6% in a year — which is the source of the 12.3% max drawdown and why gated-B1 is the frozen deployment bar. (Caveat in the report: single historical path; −5.5% is descriptive, not a guaranteed floor.)
- **Plan:** `docs/plans/00026-benchmark-regime-slices.md` (light doc iteration; no spec). Completes the §12 Phase-3 exit-bar "regime slices" dimension. Eleventh Phase-3 component — the benchmark family is now validated across cost stress + bootstrap CIs + regime slices, and the deployment bar is frozen.
- Independent whole-branch review (Sonnet): APPROVED — reproduced the annual table exactly (0.0pp off), traced the year-bucketing (no off-by-one) and the sma_gate/vol_target look-ahead alignment, confirmed all load-bearing claims + the honest bull-year-cost framing, scope clean.

## 2026-07-08 — iter-035: Phase 3 close-out (benchmarks & the frozen bar)

- **Wrote the Phase-3 close-out report** `docs/research/04.phase3-benchmarks-closeout.md` — the §12 exit bar is fully met (reproducible reports; B3/B4 bootstrap CIs; deployment bar frozen), the B0–B4 family is validated across cost stress + bootstrap CIs + regime slices, and **the deployment bar is frozen: gated-B1** (vol-targeted BTC × 200d gate; full-history Sharpe 1.247, maxDD 12.3%, ~1.12 after fees, worst year −5.5%). Synthesizes the dossier's honest findings (bear-year elimination is the edge; comparisons dominated by estimation uncertainty; the multi-asset basket does not beat single-asset BTC; shorting backfires) and flags the one carried-forward §12 handback — the human confirmation of B4 as the fallback deployable (gates Phase 5's fallback, not the Phase-4 kickoff).
- **Persisted the Phase-3 decision log** — drained `.tmp/decisions.md` (iter-024–034) **verbatim** into the committed sibling `docs/research/04.phase3-decisions.md` and truncated the running log to empty, per `.claude/rules/decisions-log.md` (*Phase persistence*). The close-out report is added to the mdformat allowlist; the `-decisions.md` sibling is kept OFF it (verbatim).
- **Open-topics swept** at the phase boundary: T0003/T0005 human-gated (live-trading prep), T0004/T0007 deferred to a future phase (Phase-4) — all correctly parked, none Phase-3-relevant.

## 2026-07-08 — iter-036: longer-history basket robustness check (Phase-4 prep)

- **Added a `## Longer-history robustness check (4 majors, ~9 years)` section to `docs/benchmark-b2-basket-report.md`** — re-ran the (reviewed) `inverse_vol_basket` on the 4 majors with long history (BTC/ETH/LTC/XRP) over their common window **2017-05-18 → 2026-03-31 (~8.9 yr, multiple full cycles)** vs BTC B0/gated-B1 over the same window. No new code. Directly answers finding 1 of the Phase-4 orientation memo (structural vs window artifact).
- **The 10-asset basket's catastrophic loss was largely a window artifact:** over ~9 years the raw 4-major basket returns **16.31×** (Sharpe 0.798), not the 10-asset basket's 0.68× *loss* over the alt-hostile 4.3-yr window.
- **A residual structural drag remains:** even over ~9 years the raw basket does not beat single-asset BTC risk-adjusted (Sharpe 0.798 < 0.929; maxDD 86.4% > 82.5%) — BTC was the best single major (34.6×) and inverse-vol weighting dilutes it with laggards (LTC 1.8×, XRP 3.6×).
- **The vol-targeted basket is competitive with the bar over this window** (Sharpe **0.841** vs same-window gated-B1's 0.759, though worse drawdown and single-window) — so A1's basket premise is *viable*, more so than the 4.3-yr window implied. Caveat: fixed 4-asset proxy, window still starts 2017 not 2013; the full dynamic-composition basket (**T0007**) remains the honest test.
- Independent whole-branch review (Sonnet): APPROVED — reproduced all four numbers exactly, verified the window-artifact / residual-structural-drag / vol-targeted-competitiveness claims against the data (all correctly hedged), no confusion of the frozen bar's full-history 1.247 with its window-specific 0.759.

## 2026-07-08 — iter-037: BTC short-state check (Phase-4 prep / B4 handback)

- **Added a `## Short-state check (does adding a short to gated-B1 help?)` section to `docs/benchmark-b0-b1-report.md`** — tests two short overlays on full-history BTC (both vol-targeted, zero-fee): a **naive** long/short (gate flips to full short below the 200d SMA) and a **disciplined** long/short (§5 design: short at −0.5× only when price is >5% below the SMA *and* 90-day momentum is negative). Both look-ahead-free (signals decided from prices at the start of each return period). No new code (composes `sma_gate`/`vol_target`/`run_backtest` + a scratchpad short overlay).
- **Adding a short is a net drag over 2013–2026, even done well:** disciplined Sharpe **1.105** < the long/flat bar's **1.247** (lower return 2.43× vs 2.75×, deeper drawdown 15.4% vs 12.3%). Robust — across band ∈ {3,5,8%} × momentum ∈ {60,90 d} the disciplined Sharpe stays **1.076–1.119**, always below the bar.
- **Discipline matters enormously:** the disciplined short (1.105) massively beats the naive gate-flip short (**0.839**) — the confirmed-bear band + negative-momentum filter + half size cut exactly the whipsaw that made the basket's B4 disastrous (−0.136). If a short is used, it must be built this way — but even so it doesn't clear the long/flat bar.
- **Why (honest):** 2013–2026 is a secular BTC uptrend (~600×), so a short state mostly fights the primary trend and is penalized; its value is regime-dependent insurance for a prolonged bear the sample lacks — exactly why §5 keeps the short reduced-size / confirmed-bear-only. **Implications:** for the §12 B4 handback, a BTC short is a net drag so gated-B1 (long/flat) dominates B4 as a fallback; for A1 (`05.phase4-orientation.md` finding 2), the disciplined design is the right one to carry, but the short must earn its place in a regime the backtest can price, with the kill bar isolating the short leg.
- **Distrust-the-instrument:** the whole-branch review caught a real self-contradiction in the first draft — the new table used the final-wealth multiple (3.75×) while the rest of the report reports `total_return` (gated-B1 = 2.75×); fixed to the report's convention (2.75/2.12/2.43×), and tightened the robustness range to the exact 1.076–1.119.
- **Plan:** N/A (light robustness-check addendum using existing components). Independent whole-branch review (Sonnet): APPROVED after the two fixes — reproduced every number, confirmed the short logic is look-ahead-free, verified the honest regime-dependent framing.

## 2026-07-08 — iter-038: T0003 D2 capture pipeline — built + deployed LIVE (Phase 1)

- **The Phase-1 exit-bar capture pipeline is live.** Designed interactively (spec/plan `00027`) then built unattended and deployed to the provisioned Linode `zcrypto.zhaow.me` (Debian 13, 2 vCPU / 4 GB / 80 GB). New tree `infra/ansible/` (portable IaC) + `cli/capture/` (the daemon) + `infra/docker/` + GHCR CI.
- **Host hardened** (`infra/ansible/`, executed against the real host, no self-lockout): `deploy` sudo user, SSH → **10022** (root + password off), dev-sec.io `os_hardening`+`ssh_hardening` (CIS-L1), **nftables** default-drop inbound (10022 + ICMP; manages only its own table so Docker NAT survives — a bug caught + fixed when the first ruleset's `flush ruleset` + `forward drop` broke container egress), fail2ban, **chrony NTS**, unattended-upgrades (04:00 reboot), Docker Engine + compose.
- **Capture daemon LIVE** (`cli/capture/`): thin Kraken WS v2 client (book **depth-100** + trades, 10 EUR majors), per-pair L2 book with **CRC32 checksum validation** (verified vs Kraken's documented example `3310070434` + **3874 live checksums, zero mismatches**; parses with `Decimal` since the checksum depends on wire-format trailing zeros), hourly **zstd-Parquet + sha256 manifests**, gap accounting, disk watermark. Containerized (on-host build tonight; GHCR public-visibility deferred) and run via compose (`restart: unless-stopped` + docker-enabled → survives the 04:00 reboot): **19 % CPU / 74 MiB / 0 errors, all 10 pairs flowing**. The **≥7-day exit-bar clock started 2026-07-08 ~13:39 UTC**.
- **Liveness monitoring**: a healthchecks.io dead-man's-switch check provisioned + wired (`HEALTHCHECK_URL`, status **up**) → emails on a missed ping; status badge added to the README.
- **Secrets** (two-layer, self-contained public repo): sops+GPG-encrypted ansible-vault password; vault-encrypted deploy + pull-only SSH keys, hostname, and healthchecks key. The GPG key is passphrase-protected (no unattended decrypt), so tonight's runs used a transient vault password from a `0600` temp file (shredded at closeout); nothing plaintext is committed and the committed sops→GPG mechanism is what future runs use.
- **Built by three parallel subagents** (daemon / hardening roles / docker layer) + controller-orchestrated real-host execution with verify-before-tighten rails; the daemon self-validated on the real Kraken feed.
- **T0003 → `partial`** (index synced). Remainder (follow-ups): the workstation pull/NAS-sync + alerting drill, the full monitoring role, the ≥7-day clean run, GHCR public-visibility, the `ansible-lint` pre-commit hook, and REST trade-backfill. Spec/plan `docs/{specs,plans}/00027-t0003-capture-pipeline*`.

## 2026-07-08 — iter-039: T0004 tick-derived bar reconciliation + true VWAP (Phase 1)

- **Built `cli/tick/`** (spec/plan `00028`) — the tick→bar machinery, TDD, 25 synthetic-fixture tests: `read.py` (`read_trades_csv` — Kraken trades CSV from a path or a ZIP member, header/headerless auto-detect by sniffing whether line-1's first field is numeric; extra trailing columns ignored positionally; `TickError` on malformed/NaN, never a silent misparse), `aggregate.py` (`ticks_to_bars` — `group_by_dynamic` left-closed epoch-aligned OHLCV + `count` + **true tick-weighted `vwap` = Σ(price·volume)/Σ(volume)**), `reconcile.py` (`reconcile` — per-interval O/H/L/C match vs canonical OHLCVT, strict + loose bands, worst-mismatch surfacing; `KRAKEN_TICKER_MAP` + `csv_pair_to_canonical` `XBTEUR→(BTC,EUR)`). No CLI subcommand (a scratchpad run drives the sample, per spec).
- **Sample reconciliation cleared the exit bar at 100%** (`docs/tick-reconciliation-report.md`): all 10 EUR majors, Q1-2026, **~7.98 M ticks** → **100.000 % O/H/L/C match within a 1e-6 relative tolerance on every one of the 30 pair×interval cells (1h/4h/1d), at 100.0000 % coverage** (27,889 canonical bars, 0 not-covered — the AVAX 1h shortfall is real no-trade hours the canonical also omits, not a blind spot). Master-plan tolerance bar is ≥99.5 %.
- **Distrust-the-instrument:** 100 % OHLC match is the *expected* result (both paths derive from the same Kraken trades) and is provably not aliasing — the **VWAP genuinely differs** (true tick VWAP vs the stored close-weighted proxy: ~1 bp median for liquid pairs, tails to ~200 bps on the illiquid ones like AVAX), and the planted-mismatch unit test confirms `reconcile` returns <100 % on real mismatches. The VWAP gap is the "treatment engaged" evidence and a real finding — the tick VWAP is a material refinement over the proxy, most on the thin pairs.
- **Data-schema discovery** (corrects the spec's single-schema assumption): the quarterly ZIPs are 7-field-with-header (`Price,Volume,Timestamp,Type,OrderType,Misc,TradeID`); the **complete** dataset is headerless **3-field `Timestamp,Price,Volume`** (different order, no side). The reader handles the quarterly and raises cleanly on the 3-field layout — reading the complete dataset needs a second recognized schema (the full-history batch remainder).
- **Fix found via the sample run** (not the unit tests): `read_trades_csv` assumed the ZIP path was a `Path` (`.name`); passing a `str` `AttributeError`'d. Widened to accept `str | Path` (coerced) + added a regression test.
- **T0004 → `partial`** (index synced). Remainder: the 3-column complete-dataset reader → the full-universe/full-history reconciliation batch (→ then `resolved`); 15m bars + tick storage/catalog when Phase-4 microstructure needs them. Report is a top-level `docs/` doc (self-formatted, not on the mdformat allowlist). Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-040: A1 causal feature primitives (Phase 4 kickoff)

- **Built `cli/features/`** (spec/plan `00029`) — the leak-free feature substrate the Phase-4 alpha sprint (A1/A2 and the Bucket-B families) consumes, TDD, **59 new tests** (621 total). Three pure-stdlib primitives, all sharing the reviewed **`sma_gate` causal alignment** (return length `len(prices)-1`; `feature[k]` uses only `prices[≤k]`, the price at the *start* of return-period k; warm-up → `0.0`): `momentum(prices, *, lookback)` (multi-horizon past return), `channel_position(prices, *, window)` (Donchian position in `[-1,+1]`, +1 at the channel high), `realized_vol(prices, *, lookback)` (trailing-return stdev vol-state). Shared `_validate.py` (rejects `bool` for both prices and window args); `FeatureError`.
- **Distrust-the-instrument — the leak test is the centerpiece, and it was proven to bite.** Each feature has a look-ahead test asserting `feature(prices)[:k+1]` is bit-identical when `prices[k+1:]` are mutated. The build **verified each leak test genuinely fails on a deliberately-broken (look-ahead) implementation** — including swapping `realized_vol`'s return-window bound to the exact off-by-one (`returns[k-lookback+1:k+1]`, which would peek at `prices[k+1]`) the plan flagged; a dedicated `test_realized_vol_window_bound_is_exclusive_of_return_k` guards it. This locks down the leak class that mints false alpha verdicts, before any family is built on top.
- **Scope was deliberately the substrate, not the family.** A1's full long/flat/short book + kill-bar verdict is large and entangled with the parked full-history basket (T0007); building the leak-prone features first (a clean, completable, reversible deliverable) is the honest first step. No strategy is measured here — no A/B verdict. The scoping decision is logged in `.tmp/decisions.md` `[iter-040]` (Decision 2). Next iteration specs the A1 book on these primitives.
- Built by a subagent (Sonnet 5) with **per-task independent review** (separate reviewer subagent per commit, all PASS with the leak verification) + controller holistic cross-task check.

## 2026-07-08 — iter-041: A1 feature substrate round 2 (Phase 4)

- **Completed A1's per-asset feature layer** (spec `00030`, extends iter-040's `cli/features/`), TDD, **36 new tests** (657 total): `trend_agreement(prices, *, lookbacks)` — the ensemble-regime input, `mean(sign(momentum(prices, lookback=L)[k]) for L in lookbacks)` ∈ `[-1,+1]` (+1 every horizon up, −1 every down, 0 split/warm-up), reusing `momentum` so causality is inherited; and `drawdown_state(prices)` — the risk state, `dd[k] = prices[k]/max(prices[0..k]) - 1` ∈ `[-1,0]` via a running expanding peak. Both keep the `sma_gate` alignment (length `len(prices)-1`, `feature[k]` from `prices[≤k]`, warm-up → 0.0).
- **Leak tests again verified to bite** — each new feature's look-ahead test was confirmed to fail on a deliberately-broken implementation (drawdown peeking at `prices[k+1]` for the peak; trend_agreement reading `m[k+1]`), then restored. Per-task independent review on both commits (Sonnet 5), PASS. Controller verified the alignment on the two new files directly.
- **Substrate now complete for the A1 spec.** The per-asset feature inputs the orientation memo names (momentum, channel position, realized-vol, trend agreement, drawdown; BTC-regime = existing `sma_gate`) are all built + leak-tested. No strategy verdict this pass. Scoping decision in `.tmp/decisions.md` `[iter-041]`. **Next step: spec the A1 book** (base BTC-anchored vs equal-risk basket / single-gate vs ensemble regime / off vs confirmed-bear short, vol-targeted) on these primitives — a full-session job with the CPCV/DSR/SPA kill-bar verdict + the trial registry's first real records.

## 2026-07-08 — iter-042: complete-dataset tick reader + full-history BTC/EUR reconciliation (Phase 1, T0004)

- **`cli/tick/read.py` now reads the complete-dataset schema** — the full-history dump (`Kraken_Trading_History.zip:TimeAndSales_Combined/*.csv`) is headerless **3-field `Timestamp,Price,Volume`** (no side), vs the quarterly ZIPs' `Price,Volume,Timestamp,Type[,…]`. `_detect_schema` picks the layout by the first data row's field count, disambiguating a 3-field row by whether field 0 is a plausible Unix timestamp (`≥ 1e9`) — so a genuinely-short 4-field row (small first field) still errors rather than being silently reinterpreted. Complete-layout rows get `side` = null (unused by the O/H/L/C/VWAP math). TDD (2 new tests; the existing malformed-row tests stay green via the disambiguation).
- **Full-history BTC/EUR reconciliation** (`docs/tick-reconciliation-report.md`): 102.4 M ticks (2013-09-10 → 2025-12-31) → 106,626 hourly bars vs the canonical OHLCVT, **100.0000 % coverage**. Match rate **99.94 % within 1 %, 97.23 % within 10 bp**, but **77 % at 1e-6**.
- **Distrust-the-instrument — the strict miss is a real data property, not a bug.** Same code gives 100 % at 1e-6 on Q1-2026; coverage is 100 %; the **median close reldiff is 0.000 bps in every year** — the centre is exact. The ~23 % strict miss is sub-10-bp cross-source storage-precision noise (the historical OHLCVT reconstructed from 1-minute dumps vs the raw tick export carry different rounding). Only **68 hourly bars (0.064 %) genuinely diverge > 1 %**, isolated to 2013–2015 early-illiquid BTC (+ 1 lone 2024 bar) — flag-candidates only if early-2013 bars ever become load-bearing.
- **Exit-bar reading:** the ≥99.5 % tolerance test is met at a 1 % band (99.94 %) but is precision-noise-limited at 1e-6; the canonical dataset faithfully reproduces 12+ years of tick-derived bars to within ~10 bp. **T0004 stays `partial`** — the per-pair full-**universe** full-history batch (each ~GB member) remains the deferred remainder; this iteration proves the reader + a representative 12-year BTC/EUR run. Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-043: full-universe full-history tick reconciliation (Phase 1, T0004)

- **Ran the full-universe full-history reconciliation** — all 10 EUR majors, complete dataset, 2013→2025 (`docs/tick-reconciliation-report.md`): **660,343 hourly bars, 100.0000 % coverage** (0 not-covered). The iter-042 BTC finding **generalizes** — **9 of 10 pairs match ≥ 99.7 % within 1 %**; 1e-6 is precision-noise-limited (77–91 %); total **1088 bars (0.165 %) diverge > 1 %**, all in each pair's early-illiquid history. Doc-only iteration (no code — the iter-042 reader + reconcile handle everything); numbers verified from the reviewed reconcile path.
- **Distrust-the-instrument on the weakest pair.** LTC/EUR is the low outlier (99.37 % within 1 %, 611 > 1 % bars — 3–4× the next pair); rather than hand-wave it, verified the per-year distribution: **606 of 611 fall in 2013–2017** (220 in 2014, 211 in 2015), near-zero after 2018 — LTC was very thinly traded early, so its divergence is concentrated in sparse history while modern LTC reconciles near-exactly. Consistent with the universe-wide early-illiquid pattern, not an anomaly.
- **T0004 stays `partial` — reconciliation complete, one judgment left.** The heavy batch work is done and characterized; what remains is a **human exit-bar tolerance judgment** (accept the ~1 % band / ≥99.7 % for 9-of-10 with the characterized early-illiquid residual → flip to `resolved`, or tighten/flag the pre-2018 sparse years) — deliberately not autonomously resolved because it touches the Phase-1 exit-bar standard. The optional 15m-bars / tick-storage enhancement remains deferred to Phase-4 microstructure need. Engineering/data iteration — not logged in `.tmp/decisions.md`.
