# Phase 6 — mutation audits of the gate-critical code (2026-07-20)

Two mutation audits of the code deciding whether the deployed strategy may trade real money: the concordance gate arithmetic (`cli/engine/concordance.py`) and the gate-evidence cache (`cli/engine/gate_cache.py`).

**Neither found a live defect.** Every finding is test coverage, not correctness — the code computes the right thing everywhere probed. What the audits measured is which *guarantees* would survive being broken, i.e. which claims decay silently at the next refactor.

**Why they were run.** Not a scheduled sweep: iter-111 (spec `00061`) found several of its own tests vacuous — swapping two labels, or attributing a result to a source that never ran, left the suite green because the assertions were order-agnostic membership checks. That is a defect in the *testing practice*, not in one test, and the retroactive-audit rule requires re-auditing everything built under the same practice. These two modules are the most consequential such code.

**The findings are tracked in \[[T0076]\] (concordance) and \[[T0075]\] (gate cache).** This document is the durable evidence behind them — the per-mutation tables below are what a future investigator needs to re-run or extend the work, and they are committed here rather than left in a session scratchpad precisely because an audit whose evidence evaporates is an audit nobody can check.

**Two methodological cautions for anyone repeating this**, both learned the hard way here:

1. **Purge `__pycache__` between mutations and assert on disk that the mutation is still present after the test run.** CPython validates `.pyc` on source mtime+size; a detected mutation completes in ~2 s, so consecutive iterations collide inside the same mtime second and pytest silently re-runs the *previous* mutation's bytecode. This biases in **both** directions — the concordance audit's first pass produced two false "undetected" and two false "detected" before it caught itself.
2. **Give each concurrent mutation agent its own git worktree.** The first concordance run shared a working tree with the cache run; both restored files via broad `git checkout -- cli tests`, so each could revert the other's in-flight edit, after which a test runs against unmutated code, passes, and is recorded as "nothing caught this" — fabricating gaps that do not exist.

**A raw undetected count is not a finding.** Both audits triaged every undetected mutation into genuine gap vs. equivalent/safe-direction mutant, the cache audit by writing an exploitability script that constructs the input which would now produce a wrong verdict. Report the triaged number, not the raw one.

______________________________________________________________________

## Audit A — the gate-evidence cache (`cli/engine/gate_cache.py`)

60 mutations, 44 detected, 16 undetected raw, **8 genuine gaps** after triage. Auditor: Claude Opus 4.8.

**Auditor model:** Claude Opus 4.8 (1M context) — exact model ID `claude-opus-4-8[1m]`.
**Date:** 2026-07-20. **Repo:** `/home/zhaow/Projects/zcrypto-kraken`, branch `develop`, HEAD `51397c1`.
**Mode:** read-only audit. Every mutation applied, tested, and reverted via `git checkout -- cli tests`.

## Scope

- Target code: `cli/engine/gate_cache.py`, `_evaluate_journal` / `_replay_one` / `CacheStats` / `_write_prom_textfile` in `cli/engine/command.py`.
- Covering tests found (exhaustive `grep` for `gate_cache|GateCache|replay_fingerprint|evidence_fingerprint|from_cache|cache_stats` across `cli/` and `tests/`): `tests/test_engine_gate_cache.py` (13 tests), `tests/test_engine_gate_export_cache.py` (9 tests). No other test file references the cache. 22 tests total.
- Baseline: `uv run pytest tests/test_engine_gate_cache.py tests/test_engine_gate_export_cache.py -q` → **22 passed in 0.35s**.
- Harness: `scratchpad/mutate.py` — asserts the target string is present *and unique* before writing (per `.claude/rules/agent-ops.md`: an unasserted `str.replace` miss silently no-ops), runs the targeted suite, restores, records.

## Result summary

|  | count |
| -- | -- |
| Mutations applied | **60** |
| Detected | 44 |
| **Undetected (raw)** | **16** |
| — of which equivalent / safe-direction (analysed below) | 8 |
| — **genuine coverage gaps** | **8** |

Genuine gaps: **M01, M02, M03, M05** (module list) · **M19, M20, M22** (evidence key) · **M51** (fail-trusting).

______________________________________________________________________

## Category 1 — Under-invalidation (the catastrophic direction)

Each of the ten hashed module paths removed individually from `_REPLAY_CODE_PATHS`, then config / replay path / numpy / Python dropped one at a time.

| ID | Mutation | Result |
| -- | -- | -- |
| M01 | drop `cli/portfolio/crossfreq_system.py` | **UNDETECTED** — 22 passed |
| M02 | drop `cli/portfolio/crossfreq.py` | **UNDETECTED** — 22 passed |
| M03 | drop `cli/risk/limits.py` | **UNDETECTED** — 22 passed |
| M04 | drop `cli/risk/governor.py` | DETECTED — 2 failed |
| M05 | drop `cli/engine/concordance.py` | **UNDETECTED** — 22 passed |
| M06 | drop `cli/engine/journal.py` | DETECTED — 2 failed |
| M07 | drop `cli/alpha/a1.py` | DETECTED — 1 failed |
| M08 | drop `cli/alpha/a2.py` | DETECTED — 1 failed |
| M09 | drop `cli/portfolio/builder.py` | DETECTED — 1 failed |
| M10 | drop `cli/benchmark/strategies.py` | DETECTED — 1 failed |
| M11 | drop `repr(config)` from the digest | DETECTED — 1 failed |
| M12 | drop `path` from the digest | DETECTED — 1 failed |
| M13 | drop numpy version from the digest | DETECTED — 1 failed |
| M14 | drop `sys.version_info[:2]` from the digest | DETECTED — 1 failed |
| M15 | hash module *basenames* instead of source bytes | DETECTED — 2 failed |

### Finding 1 (HIGH) — the four original modules are pinned by nothing

The detection split is exactly along the review-history fault line. The **six modules added at review** are held by an explicit membership assertion in `test_replay_fingerprint_covers_the_live_and_latent_gap_modules`:

```python
expected_new = {governor.py, journal.py, a1.py, a2.py, builder.py, strategies.py}
assert expected_new <= covered
```

The **four modules from the original design** — `crossfreq_system.py`, `crossfreq.py`, `limits.py`, `concordance.py` — appear in **no** assertion anywhere. The only test touching the real list, `test_replay_fingerprint_default_covers_the_real_files`, asserts merely that the result is a stable 64-character string, which holds for a list of any length ≥ 0.

This is the highest-severity coverage finding in the audit. `concordance.py` (M05) contains `replay_cycle`, `compare_targets` *and* `evaluate_gate` — the entire verdict pipeline. `crossfreq_system.py` (M01) contains `build_crossfreq_system_fast`, the builder the fast route actually runs. Silently dropping either leaves the cache serving pre-change verdicts forever, with a green suite.

The regression-shaped risk is concrete: the fix for review-hole #1 wrote a test that pins *only what that fix added*, leaving the pre-existing majority of the list unguarded. The list's completeness — named in the task brief as "the property most likely to rot" — is guarded for 6/10 of its members.

**Recommended pin:** assert the full expected set with equality against the real list, not `<=` on a subset:

```python
assert covered == {crossfreq_system.py, crossfreq.py, limits.py, governor.py,
                   concordance.py, journal.py, a1.py, a2.py, builder.py, strategies.py}
```

Equality also catches an accidental *addition*, and makes any future edit to the list a deliberate, test-visible act.

______________________________________________________________________

## Category 2 — Evidence-key completeness

Each component of `evidence_fingerprint`'s payload dropped in turn.

| ID | Mutation | Result |
| -- | -- | -- |
| M16 | drop `SnapshotEntry.pair` | UNDETECTED (equivalent — see below) |
| M17 | drop `SnapshotEntry.grid` | UNDETECTED (equivalent — see below) |
| M18 | drop `n_bars` (regression of fixed hole #2) | DETECTED — 1 failed |
| M19 | drop `first_ts` | **UNDETECTED** — 22 passed |
| M20 | drop `last_ts` | **UNDETECTED** — 22 passed |
| M21 | drop `content_hash` | DETECTED — 1 failed |
| M22 | drop `path` | **UNDETECTED** — 22 passed |
| M23 | drop `cycle_ts` | DETECTED — 1 failed |
| M24 | drop `completed_at` | DETECTED — 1 failed |
| M25 | drop `final_targets` | DETECTED — 2 failed |
| M26 | revert to content_hash-only snapshots (original hole #2) | DETECTED — 1 failed |
| M27 | drop the whole snapshots set | DETECTED — 2 failed |
| M28 | constant digest (accept anything as unchanged) | DETECTED — 3 failed |
| M29 | hash only the first snapshot | DETECTED — 2 failed |

### Exploitability check

Undetected ≠ exploitable, so each undetected drop was tested against a *realistic tamper* of that field (`scratchpad/exploit.py`), comparing the base and tampered digests with the field removed:

```
field         real code    field dropped  verdict
pair          caught       caught         still caught (sort key masks it)
grid          caught       caught         still caught (sort key masks it)
first_ts      caught       MISSED         STALE HIT (exploitable)
last_ts       caught       MISSED         STALE HIT (exploitable)
path          caught       MISSED         STALE HIT (exploitable)
```

`pair` and `grid` are self-healing: they are the `sorted(..., key=lambda s: (s.pair, s.grid))` key, so tampering either reorders the payload list and changes the digest anyway. **M16/M17 are therefore near-equivalent mutants, not gaps.**

### Finding 2 (HIGH) — `first_ts` / `last_ts` are the *same* tamper class as the fixed `n_bars` hole, and are unpinned

`replay_cycle` reconciles all three metadata fields in one condition (`cli/engine/concordance.py:118`):

```python
if len(ts) != entry.n_bars or ts[0] != entry.first_ts or ts[-1] != entry.last_ts:
    raise EngineJournalError(...)
```

Review-hole #2 was "the evidence key hashed only `content_hash`, so tampering `n_bars` let a cached PASS survive a replay that would have failed validation." The fix correctly added all seven fields to the payload — but `test_evidence_fingerprint_changes_when_n_bars_tampered_leaving_content_hash_untouched` pins **only `n_bars`**. `first_ts` and `last_ts` sit in the identical `or` clause of the identical guard and produce the identical failure mode, and nothing would catch their removal.

The code is **correct today**; this is a coverage finding. But it is the precise shape of defect the audit was commissioned to find: the fix was generalised, the test was not, so two-thirds of the fix is unprotected against regression.

### Finding 3 (MEDIUM) — `path` is unpinned and exploitable

`SnapshotEntry.path` is resolved by `_snapshot_reader` into the file the replay actually reads. Repointing one entry's path at a different pair's parquet is a verdict-changing tamper that a real replay would reject (hash mismatch), and it survives a `path`-less fingerprint as a stale cache hit. Unpinned.

______________________________________________________________________

## Category 3 — A cached failure must never come back as a pass

| ID | Mutation | Result |
| -- | -- | -- |
| M30 | deserialize `mismatch` as `False` | DETECTED — 2 failed |
| M31 | deserialize `validation_failed` as `False` | DETECTED — 1 failed |
| M32 | deserialize `compare_passed` as `True` | DETECTED — 1 failed |
| M33 | serialize `mismatch` as `False` | DETECTED — 2 failed |
| M34 | serialize `validation_failed` as `False` | DETECTED — 1 failed |

**Fully pinned.** `test_cache_round_trip_preserves_outcome_exactly` and `test_cached_failure_stays_a_failure` cover both directions of the round trip. No gap.

______________________________________________________________________

## Category 4 — Fail-open vs fail-trusting

| ID | Mutation | Result |
| -- | -- | -- |
| M35 | serve entries despite a `replay_fp` mismatch | DETECTED — 2 failed |
| M36 | drop the `schema_version` check | DETECTED — 1 failed |
| M37 | corrupt cache serves partially-parsed entries (weak form) | UNDETECTED — see M51 |
| M38 | `rejected` never set (invalidated metric always 0) | DETECTED — 2 failed |
| M52 | narrow the `except` so an unreadable path propagates | DETECTED — 1 failed |
| M53 | absent file reports `rejected=True` | DETECTED — 2 failed |
| M58 | `save_cache` raises on write failure | DETECTED — 1 failed |
| M59 | write a wrong `schema_version` | DETECTED — 5 failed |
| M46 | non-atomic write (no tmp + `os.replace`) | DETECTED — 1 failed |
| M50 | `replay_fingerprint` OSError aborts instead of degrading | DETECTED — 1 failed |
| M51 | **per-entry tolerant parse — a partially corrupt cache serves its good entries** | **UNDETECTED** — 22 passed |

M37 was a weak mutation (`locals().get("entries", {})` — `entries` is unbound when `json.loads` raises, so it returned `{}` regardless). It was superseded by M51, which is the real form of the mutation.

### Finding 4 (MEDIUM) — partial-corruption tolerance is unpinned

Wrapping the per-item parse in `try/except: continue` makes a cache file with two valid entries and one malformed entry serve the two valid ones instead of discarding the file wholesale. That directly violates the stated design property — *"the cache must invalidate wholesale whenever anything that could change a verdict changes"* — and nothing catches it.

Every existing fail-open test exercises corruption that fails **before** the entry loop (truncated JSON, wrong schema, `replay_fp` mismatch, a directory instead of a file). None constructs a **structurally valid file with a malformed entry inside `entries[]`**, which is the one path where "discard the file" and "skip the bad row" diverge. The current code is correct (the bare `_outcome_from_dict` call propagates into the outer handler and rejects the whole file) — but that correctness is incidental to the code's shape, not asserted.

______________________________________________________________________

## Category 5 — Attribution / order (the shape that survived the audited iteration)

| ID | Mutation | Result |
| -- | -- | -- |
| M39 | ignore the evidence fp, serve any entry for that `cycle_ts` | DETECTED — 1 failed |
| M40 | swap `replayed` / `from_cache` in `CacheStats` | DETECTED — **9 failed** |
| M41 | swap the two prom counter names | DETECTED — 2 failed |
| M42 | key the cache by `completed_at` instead of `cycle_ts` | DETECTED — 5 failed |
| M43 | store `replay_fp` where `evidence_fp` belongs (two hashes confused) | DETECTED — 5 failed |
| M44 | swap `cycle_ts` / `completed_at` on serialize | DETECTED — 5 failed |
| M45 | rotate `evidence_fp`s across entries on save | DETECTED — 3 failed |
| M48 | increment `replayed` on a hit and `from_cache` on a replay | DETECTED — **9 failed** |
| M49 | never re-save cache hits (entries decay after one generation) | DETECTED — 1 failed |
| M60 | drop the `fp is not None` guard on the cache lookup | UNDETECTED (equivalent — proven below) |

**This category is the strongest in the module — and notably, it is exactly the category that failed in the iteration that triggered this audit.** Every same-typed confusion is caught, most of them loudly (9 failures for a counter swap). The credit goes to two design choices:

1. `test_warm_cache_equals_cold_cache` asserts **asymmetric, non-interchangeable** values (`cold: replayed==2, from_cache==0` / `warm: replayed==0, from_cache==2`), so a swap cannot satisfy both.
2. `_counted_replay_cycle` records **which `cycle_ts` was actually replayed**, so `assert calls == [CYCLE_TS + timedelta(hours=4)]` is an identity check, not a count — the single most valuable pattern in the suite, and precisely the antidote to the order-agnostic membership assertions that failed elsewhere.

M60 is an **equivalent mutant**, verified: when `evidence_fingerprint` raises, `fp` is `None`, and the surviving comparison `cached_entry[0] == fp` compares `str` to `None` → `False` → the replay branch is taken. The `if fp is not None` guard is redundant defence-in-depth; removing it changes no behaviour. Not a gap.

______________________________________________________________________

## Category 6 — Determinism / canonicalisation (informational)

| ID | Mutation | Result | Classification |
| -- | -- | -- | -- |
| M54 | sort key `(pair, grid)` → `(grid, pair)` | UNDETECTED | equivalent — still a canonical total order |
| M55 | no canonical ordering at all | UNDETECTED | **safe direction** — strictly more sensitive (over-invalidation) |
| M56 | drop `sort_keys` in `evidence_fingerprint` | UNDETECTED | near-equivalent — payload is a dict literal with fixed insertion order |
| M57 | drop `sort_keys` in `save_cache` | UNDETECTED | near-equivalent — cosmetic, digest not derived from this text |
| M47 | fingerprint layer runs when `cache_path is None` | DETECTED — 1 failed | D1 is well pinned |

M55 is worth noting as a *correct non-finding*: it makes the fingerprint order-sensitive, which is over-invalidation — the design explicitly calls that safe. A test failing on M55 would be a test over-constraining the implementation.

M56/M57 are latent fragility rather than defects: the digest's stability currently rests on CPython dict insertion order rather than on the explicit `sort_keys=True`. Removing the belt leaves the braces. No action required.

______________________________________________________________________

## Design-level finding (not a mutation) — a candidate third hole

The brief said to assume a third hole exists. Mutation testing found none in the *implemented* logic — but tracing the fast-route dependency closure of `replay_cycle` against `_REPLAY_CODE_PATHS` surfaces two verdict-determining modules that are **not hashed**:

**(a) `cli/engine/command.py`** — contains `_replay_one`, which is the *sole* place a replay's exception is converted into a verdict:

```python
except HashMismatchError:   return CycleOutcome(..., mismatch=True)
except EngineJournalError:  return CycleOutcome(..., validation_failed=True)
verdict = compare_targets(record.final_targets, replayed)
```

It also holds `_snapshot_reader` (which converts an unreadable parquet into `EngineJournalError` → `validation_failed`) and `_EVALUATE_JOURNAL_REPLAY_PATH`. Editing the exception→verdict mapping changes every future verdict while every hashed module's bytes stay identical — cached verdicts would then be served under the old classification indefinitely.

**(b) `cli/ohlc/dataset.py`** — `read_parquet` produces the `(ts, closes)` that feed *both* `snapshot_content_hash` and the builder. A dtype or timezone-handling change there alters computed hashes and therefore verdicts, again with no hashed module touched.

This is the same class as the two holes already found at review (a verdict-determining module absent from the list). Severity is bounded by a mitigating factor — a change to either module would *usually* accompany a change to a hashed module — but "usually" is exactly the reasoning the design rejects ("over-invalidation costs one rebuild, under-invalidation silently corrupts gate evidence"). I flag it as a design gap for the owner to rule on rather than a proven exploit, since the ten-module list is a spec-fixed number (spec `00060` D3) and extending it is a design decision, not a bug fix.

**Docstring inaccuracy (cosmetic, no safety impact):** `_REPLAY_CODE_PATHS` labels `a1.py`, `a2.py`, `builder.py`, `strategies.py` as "LATENT — only reachable on the verified route". They are in fact reachable on the **fast** route: `cli/portfolio/crossfreq_system.py:53-56` imports all four at module scope and `build_crossfreq_system_fast` uses them. They are covered either way, so this is a comment correction only — but the comment currently understates why they matter, which invites a future cleanup to drop them as dead weight.

______________________________________________________________________

## Tree state

My mutations are **fully reverted**. `cli/engine/gate_cache.py`, `cli/engine/command.py` and both test files are byte-identical to HEAD `51397c1` (`git diff 51397c1 -- <targets>` is empty).

**Final state at hand-back:** only `docs/open-topics/T0065-...md` remains modified (not mine). The `concordance.py` edit below was reverted by the concurrent session itself while I was writing this report — further confirmation that another session is live in this tree.

⚠️ **Two modifications appeared in the working tree during the audit that are NOT mine, and I deliberately did not revert either:**

- `cli/engine/concordance.py` — removes the `len(ts) != entry.n_bars` clause from `replay_cycle`'s metadata reconciliation (mtime `06:17:16`, *after* my final restore).
- `docs/open-topics/T0065-...md` — adds a `ripe_when:` frontmatter line (mtime `06:12:53`, during my run).

A **concurrent session is active in this working directory** (reflog shows a `pull --ff-only` + checkout at 06:09, and both files were written while I was running). My audit never touches `docs/`, and my only `cli/` edits were to `gate_cache.py`/`command.py`. Reverting another session's in-flight work would be destructive, so I left both in place.

**Operational hazard to note:** my harness ran `git checkout -- cli tests` ~60 times between ~06:10 and ~06:17. Any concurrent edit under `cli/` in that window would have been silently clobbered. The `concordance.py` edit survived only because it landed after my last restore. A mutation harness that restores by path-scoped `git checkout` is unsafe in a shared working tree — it should use a `git worktree` (or `EnterWorktree`) instead. Recommend the owner confirm nothing was lost.

**Substantive note on that foreign edit:** removing the `len(ts) != entry.n_bars` check from `replay_cycle` deletes the very guard that makes the `n_bars` half of the evidence key meaningful. Review-hole #2 was fixed by making `evidence_fingerprint` sensitive to `n_bars` *because* `replay_cycle` rejects an `n_bars` disagreement. If that clause goes, the cache's `n_bars` sensitivity becomes over-invalidation with no matching validation behind it — and, more importantly, a real `n_bars` tamper stops being rejected on the replay path itself. Worth a look by whoever is making that change; I have not touched it.

______________________________________________________________________

## Audit B — the concordance gate arithmetic (`cli/engine/concordance.py`)

46 mutations (plus 2 higher-order probes and 2 manual re-verifications), **10 undetected**. Auditor: Claude Opus 4.8. Results below are from the cache-purged re-run with on-disk mutation assertions; the first pass was discarded (see caution 1 above).

**Target:** `/home/zhaow/Projects/zcrypto-kraken/cli/engine/concordance.py` (`evaluate_gate`, `replay_cycle`, `compare_targets`)
**Auditor model:** Claude Opus 4.8 (1M context) — model ID `claude-opus-4-8[1m]`
**Repo state:** branch `develop`, HEAD `51397c19e5c82ede9cf6e504a859e988ce4a1657`, `cli/` + `tests/` clean before and after.
**Date:** 2026-07-20
**Mode:** read-only audit. Every mutation was applied, measured, and reverted from a pristine snapshot. No commit, no amend, no fix.

## Covering tests (found by grep for `concordance|evaluate_gate|replay_cycle|CycleOutcome|compare_targets`)

| File | Role |
| -- | -- |
| `tests/test_engine_concordance.py` | primary — 20 tests, direct unit coverage |
| `tests/test_engine_gate_export.py` | `gate-export` command end-to-end over `evaluate_gate` |
| `tests/test_engine_gate_cache.py` | cache layer producing `CycleOutcome` |
| `tests/test_engine_gate_export_cache.py` | cached vs uncached `evaluate_gate` equivalence |
| `tests/test_engine_cycle.py` | real `replay_cycle` + `compare_targets` round-trip |
| `tests/test_engine_command.py` | command-level journal evaluation |
| `tests/test_engine_soak.py` | soak-path `replay_cycle` |

Baseline: all 236 tests pass in ~19 s. Each mutation ran the four fast files first (~0.7 s); if they all passed, the full 236-test set was run before declaring the mutation undetected — so "undetected" means undetected by *every* test that touches this module.

## Methodology note — a false-result trap that had to be fixed mid-audit

The first pass reported 10 undetected, but two of them (inverting the compare check; shifting the day cutoff) were verifiable by hand as detected. Root cause: **stale `__pycache__`**. CPython validates a `.pyc` on `(source mtime, source size)`. A detected mutation's iteration completes in ~2 s, so consecutive iterations could write their mutated source within the *same mtime second*; when the new mutation also happened to match the cached size, pytest silently re-ran the *previous* iteration's bytecode.

This biases in **both** directions — it can report a killed mutant as surviving, and it can report a surviving mutant as killed (by inheriting the previous, detected mutation's bytecode). The whole 46-mutation battery was therefore re-run with `__pycache__` purged after every write plus an on-disk assertion that the mutation was still present **after** the test run. Between the two runs, M09/M19 flipped to DETECTED and M02/M08 flipped to UNDETECTED. **Only the cache-clean run is reported below.** Drivers: `gate_mutate.py`, `gate_recheck.py`, `gate_full.py`; raw results in `gate_full.json`.

______________________________________________________________________

## Results — 46 mutations, 36 detected, 10 undetected

### Category 1 — streak / counting arithmetic (7 applied, 6 detected)

| ID | Mutation | Result |
| -- | -- | -- |
| M01 | `gate_met=streak >= _GATE_STREAK_DAYS` → `>` | DETECTED — `test_gate_met_on_14_consecutive_clean_days` |
| **M02** | `_GATE_STREAK_DAYS = 14` → `13` | **UNDETECTED** — 236 passed |
| M03 | `streak = streak + 1 if day_clean else 0` → `streak = streak + 1` (no reset) | DETECTED — 6 failures |
| M04 | failing day freezes streak instead of resetting to 0 | DETECTED |
| M05 | clean day counts `+2` | DETECTED |
| M06 | `streak` initialised to `1` | DETECTED |
| M45 | intra-day `if not day_clean: break` → `pass` | DETECTED |

### Category 2 — day-boundary handling (12 applied, 9 detected)

| ID | Mutation | Result |
| -- | -- | -- |
| M07 | `while d <= upper_bound_day` → `<` | DETECTED |
| **M08** | `upper_bound_day = max(last_observed_day, now.date())` → `min(...)` | **UNDETECTED** |
| M09 | day cutoff shifted `+1 day` | DETECTED — 13 failures |
| **M10** | cutoff `20:00 + _FRESHNESS_WINDOW` → `20:00` | **UNDETECTED** |
| M11 | `_CYCLE_HOURS` shifted to `(1,5,9,13,17,21)` | DETECTED |
| M12 | `_CYCLE_HOURS` drops `20` (day clean with 5 cycles) | DETECTED |
| M13 | mid-day-start exclusion disabled | DETECTED |
| M14 | mid-day start does not skip to next day | DETECTED |
| M15 | `first_cycle_ts = min(...)` → `max(...)` | DETECTED — 13 failures |
| M16 | empty-journal early return claims `gate_met=True` | DETECTED |
| M44 | `_FRESHNESS_WINDOW` 30 min → 24 h | DETECTED — 13 failures |
| **M41** | `if h4_ts[-1] != expected_h4_last:` → disabled | **UNDETECTED** |

### Category 3 — classification (17 applied, 12 detected)

| ID | Mutation | Result |
| -- | -- | -- |
| M17 | **catastrophic**: `elif entry.mismatch:` → never fires | DETECTED — 2 failures |
| M18 | **catastrophic**: `elif entry.validation_failed:` → never fires | DETECTED — 6 failures |
| M19 | **catastrophic**: `elif not entry.compare_passed:` → `elif entry.compare_passed:` | DETECTED — 8 failures |
| M20 | **catastrophic**: `if entry is None:` → never fires | DETECTED — 2 failures |
| **M21** | freshness lower bound dropped (`completed_at < boundary` passes) | **UNDETECTED** |
| M22 | freshness upper bound dropped (arbitrarily late cycle passes) | DETECTED |
| M23 | **catastrophic**: `passed=worst_abs_diff <= tol` → `>= tol` | DETECTED — 11 failures |
| M24 | structural asset-set mismatch never detected | DETECTED |
| M25 | **catastrophic**: `replay_cycle` hash verification disabled | DETECTED |
| M26 | duplicate `cycle_ts` last-wins instead of raising | DETECTED |
| **M36** | metadata reconcile: `len(ts) != entry.n_bars` dropped | **UNDETECTED** |
| **M37** | metadata reconcile: `ts[-1] != entry.last_ts` dropped | **UNDETECTED** |
| **M38** | metadata reconcile: `ts[0] != entry.first_ts` dropped | **UNDETECTED** |
| M39 | `validate_record(record)` call removed | DETECTED — 3 failures |
| **M40** | `_assemble` cross-pair shared-calendar check dropped | **UNDETECTED** |
| M42 | `compare_targets({}, {})` returns `passed=False` | DETECTED |
| M43 | `tol` 1e-6 → 1e-2 | DETECTED |

### Category 4 — attribution / order swaps (10 applied, 9 detected)

| ID | Mutation | Result |
| -- | -- | -- |
| M27 | validation-failed reported with the **hash-mismatch** reason string | DETECTED |
| M27b | hash-mismatch reported with the **validation-failed** reason string | DETECTED |
| **M28** | `last_failure` keeps the **first** reset instead of the most recent | **UNDETECTED** |
| M29 | `FailureDetail` reports the day's 00:00 boundary, not the failing cycle | DETECTED |
| M30 | builder called `(h4, daily)` instead of `(daily, h4)` | DETECTED |
| M31 | `path="fast"` selects the verified builder and vice versa | DETECTED — 21 failures |
| M32 | grids swapped at assembly (240 as daily, 1440 as h4) | DETECTED — 11 failures |
| M33 | newest row read by blind `[-1]` instead of `series[result.n_periods]` | DETECTED |
| M34 | `worst_asset = max(...)` → `min(...)` | DETECTED |
| M35 | `expected_h4_last = cycle_ts + 4h` instead of `- 4h` | DETECTED — 11 failures |

**Good news worth stating:** the attribution category — the exact shape that survived in the audited iteration — is the *strongest* here (9/10). Both reason-string swaps (M27/M27b), both grid/argument-order swaps (M30/M32), the builder-selection swap (M31), the blind-`[-1]` read (M33) and the worst-asset swap (M34) are all pinned by assertions that compare exact values, not membership. Whoever wrote `test_replay_cycle_locates_newest_row_by_cycle_ts` deliberately chose `series[2] = 0.1` distinct from `series[-1] = 0.2` so the mutation cannot hide — that is the correct practice, applied.

______________________________________________________________________

## Higher-order probes (metadata-reconcile line)

Because M36/M37/M38 all survived individually, two combined mutations were run to find out whether the line is pinned *at all*:

| Probe | Mutation | Result |
| -- | -- | -- |
| H1 | whole reconcile condition → `if False:` | **DETECTED** — `test_replay_cycle_daily_peek_disagrees_with_metadata_raises_before_build` |
| H2 | keep **only** the `first_ts` sub-check | **DETECTED** — same test |

Interpretation: exactly **one** scenario exercises the whole line, and that scenario trips `n_bars` *and* `last_ts` simultaneously (the peeked daily snapshot has both an extra bar and a later last stamp). So the line as a whole is pinned, but no test can tell you **which** sub-check fired — either of the two can be deleted silently. H2 additionally proves `first_ts` contributes to **no** test at all.

`validate_record` (`cli/engine/journal.py:139-152`) independently enforces `grids["240"].last_ts == cycle_ts - 4h` on the *metadata*, and the reconcile step forces data == metadata — which is why **M41 is an equivalent mutant by redundancy**, not a coverage hole: the `h4_ts[-1] != expected_h4_last` guard is logically implied by the two upstream checks and cannot be killed in isolation by any test. It is genuine defense-in-depth; it just isn't independently observable.

______________________________________________________________________

## Additional checks that found nothing wrong

- **Timezone contract.** `evaluate_gate` derives `tzinfo` solely from `now.tzinfo` and assumes journal entries share it. `tests/test_engine_concordance.py` uses naive datetimes exclusively (0 occurrences of `timezone`), which initially looked like the production mode (`_utc_now() → datetime.now(timezone.utc)`, `cli/engine/command.py:47-48`) being untested. It is **not** a gap: `tests/test_engine_gate_export.py:32` drives the whole command with `DAY0 = datetime(2026, 7, 1, 0, 0, tzinfo=UTC)`, so the aware path is covered end-to-end.
- **Boundary scenarios walked by hand** — empty journal (M16, pinned), single cycle, mid-run multi-day gap, `now` far past the last journaled cycle. In every case the code's verdict moves in the direction its docstring claims. The gap-in-the-middle case correctly resets the streak because the day loop walks calendar days, not journal entries.
- **`last_failure` semantics.** The docstring's "most recent break … that reset the streak to its current run" is what the code computes: later failures overwrite earlier ones, so the surviving value is necessarily the one immediately preceding the current run. Consistent (though see U9 — untested).

______________________________________________________________________

## VERDICT ON ACTUAL BUGS

**No live defect was found in the gate arithmetic.** Every one of the 10 undetected mutations is a *test-coverage* finding: the shipped code computes the correct thing, and the mutation I applied made it compute the wrong thing without any test objecting. I want to be explicit about this distinction because the brief asked for it — I am **not** reporting a bug in `evaluate_gate`, `replay_cycle`, or `compare_targets`. The arithmetic is right; parts of it are simply unguarded against future edits.

The catastrophic direction was probed specifically and is **well defended**: a cached or computed FAILURE silently becoming a PASS is caught in every form tested — hash mismatch (M17), validation failure (M18), inverted compare (M19), missing cycle (M20), inverted tolerance (M23), disabled hash verification (M25). That is the single most reassuring result of this audit.

______________________________________________________________________

## The 10 undetected claims, by severity

### U1 — M02 · `_GATE_STREAK_DAYS = 14 → 13` · **HIGH**

The ratified 14-day requirement — the number that decides whether real money may trade — is not pinned in the loosening direction. `test_gate_met_on_14_consecutive_clean_days` builds exactly 14 clean days and asserts `gate_met`; with the threshold at 13, `14 >= 13` is still true, so the suite stays green. Nothing anywhere asserts that **13** clean days does *not* meet the gate. Only the tightening direction is caught (M01, `>=` → `>`, fails at exactly 14). A one-character edit that opens the gate a day early ships silently.

*Fix:* add `assert not evaluate_gate(13_clean_days, now=...).gate_met` alongside the existing 14-day test. Two lines close it.

### U2 — M08 · `upper_bound_day = max(...)` → `min(...)` · **HIGH**

With `min`, the evaluable-day loop stops at the last day the journal has entries for, instead of walking forward to today. A dead engine — journal frozen days ago — would therefore keep its **old streak and its `gate_met=True`** instead of accumulating all-missing days and resetting to 0. That is the catastrophic direction, and no test catches it.

The trap here is that `test_gate_export_stale_journal_pings_fail` *looks* like it covers this. It does not: it passes `--lag-fail-seconds 1` and exercises a **separate command-level lag check**. It stays green under the mutation. `evaluate_gate`'s own stale-journal behaviour — the reason `max` is there — is entirely unpinned.

*Fix:* a direct `evaluate_gate` test with 14 clean days and `now` set 3 days past the last cycle, asserting `streak == 0` and `not gate_met`.

### U3 — M40 · `_assemble` cross-pair shared-calendar check · **MEDIUM**

Every `replay_cycle` test uses a single pair (`"BTC"`), so the `elif ts != shared_ts: raise EngineJournalError` branch is never executed by any test. Production is multi-pair. Two pairs whose 4h calendars disagree would be assembled into the builder as if aligned, and the replay would silently compare against a mis-built system.

*Fix:* one two-pair `replay_cycle` test where the second pair's `ts` differs by one bar.

### U4 — M10 · day-evaluability cutoff drops `_FRESHNESS_WINDOW` · **MEDIUM**

The docstring's opening claim is "a day enters evaluation only after its 20:00 cycle's 30-minute freshness window has elapsed". Tests only ever set `now` to 21:00 (past both) or 09:00 (before both) — nothing lands in the 20:00–20:30 window where the `+ _FRESHNESS_WINDOW` term is the only thing doing work. A day could be scored 30 minutes early, marking a not-yet-late 20:00 cycle as missing.

*Fix:* one test with `now = day 20:15` asserting the day is excluded, not failed.

### U5 — M28 · `last_failure` keeps the first reset, not the most recent · **MEDIUM (attribution)**

This is the audited iteration's exact defect shape. The docstring promises "the boundary + reason of the **most recent** reset"; every multi-day test contains exactly **one** failing cycle, so first and most-recent coincide and the word "most recent" is untestable as written. An operator reading `last_failure` to answer "how long since the last blip?" would get the oldest blip instead, and no test would object.

*Fix:* a test with failures on day 2 *and* day 9 of a 15-day run, asserting `last_failure.cycle_ts` is day 9's.

### U6 — M38 · metadata reconcile `ts[0] != entry.first_ts` · **MEDIUM**

Confirmed by probe H2 to contribute to no test at all. No scenario anywhere varies `first_ts` between the read data and the declared metadata. A reader returning a same-length window shifted backwards would be caught only by this sub-check — which nothing proves works.

### U7 — M36 · metadata reconcile `len(ts) != entry.n_bars` · **MEDIUM**

### U8 — M37 · metadata reconcile `ts[-1] != entry.last_ts` · **MEDIUM**

Individually redundant: the single peek scenario trips both at once, so either can be deleted in isolation and the suite stays green (probe H1 shows the line as a whole *is* pinned). The no-peek hole reopens partially if `last_ts` alone is later removed — e.g. by someone "simplifying" the condition after noticing the test still passes. This is the failure mode the audited iteration was about: a test that proves *something* fired, not *which thing* fired.

*Fix:* split the one peek test into three, each varying exactly one of `n_bars` / `first_ts` / `last_ts`.

### U9 — M21 · freshness lower bound `boundary <= entry.completed_at` · **LOW–MEDIUM**

No test has a `completed_at` earlier than its own boundary, so the lower half of the window check pins nothing. A record claiming to have completed *before* its cycle boundary — clock skew, or a fabricated/replayed record — would score as an on-time clean cycle. Low likelihood, but it is the half of the freshness check that guards against forged rather than late evidence.

### U10 — M41 · re-derived `cycle_ts` boundary check in `replay_cycle` · **LOW (redundant, not a gap)**

Listed for completeness. This carries the module docstring's headline claim ("locates the newest-row targets by an independently re-derived `cycle_ts` boundary rather than by raw last-index"), and disabling it changes nothing observable. But as established above, it is **logically implied** by `validate_record` plus the metadata reconcile — an equivalent mutant, not missing coverage. No test can kill it without first removing one of the upstream guards. The honest characterisation is: the guard is real defense-in-depth, the docstring slightly oversells it as the mechanism when it is the third of three, and there is nothing to fix in the tests.

______________________________________________________________________

## Cleanliness

- `cli/engine/concordance.py` byte-identical to its pristine snapshot (`diff -q`, clean).
- `git status --porcelain -- cli/ tests/` → empty.
- HEAD unchanged: `51397c19e5c82ede9cf6e504a859e988ce4a1657`.
- Baseline retest after all mutations: 57 passed.

**Unrelated concurrent changes observed** (not mine, not touched): during the audit, `docs/open-topics/T0065-*.md`, `docs/open-topics/README.md`, `docs/open-topics/T0075-gate-evidence-guarantees-are-unpinned.md` (new) and `.claude/worktrees/` (new) appeared in `git status`. Another session is working in this repo concurrently. I left all of them alone.
