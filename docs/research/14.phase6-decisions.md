[iter-079] Phase-6 execution engine: which path? (Decision: freestyle — Nautilus primary)
  1. **Thin custom engine now** — exercise the pre-approved §7 fallback for the long-only spot daily-cadence book; revisit Nautilus when a margin/short sleeve arrives. Smallest surface, full auditability; foregoes Nautilus's execution hardening.
  2. **Verify Nautilus first, decide after** — run the §7 hands-on verification, then choose. ~A week before engine work; wins framework maturity for features the current book doesn't use yet.
  3. **Nautilus without verification** — adopt on trust; skips the ratified entry criterion.
  Human (freestyle): "It was pointed as a fallback solution for a reason. Let's not waste time on it until it becomes necessary. Focus on the Nautilus path and make it fully functional." — Nautilus is the primary, verification (§7 entry criterion) proceeds now; the custom engine stays strictly a fallback.

[iter-079] Deployment host for the Phase-6 engine? (Decision: 1)
  1. **D2 VPS (the capture host)** — only 24/7 host, T0003 hardening applied; engine as its own resource-limited compose service so capture is never starved. Recommended and chosen.
  2. **Second VPS** — full isolation from capture, but new monthly spend (a D3 money decision) and a second host to operate; premature for shadow + tiny-live.
  3. **Workstation first, VPS later** — zero setup for 6a but the engine gets redeployed for 6b anyway and concordance evidence would come from a non-production environment.

[iter-079] API-key staging (D3(ii)): stage read-only→trade, or activate the trade key now? (Decision: 2)
  1. **Stage: read-only now, trade key at 6b** — least privilege in time as well as scope; trade key created only when tiny-live starts.
  2. **Activate the trade key now** — the literal §12 kickoff reading; one ceremony instead of two. Chosen by the human. Least privilege in scope holds absolutely: query + orders only, NO withdrawal ever, IP-restricted.

[iter-079] Stage-6a build sequencing? (Decision: 1)
  1. **Shadow engine first** — the daily shadow cycle as committed code (pull → targets → intended-order log → concordance report), cadence-agnostic so a 00038 adoption slots in; ops layer follows as the 6b build. Recommended and chosen.
  2. **Ops layer first** — safety-first ordering, but nothing exercises it until the shadow cycle exists; drills would be synthetic.
  3. **Attended live-prep first (T0003/T0005/T0008)** — tidy, but none of them block the shadow build and T0003's clock runs to ≈Jul 15 anyway.

[iter-079] Adapter-verification depth? (Decision: 1)
  1. **Validate-only sweep + one tiny real spot fill (~€10 BTC, closed immediately)** — all semantics at zero risk, plus one real submit→fill→reconcile proof; real margin fills wait for the 6b T2 tax-probe. Recommended and chosen.
  2. **Validate-only strictly** — zero money moves, but the first real fill then happens inside tiny-live where a surprise is more expensive.
  3. **Full probe incl. tiny margin orders** — most complete but duplicates the T2 set and moves real margin risk earlier than §12 schedules it.
  [Amended same-day during spec self-review (adversarial verification pass): the Nautilus Kraken adapter 1.230.0 exposes no AddOrder `validate` parameter (verified in the release source — absent from the execution client and the Rust KrakenSpotAddOrderParams), so a validate-only probe cannot exercise the adapter, which is the §7 subject. Substituted a **zero-fill adapter sweep** preserving the ratified zero-cost/no-fill intent: far-from-market resting post-only/limit orders (spot + leveraged long/short) canceled immediately after acknowledgment, plus a crossing post-only expecting Kraken's rejection through the adapter. No fill ⇒ no fee ⇒ no margin opened. The one real ~€10 spot fill stands. Human sign-off on the substitution collected at spec review, and again immediately before any order-placing probe runs.]

[iter-079] Trade-key ceremony timing? (Decision: 1)
  1. **Now, guided in-session** — exact Kraken UI steps + least-privilege permission set + sops placement, verified immediately with a balance read. Recommended and chosen.
  2. **Async before 6a deploys** — from a written checklist; integration deferred to deployment.
  (Fact confirmed by the human in the same exchange: margin trading is already enabled on the account — D3(i) — so margin validate-only probes can run.)

[iter-079] Shadow-engine architecture on Nautilus? (Decision: 1)
  1. **Production TradingNode from day one, REST-fed signals** — Kraken adapter live for orders/positions/reconciliation; strategy timer-driven at daily close, pulling OHLC via the same REST path the research pipeline uses → build_combined_system → intended-order log (shadow) / submission (6b). Signal inputs bit-identical to the backtest ⇒ the 6a concordance gate measures execution fidelity, not bar-aggregation drift; the shadowing node is the trading node. Recommended and chosen.
  2. **Nautilus node, WS-bar-fed signals** — maximal framework usage, but WS bar aggregation diverges from canonical REST OHLC, injecting concordance noise the gate must disentangle from real execution issues.
  3. **Plain scheduler now, Nautilus at 6b** — smallest 6a build, but the shadow evidence covers a component that is replaced before live trading, and the "make Nautilus fully functional" direction is deferred again.

[iter-079] Go-live criteria pre-registered (Decision: ratified — "all good, go ahead" on the presented design; tightened same-day at spec self-review to match master-plan §12 exactly)
  6a hard gate: ≥14 consecutive days, zero missed daily cycles (daily close = 00:00 UTC bar boundary, cycle complete by 00:30 UTC), node target weights vs a replay of the node's journaled input snapshot through build_combined_system |Δweight| ≤ 1e-6 per asset; miss or mismatch resets the clock. 6b band: weekly |live−sim| sleeve return ≤ 10 bps of sleeve NAV; per-trade realized cost ≤ 2× modeled 0.6%/side; zero unreconciled states; ≥3 consecutive weeks; full §12 weekly tracking-error report (fills, fees, rollover accrual, slippage vs cost model) feeding cost-model recalibration. Model consistency (§12's fourth criterion, restored by the review pass): realized 6b performance within the validation CI — a >20%-style divergence ⇒ the backtest is wrong ⇒ return to Phase 5. Ops drills each passed once before go/no-go (kill-switch, WS-loss, restart→reduce-only, VPS reboot). Ramp: each step a D3(vi) human decision — go/no-go admits 25%; each later step requires no gate breaches + DD ladder untouched or correctly handled; >25% requires the §11 tax verification closed (T2 pass OR T3 fallback deployed). Governor enters live at its true carried state (×0.5).
  Also decided at spec review (restored §12 blockers the first draft missed): D3(iii) committed — the 6b sleeve trade plan includes ≥1 margin long + ≥1 margin short, each across ≥2 rollovers, one closed one settled, within the first two 6b weeks; §8 hardening verified on the VPS before the trade key is delivered (iter-081 gate); the workstation IP on the key allowlist is a verification-window-only exception removed once the memo lands; fallback auto-trigger restricted to §7's failure class (order semantics/reconciliation), everything else escalates to the human.

[iter-079] §7 adapter-verification verdict (Decision: PASS — Nautilus confirmed as the Phase-6 engine)
  All six probes passed attended 2026-07-10 (memo docs/research/14.phase6-adapter-verification.md; account funded €100 by the human; order probes run on explicit go): auth/account read; reconciliation empty-or-actual; WS data 10/10 pairs first-tick at 2.0s; zero-fill order semantics ×4 (spot post-only rest+cancel; crossing post-only venue-canceled with no fill; margin long leverage-2 rest+cancel; margin short leverage-2 rest+cancel); the ~€10 real round-trip (fill → reconcile → close → flat; wallet XXBT 0.0000000000 / ZEUR 99.8382; total cost €0.1618); post-run reconciliation clean. NautilusTrader 1.230.0 exact-pinned; thin-custom fallback stays dormant. Build-relevant findings recorded for iter-080 (XBT→BTC normalization; crossing post-only surfaces as accept-then-venue-cancel, not OrderRejected; MARGIN account type reports TradeBalance equity, not wallet balances; adapter nonces outpace ms-nonce sidecars; observed taker fee exactly 0.80%/side → appended to T0014's findings).

[iter-082] Stage-6a concordance gate restated for the 4h deployable (record 44) — the kickoff wording pre-registered daily cycles for record 33. (Decision: 1)
  1. **Ratify the 4h restatement** — ≥14 consecutive days with zero missed 4h cycles (6/day at 00/04/08/12/16/20 UTC), each complete within 30 min of its bar boundary, targets vs journaled-input replay |Δweight| ≤ 1e-6 per asset, miss/mismatch resets the clock. Same 14-day bar, replay mechanism, and tolerance — only the cycle unit changes to the system's actual cadence. Chosen by the human; amended decision-dated in spec 00039.
  2. **Daily-only concordance** — leaves 5 of 6 daily position changes ungated; the A2 sleeve's intraday behavior is exactly what's new.
  3. **Defer to spec review** — blocks the concordance-reporter design.

[iter-082] Build scope: one iteration or two? (Decision: 1)
  1. **Two — builder first**: iter-082 = committed record-44 builder + concordance/replay core (frozen-figure regression tests vs trials 43/44); iter-083 = node/store/journal-writing/CLI. Two clean PRs; verified-math review not entangled with Nautilus plumbing. Chosen.
  2. **One — full local shadow** — fewer ceremonies, bigger mixed review surface.

[iter-082] CLI naming (lands iter-083): (Decision: 1) — `zcrypto engine` sub-app (run/replay/report) per the cli/<name>/ convention; grows into 6b without renames. Option 2 `zcrypto shadow` becomes a misnomer at 6b.

[iter-082] Cycle-compute strategy (~13 min full recompute on the workstation; VPS weaker; 6 cycles/day, 30-min deadline): (Decision: 2)
  1. **Measure first, optimize if needed** — verified path ships alone; vectorize only if the VPS benchmark misses budget.
  2. **Vectorize now** — the fast (polars/numpy) path built in iter-082 alongside the verified path, gated by an elementwise ≤1e-12 equivalence test over the full history; production + replay run the fast path (same-code concordance), the verified path stays the oracle. Chosen by the human.
  3. **Relax the deadline** — a late target is stale for the next bar; structural slippage in 6b the cost model never priced.

[iter-083] Shadow-node iteration decisions (attended, 2026-07-10):
  1. Scope — SINGLE iteration (Decision: 1): store+seeder, cycle core, Nautilus node wrapper + scheduler, `zcrypto engine` CLI, ending with a locally-runnable shadow cycle; contracts pre-pinned by iter-082 make the split's insurance unnecessary. (Option 2, store-first split, rejected as ceremony.)
  2. Shadow NAV for intended orders — €1,000 (Decision: 1), the §12 tiny-live upper bound, configurable; rehearses exactly what 6b submits. (€500 and live-equity options rejected.)
  3. Local soak — YES, immediately at closeout (Decision: 1): systemd user service on the workstation, clearly labeled dev evidence (the gate clock starts on the VPS); doubles as the rsync fallback for the store if deployment slips past the ≈Jul 25 REST window.
  4. Directories (human directive): purge the legacy empty `runs/` placeholder; store_dir = data/engine-store, journal_dir = data/engine-journal — both under data/.gitignore's `*` rule (transactional data is never git-controlled); CLAUDE.md layout line updated accordingly.

[iter-083] Journal retention — explicit DROP for Phase 6 (one-line rationale per the deferral rules): the shadow journal is append-only gate evidence (~0.3 GB/month measured); no pruning/dedup scheme is built or deferred-to-a-topic this phase — disk headroom is an iter-084 VPS sizing input (noted in T0018 at closeout). Revisit only if a phase-7+ operational need emerges.

[iter-084] VPS-deployment decisions (attended, 2026-07-10):
  1. Gate-clock start — DEPLOY NOW (Decision: 1): the VPS runs byte-identical code to the attended first cycle (bit-exact replays); the workstation soak parallels as a comparison instrument; teething defects resetting the streak is the gate doing its job. (Observing 2-3 workstation days first rejected — pushes the whole 6a timeline right for evidence that doesn't count toward the gate.)
  2. Engine dead-man's switch — healthchecks.io check created VIA THE VAULTED API KEY (Decision: 1), ping URL vaulted like capture's; run_cycle pings on completion (plain/success, /fail on sidecar). (Manual creation and skip-alerting rejected; the §10 watchdog wants a page when the node dies quietly.)
  3. Workstation soak — KEEP BOTH until the 6a exit (Decision: 1): two independent nodes on one venue; divergence between their journals would expose venue-side nondeterminism the gate alone can't see. Decommission at the 6a exit review.

[iter-108] How should a verdict treat a realized series with ZERO variance? The live window pinned the governor multiplier at exactly 0.5 on all 54 scored cycles and cap-breach at 0 on all 54, so both new metrics' realized series are constant. (Decision: 1+3)
  1. **A constant realized series is a legitimate verdict** — the realized value is a *window statistic* judged against the null's distribution of window statistics, exactly like gross or turnover, which are also window means. Constancy of the underlying per-bar series has no bearing on whether that statistic falls inside the band. Recommended — and the case where the metric is most informative.
  2. **Treat zero variance as degenerate/`n-a`** — superficially "safe", but it blinds the instrument precisely where it would speak loudest (a realized rate of 1.0 against a null median of 0.27 is the signal, not noise). Rejected.
  3. **Judge it, and disclose the constancy** — the reader needs "multiplier was 0.5 on all 54 cycles (no variance)" to interpret the verdict. Adopted alongside 1.
  `n/a` stays reserved for an *undiscriminating instrument* (zero-width band, tiny effective-n) — never for a constant *observation*. Pinned by a test asserting the constant case yields a real verdict rather than `n/a`.

[iter-108] A null band that spans the metric's entire attainable domain — vacuous "consistent", or `n/a`? Found only by running against the real journal: `governor_engagement` returned live=1.0, band=[0.0, 1.0], width=1.0, verdict "consistent". Nothing can fall outside a full-range band, so the test has zero discriminating power, yet it was still counted in the multiplicity denominator and so *inflated* the panel's reassurance. (Decision: 2)
  1. **Leave it "consistent"** — technically true (the value is in-band) and harmless in isolation. Rejected: on a go/no-go instrument a vacuous pass reads as evidence.
  2. **Mark it `n/a` (undiscriminating)** — `metric_verdict` already escapes a *zero*-width band as a degenerate discriminator; a *full*-width band is the same failure in the opposite direction (under-sensitive rather than over-sensitive). Implemented as an optional `domain` on `metric_verdict`, firing only when the band covers **both** ends, so a one-sided touch (active_frac's real `[0.0074, 1.0]`) keeps its power. Because `n/a` is already excluded from the multiplicity denominator, the panel self-corrects to "0 of 6". Adopted.
  3. **Keep "consistent" plus a low-power note** — relies on a reader weighting prose against a verdict column. Rejected as weaker than 2, though the disclosure is kept *in addition*.
  Recorded as spec `00059` D8 so the rule outlives the code comment.

[iter-108] CORRECTION — the realized governor rate is NOT a divergence from backtest, and the loop's own first reading of it was wrong. Mid-iteration I (and a reviewer) flagged "the deployed system has been continuously throttled at half size while the backtest engages the governor on only ~27% of days" as a likely INCONSISTENT verdict and a genuine OOS divergence. That compared a **window statistic** (the realized 9-day engagement rate, 1.0) against the null's **global mean** (0.266). Judged correctly — against the distribution of same-length 9-day window rates — short windows in the backtest reach 100% engagement often enough that p95 = 1.0, so nine-of-nine engaged days is unremarkable. **Verdict: not a divergence; at this window length the metric cannot discriminate at all** (see the D8 entry above). The economically decisive quantities were checked alongside the metric rather than the metric alone: realized cumulative net -0.1009% over the window, mean -0.000015/cycle, all five weight-derived metrics unchanged and in-band. Recorded as spec `00059` D9: every metric is judged against a same-unit, same-window null distribution, never a scalar summary — this is exactly the comparison error the windowed null exists to prevent, and it was caught before any verdict was registered. No trial was registered and no budget was spent; this iteration builds instrument, not evidence.

[iter-108] Explicit DROP (deferral sweep, one-line rationale): `realized_internals` hardcodes the default `CrossfreqSystemConfig` and the default position caps rather than threading a config, mirroring the pre-existing `_net_live_from_result` pattern. Dropped, not deferred to a topic: the defaults are identical to `CrossfreqSystemConfig`'s (0.20/0.10), no non-default config exists or is planned, and the `reconcile_ok` / `cap_consistent` invariants would VOID the run if the two sides ever diverged — so the failure mode is loud, not silent.

[iter-111] What should the fingerprint table show once every metric carries two verdicts? Spec `00061` D2 said "the primary's numeric columns, **plus one new column** showing the secondary null's verdict label", and that literal reading is what shipped: `verdict` (reconciled) + `secondary` (bootstrap raw). A reviewer enumerating D1's five reconciliation branches — which the live data never does, it only ever exercises one — found the two collide on **three of the five**: with `primary='inconsistent', secondary='weakly-consistent'` the row rendered `weakly-consistent | weakly-consistent`, reading as agreement while suppressing an `inconsistent`, and the primary's own label appeared nowhere in the table. (Decision: 2)
  1. **Keep two columns, add a disclosure line when the reconciled label equals the secondary** — minimal diff, no width cost. Rejected: it makes correctness depend on a reader pairing a prose line with the right table row, and the concealment remains in the artifact itself.
  2. **Three columns — `verdict | primary | secondary`** — the reconciled label (what the panel counts) beside both raw labels. Concealment becomes *structurally* impossible rather than contingent on the two labels happening to differ. Costs ~17 characters of width. Adopted, and D2's wording in spec `00061` was updated to match, with the reason recorded — this refines D2's **intent** (the primary supplies the numbers, the secondary contributes only a verdict) rather than contradicting it, but it is a deviation from the ratified text and is flagged as such for review.
  3. **Show only the two raw labels and drop the reconciled one from the table** — honest, but then the column the panel's multiplicity count is computed from is invisible, trading one concealment for another. Rejected.
  The same shape was then found on the P&L line (`{reconciled} (secondary null: X)`, primary never named) and fixed identically; both sites now share one `_dual_columns` helper so they cannot drift apart.

[iter-111] Should an unrecognized verdict label degrade or abort? `reconcile_verdicts` initially returned `"indeterminate (instrument-fragile)"` for a label outside `metric_verdict`'s closed vocabulary — the **same string** a legitimate opposite-extremes disagreement produces. (Decision: 2)
  1. **Keep degrading** — nothing crashes, the run completes. Rejected: it renders a **code defect** as a **data finding**, indistinguishable to a reader scanning the verdict column, so a typo could survive for many runs looking like genuine instrument-fragility.
  2. **Raise `SoakError`** — the vocabulary is closed by construction, so an unrecognized label is always an internal contract violation, never real-world variety. Adopted. Note the raise does **not** produce a VOID report as first assumed: `soak_report` guards only `realized_series`, so it propagates to the CLI's `EngineError` handler and aborts with a one-line message. That is the better outcome anyway — a void presents as "the instrument could not decide", which is precisely the conflation being prevented, whereas an abort reads as "this build is broken". Verified end-to-end rather than assumed, after the original claim proved false.
  3. **A visibly distinct degrade string** — keeps the run alive while separating the two meanings. Rejected as strictly weaker than 2 given the codebase already reserves `SoakError` for "the instrument is lying".
  A later revision extended this: validation became an **unconditional precondition** rather than a check below the equality and `n/a` short-circuits, because `("probably-fine", "n/a")` was returning `probably-fine` as a rendered verdict — the guard existed and the defect walked around it. This also reversed an earlier call to exempt the equal-label case ("agreement never needs the severity order"): agreement removes the need for the **order**, never for **validity** — two nulls agreeing on a typo is still a typo.

[iter-111] Standing methodological finding, recorded because it changed how this iteration was verified and should change how later ones are. All **eleven** defects found before merge were the instrument **describing** its output falsely, never **miscomputing** it — merged columns, a summary contradicting its own table, a mode-blind JSON note, a disclosure contradicting the row beside it, a docstring promising an error never raised, a README overstating the guarantee. Two originated in claims **I** made and an implementer faithfully wrote into the code. Three were found only by *reading rendered output*; the sharpest only by *enumerating branches the live data never reaches*. Two consequences adopted: **(i) the real-journal run is a sample, not a test** — it exercised one of five reconciliation branches and the bug lived in a direction the data never took; **(ii) "the test fails pre-fix" does not mean the test pins the claim** — mutation testing found that swapping the primary/secondary labels, or attributing a verdict to a construction that never ran, left 128/128 green, because three pins were order-agnostic membership checks (and `"inconsistent"` contains `"consistent"`, so the obvious assertion passes against broken output). Attribution is now pinned positionally, each mutation applied and watched to fail.

[iter-111] Explicit DROP (deferral sweep, one-line rationale): a review found that an invalid `--path` value goes unvalidated inside `soak_report` when the canonical dataset is absent, so the report could print `builder path: bogus` for a build that never ran. Dropped, not deferred to a topic: the CLI validates `--path` against its accepted vocabulary *before* calling in, so the state is unreachable through any real entry point; the accompanying docstring — which promised a `SoakError` that is not in fact raised on that route — was corrected instead, since the false guarantee was the only part that could mislead.

[T0071] Capture-era data-hygiene verdicts (map: `docs/reference/capture-era-data-hygiene-map.md`), decided by the /zcrypto-auto-exec run 2026-07-21. **The T0014/T0024 calibration reads the full capture window (from 2026-07-08 13:00) for both the spread and depth legs** — the informal "post-07-13 only (~9 days)" fallback is superseded, in the admitting direction. Basis: the desync-era archive was never contaminated — the 810/468-vs-100 measurement describes the *in-memory* book only; the archive stores Kraken's wire messages verbatim (`cli/capture/command.py` writes message-level levels; the `OrderBook` only detects), T0008 says so in bold ("No data was ever lost… the archive is sound"), and the panel replays everything through the *fixed* pruning book (482/117/398 → 0 replay; 2,190/2,190 CRC verify-replay). Content caveats are **era-independent**: ranks beyond 10 are venue-unverified in every era (state once if load-bearing); completeness questions go to `continuity.py`/panel gap accounting, never manifests (T0036 truncation is hash-invisible). Era-dependent findings are structural only (launch-day mess, ~200/day heal pocks, the single-host window 07-14 04:00:42 → secondary bring-up). No excision, reader-side or physical. An optional falsification probe (no depth-metric discontinuity expected at 2026-07-14 04:00) is registered in T0014. **Methodological note**: the pre-review draft inherited T0071's own false premise and issued an era-bounded depth EXCISE — the same numbers, read as archive contamination — and was killed by the pre-push review against T0008's bold conclusion and the capture code. The instrument-distrust rule held only because the review re-read the sources instead of trusting the map's citations of them.

[iter-114] Finding recorded because it corrects the topic's premise — and the correction runs opposite to the first draft. `0.006/side` is **not** a fee: spec `00036` and the Phase-5 runbook decompose it as **tier-1 maker 0.40 % × 1.5 — 40 bps of maker fee plus 20 bps of explicit "spread/slippage headroom"**, with the runbook stating outright that [[T0014]] exists to *replace that headroom guess*. Measured, the guess is **5–10× too large**: the real spread is 2.11 bps/side at €1k and 4.15 at €10k. But iter-079's only live fill was **taker at 0.80 %/side** while the convention assumes **maker**, and those readings point opposite ways — maker-executed the true cost is 42–44 bps/side (**×0.70–0.74**, the model over-charges); taker-executed it is 82–84 (**×1.37–1.40**, the model under-charges). **The spread term is not the lever; execution style is** — the whole calibrated spread is small beside the 40 bps maker↔taker delta. No verdict flips at either end (taker end measured at ×1.403 → governed 1.3645). What is unresolved is the *direction* of the record's cost basis, and it stays unresolved until the 6b executor design decides whether to post or cross. Split out as [[T0090]]. (An earlier version of this entry called 0.60 % "a maker-leaning blend the live fill did not exhibit" and quoted the gap as a one-directional understatement — wrong on both counts; the constant is maker + headroom, and the sign depends on an unmade decision.)

[iter-116] How should the app's logs reach Grafana Cloud once the docker-socket path is retired? (Decision: 1 — the owner's, after challenging the alternatives)
  1. **Direct-ship from the CLI** — an opt-in `--ship-logs` sink POSTing to Loki's push API from a daemon thread. No socket, no Alloy in the CLI log path (so it survives Alloy being down, wedged or restarting — the T0089/T0048 neighbourhood), fewest moving parts, and `level` becomes a label at the source so no regex exists in the path at all. Costs accepted knowingly: Loki write credentials in the app container's env (bounded to a `logs:write`-only token), a crash loses the in-flight ring (stdout stays the local ground truth), and the app owns ~150 lines of retry/batching. **Taken.**
  2. **journald log driver for every container, Alloy tails the journal** — zero app code, kills the socket and the wedge class outright. Rejected: it keeps Alloy as the single intermediary in the CLI log path, which was the stated thing to remove, and keeps regex level-extraction alive since journald carries unstructured lines.
  3. **App writes JSON to a host-mounted file, Alloy tails it** — strictly better loss envelope (disk-durable + Alloy's WAL) and credential isolation, both explicitly waived by the owner. Rejected on the file-lifecycle argument: T0089's root cause was two processes coordinating over a rotating file, and this rebuilds that shape with better primitives on hosts where disk is also watermark-sensitive (T0032). **Recorded as the designated fallback** if the creds-in-env decision or the loss envelope ever hardens the other way.

[iter-116] What loss envelope when Grafana Cloud is unreachable? (Decision: 1)
  1. **Bounded in-memory ring, drop oldest and count** — fixed 4096 slots; on recovery one WARNING reports the count. Zero disk, zero unbounded anything, and the literal reading of "logging must never block the application". **Taken** — it is only acceptable because stdout stays the local ground truth (decision above).
  2. **Small capped disk spool (WAL)** — survives process crashes and longer outages, but reintroduces on-host log files with size management on the capture hosts, which is the exact file-lifecycle class T0089 lived in.
  3. **Bounded memory sized for hours** — same shape, tens of MB. Rejected as unnecessary: the ring holds days at capture's line rate, and a bigger burst on recovery is its own risk.

[iter-116] Non-429 4xx: retry or drop? (Decision: 2 — forced by a review finding, not designed up front)
  1. **Retry every failure forever** with the capped backoff, treating all errors alike. Simplest, and what the first draft did.
  2. **Retry transport errors, 5xx and 429; DROP any other 4xx**, counting it and announcing on the next success. **Taken.** After an outage longer than Loki's out-of-order window (~2 h on Grafana Cloud), the held batch's honest timestamps are rejected with 400 on every retry — option 1 wedges shipping permanently and silently, which is precisely the failure class this iteration exists to remove. The drop is bounded and visible; the wedge would not have been.

[iter-117] Container resource metrics: cadvisor, or the app's own endpoint? (Decision: 3)
  1. **cadvisor on every host** — the standard answer, per-container CPU/memory/network for free. Rejected twice over: `docs/plans/00054-ops5-offload.md` records it SIGSEGVing on the NAS (Synology kernel), and it would admit a large container-metrics surface across four hosts against a ≤150-series increment budget.
  2. **cadvisor on the three healthy hosts, skip the NAS** — keeps most of the value, but buys an asymmetric fleet where one host's dashboards silently differ, and still spends most of the series.
  3. **No cadvisor; each daemon serves its own `/metrics`, and Alloy's self-scrape supplies `process_*`.** Taken. The six `process_*` families answer the actual question (is this daemon leaking / wedged / restarting) at 24 series for the whole fleet, and the app endpoint carries domain metrics cadvisor could never produce (cycle success, disk watermark, shipped lines). The container-resource question is not answered for the NAS, and that is accepted, not hidden.

[iter-117] When does the engine get the exporter — with the other daemons, or after the gate? (Decision: 2)
  1. **With the rest, at the same rollout step.** One window, one verification pass, done. But it restarts the live trade engine mid-soak, which is exactly what the Stage-6a gate is accumulating evidence against.
  2. **Last, gated on the 14-clean-day streak.** Taken, and written into the plan as Step 7 with Step 6 explicitly forbidding `engine_image_digest` earlier. Executed 2026-07-26 with the gate measured at `streak_days 15 / status 1 / mismatch_total 0` beforehand. Cost: the engine ran ~4 days without its exporter, and [[T0089]]'s sixth wedged container stayed wedged that whole time.
  3. **Never restart it; wait for a natural restart to pick the change up.** Rejected: nothing schedules one, so it defers indefinitely, and the config would sit rendered-but-unapplied where a later unrelated converge fires it at an unchosen moment.

[iter-117] What is the series budget measured against? (Decision: 2 — a correction made during the closeout)
  1. **Per-host total active series ≤150.** The reading the raw query invites; on that basis the ops node's 308 is a breach and the rollout fails its own bar.
  2. **The increment this spec adds ≤150.** Taken, and it is what D9's table actually says — the column is headed "New series". Measured directly at 135 by counting the three new app jobs plus the newly-admitted Alloy `process_*`, NOT by subtracting a base: 00068 retired the docker-sd metrics, so the old and new sets do not nest and the subtraction would have understated the additions.

[iter-118] How deep should the gross decomposition go — the governor split the topic asked for, or the whole chain? (Decision: 2)
  1. **The governor split only**, which is what [[T0117]] registered: report `(capped_gross, multiplier, final_gross)` per cycle and answer "is the ~5 % gross the vol-target's output, or governor derating?". Cheapest, and it answers the question as written. But the one-cycle probe had already shown the topic's framing to be a false binary — the governor IS at ×0.5 *and* the pre-governor book is 3.30 %, still ~5× under the naive 15–20 % — so this option closes the topic while leaving the actual gap unexplained.
  2. **Attribute the full pipeline** — per-sleeve → combined → capped → governed, with the sleeve-cancellation ratio as a first-class term. Taken (owner, 2026-08-02). **This is the decision that produced the iteration's real finding**: option 1 would have stopped at the multiplier and never looked at the sleeves, so the measurement would have reported "the governor explains 2×, residual unexplained" and closed. Decomposing instead showed the sleeve→combined ratio is 1.000 (at reporting precision; the exact fact is that **B and A1 hold float-exactly zero in all 136 cycles**) — the book runs on one of its three sleeves, which is the mechanism behind both the ~5 % gross and the placeability problem, and is now [[T0124]]. The spec's own hypothesis for that ratio (sleeve *cancellation*) was refuted by the same measurement.

[iter-118] T0124: is a one-sleeve book what rung 3 goes live on? (Decision: 1)
  1. **Proceed knowingly, three conditions attached.** Taken. The reasoning that carried it: the one-sleeve state is a *normal state of the validated system*, not a different system — it covers **38.6 % of the registered validation window** (40.3 % of the extended backtest; the 742 post-registration bars are all one-sleeve), so the registered figures already average over it, and the conditioning is live-observable (sleeve positions are known every bar — a regime filter, not hindsight; the construction is proven registered, reproducing 1.5609 exactly on the registered window). Dormant sleeves cost nothing (zero positions, zero turnover) and re-arm mechanically; the 22-day concordance streak describes exactly this book; and 6b proves execution, not alpha. Conditions: the go/no-go's model-consistency baseline is the **conditional live-state expectation** — governed Sharpe **0.63 extended / 0.75 registered-window** for B∧A1-flat-with-A2-active (a first-cut 0.47 was diluted by fully-flat bars the live book never occupies, standalone Sharpe −4.88), vs 1.94 multi-sleeve — **quoted as a band of roughly 0.5–0.75 with its CI disclosed (IID SE ≈ 0.44, spanning zero), never a point** and never the 1.5609 headline, which would read a perfectly-executing 6b as a ~60–70 % shortfall; the [[T0116]] amendment states the divergence explicitly; the sleeve-occupancy gauge ([[T0018]]'s metrics bullet) lands **before rung 3**.
  2. **Wait for either sleeve to re-arm** — aligns the live window with the headline Sharpe, and nothing else: unbounded (the current stretch is 8.7 months and regime-dependent), couples the execution-proof timeline to the market, and the band/placeability re-derivation is owed at re-arming either way.
  3. **Re-assemble the deployable around A2** — structurally honest, rejected as regime-chasing: dropping sleeves *because* they are currently flat is selecting on recent state, the anti-pattern T0019's fixed weights were adopted against; it would also discard the concordance evidence (the streak is against record 44), force a new record and a shadow re-gate, and spend the free optionality of sleeves that re-arm on their own.

[iter-118] Ratify the §12 Stage-6b amendment — the three-rung ladder, the measured band, the conditional baseline. (Decision: ratified as drafted)
  **This is a recorded plan amendment, not informal drift**: `00.master-plan.md`'s Stage-6b and go/no-go lines are **rewritten in place** (822 → 3,245 chars), because a master plan that carried the original text with a correction appended below would read wrong cold — the uncorrected version reads first and more confidently. The original single-step form is stated as measured-unexecutable rather than deleted silently.
  **What the amendment fixes, each against a number rather than an argument**: the stage as written cannot run (**0 of 801** journaled intended orders clear Kraken's `ordermin` at €500 quarter-size), and its 10 bps weekly band sits **5.1× below the venue-imposed drift floor at the median and 11.6× at p95** for a €1,000 book — it only becomes meetable at ~€5,000/€10,000, the far end of the ramp. The band is therefore re-registered as *the measured floor at the funded NAV*, re-derived by `zcrypto engine accum-replay` rather than fixed as a constant.
  **The three rungs, each proving what the next depends on**: rung 1 = the §11 T2 probe as the plumbing proof (~€10–30) — one rung proving order path, fills, fees, rollover rows, reconciliation AND the tax pipeline, and supplying [[T0090]]'s first measured fills; rung 2 = time-boxed operational confidence with a skip-or-carry policy fixed at entry, and **its tracking error explicitly excluded from the gate** (concentration fixes entry placeability, not delta placeability, so the number describes something adjacent to the deployable); rung 3 = full weights at €1,000 under `delta = target − actually held` ([[T0119]]), gated on the measured band.
  **Two things the gate now reads differently, both from [[T0124]]'s ruling**: performance is judged against the expectation **conditional on the book's realized composition** — quoted as a band with its CI (measured ≈0.5–0.75 in today's one-sleeve state), never as a point and never against the 1.5609 headline, which would read a perfectly-executing 6b as a ~60–70 % shortfall; and the composition divergence is **stated knowingly in §12 itself** rather than inherited silently. Sleeve occupancy becomes a rung-3 precondition.
  **Numbers deliberately kept OUT of §12**: the concrete figures (5.371 % expected gross, 51.2/115.7 bps at €1,000, the 0.5–0.75 band, the full NAV curve) live in [[T0116]] and the resolved [[T0117]]/[[T0118]], because §12 is the north-star narrative and a figure quoted there is quoted for years — while every one of these moves the moment the band is re-derived or the governor lifts.

[iter-119] The pre-registered production DD kill floor — at what drawdown is the deployable retired, not merely de-levered? (Decision: 2)
  1. **18 %** — 1.33× record 44's registered governed maxDD (0.1357), 3 points past the ladder's terminal rung. Rejected as too close to the ladder's own working range: the governor goes flat at 15 % and then re-arms, so a single failed re-arm cycle reaches 18 % — the criterion would retire the system for the ladder working exactly as designed.
  2. **20 %** — 1.47× the registered governed maxDD, 5 points past terminal. Taken (owner, 2026-08-02). Reaching it means the governor already went flat **and** the book lost 5 further points across re-arm cycles, a state the validated system never entered. Clear of the ladder, tight enough to bind before serious capital damage.
  3. **22 % / 25 %** — 1.62× / 1.84×. Both give the flat-then-re-arm cycle more attempts, at 2–10 further points of capital if the system is genuinely dead. 25 % essentially never fires on noise, but a quarter of capital is gone before the pre-registered criterion says anything.
  **The operand is the deployed ladder, not a fresh estimate**: `cli/risk/governor.py`'s `((0.075, 0.5), (0.11, 0.25), (0.15, 0.0))`, where the 0.15 rung is terminal-with-re-arm rather than a stop. The floor is deliberately expressed against *governed* drawdown, the same quantity the ladder acts on.

[iter-119] §12's "N-month underperformance beyond CI vs its own simulation" — what statistic can actually carry it? (Decision: 1)
  1. **The paired per-cycle live-minus-simulation difference**, statistic and window registered now, numeric bound derived from 6b's first weeks of real fills. Taken (owner, 2026-08-02). Both series run the same signal on the same bars, so the common market factor cancels and the residual is execution error alone (fees, slippage, timing) — which is why it is decidable in weeks when the standalone quantity is not. This is also the faithful reading of §12's existing wording, so no amendment is owed.
  2. **Retire on the DD floor alone**, dropping the underperformance leg. Honest about what is measurable but blind to the slow bleed: a system that underperforms its simulation steadily without ever drawing down hard would run indefinitely with no criterion firing.
  3. **Cumulative-path percentile** — kill when cumulative live P&L sits below the simulation's 5th-percentile path for N consecutive weeks. Calibratable from the backtest today and needs no fills, but path-dependent: one early bad week biases the comparison for months.
  **This decision changed the criterion rather than filling in its number, and the reason is measured**: a kill on **live Sharpe** can never fire at 6b horizons. At the [[T0124]] live-state baseline (governed Sharpe ≈ 0.63 extended / 0.75 registered-window), the IID standard error of a Sharpe estimate over `T` years is `sqrt((1 + S²/2)/T)` — **±2.15** as a 95 % half-width after a full year, and **8.2 years** (one-sided) or **11.6 years** (two-sided) to separate 0.63 from zero at all. Even the 1.5609 headline needs 2.5–3.5 years. So the pre-registration hole here was never a missing threshold; it was a statistic that cannot reach significance inside the life of the decision it governs.
  **Consequence for sequencing**: the bound is the one kill-criteria input that cannot be ratified before 6b produces paired fills, so it is registered with its trigger rather than parked as prose. The DD floor above is fully ratified now and is the criterion that must exist before capital scales past the first ramp step.

[iter-119] The live-order adapter settings that are inert in shadow — set them now, or with the executor? (Decision: 1)
  1. **Set what is knowable now; leave the rest to the executor.** Taken. `spot_positions_quote_currency` lands now; the venue open-order / position-discrepancy polling intervals wait, because their values must respect a REST budget that only the executor's own call pattern defines.
  2. **Set all of them now** — rejected: picking polling intervals against an unmeasured request budget invents a number that reaches a live path, and the discrepancy poll is precisely what the go/no-go's "zero unreconciled states" leans on.
  **`default_leverage` is deliberately left UNSET, and that is a ruling, not an omission**: the adapter treats `None` as cash — no leverage sent — and the node already runs `spot_account_type=AccountType.MARGIN`, which enables per-order leverage via `SubmitOrder.params={"leverage": N}`. Setting a global default would silently lever every ordinary rebalance of a book §10 caps at 1.5×/2.0× *gross*; §12 rules the book's gross cap and does not rule a per-order default. Rung 1's T2 probe still gets its margin long and margin short by passing leverage per order.
  **`spot_positions_quote_currency` is `"ZEUR"`, and the near-miss is worth recording because the wrong value fails silently**: the field is compared literally against the instrument's quote-currency `code`. Measured against the real loaded Kraken spot universe — 1,592 instruments — **546 carry code `ZEUR`, 0 carry `EUR`**, and 31 carry `XXBT`. The trap is that the instrument *ID* renders `ADA/EUR.KRAKEN` (a symbol rename) while the quote `Currency` keeps the legacy `Z` prefix, so the plausible `"EUR"` would have matched nothing and reported an empty book — the same failure as the `"USDT"` default it replaces. A first implementation did set `"EUR"`, reasoning from `normalize_currency_code`'s existence without checking whether that path reaches the quote currency; it does not. **Known limitation, recorded not built for**: the field takes a single quote currency, so the EUR book and the `XXBT`-quoted `ETH/BTC` / `SOL/BTC` pairs cannot both be covered by it.

[iter-120] Does the 6b executor post or cross? — the deployable record's cost basis. (Decision: 1)
  1. **Maker-first with a taker fallback.** Taken (owner, 2026-08-03). Post-only inside the spread with a time-box; cross only if still unfilled; the realized blend is **measured in rung 1**, not assumed. Three things carried it, each checkable rather than argued: resting post-only orders are **free until filled** (`kraken-fee-schedule.md`, verified against Kraken's own article), so attempting maker costs nothing but latency; the master plan's own thesis is already maker-first; and decisively, [[T0119]]'s `delta = target − actually held` **is** the mechanism that absorbs an unfilled delta — maker fill risk is precisely the risk that design exists to handle, so choosing maker spends no new risk budget.
  2. **Taker only** — guaranteed fill, simplest executor, no queue logic. Rejected: it pays **~44 % of the conditional baseline** (0.9072 → 0.5066) for executor simplicity, and parks the go/no-go at the floor of [[T0116]]'s registered band with no room left for the statistical uncertainty that band was written to express.
  3. **Maker-only, never cross** — the best number on the board (0.9072 conditional), rejected as unsound: an unfilled delta would never place, turning accumulation from a mechanism into an unbounded journal-vs-venue gap, and rung 3's tracking-error gate would then measure queue luck rather than the deployable.
  **The measurement that made this decidable, and its instrument validation.** Re-quoted on the committed builder after reproducing all three registered anchors exactly (×1.0 → 1.5609, ×1.5 → 1.3029, ×2.0 → 1.2106) — headline **1.6304** maker@€1k / **1.5609** registered / **1.3641** taker@€10k; conditional live-state **0.9072 / 0.7509 / 0.5066**, the conditional arm validated against [[T0124]]'s registered 0.75 and 1.94 on the same 27,337-bar window with the state split nesting exactly (live 6,885 + fully-flat 3,680 = 10,565 = 38.6 %).
  **The non-obvious result: the execution decision matters ~2.7× more for the live book than for the headline** — headline falls 16.3 % across the range while the conditional baseline falls **44.2 %**, because the one-sleeve book runs at ⅙ gross and carries more turnover per unit of return, so cost bites hardest in exactly the state the live book occupies. Reading only the headline curve understates the stake by nearly three times.
  **Recorded so it is not tripped over later**: [[T0116]]'s registered band "roughly 0.5–0.75" was written for *estimation* uncertainty (IID SE ≈ 0.44, CI spanning zero); it coincides almost exactly with the *cost-basis* range from taker to ×1.0. Two unrelated uncertainties, one interval — so the honest quote is two-dimensional, conditional **and** cost-basis-dependent, and the band cannot carry both as written.

[iter-120] The go/no-go band carries two uncertainties — merge them, or state them separately? (Decision: 2)
  1. **Widen the single band to cover both.** One interval, sized to contain the statistical CI and the cost-basis range together. Simplest to write and to read. Rejected: a band wide enough to contain both passes execution it should fail, which is the exact failure mode §12 already warns about for a *stale* band — and it destroys the diagnostic information, because a breach of the merged band says nothing about which term caused it.
  2. **State them as two separate terms.** Taken (owner, 2026-08-03), amended into §12 in place. The band now names (a) the statistical confidence interval of the conditional estimate and (b) the cost basis — which execution style the book actually achieves — with the explicit note that they are **not merged into one interval**. Also recorded: they behave differently, so treating them alike is itself an error — **(b) resolves to a point once rung 1 measures the realized maker/taker blend; (a) never resolves.**
  3. **Leave §12 as written** and carry the collision only in [[T0090]]. Rejected on the standing rule that a ruling recorded only in a topic is invisible at execution time: the go/no-go reads §12, and §12 said "a band with its confidence interval", which names one source while two are in play.
  **What made this necessary is a measured coincidence, not a design preference.** [[T0116]]'s amendment registered the baseline band to express estimation uncertainty (IID SE ≈ 0.44, CI spanning zero). [[T0090]] then measured the conditional expectation across the execution range and found the cost-basis span **numerically almost identical to that band** — two unrelated uncertainties occupying one interval. At the taker end the baseline sits at the band's floor with nothing left for the statistical uncertainty the band was written for, so a gate reading it would have been silently consuming its own margin.
  **The amendment also splits the failure diagnosis, which the single-band form could not express**: divergence attributable to (a) means the backtest or its conditioning is wrong ⇒ return to Phase 5; divergence attributable to (b) is an **execution finding**, which re-prices the basis and may re-open the execution-style decision but does not invalidate the backtest. Both misattributions are costly in opposite directions — sending a sound system back a phase, or ramping capital onto a broken one — which is why the diagnosis is written into the gate rather than left to judgement under drawdown stress.
  **Figures stay OUT of §12**, per the same discipline T0116 set: the concrete numbers move whenever the band is re-derived, and a number in the north-star document is quoted for years. The re-derivation trigger list gains "whenever the realized maker/taker blend moves" alongside the existing governor-lift, sleeve-re-arm and ramp-step triggers.

[iter-121] The deployable has zero out-of-time evidence of edge — accept, or mitigate? (Decision: 1)
  1. **Accept the limitation, and narrow what the gate claims.** Taken (owner, 2026-08-03). §12 now states that passing the go/no-go certifies **execution** — the live book reproduces the intended one, orders place and reconcile, costs and drift sit inside the band, ops paths hold — and explicitly does **not** certify **edge**; it also names both limitations the decision inherits, this one and [[T0125]]'s unrebuildable adoption criterion. The limitation now sits where the decision is taken rather than in a ledger nobody opens at the moment of deciding, which is the gap [[T0064]] was opened to close.
  2. **Accept, leave the gate wording alone.** Smallest change, and the fact is already disclosed in three places — but its *consequence* stays inferable rather than stated, so a future reader sees criteria met and no mention that edge was never validated. That is precisely the failure the topic names.
  3. **Mitigate: ratified holdout budget expansion, a fresh look at trial 44.** Rejected — and **not because it is costly, because it is measured as unavailable.**
  **The measurement that removed option 3.** The only window that exists is 2026-04-01 → 2026-08-03: **124 days = 0.339 years**. A Sharpe estimate over it returns 95 % CIs of **[−3.45, +6.57]** at the registered 1.5609, **[−3.06, +4.56]** at the conditional 0.75, **[−3.05, +4.31]** at 0.63 — and would need **3.5 / 8.8 / 11.6 years** respectively to separate from zero. So a fresh look could not discriminate, would repeat the degenerate-window failure that wasted the first one, and would burn the option for a window that might one day be long enough. Accepting forecloses nothing: the budget expansion stays available.
  **The decision-support read, run at full window before the ruling.** `zcrypto engine soak-check` over the live journal: **L = 140 scored bars / 23.17 days**, self-tests all `ok`, chain consistent; **1 of 7 outside band, 2 of 7 indeterminate**; realized cumulative net **−0.4559 %** (non-gating, near-vacuous at this length). Both disagreements trace to **already-registered conditions rather than new findings** — `governor_engagement` reads inconsistent because the multiplier is 0.5 on all 140 cycles with zero variance ([[T0018]]'s recorded entering state), and `gross`/`net` sit at 0.0501 against a null median of 0.0829 — a level consistent with the one-sleeve book [[T0124]] measured, since a single live sleeve at fixed ⅓ weights carries less gross than the diversified combination the null samples. **The *level* is attributed; the *verdict* is not.** "Indeterminate" is a severity label meaning the two null constructions disagree — instrument fragility — and nothing here demonstrates that composition causes that disagreement. Attributing it would be explaining away a result rather than reading it. The instrument's own closing line governs the reading: these are structural-conformance checks, not evidence of edge.
  **The read had to be repaired before it could be believed, and that is why the ruling waited on it.** Run against the configured store it scored **54 bars ending 2026-07-19**, silently dropping **87 of 141 cycles** — iter-103 retired the workstation soak that kept that store warm, and nothing replaced it. **The verdict genuinely differed**: 0 of 6 outside band on the stale window, where `governor_engagement`'s band spanned all of [0,1] and had no discriminating power at all. Ruling on that would have been ruling on 38 % of the evidence while believing it was current.

[iter-121] soak-check's "regime context" section (spec `00058` report-shape item 9) — define it, or drop it? (Decision: 1)
  1. **Drop item 9 explicitly, on a measurement.** Taken (owner, 2026-08-03). Recorded in spec `00058` itself with the experiment, so the spec no longer reads as owing a section.
  2. **Define it as disclosure-only** — a section stating regime state without conditioning any verdict. Rejected as pure width: `soak-check` *already* prints "realized multiplier was 0.5 on all 140 scored cycles (no variance)" and the same for cap-breach, and sleeve occupancy now has its own gauge and alert ([[T0124]]). A new section would restate them, and on a go/no-go instrument added width is the anti-pattern the topic itself names.
  3. **Defer a third time to a longer window.** Rejected — but on the right leg. The measurement only **brackets** the power threshold in (≈70, 140]; it does not show that the ~90-bar cells a ≥30-day window would give are powerless. The rejection rests instead on the regimes **not alternating**, which window length cannot fix: the governor has been ×0.5 for the entire soak (carried from the 2025 drawdown) and both dormant sleeves have been flat ~9 months, so a split on either leaves **one cell empty at any horizon** — and an empty cell is not a comparison at any L. This topic had already been re-pointed **twice** for triggers that could not fire.
  **The experiment, because any regime split performs it anyway: halve the window.** On the 23.17-day realized window (L = 140 scored bars), split into contiguous halves of L = 71 and L = 68 — both the **same regime** by every system-internal measure (multiplier 0.5 with zero variance, cap-breach 0, one active sleeve throughout):

| metric | full (L=140) | first half (L=71) | second half (L=68) |
| --- | --- | --- | --- |
| `governor_engagement` | **inconsistent** | **n/a — no discriminating power** | **n/a — no discriminating power** |
| `gross` / `net` | indeterminate | **consistent** | **indeterminate** |
| realized cumulative net | −0.4559 % | **+0.0302 %** | **−0.5514 %** |

  **Two findings, and the second is the decisive one.** Conditioning destroys the *only* discrimination the instrument makes — `governor_engagement` is the single metric outside its band at full window, and at half window its null band spans the full [0, 1] on both halves, whichever variable the split is drawn on. And two windows in the **same regime** already disagree with each other, on both the structural verdict and the sign of P&L. So a regime-conditioned section cannot separate a regime effect from sampling variation at any window this instrument will plausibly read; it would present a split like the one above and invite a reader to see structure in noise. The topic's own test — *what would a reader do differently, and does unactionable context read as evidence* — is answered by measurement rather than by argument.

[iter-122] The deployable's ADOPT criterion is unrebuildable — accept the hole, or re-ground the gate? (Decision: 3)
  1. **Accept the limitation as-is.** Record that record 44's adoption criterion cannot be re-examined, alongside [[T0064]]'s, and proceed; §12 already named it. Honest, but it leaves the gate resting on a criterion nobody can re-examine when a reproducible one demonstrably exists underneath it.
  2. **Re-ground on the benchmark-relative basis** — state that the gate rests on record 44's benchmark legs, which are reproducible, rather than on the incumbent comparison, which is not. Strictly better than (1), and needs no registry edit: the append-only record stands exactly as written; what changes is which basis the gate reads.
  3. **Re-ground AND re-derive the remaining legs**, so the whole basis is measured rather than partly registry-asserted. Taken (owner, 2026-08-03), and completed the same day.
  **What made a real decision out of what looked like a one-option ruling.** The topic framed the remainder as a bare acceptance. But record 44 registers **benchmark-relative** legs — SPA, DSR, worst-slice-relative, cost-stress — that are independent of trial 43 entirely. The 4h benchmark reproduced **exactly** (1.2128 full / 1.2447 decisive, union 27,338 bars), so a second and reproducible basis existed underneath the unrebuildable one, and accepting a hole would have discarded it.
  **Every leg re-derived, instrument validated first**: `spa_p_full` 0.001999000499750125, `spa_p_decisive` 0.004497751124437781 and all five `spa_grid_*` cells **bit-identical**; `var_trials_4h` 1.3058096692086857e-05 and `per_period_sharpe_4h` 0.03335455562889141 **bit-identical**; `dsr` 0.9999994822040257 against a registry that stored it rounded to 1.0; `worst_slice_relative_pass` 1; `cap_breach_bars` 1318 and `governor_engaged_bars` 7302 exact; margin over the reproduced benchmark **+0.3481 full / +0.3136 decisive**.
  **Why this re-derivation is legitimate where rebuilding trial 43 would not be** — the distinction is the substance of the ruling, not a caveat on it. Every primitive here is **committed**, and the benchmark's construction was **specified in advance** in the phase-4 history: a first attempt using the wrong construction missed by 0.06 Sharpe and was **discarded rather than adjusted**. Trial 43 has neither property — no committed code and no specification to reproduce against — so any rebuild of it could only be tuned until it matched, which proves nothing. Reconstruction of trial 43 therefore stays explicitly rejected.
  **The re-derivation is committed** (`cli/portfolio/record44_legs.py` + tests) rather than left in a scratchpad, since this topic exists precisely because a scratchpad vanished. Two details spec `00038` never pinned were recovered and written there: the SPA headline cell is `(mean_block 30, seed 42)` — whose full and decisive readings became `spa_p_full`/`spa_p_decisive`, which is why no `spa_grid_b30_s42` key exists — and the worst-slice test runs on the governed net over full history with the year taken from each bar's close stamp. Both were proved discriminating by mutation rather than assumed.

  **A near-miss caused by this closeout, and the operating rule it produced.** The two recovered conventions were first appended to spec `00038`. Review caught that registry records 43 and 44 store `spec_hash a25d7102…`, which is the **sha256 of that spec file** — the append changed it to `914c9fc4…`, silently breaking the pin that verifies the ratified record, inside a change whose whole subject is that record's verifiability. Reverted, hash restored, conventions moved into the committed re-derivation module. **A spec named by a registry `spec_hash` is immutable**; a recovered convention's durable home is runnable code, not the pinned document. Also corrected in the same pass: the re-derivation covers the benchmark-relative legs only — `cost_stress_*` and the drawdown figures stay covered by the frozen-figure regression at its own tolerance, which the module states about itself and §12 had blurred.
  **What stays lost, and the ruling does not pretend otherwise:** the counterfactual. Nobody can re-read the 43-vs-44 ordering, including at the taker end where the registry itself discloses that the ordering reverses at ×2.0. Re-grounding does not recover that — it makes the gate not depend on it.

[iter-124] T0111 wave-1 scope — how much mechanized converge discipline lands in one iteration? (Decision: 1)
  1. **Wave 1 + both M-asserts + `mutate-probe.sh`.** Taken (owner, 2026-08-03). The nine wave-1 role asserts, the two M-sized asserts the topic had parked for a design pass (canary digest-parity, fleet-pins recording), and the mutation-probe script from the `agent-ops.md` fold-in — one spec, one branch, each guard TDD'd against its constructed violation. Recommended: the M-asserts share all their probe machinery with the wave-1 asserts, so deferring them would rebuild the same delegated-read scaffolding twice.
  2. **Wave 1 role asserts only.** Smallest reviewable unit, but the two M-asserts guard the two worst recorded incidents (un-baked primary re-pin, the 2026-07-31 armed wrong-pin), and splitting them off leaves the heaviest hazards prose-guarded for another iteration.
  3. **Wave 1 + wave-2 scripts in one pass.** Rejected — the wave-2 scripts (`converge.sh`, `zcrypto-panel-regenerate`) need ops-host drills in a maintenance window, an attended tail that would park the whole branch.

[iter-124] Override semantics for guards that must stay bypassable in an emergency (spec 00082 D1) (Decision: 2)
  1. **Boolean flag** (`-e x_override=true`). Familiar, but records nothing: the play log shows only that someone bypassed the guard, and the post-incident review starts from zero.
  2. **Reason-required free text.** Taken (owner, 2026-08-03): `-e <name>_override="<reason>"`, refused when empty, boolean-like (`true`/`false`/`1`/`yes`), or shorter than 9 characters; the accepted reason is echoed into the play log so the bypass and its justification land in the same artifact. Applied uniformly: the canary-parity override (`canary_override`) and the fleet-pins assert (`pins_override`, controller-tree read) both carry the same convention — the two per-question acks in the same ruling.
  3. **No override.** Strictest, rejected: a genuine emergency (secondary host dead, primary needs a rollback re-pin) must not require editing committed YAML to proceed.

[iter-124] agent-ops fold-ins — which mechanizable session lessons ride wave 1? (Decision: 2)
  1. **All three candidates** (mutate-probe script, `git mv` PostToolUse hook, network-timeout PreToolUse hook).
  2. **Mutate-probe + the `git mv` hook; drop the timeout hook.** Taken (owner, 2026-08-03): the four mutation bullets collapse into `infra/scripts/mutate-probe.sh` (the script refuses what the prose warned about), and the `git mv` RM-trap gets a PostToolUse warning hook — both are observed-failure-backed. The network-timeout hook was dropped: wrapping every network command in a hook duplicates what the timeout-prefix rule already achieves at lower complexity, and a hook that rewrites commands is a heavier failure domain than the failure it prevents.
  3. **Mutate-probe only.** The hook candidates are Claude-harness config rather than repo tooling — rejected because the `git mv` trap bit twice on 2026-08-03 and repo-side pre-commit structurally cannot see it (the framework stashes unstaged changes before hooks run).

[iter-125] T0111 wave-2 scope — which of the eleven registered items land in this iteration? (Decision: 1)
  1. **All eleven.** Taken (owner, 2026-08-03). The five wave-2 scripts, the three guard gaps wave 1 left registered — the engine parity mirror, surfaced by its final whole-branch review, plus the window-floor journal probe and the skip-tags tightening, both recorded during its execution — and the three polish items that same final review raised (mutate-probe hermeticity, liquidations rc split, git-mv hook coverage) — one branch, fully draining T0111's registered backlog so only the attended drills remain. Largest option, but every item was already registered with its mechanism described, so the marginal design cost was near zero.
  2. **Scripts + guard gaps only.** Parks the polish items — but they guard the tooling the fleet's own verification rides on, and each was XS/S.
  3. **Scripts only.** Smallest; leaves the engine parity gap (live trade path, prose-only gate) open longer — rejected on that alone.
  A scope correction was measured before design rather than discovered mid-build: the registered genesis-annotation item was already shipped (spec-00079-era `genesis_skipped` + row mark), so wave 2 books it instead of rebuilding it — ten build items, one record.

[iter-125] converge.sh's gate between the displayed preview and the real pass? (Decision: 1)
  1. **Typed-limit confirm.** Taken (owner, 2026-08-03): after the `--check --diff` preview, the operator types the `--limit` value back (byte-exact, `/dev/tty` only — a pipe cannot drive it); anything else aborts with nothing executed. The confirmation names the blast radius, which is the thing a reflexive keystroke skips.
  2. **y/N confirm.** Lighter, but a reflexive `y` confirms without re-reading which host is about to converge.
  3. **`--execute` flag, no prompt.** Scriptable, but nothing forces the operator to read the diff between the two invocations.

[iter-125] Does converge.sh become the documented converge path immediately, or land as an optional wrapper until drilled? (Decision: 1)
  1. **Documented path + rule shrink now.** Taken (owner, 2026-08-03): the `--limit`/preview rule lines shrink to pointers at the script in the same PR (per-edit protected-set sign-offs), and the attended drill validates the script before the next real converge relies on it.
  2. **Optional wrapper until the drill.** Safer sequencing on paper, but the rule keeps carrying lines a mechanism now covers — the drift T0111 exists to kill; rejected.

[iter-127] How should the fleet's dashboards be split, now that the telemetry layer is complete? (Decision: 1, amended)
  1. **Split by audience — fleet health vs data integrity.** Taken (owner, 2026-08-04), *plus a third board the owner added*: the engine's families are neither host health nor pipeline integrity. Each board fits a screen and matches how they are actually read.
  2. **One board, more rows.** One link to remember and the simplest push, but ~10 rows of scroll once the uncovered subsystems land.
  3. **One board per host.** Good for "what is wrong with the secondary", bad for "is any host degrading" — the more common read.

[iter-127] What bounds the new panels, given ~50 uncharted families? (Decision: 2)
  1. **Every alerting family gets a graph**, plus one liveness panel per subsystem. Bounded and testable.
  2. **That, plus every app-level `zcrypto_*` family** whether or not it alerts. Taken (owner): a fuller operational picture, at the cost of more panels to maintain. The owner also sharpened the invariant — coverage is *family*-level, never rule-level (two rules on one family are served by one timeseries showing both thresholds), and the complement is that **a firing alert must point at the panel that shows it**, which became `__dashboardUid__`/`__panelId__` on all 63 rules.
  3. **Curated per subsystem, no mechanical rule.** Leanest, but no invariant a test can enforce, so drift returns silently.

[iter-127] The engine board was to be titled "engine — intent", since no executed order/position/PnL family exists. Keep the qualifier? (Decision: 2)
  1. **Title the board for intent**, matching what it can show today.
  2. **Title it `engine`; the intent mark lives at panel and `/metrics` level.** Taken (owner, 2026-08-04). The distinction is a property of the *metric*, not of the board, and it is already carried at `/metrics` (`zcrypto_engine_orders_total` is documented "Intended orders emitted"). The consequence is the point: [[T0018]]'s executed families join **this same board** without a rename.

[iter-127] A newly surfaced defect whose fix is in application code — park it as a topic, or fix it in the iteration that found it? (Decision: 2)
  1. **Register a topic with a `ripe_when`.** Keeps the iteration's scope bounded.
  2. **Fix it now, accepting the extra converge.** Taken (owner, 2026-08-04), stated as a standing rule rather than a one-off: *"It is better than putting the trigger on an unarmed activity. My memory isn't that reliable."* A `ripe_when` on an activity nobody has scheduled is an unarmed trigger, so the topic silently becomes permanent; a converge is a known, bounded cost. `T0127` (two engine gauge-lifecycle defects) was deleted and folded in under this ruling.

[iter-127] Three alerting defects surfaced mid-iteration, two on the unbackfillable path. Fold in, or defer? (Decision: 1 for all three)
  1. **Fix all three now.** Taken (owner, 2026-08-05): the watermark rule's underived `for: 2m` → `0s` (on a boolean gauge checked every 30 s and scraped every 60 s, the pending period added ≥2 min of guaranteed further discard and blinded it to breaches under ~3 min); the missing precondition in `capture-deploys.md`'s restart-window bullet, which is where a false `for: 35m` premise came from; and an engine ERROR-log rule, since `container="engine"` was matched by no rule and an engine ERROR paged nobody on the host holding the live trade key.
  2. **Defer the two alert changes to a dedicated alerting iteration.** Bounded scope, at the cost of leaving a known-blind page on the unbackfillable path.
  3. **Fix only the code-side one.** Splits a coherent set on an arbitrary line.

[iter-127] The engine ERROR rule is presence-fired, so a regressed ship path reads as a healthy engine. Add its dead-man canary now? (Decision: 2)
  1. **Add the rule and a canary together**, closing the gap symmetrically.
  2. **Ship the rule; register the canary.** Taken (owner, 2026-08-05) → [[T0128]]. The engine's log cadence is bimodal around its 4-hourly cycle, so a window borrowed from capture pages on every healthy gap — the threshold needs measuring before it can be trusted, which is a real precondition rather than a scheduling excuse.

[iter-129] Does today's first real venue event settle the T0105 pre-drain decision, and how? (Decision: 1)
  1. **Drop the pre-drain; the executor checks venue state at cycle entry (T0018)** — the deciding number the topic named was the `effective_time` lead, and the first real maintenance event delivered `None` on all five transitions on both hosts: zero advance notice, so there is nothing to key a *pre*-drain on, and the ~60 s status-line lead is naturally consumed at the executor's own cycle entry rather than by a standing drain mechanism. Recommended and chosen — building against the unobserved planned-maintenance shape is the mechanism-nobody-proves-runs pattern the topic itself documents.
  2. **Drop the pre-drain; register a separate successor topic for the cycle-time check** — same ruling, one more topic to track; rejected in favour of keeping the executor's venue interaction in one place (T0018 owns the order path).
  3. **Re-defer on a sharper trigger (a PLANNED maintenance with a real `effectiveTime`)** — keeps the topic open indefinitely against an event that may never come, for a number that would at most resurrect a mechanism option 1 already covers at the only point it can act.

[iter-131] Where should the recalibration window END, given the two legs' book began 2026-07-23? (Decision: 2)
  1. **The window drafted in the plan — through 2026-08-06T06:00Z.** 328 h / 13.67 days, and uncontaminated: it stops an hour before Kraken's first observed venue maintenance (2026-08-06 07:01–07:18Z), so no frozen-book seconds enter the means. Rejected — it falls below Phase 2's "≥2 weeks of captured spreads" exit-bar row, which T0091 discharged at 14.68 days and a 13.1-day predecessor explicitly failed. Nothing in the suite checks span, so this would have un-discharged a stated gate with every test green.
  2. **Through the last settled hour common to all twelve pairs — 2026-08-07T19:00Z.** 365 h / 15.21 days, which keeps the exit bar discharged and is what D4's own wording ("the last settled hour available at execution time") actually asks for once resolved at execution rather than at planning. Costs: the window now contains the maintenance, so the ~1,020 frozen-book seconds must be disclosed. **Chosen (owner, 2026-08-08).** The trade is asymmetric — a 17-minute interval is 0.078 % of the window (an order of magnitude under the 0.602 % the superseded table already carried) and is disclosable in the calibration doc, whereas a regressed exit-bar criterion is invisible and unchecked. Coverage over the longer window also turned out to be exact: `min_rows == max_rows == 365 × 3600`, zero missing seconds.
  3. **Two windows — keep the EUR rows on their old stamp, give the `/BTC` legs their own.** Preserves the exact numbers T0024's measured effect cites. Rejected at spec time (D4) and not reopened: it turns three module constants into per-symbol structures and leaves two windows to explain forever.

[iter-131] The restamp moved the EUR rows far more than the spec predicted — is that a defect or the data? (Decision: 2)
  1. **Treat it as a pipeline defect and investigate before restamping.** The spec estimated "under 2 %" from T0091's precedent; the measurement is 9 of 10 pairs >2 %, worst −25.01 % (DOT @1k). A move that size is a reasonable trigger for suspicion.
  2. **Attribute it to the window, on evidence, and restamp.** **Chosen.** Two independent signals: the drift is *mixed-direction* (BTC −22.5 %, ETH −16.9 %, XRP −15.3 % tightened while ADA +9.2 %, LINK +7.9 % widened), which a systematic computation error would not produce; and re-running the committed script over the OLD window against the REGENERATED tree reproduces the OLD table exactly, so the ladder change did not touch EUR semantics. The spec's estimate is corrected in place rather than left to be read as fact.
  3. **Restamp but average the two windows to damp the move.** Rejected — it invents a provenance that matches no captured interval, and the recalibration discipline is that a table names one window it was measured over.

  **Standing consequence, recorded because it outlives this iteration:** these constants are less stable across adjacent windows than T0091's precedent suggested, so downstream use should treat a single window's calibration as a point estimate rather than a converged parameter. The reproduction-control test is the permanent instrument for re-checking that attribution at the next restamp.

[iter-133] What should a trial record's dataset reference actually BE? (Decision: 3)

1. **What a manifest declares** — read the dataset's manifest and record the declared slice, set digest and extent. Cheapest to state; it is also what two designs tried, and both died on the same fact: the manifest ecosystem has no contract (five writers, four `series` shapes, two digest spellings, a per-run nonce, a machine-local path, a holdout with no per-series hashes at all), so every fix for one writer's quirk tripped another's normal output. Nine cold-review rounds, 10→6→6→5 then 13→8→6→4→5.
1. **A committed pin per frozen dataset** — mint an identity file into `docs/reference/` and have records reference that, so provenance is verifiable from a bare checkout. Genuinely attractive: it is the only shape where CI can check a record's *content* at the PR that adds it. Rejected as the largest never-reviewed delta in exactly the surface that bred findings, and because a pin is still a projection of a manifest — a lying writer produces a faithfully-committed wrong pin, so it buys a verification venue rather than new truth.
1. **What the run actually read.** **Chosen.** A capturing loader hashes each file's bytes as it opens them and records the rows and span it returned. No manifest is parsed in the identity path, so the wall behind the nine rounds is unreachable rather than better-handled; a new dataset backing a trial needs zero provenance code; the holdout's externally-produced opaque digest — six candidate recipes fail to reproduce it, so a byte-identical re-pull may move it — never enters a record's identity; and a windowed fit moves rows/span honestly, which the declared-slice shapes could not express.

  **Costs taken deliberately, recorded so nobody re-litigates them cheaply:** it discards an implemented, five-rounds-reviewed adapter module (158 + 168 unmerged lines); byte identity is file-grain, so a re-encoded parquet with identical logical content reads as a different set (correct under §8's immutability rule, but sibling re-freezes never compare equal by digest); and reads that bypass the loader are invisible, so "provenance at the point of consumption" is honest only for loads through it.

[iter-133] Should the round also build the committed research command, or leave it to its own round? (Decision: 2)

1. **Registry-side floor only, command later** — smallest diff, fastest to land, and the topic did list the command as its own design-bearing round. But it leaves the enforcement guarding `append()`, which has no production caller, and leaves the next record still written by a scratchpad driver: the precise mechanism that lost 44 hashes.
1. **Both, in one round.** **Chosen** — the owner's ruling: solve the round whole, no new topics forked. `zcrypto research eval` becomes the first production caller of `append()` in the project's history, and registration requires a committed subject, which turns the discipline record 44 already followed into the paved path. The two halves stayed separable in review (a loader library and a CLI) without being deferred.

[iter-133] What to do about the two dataset hashes that resist recovery? (Decision: 1)

1. **Accept them as unverifiable and say so wherever they are cited.** **Chosen.** `ba47e37e` and `81dc9b44` resisted ~226,000 candidate recipes and no driver was ever committed, so further reconstruction spends effort against a closed set. The ruling is executed as `docs/reference/legacy-dataset-pins.jsonl` rather than narrated in prose, with the epistemics in the referent *value* so a careless grep cannot return a bare path that reads as verified fact.
1. **Spend effort reconstructing the iter-046 driver** — rejected: the registry is append-only and hash-chained so nothing could be restamped anyway, and what is lost is the independent cross-check, not the dataset's identity, which passing extent tests already pin.

  **Recorded because it is the honest limit:** identification by extent is not verification by digest. Record 44's dataset is known; its hash simply can no longer be recomputed to catch a case where the data changed but the extent did not.

[iter-134] What proves a captured trade day is ready to publish as bars? (Decision: 2)

1. **A wall clock.** Publish once `now - day_end >= TAPE_SETTLE`, with the constant derived from the healer's nominal cadence — `SETTLE_HOURS = 2` defers each day's hour 23, the once-daily backfill heals it the following run, so a day is heal-complete ≈24.2 h after it closes and 26 h buys margin. Cheap, no reads, and the panel's precedent. Rejected under cold review: it is a **proxy** for "the backfill has run", and the healer's *designed* delay modes exceed the margin by an order of magnitude — the fail-closed NAS gate skips a whole cycle, the UTC-day stamp is written *before* the run so one failure costs the day's only attempt, and a leftover container after a dockerd crash extends an outage by up to 24 h. The dataset has no rewrite path, so a proxy failure is silent, permanent, and indistinguishable from a quiet market.
1. **The bytes.** **Chosen.** `cli/trades/gaps.py::detect` proves `trade_id` contiguity directly — Kraken's ids are dense and per-pair monotone, so a hole *is* missing data, provable with no REST call. The clock survives only as a cheap pre-filter and is no longer load-bearing, so its drift is no longer dangerous. Costs one extra read of the day plus a neighbouring segment each side; buys a gate that cannot pass on un-healed data whatever the healer's schedule did.
1. **Ledger-driven invalidation** — record every healed pair-hour and re-derive affected days. Rejected: it keeps freshness nobody wants and adds the rewrite path the daily-final grain exists to avoid.

  **Consequence recorded because it outlives the iteration:** the same reasoning retired a second premise. A rule refusing any day missing one of its 24 hours *looks* like completeness and is not — the capture writer commits no final for an hour with no events, and zero-print trades hours are production-measured, so that rule would have made every day containing a quiet hour permanently unpublishable. Contiguity distinguishes a missing hour from a quiet one; file presence never could.

[iter-134] What grid does the tape-bars dataset publish, and how do coarser grids exist? (Decision: 1)

1. **A 15m base with 60/240/1440 derived on demand.** **Chosen** — 15m divides all three evenly, and the derivation is exact rather than approximate because `ticks_to_bars` computes a true tick-weighted vwap, so the sub-bar weighted sum telescopes. One materialized grid means cross-grid disagreement cannot exist, and the derivation is a pure function of the base.
1. **Materialize each grid independently from the tape** — four times the work, and it makes cross-grid disagreement *possible*, which is a class of bug the chosen shape cannot have.
1. **Publish only the finest grid and leave aggregation to consumers** — rejected as the same thing with the exactness argument undocumented, which is how a consumer reaches for the mean-of-vwaps form that is wrong on non-uniform volume.

[iter-134] Does tape-bars owe a NAS replica? (Decision: 2)

1. **Build a replication channel**, matching the other accruing operational members (ops-primary + NAS replica), each of which is pulled through its own rrsync forced-command key.
1. **Ops-host only.** **Chosen**, and the spec line claiming a replica is corrected rather than the channel built: `tape-bars` is **fully derived**, and its source — the captured trade archive — is already NAS custody kept forever. A lost ops copy costs a re-materialization, not data. The other accruing members are replicated because they are *primary* observations that cannot be rebuilt; this one is not, and replicating a rebuildable set would buy storage cost and a new key for no durability.

[iter-135] The 6b executor build: one iteration, a vertical slice straight to the venue, or five risk-first specs? (Decision: 3)
  1. **One iteration.** Smallest ceremony. Rejected: the pieces have genuinely different evidence regimes — provable offline, provable read-only against the venue, provable only by spending money, provable only after fills exist, provable only after weeks of live operation — and folding them together would put this project's first `submit_order` into the same reviewable diff as a reporting tool.
  2. **A vertical slice cutting straight to the order path.** Reaches the venue soonest. Rejected: it arms a host that already holds the trade key before any revoke path exists. The finding that set the whole ordering: the deployed engine is already an authenticated trading client — `exec_enabled = true`, the trade key mounted, `KrakenLiveExecClientFactory` registered, `LiveExecEngineConfig(reconciliation=True)` — with zero submission call sites anywhere in `cli/` or `tests/`, so nothing structural distinguishes "half-built" from "live" until containment lands first.
  3. **Five specs, risk-first, containment before capability.** **Taken** (owner, 2026-08-11): `00088` (this one, no blast radius, shadow, submits nothing) → `00089` (venue truth, read-only) → `00090` (rung-1 order path, real money ~€10–30) → `00091` (tracking-error report + recalibration, read-only) → `00092` (rung-3 accumulation, real money ~€1,000), with both monetary specs sitting behind machinery proven while a mistake still cost nothing. Registered in [[T0018]] rather than only in the deciding spec, since a decomposition living only in a spec is invisible at pick time.

[iter-135] How does the engine get armed for a live submission? (Decision: 3)
  1. **Config-only** (`exec_armed` in `zcrypto.toml`). Slow and recorded, but disarming would then also need a converge window, leaving a kill file as the sole fast revoke — and the resting state after any probe would be "armed, stopped only by a kill file", undoable by anyone tidying up.
  2. **File-only** (a host-side arm file, no config key). Fast to create and remove, but nothing in git or the deploy record gates arming, so one stray or restored file would arm a host that already holds the live trade key with no recorded act behind it.
  3. **Two independent keys, ANDed, plus a third independent kill switch that overrides both.** **Taken** (owner, 2026-08-11): `exec_armed: bool = False` in `EngineConfig`, rendered explicitly so a converge diff always shows it, AND a host-side arm file the operator creates/deletes in seconds with no converge; the kill file is independent of both and, present, forces `none` regardless. Arming needs a slow recorded act and a fast human one; disarming needs only deleting a file. Stated as a known limitation rather than left implicit: the two-key claim degrades to one key between a probe and the next converge (config stays true, only the file was removed), and the arm file lives inside the state directory that is also the backup/rsync unit — both consequences registered as `00090` probe-checklist items rather than solved here.

[iter-135] Where do execution outcomes get recorded, given the Stage-6a concordance streak (30 days clean measured 2026-08-11, bar is 14) must never see a deliberate refusal to trade as a broken research day? (Decision: 3)
  1. **Reuse `failed-cycle-*` sidecars.** Smallest change, but it would reset the streak on every correct venue deferral, conflating "we deliberately did not trade" with "the cycle malfunctioned".
  2. **Extend `CycleRecord` with an execution section at schema v2.** One artifact instead of two, but the snapshot content hash's byte layout is pinned *by* `schema_version`, so replay, compare and `evaluate_gate` would all have to straddle two versions — putting the machinery the whole streak rests on under the knife for a cosmetic gain.
  3. **A separate per-cycle ledger** (`exec-<HH>.json`, own `schema_version`), filename deliberately disjoint from `cycle-*.json`/`failed-cycle-*.json` so the concordance glob never sees it. **Taken** (owner, 2026-08-11): `cycle-HH.json` — schema, validation, no-peek invariants, hash layout — stays untouched. Written unconditionally rather than only when the metrics port is set, since a manual `engine run` producing no forensic record of a refusal would be worthless, and `00090` extends this same artifact with the orders actually submitted. Pinned directly: a day whose every execution artifact records a refusal still scores clean under `evaluate_gate`.

[iter-135] Where does venue status come from, evaluated at process start and after each cycle completes? (Decision: 3)
  1. **Read the capture daemon's own continuous WS observation over a local transport.** Reuses a proven observer on the same host and adds no external call, but needs a new transport and makes the engine's ability to trade depend on an unrelated process's liveness.
  2. **The Nautilus adapter's per-instrument status.** Verified absent in the installed adapter: `nautilus_trader/adapters/kraken/` carries only per-instrument `InstrumentStatus`/`MarketStatusAction`, no system-wide field — a grep for `systemStatus|system_status|SystemStatus` across the adapter returns nothing — so it answers a narrower question, on a poll interval we do not control, and would not see a venue-wide halt.
  3. **Kraken's public REST `SystemStatus` endpoint at process start and after each cycle completes, snapshot bounded at 30 s.** **Taken** (owner, 2026-08-11): follows the existing public-REST pattern (`cli/ohlc/fetch.py`, `cli/trades/rest.py`), no API key; anything but `online` — and any error, timeout or unparseable payload — refuses. The 30 s bound is reasoned from the one observed real maintenance transition (2026-08-06, `effective_time=None` on every transition, ~60 s of status-line lead ahead of feed silence) and also caps REST calls at ~2/min regardless of how many orders a cycle submits.

[iter-135] What clears the restart-triggered reduce-only hold? (Decision: 3)
  1. **Auto-clear once reconciliation agrees.** The smoother operational answer, but neither the venue read nor the realized-state journal exists until `00089` — it would ship as untested prose in a spec whose whole purpose is provable containment.
  2. **Clear after N clean cycles.** Buildable today, but it measures the research path's health, when what is actually in doubt after a restart is the engine's view of the position book — an unrelated signal.
  3. **Latch unconditionally at every start; only a human clears it.** **Taken** (owner, 2026-08-11): restarts are routine (converges, the supervision watchdog's `os._exit(1)`, the 02:25 reboot slot), and after any of them the engine's belief about what it holds is exactly what has not been re-established — latching is the honest response. Ruled as a floor, not a final state: a later spec may *narrow* when it clears (e.g., "and reconciliation agrees" as a further precondition), never widen it by removing the human act.

[iter-138] Should the traded basket track the refreshed universe live, or stay frozen as code with the divergence made observable? (Decision: 2)
  1. **Rebuild the executor to match the refreshed universe** — drop DOT, solve the multi-quote margin problem for ETH/BTC and SOL/BTC, and let a universe refresh flow straight into what the engine trades. Keeps basket and universe in permanent lockstep, but changes what a live trade-key host does on every future universe regeneration — the opposite of `00088`'s containment discipline — and it would have absorbed [[T0137]]'s still-open re-ratification call as an unreviewed side effect of a spec that is supposed to be read-only.
  2. **Freeze the basket as record 44's ten EUR legs (a code constant); add concordance that makes every divergence observable instead of silent.** **Taken** (owner, 2026-08-13). A data refresh must not change what the live engine trades on its own — the basket was ratified as part of the deployable, and the 2026-08-13 universe refresh (iter-137) already proved the isolation held by construction. What was missing was the *observation*, not a mechanism to auto-follow the universe: a repo-side test and a runtime check now judge the basket against a ruled-exceptions baseline (DOT `traded-but-deselected`; ETH/BTC + SOL/BTC `selected-but-unreachable`, both owned by [[T0137]]), so a future shift turns the test red instead of arriving silently.
  Whether the basket ever actually changes is [[T0137]]'s decision, not this spec's — see the D9 entry below.

[iter-138] How does venue truth cross into the cycle? (Decision: 1)
  1. **A frozen snapshot, taken once at the boundary and passed through a new injectable `venue_state` seam.** **Taken** (owner, 2026-08-13). The strategy reads the Cache once per cycle and freezes it into a plain `VenueState`; `run_cycle` itself never touches Nautilus. The one moment of staleness is bounded by a cycle's duration and is treated as a feature, not a defect — every number in the artifact was true at one instant the artifact names.
  2. **Pass the live Cache/strategy handle into `run_cycle`.** Rejected: it drags Nautilus into `run_cycle`'s test surface and lets the cycle read live mutable state mid-computation — exactly the coupling the frozen-snapshot seam exists to avoid.
  3. **An independent REST read inside the cycle.** Rejected: reconciliation already maintains one continuous source of venue truth in the Cache; a second, independently-polled source that can disagree with it is the disease this spec exists to treat, not a second opinion worth having.

[iter-138] Where does costmin come from, once the probe shows the installed adapter never maps it? (Decision: 1 — forced by measurement, not designed up front)
  1. **A committed per-base constant** (`COSTMIN_EUR`, derived from the refdata snapshot), carried in `InstrumentConstraints` alongside the three live Cache values, labelled `costmin_source: snapshot-constant` in the artifact, and pinned by a repo-side drift test (`tests/test_costmin_drift.py`) against `load_minimums` over the newest snapshot. **Taken** (owner, 2026-08-13) — the only option compatible with both measured facts: the adapter's `min_notional` is always `None` (loopback-probed through the compiled parse path, not guessed), and the engine host mounts no refdata snapshot to read at runtime instead.
  2. **Drop costmin from venue truth entirely.** Simpler, but it discards a real venue constraint `00090`'s sizing function needs — `size_order` checks `ordermin`, lot, tick AND costmin, and Kraken's costmin is not uniform enough (0.5 / 0.45 / 0.00002 across three bands) to omit and hope the other checks cover it.
  3. **Leave it in the runtime Cache-supplied concordance check, reading `None`.** Rejected on a measured consequence, not a preference: it would fail all ten legs on the very first cycle and hold D6's alert red forever — the exact T0135 failure D2 exists to avoid (a signal red from day one trains its reader to ignore it).
  Split accordingly: the runtime check covers only the three Cache-supplied constraints; costmin's correctness is the repo-side drift test's job, which is the honest split given where each value actually comes from.

[iter-138] What happens to the basket-re-ratification question this spec's concordance surface makes visible but cannot itself close? (Decision: 1)
  1. **Register it as [[T0137]], human-gated, the spec's only deferral.** **Taken** (owner, 2026-08-13). The owner's own framing was genuinely open — "maybe" the /BTC legs go live during 6b — so ruling it frozen here would misrepresent a live decision as settled. T0137 carries both halves: DOT's membership (universe says drop; record 44 says trade) and the /BTC legs' reachability, naming the single-`margin_balance_asset` multi-quote problem as what a "yes" would have to solve.
  2. **Rule the basket frozen for the rest of Phase 6**, closing the question rather than deferring it. Rejected: it forecloses an option the owner explicitly left open, on a read-only spec that has no basis to make a monetary/strategy call — the same discipline that kept D2's basket a constant rather than an auto-follower.

[iter-139] The `/BTC` half of [[T0137]] — when, and at what depth? (Decision: 3)
  1. **Defer to phase 7, as the topic originally recorded it.** Keeps 6b's scope on the executor and leaves the engine EUR-only for the whole phase. Costs nothing now, but the ratified strategy surface stays narrower than the selected universe for the entire live-trading build, and the re-plumb then lands *after* `00090`'s order path has been built against base keys — so the state machine, sizing and fill ingestion are all reworked on the live-trade path after real money has touched it.
  2. **Pursue during 6b, but make the legs only ADDRESSABLE** — symbol-keyed store, instrument ids and venue state, with the traded basket left at record 44's ten. Cheaper and reversible, and it decouples the plumbing from the registry. But it leaves the engine able to address twelve legs while the ratified record trades ten: a standing half-widened state that reads as a defect to anyone comparing the two, and a second iteration is still owed before anything changes.
  3. **Pursue during 6b at FULLY-TRADEABLE depth, ahead of `00090`** — the re-plumb *and* the deployable re-ratified onto twelve legs. **Taken** (owner, 2026-08-14). The survey's finding that the work was cheaper than the topic recorded (an engine re-plumb, not adapter surgery — the adapter already sees both legs) did not create the inclination; the owner already wanted these legs live before phase 7 and confirmed it against the measured cost. Sequencing it ahead of the first real-money rung follows the same risk-first logic that put `00088` before `00089`: `00090` consumes this symbol keying, and building that path twice on the live-trade path is the waste. The accepted cost is that first real money moves one iteration later.

[iter-139] DOT/EUR misses the refreshed universe's volume floor — retire it from the traded basket, or keep it? (Decision: 2)
  1. **Retire DOT**, matching the traded basket to the 2026-08-13 universe selection. Restores basket-universe agreement on that leg, but the mechanism is worse than the mismatch: it needs a basket mask zeroing a computed target in the cycle and in gate replay alike, which is a standing deviation between the live book and record 44's evidence base — created to honour a 2 % miss.
  2. **Keep DOT traded, and let the concordance test carry the exception as a ruling.** **Taken** (owner, 2026-08-14). The floor DOT misses is a **footprint-sizing** rule — 150,000 median quote volume was calibrated so a full €1,400 position is about 1 % of it — and DOT at 146,957 is 2.03 % under, while 6b's rung sizes sit far below that footprint, so the exposure is structural rather than financial until the ramp. No revisit deferral dangles from the keep, because `tests/test_basket_concordance.py` *is* the trigger: any future refresh that changes selection, DOT's re-entry included, turns it red and forces a conscious baseline edit.
  3. **Keep DOT but re-run the volume measurement first**, on the chance the 25 % three-week decline was a measurement artifact. Rejected as already answered: iter-137's refresh established the decline as real, so a re-measurement would spend an iteration to reconfirm a number that is not near the decision boundary either way.

[iter-139] Do the engine re-plumb and the deployable re-ratification land in one iteration, or in two? (Decision: 1)
  1. **Coupled — one iteration, with the ratified record produced against the re-keyed code.** **Taken** (owner, 2026-08-14). The basket then never sits half-widened: there is no window in which the engine can address twelve legs while the record it trades names ten. The accepted risk is written down rather than left to be rediscovered — this couples a live-trade-path code change to a strategy-record change, so if either half needs rollback, both go.
  2. **Capability-first — land the plumbing, let it sit unused, re-ratify in a later iteration.** Each half is separately reviewable and separately revertible, which is the honest argument for it. Rejected because the intermediate state is exactly the divergence [[T0137]] exists to close, and it would be *self-inflicted* rather than inherited from a universe refresh.
  3. **Re-ratify first, then re-plumb.** Rejected outright: a record naming twelve legs that no code can express is a ratification of something unbuilt, and the registry is append-only — the record could never be withdrawn if the plumbing then failed review.

[iter-139] How do the two `/BTC` legs enter the book — through the model, or downstream of it? (Decision: 2)
  1. **Widen `CrossfreqSystemConfig.assets` to the twelve legs** and let the existing sleeves price them. The natural-looking edit and the smallest diff, but it is an unvalidated strategy change smuggled in as plumbing: momentum, A1, A2 and B would compute *real* weights for two legs record 44's holdout evidence never covered, contradicting the discipline [[T0064]] archived.
  2. **Leave the model at the ten EUR bases and expand downstream** — `_expand_to_basket` maps the ten base-keyed model outputs onto the twelve-symbol basket with `ETH/BTC` and `SOL/BTC` emitted at exactly `0.0`, and `select_model_inputs` contracts the store's twelve series back to the ten `/EUR` legs on an EUR-only calendar union. **Taken.** What is *traded* widens while the *model* does not change at all, which is what makes the identity criterion checkable: `cli/portfolio/` is byte-identical across the branch and the widened path reproduces record 44's book with max absolute difference `0.0`. A future relative-value sleeve flips those structural zeros through its own ratified record — the point of "addressable and tradeable, not yet traded".
  3. **Build a relative-value sleeve now**, so the `/BTC` legs carry model-derived weights from the first day they are tradeable. Rejected and recorded as an explicit drop, not a deferral in disguise: an RV sleeve is a new alpha family with its own validation, SPA and ratification burden, and attaching it to a plumbing iteration would put an unvalidated sleeve on the live trade path.

[iter-139] The journal schema break lands mid-window — how does the gate score a window that straddles the deploy? (Decision: 2)
  1. **Normalize schema-1 replay output to symbol keys**, so the whole window compares in one key space. This was the plan's first draft and it is the design defeating its own goal: the gate compares each record against *its own* replay, and a v1 record on disk stays base-keyed, so lifting only the replay side would turn **every** pre-deploy record into a structural mismatch and zero the ratified 30-day streak at the turnover. Caught by the cold review, before any code.
  2. **Each schema replays AND compares in its own native key space, with no cross-schema normalization anywhere.** **Taken.** A v1 record's journaled pairs already *are* the ten-asset model's base keys, so its grids reach the builder untouched; a v2 record goes through `select_model_inputs` + `_expand_to_basket`, both imported from the cycle rather than copied. `evaluate_gate` scores a mixed run with no discontinuity. Proven by a straddle test driven through the **real** replay path over real v1 and v2 fixture records — `evaluate_gate` is structurally blind to schema, so hand-built outcomes would have proven arithmetic that was never at risk.
  3. **Accept the discontinuity: let the streak reset at the deploy and re-earn 30 days.** Rejected because a deploy that zeroes the ratified streak is a self-inflicted gate breach — it would delay the go/no-go by a month for a plumbing change, and it hides the mixed-window question rather than answering it, leaving the same defect to surface at the next schema bump.

[iter-140] Rung 1 is the first spec that can submit — does it re-open the execution style, or execute the one already ruled? (Decision: 2)
  1. **Re-decide the style now that a real order path exists.** Superficially attractive: the maker-first ruling was made on modelled costs, and the executor is where it becomes real. Rejected because it inverts the evidence order — re-deciding before a single fill exists would replace one unmeasured judgement with another, and rung 1 was scheduled precisely to produce the measurement.
  2. **Execute the ruled style unchanged, and measure the realized blend.** **Taken** (re-affirming the owner's 2026-08-03 [[T0090]] ruling: maker-first with a taker fallback). The ladder joins the touch post-only, reprices at most five times, time-boxes at 15 minutes, then crosses with at most three price-bounded limit IOCs — never a MARKET order, because an unbounded price on the live path buys nothing a marketable limit does not. `zcrypto_exec_fills_total{liquidity=maker|taker}` is the instrument: rung 1 **measures** the blend, it does not re-decide the style, and the measurement flows to [[T0090]]'s re-pricing and [[T0116]]'s band re-derivation. Pricing INSIDE the spread was rejected separately — better queue position bought against adverse selection, and at €10–30 the queue at the touch fills on noise.
  3. **Maker-only, no fallback.** Already rejected at the [[T0090]] ruling and re-rejected here on a sharper ground: an unfilled leg strands the whole T2 tax probe, whose deliverable is the realized blend — which needs the cross to be *reachable* for the measurement to mean anything.

[iter-140] The T2 probe's long leg is settled by hand in the Kraken UI, which converts it into a spot BTC holding — what happens to that residual? (Decision: 3)
  1. **Keep it as a standing lot.** Simplest on probe day, and the position is small. Rejected: its basis is unverified and its future disposal uncontrolled — the lot would be unwound at some uncontrolled later moment by `00092`'s loop, inside live-trading FIFO on a traded leg, which destroys the probe's self-contained tax evidence exactly where the T2 verdict wants to inspect it.
  2. **Dispose of it by hand in the UI, beside the settle.** Identical tax outcome and the least new code. Rejected on what it forfeits rather than what it costs: the machine loses its only **spot-class** order before `00092`, and reconciliation absorbs a second account-external fill for no gain.
  3. **The engine disposes it, as a third signed-off plan carrying an explicit base `qty` read from the owner's ledger export.** **Taken** (owner, 2026-08-18). Three grounds, and the third is the one that decided it: the sale forces Blockpit to compute gain/loss off the carried basis, completing the settled-in-kind lifecycle instead of leaving the basis treatment latent; it keeps the probe self-contained and ends it **flat**; and it is the machine's only spot-class order before `00092` — which is cash-by-default, so the first live spot submission happens here, attended and tiny, rather than unattended and full-size. The `qty` comes from the export and never from the Cache: whether a UI-placed settle propagates live into `balances_free` is unproven on the installed adapter, and a plan-carried quantity removes the dependency entirely. An over-quantity sell is venue-rejected and halts attended; a remainder below `ordermin` is accepted as terminal dust.

[iter-140] Does [[T0119]]'s `target − held` delta formula belong in `00090`? (Decision: 2)
  1. **Build it here, since `held` finally exists.** The read landed with `00089` and the executor is the first consumer of venue truth, so the formula could be written now. Rejected: it builds rung 3's accumulation loop inside the money-proving spec, widening a live-trade-path diff for a mechanism nothing in rung 1 exercises.
  2. **Ruled OUT of `00090`, owner `00092`.** **Taken**, with grounds rather than by preference. [[T0018]]'s execution table names "`00092` — rung-3 accumulation — [[T0119]]'s `target − held`, skip-or-carry, the full loop" verbatim, and the topic file itself says the formula is rung 3's mechanism. Structurally there is also no call site: probe intents carry explicit side/notional/leverage, so no delta is computed anywhere in this spec and there is nothing for the formula to be right or wrong *at*. What `00090` hands T0119 instead is its prerequisite — fills flow into the Cache-backed position view and into the journaled venue/exec records, so `00092`'s `target − held` will read a `held` proven against real fills rather than against a constructed fixture.
  3. **Build it, but leave it uncalled** (the `size_order`/`fx_eur_notional` precedent). Rejected here even though the precedent is this repo's own: those two are pure functions with a pinned contract, while the delta formula's correctness is inseparable from the skip-or-carry policy around it — landing the arithmetic without its policy would pin half a mechanism and invite a later caller to supply the other half unreviewed.

[iter-140] [[T0140]] — the venue record's schema stamp is write-only: enforce it, or label it provenance-only? (Decision: 1)
  1. **Option (a) — a schema-aware `validate_venue_record` mirroring the cycle journal's `validate_record`**, refusing a record whose key shape disagrees with its declared version, `{1, 2}` loadable. **Taken**, and forced by this spec's own consumers rather than chosen on cost: the executor seeds `zcrypto_exec_position` from `state.positions` and refutes a spot-disposal quantity against `state.balances_free`, so the payload is read **structurally** — which is the exact condition T0140 itself named as removing option (b). The exec ledger gets the identical treatment for the identical reason: `00091` will read its fills structurally, and an unvalidated stamp on a forensic record beside real money is the same trap one artifact over.
  2. **Option (b) — accept the stamp as provenance-only and say so at the constant.** A legitimate outcome while both readers were key-agnostic, and cheaper. No longer available: a structural reader makes the two shapes indistinguishable to code that has started caring, which is precisely when a silent normalization becomes a wrong number rather than a degraded dashboard.
  3. **Validate, but normalize a mismatched shape instead of refusing.** Rejected as the worst of both: it would make a v1 record readable by v2 code while hiding that a shape changed, so the one signal the stamp could ever give is consumed by the thing that should have raised.

[iter-140] Where does [[T0138]]'s costmin denomination guard live? (Decision: 3)
  1. **A `__post_init__` validator on `InstrumentConstraints`.** The obvious home, and rejected on the topic's own findings rather than on cost: it couples a plain evidence dataclass to a committed constant, it rejects the deliberately-wrong hand-built states the concordance tests depend on, and it guards the wrong proposition — the defect is not "`costmin_quote` disagrees with `COSTMIN`", it is "a consumer compared a BTC floor against a EUR notional."
  2. **Thread a `(value, quote)` pair into `size_order`.** Type-safe and tempting. Rejected because `00094` D4 drew the denomination seam at the caller deliberately, and churning a proven pure function one spec after it was pinned trades a real regression risk for a cosmetic gain.
  3. **An assertion at the comparison itself, in the executor's one sizing call site.** **Taken**. `size_probe_order` refuses a non-EUR-denominated floor before anything reaches `size_order`, naming the symbol, the denomination and the remedy. The guard is proven by constructing the defect it names — the `/BTC` leg's `2e-05` BTC floor against a EUR notional, asserted on the message so it reads *which* failure fired — and the fail-open direction is pinned separately, so a costmin dropped to `0.0` cannot pass silently. `fx_eur_notional` stays pure and uncalled: rung 1's notionals are all EUR, and the guard is what forces a future `/BTC`-leg notional through that conversion rather than through a second one written beside real money.

[iter-141] [[T0143]] — should the reconciler change what it BOOKS when a fleet-dark episode looks venue-side, or only how it is triaged? (Decision: 1)
  1. **The booking stays, by design; attribution lives beside it.** **Taken, on the owner's ruling.** `residual_gap_seconds_total`'s contract is "no book data exists for this window on either mirror, and no later cycle can heal it" — true on a venue outage and true on a capture failure, so the counter is honest and only its *label* misleads. Three grounds beyond preference: the counter has already survived one traumatic ledger correction (2026-07-14, 6261.8 → 2661.8 s) whose `resets()` guard still costs a 24 h blind spot; the failure direction is asymmetric, because *not* booking a `venue_silent`-classified window would let a correlated shared-mode capture failure go unbooked and unpaged; and the verdict is evidence-weighting rather than proof, which must never gate a monotonic, unwalkbackable record.
  2. **Reclassify venue-attributable silence OUT of `residual_gap`.** The intuitive fix, and the dangerous one: the discriminator is blind exactly when an episode carries no interior evidence, and there its failure mode is silently not booking real permanent loss. Rejected.
  3. **Take a venue-status input before booking.** Refuted by the event that motivated the topic — Kraken posted nothing for 2026-08-20, so a `SystemStatus` read would have booked it identically. Later measurement made the refutation sharper rather than weaker: status *did* catch 2026-08-06, so the two signals are complementary, and the reason status cannot serve here is that the public endpoint reports current state only while the reconciler runs hours later ([[T0144]]).

[iter-141] Where should the verdict live — a log line, or the ledger and a counter? (Decision: 2)
  1. **Log line only.** Cheapest, and the original ruling. Rejected once its cost objections were measured false and its real weakness surfaced: a log line ages out with retention, so for every FUTURE episode the durable record — the ledger — would carry no verdict at all, leaving 3am triage evidence to expire.
  2. **A ledger field plus a label-partitioned derived counter.** **Taken.** `zcrypto_reconcile_dark_episode_seconds_total{verdict=...}`, derived by summing the ledger exactly as every sibling counter is, its three label values partitioning the `both_streams_silent` seconds so the parts sum to the whole and the metric checks itself. Three cost objections to option 1's rejection were all measured rather than assumed: readers-before-writer converge ordering is NOT owed (the only reader of the ledger is `cli/archive/command.py` itself, the NAS transports it unparsed, and every read is `record.get(...)` with a default); no Alloy keep-list edit is owed (the ops keep-list admits `zcrypto_reconcile_.*` by prefix); and "a misclassification is permanent" was overstated, since counters are derived by summing rather than accumulated.
  3. **Ledger field, no metric.** Durable for forensics, but the 3am operator and every dashboard still cannot see the split without reading the ledger by hand — which is most of the harm the topic named.

[iter-141] What counts as proof that a fleet-dark episode was the venue rather than the fleet? (Decision: 3)
  1. **Any interior event, agreeing across mirrors.** The first rule. Broken by construction three separate review rounds running, each in a different way — a snapshot-only interior (a regression writing only resubscribe snapshots on both hosts, which the canary rule guarantees run the same image), a whole-span interior definition that promoted on healthy traffic between disjoint blips, and an aggregate dominance check that let one large outage's dark time carry an unrelated pair of blips.
  2. **Patch each hole as it is found.** Rejected at the third round: `spec-plan-locations.md` rules that a review loop which does not converge is a verdict on the design's SHAPE, not on the reviewers.
  3. **Re-derive around what interior evidence actually proves.** **Taken.** Every interior event is a BRACKET for the silence on either side of it — identical rows establish only that both hosts were receiving *at those instants*. What carries the verdict is that the interior is BRIEF, because a synchronised failure and recovery on two independent hosts aligned to the microsecond is implausible in a way that "both hosts were fine for the twenty minutes between two separate incidents" is not. So: exactly two windows (three or more are unclassifiable and cost nothing — all four historical records have one or two), an interior `update` required (a snapshot proves nothing about a live feed), `interior_seconds < dark_seconds`, and an absolute 300 s bound measured with the real event 3.0× inside it. A density test was measured as the alternative — 58 interior timestamps in 98.5 s against 58,384 in an equal healthy stretch, ~1000× separation — and rejected on surface area, since surface is how all three defects arrived.

[iter-142] [[T0148]] — on what axis do we sweep cost to re-read the 43-vs-44 ordering? (Decision: 1)
  1. **The registered stress axis — re-cost each book's own x1.0 net on its own turnover and re-govern.** **Taken**, and not as the cheap option: it is the axis both registered drivers actually wrote (`noc[k] - turn[k] * 0.003`, re-governed through the same daily cadence), so it is the only axis on which the registry's four `cost_stress_*` figures are reproducible — which is what lets the instrument be validated before any new point counts. It reproduced all four at full precision, and record 44's two are bit-identical to the values spec `00095` measured independently through the production surfaces. It also answers the right question: "what does THIS book earn when execution costs more", not "what book would we have built at that cost". Consequence taken with it: the map is jagged (the governor re-engages on the net-of-cost series), so the sweep reports the whole flip census and the band interior rather than a single crossing — a lone number is a fiction at any resolution, as a finer re-sweep confirmed: the 0.01 grid's apparent last flip dissolves at 0.002, and neither book holds a durable lead anywhere out to x3.0.
  2. **Rebuild both books at each cost.** Superficially more principled, and wrong twice over. For the ADAPTIVE book a rebuild moves the sleeve net-of-cost series, hence the 180-bar inverse-vol weights, hence the positions — so it answers a different question and cannot reproduce trial 43's registered stress figures at all. It is also ~2 min per grid point on the verified path (over 3 h for a 0.01 grid), which would have forced a coarser grid onto the more expensive answer.
  3. **Replace the multiplier with a per-leg maker/taker fee-and-spread schedule.** The most "realistic"-sounding, and unanchored: there is no registered figure to validate it against, so the instrument could not be gated before use — exactly the reconstruction trap [[T0125]] named. The measured execution band is already expressed as a per-side cost ([[T0090]]: 42.1-84.2 bps against the 60 bps basis), which maps onto the multiplier axis exactly, so the schedule detail buys nothing this question needs. Deferred to rung 1, where the realized blend replaces the whole band with one measured point.

[iter-143] [[T0142]] — how do a restart-adopted resting order's fills become observable without widening the unknown-order kill trip? (Decision: 5 — subscribe+filter)
  1. **Candidate (a) — claim the basket instruments and re-scope the unknown-order trip so a sanctioned act is exempt.** The library's own answer: a claimed instrument's reconciled orders carry this strategy's id, so their events arrive on the topic `Strategy.register` already installs and every existing handler works unchanged. Rejected on the objection the topic itself raised — the exemption has to survive the very restart that created the problem, so "sanctioned" becomes a persisted allowlist sitting on the same disk as the kill file, i.e. a durable mute switch for the one guard that has no backstop behind it. And the claim is account-wide: every genuinely unknown order on a claimed instrument arrives at the same handler and is judged by the same exemption, so getting it wrong fails silently in the unsafe direction. Buying observability by weakening the property that makes the trip worth having is the trade this topic existed to refuse.
  2. **Candidate (b) — claim nothing, and reconcile adopted orders at startup from the venue's own trade history, writing the missing rows from that read rather than from events.** It needs no library semantics at all and it does close the pre-restart gap. Rejected on what it structurally cannot reach: a startup read sees only what has already happened, and the defect is a fill landing **after** startup on an order still resting — which is the entire preserved-reducer case, the one this pass exists to keep alive. It would leave the engine blind to exactly the fills it was built to observe, while adding a REST surface on the live trade path for the half already covered.
  3. **Candidate (c) — cancel every adopted resting order unconditionally at startup, giving up the preserved-reducer case in exchange for one rule with no exceptions.** Genuinely the simplest, and it dissolves the observability question by removing the thing to observe. Rejected because it deletes `00090` D10's whole point: a signed-off reduce-only close leg would be canceled and re-dropped as a fresh plan, putting a reducer's re-authorization on the restart path — the position stays open across the gap, and the one order class the hold exists to protect becomes the one a restart destroys.
  4. **Accept the unobservability and carry it in the runbook.** The status quo at `00090`'s merge, and honest while `exec_armed` renders `false`: a restart inside an attended window is an abort, so the gap costs a present operator one venue-truth read. Rejected as a resting state — the moment continuous arming lands, a restart is routine and every post-restart fill is a permanent hole in the exact record the reconciliation trip is measured against, so the divergence grows silently in the artifact built to detect divergence.
  5. **Subscribe to `events.order.EXTERNAL` and filter by disposition against the ledger-vouched rows.** **Taken** — an option none of the three candidates contained, because all three read "claim" and "observe" as the same act. They are not: the claim is only nautilus's *routing* decision, and the publish happens regardless — the installed engine publishes every fill on `events.order.<strategy_id>` unconditionally, so the external topic already carried these events and merely had no subscriber. Subscribing with the claim list still empty gets both properties at once: a matched event (one of the rows the adopt pass re-attached from the ledger) appends to its row, moves the counters, and latches the overfill trip with the own path's arithmetic; an unmatched one — the hand settle, any genuinely external act — is counted under `zcrypto_exec_external_events_total{disposition="unmatched"}`, logged, and dropped before any row write, cancel, or trip. The unknown-order trip stays scoped by a filter that is *stated*, not by an absence nobody chose, and no exemption list exists to get wrong. It also dissolves the topic's second sub-item, which wanted a probe-window measurement of whether external acts are visible to this process at all: the unmatched counter instruments that permanently, at zero behavioural cost -- it takes the reading at the first settle rather than answering in advance, since whether the settle emits an order event at all has never been measured.

[iter-144] [[T0149]] — the sleeve-composition reversal fired [[T0116]]'s band re-derivation trigger; how should the band be re-derived when the new state is four cycles old? (Decision: 3)

  1. **Re-derive on the three-sleeve cycles alone.** Cleanest attribution — it measures the state the book is actually in and nothing else. Rejected on sample size: four cycles cannot carry a p95, and T0116's own edge definition requires ≥3 complete ISO weeks, so the figure would wear a percentile's name without one.
  2. **Wait for ≥3 complete ISO weeks before re-deriving at all.** Methodologically correct and matches T0116's basis exactly. Rejected as the sole answer because it leaves a gate that reads today reading the *registered* one-sleeve band, which is exactly the staleness the trigger exists to prevent — and the direction of the error was unknown until measured.
  3. **Re-derive over a recent window spanning both states, report it as the current combined floor, and state what it cannot separate. Taken.** It answers the question a gate reading today actually faces, and the measurement earned its cost: the band widened (€1,000 median 51.2 → 95.0 bps, p95 115.7 → 168.1), which falsifies T0116's stated reasoning that a bigger book shrinks the relative floor. The larger target moves still miss `ordermin`, so they land in drift rather than in an order — meaning the stale band is too narrow and fails safe, not too wide and permissive. The window's confound (raised-gross one-sleeve plus four three-sleeve cycles) is stated in the resolution rather than hidden, with ≥3 complete ISO weeks named as the condition for a clean three-sleeve figure.

  Two findings came out of the same pass and are recorded because neither was the question asked. The binding placeability floor is `ordermin`, a base-unit quantity floor, **not** `costmin` — a distinction neither T0116's chain nor the alert's runbook drew, and it is why placeability stays 0 of 10 while gross doubles. And the book must be read from `sum(abs(zcrypto_engine_target_weight))` rather than derived from the summed sleeve gross ÷ 3, since the ⅓ combination nets opposing sleeve positions asset by asset; on this transition nothing cancelled (ratio 1.0 to within one ulp), which made the wrong derivation look right.
