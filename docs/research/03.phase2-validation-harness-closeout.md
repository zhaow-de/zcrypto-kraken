# Phase 2 Close-Out — Validation Harness & Cost Model (autonomous core)

**Phase:** 2 (Validation Harness & Cost Model First) · **Iterations:** iter-013 – iter-023 (autonomous) · **Closed (autonomous core):** 2026-07-08

This report closes Phase 2's **autonomous harness + cost-model core** and, with its sibling `03.phase2-decisions.md`, persists the phase's subject-matter decision log (drained from `.tmp/decisions.md`, iter-013 – iter-023; iter-021 was engineering hardening, correctly not logged as a research decision). Like the Phase-1 close-out, it is **scoped**: two §12 items are carried forward — one **T0003-gated** (captured-spread cost calibration, exactly as Phase 1's capture pipeline), one **YAGNI-deferred** (the multi-seed runner). The §9 harness — "the harness is the product" — is complete and proven on synthetic data before any strategy exists.

## Exit bar — status (§12, honest)

Master-plan §12 Phase 2 exit bar, item by item:

| Exit-bar item                                             | Status                            | Evidence / why                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Acceptance suite green in CI                              | **Met**                           | The §9 acceptance triad on synthetic data — planted-signal recovery + null rejection (iter-020), injected-leak detection (iter-022), registry-corruption detection (iter-019) — all in the pytest suite (410 tests).                                                                       |
| A deliberately leaky strategy is caught automatically     | **Met**                           | iter-022: `label_horizon`+`embargo` remove a genuine overlapping-label leak (unpurged ŷ·y ≈ 26.7 → purged ≈ −0.3); review reproduced it live + swept 6 purge settings against the closed-form `H−(d+1)`.                                                                                   |
| Cost model validated against ≥2 weeks of captured spreads | **Carried forward — T0003-gated** | The fee-ladder + margin-accrual **structure** is built and validated **line-by-line** against the confirmed July-9 schedule (iter-017, `docs/kraken-fee-schedule.md`); the **captured-spread** term needs ≥2 weeks of L2 from the capture pipeline → **T0003** (same VPS gate as Phase 1). |

## Autonomous-loop items (§12) — status

| §12 item                                               | Status                                      | Where                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CPCV / purge / embargo                                 | **Met**                                     | `cli/validation/cpcv.py` (iter-013); property-tested (no train leaks into test), mutation-verified                                                                                                                                                                                                                                                                    |
| DSR (deflated Sharpe)                                  | **Met**                                     | `cli/validation/dsr.py` (iter-014); never-NaN (the PoC NaN-DSR lesson, §1 preamble)                                                                                                                                                                                                                                                                                   |
| PBO (backtest overfitting)                             | **Met**                                     | `cli/validation/pbo.py` (iter-015)                                                                                                                                                                                                                                                                                                                                    |
| Stationary block bootstrap                             | **Met**                                     | `cli/validation/bootstrap.py` (iter-016)                                                                                                                                                                                                                                                                                                                              |
| SPA-vs-benchmark                                       | **Met**                                     | `cli/validation/spa.py` — White's Reality Check (iter-023); benchmark family it runs against is Phase 3                                                                                                                                                                                                                                                               |
| Multi-seed runner                                      | **Carried forward — YAGNI**                 | Deferred: the plan de-emphasizes stochastic learners; build when one is actually used                                                                                                                                                                                                                                                                                 |
| Trial registry integrity + hash chain                  | **Met**                                     | `cli/registry/` (Phase-0 + iter-019 `prev_hash` tamper-evidence chain)                                                                                                                                                                                                                                                                                                |
| Cost model (fee ladder + margin accrual)               | **Met (structure); spread carried forward** | `cli/costs/` (iter-017); per-pair spread from captured L2 → T0003. Of §12's other named sub-items: **AoP** omitted (moot below $20k held — YAGNI, per the decisions log); **borrow switch** is implicit — the caller applies `margin_carry` on margin legs vs spot-only `round_trip_fee`; a combined spot+margin+spread cost helper is deferred with the spread term. |
| Acceptance suite (recovery / null / leak / corruption) | **Met**                                     | iter-020 + iter-022 + iter-019                                                                                                                                                                                                                                                                                                                                        |
| Property-based tests on purging math                   | **Met**                                     | iter-013 CPCV property tests (coverage `C(N-1,k-1)`, no-leak)                                                                                                                                                                                                                                                                                                         |
| Performance statistics (Sharpe/vol/return/maxDD)       | **Met (added)**                             | `cli/validation/metrics.py` (iter-018) — the `statistic` callables the harness scores on                                                                                                                                                                                                                                                                              |

## Delivered — artifacts

| Artifact                | Where                                                                                                                       |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Harness package         | `cli/validation/` — cpcv, dsr, pbo, bootstrap, metrics, spa, synthetic, errors                                              |
| Cost model              | `cli/costs/` — fees (17-tier July-9 ladder), margin (per-base open/rollover)                                                |
| Registry hash chain     | `cli/registry/` (schema v2, `prev_hash` tamper-evidence)                                                                    |
| Coverage                | the pytest suite — **410 tests** (from 201 at Phase-2 start, +209), every component property/mutation-tested                |
| Acceptance-test dossier | `tests/test_acceptance.py`, `tests/test_acceptance_leak.py`, `tests/test_acceptance_spa.py` + the registry-corruption tests |
| Phase-2 decision log    | `docs/research/03.phase2-decisions.md` (this report's sibling)                                                              |

Every component was landed via subagent-driven TDD + an independent whole-branch review — and the **distrust-the-instrument** discipline paid off repeatedly: reviews caught a live NaN-leak in DSR (iter-014), two crash-paths in PBO (iter-015) and two in the bootstrap (iter-016), a tautological test gap in metrics (iter-018), and a bug in my own spec (the `$9,999→Tier 2` fee-tier boundary, iter-017); and the subtle acceptance checks (leak, SPA) were each empirically + mutation-verified to be meaningful, not merely green.

## Carried forward — remainder (parked / deferred)

- **Captured-spread cost calibration (T0003).** The cost model's fee/margin structure is done; the per-pair spread term + the "validated against ≥2 weeks of captured spreads" exit-bar clause need the D2 capture pipeline → **T0003** (human-gated on VPS provisioning, exactly as Phase 1).
- **Multi-seed runner (§9.5) — YAGNI.** Deferred until a stochastic learner is actually used (the plan concentrates residual-alpha probability away from ML).
- **Studentized Hansen SPA (SPA_c/l/u) + HAC variance** — a power/robustness refinement over White's Reality Check; the calibrated v1 suffices for Phase 3's benchmark comparison.
- **Cosmetic:** folding the three acceptance tests + the registry-corruption test into one named `acceptance` suite module (all already run in CI).

## Open-topics swept (phase boundary)

Per `.claude/rules/open-topics.md`, the list was swept before this close-out: **T0000 → resolved + archived** (its last sub-item — folding the July-9 fee schedule into the Phase-2 cost model — landed in iter-017 `cli/costs/`); **T0006** was resolved in iter-021. The remaining open topics are all correctly either **human-gated** (T0003 capture pipeline, T0005 Blockpit T1) or **deferred to a future phase** (T0004 tick history → Phase-4 microstructure) — the invariant end-state.

## Why Phase 3 can proceed

Phase 3 (Benchmarks & the Bar to Beat) fixes the benchmark family (B0 buy-and-hold BTC … B4 + short) that DSR/PBO/SPA judge against, on the historical dataset (Phase 1) using this harness (Phase 2) — both complete. The carried-forward captured-spread calibration is a **cost-stress refinement** that folds in when T0003 runs (in parallel), not a Phase-3 blocker: Phase-3 benchmarks are evaluated with the built cost-model structure at its confirmed fee/margin rates (spread stress applied at assumed/parameterized levels until captured L2 exists), exactly as §9.6 prescribes cost stress at 1.5×/2×.

## Decisions persisted

The phase's subject-matter decisions (iter-013 – iter-023, excluding the engineering-only iter-021) are drained **verbatim** from the running `.tmp/decisions.md` into the sibling `docs/research/03.phase2-decisions.md`, per `.claude/rules/decisions-log.md` (*Phase persistence*). The running log is truncated to empty; the next entry it receives starts Phase 3.
