# Phase 6b session brief — the parking document

**Assembled 2026-07-23, pre-departure** (owner away 2026-07-29 → 2026-08-20). This is \[[T0018]\]'s parking document, not the 6b executor iteration: it exists so the session that convenes **after the return** starts from measured state rather than re-deriving a week-old context. Everything below was measured on 2026-07-23 unless the line says otherwise; every figure carries the command or file that produced it.

Read this first, then `docs/open-topics/T0018-phase6-build-sequence.md` for the build sequence, then `docs/research/12.phase5-system-spec-runbook.md` for the record-44 spec.

______________________________________________________________________

## 1. Where the gate stood at parking time

The Stage-6a gate needs **14 consecutive clean complete-UTC days**, clock started **2026-07-11 00:00 UTC** (the first VPS cycle).

| quantity | value at 2026-07-23 15:20 UTC | source |
| -- | -- | -- |
| `zcrypto_gate_streak_days` | **12** | Grafana Cloud Prom, `host=nas` |
| `zcrypto_gate_mismatch_total` | **0** | same |
| journaled cycles | **76**, unbroken | `find /volume1/ZhaoCrypto/engine-journal -name 'cycle-*.json'` |
| journal span | 2026-07-11T00:00 → 2026-07-23T12:00, **6 cycles/day** | same |

**Gate day is 2026-07-25** — the 14th complete clean day is 07-24, scored by the 07-25 daily run. Streak 12 on 07-23 is consistent with that and with nothing having broken since the clock started.

**By the return (~2026-08-20) the soak will carry ~40 clean days (~240 cycles)** instead of the bare 14. That is the whole reason 6b was re-anchored past the absence at the 2026-07-23 grooming — the session reads a materially longer window for free.

### The engine's cadence, since it is easy to misread

Cycles are **4-hourly** (`cycle-00/04/08/12/16/20.json`), six per day — *not* daily. The gate counts clean **days**; `soak-check`'s floor counts scored **bars**. Conflating the two makes the instrument's floor of 30 look like a month when it is five days.

______________________________________________________________________

## 2. What `soak-check` actually delivers — measured, not assumed

\[[T0064]\]'s instrument was last recorded as *refusing* (`L=14 < floor=30`, 2026-07-20), which left open whether its gate-day run would produce evidence at all. **It runs.** Dry-run executed this session, read-only, against a copy of the live journal:

```
uv run zcrypto engine soak-check --journal-dir <copy of /volume1/ZhaoCrypto/engine-journal>
```

```
REALIZED-SERIES WINDOW
  first cycle_ts : 2026-07-11T00:00:00+00:00
  last  cycle_ts : 2026-07-21T16:00:00+00:00
  L (scored bars): 65          span: 10.67 days      chain_ok: True
SELF-TESTS
  instrument_ok: ok    identity_ok: ok    reconcile_ok: ok
STRUCTURAL FINGERPRINT (band=90%)
  0 of 6 outside band (~0.6 expected by chance at 90%)
P&L (NON-GATING)
  realized cumulative net : +0.0618%
```

**Read this correctly.** The instrument's own closing lines are the honest summary and belong in any go/no-go discussion (reproduced here with `--` rendered as an em-dash; the run's raw text is otherwise unchanged):

> A 'consistent' row only means the realized behaviour sits inside the backtest's own range; an overfit strategy lands in-band most of the time at L≈84, so this is not out-of-sample evidence. These are structural-conformance checks — does the live book look like the backtest book — not evidence of edge.

Two artifacts of the dry run, so a later reader does not mistake them for findings:

- **`dropped_tail: 11` is an artifact of this run, not a system property.** It used the default `--store-dir` (`data/engine-store`), the retired workstation store whose newest write is 2026-07-21 21:59; scoring a cycle needs the forward price, so exactly the 11 cycles after 2026-07-21T16:00 could not be scored. A run on the ops node against the live store scores essentially all cycles. Verified by counting: 07-21T20:00 + six on 07-22 + four on 07-23 = 11.
- **`governor_engagement` reads `n/a` by construction here** — the null band spans the full [0,1] at this window length, so the test has no discriminating power, and the realized multiplier was **0.5 on all 65 cycles with no variance**. That matches the design (the governor enters live at its true carried state ×0.5); it is not a stuck knob.

**Consequence for the queue:** \[[T0064]\]'s gate-day slot will produce a report, not a refusal. But the report it produces is structural conformance — it does **not** discharge the zero-OOS problem in §4 below, and no amount of soak length will, because the soak is not an out-of-sample test of the strategy.

______________________________________________________________________

## 3. The build contract — what the executor is built against

**Registry record 44**, the P1 cross-frequency combination. Builder: `cli/portfolio/crossfreq_system.py` → `build_crossfreq_system` (verified path) and the bit-identical `build_crossfreq_system_fast`. `dataset_hash 45275ebe…` over the daily‖4h manifests.

- Three sleeves at **fixed ⅓ weights** (\[[T0019]\]'s adopted simplification — no adaptive-weight state to build, journal, or reconcile): the daily benchmark `w·l3` intraday-held, A1-lf weekly v0.12 offset-mean, and the equal-weight A2-4h ensemble.
- **4h union calendar**; cap 20/10; governor at **daily** cadence, entering live at its true carried state **×0.5**.
- The daily→4h expansion uses **close-time-shifted** boundaries per `expand_daily_positions`' pinned contract — this is the iter-080 look-ahead finding, and it is the single easiest thing for an executor to get wrong.

### Quote the Sharpe honestly

`1.5609` is a **×1.0-cost figure** resting on a headroom guess now known to be wrong in both directions (\[[T0090]\]). The registered anchors for trial 44:

| cost multiple | governed Sharpe | note |
| -- | -- | -- |
| ×1.0 | 1.5609 | the headline; **not** deliverable under the live fee schedule |
| ×1.403 | 1.3645 | the realistic taker end, measured on the committed builder |
| ×1.5 | 1.3029 | the registered anchor that still clears the benchmark |
| ×2.0 | 1.2106 | **below all three benchmarks** — the bar is genuinely cost-sensitive |

Benchmarks: 4h-rebuilt **1.2128** full / **1.2447** decisive (trial 44 *is* a 4h construction), and the daily frozen **1.2455** from record 33 — the last is the one usually quoted and it is **cross-cadence**.

**Do not restate the monotonicity claim.** It was false and is withdrawn: 13 of 50 grid steps in [×1.0, ×1.5] *increase* Sharpe, because the drawdown governor re-engages on the net-of-cost series. Coverage is swept points only, and the grid's margin to the benchmark (0.056) is smaller than the largest single-step drop observed (0.075).

______________________________________________________________________

## 4. The decision queue

> **Items 1–6 are OVERTAKEN as framed (2026-08-02).** They describe the un-sub-phased Stage 6b. §12 has since been amended: 6b runs as a **three-rung ladder** (T2 probe → time-boxed concentrated → full weights at €1,000 under accumulation), the 10 bps band is replaced by the measured venue-imposed floor at the funded NAV, and the go/no-go judges performance against the expectation **conditional on the book's realized composition**, quoted as a band whose TWO uncertainties are stated separately — the statistical CI, and the cost basis (which execution style the book achieves) — never merged into one interval *(amended again 2026-08-03; §12 is the authority)*. Read `docs/research/14.phase6b-orientation.md` first, then this list for the decisions that survive: **items 4, 5 and 6 stand unchanged** (the out-of-time-evidence ruling; sleeve funding; the T2 probe schedule — which is now rung 1 itself); **item 2 (maker-vs-taker) is RULED 2026-08-03 — maker-first with a taker fallback**. Item 1 is spent — the Stage-6a gate it asks about closed PASS 2026-07-30; what survives of it is "every ramp step is a separate D3(vi) call", and the go-LIVE decision now sits at rung 3's exit. Item 3 stands. **Items 7–10 are untouched.**

The numbered list the memo's `queue #N` references point at. Items 1–6 are go-live decisions; 7–10 are research/preparation calls that the same session is the natural place to make.

01. **Go/no-go on the Stage-6a gate.** D3(vi), human, conditional on no gate breaches and the DD ladder untouched or correctly handled — never an automatic calendar promotion. Every ramp step is a separate D3(vi) call.
02. **Maker or taker?** (\[[T0090]\]) **RULED 2026-08-03: maker-first with a taker fallback.** The cost basis was directionally unresolved until then, and the number moves ±30–40 % on it: maker ⇒ the model *over*-charges (×0.70–0.74), taker ⇒ it *under*-charges (×1.37–1.40). The entire calibrated spread (2–4 bps) is small beside the 40 bps maker↔taker delta — **execution style is the lever, not the spread**. This decision must precede any quoted go-live Sharpe.
03. **Re-read the 43-vs-44 ordering at the cost extremes.** Named as unread in \[[T0090]\] and worth doing before the go/no-go: trial 44's registered verdict is ADOPT *vs incumbent 43* ("1.5609 ≥ 1.5366"), not versus a benchmark. At ×1.5 that lead is **+0.0021**; at ×2.0 the ordering **reverses** (1.2106 vs 1.2400, disclosed in the registry row itself). That ordering is the criterion the deployable record actually rests on.
04. **Accept or mitigate zero out-of-time evidence.** (\[[T0064]\]) The one budgeted holdout look was spent on the **superseded record 33** in a degenerate [0, 0] window that discriminated nothing; the deployable has **no out-of-time validation at all**, and the holdout budget is now **0** — a fresh look is a §12 escalation (human-ratified budget expansion), not an autonomous act. §2 above is the only realized evidence, and it is conformance, not edge.
05. **Tiny-live sleeve funding.** D3(ii) second half — a human money action.
06. **The D3(iii) T2 tax-probe set**, scheduled **within the first two 6b weeks** so the tax verification (T2 pass, or the T3 fallback) closes *before* any ramp past 25 %: ≥ 1 margin long and ≥ 1 margin short, each held across ≥ 2 rollover events, one closed and one settled. \[[T0005]\]'s Blockpit T1 depot check rides alongside.
07. **B1 — open with a new hypothesis, or close the shared B budget.** (\[[T0022]\]) The conditioning-overlay class was attributed out by trials 45+46; the family needs a *genuinely new* pre-registered hypothesis to reopen. Binding design constraint from §5: seasonality as **conditioning features on the adopted signals**, never a standalone high-turnover system.
08. **B2 — open the family?** (\[[T0023]\]) Sourcing is fully unblocked (funding backfilled iter-090; liquidations accruing live via the Coinalyze poller since 2026-07-15; the Binance WS recorder shelved as geo-fenced). Only the OI backfill + harness remain, and both are autonomous pre-work.
09. **The pre-live universe refresh**, which is a three-link chain and must run in order: \[[T0093]\] (a **live-tailed volume source** — the rebuild currently reads `data/ohlc-full`, last bar 2026-03-31) → \[[T0025]\] (full symbol & corporate-action ledger) → \[[T0024]\] (the artifact rebuild alone; the criterion itself is delivered). **A rebuild today does not complete at all — it fails closed**, and that is the current, correct behaviour: `_require_fresh_ohlc` raises `DataSyncError` once the stalest daily bar exceeds `UNIVERSE_MAX_OHLC_STALENESS_DAYS = 7`, and `data/ohlc-full`'s last bar (2026-03-31) is **114 days** stale as of this writing. What the guard is protecting against is the pre-guard outcome: the same rebuild **would have selected ELEVEN**, dropping AVAX/EUR at 132,274.82 against the 150,000 floor — because the volume floor was being measured on a four-month-stale window, not because the name genuinely fell out. That is precisely why T0093's remainder is a **data** gap and not a code fix: the guard correctly refuses, and only a live-tailed volume source lets the refresh proceed. Note the quarterly OHLCVT dumps **cannot** discharge T0093: even a freshly-ingested just-closed quarter leaves the frontier weeks stale.
10. **Regime context — define it or drop it.** (\[[T0073]\]) Spec 00058's report-shape item 9 named a "regime context" section without saying what it contains. The open question is which regime variable, if any, is worth conditioning a soak verdict on; an explicit **drop** of item 9 is an honest and acceptable outcome. Everything else in \[[T0073]\] is built and merged.

### Already ruled — do not re-litigate

Three items that sat in earlier drafts of this queue were decided in the week before departure and are **closed**:

- **\[[T0088]\]** — both capture hosts on one provider: **accepted as-is**. Hot standby declined (split-brain with one trade key and no fencing); warm standby assessed feasible but declined as over-engineering; provider diversification and account-level de-correlation both dropped.
- **T0042** — go-live boundary artifacts: closed by supersession, the socket-free end-state shipped as specs 00068/00069.
- **\[[T0027]\]** — reboot policy: **attended reboots on the capture VPSes only**, ops keeps auto.

______________________________________________________________________

## 5. What the executor iteration has to build

Unchanged from \[[T0018]\] constraint 4, restated so this document stands alone: order submission, the order state machine, the reconciliation loop, and the kill-switch — **plus** §12's weekly tracking-error report in full (fills, fees, rollover accrual, slippage vs the cost model), cost-model recalibration from real fills, the item-5 funding action, and the item-6 tax probes.

The §8 hardening gate is **already discharged** (iter-084) — it ran as asserts before any secret rendered and caught a real defect. It does not need re-running, but the executor's own key delivery inherits the same discipline.

______________________________________________________________________

## 6. State the session should re-measure before trusting this document

This brief is a snapshot taken ~4 weeks before it will be read. Re-measure, do not assume:

- **The gate result itself** — this document was written at streak 12/14. Read `zcrypto_gate_streak_days` and `zcrypto_gate_mismatch_total`, and the 07-25 gate-day run's verdict.
- **The absence's soak record** — whether the ~40 clean days actually accrued, and whether anything broke unattended. `soak-check` at that length is the direct read.
- **Whether PR #191 merged** and the rollout tail (Steps 7–8) completed. While it is unmerged the **standing deploy hazard** applies: the fleet is deployed from that branch, so a converge from a develop-based tree silently reverts the 00069 metrics config — caught only by `--check --diff`, never by exit code.
- **Anything the fleet alerted on during the absence** — Slack `#zcrypto` is the record.

______________________________________________________________________

## 7. Deferred to post-return, already registered

Not part of the 6b decision queue, but these all come due at roughly the same moment and will compete for the same attention. Each is registered with its own trigger; none is tracked only here.

| topic | what is ripe | note |
| -- | -- | -- |
| \[[T0092]\] | spread calibration extended to the BTC-quoted legs | ripe ≈ 2026-08-06 (≥ 2 weeks of book) |
| \[[T0097]\] (**not on `develop`** — the topic file lives only on the unmerged `feat/t0092-btc-quoted-capture`; confirm it landed before relying on it) | the `continuity.py` threshold re-pin | needs ≥ 1 week of BTC-quoted streams so the statistic is *fitted*; a candidate clamp was already refuted at 28.61 % phantom gap |
| \[[T0081]\] | the first real `/zcrypto-bump-alloy` run | deliberately deferred past the absence — an optional fleet-wide telemetry change before three unattended weeks is the change-freeze class ruled against |
| \[[T0021]\] | the 14-day journal-retention build | retention already **decided** (14 d flat); only the safety semantics + small attended build remain |
| \[[T0069]\] | gate-export relocation to ops | urgency reduced to architectural — `--cache` (opt-in, built and verified) cut a **warm** run to **0.30 s** against 63.11 s uncached, measured on the ops journal mirror at 39 cycles (T0069's own table) |
| \[[T0049]\] | the go-live drill matrix + day-2 runbook | drills execute only in maintenance windows |
| \[[T0085]\] | arm64 image re-enable | before go-live, not on go-live day |
| \[[T0020]\] | dashboards + alerting iteration | the exporters are delivered; presentation remains |
