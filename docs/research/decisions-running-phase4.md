# Running decisions log — phase 4 (git-tracked since 2026-07-18, spec 00056 D7)

# Running decisions log — Phase 4 (alpha research sprints)

<!-- Staging log per .claude/rules/decisions-log.md. Drained at the ACTIVE phase's close-out. -->

[iter-074] Pick: T0011 sub-item 1 (4h A2 at its native band; the trigger fired — T0009 resolved; invocation names T0011 explicitly). Design decisions for the 4h build:
  1. 4h benchmark mapping (Decision: 1) — TIME-PRESERVING: B3+vt-dynamic with the same TIME horizons on 4h bars (basket lookback 180 bars = 30d; sma_gate 1200 bars = 200d on the basket's own equity; vol_target 10%/yr with per-bar target 0.10/sqrt(2190), lookback 180, max 1.0x; ppy 2190; per-asset turnover x 0.006/side). Alternative (bar-count-preserving, 30/200 bars = 5/33 days) rejected — it changes the benchmark's character; the frozen bar's identity is its horizons, not its bar count. Decisive window = benchmark warm-up analog: 1200 + 180 = 1380 bars (the k>=230 daily analog).
  2. A2 4h grids (Decision: chosen pair) — grid1 (20,50,100) bars = 3.3/8.3/16.7 days (the native-faster band §5 actually hypothesizes) and grid2 (60,120,240) bars = 10/20/40 days (the daily fast grid time-preserved — a clean same-horizon/finer-sampling A/B); short="off" only (the short is four-times dead); vols 0.10/0.12 -> 4 trials (n_trials_in_family 34..37; 3 of A=40 will remain). Alternatives: only time-preserved (misses the actual §5 premise), only native-faster (loses the clean A/B), full 8-arm grid with shorts (re-spends on a dead toggle).
  3. Protocol: the RATIFIED bar via a1_kill_bar — net-of-cost both sides, decisive_start=1380, benchmark-relative worst-slice on calendar years with 2013/2026 stubs excluded, DSR>0.95 at the family's true count, x1.5 cost stress, n_resamples=2000, seed 42; mean_block = n^(1/3) ~ 30 for ~27.5k 4h bars (the daily 17 was the same convention at n=4581). var_trials = variance of the family's recorded per-period sharpes (33 rows with the field... note records 34+ may lack per_period_sharpe naming consistency — derive uniformly and document).
  4. QA gates pre-registered: 240.parquet data QA (10 assets, bar counts, no duplicate ts, span sanity); the 4h benchmark must ENGAGE (gate transitions > 0, occupancy sane) and land in a plausible band vs its daily self (no exact target exists — first build; plausibility: annualized net-of-cost Sharpe within [0.6, 1.9] and maxDD < 60%, else instrument-hunt); A2 books: reconstruct identity to 1e-9, arms differ, zero negative positions (short=off).

[iter-074] INSTRUMENT HOLD before verdicts — two protocol-application flaws caught on first read (no registry rows written):
  1. DSR units cross-periodicity: var_trials was the family's DAILY per-period Sharpe variance (7.835e-5) while the 4h arms' sr is 4h-per-period (~1/sqrt(6) the scale for equal annualized performance) — the deflation benchmark was ~sqrt(6) too large, mechanically depressing all four DSRs into 0.91-0.95 (all four "rejects" flowed through this). Fix: convert the family dispersion to the trial's periodicity — var_4h = var_daily/6 (pp scales 1/sqrt(6)) — and recompute DSR standalone (other legs unaffected). This is the caller-convention trap class again (case-log); the killbar's var_trials>1 guard cannot catch cross-frequency mixing.
  2. SPA block-length convention: mean_block = n^(1/3) BARS gives 30 bars = 5 DAYS at 4h vs 17 days at daily — shorter time-blocks may understate regime persistence and make p anti-conservative exactly when 6x observations make everything "significant". Pre-registered robustness rule BEFORE seeing the re-run: an arm is ADOPTED only if its decisive-window SPA holds (p<0.05) at BOTH the convention block (30) and the time-matched block (102 bars = 17 days); if the time-matched block kills significance, the arm is PARKED with the block-sensitivity finding, not adopted. Family-level K=4 reality check also computed and reported (iter-053 practice) at both blocks.
  Also caching all series to scratchpad pickle so no further recomputation is needed for appends.

[iter-074] VERDICTS (recorded, registry trial_ids 36-39, family-n 34-37; 3 of A=40 remain): under the ratified bar vs the 4h-rebuilt frozen benchmark (1.2128 full / 1.2447 decisive, QA'd + engaged) — trial-n34 (20,50,100)v0.10 REJECT (SPA 0.0620/0.0655 at blocks 30/102, the only failing leg after the DSR units fix); n35 (20,50,100)v0.12 ADOPT (1.3274 full, SPA 0.011-0.018 across blocks+seeds); n36 (60,120,240)v0.10 ADOPT (1.3017, narrowest margins 0.040-0.044, passing all); n37 (60,120,240)v0.12 ADOPT (1.3585 full / 1.3317 decisive, SPA 0.0065-0.0110). Family K=4 multiplicity-corrected reality check p=0.0145/0.0165 — the FIRST family-level significant beat of the frozen bar in the A family (daily was 0.057). Both pre-registered instrument rules held: block-102 robustness and seed stability. The 4h sampling is the binding difference vs the daily kill: same horizons time-preserved (60,120,240 bars = the daily fast grid) now clear the bar — 6x observations tightened the test AND the point estimates rose slightly.

[iter-074] Disposition of the new survivors (logged, not escalated — §12 review): three adopted 4h arms are candidate sleeves for the deployed system; they are NOT "conflicting survivors" (same-direction long/flat books, different signals), so the §12 combination default (inverse-vol of sleeves, a new registered P1 trial under the [iter-073] pre-registered DD-aware criterion) applies autonomously. The blocker is engineering, not judgment: a cross-frequency combination (4h sleeve + daily sleeve) needs a union-calendar/resampling construction design that does not fit before this run's gate — registered as T0011's new next-step (the natural home; it consumes T0011's own output). The breakout-hold sweep (T0011 item 2, 3 trials available) also remains for the next loop.

[iter-075] Pick: T0011 item 2 — breakout-hold/cadence sweep on the DAILY A2 long/flat arms (iter-048 offset-averaged method). Spends the LAST 3 A-family trials (n 38/39/40 -> family closed at exactly 40/40; spending the last is within the rules, expansion would be the park). Protocol pre-registered before any number:
  1. Arms (Decision): the two best daily long/flat configs at held cadences — (10,20,40)v0.12 weekly C=7 (n38), (20,50,100)v0.12 weekly C=7 (n39), (20,50,100)v0.12 biweekly C=14 (n40). Rationale: v0.12 dominated v0.10 in every recorded daily arm; (20,50,100) is the best daily arm (1.3265) and gets both cadences; (10,20,40) gets weekly. Offset-AVERAGED (all C offsets), positions held between rebalances — the iter-048 method exactly.
  2. Bar: the ratified kill bar vs the DAILY frozen benchmark (noc 1.2455 full / 1.2781 decisive k>=230 — QA must reproduce), decisive_start=230, mean_block=17 (the daily convention — no cross-frequency issue this time), n_resamples=2000; DSR at n 38/39/40 with var_trials in native daily units (no conversion needed; the 33 recorded per_period_sharpe rows); adopt additionally requires seed stability (7/1234) on the decisive SPA.
  3. Hypothesis: the held cadence cuts vol-target churn (iter-048 showed the mechanism on A1); if the daily A2 arms' 0.057-class near-miss was cost drag, the cadence variant may clear the ratified bar at daily frequency too. An honest kill closes the A family at 40/40 with the 4h arms as its survivors.
  4. QA gates: benchmark reproduction; book reconstruct identity; arms differ; no negative positions; engagement (turnover drops vs cadence-1 — the treatment must demonstrably reduce drag).

[iter-075] VERDICTS (registry trials 40-42, family-n 38-40 — THE A-FAMILY BUDGET IS FULLY SPENT at 40/40): all three cadence-held arms REJECT under the ratified bar vs the daily frozen benchmark (QA reproduced 1.2455). n38 (10,20,40)v0.12 C=7: SPA fail (0.0545 decisive; decisive Sharpe 1.1099 below the bar). n39 (20,50,100)v0.12 C=7: worst-slice-relative fail (book worst 2018 -0.1451 vs bench worst -0.0893; smaller DD 1/11) with SPA/DSR/stress passing. n40 C=14: worst-slice-relative fail (2018 -0.1375 vs -0.0893; 2/11) DESPITE the project's highest recorded net-of-cost Sharpe (1.4348 full / 1.3211 decisive, SPA 0.0165) — the ratified guard vetoes slow de-risking through choppy years; the bar is the bar, no post-hoc relitigation tonight. Engagement held (drag 2.86->1.72%/yr at C=14 — the mechanism worked; the tail risk it creates is why the leg exists). A-family final tally: 40 trials, survivors = trial 34 (A1-lf weekly v0.12) + trials 37-39 (three A2-4h arms); everything else honestly killed. T0011 item 2 done; the topic's sole remaining item is the cross-frequency P1 fold-in.

[iter-078] Night-audit sweep of iters 074-077 (4-lens: numbers, deferrals incl. PR bodies, decisions-log ordering, state invariants): ONE defect — T0011's open-topics INDEX bullet went stale across four PRs (the frontmatter was refreshed twice; the index-sync half of the rule was missed every time; the bullet still claimed reserved A-family trials + a T0009 gate) — fixed, now matching the topic (40/40 spent; sole remainder = the 00038 trial, ripe now). Lesson: "index sync (every change)" means the README bullet EVERY time the topic file changes, not only on lifecycle transitions. Also folded: the pre-existing CLAUDE.md suite-size drift. Everything else verified consistent (registry 42 chain-intact, A=40/P1=2, suite 954, no stray branches, clean tree).

[iter-085] Which research package first for the resumed loop (post-iter-084)? (Decision: 1)
  1. **B1 substrate via T0012 (15m bars)** — §5's ranked queue puts B1 first, and T0012 is its named prerequisite; no trial spend, fully reversible derived data. Recommended and picked: highest information-per-iteration toward the top-ranked un-started family.
  2. **B2 first (derivatives-positioning features)** — zero prerequisites, but starting it before B1 inverts the pre-registered queue order and starts spending the shared B budget on the lower-ranked family.
  3. **§10 portfolio-limits code package** — clean no-trial-spend engineering, but nothing consumes it until a short-carrying family (B3/B4) starts; premature by the same queue order.

[iter-085] 15m derivation source? (Decision: 1)
  1. **Aggregate the 1-minute OHLCVT dumps via the existing `cli/backfill` machinery** (add `"15": 900` to INTERVAL_SECONDS) — same provenance and code path as the canonical 1h/4h/1d (iter-008, bit-verified then), tested aggregation, manifest+hash convention for free. Picked.
  2. **Ingest the dumps' native 15m interval files** — new ingest path for the same numbers; only worth it if aggregation disagreed with native (which the reconciliation cross-check would surface anyway).
  3. **Tick-primary aggregation** — most expensive; ticks serve better as the independent CROSS-CHECK (T0004's reconcile pattern at 15m) than as the primary source.

[iter-085] Tick-level Parquet catalog now? (Decision: 2)
  1. **Build it now** — serves C2/C1 later, but no current consumer; ~30–80 GB with catalog design questions best answered by the family that needs it.
  2. **Drop for now, note in T0012's resolution** — 15m parquet mirrors ohlc-full and is what B1 consumes; the catalog re-opens under C2/C1's own family topic when those become live. Picked (YAGNI; the raw zips remain on the NAS untouched).

[iter-085] Seam acceptance adjudication — the "exact volume equality" leg failed on all 12 pairs; loosen, stop, or redefine? (Decision: 3)
  1. **Ship on the reconciliation evidence alone, drop the seam leg** — the tick cross-check passed 100%; but silently dropping a failing guardrail is the forbidden move.
  2. **Stop the substrate (literal bar reading)** — honest but wrong: the failure was fully characterized, not mysterious — every one of the 5,621 volume diffs is EXACTLY 1 ULP (float summation order: 4 partial sums vs ~60 sequential adds), prices bit-identical on all 25,909 joined hours, bucket sets identical.
  3. **Redefine the leg STRONGER: volume rel ≤ 5e-16 (≤2 ULP) AND a new exact integer `count` equality leg** — the count sum is Int64-exact and order-immune, so any missing/extra/changed minute shifts it by ≥1; verified zero count mismatches across all 25,909 hours × 12 pairs, which PROVES per-hour minute-set identity. Reviewer-recommended; adopted. (The naive rel ≤ 1e-12 band was rejected as permeable: ~4,500 ULPs of slack could hide a real dust-scale minute on DOGE.)

[iter-085] Verdict — the 15m substrate ADOPTED (no trial spend). Dataset: data/ohlc-15m, basket_sha256 0fed24a65b0bf3953a1dc266e2de9be68169b879fb8faeafa343d8daf5ec5de1, fetched_at 2026-07-11T01:13:45Z, 12 pairs / ~3.12M bars from the 1m OHLCVT dumps (spec/plan 00044). Instrument proof: tick reconciliation 100.0000% coverage AND 100% within 1% (bit-exact, worst rel diff 0) on Q1-2026 windows, all 12 pairs, 100,759 comparisons; QA gap/density shows only known anomaly classes (2013 illiquidity, 2018-01-11 venue outage, thin AVAX/LINK/DOT recent years — B1 design input); seam 15m→1h passes the ratified predicate (prices bit-identical on 25,909 hours, volume ≤1 ULP, Int64 count leg zero mismatches). T0012 resolved+archived; tick catalog dropped (re-opens under C2/C1). Next: open the B1 family (own topic/spec per T0016's split rule) against this dataset hash.

[iter-086] Next package after the substrate: B1 opening scope? (Decision: 2)
  1. **Full B1 opening incl. first trial tonight** — highest §5 value, but a first-trial verdict minted under gate pressure (loop ends 10:00 Berlin) risks exactly the rushed-verdict failure the case-log warns about.
  2. **B1 opening = family topic (T0022) + spec + plan + tested harness/feature code; trial 1 pre-registered, run only if the harness lands with comfortable margin** — settled, mergeable output either way; the trial keeps full QA discipline. Picked.
  3. **§10 portfolio-limits package instead** — bounded and clean but gates B3/B4, not B1; inverts the ranked queue for a second time. Rejected.

[iter-086] B1 trial 1 shape (pre-registered now, per the master plan's B1 design brief)? (Decision: 1)
  1. **Conditioning A/B on the adopted A2-4h ensemble**: arm A = the ensemble as adopted (trials 37–39 equal-weight); arm B = the same with hour-of-day/day-of-week favorable-window conditioning + intraday-vol-state scaling computed on the 15m substrate (0fed24a6…). One knob (the conditioning overlay), judged net-of-cost per T0009's revised kill bar vs the frozen benchmark. Cleanest attribution; directly the master plan's stated design. Picked.
  2. **Standalone intraday trend on 1h** — contradicts the plan's own warning (standalone high-turnover lives or dies on maker realism we don't have until T0014).
  3. **Condition the full record-44 combination** — muddier attribution (three sleeves move); the ensemble-only arm isolates the treatment.

[iter-086] Fold-assignment convention (recorded per the Task-1 review adjudication): a stamp belongs to the fold year of its BAR-START ts[k] (not its decision boundary ts[k]+4h) — the unique resolution consistent with the spec's inclusive completion-time training cut, the plan's pinned test-3 membership, and leak-freeness (boundary-year assignment would gate the New-Year midnight stamp on a table containing its own not-yet-complete return — a 4h look-ahead once per year). Cost: that one stamp/year is gated by a one-year-staler table (conservative direction). A future §6 execution-scheduler consumer must key fold-years the same way.

[iter-086] Verdict — B1 trial 1 REJECT (registry trial 45, family B1 n=1, chain intact at 45). The pre-registered matrix's fail-row fired on both independent reads: arm B fails the kill bar's SPA leg at every grid point (p ≈ 0.31, blocks 30/102 × seeds 42/7/1234, 2000 resamples) while arm A passes the identical bar (p 0.0085) — the instrument discriminates; and the head-to-head has B ≤ A on both windows (mean outperformance −2.6e−5/bar from 2016, p 1.0). Mechanism finding, the trial's real information: the hypothesized turnover REDUCTION inverted — the vol scaler's 0.5↔1.0 state flips re-trade the whole book (+33.8% turnover, A 146.2 → B 195.5) and hold-through worsens maxDD (30.07% vs 21.54%). Engagement proven (5,506 held stamps from 2016, 4,167 scaled boundaries, 7,132 differing stamps); QA anchors: benchmark + all three arms reconstruct elementwise <1e-12 against the iter-074 caches. Candidate follow-ups for the family (NOT pre-registered yet): window-only arm (drop the scaler — the turnover-adder), and a hysteresis/banded scaler; each would be its own [iter-NNN] pre-registration against B's remaining budget.

[iter-087] B1 trial 2 pre-registration — the window-only arm (Decision: 1)
  1. **Arm B2w = seasonality gates ONLY (hold-through), vol scaler disabled (scales ≡ 1.0)** vs the same arm A — isolates the gate mechanism that trial 1's turnover attribution exonerated (hold-through SAVED trades; the scaler's state flips added them). Same spec-00045 protocol + decision matrix verbatim (arm B := B2w); kill bar n_trials=2, var_trials=0.0 (DSR still declared non-binding — no family variance exists at n=2; burden on SPA + worst-slice, per the trial-45 note precedent); SPA grid blocks {30,102} × seeds {42,7,1234}, 2000 resamples; head-to-head windows full + from-2016; engagement proof per mechanism (scaled count must be ZERO by construction — its check inverts: any nonzero scaled stamp is an instrument finding). Registry: family B1, n_trials_in_family=2, variant "B1-window-only-a2eq-v1", same dataset-hash recipe. Picked.
  2. **Hysteresis/banded scaler arm** — the other candidate; changes two things vs trial 1 (band + thresholds), weaker attribution; later.
  3. **Stop the family at n=1** — premature: the informative residue is pre-identified and cheap; budget 24/25 remains.

[iter-087] Verdict — B1 trial 2 REJECT (registry trial 46, family B1 n=2, chain intact at 46). Matrix row 3 fired: B2w passes the kill bar at every grid point (SPA 0.0085–0.0155 — pure edge inheritance from arm A) but B ≤ A on both head-to-head windows (mean outperformance −1.4e−6/bar from 2016, p 0.81). Mechanism finding #2: hold-through DEFERS rather than eliminates trades — the catch-up trade on gate-reopen cancels the held-stamp savings (turnover −1.6% only vs the hypothesized material reduction), while adding drift risk (maxDD 24.25% vs 21.54%). Joint family state after trials 45+46: BOTH overlay mechanisms attributed non-additive on the A2 book — the scaler actively harms (+33.8% turnover), the gates are cost-neutral but edge-diluting. No live pre-registered candidate remains (the hysteresis-scaler variant's prior is now weak: the scaler harmed even before flip costs). B1 pauses with 23/25 shared budget intact; the §5 queue's next family is B2.

[iter-089] B2 pre-work — the free derivatives-data landscape mapped (funding/OI/liquidations for the 10-name USDT-M perp universe), registered as T0023. Decision recorded: do NOT open B2 tonight (its data layer has one human-gated sub-item — the liquidations approach — that sets the harness scope and can't be deferred past the spec). Autonomous research done (six sources; public web, no signup): funding + OI are keyless-backfillable to ~2020 via Binance Vision dumps (monthly fundingRate + daily metrics; REST openInterestHist is 30-day-capped, history-useless); liquidations have NO free historical source (Binance removed allForceOrders in 2021, no Vision dump, WS is lossy largest-only) — so liquidations are live-only-free or paid (Coinglass $299/mo commercial). This FEEDS the human's B2-scope decision (per the decomposition dependency check → run now); the funding/OI backfill build is independent and runs first when B2 opens. No trial spend; B budget untouched at 23/25.

[iter-090] Delivery — the B2 funding substrate landed (spec/plan 00047; cli/derivatives/funding.py; data/derivatives-funding/, basket_sha256 e08ea1a9d2cdc89e4d9c6e7491c3426f3b371d67ec673ec24f2da1e4fe0f5a69). 10 USDT-M perps, 68,281 realized-funding prints from Binance Vision monthly dumps, checksum-verified per file. QA: coverage matches T0023 exactly (early-2020 for the majors; DOGE Jul / DOT Aug / SOL Sep-13 / AVAX Sep-22 2020); balanced-panel start 2020-09-22 (AVAX); zero cadence gaps and zero out-of-band rates except SOL's contiguous 2022-11-09→18 window of 4h→2h funding (the FTX-collapse venue action — real, preserved via interval_hours). Robustness: bounded retry-with-backoff added to the fetcher (a bulk backfill hits transient SSL timeouts — proved itself when LINK 2024-11 timed out once and recovered). Design note: funding-ONLY by scope — OI (the daily 5m metrics dumps, ~10× the files) and liquidations (T0023's human decision) remain for the B2 opening iteration; this substrate de-risks the funding factor now, independent of the human liquidations gate. No trial spend; B budget untouched at 23/25.
