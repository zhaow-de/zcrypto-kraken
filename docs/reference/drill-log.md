# Drill log

One entry per drill run, appended in date order. The heading is `## <YYYY-MM-DD> — <scenario id> — <pass | fail | partial | blocked>`, and the body is one paragraph of labelled clauses: *host* · *preconditions* (the by-value readings taken before the induction, where a section requires them) · *induction* · *time-to-alert* (wherever a Slack path is involved: rule `activeAt` and message always; a device timestamp only where that route's device leg is this drill's to measure — otherwise name the drill that measures it) · *channels* · *collateral* (what else moved, or provably could not) · *operator action* · *follow-ups* (a `T<NNNN>` or an explicit drop). *preconditions* and *collateral* are optional; the rest are not.

`blocked` is a status, not prose. It is the outcome when the induction never landed — a precondition refused it, or the instrument did not take — and the reason is then a required clause. The alternatives are both false records: `fail` asserts a guard did not fire when nothing exercised it, and `partial` a run that half-happened.

`tests/test_drill_log.py` guards the heading shape and the date order; everything below a heading is prose it does not read.

## 2026-08-31 — J′ — pass

*host* workstation, against the engine's own dead-man `zcrypto-engine-shadow` on `zcrypto`; engine up and **disarmed** (`zcrypto_exec_armed` 0, `zcrypto_exec_gate_level` 0, read by value). *preconditions* read green by value at 07:07:53Z — `hc_check_up{name="zcrypto-engine-shadow"}` 1 and `max(hc_checks_down_total)` 0. That green read alone does **not** make the timestamps below this run's: it is blind to a check going down after the scrape it served, and this induction came 40 s after it, inside one scrape interval. What establishes it is the watchdog's **`Pending` `activeAt` of 07:09:40Z**, read at 07:11:35Z while the rule was still pending: in that state `activeAt` is condition-onset, and 07:09:40Z postdates the induction by 67 s, so no earlier scrape saw any check already down. *induction* the `<url>/fail` GET issued from the resolving Python process at **07:08:33Z**, HTTP 200 `OK`; the URL is the ping secret and never entered argv, stdout or disk. *time-to-alert* two routes, both from the induction: the healthchecks.io **native** page reached Slack at **07:08:35Z — 2 s**, carrying `Reason: received a failure signal`, and a `/fail` has no timeout + grace term, so what this route measures is notification latency alone; the Grafana **watchdog** (`zcrypto-hcio-watchdog`, critical) went `Pending` at 07:09:40Z and `Alerting` at **07:14:40Z**, with its Slack message at 07:15:13Z. **Two intervals, and they are not interchangeable**: induction→`Alerting` is **367 s**, and that is the one the ~7 min derived bound governs (60 s hc.io scrape + `for: 5m` + 60 s group interval = 420 s), leaving 53 s of margin; induction→Slack is **400 s**, which adds 33 s of Grafana notification delivery that the bound formula contains no term for. Booking the 400 s against the 420 s would read as 20 s of margin and would put a healthy run outside the bound the moment delivery slowed. Its body measured `1 checks down`. The **device** timestamp was not taken and is **not owed by this drill**: the device leg of the healthchecks-native route is drill Q's path 3, which rides drill I's throwaway check and has no separate induction. J′ and I traverse the same route — the same healthchecks.io integration into the same channel — so measuring it twice would measure it once. *channels* both routes landed in Slack `#zcrypto` on separate integrations — healthchecks.io's own notification (`B0BHKV8E1AM`) and Grafana's `metrics` receiver (`B0BH2K3ARE3`) — so the two halves of the dead-man domain were exercised independently rather than one standing in for the other. *collateral* `Engine · cycles have stopped` stayed **inactive** throughout — recorded, but it **discriminates nothing and is not evidence of route separation**. That rule evaluates `time() - zcrypto_engine_cycle_completed_at_seconds{host="zcrypto"} > 16500` (`infra/grafana/alerts.yaml`), an engine-side gauge on a 4 h 35 m bar; a healthchecks.io ping cannot move that series by any mechanism, and this window was nine minutes. Its silence was guaranteed under every hypothesis, so quoting it as a true-negative would be the degenerate-fixture error `agent-ops.md` names. **Route separation is asserted by `zcrypto-engine-shadow`'s own healthchecks.io description** — "A stale check with NO preceding /fail means a cycle raised before either ping" (`tests/fixtures/healthchecks_descriptions.json`) — and this run did **not** test it: doing so needs the silence route induced and its page compared, which is a different drill. *operator action* success ping at 07:15:57Z, HTTP 200 `OK`; the check re-read by value and confirmed up at **07:17:04Z** (one 60 s scrape later), the aggregate back to 0 — the induction is not reverted until it is measured up again. *follow-ups* two traps this run uncovered, both fixed in this page's standing rules in the same commit rather than parked: the rule-state endpoint matches a rule's **title**, never its uid, so a uid filter returns nothing and reads as *not firing*; and a firing rule's `activeAt` is **firing-onset, not condition-onset**, which would understate a drill's notice by the whole `for` if quoted directly. No topic opened — both were resolvable here.

## 2026-08-31 — K — pass

*host* `zcrypto-ops`. *preconditions* `max(hc_checks_down_total)` 0 and all four `up{host="ops"}` targets 1, both by value at 07:50:39Z; nothing else induced, so the page set is attributable. *induction* `sudo docker stop grafana-alloy` at **07:50:49Z** (container had been `Up 2 days`). *time-to-alert* all six expected pages fired. **Every interval below is induction → the rule's own `Alerting` transition**, read per uid from `GET /api/v1/rules/history`, because that is the interval the derived bounds govern; Slack delivery is reported separately and is **not** part of the bound. `zcrypto-hcio-watchdog` `activeAt` 08:00:40Z — **9 min 51 s**; `zcrypto-trade-backfill-stale` 08:00:50Z — **10 min 01 s**; `zcrypto-reconcile-exporter-stale` 08:01:00Z — **10 min 11 s**; `zcrypto-ops-archive-pull-stalled` and `zcrypto-ops-verified-replay-stale` both 08:01:40Z — **10 min 51 s**; all five against the ≈11 min derived bound. `zcrypto-alloy-dark-ops` `activeAt` 08:06:40Z — **15 min 51 s** against ≈16 min. Every one inside. **Slack messages, and delivery measured separately: 08:01:13Z (33 s), 08:01:25Z (35 s), 08:01:38Z (38 s), 08:02:15Z (35 s), 08:02:19Z (39 s), 08:07:11Z (31 s)** — a 31–39 s band. These are two numbers and not one because booking the delivery-inclusive interval against a firing bound would have put **three** of the six outside their bounds: archive-pull 11 min 26 s and verified-replay 11 min 30 s against 11 min, and Alloy-dark 16 min 22 s against 16 min. **Those three are also the three with the least room: margins to the derived bound run 69 s, 59 s and 49 s for the watchdog, trade-backfill and reconcile-exporter, then 9 s, 9 s and 9 s for archive-pull, verified-replay and Alloy-dark** — under a single 60 s evaluation interval. They passed, and they are tight. **The margins are not noise — they are exactly `60 s − residue`, where the residue is how far each series' staleness onset ran past the 300 s term**: subtracting each rule's own `for` from its `Alerting` time gives `Pending` at 291 / 301 / 311 / 351 / 351 / 351 s, i.e. residues of −9 / +1 / +11 / +51 / +51 / +51 s, and residue + margin is 60 s for every one of the six. So the whole spread is one group evaluation interval, and the three tight rules ran **51 s** of that 60 s budget rather than the 9 s their margin suggests. **What fills the budget is not scrape phase alone**: these rules `Alerting` at :40, :50 and :00 past the minute, so their groups tick on different phases, and the residue is tick alignment plus scrape age — this run separates neither. A rule whose residue reached the full 60 s would fire exactly at its bound — which is what makes ≈11 min a ceiling with no slack left rather than a round number, and it is measured here rather than assumed. Each rule reached `Alerting` exactly its **own** `for` after going `Pending` — `5m` for five of them, **`10m` for `zcrypto-alloy-dark-ops`**, which is why its bound is ≈16 min and not ≈11. The `Pending` transitions clustered at +4 min 51 s to +5 min 51 s, consistent with the ~5 min staleness term the standing rules use, though **not a clean confirmation of it**: staleness runs from each series' last sample rather than from the induction, and the watchdog's +4 min 51 s is fractionally *under* the term — the residue is a scrape offset this run did not measure. **Drill Q's metrics path** is read here per that section: rule `activeAt` **08:06:40Z**, Slack message **08:07:11Z**, quoted from `zcrypto-alloy-dark-ops`, the page this induction is for; the device timestamp is **owed** and is this entry's to carry, per drill Q — as are that channel's **mobile setting and DND state**, which drill Q requires beside it: without the mobile setting a push that never arrived cannot be told from a channel set to mentions-only. *channels* all six through Grafana's `metrics` receiver into Slack `#zcrypto`. *collateral* **the watchdog measured 999, not 1** — its `or on() vector(999)` fallback, because the ops Alloy *is* the healthchecks.io scrape, so this drill exercised the "hc.io dark" half of that rule where drill J′ exercised the "check down" half at value 1. Both halves of one critical rule are now proven, by two inductions, neither designed for it. The four NoData rules each rendered `-1 seconds` in their Slack bodies and transitioned through `Pending (NoData) → Alerting (NoData)`, the sentinel path for a vanished series. **`zcrypto-fleet-daemon-restarted` did not fire, measured rather than assumed**: its history returns **0 rows** across the same window in which the other five return three each, so the empty result is an observation and not an unvalidated filter. The hold ran 26 min 29 s, so the pre-stop sample had aged out of that rule's `[15m]` window before the restore. *operator action* restored with **`sudo docker restart grafana-alloy`** at 08:17:18Z — the runbook's own recipe, run against a *stopped* container, which is half of what this drill proves; not `docker start`, which would restore service while exercising nothing. All six rules returned to `Normal` between 08:17:40Z and 08:18:40Z, and recovery was confirmed by value at **08:19:00Z**: `up{host="ops"}` back to 4 targets, `max(hc_checks_down_total)` 0, every rule inactive. *follow-ups* two instrument findings, both resolved into this page's standing rules rather than parked. My live watcher used substring title filters, three of which matched **two rules each** (`archive-pull stalled` → `NAS ·` and `Ops ·`; `exporter stale` → `Gate ·` and `Reconciler ·`; `Trade backfill` → both of its rules): such a watch reports one name flipping between states when it is two rules disagreeing, and it will book **another host's** page as this induction's. Nothing above rests on it: the six firing and `Pending` transitions were re-sourced per uid from the history API, the delivery figures and the `999` and `-1 seconds` readings come from the Slack message bodies, the preconditions and the `up`/`hc_checks_down_total` recovery reads are by-value Prometheus, the rules-back-to-`Normal` window is the same history API, and the induction and restore timestamps are this operator's own `date -u` on the host beside each command — no instrument carries the moment a drill is induced. Four sources, named here because the `999` — this drill's most useful result — is reachable from none of the others. And that history endpoint takes **epoch seconds**: the same query in milliseconds returns a well-formed response with three empty frames — an absent event indistinguishable from an unavailable store, which is why the empty `zcrypto-fleet-daemon-restarted` result above is stated as validated.

## 2026-08-31 — O — pass

*host* `zcrypto-ops`. *preconditions* `hc_check_up{name="zcrypto-panel"}` 1 and `max(hc_checks_down_total)` 0, both by value at 08:34:54Z, with the check's last clean ping 08:24:03Z — inside one timer period, `OnCalendar=*-*-* *:22:00`. Verified in the repo before inducing, and both halves matter to the verdict: **no rule in `infra/grafana/alerts.yaml` reads `ops_panel_last_success_timestamp`** (the gap this drill tests), and the metric IS published and fresh (`1788164643`), so an owed rule would have had a working series to stand on. *induction* `sudo systemctl stop zcrypto-panel-materialize.timer` at **08:35:04Z**, confirmed `inactive` by reading the word, never `$?`; `NextElapseUSecRealtime` empty, so nothing was queued. *time-to-alert* **the dead-man alone caught it.** The check went down at **11:24:03Z** — exactly its own `timeout` 7200 s + `grace` 3600 s after the last clean ping, to the second, both re-quoted from healthchecks.io rather than from this page — and hc.io's native Slack page landed the same second, reading *"success signal did not arrive on time, grace time passed"*. `zcrypto-hcio-watchdog` **trailed** it — where at drill K it led the other five rules, there being no native page there at all (the ops Alloy scrapes hc.io, it does not ping it): `Pending` `activeAt` 11:25:40Z, `Alerting` `activeAt` 11:30:40Z (its own `for: 5m`), Slack 11:31:13Z — **33 s** delivery, inside the 31–39 s band K measured. **Operator notice from the induction: 2 h 48 m 59 s.** No device timestamp is carried: both routes here are measured by drill Q elsewhere — the metrics path at K, the healthchecks-native path at I — and two readings of one route measure it once. *channels* hc.io's own Slack integration (`B0BHKV8E1AM`) for the native page, Grafana's `metrics` receiver (`B0BH2K3ARE3`) for the watchdog. *collateral* no other rule was observed to move, and none reads this timer's metric — which is the point. **Stated as an observation, not a validated negative**: unlike drill K, this run did not query rule history across the hold to prove the absence. *operator action* `sudo systemctl start zcrypto-panel-materialize.timer` at 11:31:22Z, `active` by the word. **`Persistent=true`, so systemd fired the missed run immediately** and `LastTriggerUSec` jumped to the restore second — a catch-up tick, not a scheduled one, and not evidence the timer is healthy. What is evidence: the ping landed at **11:35:01Z**, 3 m 39 s after the timer start — **not a figure to anchor on**: this was the `Persistent=true` catch-up run with roughly three hours of hours to materialize, where the same unit's ordinary trigger-to-ping gap was 1 m 48 s, Grafana agreed at 11:36:15Z after its scrape, and the watchdog went inactive at **11:36:56Z**. *follow-ups* **no staleness rule is owed** — the dead-man answers, so `ops-node.md`'s no-staleness-rule paragraph is re-tensed in this same commit from "exists to establish" to what this run established, and it now records the cost that gap carries: a panel dying just after a clean ping is unnoticed for nearly three hours. One method finding, fixed in the section: **anchor the bound on the check's `last_ping`, not on the timer's `LastTriggerUSec`** — the unit pings on completion, 1 m 48 s later here, so the trigger predicted the page two minutes early and made an on-time page look late.

## 2026-08-31 — I — pass

*host* `zcrypto-ops`, and no capture host was touched. *preconditions* **the green control ran first and is what makes this drill non-degenerate**: the same throwaway container on a 4 GiB tmpfs reached Kraken's WS (`venue status system=online`), wrote segments, and pinged its check **`up` at 12:01:48Z**, 61 s after start — matching `HEALTHCHECK_INTERVAL_SECONDS` 60. Without it a container that never reached the venue, or whose disk probe raised leaving `watermark.measurable` False, would withhold the ping identically and be indistinguishable from a breach. Then `max(hc_checks_down_total)` **0** by value at 12:02:08Z, taken after the control so nothing sits between it and the induction. *induction* the throwaway container restarted on a **300 MiB tmpfs** at **12:02:17Z**, from the capture-image digest `sha256:08f6abb379a7…` read off the ops host and matched against `fleet-pins.md` rather than assumed. The tmpfs width **is** the induction — nothing was filled: `df` inside the container read 314572800 B free against `DEFAULT_MIN_FREE_BYTES` 1073741824, and the ERROR landed 1.8 s after start. *time-to-alert* **this check's own `timeout` 120 s + `grace` 120 s — the bound this drill CHOSE, and not the production `capture-daemon` check's 600 s + 600 s**, which answers a different question and would read as capture paging in four minutes when it takes twenty. The withheld ping paged the check at **12:05:48Z**, exactly 240 s after the last green ping, with hc.io's native Slack message at **12:05:50Z — 2 s**, the same native-route latency drill J′ measured. `zcrypto-hcio-watchdog` followed: `Pending` `activeAt` 12:06:40Z, `Alerting` `activeAt` 12:11:40Z (its own `for: 5m`), Slack **12:12:14Z — 34 s**, inside the 31–39 s delivery band drill K measured. The device timestamp is **owed** and is this entry's to carry: drill Q's path 3, the healthchecks-native route, is read here. *channels* **two pages, not one** — hc.io's own Slack integration (`B0BHKV8E1AM`) for the check, Grafana's `metrics` receiver (`B0BH2K3ARE3`) for the watchdog. The minted check carried `channels` naming the `#zcrypto` integration explicitly at creation, because a check created through the management API inherits **none**; unchannelled it would have breached in silence and that silence would have been booked as a dead-man failure that never happened. *collateral* the watchdog measured **1**, where drill K's induction drove the same rule to **999** — K took out the hc.io scrape itself, this one only put a check down. Three readings of that rule now exist across J′ (1), K (999) and I (1) — so the **check-down branch is proven twice** (J′ and I) and the **hc.io-dark branch once** (K). They are not equally evidenced, and the re-verification table decides re-runs from what proved a scenario: recording a double proof of the 999 branch would retire a re-proof nobody has made. *operator action* the by-value read is `sudo docker logs zcrypto-drill-i --since 15m` — ops runs no `capture_app` scrape job and its Alloy keep-regex admits no `zcrypto_capture_*` series, so no capture gauge is readable from Grafana on that host at all. **The line appears exactly once, not once per probe**: `disk watermark breached path=/data free=314572800 min_free_bytes=1073741824` at 12:02:18.840Z — the gap opens once and stays open, so an executor grepping for a repeating line finds one hit and should not read that as a stalled probe. Container removed with its tmpfs, the `0600` env file deleted, and the throwaway check deleted through the management API (`http=200`, none remaining, 10 checks left) — all in the same sitting. Fleet read clear by value at **12:13:59Z**. *follow-ups* none owed beyond Q's device reading above; both instrument notes are recorded in this entry rather than parked.

## 2026-08-31 — Q-logs — pass

*host* `zcrypto-ops`. *preconditions* `Ops · ERROR logs` read `inactive` at 12:15:48Z, carrying only its permanent `Normal` fallback instance — the rule's `or on() vector(0)` arm, `activeAt` 2026-08-27, which is not this run's and is why the firing instance is identified by its `container` label rather than by the rule being non-empty. *induction* `sudo docker exec -e COINALYZE_API_KEY= zcrypto-ops-liquidations zcrypto --ship-logs liquidations-poll` at **12:16:04Z**; the process logged `COINALYZE_API_KEY is required` at 12:16:05.270Z and exited 1. **The shape of that command is load-bearing and a bare failing command would have been recorded as a FAIL of a healthy receiver.** `docker exec` writes to the exec client, never to the container's log stream, and `config.alloy`'s `journal_units` relabel keeps only the five `zcrypto-*.service` units plus Alloy's own container stream — the liquidations container is dropped deliberately because it ships its own logs. So a process that merely raises there reaches Loki by neither path. `--ship-logs` installs `cli/logging/ship.py`'s push handler from the container's own environment, which `docker exec` inherits, and pushes straight to Loki. `-e COINALYZE_API_KEY=` scopes the emptied key to this exec alone, so the running poller was untouched; the data dir's `flock` is the second guard against a double poll. *time-to-alert* **the line was confirmed in Loki before the rule was waited on** — `{host="ops", container="liquidations", level="ERROR"}` returned one stream carrying the message, so the induction provably landed and a non-firing rule could not have been mistaken for one. `Ops · ERROR logs` (warning, `logs` receiver) went `Alerting` `activeAt` **12:16:40Z**, 35 s after the line and one evaluation tick given its `for: 0s`, with the Slack message at **12:17:16Z — 36 s**, inside the 31–39 s delivery band drill K measured. The alert body carried the log line itself. *channels* Grafana's `logs` receiver into Slack `#zcrypto`. **This completes drill Q's three receivers**, each read off the induction that produced it: the `metrics` path from `zcrypto-alloy-dark-ops` at drill K (`activeAt` 08:06:40Z, Slack 08:07:11Z, 31 s), the healthchecks-native path at drill I (check down 12:05:48Z, Slack 12:05:50Z, 2 s — no rule `activeAt` exists on that route, because the page is healthchecks.io's own notification and not a Grafana rule), and this one. **The device timestamp on all three is owed, and so are the channel's mobile setting and DND state** — [[T0158]]'s human step. Without the mobile setting a push that did not arrive cannot be told from a channel set to mentions-only; and Q asks for the DND state *at the time*, which these three inductions can no longer supply, so that instantaneous value is recorded in [[T0158]] as uncapturable rather than left looking pending — for K, I and this run, the three that actually carry a Q reading. *collateral* none — and this line does **not** traverse the ops Alloy, by design: the poller direct-ships, `config.alloy` drops `liquidations` from its parse selector for exactly that reason, and the stream's own loss is bounded by `ship.py`'s ring buffer and reported by `zcrypto_logship_dropped_lines_total`. That is why this reading survives the C-ops hold that follows. *operator action* none — the induction is a one-shot process that exited by itself, and the rule clears when its own 15 min window rolls past the line. *follow-ups* none; Q's machine half is complete and its human half is registered in [[T0158]].

## 2026-08-31 — C-ops — pass

*host* `zcrypto-ops`. *preconditions* `max(hc_checks_down_total)` 0 and all four `up{host="ops"}` targets 1, by value at 12:32:19Z. **The backfill query was validated before the induction, not after** — the same LogQL selector was run while lines certainly existed and returned two ops journal streams, so an empty result at restore would mean absent data rather than a broken query. *induction* `sudo docker stop grafana-alloy` on ops at **12:32:25Z**, held **2 h 00 m 55 s** to the backfill horizon (12:32:25Z → 14:33:20Z; the watcher's own exit at 14:33:01Z is 19 s earlier and is not the hold). Not `docker network disconnect`, whatever the drill letter suggests: both Alloy containers are `network_mode: host`, which Docker refuses to disconnect, and an egress rule is a firewall change outside the authorization this program was granted. *time-to-alert* the same six pages drill K timed, and this run is not their measurement — K's short hold is. All six fired and **none outside the set** did, watched on a 5 min heartbeat across the whole hold: `zcrypto-hcio-watchdog` and the four NoData rules first seen firing at 12:42:52Z, `zcrypto-alloy-dark-ops` at 12:47:52Z, consistent with K's measured ≈11 min and ≈16 min. *channels* Grafana's `metrics` receiver into Slack `#zcrypto`. *collateral* **the `logs` reading taken at drill Q-logs survives this hold by design and was not re-taken**: the liquidations poller direct-ships to Loki via `cli/logging/ship.py`, and `config.alloy` drops that container from its parse selector for exactly that reason, so its stream never traverses the Alloy that was down. *operator action* restored with **`sudo docker start grafana-alloy`** at **14:33:20Z** — this drill's own recipe; `docker restart` is drill K's, and running K's here would prove K's recipe a second time and this one not at all. Everything read clear by value at **14:34:56Z**: four `up{host="ops"}` targets, the aggregate 0, all six rules inactive — 1 min 36 s. *backfill, per plane, which is what this drill exists to measure* — **the two planes recover differently and the asymmetry is the finding.** The **logs** plane replayed the outage in full: 8 journal lines inside the hold window, spanning 12:43:04Z to 14:23:01Z, from the positions file under `config.alloy`'s `max_age = "48h"` — a ceiling a 2 h outage never approaches. That replay is **complete rather than merely non-empty, derived from the timers' own schedules** — read off the units rather than inferred from a neighbouring window: `archive-pull` is `OnCalendar=*-*-* *:12,42` and owes 4 runs in this window (12:42, 13:12, 13:42, 14:12), `tape-bars` is `*:52` and owes 2, `panel-materialize` is `*:22` and owes 2 — **8 owed, 8 recovered**, and each observed ping falls about a minute after its trigger. **A comparison against the preceding 2 h is NOT the proof and would have misled**: that window is contaminated by drill O, which held this same panel timer stopped from 08:35:04Z to 11:31:22Z, so its 11:22 run never fired and its `Persistent=true` catch-up landed at 11:35:01Z instead. Counting a neighbour is only sound when the neighbour was undisturbed, and on a day of back-to-back drills it rarely is. The **metrics** plane recovered **nothing**: `up{host="ops", job="integrations/self"}` carries 5 samples across the 120 min window, all between 12:32:25Z and 12:36:25Z, and none after. **Those five are not late ingestion** — a stopped scraper cannot produce samples stamped four minutes past its own stop; five points spanning exactly four minutes at a 60 s query step is the signature of the range query carrying the last real sample forward under Prometheus's 5 min lookback. Nothing scraped means there is no buffer to replay from, so that window is permanently absent, and a responder reading a metrics dashboard across an Alloy outage is reading a hole rather than a healthy flat line. *misfires on return, the clause this section requires* — **none, and that is a validated negative rather than an unwatched window.** The candidates are the rules whose expressions are blind to a condition already present in a series' first sample after a gap: `zcrypto-fleet-daemon-restarted`, `zcrypto-logship-lines-dropped`, `zcrypto-ops-verify-replay-new-breakage` and `zcrypto-gate-streak-reset`, all `changes()`/`delta()`/`increase()` reads. **The first window used was 14:30–14:40Z, and it was too short to license the claim**: `zcrypto-logship-lines-dropped` carries `for: 15m`, so after a 14:33:20Z restore its earliest possible `Alerting` is 14:48:20Z — eight minutes after that window closed, making its silence guaranteed by construction rather than measured. Re-run across **14:30–14:50:46Z**, which outlives every `for` up to 15m, nine candidates return **0 rows**: `zcrypto-reconcile-residual-gap` and `zcrypto-ops-verify-replay-new-breakage` (`for: 0s`), `zcrypto-fleet-daemon-restarted` (`2m`), `zcrypto-gate-streak-reset`, `zcrypto-engine-sleeve-count-changed` and `zcrypto-capture-ts-past-dated-hour` (`5m`), and `zcrypto-logship-lines-dropped`, `zcrypto-capture-rows-quarantined` and `zcrypto-capture-hour-finalized-early` (`15m`). The same query over the same window returns `zcrypto-alloy-dark-ops`'s `Alerting → Normal` at 14:33:40Z, so the zeros are observations. **Three candidates remain unverified and are named rather than quietly counted as clean**: `zcrypto-reconcile-healable-gap-rate` and the two `zcrypto-capture-resubscribe-*` rules carry `for: 30m`, and `zcrypto-ops-verify-replay-backlog-stuck` carries `for: 27h`, which no drill window can outlive. `zcrypto-fleet-daemon-restarted` is quiet for a reason worth recording: its `[15m]` window holds only the post-restore value after a 2 h gap, so `changes()` counts none. **A SHORT hold is what would make that rule fire on return, not a long one** — the opposite of the intuition, and what drill K's *collateral* clause turns on. That is derived from the expression, not observed: both K and C held long enough to stay quiet, so no run in this program has yet seen it fire. *follow-ups* one, owed and open: `infra/runbooks/observability.md`'s Grafana-dark section still carries the literal `PENDING-DRILL-C-PLANE-LOSS` placeholder, and this drill's *Record* says the measured half lands there in the same sitting. It is filled once C′ is measured, since that section pairs both halves.

## 2026-08-31 — C-secondary — pass

*host* `zcrypto-red`. *preconditions* the primary read whole by value at 14:40:29Z — `up{job="capture_app",host="zcrypto"}` 1 and `min(zcrypto_capture_seconds_since_last_book_message{host="zcrypto"})` 0.000791 s — with `max(hc_checks_down_total)` 0. *induction* `sudo docker stop grafana-alloy` on the secondary at **14:40:46Z**, held **2 h 01 m 01 s** to 16:41:47Z. **Only the telemetry shipper was touched and the capture daemon kept running**, which is a property of this instrument rather than a hope: Alloy is its own compose project and its own container, the daemon is a separate one, and `zcrypto-capture` read `Up 3 days` and `active` before, throughout and after. That assurance does **not** transfer to an egress rule against a host-netns Alloy, which cannot be scoped to Alloy alone; on a live capture host the cost of getting that wrong is unbackfillable L2. Corroborated independently of the watcher: `zcrypto-capture-red`'s own dead-man showed `last_ping 15:32:11Z, next_ping 15:42:11Z` while its Alloy had been down 52 minutes. *time-to-alert* **two pages, not the six drill K timed on ops** — the secondary carries far fewer series. `zcrypto-alloy-dark-capture-secondary` `Alerting` `activeAt` 14:55:40Z — **14 m 54 s**, the target. Then `zcrypto-capture-textfile-missing` at 15:06:00Z — **25 m 14 s** — and it is worth naming rather than dismissing: `count(node_reboot_required{host=~"zcrypto|zcrypto-red"})` with evaluator `lt 2` (*both* capture hosts must publish) and `for: 20m`, so it is a **value** page rather than a NoData one, a different mechanism from C-ops's four, and it doubles as an Alloy-dark detector for the capture **pair**. Its arithmetic checks out: ~5 min staleness + `for: 20m`. *channels* Grafana's `metrics` receiver into Slack `#zcrypto`, both pages. *backfill, per plane — and this is where the drill overturns what its ops half alone would have supported.* **Neither plane recovered anything.** Metrics: `up{host="zcrypto-red", job="integrations/self"}` carries 5 samples across the 121 min window, all between 14:40:46Z and 14:44:46Z, the range query's 5 min lookback carrying the last real sample forward. Logs: a naive count returns **76 lines inside the window and would be read as a full replay — it contains none.** Split by where they fall: the `alloy` stream's 28 lines are stamped **14:40:46Z to the second, every one**, the shutdown burst at the instant of the stop with **zero in the middle**; and the `capture` stream's 48 lines fall entirely in the middle, 15:00:00Z to 16:07:08Z, in `cli/logging/ship.py`'s JSON shape rather than journal text — **direct-shipped, arrived live, never at risk and never replayed.** Alloy wrote no journal lines while stopped because it was the thing stopped, and the capture daemon's logs never traverse it at all. **So C-ops's asymmetry is bounded, not contradicted**: "a stopped shipper loses metrics and recovers logs in full" holds only where something *writes to the journal during the outage*. Ops has timer units doing exactly that and replayed 8 of 8; this host has none, and replayed nothing. A host's recovery depends on its workload, not on Alloy. *collateral* on restart Alloy logged `msg="Done replaying WAL" component_id=prometheus.remote_write.grafana` at 16:42:12Z. **That message is a trap**: the WAL did replay, and it recovered nothing for the outage, because the WAL buffers what was scraped-but-unsent and a stopped Alloy scraped nothing. It attests to a dark *destination*, never to a stopped *shipper*. *operator action* `sudo docker start grafana-alloy` at 16:41:47Z; both rules `inactive`, the cross-host count back to **2**, and three `up{host="zcrypto-red"}` targets by value at **16:43:51Z** — 2 min 04 s. *follow-ups* none of its own; the per-plane statement it corrects lands in `observability.md#grafana-cloud-dark` at this task's step 8, and must carry the workload qualifier this half established.
