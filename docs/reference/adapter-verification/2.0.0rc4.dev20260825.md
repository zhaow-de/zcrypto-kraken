# Phase 6 adapter verification — NautilusTrader 2.0.0rc4.dev20260825 official Kraken adapter

## Summary

The attended order-semantics pass for `2.0.0rc4.dev20260825`, run 2026-08-26 08:40–08:44 UTC from the workstation against the live Kraken account. **PASS on all six probes.**

Cost, read from the account's own balance rather than from the harness's arithmetic. The readings, each as the run printed it:

| Stage | Balance | |
| -- | -- | -- |
| 1 — dry run, nothing submitted | `99.67730000` | |
| 2 — four orders placed and cancelled, zero fills | `99.67720000` | **0.0001 lower with no fill to explain it** |
| 4 — fresh invocation after the round trip | `99.51730000` | |

The money leg therefore ran 99.6772 → 99.5173, a drop of **0.1599**, against an expected 0.16001 (two EUR 0.08 commissions plus the −0.00001 spread). That is consistent to about a hundredth of a cent but it does **not** reconcile exactly, and neither does the 0.0001 that moved between stages 1 and 2 across four zero-fill orders. Both residuals are recorded rather than rounded away: the venue reports balances to 8 decimals and prices per leg to 4, so per-leg rounding is the likely account, but that is an explanation offered, not one measured. The account was flat before and after, the after confirmed by a fresh invocation whose startup reconciliation re-read the venue rather than trusting the previous process's cache.

This is the version the engine deploys and arms on. `2.0.0rc4.dev20260825` is a development wheel, not a release: spec `00100` D1 was amended to rule that the deployable pin is the pinned nightly at probe scale, accepting upstream's recommendation against development wheels for real capital knowingly, at a scale where the loss is bounded by the probe's own sizing. The pin is **frozen** from this pass until the engine is armed on it.

## Harness

`infra/scripts/kraken-order-semantics-probe.py`, rebuilt for this version: the probes are the node's own callbacks driving an explicit step queue, with waits resolved by order events, quotes, or clock alerts. **This was its first live run** — every earlier exercise was `--selftest`, `--help`, or `--no-exec`.

Run in four staged invocations, per `infra/runbooks/order-semantics-verification.md`: a full dry run, the four zero-fill probes under `--apply`, the money round trip under `--apply --probe5`, and a fresh flat check. Evidence JSON for each is in `$HOME/probe-evidence/`, outside the repo tree.

Credentials were supplied by `infra/scripts/probe-with-vaulted-key.sh`, which decrypts the vaulted trade key into the harness's environment and executes a hardcoded target, so the operation can be permitted narrowly without granting a general secrets-reading capability.

## Probe results

| # | Probe | Expected | Observed | Verdict |
| -- | -- | -- | -- | -- |
| 1 | Auth + account read | `AccountState` via the exec client; the account's actual balances | MARGIN-type account `KRAKEN-901`; balances `{'EUR': '99.67730000 EUR'}` | PASS |
| 2 | Reconciliation at node start | Open orders + positions empty-or-actual, no spurious entries | open orders 0, open positions 0 (both empty) | PASS |
| 3 | WS market data | Quotes for all 10 pairs within seconds; clean unsubscribe | first tick on all 10 pairs by 0.04 s after subscribe; 1,613 instruments loaded; unsubscribe clean | PASS |
| 4a | Spot post-only limit, resting | Accept → rest → cancel confirmed | accepted (`OYTHDX-QUDXN-RGD4VO`), rested, cancel confirmed; `status=CANCELED`; `filled_qty=0.0` | PASS |
| 4b | Crossing post-only | Venue post-only protection, no fill; RECORD which terminal event it arrives as | `status=CANCELED` via `OrderInitialized → OrderSubmitted → OrderAccepted → OrderCanceled` (`O5AK4D-MJAM6-TSS7K4`); `filled_qty=0.0` | PASS |
| 4c | Margin long (leverage 2), resting | Accept with leverage 2 → rest → cancel confirmed | accepted (`OUHE7L-V5JOD-BYLD2W`), rested, cancel confirmed; `filled_qty=0.0` | PASS |
| 4d | Margin short (leverage 2), resting | Accept with leverage 2 → rest → cancel confirmed | accepted (`OW32NB-HW7VM-LJQLVM`), rested, cancel confirmed; `filled_qty=0.0` | PASS |
| 5 | Real ~EUR 10 fill round-trip | Submit → fill → reconcile → close → flat | market buy 0.00014788 BTC @ 67,622.0 filled (`OQMVJ7-KQSBR-QA2U7U`); market sell of the filled quantity @ 67,621.9 (`OQ23BY-EYRT6-VS2IX5`); flat; gross spread P&L **EUR −0.00001**; commissions EUR 0.08 + EUR 0.08 | PASS |
| 6 | Post-run reconciliation | No open orders/positions; balances reflect only probe 5 | fresh invocation, startup reconciliation: open orders 0 (ours 0, other 0), open positions 0; `{'EUR': '99.51730000 EUR'}` | PASS |

## Observations

**1. Post-only protection arrives as `OrderCanceled`, not `OrderRejected`** — and via accept-then-cancel, with the venue assigning an order id before withdrawing it. The harness's expectation string for this probe was deliberately stripped of the previous version's answer and left as "RECORD which terminal event it arrives as", so this is a re-derivation against 2.x rather than a carried-forward observation that happens to agree. `_on_rejected`'s post-only marker path is therefore still unexercised by any live event at this venue.

**2. The account object reports a single figure keyed `EUR`**, not per-asset wallet balances — `{'EUR': '99.67730000 EUR'}` under `spot_account_type=MARGIN`. Recorded as the shape this build gives; wallet truth remains the Kraken UI or the raw `Balance` endpoint.

**3. A SPOT buy under `spot_account_type=MARGIN` DOES open an OpenPositions row.** The harness's note flagged that it may open none and asked for the reading; probe 5 reported `open positions=1` after the buy, and 0 after the closing sell.

**4. The fill's commission currency is `EUR` at precision 2** — `Currency(code='EUR', precision=2, iso4217=978, name='Euro', currency_type=FIAT)` — on both legs. This contradicts an assumption made in this project's own test fixtures, which were changed during the migration from `EUR` to `ZEUR` at precision 8 on the reasoning that "ZEUR is what production sees". That reasoning was correct about instrument *quote currencies* (546 live instruments carry `ZEUR`, zero carry `EUR`) and wrong about *commissions*. It is not a production defect — `cli/engine/instruments.EUR_CODES` accepts both spellings, so `_fee_eur` sums either — but a fixture fee of `0.012` is a value this venue cannot produce, since a real EUR `Money` quantizes to two decimals.

**5. The engine was untouched by the run.** Read by value from the workstation after the probes: `zcrypto_exec_kill_tripped` **0**; every `zcrypto_exec_position` series **0** across all twelve symbols; `zcrypto_exec_external_events_total{disposition="matched"}` **0** and `{disposition="unmatched"}` **24**. The matched-at-zero reading is the scope property the sealed external observer exists to provide: the engine saw the probe orders, claimed none of them, and acted on none.

## Owed checks not discharged by this pass

**This section is the registration for both, and arming is what collects them.** `infra/runbooks/engine.md`'s pre-probe step 3 refuses to arm on a version whose record still carries an open item here, so neither can be lost to the write-up finishing before the clock did. Discharge an item by taking the reading and rewriting its bullet as the outcome, in this file — the section settles, it does not accumulate.

- **OPEN — the next boundary cycle, 12:00 UTC on 2026-08-26.** The probes ran at B+40 of the 08:00 UTC cycle, which had already journaled at 08:01, so the run proved nothing about a cycle it did not overlap. `infra/runbooks/order-semantics-verification.md` §7.2 asks for a `cycle-<HH>.json` with `completed_at` inside `[B, B+30 min]` for the first boundary **after** the run — `cycle-12.json` under `/var/lib/zcrypto-engine/journal/2026-08-26/`. That is the outcome that says the probes cost the engine nothing. A `failed-cycle-12.json`, or no artifact at all, is a FAIL to investigate before arming, not a reading to retake.
- **OPEN — the `unmatched` baseline.** §7.2 asks whether the counter *rose* by roughly the number of probe events. No before-reading was taken, so the 24 is an absolute value and not a delta. A healthy-boot baseline was already an owed item on the arming checklist — `infra/runbooks/engine.md`'s pre-probe step 4 carries the procedure and [[T0152]] the registration; it stays owed, and it must be taken before the unmatched-external alert's threshold is set.

## Version re-check rule

This pass binds to the exact string `2.0.0rc4.dev20260825` and to nothing else. Both arming guards do exact membership against `cli/engine/order-semantics-verified.json`: the engine Ansible role refuses a converge rendering `exec_armed=true` on a version absent from it, and `cli.engine.execgate` refuses the gate at runtime when the running interpreter's version is absent.

Any bump from here is a decision to re-run this pass at its full attended cost, or to revert the pin. A bump does not disarm a container that is already running — what it kills is the path forward, and the refusal surfaces at the arming step.

## Key-allowlist closure

The workstation's public IP was added to the `zcrypto-engine` key's IP restrictions for this run and removed afterwards, restoring the engine host as the key's only allowlisted host (spec `00039` decision 3's closure step).
