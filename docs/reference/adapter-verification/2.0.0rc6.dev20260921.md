# nautilus-trader 2.0.0rc6.dev20260921 — order-semantics verification

**The attended pass on this version has not run, and this record holds no result yet.** The version is absent from `cli/engine/order-semantics-verified.json`, so both arming guards refuse it. PR #599 pins it for an attended live window before Rung 1, and until that window this record carries what exists without it: the build evidence, what the PR proves offline and with which tests, and the owed checks, each with the window step that discharges it. A reading is written here when it is taken, never before.

**Why this version.** It is the first published build carrying `nautechsystems/nautilus_trader` #5034 (issue #5033), this project's own fix: once the instrument listing is cached, an unscoped order or margin-position read resolves a pair under either of Kraken's spellings, so orders on BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR and ETH/BTC no longer drop out of it. The owner chose `2.0.0rc6.dev20260923` on 2026-09-23; that nightly failed on the 22nd and again on the 23rd, on macOS jobs only, and was never published, so on the owner's word the pin takes the published `2.0.0rc6.dev20260921`. It is a development wheel at probe scale under spec `00100` D1 as amended, as its predecessors were.

## Build evidence

- The upstream `build` workflow on branch `nightly` ran on `70d887545790` from 2026-09-21T14:21:19Z to 18:49:03Z and concluded `success`, read on 2026-09-23 through `gh api repos/nautechsystems/nautilus_trader/actions/runs?branch=nightly`.
- #5034's merge, `16f2163a3c`, is in `70d887545790`'s history, read through `gh api repos/nautechsystems/nautilus_trader/compare/<sha>...70d887545790`. `70d887545790` is itself upstream's startup check for positions reconciliation cannot rebuild, the change behind the operating rule below.
- Not in this build: #5043 (`2c5364a5ca`); the fee-removal commits `042f71eaf6`, `3b25a248f9` and `1072687f80`, so TradeVolume stays in the credentialed listing with #5005's fallback; #5065, the entry average on Kraken margin position reports, open and unmerged; and any fix for #5067, the scoped read, filed upstream.
- The wheel is `nautilus_trader-2.0.0rc6.dev20260921-cp314-cp314-manylinux_2_34_x86_64.whl` from `https://packages.nautechsystems.io/simple/nautilus-trader/`, locked in `uv.lock` at `sha256:d706a9b44eb21d4743464b8df254d3d77d558795b5701633b9be933e1580f66b`. Installed, its `WHEEL` file reads `Generator: maturin (1.15.0)` and `Tag: cp314-cp314-manylinux_2_34_x86_64`, and `nautilus_trader.__version__` prints `2.0.0rc6.dev20260921`.
- The index answered that wheel with `last-modified: 2026-09-21T18:47:44Z`, inside the build run. Read on 2026-09-23 from uv's HTTP cache entry for the wheel (`wheels-v6/index/<index hash>/nautilus-trader/2.0.0rc6.dev20260921-cp314-cp314-manylinux_2_34_x86_64.http`, fetched 2026-09-23T19:40:42Z); the field was identified by decoding the same entry for `2.0.0rc6.dev20260918` to the `18:29:26Z` that version's record states. `curl -sI` on the wheel's URL reads it from the index directly.
- The wheel is one stripped extension, so the build's own sha is the evidence of what it carries, and every source statement below is upstream at `70d887545790`.

## What this version changes that this repository reads

1. **Unscoped reads resolve both spellings, and fail whole on a row they cannot place.** `request_instruments` records each pair's altname beside its AssetPairs key (`record_pair_aliases` in `crates/adapters/kraken/src/http/spot/client.rs`), so once the listing is cached an unscoped OpenOrders read, in both `open_only` shapes, and an OpenPositions read resolve `XBTEUR` and `XXBTZEUR` alike to BTC/EUR. An order or position on a pair the cache cannot place fails the whole read, so a bare client with nothing cached raises on the first row its read returns. At node start that failure is `Failed to get mass status`.
2. **A scoped read still skips a two-way-spelled pair** (#5067): it keeps only the rows spelled by the pair's AssetPairs key, so scoped to BTC/EUR it returns no row for an order Kraken's row spells `XBTEUR`.
3. **Kraken's spot REST order reports carry no client order id.** `parse_order_status_report` (`crates/adapters/kraken/src/common/parse.rs`) builds each with `client_order_id` None, and startup reconciliation names such an order `ClientOrderId(<txid>)` (`crates/live/src/execution/manager.rs`). After a restart the engine's own id therefore finds none of its orders, which is what the row keying below answers.
4. **Client order ids are cut on the wire.** `truncate_cl_ord_id` (`common/parse.rs`) sends a free-text id of at most 18 characters as it is, a 36-character UUID and a 32-hex id whole, and any other longer id as `O` plus its last 17 characters, on every AddOrder, cancel and amend. The red button's `FLT<yymmddHHMMSS>-<n>` (17 characters through `n`=9, 18 through 99) and the fixture mint's 17-character ids reach Kraken as minted; the engine's `O-YYYYMMDD-HHMMSS-001-000-n` reaches it as `O-HHMMSS-001-000-n`.
5. **A node refuses to start over a Kraken margin position its reconciliation cannot rebuild.** The adapter's OpenPositions report carries no `avg_px_open`, and with `filter_position_reports` at its `False` default the start stops with `Unresolved positions during startup reconciliation ... missing avg_px_open for position recovery`. The operating rule below is the owner's answer.
6. **`LiveExecutionEngineConfig` gains `submission_recovery_policy`**, default `RESOLVE_LOCALLY`. The wheel reports 38 fields, and `tests/test_nautilus_interface_pin.py::test_every_exec_engine_default_is_the_one_we_measured` pins all 38.
7. **TradeVolume.** The credentialed listing takes the account's fee tier from `POST /0/private/TradeVolume`. An error, or an answer that does not deserialize, falls back to the public fees (#5005, logged as the WARN `Failed to request Kraken account fee rates, falling back to public rates`); a well-formed answer that omits a pair's fee fails the whole listing.

## The operating rule on this pin: no engine restart with a margin position open

The owner's ruling of 2026-09-23, binding until a pin carries upstream #5065: no engine converge or restart while a Kraken margin position is open. On this version such a restart either fails startup (`Unresolved positions during startup reconciliation ... missing avg_px_open`) and crash-loops, or boots with the position's entry price at 0, where only the realized-PnL gauge is wrong; which of the two depends on the pair's fill history. The red button clears a crash loop: its wrapper stops the unit, and it does not reconcile. So the window below has no step 6, the restart with the fixture's margin position open, and each restart in it follows a `kraken positions -o json` that prints no position.

## What PR #599 proves offline

Each test below runs the installed wheel's real `KrakenSpotHttpClient` against a loopback venue (`tests/kraken_loopback.py`, bound to 127.0.0.1, stand-in credentials) or drives the module under test through a fake that raises where the wheel raises. None of them reaches the venue.

- **The wheel's read and write contract**, `tests/test_kraken_wheel_contract.py`: a bare client's unscoped order and position reads raise on a row they cannot place (`test_a_bare_client_s_unscoped_order_read_raises_on_an_order_it_cannot_place`, `test_a_bare_client_s_position_read_raises_on_a_position_it_cannot_place`); after `flatten.read_listing`, an `XBTEUR`-spelled order resolves to BTC/EUR in both `open_only` shapes and `XBTEUR`/`XXBTZEUR` positions resolve alike (`test_after_the_listing_is_cached_an_order_resolves_under_either_spelling`, `test_after_the_listing_is_cached_a_margin_position_resolves_under_either_spelling`); an order on a pair the listing lacks fails the whole read (`test_an_open_order_on_a_pair_the_listing_lacks_fails_the_whole_read`); a scoped BTC/EUR read returns no row, and a bump that fixes #5067 turns `test_a_scoped_read_skips_the_two_way_spelled_leg_before_resolving_it` red; `client_order_id` is None even when the venue row carries `cl_ord_id` (`test_an_order_report_carries_no_client_order_id`); the fee tier comes from TradeVolume, a TradeVolume error or an unparseable answer falls back to public fees, and an answer missing a pair's fee fails the listing (`test_the_listing_takes_the_account_s_fee_tier_from_trade_volume`, `test_a_trade_volume_failure_still_yields_the_listing_on_public_fees`, `test_a_trade_volume_answer_missing_a_pair_s_fee_fails_the_whole_listing`); and the AddOrder body carries each id as item 4 states (`test_a_client_order_id_goes_out_verbatim_up_to_18_characters_and_as_o_plus_its_last_17_beyond`).
- **The red button**, `tests/test_engine_flatten.py`: every listing row is cached, a symbol listed under two keys resolving under either spelling (`test_a_symbol_listed_under_two_keys_resolves_under_either_spelling_once_the_listing_is_read`, on the real client); the dry run names each resting order under the count (`test_the_dry_run_names_each_resting_order_under_the_count`); a failed open-order read before the cancel still sends the cancel and exits 2 (`test_a_failed_open_order_read_before_the_cancel_still_sends_the_cancel_and_exits_two`); a failed whole-account position read is retried pair by pair and never reads as flat (`test_a_whole_account_position_read_one_row_fails_is_retried_pair_by_pair_and_never_read_as_flat`, `test_a_position_read_that_failed_before_the_cancel_exits_two_even_when_the_final_read_is_whole`, `test_the_pair_by_pair_position_read_binds_against_the_real_client`); every id a run can mint reaches the venue as minted and carries no `-001-000-` infix (`test_every_client_order_id_a_run_can_mint_reaches_the_venue_as_minted`); and a spot balance the adapter rounded up is sold one 8-decimal unit short where the floors allow, one at exactly `ordermin` whole (`test_a_balance_the_adapter_rounded_up_is_sold_one_unit_short_so_the_venue_can_fill_it`, `test_a_balance_exactly_at_ordermin_is_sold_whole_and_never_shaved_into_dust`).
- **The fixture mint**, `tests/test_kraken_fixture_mint.py` and `tests/test_kraken_fixture_mint_loopback.py`: the whole listing is cached before the first read, and every read failure is a refusal naming the read (`TestTheListingIsCachedBeforeAnyRead`, `TestEveryReadFailureIsARefusalNamingTheRead`); each leg's id is at most 18 characters, distinct, uppercase, and unchanged by a replica of upstream `truncate_cl_ord_id` held to upstream's own test vectors (`TestTheClientOrderIds`); each sent line carries the wire id and the venue txid, and a failure partway names what went out (`TestTheSendLoop`); and the mint's own reads, run through the real client against the loopback, return what the venue holds once the listing is cached and raise without it (`test_after_the_cache_step_each_read_returns_what_the_venue_holds`, `test_without_the_cache_step_the_book_read_raises`, `test_the_dry_run_reads_the_minted_fixture_as_complete`).
- **The step-1 read**, `infra/scripts/kraken-window-reads.py` and `tests/test_kraken_window_reads.py`: `orders` reads SAFE only when both unscoped shapes see each target once inside one tick, the scoped read printed and never deciding; `absent` reads SAFE once no target is open; both modes send nothing but reads (`test_both_modes_send_nothing_but_reads`).
- **The six-probe harness**, `tests/test_order_semantics_probe.py` and `tests/test_kraken_order_semantics_probe.py`: probes 2 and 6 sort each open order by its client id or txid into `ours`, `known` (`--known-order`), `unclaimed` or `other`, an unnamed order on `--pair` ending the run at exit 3 (`test_an_unnamed_open_order_on_the_pair_exits_3`, `test_the_same_order_named_with_known_order_exits_0`, `test_a_leftover_an_earlier_evidence_file_records_exits_3`); a start refused over an open margin position is a named FAIL on probe 2 or 6 (`test_a_start_refused_over_an_open_position_fails_each_reconciliation_probe_and_names_it`); and each run logs at DEBUG into `--evidence-dir` and records `tradevolume_fallback` in its evidence (`test_every_run_logs_at_debug_into_the_evidence_dir_beside_its_evidence`, `test_the_evidence_json_records_what_the_runs_log_says`).
- **The row keying**, `cli/engine/executor.py` and `tests/test_engine_executor.py`. The `OrderAccepted` event and each fill event in a row's `events` record the order's txid as `venue_order_id`; `_row_venue_order_id` reads the one txid a row carries, and a row with none, or with two that disagree, counts as having none. A row whose own id misses is resolved through the Cache's venue-order-id index (`_cached_order`), resting orders are matched by client id and then by txid in `_adopt_resting_orders`, and events are matched the same way (`_attached_for`, `_attached_by_venue`), every ledger write naming the row by its own id. Open rows whose order is gone and finished rows with fills are compared against a closed-order read at the venue by txid (`read_venue_orders`: a bare `KrakenSpotHttpClient` on the engine's credentials, the whole listing cached, `request_order_status_reports(..., open_only=False)` from the earliest such row's boundary minus one hour, under a 30 s bound), made once per process in the startup pass and only when a row needs it; a failed read leaves the rows, logs one CRITICAL line and refuses every plan until a restart, with no kill trip. A row with no txid recorded, or whose txid the venue read does not return, is marked `ambiguous` once: an open row takes the state and the event, and a finished row with fills takes the event only and keeps its state. A plan is picked up only after the startup pass has run, and the pass's cancel line names the gate's reasons (`-- the gate reads none (<reasons>)`). The tests drive the reconciled shape, an order whose client id is its txid, and include `test_a_ledgered_reducer_reconciled_under_its_txid_is_left_resting`, `test_an_order_that_closed_while_down_is_read_at_the_venue_by_its_txid`, `test_a_withdrawn_fill_on_a_finished_row_is_read_at_the_venue_by_its_txid`, `test_a_failed_venue_read_leaves_the_rows_and_refuses_every_plan_for_the_life_of_the_process`, `test_an_open_row_with_no_recorded_txid_is_marked_ambiguous_once`, `test_a_finished_row_with_fills_and_no_recorded_txid_takes_the_mark_but_keeps_its_state`, `test_no_plan_is_picked_up_before_the_startup_pass_has_run` and `test_the_startup_cancel_at_level_none_names_the_gates_own_reasons`, with `read_venue_orders` itself run on the real client against a loopback venue (`test_read_venue_orders_returns_open_and_closed_orders_by_txid_with_no_client_order_id`).

Offline cannot say what Kraken does on this account: the spellings in its own answers, TradeVolume on this key, what it stores for a client order id, `reduce_only` on a live closer, and the engine's boot on the host. The owed checks read those.

## Probe results

None yet: the pass has not run. The harness prints the rows to paste here (`infra/runbooks/order-semantics-verification.md` §7.4).

## Owed checks not discharged by this pass

Arming collects this section: `infra/runbooks/engine-procedures.md`'s `engine-probe-window` pre-probe step 3 refuses to arm on a version whose record still carries an open item here. Discharge an item by taking its reading and rewriting its bullet as the outcome, in this file. Every item below is open.

- **OWED — the attended six-probe pass (window P1).** Per `infra/runbooks/order-semantics-verification.md`, with the owner's resting BTC/EUR order named by `--known-order OTFW3E-R2Z7O-VRCASR` on every `$RUN` line, and each credentialed invocation's `tradevolume_fallback` recorded. A PASS on all six probes is what adds `2.0.0rc6.dev20260921` to `cli/engine/order-semantics-verified.json`, beside the probe table above. The same write-up names the boundary §7.2's journal check waits on.
- **OWED — the engine's own unscoped reads see the resting BTC/EUR order (window step 1).** `infra/scripts/kraken-window-reads.py orders` on the engine's image: one row per txid in each unscoped shape, each round trip inside one 5 s tick. This is T0160's check 6 on the engine's own read shapes.
- **OWED — the engine restart with both orders resting (window step 2).** Three readings from one restart: the `unmatched` baseline on this version, predicted `+4` for the two cancelled orders and `matched` `+0`; the row keying's startup pass on the live node, each hand-placed order reconciled under its txid, matched to no row and cancelled, with the ledger's rows read at 2.6; and the credentialed listing's success half, the boot's TradeVolume WARN present or absent (the fallback half is pinned offline by `test_a_trade_volume_failure_still_yields_the_listing_on_public_fees`). A ledgered row this engine submitted under the row keying is first resolved by its txid live at Drill G, whose `matched` reading is recorded beside the probe table when the drill runs.
- **OWED — the red button's dry run names the BTC/EUR order (window step 3).** `2 resting order(s) seen`, then one line per order naming BTC/EUR and SOL/EUR by their txids: the live corroboration of spec `00111` D1's disposition, superseded by #5034 plus PR #599's listing cache.
- **OWED — the press against resting orders only (window step 4).** Exit 0 with `cancel.ok` true, `cancel.orders_after` 0, no leg and no residual: the first live account-wide cancel.
- **OWED — the fixture, and the dry run that proves the five reads (window step 5).** The mint's three legs sent, each line naming its wire id and venue txid; then a dry run that reads the resting leg, the margin position and the spot balance: the row `infra/runbooks/engine-procedures.md`'s read-only dry run asks for, T0159's dry-run sub-item, and, in its margin line, the live reading of spec `00111` D5's disposition. The id read-back taken at 5c is recorded beside it as an observation.
- **OWED — the full press to flat (window step 7).** Exit 0 with the margin closer (reduce-only, leverage 2) and the spot sell each answered by a txid and no error, and Kraken's positions and open orders empty: the first live `reduce_only` from this repository, and T0159's first-press row.
- **OWED — the terminal-event minting rate (pre-probe step 5).** Unmeasured on this venue at any version; taken at the first arming window, after step 2 has given the baseline a number, and open until then.

## The window, step by step

The owner's rulings of 2026-09-23 are applied: there is no step 6 (the operating rule above), no `locked` reading (on the MARGIN account EUR `locked` is margin in use; the two spec `00111` decisions it was to settle are dropped, and draft PR #387 is closed unmerged), and the six-probe pass names the owner's resting BTC/EUR order with `--known-order`.

Conventions: `W$` is the workstation, in the main checkout at PR #599's tip, `uv sync`ed; `infra/scripts/mint-with-vaulted-key.sh` runs that tree's `.venv/bin/python`, not the image. `H$` is a shell on the engine host (`ssh -t zcrypto`). `CAP=.tmp/window` is a local capture directory. Each wrapper run is piped through `tee`, because a flatten dry run writes no record. Container reads on the host name their field (`.State`, `.RestartCount`, `.Config.Image`) and print no environment.

### Before the window

- P0. No margin position is open: `W$ kraken positions -o json` prints no position. Take it before the six-probe pass and again before step 2.
- P1. The attended six-probe pass, per `infra/runbooks/order-semantics-verification.md`, with `--known-order OTFW3E-R2Z7O-VRCASR` on every `$RUN` line and a further `--known-order <txid>` for any other order placed on purpose. `--pair` stays at its BTC/EUR default: unnamed there, the owner's order reads `unclaimed`, probe 6 FAIL and exit 3. Named, it reads `known`. An order an earlier interrupted run left is `ours` by the txid its evidence file in `$EVID` records. Each run writes `probe-<stamp>.log` at DEBUG beside its evidence, which keeps the TradeVolume WARN if it fires. On PASS, write the probe table into this record and add the version to `cli/engine/order-semantics-verified.json` (§7.4).
- P2. The converge has landed, inside an inter-cycle gap and with P0 true: `H$ sudo docker exec zcrypto-engine python -c "import nautilus_trader; print(nautilus_trader.__version__)"` prints `2.0.0rc6.dev20260921`, and `H$ sudo grep '^IMAGE=' /usr/local/sbin/zcrypto-flatten` names the digest `H$ sudo docker inspect --format '{{.Config.Image}}' zcrypto-engine` prints.
- P3. The fleet-pins engine row names the digest P2's inspect printed, and a revision that pins this version and carries PR #599's code, the row keying included: `git show <rev>:pyproject.toml | grep nautilus-trader` reads `2.0.0rc6.dev20260921`, and `W$ git fetch origin && git merge-base --is-ancestor "$(gh pr view 599 --json headRefOid -q .headRefOid)" <rev>` exits 0. Steps 2.3, 2.6, 3.2, 3.4, 4.2, 5b.2, 7.2 and 7.4 quote lines that PR #599's engine and red button print; a revision with the version bump and without the rest of PR #599 passes P2 and the pyproject read, yet prints other lines at those steps, so on a revision that fails the ancestry read, stop here.
- P4. Read the Kraken maintenance feed whole, at planning time and immediately before step 2: `W$ curl -fsS https://status.kraken.com/api/v2/scheduled-maintenances.json > $CAP/maint.json`, read in full, not through `head` or `tail`. An empty feed is no evidence the window is clear.
- P5. Step 2 falls inside one inter-cycle gap, which opens at the boundary cycle's journalled completion and closes before the next boundary. From step 4 the press has stopped the engine, and it misses its boundary cycles until 7.6 starts it: plan 7.6 before the next boundary, or accept the lapse.
- P6. Baselines: `W$ kraken open-orders -o json > $CAP/p6-open.json`, `W$ kraken positions -o json > $CAP/p6-positions.json`, `W$ kraken extended-balance -o json > $CAP/p6-balance.json`, and `W$ uv run python infra/scripts/grafana-query.py 'zcrypto_exec_external_events_total{host="zcrypto"}' > $CAP/p6-counter.txt`. Write down each non-EUR balance above dust: a press sells them all at market. Step 2 cancels each open order, whatever its pair.

### Step 1: the engine's own read shapes see the resting BTC/EUR order

- 1.0. The owner's BTC/EUR order `OTFW3E-R2Z7O-VRCASR` rests (`W$ kraken open-orders -o json`). By hand in Kraken's UI, rest a SOL/EUR control far from the market and note its txid.
- 1.1. Inside the engine's inter-cycle gap: `W$ IMAGE=$(ssh zcrypto sudo docker inspect --format '{{.Config.Image}}' zcrypto-engine)`, then `W$ ssh zcrypto sudo docker run --rm -i --env-file /opt/zcrypto-engine/engine.env --entrypoint python "$IMAGE" - orders OTFW3E-R2Z7O-VRCASR <SOL-TXID> < infra/scripts/kraken-window-reads.py | tee $CAP/step1.txt`.
- 1.2. SAFE, exit 0: in both unscoped shapes (`open_only=True`, startup reconciliation's and `query_order`'s read; `open_only=False`, the single-report read, which also pages the closed orders) exactly one row per txid, each round trip inside 5 s, and the last line `SAFE`. By eye, since the verdict does not read them: the BTC row spelled `BTC/EUR.KRAKEN`, `client_order_id=None` and a zero `filled_qty` on both rows, and no `NOT A TARGET` row.
- 1.3. Readings outside the verdict: the empty-cache line raises `instrument not in cache for pair ...`; the scoped BTC/EUR contrast reads `0 row(s)` (#5067); the scoped SOL/EUR contrast reads `1 row(s)`.
- 1.4. HAZARD, exit 1: a txid missing or doubled in either shape, a raise, or a round trip over 5 s; or, by eye, another spelling on the BTC row. Stop: step 2 would reproduce the 2026-09-19 hazard. Record the reading and take T0160's repair branch. A `NOT A TARGET` row is another open order, and step 2 would cancel it: cancel it by hand, or stop. Exit 3 is a listing that could not be read, exit 2 a read that never started.
- 1.5. Record the capture here and in T0160 as check 6.

### Step 2: an engine restart with both orders resting

- 2.0. Immediately before: re-read P4; P0 still prints no position (the operating rule); `W$ kraken open-orders -o json` lists exactly the two txids, because the restart cancels each open order whatever its pair.
- 2.1. `H$ sudo docker inspect --format '{{.State.StartedAt}} {{.RestartCount}}' zcrypto-engine`, then `H$ sudo systemctl restart zcrypto-engine`, then the same inspect again.
- 2.2. Read the boot: `H$ sudo docker logs --since "$(sudo docker inspect --format '{{.State.StartedAt}}' zcrypto-engine)" zcrypto-engine 2>&1 | grep -E 'Received [0-9]+ order|Reconciliation complete|canceling adopted resting order|instrument not in cache|Unresolved positions|Failed to get mass status|could not be read at startup|falling back to public rates' > /tmp/step2.txt`. Copy it back, then delete it on the host.
- 2.3. PASS: `Received 2 order(s)`; `Reconciliation complete for KRAKEN` with `external=2, open=2`; two `canceling adopted resting order <txid> -- the gate reads none (<reasons>)` lines, the reasons those `H$ sudo docker exec zcrypto-engine zcrypto engine exec-status` prints; no `could not be read at startup` line; `.RestartCount` unchanged.
- 2.4. Wait at least 45 s after the timestamp of the last `canceling adopted resting order` line in 2.2's capture, since the venue's confirmation of a cancel can take about 32 s. Then `W$ ssh zcrypto sudo docker run --rm -i --env-file /opt/zcrypto-engine/engine.env --entrypoint python "$IMAGE" - absent OTFW3E-R2Z7O-VRCASR <SOL-TXID> < infra/scripts/kraken-window-reads.py | tee $CAP/step2-absent.txt` reads `SAFE`, and Kraken's page agrees.
- 2.5. TradeVolume: the WARN `Failed to request Kraken account fee rates, falling back to public rates` in 2.2's capture means the exec client's credentialed fee read failed and its listing took the public fees; its absence means the credentialed read answered. Record which.
- 2.6. Venue truth and the ledger, with `infra/runbooks/engine-procedures.md`'s venue-truth read and ledger read (`HOURS=48`): the newest venue record's `positions` empty. An open row the pass could not match reads `ambiguous` (a finished row with fills keeps its state and carries the mark only in its `events`, which the ledger read does not print): find its order on Kraken's Open Orders and Closed Orders pages, or with `W$ kraken open-orders -o json` and `W$ kraken closed-orders -o json`, by `cl_ord_id` (sent as `O-HHMMSS-001-000-n`) and by pair, side and quantity; cancel it on Kraken if it rests; if it filled, compare its executed volume with the row's `filled_qty`.
- 2.7. The counter, from the workstation as in P6. Prediction: `unmatched` `+4`, two events for each cancelled order, and `matched` `+0`. `(no series)` is a failure of the telemetry path, never a zero.
- 2.8. HAZARD (a): the BTC/EUR order is absent from the boot and still resting. Cancel it on Kraken's page and take T0160's repair branch. HAZARD (b): `Failed to get mass status`, `instrument not in cache`, `Unresolved positions`, or `.RestartCount` climbing is a crash loop. Causes, most likely first: the key's permissions; the WARN lines before it; an order or position on a pair outside the listing. `Unresolved positions` is an open margin position: press the red button (`W$ ssh -t zcrypto sudo zcrypto-flatten --execute`), which stops the unit and does not reconcile. Otherwise cancel both orders on Kraken's page and the engine starts clean; the red button's dry run prints the adapter's message whole.
- 2.9. Record here and in T0160: the re-read on this pin, which decides the open-order half of T0160's second sub-item, and the row keying's startup pass on the live node.

### Step 2b: re-rest the orders step 2 cancelled, the engine left running

- 2b.1. By hand in Kraken's UI, rest a BTC/EUR and a SOL/EUR order far from the market and note both txids. `W$ kraken open-orders -o json > $CAP/step2b-open.json` lists exactly these two.
- 2b.2. Leave the engine running. Its cancel sweep runs only on a kill it trips itself (`_trip_kill`), never on a kill file written by hand, and an order placed during the run arrives as an external event, counted and dropped; a stop would lapse a boundary cycle for nothing.

### Step 3: the red button's dry run, through the wrapper

- 3.1. `W$ ssh -t zcrypto sudo zcrypto-flatten 2>&1 | tee $CAP/step3.txt`. `-t` because the wrapper runs `docker run -it`.
- 3.2. PASS, exit 0: `2 resting order(s) seen -- the cancel is account-wide`, then `  order BTC/EUR <SIDE> <qty> -- venue txid <2b's BTC txid>` and the same line for SOL/EUR; `no margin position to close`; `no non-EUR spot balance to sell`; `nothing was sent: this run reads and prints only`.
- 3.3. STOP before step 4 if the plan lists any spot or margin leg: step 4 would sell or close it at market, and it would no longer be a press against resting orders only.
- 3.4. FAIL: a count of 1, or no BTC/EUR line, means the bare client does not resolve the altname, and spec `00111` D1 is not superseded. `open orders could not be read: ... -- the cancel is account-wide and reaches them unread` in place of the count means an order on a pair outside the listing, or the open-order request itself failing; the error after the colon says which. Stop either way.
- 3.5. Record the capture here. The bare client carries no nautilus logger, so it leaves no trace of which TradeVolume path answered; 2.5 is where that is read.

### Step 4: a press against resting orders only

- 4.1. `W$ ssh -t zcrypto sudo zcrypto-flatten --execute 2>&1 | tee $CAP/step4.txt`, then type `FLATTEN`.
- 4.2. PASS, exit 0: the two lines `the account reads flat: no resting order, no open position, no sellable balance left` and `  the reads before the cancel and the final reads saw every order and position the venue reported; a read that could not ends a run at 2, never 0`, followed by `the record of this run is <path>`, the path 4.3 copies. `W$ kraken open-orders -o json` lists neither txid, `H$ systemctl is-active zcrypto-engine` prints `inactive`, and the kill file reads `<timestamp> flatten`.
- 4.3. Copy the record: `W$ ssh zcrypto sudo cat /var/lib/zcrypto-engine/exec/flatten-<stamp>.json > $CAP/step4-record.json`. It reads `cancel.ok` true, `cancel.orders_after` 0, `legs` `[]` and `residuals` `[]`.
- 4.4. FAIL, exit 2: `infra/runbooks/engine-procedures.md`'s flatten step 5: read the record, check Kraken's page, run it again.
- 4.5. Record the account-wide cancel here, as the red button's first row, and in T0159.

### Step 5: mint the fixture, then dry-run again

- 5a.1. Open the key's IP allowlist per `infra/runbooks/order-semantics-verification.md` §1.3. `W$ infra/scripts/mint-with-vaulted-key.sh 2>&1 | tee $CAP/step5-mint-plan.txt` prints the plan and sends nothing: three SOL/EUR legs, a resting limit buy (GTC), a margin market buy at leverage 2 and a spot market buy, each line ending `as <id>`, the id 17 characters.
- 5a.2. `W$ infra/scripts/mint-with-vaulted-key.sh --execute 2>&1 | tee $CAP/step5-mint.txt`, then type `MINT`. PASS: three `  sent <kind> as <id> -> venue txid <txid>` lines, then `minted 3 leg(s). Nothing here cancels them.` Close the allowlist per §7.3; that step is mandatory.
- 5a.3. FAIL partway: the refusal names the legs sent, with their ids and txids, the leg that failed (`was not sent`, or `failed at submit, and whether the venue took it is unknown`), and the legs not attempted. Do not loop: read Kraken's page, then run the dry-run plan once more, which plans only what is missing.
- 5b.1. The dry run: `W$ ssh -t zcrypto sudo zcrypto-flatten 2>&1 | tee $CAP/step5-dryrun.txt`. The engine is stopped and the kill file in place, and a dry run checks neither.
- 5b.2. PASS, exit 0: `1 resting order(s) seen -- the cancel is account-wide` and `  order SOL/EUR BUY <qty> -- venue txid <the resting leg's txid>`; `  margin SOL/EUR SELL <qty> -- market, reduce-only, ...`; `  spot   SOL/EUR SELL <qty> -- market, ...`; `nothing was sent: this run reads and prints only`. The five reads, the position read with a position known to be there.
- 5b.3. Record the capture verbatim here, in the red button's dry-run row. Its `  margin SOL/EUR SELL` line is the live reading of spec `00111` D5's disposition, the margin position read resolving on this version, as step 3's BTC/EUR line is D1's.
- 5c.1. The id read-back, before anything else touches the resting leg: `W$ kraken open-orders -o json > $CAP/step5-open.json` and `W$ kraken closed-orders -o json > $CAP/step5-closed.json`. For each leg quote the id as sent (the plan's and the sent line's, the wire form), its txid and the `cl_ord_id` Kraken returns, whether each came back unchanged, and whether any leg was refused. It is recorded here as an observation.
- 5c.2. Before step 7, read the fixture's SOL in `W$ kraken extended-balance -o json` to all 10 decimals and record it. A buy's fee is taken in the quote, so it should equal the spot leg's quantity to the last decimal.

### Step 6

Not run: the operating rule forbids a restart while the fixture's margin position is open.

### Step 7: the full press to flat

- 7.1. `W$ ssh -t zcrypto sudo zcrypto-flatten --execute 2>&1 | tee $CAP/step7.txt`, then type `FLATTEN`. Copy the record as in 4.3.
- 7.2. PASS, exit 0. The record's `legs` hold the margin SOL/EUR SELL and the spot SOL/EUR SELL, each sent, answered with a txid and carrying no `error`; the `requests` entries whose `call` is `submit_order` carry in their `params` `reduce_only` true, `leverage` 2 and `account_type` MARGIN on the closer, CASH on the sell, and `time_in_force` `not sent (market order)` on both. `W$ kraken positions -o json` and `W$ kraken open-orders -o json` print nothing, and `W$ kraken extended-balance -o json` shows no SOL above dust.
- 7.3. This is the first live `reduce_only` from this repository. A leverage error on the closer is the unmeasured case `MARGIN_LEVERAGE`'s comment in `cli/engine/flatten.py` names. A spot leg rejected after an accepted one can be settlement lag in `BalanceEx`: read the record and run it once more.
- 7.4. Quote each leg's client order id as the record shows it, minted `FLT<yymmddHHMMSS>-<n>` and sent as minted, and as `W$ kraken closed-orders -o json` shows it, and record whether the two agree.
- 7.5. FAIL, exit 2: the runbook's flatten step 5.
- 7.6. With `W$ kraken positions -o json` printing no position, clear the kill file and start the engine per `infra/runbooks/engine.md#zcrypto-engine-exec-kill-tripped`, inside a gap. `exec-status` shows `restart_hold`, the boot reads `0 position(s)`, and the startup pass cancels nothing.
- 7.7. Record here and in T0159: the first press, which lifts the runbook's first limit.

## For the next pin

Two readings a later pin owes, recorded here because they turn on this version's contents; neither is owed by this version, and neither is collected at its arming.

- **A latent RiskEngine denial on margin funds.** Upstream `ba95d4e72a`, *Refactor RiskEngine order modification checks* ("Reject missing risk inputs and check funds in the account currency"), is not in this build: `gh api repos/nautechsystems/nautilus_trader/compare/70d887545790...ba95d4e72a` reads `ahead`, 4 commits ahead and 0 behind. It is in `2.0.0rc6.dev20260923+18723`, an ad-hoc build on the index: its label and its timing tie it to upstream's `build` run 18723 on `develop` at `3adf5a8dc0c9`, which ran from 13:47:49Z to 14:32:58Z on 2026-09-23 while the wheel answers `last-modified: 2026-09-23T14:32:17Z`, and `compare/ba95d4e72a...3adf5a8dc0c9` reads 36 ahead and 0 behind. Measured offline on 2026-09-24 in scratch environments, not in this repository's lock: a `BacktestEngine` with a MARGIN account holding EUR 1000, the adapter's own `XXBTZEUR` instrument as the real client lists it from `tests/kraken_loopback.py` (quote currency `ZEUR`, `margin_init` and `margin_maint` 0 on all three builds), and one post-only limit BUY of 0.001 at 50000; the fee model is `MakerTakerFeeModel`, on the instrument's own fees on the two older builds and at the loopback's 0.16 %/0.26 % tier on the newest, which requires the rates explicitly. With the adapter's zero margin rates the order is accepted on `2.0.0rc6.dev20260918`, on this version and on `2.0.0rc6.dev20260923+18723`. With a margin rate of 0.5 that last build denies it, `INITIAL_MARGIN_EXCEEDS_FREE_BALANCE: free=0.00000000 ZEUR, margin=2.50000000 ZEUR`, while `2.0.0rc6.dev20260918`, this version, and the same instrument quoted in `EUR` on that build accept it. The instruments are quoted in `ZEUR` and the account holds `EUR`, so on a build carrying `ba95d4e72a` only the adapter's zero margin rates mask the check. Upstream #5067 (open) reports that asset-code split. The next pin carrying `ba95d4e72a` re-reads it in its order-semantics pass: a probe order that comes back `OrderDenied` with that reason is this check, not the venue.
- **When the operating rule lifts.** The rule above holds until a pin carries upstream #5065. On that pin the restart this window skips as step 6 is taken and read: an engine restart inside an inter-cycle gap with a Kraken margin position open, whose boot logs no `Unresolved positions during startup reconciliation` line, leaves `.RestartCount` unchanged, counts the position in its `Reconciliation complete for KRAKEN` line, and books the position at Kraken's entry price, not at 0. The first three reads do not show the fourth: this version's other failure, the boot with the entry price at 0, passes all three. Nothing the engine publishes carries that price directly: the `positions` of a `venue-<HH>.json` record are signed net quantities, and `zcrypto_exec_realized_pnl_eur` takes a position in only once this engine records a fill on its instrument (`_publish_fill` in `cli/engine/executor.py`), when a position booked at 0 shows a realized figure the size of its whole close value. So the reading names where it read the entry price the engine booked on that pin, and the rule stays until a read shows it is Kraken's. On a clean reading the rule comes out of the pages that carry it: `infra/runbooks/engine-procedures.md` (the `engine-restart-margin-position` paragraph and each step that links to it), `infra/runbooks/engine.md` (the restart and converge steps that link to that anchor), `infra/runbooks/drills-order-path.md` (the standing rule, A2's `blocked` precondition, drill D's precondition and restore step, F2's operator step 4 and G's induce step), `infra/runbooks/order-semantics-verification.md` (§1.7, §6's `Unresolved positions` paragraph and §8's closing paragraph) and the Reboots section of `docs/reference/fleet.md`.

## Series

The preceding pass is [`2.0.0rc6.dev20260918.md`](2.0.0rc6.dev20260918.md). The two items that record leaves OPEN, the terminal-event minting rate and the credentialed listing with its fallback, are re-registered above against this version, the one the window and the first arming window hold; that record's own readings stand where they are.
