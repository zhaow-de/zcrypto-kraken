# Fleet pins

The durable record of the image digest each service runs. For the Alloys on ops/capture hosts the pin is a converge-time-only extra-var with **no repo default**, so this file is its only record. Update at every re-pin/converge, **in the same change** — a pin recorded only on a host is one `docker system prune` from unrecoverable.

Several rows share the image repo (`ghcr.io/zhaow-de/zcrypto-capture`) — capture, engine, ops, the NAS archive-pull — each pinned **independently**: match the row to the service, never to the repo.

| service | host | digest (sha256, first 12) | since (UTC) | prior |
| --- | --- | --- | --- | --- |
| capture | zcrypto | `99faf16514e3` | 2026-07-29 07:36 (T0107 payload) | `828128f80cb3` (2026-07-28) |
| capture | zcrypto-red | `6c5151d9f3af` | 2026-08-11 14:13:17 (`.State.StartedAt`; spec `00088` / iter-135 execution-safety-envelope — the secondary's capture bake IS the ENGINE re-pin's canary gate, there being no engine secondary. **Bake started 14:13:17Z**, three full rotation hours 15/16/17; **gate PASSED 18:00Z** — every abort signal clear, `continuity.py` on the pulled copy 0 truncated hours across all 12 pairs (hour 17 folded in once the NAS pull carried it). Prune fired immediately under the new image: **`deleted=528`, `Result=success` — the STRONG form**, not the weak `deleted=0`. Abort signals at T+2 min all clear: 41 parquet within 3 min, `dropped_lines_total` 0, logship cycle stamp 15.9 s fresh, `RestartCount` 0 on capture and Alloy. **RSS baseline 116.0 MiB** (limit 1 GiB) — the T+24 h re-read owed ~2026-08-12 14:13Z. Payload verified IN the pulled image, not assumed from the tag: the image's own CLI lists `exec-status`, label `org.opencontainers.image.revision` = `0daa2c12` (the merge commit), `polars-runtime=default` (AVX — capture hosts never take `-compat`). **Capture on the PRIMARY deliberately stays on `99faf16514e3`**: its own code moved by one comments-only commit since, so re-pinning it would restart live capture on the unbackfillable path for no functional gain.) | `ccedc9dd6bf4` (2026-08-05) |
| engine | zcrypto | `6c5151d9f3af` | 2026-08-12 04:44:51 (`.State.StartedAt`; spec `00088` / iter-135 the execution safety envelope — canary was the secondary's capture bake of this same digest, gate PASSED 2026-08-11 18:00 after rotation hours 15/16/17, prune STRONG-form `deleted=528`, continuity 0 truncated hours across all 12 pairs. Verified by VALUE, not presence: the exec `/metrics` read `gate_level 0`, `armed 0`, `restart_hold 1`, `venue_ok 1`, live `last_evaluation` — the disarmed resting state seeded defaults cannot produce, so the new code ran, evaluated the gate at startup, and latched reduce-only after the restart; `RestartCount 0` (the crash-loop hazard from `_build_engine` rejecting `exec_armed` did NOT fire — config and image moved together), and all six `zcrypto_exec_*` families reach Grafana Cloud 31 s fresh, both directions of the admission trap closed. Verified by outcome: `cycle-08.json` completed 08:01:40Z inside `[B, B+30]` — the first cycle under this image — then `cycle-12.json` clean, zero `failed-cycle-*.json`, and the execution ledger wrote `exec-08.json`/`exec-12.json`. Config-only capture on `zcrypto` deliberately stays `99faf16514e3`) | `ccedc9dd6bf4` (2026-08-06) — **the rollback operand, verified resident on the host at the re-pin** |
| alloy | zcrypto, zcrypto-red, zcrypto-ops, nas | `491b0578c049` (v1.18.0, published 2026-07-20) | 2026-07-27 | `4f6ddc56ffdc` (v1.17.1) |
| ops (timers + liquidations) | zcrypto-ops | `a8cd3a9524eb` on the timers and the five `/usr/local/sbin/` runners (the tape-bars runner is the fifth, new this converge); the `zcrypto-ops-liquidations` container was rolled to the same digest by a MANUAL `docker compose up -d` after the converge (the flag does not roll it — see the bullet) — **read from the container, never the compose file** | 2026-08-10 (spec `00087`: the tape-bars materializer; `config.alloy` edited to admit the new gauges, so `ops_alloy_digest` was passed) | `65402dc67701` (2026-08-07) — **the rollback operand; verified RESIDENT on the host at the re-pin** |
| archive-pull | nas | `620114511f19` (repo pin `nas_capture_image`, same file; running digest unverified) | — | — |

**Bridgehead `zaccess` — apt-pinned native packages, not image digests** (spec `00075`; no Docker on the edge). These are `access_*` role defaults, held with `dpkg --set-selections`; a version bump = unhold → converge with the new pin → the role re-applies the hold.

| package | host | version | since (UTC) | notes |
| --- | --- | --- | --- | --- |
| caddy | zaccess | `2.11.4` (upstream cloudsmith `stable`) | 2026-07-30 | the mTLS edge; `verifier leaf` client-cert pinning syntax confirmed against v2.11.4 source |
| alloy | zaccess | `1.18.0-1` (Grafana apt `stable`) | 2026-07-30 | native (not the containerized `491b0578c049` the other four run); `host="zaccess"` |
| agentboard | zcrypto-ops | `0.4.8` (`@gbasin/agentboard`, npm global as `zhaow`) — repo pin and running version now AGREE | converged 2026-08-05 with the iter-127 ops converge; `0.4.5` had run since 2026-07-30 | **every re-pin is security-relevant** (D2: the mTLS edge is its only auth) — attended, no bake; the unit is spike-gated on `access_ops_agentboard_live`. Read the running version the way the role does — `sudo -u zhaow bash -c 'source /home/zhaow/.nvm/nvm.sh && npm ls -g @gbasin/agentboard --depth=0'`. A bare `npm ls -g` over ssh finds NOTHING here (the package lives under nvm's node, off the non-interactive PATH), so its empty output reads as "not installed" and is not evidence |

Full digests:

- **`a8cd3a9524eb` — spec `00087`, the tape-bars materializer; the `zcrypto-ops` timers and the liquidations container.** `sha256:a8cd3a9524eb2dd613bbe5f899f1dc8a1282d2d139e0ad1c66e469fb84d49ddb`, built from `392cf8b0` — the PR #285 merge commit, so this digest corresponds to a real merged state of `develop` (the 2026-08-07 pin deliberately did not, and that is why this one records the difference). Converged 2026-08-10 with `--limit zcrypto-ops -e ops_alloy_digest=sha256:491b0578c049… -e liquidations_decision=roll-after`.
  **`ops_alloy_digest` was owed and passed**: this iteration edits `infra/ansible/roles/ops/files/config.alloy` to admit eight `zcrypto_tapebars_*` series and the `tape-bars` journal unit, which makes Alloy the subject — the drift assert refuses an ordinary converge after a config edit. Alloy itself stays on `491b0578c049`; only its rendered config changes.
  **Payload verified from the image's own surface before converging, never inferred from the tag** — `zcrypto tick materialize --help` runs inside the pulled image and prints the command's own help, and the image's labels read `org.opencontainers.image.revision = 392cf8b0928dae8c0a8bb6cc24a1d7f21aaf2f05` and `de.zhaow.zcrypto.polars-runtime = default` (the AVX build, correct for the ops i7).
  **No panel regeneration is owed** — this converge adds a timer and touches no generation manifest, so `_check_generation` is unaffected and `ops_panel_timer_hold` was NOT passed. Contrast the 2026-08-07 entry below, where a ladder bump forced regeneration into the same window.
  **The liquidations container was two digests behind** (`193d76be5275`, `StartedAt` 2026-08-01) because the 2026-08-07 converge repinned without rolling. `roll-after` brings it current in one step; the poller re-fetches a 30 h window each cycle, so a converge-length restart self-heals well inside it.
  **VERIFIED BY OUTCOME, 2026-08-10 14:52:00 UTC** — the first tick ran (`ExecMainStartTimestamp`, not `Result`: a never-run unit reports `Result=success` by default, and reading that at 14:51 nearly produced a false pass) and published: `days_written=354 days_skipped=0 days_unsettled=24 days_unhealed=0 days_gap=0 rows=32307 errors=0`. Every figure reconciles — 354 ≈ 12 pairs × ~30 settled days since the tape began 2026-07-08; 24 unsettled is exactly 2 per pair (today and yesterday, inside the 26 h gate); 32,307 rows against a 33,984 maximum is the ~95 % expected with quiet 15m windows omitted. The gauges reached Grafana Cloud by value (`days_written=354`, `errors=0`), dashboards and both alert rules pushed with **no orphaned rules**, and both rules confirmed **evaluating** (`state=inactive health=ok lastError=none`), not merely stored.
  **`liquidations_decision=roll-after` is an ACKNOWLEDGEMENT, not an action** — the role's own refusal message says "nothing here restarts the poller". The converge therefore left the compose file pinning `a8cd3a9524eb` while the container still ran `193d76be5275`. **Rolled manually at 15:00:00Z** with `docker compose up -d` in `/etc/zcrypto-ops`, and verified by outcome rather than by the compose command's return: `{{.Config.Image}}` now reads `a8cd3a9524eb`, `RestartCount` 0, and the poller's own log shows a real cycle — watermarks primed for 10/10 coins, `submitted=6 skipped_at_watermark=1066`. Anyone repeating this converge shape owes that same manual roll; the flag is an acknowledgement of the debt, not its payment.
  **Original verification plan, kept because the reasoning outlives this converge**: `days_written` should be non-zero on the first sweep because it publishes the whole backlog, while **`days_gap` is 0 by construction on a first sweep** — a zero there is not evidence the gap alarm works. The two new alert rules (`zcrypto-ops-tapebars-permanent-gap`, `zcrypto-ops-tapebars-not-advancing`) are pushed only AFTER that first sample exists, because `(no series)` reads as FAIL, never as zero.

- **`65402dc67701` — spec `00085`, the per-quote notional ladder; the `zcrypto-ops` timers only.** `sha256:65402dc6770177d5e1539fc0dfc08c09c8158b3c7e7299fd1496f03a728a4efa`, built from `3bd6064d` on `feat/t0092-quote-aware-ladder` — **not a merge commit**: the PR does not exist yet, because this iteration's rollout precedes its PR. Converged 2026-08-07 12:41 with `--limit zcrypto-ops -e liquidations_decision=roll-after -e ops_panel_timer_hold=true`; **no `ops_alloy_digest`** — `config.alloy` is untouched, so Alloy stays `491b0578c049` and the preview showed no Alloy task changing. `daemon.json` did not diff, so no `daemon_json_ack` was owed. Capture and the engine deliberately untouched on their own pins.
  **Payload verified from the image's own surface before converging, never inferred from the tag** — `inspect.getsource` inside the pulled image confirms `NOTIONALS_BY_QUOTE` is present, `PANEL_QUOTE` is **absent** from `cli/panel/command.py`, `_check_generation` compares ladder membership rather than a single quote, and `notionals_for("BTC") != notionals_for("EUR")` with the BTC rungs at `100/REF, 1k/REF, 10k/REF` for `BTC_EUR_REFERENCE = 55876.28413495087`. The image's own labels read `org.opencontainers.image.revision = 3bd6064d91cc` and `de.zhaow.zcrypto.polars-runtime = default` — the AVX build, correct for the ops i7.
  **This converge FORCES a panel regeneration, so converge and regeneration are ONE window, not two.** The generation manifest's `notionals_eur` becomes `notionals_by_quote`, so `_check_generation` refuses on every hourly tick until the tree is rebuilt — measured before converging, against a tree carrying the old manifest — and `zcrypto-ops-panel-exit-nonzero` would page for each refusal. `ops_panel_timer_hold=true` stops the role re-arming the timer inside the window; it does **not** stop an already-armed timer, so the converge was timed just after a clean `:22` tick to keep the gap tick-free. **Regeneration outcome, 2026-08-07 12:55:29 → 22:26:13Z (9 h 30 m 44 s, 4.37 s/hour — against the drill's 4.71 and verify-replay's 4.80/4.82).** `zcrypto-panel-regenerate`, 13130 MB input, ETA ~551 min incl. its 1.2x margin. The rebuild's own completion line, read **anchored to the run** rather than by `tail -1`: `pairs=12 pairs_out_of_scope=0 hours_written=7836 hours_skipped=0 hours_unsettled=72 hours_unanchored=0 rows=28103636 errors=0`. **The wrong-run trap this file's checklist warns about fired for real**: the timer's `Persistent=true` catch-up tick ran 9 minutes later and logged `hours_written=120 hours_skipped=7836` — plausible, reassuring, and not the rebuild. `7836 + 120 = 7956` reconciles exactly with the tree's parquet count.
  **The NAS deletion was owed NOTHING, and that was measured rather than argued** — `LC_ALL=C comm -13` over both path sets returned **0** NAS-only paths (ops 7956 / NAS 7836; the 120-file difference is ops-only, exactly 10 per pair across all 12, i.e. the catch-up tick's not-yet-pulled hours). This is the first real test of the rewritten checklist's rule that a ladder bump rewrites identical paths and orphans nothing; it held.
  **Verified by outcome**: `infra/scripts/ops-postverify.sh` → ALL PASS (6/6). And the change's actual purpose confirmed on the tree, not inferred — all six `fill_bps_*` columns are **100 % non-null** on both `/BTC` legs where every one was previously null, monotonic in size (ETH/BTC 0.684 → 0.927 → 1.259 bps; SOL/BTC 0.910 → 1.649 → 2.672; BTC/EUR control 0.009 → 0.063 → 0.343).

- **`6c5151d9f3af` — spec `00088` / iter-135, the execution safety envelope; current on the ENGINE (`zcrypto`, 2026-08-12 04:44:51Z) and on `zcrypto-red` CAPTURE (2026-08-11 14:13:17Z).** `sha256:6c5151d9f3af7f9ac197a1f42b4d06c8667ebef0b5dd07146d2c9384c0f957f3`, built from `0daa2c12` (the PR #288 merge). The red-capture run IS the engine's canary — there is no engine secondary — and its bake PASSED 2026-08-11 18:00Z over rotation hours 15/16/17: prune in the strong `deleted=528` form, every abort signal clear, `continuity.py` on the pulled copy 0 truncated hours across all 12 pairs. **Payload verified in the pulled image before staging, never inferred from the tag** — the image's own CLI lists `exec-status`, label `org.opencontainers.image.revision` = `0daa2c12`, `polars-runtime=default` (AVX). **Engine verified by outcome, by value not presence**: the exec `/metrics` read `gate_level 0`, `armed 0`, `restart_hold 1`, `venue_ok 1` — the disarmed resting state seeded defaults cannot produce — with `RestartCount` 0 (the `_build_engine` crash-loop hazard did not fire; config and image moved together), all six `zcrypto_exec_*` families reaching Grafana Cloud, then `cycle-08.json` completing 08:01:40Z inside `[B, B+30]` as the first cycle under this image, `cycle-12.json` clean, zero `failed-cycle-*.json`, and the execution ledger writing `exec-08.json`/`exec-12.json`.

- **`ccedc9dd6bf4` — the iter-127 rollout digest; now current on NO tier, and the rollback operand for BOTH the engine and `zcrypto-red` capture since the 2026-08-11/12 re-pins to `6c5151d9f3af`.** It ran: capture on `zcrypto-red` 2026-08-05 13:30; the `zcrypto-ops` timers the same day; the ENGINE on `zcrypto` 2026-08-06 03:33:55 after the bake gate passed 17:07 (3 rotation hours, every abort signal clear, prune in the strong `deleted=402` form, rotation verified on the pulled copy, `continuity.py` truncated hours 0). Engine verified by outcome: `cycle-04.json` completed 04:01:40.93Z inside the window, and both payload fixes proved themselves live — `zcrypto_engine_cycle_duration_seconds` absent before the first cycle then reading 10.932155 (the lazy registration working), `target_weight` at exactly 10 labels (the departed-label `.remove()` holding). Primary CAPTURE deliberately untouched on `99faf16514e3`: `sha256:ccedc9dd6bf439adec73b2468666afcc8e950179bd82f884cd2fc89fae0ec771`, built from `7762da47` (the PR #268 merge). Carries the two engine gauge-lifecycle fixes and the reconcile `ledger_records` gauge. **Payload verified from the image's own surface before staging, never inferred from the tag** — running `inspect.getsource` inside the pulled image confirms `cycle_duration` is lazily registered, departed target-weight labels are `.remove()`d, and `_totals` counts `ledger_records`. This digest is the **engine's** canary: there is no engine secondary, so it must run as *capture* on `zcrypto-red` and pass an event-coverage bake before the primary's engine may take it.

- **`6b36bf23a0ff` — capture **two steps back** on `zcrypto-red` (its operand 2026-08-04→05, superseded by `ccedc9dd6bf4` at the 2026-08-05 13:30 re-pin and again by `6c5151d9f3af` on 2026-08-11), and the `converge.sh` drill vehicle**: `sha256:6b36bf23a0ffd85a9458dbd77b9b37c8ef028582be6d19932efb1f2e47953968`, built from `b573318c` (the websockets 17.0.1 dependabot bump, PR #262). Payload verified from the image's own surface before converging — `importlib.metadata.version("websockets")` reads **17.0.1** inside the pulled image, not inferred from the tag. Converged **2026-08-04 07:52:05Z** (`.State.StartedAt`; `RestartCount` 0 on capture and alloy).
  **This converge WAS the T0111 attended drill for `infra/ansible/scripts/converge.sh`, and both legs are on the record.** Leg 1 (`--check`): the script ran the preview and stopped itself clean at rc 0 — `ok=201 changed=2 failed=0`, the two changes being the compose pin and its restart handler, with every wave-1 guard exercised live (digest preflight, pair-add order probe, canary parity, pins recording). Leg 2 (the real pass): **the typed-confirm gate refused two agent-driven attempts with rc 3** — "no controlling terminal — the confirm gate needs an attended session" — because agent-harness commands run with pipes and **no controlling terminal at all** (not because backgrounding strips one: a plain foreground agent command has none either). The guard is therefore stronger than a tty-timing accident: **no agent invocation of any shape passes it**, which is the designed property proving itself on the fleet rather than in a test. The converge landed only when the operator ran it in a plain terminal and typed the limit value back.
  **Post-converge, all positive traces read rather than assumed**: all 12 pairs in `starting capture pairs=[…]`, `venue status system=online`, **32 parquet files across 12 distinct pairs** written within 3 min, `zcrypto_logship_dropped_lines_total` 0 with `shipped_lines_total` advancing, `up{job="capture_app"}` 1 for both hosts and both capture dead-men `hc_check_up` 1 in Cloud, zero `quarantined`/`ambiguous`/`merge failed`. The `dropping replayed event` burst at start is the healthy resubscribe replay. A first grep for "subscrib" returned zero and was a **wrong filter, not an absent event** — the daemon logs `starting capture pairs=[…]`; validated before being read as a finding.
  **No primary re-pin is owed by this drill** — the bake gate is only owed if one is wanted, and this is a patch-level dependency bump with no primary-side payload.
- **`c7ed09020fe1` — capture **two steps back** on `zcrypto-red` (its operand 2026-08-04→05, superseded by `6b36bf23a0ff` at red's `ccedc9dd6bf4` converge) AND the engine **prior** on `zcrypto` (its rollback operand since the 2026-08-06 re-pin to `ccedc9dd6bf4`)**: `sha256:c7ed09020fe19d333147a41a75f10ee8ea53e7c431a4ef5c41a58a074ee6ea3a`, built from `f67afb80` (the iter-119 merge). Carries three payloads, each verified from the image's own surface rather than the tag: iter-119's §10 whole-book limits + `"ZEUR"` spot-position reporting, [[T0124]]'s sleeve-occupancy gauges, and spec `00080`'s refactor (`PAIR_KEYS` relocated — this is the converge that discharges that spec's D6 flag). Canary leg 2026-08-02 21:47:13Z; **bake passed** — prune took the **strong** form (`deleted=720`, `Result=success`), 36 of 36 book streams (12 pairs × hours 22/23/00) began at `:00:00.0x`, and red's RSS settled at 136.4 MB, *below* its own old-image 150.0 MB. Engine converged 2026-08-03 04:45:41Z (`.State.StartedAt`, not the command's return time), inside the 04:00→08:00 inter-cycle gap and 44 min after `cycle-04.json` completed at 04:01:48.791Z. **T+24h RSS re-read done 2026-08-03 21:57Z — the bake's named slow-leak residual, discharged: flat on both hosts.** Red plateaued at 144,715,776 B (~144.7 MB; last 6 h moved −12 KB), still below the old image's own 150,978,560 steady state; the primary's capture (untouched on `99faf16514e3`, 5.6 d uptime, `RestartCount` 0) oscillated 172.7–177.0 MB over 48 h with no monotone slope, reading 175,595,520 B mid-range. Each host judged only against its own history (limits differ: primary 2 GiB, secondary 1 GiB). No Phase-4 trip.
- **The primary's CAPTURE deliberately stayed on `99faf16514e3`** — the engine payload needs no capture restart, and not restarting live capture keeps the unbackfillable path untouched. The converge still ran `--tags capture,engine` with capture's digest held at its current value, because the Alloy keep-list lives in the **capture** role: `--tags engine` alone would publish the new gauges while leaving them unadmitted, and the by-value check would have had nothing to read.
- **capture current, `zcrypto` (primary)**: `sha256:99faf16514e3ddc30712c8c63fb4892d7e5f3042823b56935cf0617a3cf2ab44`, built from `3540b0bb`. Carries [[T0102]]'s `req_id` correlation and [[T0106]]'s liveness gauge, both verified from the image's own surface rather than the tag. Canary leg `zcrypto-red` 2026-07-29 00:52:53Z; primary 07:36:13Z after a 6 h 43 m bake (three full rotation hours required; prune took the **strong** form, `deleted=210`).
- capture prior, and the rollback operand — verified still resident on both hosts at the re-pin: `sha256:828128f80cb34a7341394f7aa0bc977207c9b42435b31507b849afc81d4c4224`
- **ops prior, and the rollback operand since the 2026-08-05 converge to `ccedc9dd6bf4` — still running as the `zcrypto-ops-liquidations` container under `roll-after`, so residency is live by definition**: `sha256:193d76be5275913c8d4dffd9aedc06db22052e2ac6623b9fb503df8ed382b4c2` (revision `a7c21438`, spec `00078`) — recorded **before** the converge, per this file's own rule. Pulled and **verified resident 2026-08-01 19:4x**, and the change verified *in* the image from its own surface (`checkpoint.py` present, `verify_replay_incremental` defined, `VERIFIER_VERSION = 1`, and all four new CLI options in `--help`) rather than assumed from the tag. **This converge does NOT pass `ops_alloy_digest`** — `config.alloy` is untouched by spec `00078`, so Alloy is not the subject; the running Alloy stays `491b0578c049`. **The rules push is ADDITIVE — no prune**: one rule added (`zcrypto-ops-verify-replay-backlog-stuck`), none retired.
- **ops two steps back (spec `00077`)**: `sha256:cbf60c852f040b8aca8f75946a42c1d17caf41c5203371944a60b06cfd49a25b` (revision `2fbbb573`) — recorded here **before** the 2026-07-31 10:31 UTC converge, per this file's own rule. **Verified resident on `zcrypto-ops` 2026-07-31 at the pull (10:1x)** — load-bearing, because every ops runner is `--pull never` with no pull task in the role. The change was verified *in* the image from its own surface (`logger.warning` at the D9 comment, and the `verify-replay complete hours=… failed=…` line the runner's `sed` parses) rather than assumed from the tag. **One variable drives two things**: `ops_image_digest` repins the four timer runners *and* the liquidations compose file — which the role renders but never restarts, so after a converge that file points at the new digest while the container still runs the old one until some later `docker compose up` rolls it unobserved. End every ops converge deciding explicitly whether to roll it; **prefer rolling** — the running `liquidations-poll` re-fetches a 30 h window each cycle, so a converge-length restart self-heals (`capture-deploys.md` carries the bound). The earlier text here called that stream *unbackfillable*; that described the shelved Binance WS recorder, not what runs.
- **ops operand history (superseded 2026-08-05 — the operand is now `193d76be5275` above)**: `sha256:cbf60c852f040b8aca8f75946a42c1d17caf41c5203371944a60b06cfd49a25b`, verified still resident on `zcrypto-ops` 2026-08-01. **Rolling spec `00078` back is digest AND template together**: the prior image has no `--state-dir` option, so a digest-only rollback leaves the new template passing a flag the CLI rejects — no summary, `run_ok=0`, and a dark dead-man nightly. Revert the runner template in the same change. One step further back: `sha256:0b2fdcfff6e873b71661fb50cc42c83858394827edc69ca813f5d121deab3d19` (revision `50fc4979`).

**Converge outcome, 2026-08-01 19:51 UTC** (`ok=73 changed=4 failed=0`, `--limit zcrypto-ops`, **no `ops_alloy_digest`**). All four runners re-read from their installed scripts as `193d76be5275`; the checkpoint state dir exists at `0750 zcrypto-data`; the runner passes `--state-dir /state` with a narrow `:rw` mount while `/data` and `/nas` both stay `:ro`. **No handler fired and Alloy was untouched** (`491b0578c049`, `RestartCount` 0) — the 15 skipped tasks are the Alloy block correctly short-circuiting because the digest was not passed. The **liquidations container was rolled deliberately** and resumed with `primed bucket watermarks for 10/10 coin(s)` / `submitted=4 skipped_at_watermark=1106`, i.e. the 30 h re-fetch covered the restart with no gap. Rules push was **additive**: `grafana-push` reported *no orphaned rules*, the folder went 56 → 57, and the new `zcrypto-ops-verify-replay-backlog-stuck` reads back 200 and evaluates `state=inactive health=ok` — which also retires the one risk that could not be checked before pushing, since this is the file's first rule with two Prometheus queries feeding a `math` node.

**Verification is COMPLETE — both nights passed.** The checkpoint starts empty, so night one (sweep 2026-08-02 03:41 UTC) was the full rebuild: `replayed 6348 = hours_total`, `reused 0`, `pending 0`, `run_ok 1`, `audit_mismatches 0`, `failed_hours 0`, `duration 29,874 s` (8.30 h, 4.71 s/hour). Night two (2026-08-03 03:41 UTC) is the run that proves the checkpoint took: `reused 6348`, `replayed 288`, `pending 0`, `run_ok 1`, `audit_mismatches 0`, `failed_hours 0`, `duration 1,114 s` (18.6 min) — **26.8x faster**. Read as a partition rather than a headline, because `run_ok=1` / `failed_hours=0` are identical on the failure path: `reused + replayed = 6348 + 288 = 6636 = hours_total`, `reused` equals night one's *entire* `hours_total` (so every hour it built is reused, not a subset), and `replayed = 288` is exactly 12 pairs x 24 h. **No alert watches `reused`** — a sweep that silently stops reusing reads healthy on every rule while delivering none of the change, which is why this check is deliberately human and why the PR waited on it.

The 2026-07-28 operand `sha256:e5a44e1cb138bcc0d6291fc8be76d00375ce69512e63da532389d3c4821bd8b7` is also still resident, one step further back. Residency is the load-bearing half: every ops runner is `--pull never` and the role has no pull task, so a recorded digest that is no longer on disk is not a rollback path. `ops_image_digest` has no repo default by design, so this row IS the record — it was read from the host because **this file carried no ops row at all until then**, which by its own opening rule meant the operand existed nowhere in the repo.

- **engine current**: `sha256:6c5151d9f3af7f9ac197a1f42b4d06c8667ebef0b5dd07146d2c9384c0f957f3`, converged **2026-08-12 04:44:51Z** (`.State.StartedAt`; `RestartCount` 0) — spec `00088` / iter-135, the execution safety envelope; full evidence in its own bullet above. Canary-baked as *capture* on `zcrypto-red` first, because there is no engine secondary and that bake IS the engine's gate.
- **engine prior, and the rollback operand — verified resident on `zcrypto` at the 2026-08-12 re-pin** (`docker image ls --digests` on the host, read at the re-pin): `sha256:ccedc9dd6bf439adec73b2468666afcc8e950179bd82f884cd2fc89fae0ec771` (converged 2026-08-06 03:33:55Z; carries iter-127's two engine gauge-lifecycle fixes and the reconcile `ledger_records` gauge). Further back, each step shedding another payload generation: `sha256:c7ed09020fe19d333147a41a75f10ee8ea53e7c431a4ef5c41a58a074ee6ea3a` (2026-08-03 04:45:41Z — iter-119's §10 whole-book limits + `"ZEUR"` reporting, [[T0124]]'s sleeve gauges, spec `00080`'s refactor), then `sha256:99faf16514e3ddc30712c8c63fb4892d7e5f3042823b56935cf0617a3cf2ab44` (2026-08-02 12:13), then `sha256:e5a44e1cb138bcc0d6291fc8be76d00375ce69512e63da532389d3c4821bd8b7`, then `sha256:8574aff805c0ab6a22d82b3a6dd942c90f194f79d552412b0be6c15e1971a8ad`. **Roll back ONE step (`ccedc9dd6bf4`), never further** — one step sheds the execution safety envelope; two would also shed iter-127's gauge fixes.
- **Engine and capture NO LONGER share a digest on `zcrypto`** *(they did between 2026-08-02 12:13 and 2026-08-03 04:45)*. The engine runs `6c5151d9f3af` (since 2026-08-12); capture deliberately stays on `99faf16514e3`, because iter-119's, iter-127's and spec `00088`'s payloads are all engine-side and restarting live capture would spend risk on the unbackfillable path for nothing. They are pinned independently and the engine row governs the engine — a capture re-pin never implies the engine follows, and vice versa.
- alloy (all four hosts): `sha256:491b0578c04983fd54fe99b587b6fab4404dc46d0dc16677bd6b00cc1140b308`
- alloy prior (rollback target, all four): `sha256:4f6ddc56ffdcf8a6316748fc5162972e20cb301523cac1bb4a31957df733ae9b`

**A corrupted Alloy pin was found on `zcrypto-ops` at the 2026-07-31 pre-flight, and repaired by that converge.** `/etc/zcrypto-ops/alloy/compose.yaml` read `grafana/alloy@sha256:0b2fdcff…` — the **ops zcrypto-capture** digest pinned as the Alloy image, i.e. a past converge passed `ops_alloy_digest` the value of `ops_image_digest`. The running container was correct (`491b0578c049`), so nothing was broken *yet*: the damage was armed, not fired. Any `docker compose up -d` in that directory would have tried to pull an Alloy digest that does not exist and taken the ops host's telemetry dark. **Nothing catches this**: the drift assert compares `config.alloy`'s checksum and only runs when `ops_alloy_digest` is *omitted*, so a converge that passes the wrong digest renders it silently and exits 0. Checked at the same time: `zcrypto`, `zcrypto-red`, and `nas` all had file-vs-running agreement — the corruption was isolated to this one host and file. The repair rode the spec-`00077` converge, which had to pass `ops_alloy_digest` anyway (it edits `config.alloy`); passing the authoritative **running** digest re-rendered the file correctly. Read the running value with `docker inspect --format '{{.Config.Image}}' grafana-alloy`, never from the compose file — the file is what was wrong.

**Converge outcome, 2026-07-31 10:31 UTC** (`ok=83 changed=6 failed=0`, `--limit zcrypto-ops`). All four runners re-read from their installed scripts as `cbf60c852f04`. `archive-pull` proved the new image at its 10:42 tick: `Result=success`, `ExecMainStatus=0`, `reconcile complete … failures=0`, `reconcile.prom` mtime advanced to 10:55:11. Alloy took an HTTP **config reload**, not a container recreate — `StartedAt` stays 2 days old and `RestartCount` 0, which is correct behaviour here, not a failed converge. The **liquidations container was rolled deliberately** rather than left pinned-but-not-rolled: it resumed with `primed bucket watermarks for 10/10 coin(s)` and `submitted=4 skipped_at_watermark=1363`, i.e. the 30 h re-fetch covered the restart and no data was lost. `panel-materialize` proved it at 11:22: `ExecMainStatus=0`, `panel materialize complete pairs=10 hours_written=10 hours_unanchored=0 rows=36000 errors=0`, with no generation-guard refusal (`SCHEMA_VERSION` was untouched by this image, so none was owed). `verify-replay` and `verified-replay` carry the new digest but do not run until 2026-08-01 03:41 / 05:23 UTC. Both new alert rules read back 200. The retired uid was **left live on purpose** until the first post-converge sweep, because `delta()` cannot see breakage already present in a series' first sample. **That sweep was forced at 2026-07-31 21:48:56 and completed 2026-08-01 05:49:23 — 8 h 00 m for 6,000 hours (4.80 s/hour — the **second** full-sweep rate on record, matching [[T0114]]'s 4.82; the only other figure, 4.08, is the *windowed subset* T0114 documents as understating the true rate by 18 %, i.e. a known non-match rather than a third data point).** Gate read from Cloud, not just the host file: `failed_hours=0`, `run_ok=1`, `hours_total=6000`, zero failed-hour WARNINGs, sweep summary `6000 ok, 0 failed`. **Prune executed 2026-08-01, between the 05:49:23 sweep completion and this record's 06:08:10Z commit**: orphan list was exactly one uid, `DELETED orphaned rule zcrypto-ops-verify-replay-exit-nonzero`, that uid now **404**, both replacements 200, folder down 57 → 56 rules, nothing firing, both new rules `health=ok`. Spec `00077` is fully live.

**Rolling this back.** Re-converge to `0b2fdcfff6e8` (recorded below, resident). Two consequences, both checked rather than assumed: (1) **the new alerting keeps working** — the `verify-replay complete hours=… ok=… failed=…` line the runner parses predates this spec (added by `54e10d12`, spec `00051`), so `failed_hours`/`run_ok` keep publishing on the old image; what reverts is D9, so failed hours log at ERROR again and the ops ERROR-logs catch-all resumes firing nightly. (2) **The pruned alert rule is NOT recoverable from `develop`** — `grafana-push.sh` only upserts, and that rule left `alerts.yaml` in `b6ea3d3a`. Its YAML survives at `git show b6ea3d3a^:infra/grafana/alerts.yaml` (the `zcrypto-ops-verify-replay-exit-nonzero` block); restoring it means re-adding that block and pushing.

**T+24 h RSS residual for the 2026-08-05 `ccedc9dd6bf4` capture re-pin — DISCHARGED 2026-08-08 18:26Z (T+77.0 h). No leak; a warm-up that has settled.** This paragraph twice said the wrong thing before it said the right one — first "no leak, closed" on 12 h of flat floor (refuted at review), then "converging, not dischargeable" pending one clean read. That read is now in, and the residual is closed on it rather than on the earlier reasoning.

- **Subject: `zcrypto-red`** (the only host running `ccedc9dd6bf4` as *capture*), `RestartCount` 0, `StartedAt` 2026-08-05T13:30:00Z, current RSS 148,512,768 B (141.6 MiB).
- **The floor rose +10.12 MiB across 48 restart-free hours** — 6 h floors, oldest → newest: `131.08 · 131.70 · 132.25 · 138.84 · 139.04 · 139.16 · 138.23 · 141.26 · 141.20` MiB. A leak raises the floor and a sawtooth does not, so on the discriminator this rollout relies on, the full window reads leak-shaped. The floor slope (+5.06 MiB/day) corroborates the 48 h `deriv` (+5.27 MB/day) to under 1 %, so that deriv was trend, not sawtooth phase.
- **Do not discharge on a short flat stretch.** The rise is two discrete steps — +6.59 MiB landing 2026-08-06 15:20–18:20Z and +3.03 MiB landing 2026-08-07 15:20–18:20Z, same clock band, 24 h apart. Any window shorter than that period cannot tell a plateau from the trough between steps; the 12 h of flatness that first justified discharge was the trough.
- **The two-point forecast that predicted a ~144 MiB asymptote OVER-predicted, and saying otherwise would rebrand a miss as a hit.** It extrapolated the FLOOR series (131.08 … 141.20) to ~144 MiB at +5.3 MB/day, ~166 days of headroom. What happened instead: step 3 never came at all, and the floor settled at **141.4 MiB** — ~2.6 MiB *below* the prediction. The instantaneous 144 MiB readings are the sawtooth PEAK, a different quantity; quoting them as "matching ~144" swaps peak for floor to manufacture agreement.
- **The primary is a CONTROL, not a subject** — its capture was deliberately left on `99faf16514e3` (only its ENGINE took `ccedc9dd6bf4`), verified from the container. Its floor is flat at 146.18 → 145.46 → 145.31 MiB, 24 h slope −0.55 MB/day. Cite it only as a same-workload control on the *old* image; cross-host **absolute** comparison stays forbidden (limits differ: 2 GiB vs 1 GiB), and the earlier draft's use of the primary's 7-day 145.31–172.56 MiB range as a "normal envelope" was wrong twice over — it spans a regime shift (a −19.3 MiB step landing 2026-08-05 21:20–08-06 03:20Z), and a swing envelope cannot excuse a floor rise in any case.
- **DISCHARGED, on a band-avoiding pair rather than on a window that straddles the band.** Step 2 is a **~4 h RAMP, 2026-08-07 13:20 → 17:20Z** (1 h floors `138.23 · 139.92 · 140.43 · 141.26 · 141.43`), not the point event earlier drafts recorded — so the step-3 anniversary is a *band*, 08-08 13:20–17:20Z. Two 10 h floors that each avoid it entirely: **before 141.293 MiB** (ending 08-08 13:20Z) and **after 141.316 MiB** (ending 08-09 03:20Z) — **Δ +0.023 MiB**, against step 2's own +3.20. No third step; the daily cadence is broken. Corroborated by 36 h flat inside 0.25 MiB.
- **"No leak" as the bound it actually is, not a bare assertion.** Least-squares over the clean floors: **+0.068 ± 0.057 MiB/day**, 2σ upper **0.182 MiB/day** — 3.6 % of the 5.06 MiB/day that raised the alarm, and 13+ years to the 1 GiB limit at that upper bound. A leak slower than that is not excluded and does not matter on a container re-pinned every few days.
- **Same digest, second workload, independently flat** — `ccedc9dd6bf4` also runs as the **engine** on the primary: 12 h floor 539.1 MiB, peak 565.9, `deriv[24h]` −4.42 MiB/day, `RestartCount` 0. Two unrelated workloads on one image, both flat. **But do not carry capture's 13.8 % headroom across to it**: the engine sits at ~**53 %** of its own 1 GiB limit, 4× the RSS with a quarter of the margin, and it is the process holding the live trade key.
- Capture's sawtooth amplitude is **~3.47 MiB** (floor 141.29 → `max_over_time[24h]` 144.76), not the ~2.7 a single instantaneous sample suggested.
- **The instrument matters more than the number, and it failed FOUR times here — the fourth inside the correction itself.** (1) The original verdict read 12 h of flat floor inside a 24 h step cycle: a trough. (2) The corrected trigger specified a `[6h]` read after 18:20Z, a window that straddles the band and reports the pre-step value regardless. (3) A scheduled watch carrying that query returned "asymptote confirmed" — right answer, instrument incapable of detecting the alternative. (4) The discharge then claimed its deciding window sat "entirely past the anniversary" when it **overlapped the band by 54 minutes**, on a step-2 timing the same paragraph gave two different ways, neither matching the data. Every failure was a window misaligned with the event; not one was a wrong arithmetic result. **The rule: a floor read must sit ENTIRELY outside the event band — and the band is a RAMP, so measure its width before trusting any clearance.** [[T0131]] resolved.

**Rollout record — 2026-07-28 capture re-pin.** Secondary converged 2026-07-27T23:58:41Z, primary 2026-07-28T08:04:29Z; the bake ran 8 h 06 m between them. Gate evidence at the primary's word: 0 missing / 0 truncated hours over 7 h x 12 streams on the pulled copy (worst stream 0.0074% against the 0.1% bar), all 12 pairs flowing, `RestartCount` 0 on capture and alloy, `up{job="capture_app"} == 1` for both hosts in Cloud, `hc_checks_down_total` 0, RSS flat (+0.14% over 4.2 h). **The prune ran in the WEAK form (`deleted=0`)** — the secondary's archive begins 2026-07-14 and the 14-day cutoff was 2026-07-14, so the deletion path was never exercised; accepted explicitly by the owner at both the gate and the re-pin. One abort row read red and was discounted on the owner's word: `zcrypto_logship_last_success_timestamp_seconds` is stale whenever logging is quiet, which is a defect in the signal rather than the image ([[T0106]]).

Measure a running pin with `docker inspect <name> --format '{{.Config.Image}}'` — always `.Config.Image`, never `.Image` (host-dependent under classic storage; see the `/zcrypto-bump-alloy` skill).
