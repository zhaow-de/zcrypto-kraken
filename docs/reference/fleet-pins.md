# Fleet pins

The CURRENT pin and rollback operand for every service — a **state file, not a changelog**: every row is re-trued at each re-pin/converge **in the same change** (a pin recorded only on a host is one `docker system prune` from unrecoverable), and each converge's evidence — checks read, values quoted, bake form — lives in that update's **commit message**, so `git log --follow` on this file is the deploy chronicle. For the Alloys on ops/capture hosts the pin is a converge-time-only extra-var with **no repo default**, so this file is its only record.

Reading rules:

- Every real converge is one machine line in `deploy-log.jsonl` beside this file, appended by `infra/ansible/scripts/converge.sh` — re-true a row from that line, never from memory
- Measure a running pin with `docker inspect <name> --format '{{.Config.Image}}'` — always `.Config.Image`, never `.Image` (host-dependent under classic storage; see the `/zcrypto-bump-alloy` skill).
- Read the running digest from the **container**, never the compose file — a compose file has pinned a wrong image while the container ran the right one (the 2026-07-31 ops-Alloy find), and the drift assert cannot see that failure mode.
- Several rows share the image repo (`ghcr.io/zhaow-de/zcrypto-capture`) — capture, engine, ops, the NAS archive-pull — each pinned **independently**: match the row to the service, never to the repo.

## Current pins

| service | host | digest (sha256, first 12) | since (UTC) | rollback operand (verified resident at the re-pin) |
| --- | --- | --- | --- | --- |
| capture | zcrypto | `6ece9ceb1c18` — revision `8f4ac521`; carries [[T0037]]'s three residual detectors (`3fb8f4f5` + `de9025f2`'s clock-offset unit) and `00108`'s Alloy keep-regex admission. **The secondary's bake PASSED and the primary followed, so the capture pair is CLOSED on this digest** — bake 16:15:21Z→20:00:00Z (hours 17/18/19; hour 16 partial and excluded), prune in the strong form (`deleted=624`), abort signals clear at three samples plus `StartedAt` unchanged across the window, and `continuity.py` on a PULLED copy reading **0 truncated hours / 0.0000% gap** over all 12 streams. Converged `--tags capture`, which is load-bearing: this converge did NOT run the engine play, so it did not move the engine. The engine took this same digest separately at 21:58:39Z that evening — see its own row; do not read this line as the engine's current state. **Its past-dated counter reads 0** — see the secondary's row for why that is informative rather than merely clean | 2026-09-01 20:07:53 | `08f6abb379a7` (2026-08-28, verified resident at the re-pin) |
| capture | zcrypto-red | `6ece9ceb1c18` — revision `8f4ac521`; carries [[T0037]]'s three residual detectors (`3fb8f4f5` + `de9025f2`'s clock-offset unit) and `00108`'s Alloy keep-regex admission. **BAKE PASSED 2026-09-01T20:00:00Z and the primary followed at 20:07:53Z, so the pair is CLOSED on this digest.** Re-pinned 16:15:21Z, hour 16 partial and excluded, three FULL rotation hours 17/18/19. All three gate events met: prune fired by hand in the STRONG form (`deleted=624`, not the weak `deleted=0`), the hours, and abort signals clear at three samples with `StartedAt` unchanged across the window — which is what rules out a signal that tripped and recovered between them. **Known open on this digest, and NARROWED by the primary's leg on the same day**: the past-dated-hour detector read **1** within 4 s of start here (`SOL/BTC trades hour=15:00Z`) — benign restart replay, not a bogus stamp — while the primary, restarted 20:07:51Z onto the same digest, read **0**. The discriminator is whether the sweep had already finalized the previous hour: here it had not (hour 15 finalized 16:23:41, after the 16:15:21 restart), there it had (hour 19 finalized 20:02:30, before the 20:07:51 restart). `_recover` seeds the floor from finals only, skipping `.part`/`.held`, and the late-event guard drops anything below it before the counting branch runs. **So this is NOT a property of every restart** — it is conditional on restart timing against the sweep, and its own alert cannot see it either way because `increase()` is blind to a step in a series' first sample | 2026-09-01 16:15:22 | `08f6abb379a7` (2026-08-28, verified resident at the re-pin) |
| engine | zcrypto | `6ece9ceb1c18` — revision `8f4ac521`, carrying **spec `00106`'s `zcrypto engine flatten`** and **`00108`'s `rest-hold` plan mode**, the two payloads this window existed for. Converged `--tags engine` alone, which is correct here **because the capture leg had already landed the Alloy keep-regex admission** (deployed config matched repo at converge time) — re-running `capture` would have restarted live capture a second time for nothing. Still **DISARMED**: `zcrypto_exec_armed` reads 0 and `zcrypto_exec_kill_tripped` 0 by metric after the restart (read by METRIC, never by dumping the container env, which carries the live trade key); no arm file; arming stays the probe checklist's separate owner-worded act. **The engine's proof-by-outcome is NOT the converge's exit** — it is the next `cycle-HH.json` landing with `completed_at` inside `[B, B+30 min]`, and the next boundary is 00:00Z, so that check falls after this rollout's PR | 2026-09-01 21:58:39 | `08f6abb379a7` (2026-08-28, verified resident at the re-pin) |
| alloy | zcrypto, zcrypto-red, zcrypto-ops, nas | `491b0578c049` (v1.18.0, published 2026-07-20). **Container memory caps differ by host and are load-bearing**: `zcrypto-ops` runs **1 GiB** (`ops_alloy_memory_limit`), the other three 512m, and `zcrypto-fleet-alloy-memory-headroom` divides by these same numbers. **`GOMEMLIMIT` is 0.9 of the cap on every host and moves with it** — `ops_alloy_gomemlimit` on ops, the `460MiB` literal on the other three — which is what the rule's 0.9 bar means; the ratio is pinned by `test_gomemlimit_is_the_same_fraction_of_the_cap_on_every_alloy_host`. A cap or `GOMEMLIMIT` change is applied by `docker compose up -d`, never by a converge alone | 2026-07-27 | `4f6ddc56ffdc` (v1.17.1) |
| ops (timers + liquidations) | zcrypto-ops | `6ece9ceb1c18` — revision `8f4ac521`; the same image the capture hosts run; taken here without a bake because this tier is outside the canary regime. Carries the reconcile ledger-scan gauge, the fleet memory rules' first payload, and [[T0037]]'s two residual detector counters (`3fb8f4f5`). The liquidations container roll is always a separate act — see Standing constraints; `roll-after` was decided AND the roll was applied on this re-pin: `docker compose up -d` in `/etc/zcrypto-ops`, container recreated onto `6ece9ceb1c18` at 14:28:58, `RestartCount 0`, read back from `{{.Config.Image}}` | 2026-09-01 14:26:42 | `08f6abb379a7` (2026-08-28, verified resident at the re-pin) |
| archive-pull | nas | `ee5ba1d92b46` — the `-compat` build of `8f4ac521`, a **`develop` build**, which **retires the interim `feat/t0028-nas-pull-incremental-verify` exception the previous pin carried** — every rollout from here pins a `develop` build. Carries [[T0037]]'s two residual detector counters (`3fb8f4f5`). Repo pin `nas_capture_image` (`infra/ansible/host_vars/nas/vars.yml`), the one surface whose pin is a committed file — it is NOT settable by an extra-var, and `-e nas_capture_image_digest=…` is silently accepted as an unused extra var while the converge re-pins nothing (measured 2026-09-01). The image reaches compose through `.env`'s `CAPTURE_IMAGE`, never inline in `compose.yaml`, so read the pin back from `.env` or the container, never from the compose file. This converge RECREATED the container and therefore paid a cold gate-export replay | 2026-09-01 14:48:03 | `38fd9d703749` (2026-08-28, verified resident at the re-pin) |

**Non-image pins.** `zaccess`'s own apt packages — `caddy` and `alloy` — are **deliberately absent from this file**, and their absence is the record. They come from third-party apt repos we do not control; since 2026-08-20 the access role installs them unversioned and actively clears any `dpkg` hold, so there is no pin to record and nothing here could be a rollback operand. A version written down anyway would have **no producer and no consumer** — nothing asserts it, nothing updates it, and it would drift into a confidently-wrong number the first time anyone ran `apt upgrade`. Read the installed versions off the host when you need them (`dpkg-query -W alloy caddy`); the role's own note carries the reasoning.

| package | host | version | since (UTC) | notes |
| --- | --- | --- | --- | --- |
| agentboard | zcrypto-ops | `0.4.23` (`@gbasin/agentboard`, npm global as `zhaow`) | 2026-08-26 | **every re-pin is security-relevant** (the mTLS edge is its only auth) — attended, no bake. **The unit IS live**: `access_ops_agentboard_live` is `false` only as the role default; `host_vars/zcrypto-ops` sets it `true`, so the service is enabled and started and reachable at `tmux.zaccess.zhaow.me` behind mTLS alone. **A pin recorded here is not a pin in the runtime.** No task and no handler in the role restarts agentboard — the operator owns that call, and an automatic one would drop whoever is on the web terminal mid-play. That is NOT the same as a converge never restarting it: the unit declares `Requires=wg-quick@zaccess0.service`, and a changed tunnel conf notifies `restart wg-quick@zaccess0`, which systemd propagates. Absent that path the process keeps executing whatever it exec'd at start. `npm ls -g @gbasin/agentboard --depth=0` under nvm reads the INSTALLED package, never the running one; to read what is actually executing, hash `/proc/$(systemctl show -p MainPID --value zaccess-agentboard.service)/exe` against the binary inside the pinned version's `@gbasin/agentboard-linux-x64` tarball — the unit ExecStarts `/usr/local/sbin/zaccess-agentboard-start`, which execs the server itself, so MainPID IS the server. The attended upgrade recipe is the agentboard bullet in `fleet.md` |

## Standing constraints — they outlive any single converge

- **The NAS runs only `-compat` builds** — an AVX build is a silent `Illegal instruction` on the Atom (T0029); prove `runtime=compat` by **running** polars in the pulled image, never by reading the label.
- **Every ops runner is `--pull never`** with no pull task in the role: an operand that is not **resident** on the host is not a rollback path — verify residency (`docker image ls --digests`) at every re-pin.
- **`ops_image_digest` also repins the liquidations compose, which the role never restarts**: `liquidations_decision=roll-after` is an acknowledgement, not an action — the manual `docker compose up -d` in `/etc/zcrypto-ops` is owed after every ops converge that repins it, and verified from the container afterwards.
- **EVERY NAS converge that recreates the container replays the whole gate-export — not only a fingerprint-invalidating one.** The cache is `--cache /tmp/gate-cache.json` (`infra/nas/pull-entrypoint.sh`) and `/tmp` is on none of the container's mounts (`/keys`, `/app/zcrypto.toml`, `/textfile`, `/archive`, `/opt/pull-entrypoint.sh`), so `-e nas_apply_compose=true` wipes it and the next cycle pays a cold replay whatever the image fingerprint says. A fingerprint change forces the same replay without a recreate. The cold export measured **3505 s** over 292 cycles (2026-08-28), against 3363 s over 280 cycles two days earlier and an older ~627 s on a much smaller journal. **The journal reached ~316 cycles by 2026-09-01** (read live off `textfile/gate.prom` as `cache_hits + cache_replayed`, which is how to re-measure it without triggering a replay), so at the measured **12.0 s/cycle** the window is now **~3800 s ≈ 63 min** — size converge windows against a figure re-derived that way at planning time, never against the newest one written here, which is stale by construction the moment the journal grows. **The `zcrypto-gate-verify` dead-man tolerates it** — measured green throughout a post-recreate replay (2026-08-29), so the delayed ping is not a paging event; re-check if the journal grows much further.
- **The secondary's capture bake IS the engine's canary gate** — there is no engine secondary; the engine role's assert cites this file for that fact.
- **A single-host capture re-pin costs ~zero data while the other host is healthy** — a per-host gap is healed by splicing the other host, and a healed hour books only whatever its splice leaves UNFILLED; the real constraint is never restarting **both** capture hosts close together — that books outright as `both_streams_silent`. Measured 2026-08-20: a primary restart's own gap was 4.3–4.8 s per stream, covered continuously by the secondary, leaving nothing unfilled.

## Full digests — current pins and their operands only; everything older is in this file's git log

- `6ece9ceb1c18` = `sha256:6ece9ceb1c181888daf403329d567041ac3481ce7926d03eb32d137d30a7e912` — **capture (both hosts) + ops + ENGINE current** — one digest now runs every tier but the NAS; the AVX build of `8f4ac521`. Carries [[T0037]]'s three residual detectors and `00108`'s Alloy keep-regex admission.
- `ee5ba1d92b46` = `sha256:ee5ba1d92b461e74859ff766c4992f791021be605138796dc8ac962f64506470` — **NAS current**; the `-compat` build of the same `8f4ac521`. The one pin whose committed source is a repo file (`nas_capture_image` in `infra/ansible/host_vars/nas/vars.yml`), **so this entry and that file must agree** — they do as of 2026-09-01.
- `08f6abb379a7` = `sha256:08f6abb379a7f16215456a97009d5ca0a5f8e2cc88725407e2f0135b422f9eec` — **the capture + ops + engine rollback operand**, no longer current anywhere as of 2026-09-01 21:58Z; revision `eb6a503a`, AVX build; carries `ws_idle_timeout_ms=0` (spec `00101`). One digest in two roles at once, which is why an image prune on the primary correctly removes nothing.
- `38fd9d703749` = `sha256:38fd9d70374939d2f82b6eaeac3ab03ee12b80bb299a643e9c01cf93378c1b0b` — **the NAS rollback operand**; the `-compat` build of `28d32463`, an interim build off T0028's feature branch. Superseded as NAS current by `ee5ba1d92b46` on 2026-09-01, which retired that interim exception.
- `f70997320492` = `sha256:f70997320492441fbe527ad60ac0c2f93a622728b7e49664d1cc2a95ef003476` — **the ENGINE rollback operand only**; spec `00100` (the engine migrates to nautilus v2), revision `3671b239`, AVX build. It stopped being capture's and ops' operand on 2026-09-01.
- `491b0578c049` = `sha256:491b0578c04983fd54fe99b587b6fab4404dc46d0dc16677bd6b00cc1140b308` — Alloy v1.18.0, the current pin on all four hosts.
- `4f6ddc56ffdc` = `sha256:4f6ddc56ffdcf8a6316748fc5162972e20cb301523cac1bb4a31957df733ae9b` — Alloy v1.17.1, the rollback operand; verified resident on both capture hosts 2026-08-26.
