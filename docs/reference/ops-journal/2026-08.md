# Operations journal — 2026-08

## 2026-08-30 — all-clear

window 24 h to 2026-08-30 06:23Z · alerts none · checks all pass (8/8) · logs 0 ERROR/CRITICAL lines, 1201 WARNING on zcrypto/capture and 601 on zcrypto-red/capture · dead-men 0 down via Grafana, 10 read directly · deploys `site.yml --limit nas` at 2026-08-29T06:52Z · actions none, no alert fired and nothing reached the prepared tier · follow-ups the capture WARNING bursts below; the Slack half of this pass could not run.

**The first pass. Two things it found that no alert would have.**

*The capture WARNING volume arrives in bursts, and nothing explains them yet.* Both hosts emit `dropping replayed event` and `dropping late event` from `segment_writer`, but not continuously — `sum by (host) (count_over_time({container="capture", level="WARNING"}[1h]))` over the window shows exactly two buckets: 08-29 13:00 (zcrypto 600, zcrypto-red 601) and 08-30 05:00 (zcrypto 601, zcrypto-red none). Nothing in the other 22 hours. The rollout skill attributes these lines to post-restart resubscribe replay, and that is not what this is: `increase(zcrypto_capture_resubscribes_total[24h])` and `..._resubscribe_errors_total[24h]` are **0** on both hosts, and `max_over_time(zcrypto_capture_book_desynced[24h])` is **0**. Integrity is intact — `increase(zcrypto_capture_gap_seconds_total[24h])` is **0** on both hosts and the gate streak reads 50 — so the writer's dedupe and late guards are doing their job on venue-side replays and no hour lost rows. What is unexplained is the shape: why a ~600-line burst at all, why 16 hours apart, and why the primary burst alone at 05:00. Recorded, not chased: no metric moved and no page is owed.

*The Slack half ran late, after the fleet read.* No Slack MCP server was configured when the pass began — `mcpServers` was empty at both user and project scope, so `#zcrypto` had no tooling behind it and the first attempt to name the gap called the channel "unreachable", which understated the cause. The account-level connector was enabled mid-session; its tools bind at session start, so they arrived one session later. The summary was then posted (`p1788075796917629`) and tomorrow's trigger scheduled for 2026-08-31 08:17 CEST, carrying the WARNING-burst question forward for a second data point. The two SCHEDULED REMINDER sections (`reference-data.md#refdata-sweep-due`, `ops.md#healable-threshold-rederivation-due`) were still not evaluated: at that hour both were calendar triggers delivered as Slack messages with nothing behind them the repo could read, and none had come due in this window. The summary paragraph is the first line of this entry.

**A second read at 17:58Z, after the instrument changed under it.** `iter-157` merged this afternoon (PR #357), so this is the first pass in which `## Reminders` and the dead-man `descriptions:` line exist at all. Exit 0, all-clear, 8/8 fleet checks pass. Both reminders read `ok` — the refdata sweep due 2026-09-04, five days out, computed from the register's own last re-confirmation row rather than from a message; the healable counter unchanged in the window, with `resets()` guarding a ledger correction from reading as movement. All ten dead-man descriptions carry a resolving runbook link and no internal vocabulary. **The morning's "nothing behind them the repo could read" describes the world before that merge, and is why the sentence above is written in the past tense.**

*The burst analysis holds, from a window that slices it differently.* This pass reads `zcrypto/capture WARNING: 601` — and its 24 h window starts at 17:58Z yesterday, so it excludes the 08-29 13:00 burst entirely and contains only the 08-30 05:00 one, which the morning measured independently as `zcrypto 601, zcrypto-red none`. The two readings agree exactly on that burst without sharing a window, which is the strongest thing said about it so far. Still unexplained, still not chased: no metric moved.

*The demotion is merged and is not deployed, and the count is the evidence.* `iter-157` moves the three reconnect-replay drops to INFO in source, but the capture daemon runs from a container image that has not been rolled, so 601 is what a pass reads today and will keep reading until it is. That rollout rides on [[T0037]]'s existing trigger, the attended capture rollout of spec 00103. **No Slack message was posted for this second read and tomorrow's trigger was not re-armed** — the morning already scheduled 2026-08-31 08:17 CEST, and a scheduled message cannot be deleted through the API, so a second one would put two pings in `#zcrypto` tomorrow with no way to withdraw either.
