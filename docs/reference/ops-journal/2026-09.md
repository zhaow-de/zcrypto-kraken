# Operations journal — 2026-09

## 2026-09-07 — attention

window 72 h to 2026-09-07 09:25Z · alerts none · fired and cleared `zcrypto-nas-archive-pull-errors`, `zcrypto-fleet-daemon-restarted`, `zcrypto-nas-archive-pull-stalled`, `zcrypto-gate-exporter-stale`, `zcrypto-hcio-watchdog`, `zcrypto-reconcile-source-lag` · checks all pass · logs 2 ERROR/CRITICAL lines, 22 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys zcrypto-red, zcrypto · reminders refdata sweep due in 27 days (last sweep 2026-09-04), OWED healable re-derivation: counter moved ~+1301.0 s in 72 h · actions none autonomous; the healable recount's first step classified **prepared** and was not run · follow-ups the prepared recount, below.

**The window is three days, and the eight days before it are not a missed pass.** The owner paused the routine for drills run back in time, and set this window at three days. That re-reads everything a wider look shows: an 8-day report lists eighteen rules firing and clearing, including `zcrypto-capture-all-streams-silent` on both capture hosts and `zcrypto-fleet-daemon-restarted` across all four — the signature of deliberate exercises, not of a fleet coming apart unwatched. Those days are the drills' record to keep, not this entry's, and no attempt is made here to retro-triage them.

*The distinction that matters is unbackfillable loss, and there is none in the window.* `increase(zcrypto_reconcile_residual_gap_seconds_total[72h])` reads **0**, with `resets(...[72h])` also **0** — the pairing the rule's own expression uses, so the zero is a true zero and not a counter reset presenting as calm. What did accrue is healable: `increase(zcrypto_reconcile_healable_gap_seconds_total[72h])` = **1301.035 s**, repairable by construction. The 8-day view instead shows **2134.889 s** of residual, and that is not new: it is the already-booked 2026-09-03 Kraken venue halt, which `iterations-history-phase6.md` records at 2,134.703242 s. It sits outside this window, and the two figures agreeing to within a fifth of a second is what identifies it rather than a fresh loss.

*`zcrypto-hcio-watchdog` fired against its own blindness, not a dead check.* The rule reads `max(hc_checks_down_total) or on() vector(999)`, and the runbook's two readings are "N checks down" and "the watchdog's eyes went dark". This was the second: the Grafana Cloud stack hibernated for about 3½ hours from 2026-09-06T20:32Z, so the scrape returned nothing and the fallback supplied 999. It recovered when the stack woke on an API probe. Confirmed clear by value rather than by the rule going quiet, as step 6 of that section requires — all ten `hc_check_up` series read 1 and `hc_checks_down_total` reads 0. **The stack hibernating at all is worth naming**: for those hours every dead-man was unobserved from Grafana's side, and healthchecks.io's own notification path was the only thing watching. That is the mutual-watchdog design working as intended, and also the clearest demonstration yet of why the two domains are kept separate.

*The NAS cluster fired and cleared, and the gate is current.* `zcrypto-nas-archive-pull-errors`, `-stalled` and `zcrypto-gate-exporter-stale` are one story on one host, alongside the window's 2 `nas/archive-pull` ERROR lines. Read by value now: the gate exported 2821 s ago, `zcrypto_gate_status` = 1, `zcrypto_gate_mismatch_total` = 0, and the streak reads 58 days. `zcrypto-fleet-daemon-restarted` on `zcrypto` is **expected** — the window carries two converges, `site.yml --limit zcrypto-red` at 2026-09-04T09:27Z and `--limit zcrypto` at 17:14Z, and a restart is that action's own record.

*The healable re-derivation is OWED and its first step is prepared, not done.* The reminder is event-driven: the counter moved, so the qualifying-day count is owed again. Its step 1 reads the ledger from the ops host — `ssh hp "sudo cat /var/lib/zcrypto-ops/capture-reconciled/reconcile-ledger.jsonl"` — and the classifier returns **prepared** for it, so the pass stops there by design rather than judging the step itself. The runbook is explicit that the count comes from the ledger and never from Grafana Cloud, whose ~14-day retention would return zero over a series it has never seen move, and that fewer than three qualifying days means re-arm and stop. Nothing is wrong; what is owed is one attended read.

*No Slack tool was reachable this session, so the summary was not posted and tomorrow's trigger was not scheduled.* MCP tools bind at session start, so this is not recoverable inside this pass — the Slack half is safely run late from a session that has the connector, as 2026-08-30 did. The re-arm chain is therefore stopped, which matters less than usual: the routine is paused at the owner's word, so the next pass is theirs to call rather than a scheduled message's.

## 2026-09-08 — attention

window 24 h to 2026-09-08 09:30Z · alerts none · fired and cleared `zcrypto-nas-archive-pull-errors`, `zcrypto-fleet-daemon-restarted` · checks all pass · logs 4 ERROR/CRITICAL lines, 8 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys zcrypto-red, zcrypto · reminders refdata sweep: due in 26 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions none beyond reads, every command classified `autonomous` before it ran · follow-ups none.

**Both alerts are explained, neither is a defect, and nothing in the window is unbackfillable loss.**

*`zcrypto-nas-archive-pull-errors` is two independent transport transients on different days and different channels, not one story.* Both are `returncode=255`, which the runbook reads as ssh itself, and each names its own cause a line above the failure: the liquidations channel at 14:03:49Z failed on `Could not resolve hostname z-home-zcrypto.zhaow.pro: Temporary failure in name resolution`, and the secondary capture channel at 06:00:45Z on `connect to host zcrypto-red.zhaow.me port 10022: Cannot assign requested address`. Neither is a host-key failure, a permission failure or a traceback, which are the three shapes that section treats as real. **A failure on a capture channel is the one the runbook singles out**, so it was checked by value rather than by the alert going quiet: every `failed=` in the window reads 0, 113 of 113, and the red channel completed clean on each of the three passes after it — 07:04, 08:08, 09:13 — while the liquidations source has 68 completions in the window, the last at 09:15:29Z. The 23 pull-complete lines that carry no `failed=` at all are the engine-journal channel's `(no verify)` form, not failures; counting the lines that lack the string would have reported them as such.

*`zcrypto-fleet-daemon-restarted` on `zcrypto` and `zcrypto-red` is the C7 rollout's own record.* `deploy-log.jsonl`'s last two lines are `site.yml --limit zcrypto-red` at 2026-09-07T15:55:10Z and `--limit zcrypto` at 19:04:30Z — the same two hosts the alert names, and a converge restarting the capture daemon is that action's signature. Step 1 of the runbook resolves it; the OOM and crash-loop branches are the *otherwise* case and were not reached. Both daemons read up in the fleet checks and reconcile source lag passes.

*The capture WARNING count cannot be read from the hosts today, and the reason is worth keeping.* The report counts 4 per capture host over the window; `docker logs` on either host shows 1. The hosts are not disagreeing with the report — `docker logs zcrypto-capture` on the primary begins at 2026-09-07 19:04:31, the instant the converge recreated the container, so the host can only answer for the fourteen hours since. **After a converge, the shipping receiver is the only source that spans the window**, and a host-side count taken without noticing that is a different measurement wearing the same name. What is visible is a single WS reconnect on each host, resolved on attempt 1 within about two seconds, with venue status back to `online` immediately after.

*The Slack half ran late, and it ran twice.* No Slack tool was reachable when the pass ran, so the chain was stopped and this entry said so — a reminder whose absence produces no signal has no detector but the entry that records it. Reloading a session's plugins re-binds the MCP tools into that session, which is a cheaper recovery than the new session the routine describes, and it happened in two sessions at once. **Both posted the summary, three seconds apart at 11:45:53 and 11:45:56 CEST, and both scheduled a trigger for the same instant.**

*So two identical triggers are queued for 2026-09-09 09:26Z, and tomorrow's pass will be woken twice.* Neither can be withdrawn through the API — there is no delete for a scheduled message — so they stand until someone removes one from Slack's *Drafts and sent*. **This was a coordination failure and not a mechanism failure**: the recovery worked, and it worked in both sessions because neither stood the other down. No third message was posted to explain the first two.

*The schedule itself is right, and two sessions verified it independently.* 09:26Z is the hour the routine is due, not the hour either pass happened to run. The API renders it 11:26 CEST, which is the same instant — the read-back exists because a scheduled message cannot be deleted, so it is the only check available, and both sessions read it back and agreed.

## 2026-09-16 — incident

window the 2026-09-15 13:44Z kernel oom-kill on zcrypto-ops, found by hand on 2026-09-16 · **not the product of a daily pass** — no pass ran between 09-08 and today, and this entry records one event rather than a window's verdict · alerts none fired, which is the finding · checks not run · deploys zcrypto-red re-pinned 09-16 09:34:57Z (capture bake) · actions a cap, a version bump and a new daily-pass read, all on an unmerged branch · follow-ups the converge that makes the cap live.

**A `grep` reached 58.4 GiB inside `zaccess-agentboard.service` and the kernel took a GLOBAL oom-kill on a 62 GiB host. Nothing alerted, and nobody noticed for a day.**

*What ran.* A probe of `infra/scripts/sweep.sh` passed `/dev/zero` as a pattern source: `sweep.sh -l -f /dev/zero --control NEEDLE`. `grep -f` treats each LINE of that file as one pattern, and `/dev/zero` never emits a newline, so it is one pattern that never ends. The kernel line records `anon-rss:61250304kB` (58.4 GiB) against `total-vm:119537880kB`, and `oom-kill:constraint=CONSTRAINT_NONE ... task_memcg=/system.slice/zaccess-agentboard.service`. The service restarted at 13:45:44Z.

*How fast.* Measured on this host afterwards, `grep -f` costs about **320 bytes of RAM per byte of pattern file**, linear across 2, 4 and 8 MB inputs (317x, 319x, 321x). Run backwards against the kernel's own figure that is **~187 MB of `/dev/zero` read — under a fifth of a second**. It did not creep up over the probe's 8-second timeout; it was there almost immediately and spent the rest of the time being killed. No operator could have caught it in the act, and the agent that ran it saw only `HUNG (8s timeout)`.

*NUL is not the cause.* A 2 MB single line of `a` blows up the same way (440 MB and still climbing when killed at 60 s). The fatal combination is an unbounded single line, an endless source, and the amplification — the byte value is irrelevant.

*The defect that let it in is fixed.* `sweep.sh` now refuses a pattern source that is not a regular file (`a pattern source is a regular file or it is refused`, merged the same day). That closes the device case and not the general one: a regular file with one enormous line reproduces it.

*Why the blast radius was the whole host.* `zaccess-agentboard.service` ran with `MemoryMax=infinity` and `MemoryHigh=infinity`; its cgroup peaked at 64.1 GB. With no limit to hit, a runaway child escalates to a global oom-kill rather than being contained — the kernel's own `constraint=CONSTRAINT_NONE` says so. The cgroup is not just agentboard: `KillMode=process` means a forked child inherits it for life, so the operator's tmux server and every session started through the web terminal are accounted there.

*Why no rule fired, established by reading the rules rather than assuming them.* `zcrypto-fleet-daemon-restarted` has exactly the right shape and its summary names this case — it failed only because agentboard publishes no `/metrics` and is scraped by nothing, so the series does not exist. `zcrypto-fleet-memory-headroom` names three host/job legs that exclude ops and is spike-blind at `for: 5m`. `zcrypto-fleet-memory-leak` subtracts hourly MINIMA a day apart, which a one-second excursion cannot move, and its 30 h age gate would have silenced it after the restart regardless. The kernel line has no route either: the journal keep-regex matches on `__journal__systemd_unit` and an oom-kill carries `_TRANSPORT=kernel` with no unit, so it is dropped at the relabel — **no host on this fleet ships kernel journal lines**.

*What is being done.* `MemoryAccounting=yes`, `MemoryHigh=12G`, `MemoryMax=24G` on the unit; agentboard bumped `0.4.23` to `0.5.3`; and a new daily-pass read of `systemctl show zaccess-agentboard.service -p MemoryMax -p MemoryHigh -p MemoryPeak -p NRestarts`, which answers uncapped, throttled and restarted in one read-only ssh — chosen over a new metric and rule because it needs no converge and no dashboard push, and because it reads the HOST rather than the repo, which is the gap this incident is actually about.

*Still open.* All of that is on an unmerged branch; `zcrypto-ops` runs the old version uncapped until an ops converge, and the new check reports attention saying exactly that until it lands. A size or line-length bound on `sweep.sh`'s pattern source is not written — the repo's own worst in-scope file is 0.24 MB, and the scope walk excludes `.venv` and `data/` by gitignore and skips binaries, so the remaining exposure needs a caller naming a large path deliberately.

## 2026-09-17 — attention

window 24 h to 2026-09-17 10:30Z · alerts `zcrypto-capture-venue-not-online`, `zcrypto-capture-ts-past-dated-hour`, `zcrypto-reconcile-residual-gap` · fired and cleared `zcrypto-engine-error-logs`, `zcrypto-fleet-daemon-restarted`, `zcrypto-hcio-watchdog`, `zcrypto-capture-venue-state-recurrence`, `zcrypto-ops-error-logs`, `zcrypto-capture-all-streams-silent` · checks agentboard cgroup on zcrypto-ops · logs 2 ERROR/CRITICAL lines, 83 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys zcrypto, zcrypto · reminders refdata sweep: due in 17 days (last sweep 2026-09-04), healable re-derivation: recounted from the ledger and now DUE — 5 qualifying days against a bar of 3 · actions the ledger recount and every diagnostic read, each classified `autonomous` before it ran; the reconciler journal read PREPARED and not run; the agentboard converge PREPARED, then run on the owner's word once the window had closed, with the restart and a process kill after it — all four recorded in the last paragraph below · follow-ups the healable fit, the classifier's quoted-remote blind spot, and two fired topic triggers — [[T0190]] and [[T0119]] — all four to the memo queue.

**A Kraken maintenance window walked the full exit ladder in 5 min 31 s. Every alert that fired is that one event, and capture did not degrade.**

*What happened, from the primary's own venue-status poll.* `online` through 07:01:00, then `maintenance` at **07:01:04**, `cancel_only` at **07:02:56**, `post_only` at **07:04:41**, and `online` again at **07:06:35** — 5 min 31 s from the first non-online state to the last. Two of the firing rules are that one ladder seen from two angles: `zcrypto-capture-venue-not-online` is the state itself, and `zcrypto-capture-all-streams-silent` (fired and cleared) is the book feed stopping with it. Book data came back during `cancel_only`, ahead of `post_only`: the dark windows end 07:03:53–07:04:11 per stream.

*The third firing rule is not this halt's at all, and this entry said it was.* `zcrypto-capture-ts-past-dated-hour` was filed above as "the resumption carrying Kraken's own message timestamps from before the hour boundary". Its `activeAt` is **2026-09-16 13:11Z**, eighteen hours earlier and six minutes after the capture primary's re-pin at 13:04:40Z, so the halt cannot be its cause. Worked to its runbook rather than guessed a second time: `RestartCount=0` with `StartedAt` matching the deploy-log re-pin, so no unexpected restart; the writer logged exactly one line, `pair=SOL/BTC kind=trades hour=2026-09-16 12:00:00+00:00`, three seconds after start; and the mandatory peer comparison reads **7 rows on each host, identical first and last stamps, frames identical**. That is the runbook's shape 2 — a thin stream's hour sits in RAM below the 5,000-row flush, the stop loses it, the resubscribe replay restores it — and the peer copy is what proves the restoration complete. No fabrication and no loss.

*Both capture rules are latches, so neither clears on its own and both will read as a live gap to whoever looks next.* `zcrypto-capture-venue-not-online` is a counter-presence check and `zcrypto-capture-ts-past-dated-hour` a latch on a cumulative counter; each stays firing for the capture daemon's whole lifetime and resets only at a restart, which is bake-gated. The seconds they describe are already booked and the feed is flowing sub-second, so nothing is owed to the data. The runbook's disposition was taken, attended, at 14:08Z: **two silences, one per host**, each matching `__alert_rule_uid__=zcrypto-capture-venue-not-online`, its own `host`, and `system=~maintenance|cancel_only|post_only` — the three states read in that host's own log, the secondary's ladder having been read for this rather than assumed from the primary's, and found identical to the second. Scoped by uid rather than `alertname` because only the Alertmanager label set carries it and a uid survives a retitle. Per host because the canary restarts the secondary days ahead of the primary, so one silence would outlive the other host's counter reset. Verified by outcome: all six instances read suppressed, not merely created. `zcrypto-capture-venue-state-recurrence` is untouched and reads inactive. **They end 2026-09-24T14:08Z and must be deleted at each host's OWN next capture restart** — a silence outliving the counter reset suppresses a new outage's first sighting, which is the blind spot the recurrence rule exists to cover. A Grafana silence leaves no trace in the tree, so that deletion is carried on `zcrypto-marco`'s memo queue. `zcrypto-capture-ts-past-dated-hour` is deliberately NOT silenced: its section prescribes no silence, and reusing this one's reasoning for it would be inventing a disposition.

*Capture did not degrade, measured rather than assumed.* All 24 pair-streams read sub-second on `zcrypto_capture_seconds_since_last_book_message`, and `RestartCount` is **0** on both daemons. The silence was the venue's and both hosts recorded it identically, which is what put the episode in the ledger as `both_streams_silent` rather than as a divergence.

*What it cost, from the ledger rather than from the rate.* One `both_streams_silent` record for hour `2026-09-17T07:00`, `residual_seconds` **2208.788413** — a cross-stream sum over 12 book streams whose individual dark windows run 169.02 s (SOL/EUR) to 187.37 s (DOT/EUR). Twelve `unwitnessed` records accompany it and book nothing, by construction. The gauge agrees: `increase(zcrypto_reconcile_residual_gap_seconds_total[24h])` = **2210.32** with `resets(...[24h])` = **0**, the 1.5 s excess being `increase()`'s extrapolation and not a second event; the total now stands at 26230.861112.

*The ledger's verdict is `undetermined`, and that is the classifier working rather than failing.* `classify_dark_episode` refuses any episode that is not exactly two windows — with three or more there is no way to separate one incident's sputtering from two unrelated ones — and today's episode carries **one**, so it returns `UNDETERMINED` before looking at anything else. The seconds therefore book under `zcrypto_reconcile_dark_episode_seconds_total{verdict="undetermined"}`, where every second the counter has ever held sits: `venue_silent` and `capture_divergent` both read **0** across the whole life of the series. Nothing is owed here — it is written down so the next reader does not take `undetermined` for a question nobody answered.

*The one failing check is the one the 2026-09-16 entry left open.* `zaccess-agentboard.service` on `zcrypto-ops` still reads `MemoryMax=infinity`, `MemoryHigh=infinity`, peak 59.8 GiB, `NRestarts=1`. The cap is merged — `810a6fefd`, `infra/ansible/roles/access_ops/templates/zaccess-agentboard.service.j2` at `MemoryHigh=12G` / `MemoryMax=24G` — so what is owed is the converge alone, `infra/ansible/scripts/converge.sh site.yml --limit zcrypto-ops --tags access`, classified **prepared** and not run. The check reports attention until it lands, exactly as that entry said it would.

*The nightly data-gated run passed.* `finished 2026-09-17T02:53:58Z`, 23 min, `ok true`, `exit_code 0`, **8358 passed, 10 skipped**, `data_present: true`, `data_gated_skipped: []`, at `d4e0fb829be1`. The skipped count is one below the 11 the earlier nights read, and the difference is the fix that caused it: the `zcrypto-data-gated-tests.service` unit gained `Environment=PATH=` (PR #546), so the node-version test that had been skipping for want of a binary on the unit's `PATH` now runs. The remaining ten are 5 grandfathered and 5 live-venue opt-in.

*The healable re-derivation is no longer owed — it is DUE, which is the first time this reminder has produced work.* The report only says the counter moved (~+568 s in 24 h, scraped and extrapolated); the count itself comes from the ops ledger, and it was read this time. Total from the ledger under the production expression — `claimed_seconds or healed_seconds`, inside the per-`(pair, kind, hour)` dedup — is **21324.729448**, equal to the live `zcrypto_reconcile_healable_gap_seconds_total` to the last digit, which is what makes the per-day split trustworthy. Counting only days after the 2026-07-28 counter fix (the `correction` record names commit `1da2ea52`), the qualifying days are **five**: 07-29 534.527 s, 08-04 494.573 s, 08-28 606.233 s, 09-04 1300.734 s, 09-16 567.596 s. A sixth day, 08-31, carries only `trade_deficit` records and moves the counter by 0.000 s, so it does not qualify. Five is past the runbook's bar of three, so the fit is real work: `zcrypto-reconcile-healable-gap-rate`'s `gt [600]` is per live pair, and against today's 12 pairs the five events sit at 44.5, 41.2, 50.5, 108.4 and 47.3 s — the largest at 18 % of the threshold. Each event is one hour of one day, which is a fact the fit has to survive rather than smooth over. Handed to the memo queue: it needs a decision, a branch and an attended `grafana-push.sh`, none of which is this pass's to take.

*The classifier refuses the runbook's own command, and that is why this recount is ten days late.* `ops.md#healable-threshold-rederivation-due` step 1 writes the read as `ssh hp "sudo cat /var/lib/zcrypto-ops/capture-reconciled/reconcile-ledger.jsonl"`, and `ops-daily.py classify` returns **prepared** for exactly that string — which is what the 2026-09-07 entry recorded and stopped on. The same command unquoted (`ssh hp sudo cat …`) returns **autonomous**, as does the bare remote command classified with `--host hp`, which is the form the classifier is built for: `lands_on = target or host`, so naming the host IS saying the command runs there. The quoted blob is one token to the scanner, so nothing inside it matches a shape and default-deny answers — the safe direction, and not wrong, but it means a runbook written the natural way is unrunnable by the pass that has to run it. Two occurrences in the runbook corpus, both this one command. Queued, not fixed here; the table is never widened in the moment.

*Two topic triggers fired, out of 27 live topics.* [[T0190]] — any change to `tests/test_live_venue_opt_in.py` — fired at `cb9f76924` (2026-09-16), which added a **sixth** enumerated escape to that guard's docstring (an env read moved inside a skip helper, whose name is attributed to no gate) while the topic's own `## Suggested next steps` still enumerates five. [[T0119]] reads true on both arms: spec `00092` was picked up in PR #301 (2026-08-18, change-index row `#301`) and `cli/engine/feeders.py:412` computes `delta = target - held_qty[a]`, which is the formula the topic asked for — so it may be closeable rather than workable, and that read is the queued item. Ten more triggers were evaluated and are not ripe: [[T0183]] (T0187 not archived), [[T0187]] (the rule is present, `severity: critical`, no `zcrypto_exec_position` reference), [[T0163]] (no `FIXMINT-` in any adapter-verification row), [[T0159]] and [[T0160]] (`nautilus-trader===2.0.0rc6.dev20260915` carries both `rc` and `dev`), [[T0203]] (no release branch, `main` at one commit), [[T0123]] (no `builder_contract`/`contract_version` symbol under `cli/`), and [[T0199]], [[T0200]], [[T0202]] (their named paths untouched since PR #514 landed them). The other fifteen are milestones, activities or evaluation statements this pass cannot decide and are named unevaluated, [[T0150]] among them by its own text — the date is a handle and the engine host's oldest `cycle-<HH>.json` is the test.

*The first report of the day exited 2 and the source was transient.* `reconcile source lag could not be read: HTTP Error 522` — reproduced once, then three consecutive successes, and the check has read `PASS reconcile source lag: 7936.36` on every run since. One later run named `exec gate level present` as failing for the same reason and now reads `PASS ... 0`. Nothing is owed; recorded because an exit 2 is the report's first finding and a transient one still has to be shown to be transient rather than assumed.

*The Slack half of this pass ran late, and the recovery the skill names is the one that worked.* The `claude.ai Slack` MCP server was unreachable while the pass ran — `404`, `Server not found` — so §8's post and §9's trigger were carried on `zcrypto-marco`'s memo queue rather than dropped, which is what kept them alive across the gap: a session-local scheduler would have died with the session. `/reload-plugins` re-bound the connector inside the same session, the paragraph above posted to `#zcrypto` at 13:14Z, and 2026-09-18's trigger is scheduled there for 09:00Z carrying the three memo items this pass opened.

*After the window closed, the owner ran the prepared converge and the cgroup finding cleared.* `site.yml --limit zcrypto-ops --tags access` at **10:41:42Z**, rc 0, from a clean tree at `fb971485a` — the converge PR #543 left owed. The check that had been this pass's one FAIL now reads `PASS agentboard cgroup on zcrypto-ops: MemoryMax=24.0 GiB, MemoryHigh=12.0 GiB`. The cap went live on a unit that was never restarted: systemd applies resource control at daemon-reload, so the operator's whole tmux workspace survived the converge, which is what `KillMode=process` is for. An attended `systemctl restart` followed at **10:54:08Z** — the converge installs the pin under nvm but the start script execs whatever is installed at START time, so nothing rolls without it. The roll is proven by value rather than by `npm ls -g`, which reads the installed package and never the running one: sha256 of `/proc/<MainPID>/exe` moved `aa849d8c…` → `13b7e01f…` while MainPID moved 826245 → 646942 and the peer session `claude --resume zcrypto-zebra` (pid 459207) and 40 ssh-agents in the same cgroup kept their pids. Recorded in PR #553 with the `deploy-log.jsonl` line and the `fleet-pins.md` row, `0.4.23`/2026-08-26 → `0.5.3`/2026-09-17.

*Three processes left over from the oom-kill's own probe round were killed, and the second failure mode is worth the sentence.* `sweep.sh -l -f /dev/stderr --control NEEDLE` had been asleep in that cgroup since 2026-09-15 14:00:25Z — two `bash` frames and the `grep -I -H -l -f /dev/stderr` under them. Its fd 2 was a pipe opened write-only and its fd 3 the SAME pipe opened read-only, so `grep` sat in `pipe_read` waiting for a pattern only it could write, on a pipe that could never reach EOF because it held the write end itself. A self-deadlock at 2.2 MB RSS, where the `/dev/zero` sibling of the same defect took 58.4 GiB and the host with it — one root cause, opposite symptoms, and `sweep.sh`'s 2026-09-16 refusal of a non-regular-file pattern source closes both. The kill classified **prepared** (`kill` is not in the enumerated table) and ran on the owner's explicit word.

*`MemoryPeak` still reads 64159506432 and will not clear, which is a standing reporting defect rather than a fleet one.* It read 59.8 GiB before the converge, after the restart, and after the kill. `KillMode=process` leaves the cgroup populated, so systemd never destroys and recreates it and the pre-cap high-water mark carries indefinitely. The daily check appends `peak reached MemoryHigh -- something was throttled` whenever `peak >= MemoryHigh`, so that clause will print every morning against a figure older than the cap while `ok` — set by `MemoryMax != infinity` alone — stays PASS. Narration only, and queued to the memo rather than left to re-read as a new finding each day.

*The healable re-derivation closed the same day.* The bar moved from the provisional 600 to a bounded 300 s per pair per day in PR #556: two organic socket drops and three primary capture converges in the five post-fix days, the value the owner's word. `grafana-push.sh` ran from merged `develop` at 19:09Z, prepared tier, attended: every rule's datasource verified, no orphan, the rule read back from the provisioning API with `params [300]` and its expression unchanged, panel 202's red line at 300 in the stored dashboard. The reminder now counts organic days after 2026-08-04, and the ledger recount runs unquoted, `ssh hp sudo cat …`, which the classifier admits.

## 2026-09-18 — attention

window 24 h to 2026-09-18 18:40Z · alerts `zcrypto-capture-venue-not-online`, `zcrypto-capture-ts-past-dated-hour`, `zcrypto-ops-grafana-keepalive-stale` · checks all pass · logs 0 ERROR/CRITICAL lines, 11 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys none · reminders refdata sweep: due in 16 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions every diagnostic read classified `autonomous` before it ran — the keep-alive timer and unit on the ops host, its textfile directory, the gauge, the running Alloy digest; the ops converge the one finding needs is PREPARED and not run · follow-ups the ops converge, to the memo queue.

**The keep-alive rule is reporting the truth: its unit has never been installed. The `ops` role has not been converged since 2026-09-01, and the unit landed on 2026-09-08.**

*The two capture alerts are yesterday's latches and nothing new.* `zcrypto-capture-venue-not-online` carries the two per-host silences created at 14:08Z on 2026-09-17, ending 2026-09-24T14:08Z and owed a per-host deletion at each host's own next capture restart; `zcrypto-capture-ts-past-dated-hour` was worked to its runbook on 2026-09-17, found to be the thin-stream resubscribe replay with the peer copy identical, and deliberately not silenced. Both reset only at a capture restart, which is bake-gated, so both read as firing to whoever looks next. Nothing is owed to the data.

*The third alert is new and its cause is established rather than inferred.* The report printed its host as `?`, which is the shape of `noDataState: Alerting` — no series carries a host label. Worked to `ops-node.md#zcrypto-ops-grafana-keepalive-stale` in order: step 1, `sudo systemctl list-timers zcrypto-grafana-keepalive.timer --all` reads **0 timers listed** and `sudo systemctl status zcrypto-grafana-keepalive.service` reads **could not be found**; step 2, `/var/lib/zcrypto-ops/textfile/` holds seven writers' files and no `grafana-keepalive.prom`, and `/usr/local/sbin/zcrypto-grafana-keepalive` does not exist; the gauge reads `(no series)`. That is the fourth of the five no-data causes the runbook enumerates — the role has not yet been converged onto the host — and not the token case, because the runner script itself is absent, not silent. The rule's own section names the trap it fell into: the push order is load-bearing, and pushing a rule whose series needs a converge pages within `for: 5m` about a run that never happened. The rule merged 2026-09-08 in `a3239fc90`; the first `grafana-push.sh` after that was 2026-09-17 19:09Z, run for the healable bar, which is when this rule began evaluating.

*What the converge carries, which is why it is prepared rather than run.* The last `ops`-tagged converge on `zcrypto-ops` was **2026-09-01T21:39:43Z** (`deploy-log.jsonl`); the 2026-09-17 run was `--tags access` and never entered this role. Seventeen days of ops-role change would land at once: 10 files, +352/−255, including `files/config.alloy` (31 lines) and `templates/alloy-compose.yaml.j2` (21). A `config.alloy` edit makes Alloy the subject, so the run needs `-e ops_alloy_digest=sha256:491b0578c04983fd54fe99b587b6fab4404dc46d0dc16677bd6b00cc1140b308` — the digest the container reports through `.Config.Image`, matching the `alloy` row of `fleet-pins.md` — or the drift assert refuses it; and if that diff touches Alloy's `environment` or `memory:`, the render alone rolls nothing and a `docker compose up -d` in `/etc/zcrypto-ops/alloy` follows, verified on the container rather than from a label. `zcrypto-ops` is venue-facing, so the Kraken window was checked at planning time: of the three entries the feed still carries as `scheduled` — RAIN delisting 09-25, UAE delisting 09-25, NinjaTrader futures 09-18T23:30Z–09-19T02:00Z — none carries `WebSocket` or `REST` as a word in its name or components, so the window is clear now and is checked again immediately before the run.

*The nightly data-gated run passed.* `finished 2026-09-18T02:54:06Z`, 23 min, `ok true`, `exit_code 0`, **8387 passed, 0 failed, 0 errors, 10 skipped**, `data_present: true`, `data_gated_skipped: []`. The passed count is up 29 on 2026-09-17's 8358, which is PR #563's matcher and its fixture; the ten skips are unchanged.

*No topic trigger fired, of 31 live topics.* Three were registered today by PR #563 and PR #564 and are not fired by their own registering work: [[T0206]] — the guard's walk still counts **13** `nothing_found(...)` gates, the figure it records, once the guard's own fixture strings and a same-named test are excluded; [[T0207]] — the three commits that touched `_gates` since yesterday are all PR #563's, and PR #564's edit to that file was a docstring, adding no skip; [[T0209]] — no spec and plan pair has reached the hand-off since. Nine more were evaluated and are not ripe: [[T0183]] (T0187 not archived), [[T0187]] (`zcrypto-engine-cycle-stale` present, and the file's three `zcrypto_exec_position` references sit in the `dark-with-exposure` rule and date from 2026-08-31), [[T0119]] (no spec `00092`), [[T0163]] (no `FIXMINT-` in any reference row), [[T0159]] and [[T0160]] (`nautilus-trader===2.0.0rc6.dev20260915` carries both `rc` and `dev`), [[T0203]] (no release branch, `main` at one commit), [[T0199]] and [[T0200]] (`cli/engine/store.py` and `cli/engine/journal.py` untouched since yesterday; the only `cli/` file on `develop` since is `cli/archive/command.py`). [[T0205]] ripens 2026-10-01 and [[T0150]] not before 2026-10-25, the latter unevaluable by its own text. The rest are milestones, activities or evaluation statements this pass cannot decide.

*The agentboard peak clause printed again, as the 2026-09-17 entry said it would.* `MemoryMax=24.0 GiB, MemoryHigh=12.0 GiB, peak 59.8 GiB, NRestarts=0` — the check is PASS and the peak is a high-water mark older than the cap, which `KillMode=process` keeps alive by never letting the cgroup be destroyed. Narration, already on the memo queue, and not a finding.

## 2026-09-20 — all-clear

window 24 h to 2026-09-20 22:10Z · alerts none · checks all pass · soak 1 of 7 outside band (~0.7 expected by chance at 90%); L=431 to 2026-09-20T16:00:00+00:00, window_bound=journal; self-test ok/ok/ok; outside: governor_engagement (one construction); no-book bars 0 of 431; hhi consistent · logs 0 ERROR/CRITICAL lines, 6 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys none · reminders refdata sweep: due in 14 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions none — nothing fired, so nothing was classified and nothing was run · follow-ups [[T0065]], [[T0159]] and [[T0184]], to the memo queue.

**No entry was written for 2026-09-19.** The gap is recorded here rather than backfilled: this pass reads a 24 h window and cannot reconstruct that day's fleet state, so the honest statement is that nobody looked, not that the day was clear.

*The pass ran from the main checkout while `zcrypto-marco` held it, and the equivalence was proven rather than assumed.* The checkout sits on `t0065/q2-ohlcvt-ingest`, ten commits ahead of `develop` and none behind, so the skill's "a checkout at `develop`'s tip" was answered by measurement: `infra/scripts/ops-daily.py` is byte-identical to `develop`'s, the only files the branch changes are `cli/data/rebuild.py` and `cli/ohlc/reach.py`, and an import walk of the report pulls in neither. Running it here rather than from a fresh worktree was the correct trade in the other direction too — `data/` is gitignored and per-checkout, so a scratch worktree would have had no canonical dataset for the soak leg to read. Nothing in the main checkout was written: the branch, the index and the working tree were left as `zcrypto-marco` had them, and this entry is written from a worktree of `ops-journal`.

*Three topic triggers fired, of 27 live topics.* [[T0065]] — **the Kraken OHLCVT Q2 2026 dump has landed**, which is the condition the topic has waited on since its trigger was rewritten to name the source: `/mnt/zhao-crypto/kraken-ohlcvt-updates/Kraken_OHLCVT_Q2_2026.zip`, 537,866,903 bytes, mtime **2026-09-19 20:12:48Z**, and the archive opens — 9,795 members, 1,399 pairs across the 7 intervals `1/5/15/60/240/720/1440`, with `XBTEUR_15.csv` carrying 8,736 rows spanning 2026-04-01 00:00 → 06-30 23:45 and `XBTEUR_60.csv` 2,184 rows to 06-30 23:00, the two grids the topic says have no other source inside that quarter. `Kraken_OHLCVT_Q3_2026.zip` is absent, so the 1h 06-23 → 07-08 and 15m 07-01 → 07-08 windows still wait on Q3. Already routed: it was handed to `zcrypto-marco` on 2026-09-19, registered in the memo queue after the `00115` (#572) and [[T0209]] (#573) tracks, and the branch `t0065/q2-ohlcvt-ingest` is cut and in progress. [[T0184]] — `cli/engine/soak.py` was changed on `develop` since the last entry, which is that topic's whole first arm, and the arm it names the daily pass as deciding: eight commits touch it, with hunks at `select_clean_segment`, `realized_series`, `soak_report` and the `_SEVERITY` map the sentinel is averaged through. Its second arm did not fire — today's soak read `no-book bars 0 of 431` and `hhi consistent`. [[T0159]] — the read-only dry-run arm: the engine row of `docs/reference/fleet-pins.md` names revision `a1a39280`, whose `pyproject.toml` pins `nautilus-trader===2.0.0rc6.dev20260918`, the same string the working tree pins, and that version is in `verified_nautilus_versions` in `cli/engine/order-semantics-verified.json`. Both sub-items are attended by the topic's own text, so this is a hand-off and not something this pass could act on.

*The remaining live topics were evaluated and are not ripe.* [[T0199]] is the one that needed measuring rather than reading: three of the four files its trigger names moved on `develop` in the window, but the trigger names SYMBOLS, and `seed_store`, `_require_joinable_ts`, `write_parquet`, `seam_overlap` and `dropped_tail` each return zero commits under both `git log -S` and `-G` — `cli/ohlc/seam.py`'s change was reverted in `6bb5395d9` and nets to nothing, and `cli/ohlc/dataset.py`'s four lines are its import block. A file-level reading would have reported it fired. [[T0206]] likewise counts **13** `nothing_found(...)` gates, the figure it registers, once three non-gates are excluded — the definition in `tests/skip_gates.py`, the matcher's own four occurrences in `tests/test_live_venue_opt_in.py`, and a test FUNCTION whose name ends in `nothing_found(` in `tests/test_engine_command.py`; a bare grep counts 19 and would have read as the fourteenth gate. [[T0200]] (`cli/engine/journal.py` and `cli/engine/cycle.py` untouched), [[T0201]] (`cli/engine/store.py` untouched, and the soak page read `window_bound=journal`, not `store`), [[T0187]] (all three arms: `zcrypto-engine-cycle-stale` is present at `severity: critical` and the file's `zcrypto_exec_position` references sit in the `dark-with-exposure` rule that follows it, and the memo's RUNG 2 entry is `⬜` carrying only its `DependsOn`), [[T0183]] (T0187 not archived), [[T0119]] (no spec `00092`), [[T0163]] (no `FIXMINT-` in any reference row), [[T0160]] (`2.0.0rc6.dev20260918` carries both `rc` and `dev`), [[T0203]] (no release branch, `main` at one commit), [[T0123]] (ten `adopt` verdicts, the newest trial 47, which IS the deployable rather than a post-deploy family). [[T0205]] ripens 2026-10-01 and [[T0150]] not before 2026-10-25, the latter unevaluable by its own text. [[T0085]]'s nautilus sub-item is half-true and named rather than decided: its two repo-readable halves hold, and "the pin is frozen" does not while the version carries `rc` and `dev`. The rest are milestones, activities or evaluation statements this pass cannot decide.

*The nightly data-gated run passed.* `finished 2026-09-20T02:54:03Z`, 23 min, `ok true`, `exit_code 0`, **8460 passed, 0 failed, 0 errors, 5 skipped**, `data_present: true`, `data_gated_skipped: []`. The passed count is up 73 on 2026-09-18's 8387 and the skips are down from ten to five, both across two days rather than one, since no run was read on 2026-09-19.

## 2026-09-21 — all-clear

window 24 h to 2026-09-21 12:44Z · alerts none · checks all pass · soak 1 of 7 outside band (~0.7 expected by chance at 90%); 1 of 7 indeterminate -- the verdict depends on how the null was constructed; L=434 to 2026-09-21T04:00:00+00:00, window_bound=journal; self-test ok/ok/ok; outside: governor_engagement (one construction); no-book bars 0 of 434; hhi indeterminate (instrument-fragile) · logs 0 ERROR/CRITICAL lines, 3 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys none · reminders refdata sweep: due in 13 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions none — nothing fired, so nothing was classified and nothing was run · follow-ups none new; [[T0159]] stands from 2026-09-20.

**The pass ran from a detached worktree at `develop`'s tip, and today that was load-bearing rather than hygiene.** The main checkout is on `fix/t0199-t0201-store-refusals`, which changes `cli/engine/soak.py` and `cli/engine/store.py` — and `soak_run` shells out to `uv run --no-sync zcrypto engine soak-check` with `cwd=REPO_ROOT`, its own docstring saying `cwd` pins the CODE to the checkout the script sits in. Run from the main checkout, today's soak verdict would have been computed by unmerged work on the store's refusal path, which is the one leg that branch is rewriting. Yesterday the same shortcut was provably safe and was taken; today it is not, and the difference is which files the open branch touches. The two halves that made the worktree usable are the module's own design: `ops_daily.py` is byte-identical to `develop`'s, and `SOAK_CANONICAL` names the canonical dataset by absolute path, so the code came from `develop` and the data from the main checkout's gitignored `data/ohlc-full`. Nothing in the main checkout was read into or written from this pass.

*The soak row's `hhi` moved from `consistent` to `indeterminate (instrument-fragile)`, and that is the fix landing, not a regression.* [[T0184]] — handed to `zcrypto-marco` from yesterday's pass — was resolved and archived today, and its Resolution records this exact flip before it happened: removing the sentinel bars from the era-matched null moves the windowed band's upper edge 0.3929 → 0.4557 and `effective_n` 21.73 → 19.91, the windowed arm still reads `consistent`, the block-bootstrap arm now reads the book as more diversified than the sentinel-free reference, and the reconciled verdict moves `consistent` → `indeterminate (instrument-fragile)`. It also predicts today's row shape exactly — "one metric outside, one indeterminate, six decided against a floor of three" — and records that **the owner read the flip and let the ruling stand**, the disagreement being information about the reference rather than about the book. Yesterday's `consistent` was the defect reading; today's is the instrument reporting honestly. Not a finding, and the row is PASS on every gate the skill reads in order: no `void:`, self-test ok/ok/ok, not `NOT CURRENT` (L stamped 04:00Z against a 12:44Z pass), six decided against a floor of three, and one outside against a threshold of three.

*No topic trigger fired newly, of 24 live topics — three fewer than yesterday.* [[T0184]], [[T0200]] and [[T0206]] were all resolved and archived today, which closes yesterday's routing: T0184 was handed over from this pass and is done, and T0206 and T0200 were worked deliberately rather than by their triggers firing — yesterday's readings that neither had fired both held. [[T0065]]'s trigger was rewritten since yesterday and is now decidable rather than unevaluable, so this pass decided it: `curl -sIL -o /dev/null -w '%{http_code}' https://assets.kraken.com/marketing/institutions/Kraken_OHLCVT_2026Q3.zip` prints **404**, the same value the trigger's own text records for 2026-09-20, so the Q3 incremental is not served and the 1h 06-23 → 07-08 and 15m 07-01 → 07-08 windows stay blocked. [[T0159]]'s read-only dry-run arm is **still** true and is named as standing rather than re-routed: the engine row of `fleet-pins.md` is unchanged at revision `a1a39280`, stamped 2026-09-19 08:12:24, and its `pyproject.toml` and the tree both pin `nautilus-trader===2.0.0rc6.dev20260918`. [[T0199]] did not fire although `cli/engine/soak.py` moved on `develop`: its trigger names symbols, and `seed_store`, `_require_joinable_ts`, `seam_overlap` and `dropped_tail` each return zero commits under `git log -G` since the last entry — the same measurement that refuted it yesterday, against a differently-worded trigger, since the topic no longer names `cli/ohlc/dataset.py`. [[T0201]] did not fire on either arm (`cli/engine/store.py` untouched on `develop`; the soak page read `window_bound=journal`, not `store`) — both are being worked on the open branch regardless, which is a choice and not a trigger. [[T0187]] (RUNG 2 still `⬜` carrying only its `DependsOn`; `infra/grafana/alerts.yaml` did not move on `develop`, so yesterday's reading of the rule at `severity: critical` stands), [[T0183]] (T0187 not archived), [[T0119]] (no spec `00092`), [[T0163]] (no `FIXMINT-` row), [[T0203]] (no release branch, `main` at one commit), [[T0160]] (`2.0.0rc6.dev20260918` carries both `rc` and `dev`), [[T0123]] (ten `adopt` verdicts, newest trial 47, which IS the deployable rather than a post-deploy family). [[T0205]] ripens 2026-10-01 and [[T0150]] not before 2026-10-25, the latter unevaluable by its own text. [[T0085]]'s nautilus sub-item stands where yesterday left it — the two repo-readable halves hold and "the pin is frozen" does not. The rest are milestones, activities or evaluation statements this pass cannot decide.

*The nightly data-gated run passed.* `finished 2026-09-21T02:54:19Z`, 23 min, `ok true`, `exit_code 0`, **8584 passed, 0 failed, 0 errors, 5 skipped**, `data_present: true`, `data_gated_skipped: []`, at sha `80ea9818` with a clean tree. The passed count is up 124 on yesterday's 8460 and the five skips are unchanged.

## 2026-09-23 — attention

window 24 h to 2026-09-23 21:13Z · alerts none · fired and cleared `zcrypto-hcio-watchdog`, `zcrypto-nas-archive-pull-errors` · checks all pass · soak 1 of 7 outside band (~0.7 expected by chance at 90%); 1 of 7 indeterminate -- the verdict depends on how the null was constructed; L=449 to 2026-09-23T16:00:00+00:00, window_bound=journal; self-test ok/ok/ok; outside: governor_engagement (one construction); no-book bars 0 of 449; hhi indeterminate (instrument-fragile) · logs 2 ERROR/CRITICAL lines, 26 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys zcrypto-ops · reminders refdata sweep: due in 11 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions both alerts worked to their runbooks and classified **transient** — self-resolved, cause identified; every host read classified `autonomous` on `ops`, two of them only after they had run (both `autonomous` when asked) · follow-ups the `nas.md` returncode table's missing code 24, to the memo queue; [[T0159]] stands from 2026-09-20.

**No entry was written for 2026-09-22.** Recorded rather than backfilled, as 2026-09-19 was: a 24 h window cannot reconstruct that day's fleet state.

**The pass ran from a detached worktree at `develop`'s tip.** The main checkout is on `fix/t0160-nautilus-dev20260923`, which changes `pyproject.toml` and `uv.lock` — a different dependency environment from `develop`, which the soak leg would have run in, since it shells out with `cwd=REPO_ROOT`. `SOAK_CANONICAL` was re-read on today's tree before the run and still names the canonical dataset by absolute path, so the worktree got `develop`'s code and the main checkout's data. The `.tmp/adhoc-dev20260923` worktree already at `develop`'s tip was left alone: it is not this pass's, and whether anything is running in it is not something this pass could see.

*`zcrypto-hcio-watchdog` fired three times overnight and was pending a fourth; none of it was the ops converge.* The deploy in the window — `site.yml --limit zcrypto-ops` at 13:20:21Z — was the first thing to check, and the rule's history rules it out: it went `Alerting` 22:56:40Z → 00:41:40Z, 04:51:40Z → 05:41:40Z and 07:36:40Z → 07:41:40Z, and `Pending` 10:21:40Z → 10:26:40Z, all before the converge. Every firing carried `values {"A": 1}`, the rule's 1–N reading — one dead-man down, not the 999 of the healthchecks.io scrape going dark. A `query_range` of `hc_check_up == 0` names the one check, **`zcrypto-grafana-watchdog`** (`host=ops`), down 22:51Z → 00:40Z, 04:46Z → 05:40Z, 07:31Z → 07:40Z and 10:21Z → 10:25Z — the four episodes to the minute. Grafana was evaluating throughout, since it fired this rule, so the runbook's step 3 applied and the prober's own journal on `ops` was read: the timer ran every 5 minutes and every run finished (144 in 22:40Z → 10:40Z), and 36 of those runs logged `curl: (22) The requested URL returned error: 404` against Grafana Cloud's `/api/health`, in four bursts of 22, 11, 2 and 1 consecutive probes that line up with the four down windows. **So the cause is Grafana Cloud's own health endpoint answering 404 in four bursts**, the prober reporting that honestly, and this rule doing its job as the deliberate redundant copy. From 10:40Z to 21:10Z the prober ran 126 more times with no curl error of any kind. The 404 is not the 503 that [[T0181]] records as a hibernating instance's answer, and it is not 2026-09-07's reading of this same rule either: that day the stack hibernated, the scrape went dark and the fallback supplied 999, while today Grafana scraped and evaluated throughout and only its health endpoint answered 404.

*Today's converge installed the Grafana keep-alive, which discharges what the 2026-09-18 entry left owed — but it did not end these bursts.* `zcrypto-grafana-keepalive.timer` is now listed on `ops`, and `grafana-keepalive.prom` reads `zcrypto_grafana_keepalive_status 200` in 0.27 s with the last run equal to the last success (21:02:00Z). The bursts stopped at 10:20Z, three hours before the unit existed, so today says nothing about whether the keep-alive prevents them, and it is not credited with ending them.

*`zcrypto-nas-archive-pull-errors` was an rsync race at the hour boundary, and the mirror kept advancing.* The two ERROR lines, read from Loki with their context rather than over ssh (the runbook's `--since 2h` would have missed a 17:00Z event read at 21:10Z), are `rsync failed … dest=/archive/capture-segments-red returncode=24` and the wrapper's `secondary capture pull failed … continuing`, both 17:00:02Z, preceded at 16:59:56Z by `file has vanished` for `LINK/EUR/book/2026/09/23/16.part0118.parquet` and `16.part0119.parquet`. Code 24 is rsync's "files vanished before they could be transferred": two hour-16 parts on the secondary disappeared between the file list and the transfer, at the moment hour 16 closed — `cli/capture/segment_writer.py`'s `_commit` merges an hour's parts into its final and unlinks them, and unlinks a part only once the merged bytes containing it are durable, so the rows were already in the final when rsync looked for the parts. The runbook says any failure on a capture channel means the unbackfillable mirror is not advancing, and its step 5 asks for the next pass proven clean — so it was: the channel's next three passes, 18:04:58Z, 19:09:18Z and 20:13:36Z, each read `pull complete … failed=0`, and its lag fell from 6881 s before the failure to 3898 s after. The hour-16 final the vanished parts became was pulled and verified; nothing was lost. It is rare — the only `rsync failed` line in the last seven days.

**It exposes a runbook gap, routed as a doing.** `infra/runbooks/nas.md`'s `zcrypto-nas-archive-pull-errors` table gives returncodes 255, 23 and 12 and not 24, so a reader meeting this race is told the mirror has stopped. A row for 24 — files vanished mid-transfer; at an hour boundary it is the capture writer rolling parts into the final, and benign when the channel's next pass reads `failed=0` — closes it. Handed to `zcrypto-marco` for the memo queue. The same section's step 5 would benefit from one clause more: a verified channel's success line, `cli/archive/command.py`'s `pull complete … checked= … failed=` form, names its `source=` and carries no `dest=` (the no-verify form does), so filtering a verified channel on its destination finds every failure and no success.

*No topic trigger fired newly, of 21 live topics.* Since the last entry [[T0199]], [[T0201]] and [[T0203]] were resolved and archived and [[T0212]] was registered (2026-09-21). [[T0212]] did not fire on either arm: neither `infra/ansible/scripts/converge.sh` nor `infra/scripts/deploy-log-audit.py` has changed since it was registered, and `infra/scripts/count-list.sh engine-rows-on-the-completion-floor` reads **1**, the single row its own text names as having made deferral right. [[T0187]] was re-checked on its third arm because `infra/grafana/alerts.yaml` changed on `develop`: `zcrypto-engine-cycle-stale` occurs once, its own block carries `severity: critical` and no `zcrypto_exec_position`, and no commit since the last entry touched its uid — the file's change was elsewhere; its first arm is still `⬜` in the memo. [[T0065]]'s Q3 check prints **404** again. [[T0159]] stands: the engine row still names revision `a1a39280`, pinning `nautilus-trader===2.0.0rc6.dev20260918`, the string `develop`'s tree pins. [[T0183]] (T0187 not archived), [[T0119]] (no spec `00092`), [[T0163]] (no `FIXMINT-` row), [[T0160]] (`develop`'s pin carries both `rc` and `dev`; the open branch's bump is not merged), [[T0123]] (ten `adopt` verdicts, newest trial 47, the deployable). [[T0205]] ripens 2026-10-01 and [[T0150]] not before 2026-10-25, the latter unevaluable by its own text. The rest are milestones, activities or evaluation statements this pass cannot decide.

*The nightly data-gated run passed.* `finished 2026-09-23T02:54:31Z`, 23 min, `ok true`, `exit_code 0`, **8955 passed, 0 failed, 0 errors, 5 skipped**, `data_present: true`, `data_gated_skipped: []`, at sha `f954a785` with a clean tree. The passed count is up 371 on 2026-09-21's 8584, across two days since 2026-09-22's run was not read.

## 2026-09-24 — attention

window 24 h to 2026-09-24 09:09Z · alerts `zaccess-tunnel-stale` · fired and cleared `zcrypto-nas-archive-pull-errors` · checks all pass · soak 1 of 7 outside band (~0.7 expected by chance at 90%); 1 of 7 indeterminate -- the verdict depends on how the null was constructed; L=452 to 2026-09-24T04:00:00+00:00, window_bound=journal; self-test ok/ok/ok; outside: governor_engagement (one construction); no-book bars 0 of 452; hhi indeterminate (instrument-fragile) · logs 8 ERROR/CRITICAL lines, 44 WARNING · dead-men 0.0 down via Grafana, 10 read directly, no description finding · deploys zcrypto-ops · reminders refdata sweep: due in 10 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions every alert worked to its runbook; every host read classified `autonomous` before it ran; `wg-quick@zaccess0`'s restart classified `autonomous` and deliberately NOT run; four ops reads classified `prepared` and not run · follow-ups the home network's upstream IPv6, to the owner; `zaccess-tunnel-stale` stays firing until it recovers.

**One cause sits under everything this window moved: the home network's upstream IPv6 has been black-holed since about 00:18Z.** The workstation, `zcrypto-ops` (`ssh hp` → `192.168.100.6`) and `nas` (`192.168.100.5`) share one LAN, by the addresses `ssh -G` resolves for them; `zaccess` (a Linode VPS), `zcrypto` and `zcrypto-red` sit off it, reached by public names. The LAN side of IPv6 is healthy — the workstation holds a valid global address, a default route learned by RA, and its gateway answers `ping6` at 0.9 ms with no loss — but nothing beyond the router answers over IPv6: from the workstation `curl -6` times out against Grafana Cloud, healthchecks.io, cloudflare.com and google.com alike, while `curl -4` reaches Grafana in 0.09 s. So the fault is upstream of the home router, where only the owner can act.

**It stopped this pass from producing a report, which is the first finding.** The first run hung in a TCP handshake — one connection in `SYN-SENT` to `[2606:4700::6812:c61]:443`, one of the two IPv6 addresses `zcrypto2026.grafana.net` resolves to — and `timeout 900` killed it with no output (exit 124). `getaddrinfo` returns IPv6 first and the report's `_TIMEOUT` of 30 s applies per address, so every Grafana read paid it on each dead address before reaching IPv4. The report that gives this entry its paragraph ran with IPv4 pinned for that run alone — a `sitecustomize` dropping IPv6 results from its sockets and an `ssh` wrapper adding `-4`, from a scratch directory, with nothing in the repo or any ssh config changed — and finished in 24 s. Same code and data, only the address family different: that difference is the whole of the first run's failure. The pass ran from a detached worktree at `develop`'s tip so that a branch switch in the main checkout mid-run could not change what the soak leg executes.

*`zaccess-tunnel-stale` is firing now, on both ends, and it is the outage's clearest symptom.* It went `Pending` on `ops` at 00:26:20Z (value 433) and on `zaccess` at 00:28:20Z (591), `Alerting` at 00:36:20Z and 00:38:20Z, and was still `Alerting` on both when read at 09:02Z. The runbook's step 1 on the ops end, `sudo wg show zaccess0` at 09:03:57Z: **latest handshake 8 hours 46 minutes ago** — about 00:18Z — against the peer endpoint **`[2a01:7e01::2000:7eff:fe6d:e991]:51820`, the bridgehead's IPv6 address**. The tunnel dials the bridgehead over exactly the path that failed. The bridgehead end was not read: its sshd accepts only the deploy key `infra/ansible/scripts/run.sh` loads from the vault for ansible, and decrypting a deploy key by hand for a confirming read is not this pass's to do. Ops' own clock pins the moment independently: its Alloy logged `Failed to send batch, retrying` on the Grafana Cloud remote-write at **00:18:23Z**, error `context deadline exceeded`, then recovered by 01:00Z (usage reports sent at 01:00, 05:00 and 09:00Z). That error is a timeout and names no address family, so it dates ops' loss of egress without by itself proving IPv6; the direct test — `curl -6` from ops — classified `prepared`, as did `ip -6 addr`, `ip -6 route`, and the runbook's step 2 read of the `Endpoint` line in `/etc/wireguard/zaccess0.conf` (that file also holds the private key). None of them ran. **Classified: needs a human.** The runbook's step 4, `systemctl restart wg-quick@zaccess0` on ops, classified `autonomous` and was deliberately not run: it would re-dial the same IPv6 endpoint through the same dead path, and its `Requires=` restarts agentboard, which holds the operator's sessions. The tunnel recovers when home IPv6 does, or when the ops-side endpoint is pinned to the bridgehead's IPv4 — a config change, and a converge, which is attended.

*`zcrypto-nas-archive-pull-errors` fired twice, on the capture channels, and the unbackfillable mirror kept advancing.* 01:40Z: the primary channel's `rsync failed … dest=/archive/capture-segments returncode=255`. 04:56Z: both channels at 255, with ssh naming the cause — `connect to host zcrypto-red.zhaow.me port 10022: Cannot assign requested address`, a connect error meaning no usable source address for the destination, consistent with the NAS reaching for the host's IPv6 (`zcrypto.zhaow.me` and `zcrypto-red.zhaow.me` both publish AAAA records). This is not the benign code-24 race of 2026-09-23: rc 255 is the transport. Read by `source=` across every pass since 20:13Z yesterday, the primary failed 2 of 12 and the secondary 1 of 12, **each failure followed by a clean `failed=0` pass**, with lag bounded at 1.01–1.86 h and the latest pass 1.19 h (primary, 08:11Z) and 1.20 h (secondary, 08:12Z). Nothing is lost, and the capture hosts keep 14 days locally besides. **Classified: transient per pass, with the cause still live** — any pass can fail again until home IPv6 recovers.

*`zcrypto-hcio-watchdog` went `Pending` ten times and never fired.* Each blip lasted one minute and read `A=999` — the healthchecks.io scrape went dark for one evaluation, the opposite reading from 2026-09-23's `A=1` — at 00:44, 02:18, 02:30, 03:01, 03:06, 04:21, 04:52, 05:42, 08:31 and 08:40Z, all after 00:18Z and none reaching the 5-minute hold, which is why the report lists none. The scrape runs from ops' Alloy, on the affected network. Ops' Alloy log could not confirm the mechanism: 16 lines in 9 h, none about healthchecks, because Alloy logs no individual scrape failure at its default level — so that empty result is a limit of the source, not evidence the scrape never failed.

*No topic trigger fired, of 20 live topics.* [[T0163]] was resolved since the last entry, and none was registered. **[[T0159]]'s read-only dry-run arm is no longer true**, having stood since 2026-09-20: `develop`'s tree now pins `nautilus-trader===2.0.0rc6.dev20260921`, while the engine row still names revision `a1a39280`, pinning `2.0.0rc6.dev20260918` — the tree has moved past what the engine runs, so the arm stays false until the engine is re-pinned. [[T0187]] was re-checked on its third arm since `infra/grafana/alerts.yaml` changed on `develop`: `zcrypto-engine-cycle-stale` occurs once, its own block reads `severity: critical` with no `zcrypto_exec_position`, and no commit since the last entry touched its uid; the memo's RUNG 2 entry is still `⬜`. [[T0212]] (neither of its files changed; `engine-rows-on-the-completion-floor` reads 1), [[T0065]] (the Q3 incremental still 404), [[T0183]] (T0187 not archived), [[T0119]] (no spec `00092`), [[T0160]] (the pin carries both `rc` and `dev`), [[T0123]] (ten `adopt` verdicts, newest trial 47, the deployable). [[T0205]] ripens 2026-10-01 and [[T0150]] not before 2026-10-25, the latter unevaluable by its own text. The rest are milestones, activities or evaluation statements this pass cannot decide.

*The nightly data-gated run passed, inside the outage.* `finished 2026-09-24T02:54:47Z`, 23 min, `ok true`, `exit_code 0`, **9142 passed, 0 failed, 0 errors, 4 skipped**, `data_present: true`, `data_gated_skipped: []`, at sha `0f268b5d` with a clean tree. The passed count is up 187 on 2026-09-23's 8955 and the skips are down from five to four.

## 2026-09-25 — attention

window 24 h to 2026-09-25 15:08Z · alerts none · fired and cleared `zcrypto-fleet-daemon-restarted`, `zcrypto-engine-exec-not-evaluated`, `zcrypto-hcio-watchdog`, `zcrypto-reconcile-source-lag`, `zcrypto-gate-exporter-stale`, `zcrypto-engine-cycle-stale` · checks all pass · soak 1 of 7 outside band (~0.7 expected by chance at 90%); 1 of 7 indeterminate -- the verdict depends on how the null was constructed; L=459 to 2026-09-25T08:00:00+00:00, window_bound=journal; self-test ok/ok/ok; outside: governor_engagement (one construction); no-book bars 0 of 459; hhi indeterminate (instrument-fragile) · logs 0 ERROR/CRITICAL lines, 45 WARNING · dead-men 0.0 down via Grafana, 10 read directly · deploys zcrypto-red, zcrypto-red, zcrypto, nas, zcrypto-ops, zaccess, zcrypto-ops, zcrypto · reminders refdata sweep: due in 9 days (last sweep 2026-09-04), healable re-derivation: counter unchanged in 24 h · actions the logship check re-read (autonomous); `docker inspect` of capture on zcrypto-red and of the poller on zcrypto-ops (autonomous); the same read on zcrypto and nas classified prepared and left unrun · follow-ups [[T0212]], [[T0085]] handed to `zcrypto-marco`

**The pass's first report exited 2, on one source, and the source recovered on a re-read.** At 14:48Z the `logship drops` check could not be read: `urlopen error _ssl.c:1064: The handshake operation timed out`, a TLS handshake to the Grafana datasource that the other seven fleet checks reached in the same run. Re-read at 14:49:48Z: `max(increase(zcrypto_logship_dropped_lines_total[6h]))` 0, the same over 24 h 0, across 4 series. The paragraph above is the 15:08Z run, which read it and exited 1.

**Each of the six rules that fired traces to a recorded action.** The causes come from the rules' own state history (`/api/v1/rules/history`, the report's code path) set beside the deploy log and `process_start_time_seconds`.

*The NAS rollout's recreate replayed the gate cold, and three rules followed it (expected).* The converge at 20:00:46Z (`--limit nas --tags nas -e nas_apply_compose=true`, archive-pull re-pinned to `c4135ac75b72`, #605) recreated `zcrypto-archive-pull` at 20:00:43.6Z, which discarded its scoring cache at `/tmp/gate-cache.json`. The first gate-export ran from 20:01:50Z to about 21:36Z: 456 cycles replayed, 0 cache hits, 5664 s against a warm run's 164–322 s. `zcrypto-gate-exporter-stale` fired twice on nas, 21:13:30–21:37:30Z and 22:07:30–22:43:30Z, each an export stamp crossing 7200 s before the next publish. `zcrypto-hcio-watchdog` fired 21:21:40–21:36:40Z at value 1: healthchecks.io's `zcrypto-gate-verify` dead-man went down by staleness at 21:16:35Z and came back when the cold run pinged. `zcrypto-reconcile-source-lag` fired on ops for both sources 22:23–22:43Z at 11530.65 s, the loop's capture pulls having waited behind the cold export until 22:37Z. All three cleared by value.

*The attended red-button window on 2026-09-24 explains the two engine rules (transient).* Its record (`docs/reference/adapter-verification/2.0.0rc6.dev20260921.md`, merged with #608) stops the engine at 21:05:00Z and starts it at 21:16:12Z, inside the gap after the 20:00Z boundary. `zcrypto-engine-cycle-stale` went Pending on no data at 21:06:30Z, Alerting at 21:11:30Z and Normal at 21:17:30Z; `zcrypto-engine-exec-not-evaluated` was Alerting 21:16:30–21:17:30Z. The next boundary, 00:00Z, completed at 00:01:42Z.

*`zcrypto-fleet-daemon-restarted` fired eight times, each on a deliberate restart.* Six sit within a second of a deploy row or its recorded hand roll: zcrypto-red's capture (row 15:48:46Z), zcrypto's capture and engine (19:50:25Z), the NAS Alloy (20:00:46Z, `compose restart alloy`), ops' liquidations poller (the owner's `docker compose up -d` at 20:04:48Z, which the 20:03:14Z row's `liquidations_decision=roll-after` prescribes) and the engine again (10:14:00Z). The other two are the red-button window's engine starts at 20:51:25Z and 21:16:12Z. `docker inspect` confirms the two hosts whose read classifies autonomous: zcrypto-red's capture `RestartCount 0`, `OOMKilled false`, started 2026-09-24T15:48:46.708Z; ops' poller `0`, `false`, 20:04:48.776Z. No classification rests on the two reads left prepared.

*Owed from the triage, and handed to `zcrypto-marco` for the memo queue.* `infra/runbooks/gate.md:168` says the `zcrypto-gate-verify` dead-man tolerates a cold replay, which 2026-09-24 shows it did not. The NAS-recreate note in `.claude/skills/zcrypto-rollout-image/SKILL.md` names none of the pages a recreate now brings. The steady-state lag in `infra/runbooks/nas.md` and `ops.md#zcrypto-reconcile-source-lag`, and the time-to-alert in `infra/runbooks/drills-order-path.md`, disagree with this window's readings. A decision for the owner, to register as a topic: raise `zcrypto-gate-verify`'s grace, or keep the gate cache on a volume a recreate does not discard. And a question: whether the two red-button presses owe a `docs/reference/drill-log.md` entry, which has none dated 2026-09-24.

*The engine is armed.* At 15:09Z `zcrypto_exec_armed` reads 1 and the gate level 2, since the rung-1 arming converge at 10:14Z (#611). The engine's own position gauge reads BTC/EUR +0.00026906 and ETH/EUR −0.00840738, the rest 0; the venue was not read. #612's disarm merged at 11:22Z; its converge waits for a gap with no margin position open (`engine-procedures.md#engine-restart-margin-position`).

*Two topic triggers fired, of 18 live topics.* [[T0212]]'s file arm: `infra/scripts/deploy-log-audit.py` changed in 11cf4e531 and 2caf33cee, merged by #608, its first change since the topic was registered; the count arm reads 1 on `engine-rows-on-the-completion-floor`, under its 3. [[T0085]]'s nautilus-engine-converge sub-item: the engine row names revision 77df6273, which pins `2.0.0rc6.dev20260921`, verified PASS and frozen with its nine owed checks discharged, the last at 2026-09-25 10:21Z, while the topic's own text still says the engine has not converged onto it. Both are handed to `zcrypto-marco`. [[T0205]]'s date is 2026-10-01; [[T0150]] needs a host check; [[T0183]], [[T0123]] and [[T0119]]'s spec arm did not fire; the milestone, activity and evaluation triggers are named unevaluated.

*The venue-facing converges avoided Kraken's windows.* `infra/scripts/count-list.sh converges-inside-a-kraken-window` reads 0.

*The nightly data-gated run passed.* `finished 2026-09-25T02:54:53Z`, `ok true`, `exit_code 0`, **9161 passed, 0 failed, 0 errors, 4 skipped**, `data_present: true`, `data_gated_skipped: []`. The passed count is up 19 on 2026-09-24's 9142.
