# Plan — the soak's null reference spans only the era its basket exists in

Spec: `docs/specs/00112-soak-null-reference-span-design.md`. Branch `fix/t0184-soak-hhi-aggregate`, off `develop`.

## Global constraints

- `cli/engine/soak.py` is off the live trade path but the soak report is the instrument a go-live decision reads. Every change is fail-safe or refuses; none may make a verdict quieter without saying so on the report's own face.
- **`structural_metrics` is NOT touched.** The per-bar `0.0` stays. Spec D4.
- **The builder's INPUT is not sliced.** `build_crossfreq_system_fast` receives the full canonical arrays; only the result's per-bar series are cut. Spec D2 — reversing this reintroduces the defect.
- Do not touch `cli/capture/`, `cli/tick/`, `cli/backfill/`, `cli/xcheck/`, `cli/portfolio/record44_legs.py`.
- Every per-bar list on `NullSystem` is parallel and must be cut with the same index, and the two derived scalars recomputed. The fields are `weights`, `net_live`, `multipliers`, `day_index`, `governed_net`, `cap_breach` (lists) and `n_periods`, `cap_breach_bars` (derived). `day_index` feeds `governor_engaged_daily`, so a desynchronised cut silently misattributes governor days. Task 3's reference-span field is deliberately NOT a seventh parallel list — it holds two stamps — so this enumeration stays closed and no new series can fall out of step with it.
- **`null` is `None` on three of `soak_report`'s four paths**, and nothing in `render_report` or `_json_payload` dereferences it today, so a new read has no sibling guard to copy: `cli/engine/soak.py:1538` and `:1548` render with `None, None, None, None` (no journaled cycles; `realized_series` raised) and `:1598` with the canonical-absent branch's `null=None`, each followed immediately by `_json_payload` on the same arguments. Four existing tests call the renderer that way too — `tests/test_engine_soak.py:1387`, `:1429`, `:1456`, `:1594`. Every new read of `null` on either face is guarded.
- Tests live in `tests/test_engine_soak.py` (136 collected by `uv run pytest --collect-only -q` today) and `tests/test_engine_soak_command.py` (10). `test_build_null_on_real_canonical` reads `data/ohlc-full`, which is gitignored and absent in CI **and absent from this worktree** — a test that needs it is data-gated and must be run locally before the PR, on the precondition Task 4 states.

## Task 1 — derive the first bar at which the basket is complete

`cli/engine/soak.py`. Add a module-level helper beside `_load_canonical`:

```python
def _basket_complete_index(prices: dict[str, list[float | None]], assets: tuple[str, ...]) -> int:
    """The first index at which every basket asset carries a finite price, derived per call because the basket
    changes and a constant would not (spec 00112 D3)."""
```

That docstring is the whole of it: one sentence of contract plus the one clause that stops the wrong edit this task invites, hardcoding the index (`.claude/rules/prose.md`). Why the cut exists at all belongs at the call site, where Task 2 puts it.

It returns the index, and where no such index exists raises `SoakError` naming the assets absent at EVERY index — or the whole basket where that set is empty, which is the disjoint case: each asset priced at some index, never all of them at one. `None`, a non-finite entry, and an asset carrying no entry in `prices` at all count as absent — the last at every index, so a missing key reaches the refusal instead of raising `KeyError` out of a helper nobody catches.

Tests in `tests/test_engine_soak.py`:
- `test_basket_complete_index_is_the_first_all_present_bar`: three assets, one entering late, asserts the index is that asset's first bar.
- `test_basket_complete_index_ignores_a_later_hole`: three assets, the second entering at bar 2 and the third absent again at bar 4 — asserts the index is 2, the first ALL-present bar, and that the later hole does not move it. The fixture's first all-present bar must be greater than zero or a helper stubbed to return `0` passes it and the red proof below cannot be produced.
- `test_basket_complete_index_refuses_a_basket_never_complete`: one asset all-`None` → `SoakError` naming it.
- `test_basket_complete_index_refuses_an_asset_missing_from_prices`: an asset in `assets` with no key in `prices` → the same `SoakError`, naming it.
- `test_basket_complete_index_is_zero_when_all_present_from_the_start`: the true-positive control.

Red proof: each of the first four fails against a stub returning `0`.

## Task 2 — cut the null's series at that index

`cli/engine/soak.py`. Each step names its own site, and step 2's is load-bearing: step 1 is inside `_net_live_from_result`; step 2 is in `build_null` immediately after `_load_canonical` returns, BEFORE either builder call; step 3 is in `build_null` after the `_net_live_from_result` call and before the `NullSystem` construction. They are numbered as described, not as run.

1. **Keep the reconstruction-vs-count comparison where both sides still exist, and report it through the channel the tree already has for its class**: `_net_live_from_result` ANDs `sum(cap_breach) == result.cap_breach_bars` into the `reconcile_ok` it already returns, and gains `long_cap`/`short_cap` keyword arguments — defaulting to `apply_position_caps`' own, so the six existing `_net_live_from_result` call sites in `tests/test_engine_soak.py` are untouched — which `build_null` passes from its `config`. The soak's reconstruction and the builder's count are two computations of one quantity — that function's own docstring (`cli/engine/soak.py:283`) says the reconstruction "cannot disagree with `cap_breach_bars` on a rounding bit" — so a disagreement is a BROKEN CODE CONTRACT, not a data state, and `reconcile_verdicts` (`:581-582`) sets the tree's rule for that class: it aborts rather than rendering as a data finding. `reconcile_ok` is that rule's existing wiring — `null.reconcile_ok` → `self_tests` (`:878`) → `SelfTestReport.void` → the `self-tests VOID` line and `cli/engine/command.py:1104-1108`'s exit 1 — and it prints MORE than an abort would: the realized-series window, the self-tests block, and `instrument_self_check`'s record-47 comparison, which a `null=None` degrade path would skip on the one run where the instrument has just declared itself inconsistent. **What it prints for the new arm is one widened string.** `plausibility_checks` describes the flag in exactly one place — `cli/engine/soak.py:842`, `messages.append("null: reconcile_ok is False (live-cost reconstruction diverged)")` — and `soak_report` runs that function twice, inside `self_tests` (`:881`, into `SelfTestReport.messages`) and again at `:1583` into `void_reasons`, so the flag is DESCRIBED in exactly one string and that string is read on both faces under a sentence naming the P&L recost alone, while every other surface renders the bool with no reason beside it — `reconcile: FAILED` at `:879`, `reconcile_ok   : FAILED` and `self-tests VOID` in the SELF-TESTS block (`:1298`, `:1300`), and `self-test VOID: reconcile_ok=False` in NO VERDICT (`:1582`). Widen that ONE string to name both bookkeeping identities the flag now ANDs — the live-cost reconstruction and the cap-breach count — rather than returning a reason beside the bool, which would break every one of the six call sites this bullet keeps untouched. `test_soak_check_exits_non_zero_when_the_null_reconciliation_fails` below is where that wording is pinned, since it is the one test that renders the message. The caps arguments are not optional: `apply_position_caps` defaults to `long_cap=0.20, short_cap=0.10` (`cli/risk/limits.py:12-13`) while both builders cap with `c.long_cap`/`c.short_cap` (`cli/portfolio/crossfreq_system.py:337`, `:685`), so under a non-default `CrossfreqSystemConfig` the two `capped` books differ and both `reconcile_ok` arms fire on healthy data. `n_periods` has no such twin — it is `result.n_periods` and every per-bar list is already built from it — so it is the only other derived scalar and it needs nothing.
2. Compute `cut = _basket_complete_index(h4_prices, config.assets)` — the h4 grid, because the null's bars are h4 bars. **It runs before the builder because the builder does not survive state (b) below**: a leg whose two files exist and hold zero rows reads back as 0 stamps through `read_store_series` and `select_model_inputs` pads it to `[None] * len(union_ts)`, and `build_crossfreq_system_fast` on a ten-key panel carrying one such column raises `ValueError: negative dimensions are not allowed` — measured at 400 daily bars with the column dead on the h4 grid alone, the daily grid alone and both, and on both grids at 4, 20 and 60 daily bars — which is neither a `SoakError` nor an `EngineError` nor a `PortfolioError`, so it reaches no handler in `soak_report` and none at `cli/engine/command.py:1094`. Ordered after the builder, the refusal this step exists to raise would never be reached on the commonest (b) shape. The helper reads `h4_prices` and `config.assets` alone — nothing the builder produces — so running it first leaves the builder's INPUT untouched and spec D2 intact.
3. Slice every parallel list from `cut` and recompute the two scalars: `n_periods` becomes the retained length and `cap_breach_bars` is recomputed from the retained `cap_breach`, never carried over. **A cut that retains nothing refuses rather than building an empty null**: `cut >= result.n_periods` raises `SoakError` naming the cut and the last scored bar. That is the state the day a new leg's first price is the canonical's last h4 bar, and it would produce two wrongs of which only one is caught downstream. The PAGE renders "n/a" everywhere over an empty `void_reasons` — `plausibility_checks` iterates an empty `net_live` and finds nothing, `reconcile_ok` is computed pre-cut, `is_degenerate` reads the realized side — and the short-null void reason below now catches exactly that. The STAMPS are not caught by anything else: Task 3 populates `reference_span` from `(h4_ts[cut], h4_ts[n - 1])`, which at `cut == n` names a span the null holds no bars in — the WHOLE canonical span at `cut == n == 0` — and the null-reference block prints it above the void gate, so a void run carries it too. The stamps are what this refusal is for. **That refusal closes one POINT of a range, and the range is closed in `soak_report`** — the state it names holds for every retained count below the discrimination cutoff, not only for zero, and `build_null` never sees the realized length the cutoff is measured against, so it cannot evaluate it. The short-null void reason below carries the rest of the range.

A comment on the slice states what D2 fixes: the builder saw the whole history, so the legs already present are warm at the first retained bar. The newest leg is NOT, and the comment must not gain a clause saying it is — spec D2 measures that cold prefix and states that the live engine has no counterpart, so a comment analogising the two would assert the one thing D2 refutes; those two sentences are the whole family, this one and D2's. It is its OWN block, separated by a code line from the four-line key-space comment that sits directly above `weights = …` — written against that comment the two are one block and the gate refuses at `comment-block 6 > 4` (`prose-tripwire.py`'s `COMMENT_BLOCK_LINES`), so put it on a later slice line instead.

**A null too short to discriminate is a void reason, not a rendered page of "n/a".** `analyze_soak` computes each gating metric's `effective_n` as the null's bar count over that metric's own window — `cli/engine/soak.py:1003` for the five structural metrics, `:1048` and `:1060` for the two internals-dependent ones — and `metric_verdict` returns "n/a" below 3 (`:472`), so a null retaining fewer than three realized windows renders a complete-looking page with no verdict anywhere on it and an EMPTY `void_reasons`: `plausibility_checks` reads the null only for non-finite `net_live` values (`:838`) and for `reconcile_ok` (`:841-842`), which is computed pre-cut, and `is_degenerate` reads `realized.weights` alone (`:1164`). In `soak_report`, beside `void_reasons.append("degenerate window")` (`:1590`), append a reason naming the retained count and the realized length when `analysis.L > 0` and EVERY key of `analysis.gating_verdicts` carries an `analysis.effective_n` under the cutoff. The `L > 0` arm is not defensive: at `L == 0` every `effective_n` is `0.0` through the `window > 0 else 0.0` arms at `:1003`, `:1048` and `:1060`, so without it the reason fires on a realized window the store closed to nothing — the case `render_report`'s comment at `:1273-1274` says the window block deliberately still renders — on a run already void on `L < floor` and `degenerate window`, and blames the null for a realized-side state. Both sides reach it — the null shrinking on a basket change that moves the cut forward, and the realized window growing, which is the horizon spec D6 records — so the reason names both counts rather than blaming one. **The cutoff is spelled once**: lift `metric_verdict`'s literal `3` to a module-level constant and read it at ALL THREE sites that test `effective_n` against it — `metric_verdict` (`:472`), `_full_range_disclosure` (`:495`) and the new void check — so the void reason, the verdicts it explains and the full-range disclosure cannot disagree. The third is not optional: `_full_range_disclosure`'s own docstring says that `width > 0` together with `effective_n >= 3` is what isolates its `"n/a"` from `metric_verdict`'s other two triggers, so a later raise of the constant would leave it printing "the null band spans the full [0,1] range at this window length -- the test has no discriminating power here" for a metric whose `"n/a"` came from sample size, on every one of its domain-carrying call sites (`:1016` for `active_frac`, `:1073` for `governor_engagement` and `cap_breach`). The three prose lines that spell the number name the constant instead, in the same edit — `metric_verdict`'s docstring (`:450`), `_full_range_disclosure`'s (`:489`) and `_judge_dual`'s `effective_n` comment (`:960`); each of those blocks is four lines today, at the comment-block line bar and well clear of the character one, so none of them may gain a line (Task 4's ratchet bullet carries all three). The run still exits 0 — a reference too short to judge against is a data state, the class `L < floor` is already in, not the code-contract violation step 1 holds at exit 1.

**Only the DATA-driven refusals are contained at the call site — and all three of them must REACH it.** A basket change leaves the canonical in one of three states until it is republished with the new leg, and spec D3 says to expect every one: (a) the leg carries no file in the tree at all; (b) every leg's file is present, but no index carries all ten finite; (c) the leg's first price is at or past the last scored bar. Steps 2 and 3 refuse on (b) and (c), and step 2's placement before the builder is what makes (b) REACH it — ordered after, the zero-row shape of (b) dies in the builder on the bare `ValueError` that step's own bullet measures. **(a) does not reach them today**: `_canonical_present` (`cli/engine/soak.py:329`) probes `BTC/EUR` alone and says present, `_load_canonical` (`:341`) then calls `read_store_series` for every `_MODEL_SYMBOLS` leg on both grids, and that function is a bare `read_parquet` (`cli/engine/store.py:208-210`), so (a) raises `FileNotFoundError` — neither a `SoakError` for the handler below nor an `EngineError` for `cli/engine/command.py:1094`, and `_load_canonical`'s own docstring already records the class. **So `_load_canonical` PROBES before it reads**: one loop over `_MODEL_SYMBOLS` × `GRID_INTERVALS` testing `_store_path(canonical_dir, symbol, interval).exists()` — the store's own layout function, added to the import at `:19` beside `read_store_series`, never a second spelling of the path — raising `SoakError` naming the missing legs, so all three states arrive as one class at one handler. **That import routes the EXISTING probe through it in the same edit**: `_canonical_present`'s return (`:331`) becomes `return _store_path(canonical_dir, "BTC/EUR", 240).exists()`, exact because `_store_path` splits on `/` (`cli/engine/store.py:79-81`) — otherwise the file derives the layout in the new probe and hardcodes it six lines above, and a later change to `_store_path`'s layout follows the derived caller alone, leaving `_canonical_present` False on every tree and every run reporting the canonical absent. `git grep -nE '(1440|240)\.parquet|/ "EUR"' -- cli/engine/` returns that line alone, so the two probes are the whole family and the file then spells the layout once. TWO docstrings are re-worded in the same edit, each inside the bar Task 4's ratchet bullet states for it: `_load_canonical`'s `FileNotFoundError` clause, which the probe removes; and `_canonical_present`'s own one-liner (`cli/engine/soak.py:330`), whose "`_load_canonical` reads every model leg on that alone" is exactly what the probe falsifies — it carries none of the three phrases Task 4's closing grep is keyed on, so it is named here, where it is falsified. The probe reaches nothing else: the two `_load_canonical` stubs (`tests/test_engine_soak.py:459`, `:2511`) bypass it, and every unstubbed caller in the tree reads a complete canonical or none at all — `test_build_null_on_real_canonical` and `test_instrument_self_check_reproduces_the_deployable_record` (`:705`) / `_flags_mismatch` (`:718`) against `data/ohlc-full`, `_skips_when_canonical_absent` (`:712`) against a directory `_canonical_present` rejects, and `test_load_canonical_reads_only_the_ten_legs_the_model_uses` (`:2594`) against a synthetic tree carrying the ten `/EUR` legs on both grids — the only one of the five that reaches the probe in CI, the other three data-gated on `data/ohlc-full` (`:440`, `:704`, `:717`) and `:712` turned back by `_canonical_present` before `_load_canonical` is called. Containment is then one handler for all three. `build_null` is called at `cli/engine/soak.py:1559` with no handler, so it would abort `soak-check` through `cli/engine/command.py:1094` and print nothing at all — no realized-series window, no STORE-BOUND WINDOW warning, no self-tests — where every other canonical-side shortfall degrades instead. Wrap that call in `except SoakError as exc`, append `f"null unavailable: {exc}"` to `void_reasons` beside `"canonical absent — null unavailable"`, and leave `null`, `analysis`, `self_test` and `internals` as `None`, exactly as the canonical-absent branch does, so `soak_report`'s contract that it never raises on a short, void or absent-canonical run still holds. **Step 1's disagreement is deliberately out of that handler's reach** — it sets `reconcile_ok` instead of raising, so the containment cannot swallow it and a code-contract violation keeps its exit 1. The only other `SoakError` inside the `try` is `build_null`'s pre-existing `path` refusal (`:358`), which the CLI cannot reach: `cli/engine/command.py:1076` rejects a bad `--path` before `soak_report` is called.

**Two existing tests stub `_load_canonical` to an empty panel and must be widened in this task** — `git grep -n '_load_canonical", lambda' -- tests/` returns both and no others: `tests/test_engine_soak.py:459` (`test_build_null_path_selects_builder`) and `:2511` (`test_build_null_casts_its_book_onto_the_live_symbol_space`). Both return `({}, [], {}, [])`, so `h4_prices` is `{}`, no index exists, and Task 1's helper refuses before either test's own assertions run. Give each a ten-key h4 panel keyed by `CrossfreqSystemConfig().assets`, every leg finite from index 0 so `cut` is 0 and neither test's subject moves, over an `h4_ts` of `result.n_periods + 1` four-hourly stamps. Step 2 sits above `build_null`'s `path` branch, so on a panel that never completes, the basket refusal would preempt the `path` refusal `test_build_null_path_selects_builder` asserts — and that assertion is a bare `pytest.raises(SoakError)` (`tests/test_engine_soak.py:489`), which the basket refusal satisfies for the wrong reason. The cut-0 panel is what keeps the bogus-path case reaching the refusal it names. **The panel alone leaves the first test raising**: `test_build_null_path_selects_builder`'s `_stub_result` carries `n_periods=0` (`tests/test_engine_soak.py:470`), and at `cut = 0` step 3's `cut >= result.n_periods` is `0 >= 0`, so `build_null` refuses before the assertion on the line after each call. Give that stub `n_periods >= 1` as well, its sleeve, multiplier, `day_index` AND `final_targets` rows lengthened to `n_periods + 1` and `governed_net` to `n_periods` — `_fake_result`'s own shape rule, stated in its docstring (`:294`), plus `final_targets`, on which that docstring is silent because `_fake_result` derives it while this stub hardcodes `{"BTC": [1.0]}` (`tests/test_engine_soak.py:463`): `_net_live_from_result`'s `reconcile_ok` reads `final_targets[a][k]` over `range(n + 1)` too (`cli/engine/soak.py:299`), so a stub short there dies on `IndexError` inside the reconciliation before the assertion the test is for. The other stub needs nothing: `:2511`'s test builds its result at `n_periods = 3` (`tests/test_engine_soak.py:2508`), so `0 >= 3` is false. **NEITHER refusal is to be relaxed to make a stub test pass** — not Task 1's, where a helper returning `0` on a basket that is never complete ships the full-span null this pair exists to remove under a report block that says otherwise, and not step 3's, where `cut > result.n_periods` or an `n_periods > 0` carve-out ships an empty `NullSystem` whose `reference_span` names the whole canonical span as the reference of a null with no bars — step 3's own reason above, and the one thing the short-null void reason does not cover, since it catches the page and the block that prints the stamps sits above the void gate. A stub that trips a refusal is a fixture problem, and the fixture is what moves.

Tests against a stubbed builder and that widened loader stub, so they need no canonical tree — in `tests/test_engine_soak.py` unless the bullet names another file:
- `test_build_null_drops_bars_before_the_basket_completes`: a stub whose late asset enters at bar k — asserts `n_periods` equals the retained count and that `weights[0]` is the bar at index k.
- `test_build_null_cuts_every_parallel_series_to_the_same_length`: **on `test_build_null_drops_bars_before_the_basket_completes`'s late-entering stub, so `cut > 0`** — asserts `len()` is equal across `weights`, `net_live`, `multipliers`, `day_index`, `governed_net`, `cap_breach` and equal to `n_periods`. This is the desynchronisation guard; it must fail against a version that slices `weights` alone. The widened loader stub is the wrong fixture for it: at `cut` 0 every list keeps its full length and `n_periods` is that length, so the test passes against the exact defect it names. Its pair is `test_build_null_keeps_everything_when_the_basket_is_complete_from_bar_zero`, which is correctly a cut-0 fixture and stays one.
- `test_build_null_recomputes_cap_breach_bars_over_retained_bars`: a stub with a clipped bar BEFORE the cut and one after — asserts `cap_breach_bars == 1`, which fails against a carried-over count of 2.
- `test_build_null_refuses_a_canonical_that_retains_no_bars`: the stub's late leg first priced at the last h4 bar, so `cut == result.n_periods` → `SoakError` naming the cut. Step 3's refusal; the contained handler carries it, so it needs no command-level twin of its own.
- `test_build_null_keeps_everything_when_the_basket_is_complete_from_bar_zero`: the true-positive control; nothing is dropped and `n_periods` is unchanged.

Step 1 is a different seam and takes its own three, the first two in `tests/test_engine_soak.py`:
- `test_net_live_reconcile_false_on_a_cap_breach_count_disagreement`, beside `test_net_live_reconcile_false_on_inconsistent_result` (`:352`): a `_fake_result` whose `cap_breach_bars` is one above what its sleeves reconstruct → `ok is False`. `test_null_cap_breach_series_sums_to_cap_breach_bars` (`:401`) is the true-positive control already at that seam and must stay green — its `assert sum(cap_breach) == r.cap_breach_bars` is now the same comparison the code makes, so keep it as the fixture's own statement of what the tamper breaks.
- `test_build_null_carries_a_cap_breach_disagreement_into_reconcile_ok`: that tamper through the stubbed builder — asserts `ns.reconcile_ok is False` and that `build_null` still RETURNS, since step 1 refuses through the self-test rather than by raising.
- `test_soak_check_exits_non_zero_when_the_null_reconciliation_fails`, in `tests/test_engine_soak_command.py`: `_patch_canonical_pipeline`'s stubs (`:146`) with two changes — `build_null` returns a `_mk_fake_null` carrying `reconcile_ok=False` (the helper hardcodes `True` at `:138`, so it takes a parameter), and `self_tests` is NOT stubbed, since that stub (`:157-158`) is what would swallow the wiring under test; stub `instrument_self_check` to `(True, …)` instead, because the fake canonical directory has nothing to rebuild from. Asserts `result.exit_code == 1`, that the report still carries the realized-series window and the `self-tests VOID` line, that `void_reasons` names `reconcile_ok=False`, and — the step-1 widening above — that the describing message names the cap-breach count and not the live-cost reconstruction alone, which is what a reader would otherwise debug. This is what pins the split: a code-contract violation must not exit 0 the way the data-driven degrade below does, and no test today takes `cli/engine/command.py:1104-1108`'s branch — the only two `exit_code == 1` assertions in that file are the invalid `--null` and `--path` refusals.

And the degrade path, three tests in `tests/test_engine_soak_command.py` because the three arms reach the handler by different routes — the real loader proves state (a) ARRIVES, the `build_null` stub proves the WIRING, and a stubbed loader over a ten-key panel proves state (b) ARRIVES:
- `test_soak_report_degrades_when_the_canonical_is_missing_a_leg`, on the same `_mk_journal_and_store` helper and with **nothing stubbed**, so the real `_load_canonical` runs its probe: a canonical directory holding `BTC/EUR/240.parquet` and nothing else — one touched file, which is all `_canonical_present` reads (`cli/engine/soak.py:329-331`) and all the probe reads. Asserts `result.exit_code == 0`, that the report still carries the realized-series window, and that `void_reasons` carries a missing leg's own name — `"canonical absent — null unavailable"` names none, so that assertion is what tells this state apart from the absent-canonical branch it would otherwise be mistaken for. This is the pair's only end-to-end exercise of state (a); its true-positive control is `test_load_canonical_reads_only_the_ten_legs_the_model_uses` (`tests/test_engine_soak.py:2594`), which runs the same probe over a complete tree and must stay green, so a probe that refused everything could not ship. It is the control BECAUSE it is ungated: `test_build_null_on_real_canonical` runs the probe over a complete tree too and is the stronger reading, but it is data-gated and skips wherever this claim would otherwise be made. The ungated one also bites on the likeliest mis-shaping, a probe written over `BASKET` rather than `_MODEL_SYMBOLS` — the spelling the sibling probe at `cli/engine/command.py:755-760` uses — which passes on `data/ohlc-full`, that tree carrying `ETH/BTC` and `SOL/BTC`, and fails only on a ten-leg one.
- `test_soak_report_degrades_when_the_null_refuses`, beside `test_soak_check_no_canonical_short_window_is_no_verdict` (`:84`) and on its `_mk_journal_and_store` helper: a canonical directory `_canonical_present` accepts, with **`build_null` stubbed to raise the `SoakError` `_basket_complete_index` would raise**. The stub is what makes this the WIRING test: it pins the handler and the four `None`s alone, and holds whatever the builder does, where the bullet below rides on step 2 preceding the builder. `tests/test_engine_soak_command.py:154` already stubs `build_null` this way. Asserts `result.exit_code == 0` — the degrade path leaves `self_test` as `None`, so `cli/engine/command.py:1104-1108` never fires — that the report still carries the realized-series window and the self-tests block, and that `void_reasons` names the null as unavailable.
- `test_soak_report_degrades_when_a_leg_carries_no_price`, on the same `_mk_journal_and_store` helper and the one-touched-file canonical directory the bullet above uses, since `_canonical_present` is unstubbed here too. **Only `_load_canonical` is stubbed**: to a ten-key daily and h4 panel keyed by `CrossfreqSystemConfig().assets`, one leg all-`None` and the rest finite, over strictly-ascending stamp lists of the panels' own lengths — what a leg whose files hold zero rows loads as. **Neither builder is stubbed**: step 2 runs before them, so the refusal is `_basket_complete_index`'s own and no builder is entered. Not `_patch_canonical_pipeline`, whose `build_null` stub (`tests/test_engine_soak_command.py:154`) is the thing under test here. Asserts `result.exit_code == 0`, that the report still carries the realized-series window, and that `void_reasons` names the all-`None` leg — the assertion that tells this state apart from the two above, neither of which names a leg. This is the pair's only end-to-end exercise of state (b): Task 1's `test_basket_complete_index_refuses_a_basket_never_complete` proves the helper alone, `test_build_null_refuses_a_canonical_that_retains_no_bars` proves (c), and the bullet above proves the wiring against a stub. Its red proof is step 2 ordered after the builder instead — the real `build_crossfreq_system_fast` then raises the `ValueError` step 2's bullet measures, which `cli/engine/command.py:1094`'s `except EngineError` does not catch, so it escapes `soak_check` and the runner reports a non-zero exit carrying it, while the two bullets above stay green.

And the short-null void reason, in the same file:
- `test_soak_check_voids_when_the_null_cannot_discriminate`: `_patch_canonical_pipeline` gains a `null_bars` keyword defaulting to today's 40 (`_mk_fake_null`'s own default), and this test passes a count low enough that every gating `effective_n` falls under the cutoff at that test's L. Derive it rather than guessing. The governor-engagement arm is the binding one — it carries the LARGEST `effective_n`, so it is the arm that falls under the cutoff last. Its numerator is the null's DISTINCT-DAY count, `len(governor_engaged_daily(null.multipliers, null.day_index))` (`cli/engine/soak.py:1047-1048`), not its bar count; `_mk_fake_null`'s `day_index=list(range(n))` puts one day on every bar, so on THIS fixture the two coincide and the arm reads `null_bars / total_days`. The other six divide by L, or by L − 1 over a numerator itself cut to `null_bars − 1` for turnover (`:1000`, `null_series = nm[m][1:]`). `_mk_journal_and_store` journals three cycles four hours apart on one day and the last never scores, so `L = 2` and `total_days = 1`, and every arm is under the cutoff at `null_bars <= 2` — measured, and a property of THIS fixture: a `day_index` that groups six h4 bars to a day divides the governor numerator by six, so any other fixture re-derives it. Asserts `result.exit_code == 0`, that the page prints NO VERDICT, and that `void_reasons` names the retained count. `test_soak_check_json_includes_internals_and_disclosures` (`:177`) is the true-positive control and must stay non-void on the default 40 — its `assert not json.loads(json_out.read_text())["void_reasons"]` is the assertion that catches a check firing on a healthy reference.

Red proof for each by the mutation its name implies, and the length guard and the count guard must go red for DIFFERENT mutations or one of the two is redundant: a version that slices `weights` alone leaves both counts right, and one that carries `cap_breach_bars` over leaves every length equal.

**The one real-data assertion.** Every test above runs against a stub, so nothing else exercises the cut over a real price panel. Extend `test_build_null_on_real_canonical` (`tests/test_engine_soak.py:441`, data-gated) with the property that defines the cut, derived from the loader and never written as `17971` or as a date, which the basket's next change would falsify: with `h4_prices`/`h4_ts` loaded in the test and `k = len(h4_ts) - 1 - ns.n_periods` the number of dropped bars, assert `k > 0` (a cut fired at all — a helper returning `0` on the real panel is otherwise a silent no-op that passes every existing assertion in that test), that every `CrossfreqSystemConfig().assets` leg carries a finite price at index `k`, and that at least one does not at `k - 1`. In the same test, `:451`'s `assert sum(ns.cap_breach) == ns.cap_breach_bars` is DELETED — step 3 recomputes the count from the retained list, so it becomes `sum(x) == sum(x)`, an assertion that cannot fail while still reading as the reconstruction-vs-builder comparison. That comparison is step 1's now, and it reaches this test through `:445`'s `assert ns.reconcile_ok is True`, which is where the full-canonical version of it lives.

## Task 3 — the report says which span the null covers

`cli/engine/soak.py`, `render_report` (`:1238`). The `REALIZED-SERIES WINDOW` block already prints the realized span. Add a `NULL REFERENCE SPAN` block beside it, **above the void gate — the `if void_reasons:` line, `:1303`** — so a void run carries it too: the span is provenance, and a run that prints NO VERDICT still says which reference it would have judged against. `:1309` is the `return _render_lines(lines)` INSIDE that branch; a block appended there renders on void runs ONLY, which is the inverse of the point. Two runs against different references are then distinguishable on the page (spec D5). `render_report`'s own docstring enumerates what still renders above that gate — "the window block and self-tests still rendered above it" (`cli/engine/soak.py:1251`) — and this block joins that enumeration, so the docstring names it here; the block is a recorded nine-line offender, so the addition lands inside those nine lines or is re-recorded with them (Task 4's ratchet bullet). It carries none of the three phrases Task 4's closing grep is keyed on.

`null` is `None` on the no-journal and canonical-absent runs (Global constraints), and `reference_span` is `None` on any `NullSystem` built outside `build_null`, so the block carries THREE guards where the realized-window block carries two (`:1263` for the span lines, `:1275` for `window_bound`). Under `if null is not None`, with `  no null reference available` otherwise — the shape at `:1272`:

- `reference` — a constant line naming WHAT the span is: the canonical's complete-basket era, not its whole extent. A reader holding a single report otherwise sees a span and cannot tell it is a cut (spec D5).
- `retained bars` — `null.n_periods`, which Task 2 redefined to the retained length, so no second count exists to drift from it. Task 2's short-null void reason names that same `null.n_periods`, so a count too low to discriminate is stated as a refusal rather than left for a reader who knows the cutoff to infer from this line.
- `no-book bars` — how many retained null bars carry `structural_metrics`' no-book sentinel, with the total they are out of.
- `first bar` / `last  bar` — the two stamps of the reference span, under a SECOND guard, `if null.reference_span is not None`, with `  no null reference stamps available` in its place otherwise. The three lines above still print on that arm: none of them reads the stamps. The guard is load-bearing, not defensive — `reference_span` carries a default (the stamps paragraph below), 21 of the 25 `render_report(` call sites in `tests/test_engine_soak.py` hand the renderer a null built without it (`grep -c "render_report(" …` is 25, `grep -c "render_report(None" …` is 4), and `null is not None` alone makes these two lines a `TypeError` on `None[0]`. **It is not to be discharged by giving `_mk_null` a stamp pair**: that clears the tests and leaves the arm unguarded.

Under `if realized is not None`, so it still prints when the canonical is absent and the trigger below stays readable:

- `realized no-book bars` — the same count over the realized window, with its own total.

**One helper, both counts, both faces.** A module-level helper takes a `weights` list and returns the count of bars whose `structural_metrics(...)["hhi"]` is `0.0` together with the length it is out of; `render_report` and `_json_payload` both call it and neither recomputes either number. Counting on the sentinel branch's own OUTPUT rather than on a second spelling of its `gross > 1e-12` test is what stops a count disagreeing with the branch it reports; the single helper is what stops the two faces disagreeing with each other. The realized line is what `T0184`'s parked aggregation ruling is read off: a realized window carrying a no-book bar is the point at which the live figure itself is biased rather than only the reference it is compared against (spec D5).

**The stamps field.** `NullSystem` gains ONE field, `reference_span: tuple[datetime, datetime] | None = None` — the first and last retained stamp, never a seventh per-bar list, so the Global constraints' parallel-list enumeration stays closed and Task 2's length guard needs no new name. It is populated in `build_null` (`cli/engine/soak.py:369`), not in the renderer: `(h4_ts[cut], h4_ts[n - 1])` with `n = result.n_periods`. Task 2 step 3 refuses when `cut >= n`, so `build_null` never returns a null without stamps; the `None` arm is the state of a `NullSystem` constructed anywhere else, which is why the renderer guards it. **`h4_ts[cut:]` is the wrong slice** — `h4_ts` is one longer than every per-bar list (`:319` records the same offset for `multipliers`), so its last entry is the forming bar the null never scores: on today's canonical `h4_ts[-1]` is 2026-03-31T20:00 against a last scored bar of 2026-03-31T16:00, and a span whose end postdates the bars the verdicts were computed over defeats the disclosure. The field's default keeps the two `NullSystem` construction sites outside `build_null` CONSTRUCTING unchanged — `_mk_null` (`tests/test_engine_soak.py:844`) and `_mk_fake_null` (`tests/test_engine_soak_command.py:132`), both of which pass all ten of today's fields by keyword, no field of `NullSystem` carrying a default — and it is what leaves `reference_span` `None` on every null they build, which is the state the renderer's second guard and the JSON face's exist for.

**The JSON face carries the same values.** `_json_payload` (`:1395`) is the machine-readable twin and today discards the null (`del null` at `:1412`) under a docstring asserting that every null-derived number already lives in `analysis` — false the moment the span exists only on `NullSystem`, and two payloads archived across this change would otherwise carry identical `provenance` blocks and different bands with nothing saying the reference moved. Add a `null_reference` block beside `provenance` carrying the retained count, the null's no-book count and the two stamps, with the same three-way shape as the text block: `None` when `null` is `None`, and the two stamps `None` when `reference_span` is while the two counts still carry values. Put the realized no-book count at the payload's top level beside `null_reference`, not inside `provenance`: `provenance` is built under `realized is not None and realized.cycle_ts` (`:1435`) while the text line is under `realized is not None` alone, so a realized series with zero scored bars — the case the comment at `:1273-1274` says the text block deliberately still renders — would print the count on one face and omit it from the other. **That count is `None` when `realized` is `None`**, mirroring the text line's own `realized is not None` gate: `_json_payload` is called with `realized=None` on two of `soak_report`'s four paths (`cli/engine/soak.py:1539` on no journaled cycles, `:1549` when `realized_series` raised, both `None, None, None, None`), and an unguarded read of `realized.weights` there would raise `AttributeError` out of `cli/engine/command.py`, which catches only `EngineError` — breaking `soak_report`'s "Never raises on a short, void or absent-canonical run" contract on the empty-journal path, after `render_report` had already produced the page. Delete the `del null`; correct that docstring clause. The convention is the repo's own — `window_bound` lives in the realized-series block **and** in the JSON provenance (`README.md:120`, and the comment at `:1443`).

Operator-visible text: no `T<NNNN>`, no `spec <NNNNN>`, no `Phase <N>`, no `iter-<N>` and no `D<n>` decision numbers — `tests/test_internal_terms_not_operator_visible.py`'s `VOCABULARY` bans each of them over every non-docstring string literal under `cli/`, and a decision number is this task's likeliest slip because D5 is its whole subject (`.claude/rules/operator-facing-text.md`). It must pass.

Tests:
- `test_report_states_the_null_reference_span` (`tests/test_engine_soak.py`): a **NON-void** render — a full `SoakAnalysis` and `void_reasons=[]`, the shape `test_render_report_banner_and_vocabulary_lock` (`:1360`) already builds — on a null built as `replace(_mk_null(...), reference_span=(first, last))`, since `_mk_null` leaves the field at its default and the render would otherwise print the stamps fallback; `replace` is already imported (`tests/test_engine_soak.py:4`), and giving `_mk_null` a stamp pair instead is the repair the stamps-guard bullet above forbids. It asserts the block is present and carries the reference line, the retained count and both stamps, and fails against today's renderer. Non-void is what pins the placement: inside the void branch is the cheapest way to write the block and every other test here would pass through it.
- `test_report_names_the_absent_null_reference`: `render_report(None, rs, None, None, …)`, necessarily a void render since `analysis` is `None` — renders `no null reference available` and does not raise.
- `test_report_renders_a_null_whose_reference_span_is_absent`: a `_mk_null` whose `reference_span` is `None` — asserts the retained count and both no-book lines still print, that the stamps fallback stands in their place, and that the render does not raise. This is the guard for the state those 21 call sites are already in.
- `test_build_null_reference_span_ends_at_the_last_scored_bar`: on `test_build_null_drops_bars_before_the_basket_completes`'s late-entering stub, so `cut > 0` — the widened loader stub's cut of 0 collapses the two readings this test exists to tell apart — asserts `reference_span[1]` is the stamp at `result.n_periods - 1`, the LAST SCORED bar, and neither `h4_ts[-1]` nor the stamp at `null.n_periods - 1`, which is the retained count and lands `cut` bars early. The two spellings of `n_periods` inside this task are the trap: `retained bars` above is `null.n_periods` and is correct there; the population rule is `result.n_periods`.
- `test_report_counts_no_book_bars_in_both_series`: a DISCRIMINATING fixture — the two series differ in both count and total, e.g. a null of 5 bars with 2 no-book against a realized series of 6 bars with 1 — asserting all four numbers. Equal counts cannot tell the two lines apart, and equal totals cannot catch a line printing the other series' total; the reachable defect is a block computing both lines from one series, not one printing only the null's.
- **The payload's two faces take two tests, because no one run carries both arms.** In `tests/test_engine_soak_command.py`, `test_soak_check_no_canonical_short_window_is_no_verdict` (`:84`) is the ABSENT arm — its `assert payload["self_test"] is None  # canonical absent -> self-tests never ran` (`:123`) fixes `null` as `None` there, so neither null count is writable — and its payload assertions (`:119`–`:128`) gain `null_reference` `None` and the realized no-book count at the payload's top level equal to the text block's, which that run does carry since `realized` is present.
- `test_soak_check_json_includes_internals_and_disclosures` (`tests/test_engine_soak_command.py:177`) is the POPULATED arm: `_patch_canonical_pipeline` hands it a real null and its `assert not json.loads(json_out.read_text())["void_reasons"]` fixes the run as non-void. It gains `null_reference` carrying the same retained count AND the same null no-book count as the text block, with both stamps `None`, since `_mk_fake_null` builds a null without `reference_span`. Equality with the text block, never mere presence: presence pins no value, and the helper above is what makes the equality hold by construction.
- `test_json_payload_survives_an_absent_realized_series` (`tests/test_engine_soak.py`): the `render_report(None, None, None, None, …)` / `_json_payload(None, None, None, None, …)` pair Global constraints already enumerates for `null`, asserting the payload builds and that `null_reference` and the realized no-book count are both `None`. Nothing today covers it: all eight `_json_payload` call sites in that file pass a real `RealizedSeries` (`:1580`, `:2005`, `:2035`, `:2054`, `:2058`, `:2079`, `:2105`, `:2122`, with `:2054` and `:2058` building one from `realized_series(...)` inline), and `tests/test_engine_soak_command.py` has no test for the empty-journal path.

## Task 4 — closeout

- `docs/reference/data-catalog-full.md`: under `ohlc-full`, one sentence recording that the basket completes on 2021-12-21 with AVAX the last leg, and that a cross-sectional statistic computed over the full span therefore measures a growing universe before that date. The per-symbol table already carries the dates; what is missing is the consequence a consumer acts on.
- **The five surfaces that state the null's span or what `soak_report` catches** — the family is the CLASS, not one phrase, so it is closed by `git grep -in "frozen canonical\|frozen history\|full .*canonical" -- cli/ README.md`, nine hits today. Two further blocks this pair falsifies carry none of those three phrases and are re-worded where they are falsified rather than here — `_canonical_present`'s docstring in Task 2, `render_report`'s in Task 3. All five below are corrected here, and they divide: FOUR are rebuild clauses made INCOMPLETE, not false — spec D2 keeps the builder on the full frozen canonical history, so each of those keeps its rebuild clause verbatim and GAINS the span the retained series now cover; a rewrite that moves the rebuild itself onto the complete-basket era states the one edit D2 exists to forbid and licenses a later input slice. The fifth, `d4_gap_bps`, is the one this change genuinely falsifies: its mean runs over cut series. The other four hits are adjudicated OUT and stay as they are — `cli/engine/store.py:1` is the price store mirroring the canonical and says nothing about the null, `cli/ohlc/reach.py:65` is the canonical tree's symbol enumeration, and `cli/portfolio/crossfreq_system.py:627` and `:647` describe the BUILDER, which spec D2 keeps on the full history:
  - `README.md:120`, the `soak-check` row: "rebuilt from the frozen canonical dataset" stays and gains the span its series are retained over, that dataset's complete-basket era, and the report carries a null-reference block in the text **and** in the JSON payload (`.claude/rules/readme-usage.md`);
  - `cli/engine/command.py:1039`, `--canonical-dir`'s help: "Frozen canonical dataset root used to rebuild the backtest null" stays and gains that the null is judged over its complete-basket era — operator-visible under `.claude/rules/operator-facing-text.md`'s `--help` clause, and the one surface read without the repo open;
  - `cli/engine/soak.py:314-315`, `NullSystem`'s docstring: "the same strategy rebuilt over the full frozen canonical history" stays and gains "its per-bar series retained from the complete-basket era only" beside the P&L-recast clause already there — the sentence opens on `:314` and the grep reports the phrase on `:315`;
  - `:907`, `SoakAnalysis`'s `d4_gap_bps` comment: "mean(governed_net - net_live) over frozen history" — the aggregate is `_mean([g - n for g, n in zip(null.governed_net, null.net_live)]) * 1e4` at `:1136`, both series cut by Task 2, so after this change it is a mean over the complete-basket era;
  - `:1523`, `soak_report`'s docstring, THREE clauses: "rebuilt from the frozen canonical", which stays and gains the retained span the same way; the gate list at `:1521-1522` — "self-tests, plausibility, `L < floor`, degeneracy, and the internals rebuild's own `identity_ok`/`cap_consistent`" — which Task 2's short-null void reason joins; and "the one exception `soak_report` itself catches is the `SoakError` out of `realized_series`" (`:1527`), which Task 2's handler makes two.
- `docs/research/14.phase6-decisions.md`: the span ruling and the verdict it flips, in the form the entries this instrument already carries there use (`.claude/rules/decisions-log.md`; the `iteration-closeout` skill owns the entry format). Its `[iter-<NNN>]` tag is DERIVED at closeout, one above the highest tag under `docs/` — `grep -rhoE "iter-[0-9]+" docs/ | sort -u -V | tail -1` — never a number chosen while planning, since another branch may land one first; the changelog entry below carries the same tag in its heading.
- `docs/open-topics/T0184-soak-hhi-aggregate-averages-a-sentinel.md` — the topic's WHOLE update lands in this PR (`.claude/rules/open-topics.md`), not the status alone:
  - move the span item to `## Done so far`, rewritten as its outcome, and state the outcome the owner acts on: the cut flips the concentration verdict the report renders, from `indeterminate (instrument-fragile)` to `consistent`, and removes the `DISCLOSURES` line naming the two null constructions' disagreement. The spec's measured basis carries both readings, how each was produced, and why the era-matched one is the defensible reference;
  - **rewrite every full-span reading to name the reference it was taken against, with the era-matched figure beside it wherever the parked ruling is read off one.** The class is a present-tense sentence about THE NULL — its span, bar count, sentinel load, mean, band, or the live reading's standing in it — that after this change describes a reference `build_null` no longer builds. These are not deletions, and the rule is stated once here rather than re-derived per bullet: **each line KEEPS every pre-cut figure it carries, under a lead-in re-tensing it to the pre-cut null, and GAINS only the post-cut figures its bullet enumerates.** A bullet naming some of a line's figures is therefore an addition of those, never a replacement that silently strands the rest beside them. Call the post-cut null **era-matched**, the word the spec's own table uses, never "shipped": `T0184` already spends "as shipped" on the sentinel-included reading, and one word carrying both referents inside one sentence is how a bias figure ends up contradicting the two means it sits between. `grep -n "13\.46\|13%\|30\.55\|30\.75\|181 bars\|shipped\|3680\|27337\|49\.31\|0\.4740\|0\.323623\|0\.663\|0\.921" docs/open-topics/T0184-soak-hhi-aggregate-averages-a-sentinel.md` is the sweep that closes the family — `0\.663\|0\.921` where a narrower `0\.6634` missed `:50` — and it returns `34 35 36 46 50 56 58 62 64 73`, which are its members:
    - `:34` — "3680 of 27337 bars are sentinels, 13.46% of them" and the "181-bar leading run": era-matched, 783 of 9366 and 8.36%, and that leading run is inside the dropped span;
    - `:35` — "0.323623297632 as shipped", the "13.46% relative" bias, and "26980 window statistics, of which 13305 — 49.31%": era-matched, 0.240744764630, 8.36% relative, and 4840 of 9009 windows, 53.72%. The line's other two readings, the 0.373965003480 active-bar mean and the 0.325167903 / 0.370518180 pair, are pre-cut and stay so — the rule above, and this is the line where getting it wrong puts a bias figure between two means it contradicts;
    - `:36` — "the 30.55th percentile of the shipped windowed null" is 44.47 on the era-matched one; the sentinel-free 29.99th standing beside it in the same sentence is a pre-cut reading and stays one under the rule above. The conclusion the sentence draws INVERTS: "It is far below both, so the shift does not change its standing; a realized value nearer the null's centre would not be so lucky" describes the case the era-matched reference now is. This is the sentence that defused the aggregation's urgency; it must not survive as written;
    - `:46`'s table cell is the same class and is covered by the five-candidate-table bullet below, which marks the whole table;
    - `:50` — "its upper edge moves from 0.663 to 0.921" is the sentence the topic itself points the reader at ("the one to see in full") for the leading candidate's cost, and it is a pre-cut band edge: keep it under the re-tensed lead-in and add the era-matched pair for that same candidate beside it, 0.4006 to 0.4739, an 18.3% widening rather than 38.9%. Those are `:64`'s own figures after its correction below, so the two lines cannot disagree about one band edge of one candidate. The line's remaining pre-cut readings — the p5-offset drift and the 180-day widest window — stay as pre-cut readings under the same lead-in. `:60` is already era-matched and needs nothing;
    - `:56` — "moves the realized percentile from 30.55 to 30.75" is a pre-cut reading, and "Worth doing because 181 bars in which the system cannot hold anything are a null of nothing" is work this change performs and exceeds, so it reads as pending;
    - `:58` — "The null spans 2013-09-10 to 2026-03-31" is simply false of the era-matched null, and the decile census beneath it is a census of a span two thirds of which is now dropped;
    - `:62` — "all ten legs present only from bar 17971 of 27337" and "restricting the null has a derivable start date" are the finding that motivated this change: re-tense to the change made, which is where the span item's move to `## Done so far` lands;
    - `:64` — `0.4740` → `0.4739` (that cell was computed at a cut eight bars past the one D3's rule yields, the same slip the spec's own band cells carried), and the paragraph argues for a restriction that has now happened;
    - `:73`, the surviving aggregation next-step — "The null is where the 13.46% sits, and half its windows are affected" → 8.36% of bars over 53.72% of windows, and the same bullet's closing "costs a concentration comparison that reads 13% better diversified than the null actually is" → 8.36% as well, since the relative bias equals the flat-bar fraction exactly on either span;
  - **Two sentences the sweep above cannot reach, because neither carries a numeral.** They are a second family: a present-tense claim in `T0184` that this change falsifies while stating no figure, which is why the numeral sweep above is blind to them. It was closed by reading the file's every present-tense assertion against the change; the only other one this change moves is the span next-step at `:72`, which the first bullet of this list already moves to `## Done so far`. `grep -n "has been published\|same full span" docs/open-topics/T0184-soak-hhi-aggregate-averages-a-sentinel.md` names the two:
    - `:21` — "No HHI figure has been published in any research report or decisions log — the only occurrences under `docs/` are the spec and plan that define it — so nothing recorded is invalidated" is false the moment this PR lands: `docs/specs/00112-soak-null-reference-span-design.md`'s measured-basis table publishes concentration means, band edges and the live reading's percentile, and the `docs/research/14.phase6-decisions.md` entry above is a decisions-log entry on this subject. Re-tense it to name both as the recorded readings a later aggregation ruling would move, so the owner is not told that ruling invalidates nothing recorded;
    - `:66` — "The same builder over the same full span is the reference for the deployable's committed validation basis" stays true of the deployable and stops being an identity with the soak: after this change the deployable still scores over the full span and the soak's null does not. Name that as the contrast it becomes, keeping the shared-builder inheritance the spec's `## Out of scope` sends its reader here for;
  - mark the five-candidate table (`T0184:42-48`) as scored against the pre-cut null, since every "what kills it" cell was measured there — the owner's aggregation ruling is made off that table and would otherwise be made against a reference the code no longer builds;
  - keep `status: partial` and add the aggregation ruling's trigger, read off the report rather than reasoned about, as frontmatter and in the index bullet: `ripe_when: uv run zcrypto engine soak-check --journal-dir /mnt/zhao-crypto/engine-journal --store-dir <a store pulled from the engine host, which has no replica> reports a non-zero realized no-book bar count in its null-reference block, or an hhi verdict other than consistent; on a page carrying NO VERDICT or STORE-BOUND WINDOW a non-zero no-book count still fires the first arm, while a zero count and the verdict cell evaluate nothing`. **The two inputs are part of the condition, not context**: with neither flag the command reads `EngineConfig`'s defaults `data/engine-journal` and `data/engine-store` (`cli/config.py:34-35`; `zcrypto.toml` overrides neither), which on the workstation are the RETIRED pre-VPS engine state and not the live node's (`docs/reference/fleet.md:14`). Where that journal root holds no cycle files `soak_report` takes its `no journaled cycles found` exit — `realized` is `None`, neither no-book line prints, no fingerprint renders, and BOTH arms read unfired on an evaluation of nothing. Pointed at the replica but left on the default store, the window is store-bound and ends wherever that retired store does — the trap already on record at `docs/research/14.phase6-decisions.md:194`, where the configured store scored 54 bars of 141 cycles and genuinely changed the verdict. The topic's `## Done so far` names both real inputs, but `.claude/rules/open-topics.md` puts the evaluating check inside `ripe_when:` itself and the index echoes that line alone, so the inputs go there. The first clause fires when the live point estimate itself becomes biased rather than only its reference. The second fires on any reconciled label but `consistent` — a band-edge crossing, the `n/a` of a band with no power, or the `indeterminate (instrument-fragile)` two disagreeing null constructions produce — since that is the vocabulary `_dual_columns` renders. Both are read off the report: the no-book counts from the block Task 3 adds, the label from the fingerprint's `verdict` column. **The exclusion is asymmetric because those two lines sit on different sides of the gate**: Task 3 puts the realized no-book line ABOVE it under `realized is not None` alone, so a void or store-bound page still prints it and a non-zero count there is a positive trace whatever truncated the window, while a zero count on such a page could be the truncation's and a void page carries no verdict cell at all — `render_report` returns at the gate (`:1303-1309`) before the fingerprint renders. **Record in the same bullet that the second arm is FIRED on the pre-change build and unfired after it**: today's full-span null gives `indeterminate (instrument-fragile)` and the era-matched one `consistent` (the spec's measured basis states both and how each was produced), so the trigger is registered in a state this change created and must not be read as one that never fired. Use the `topic-ops` skill for the file mechanics and the index sync;
  - **`docs/open-topics/README.md:140`, the index bullet, gets its BODY rewritten and not only the trigger echo.** It reads today "the realized series carries none, while 13.46% of the null's bars are sentinels and half its windows contain one, biasing the null's concentration mean 13.46% low" — three figures this change falsifies, sitting outside the sweep above, which is scoped to the topic file alone. Rewrite them to the same re-tensed readings the `:34` and `:35` bullets give — the sentinel fraction of bars, the window fraction, the relative bias — and name the span they are taken against, so the index and the topic cannot state one quantity two ways. The trigger echo carries the frontmatter line VERBATIM, its two inputs included — the convention requires both copies, so the only defence against them drifting is that they are byte-identical. The index is what a draining agent and the owner read at pick time, and a correct `status` over a stale body is still drift (`.claude/rules/open-topics.md`).
- `docs/iterations-history-phase6.md`: one entry of AT MOST five bullets (`CHANGELOG_BULLETS` is 5), saying what a reader of the soak report now does differently — the null-reference block on both faces, and the concentration verdict that changed with the reference.
- **The prose ratchet is part of this closeout, not a surprise at the end of it.** `uv run pre-commit run -a` runs `infra/scripts/prose-tripwire.py --check-baseline infra/scripts/prose-tripwire-baseline.txt`, which consumes a recorded offender's "exact size first, else the smallest at least as large", so growing one past its record fails the gate. The RECORDED blocks this plan's edits can reach: `README.md:120` (`table-row 2915 > 200`) and the `zcrypto engine` section enclosing it (`README.md:104`, `section 17221 > 2048`), which grows by construction whenever that row does — the family is "a recorded row's enclosing section", and it has this one member here, the catalog's `### ohlc-full` being itself a section; `docs/reference/data-catalog-full.md`'s `### ohlc-full` (`section 3301 > 2048`), and `cli/engine/soak.py`'s `render_report` (`comment-block 9`), `soak_report` (`comment-block 8`) and `_net_live_from_result` (`comment-block 7`) docstrings, plus the changelog entry above. **Then nine UNRECORDED blocks, where growth past a bar mints a NEW offender rather than widening a known one** — the bars being `COMMENT_BLOCK_LINES = 4` and `COMMENT_BLOCK_CHARS = 528` (`infra/scripts/prose-tripwire.py:34`, `:38`), and `cli/engine/soak.py`'s baseline entries being `:279 :528 :577 :749 :944 :973 :1249 :1379 :1521` and nothing else: `_json_payload`'s docstring (four lines today); `_load_canonical`'s (anchored `:337`, four lines and 469 characters today against the 528 bar, so Task 2's probe clause has no line to spend — re-word the `FileNotFoundError` clause the probe removes rather than adding a sentence beside it); `_canonical_present`'s (anchored `:330`, one line today, its whole content the clause the probe falsifies); `NullSystem`'s docstring (two lines today, and Task 4 rewrites its FIRST line, so it re-keys as well as grows); Task 1's `_basket_complete_index` docstring, new and pinned at two lines by its own bullet; Task 3's no-book helper docstring, new; and the three blocks Task 2's cutoff-constant reword touches — `metric_verdict`'s docstring (anchored `:447`), `_full_range_disclosure`'s (anchored `:489`, whose FIRST line is the one carrying the number, so it re-keys) and `_judge_dual`'s `effective_n` comment (anchored `:957`) — each four lines today, hard against the line bar and well clear of the character one, so each reword must land inside four lines. Task 2's short-null void reason and its canonical probe keep any comment they need inside the same two bars, and its D2 slice comment is a block of its own under them (that step's bullet). Re-measure after editing, keep `--check-baseline`'s classification BEFORE `--write-baseline` as the script's header requires, and either trim to the recorded size or re-record in the same commit. Note that an offender's identity is `(path, kind, anchor)`, so retouching a block's FIRST line re-keys it and it must be re-recorded even if it shrank. `docs/open-topics/*.md` is exempt from the section bar only, so the `T0184` rewrite is safe from that one and not from the others.
- Run the data-gated tests locally, naming which data root was present: `tests/test_engine_soak.py`, `tests/test_engine_soak_command.py`, `tests/test_internal_terms_not_operator_visible.py`, `tests/test_code_prose_citations.py`, plus whatever `git grep -l soak -- tests/` adds. **`data/ohlc-full` is absent from this worktree and `test_build_null_on_real_canonical`'s skipif is a RELATIVE path**, so it skips silently here beside the passes and the skip is not coverage. Symlink the dataset in from the main checkout before the run and unlink it before the worktree is removed (`.claude/rules/agent-ops.md`'s snapshot bullet), and report that test by its own outcome — passed, never collected. It is the branch's only exercise of the cut against a real price panel.
