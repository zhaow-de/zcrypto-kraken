---
status: resolved
---

# Grafana Cloud observability — execute spec 00043

## Context — what

The design and plan are done (authored attended, 2026-07-11, during iter-084's boundary wait; three-agent adversarial pass applied): both VPS containers ship docker logs and three metric levels (OS / container / application incl. trades+portfolio) to the **already-provisioned Grafana Cloud instance** via one non-root Alloy behind a GET-only docker-socket proxy; one committed dashboard; API-provisioned email alerts. This topic parks the **execution**.

- Spec: `docs/specs/00043-observability-design.md`
- Plan: `docs/plans/00043-observability.md` (Tasks 1–3 subagent TDD, Task 4 attended deployment/shakedown)

## Why this matters

Tonight's iter-084 deployment ran on ad-hoc journalctl playbooks and dead-man pings alone — no ERROR alerting, no resource visibility on a 2 vCPU/4 GB host now running three workloads, no disk watch (the VPS journal grows ~1.4 GB/month). The disk-watermark alert this delivers is also the ripe trigger for the journal-retention topic, and the gap-ratio panel continuously watches the T0003 exit-bar quantity.

## Findings so far

- The adversarial pass caught and the spec now pins: per-rule no-data semantics (a blanket "no-data ⇒ alert" leaves the ERROR rule permanently firing when healthy), the §8 posture (non-root Alloy + GET-only proxy; the inspect-Env residual is a named waiver), capture-counter honesty (reconnect/resubscribe/segment counters must be added — additive state only), exporter failure isolation (telemetry may never kill a daemon or void a gate cycle), restart-safe staleness math (completed-at gauge + startup seeding), and the mechanical deploy embargo (`capture_metrics_enabled`/`engine_metrics_enabled` default `false`).
- Grafana Cloud IRM (OnCall) heartbeats offer dead-man semantics; the deliberate choice is to keep healthchecks.io as an independent failure domain. **Consolidation is an explicit open option, not a default** (see next steps).
- Series budget: keep-only relabeling seeded from the Docker integration's ~16-metric allowlist; target < 1 k active series on the free tier.

### The fleet's telemetry inventory (2026-07-16) — what ships, from where

Recorded here because this topic is the only place that sees the WHOLE picture; the per-increment specs each see one host. The end state is **four hosts + one workstation**, and the dashboards package below cannot be scoped until this table is filled in.

| Host | Alloy? | Metrics it publishes | Status |
|---|---|---|---|
| **NAS** (Synology, Atom) | yes | `node_*` (load/netdev/disk-free), `zcrypto_gate_*` (Role B), `archive-pull` container logs — the writer's `zcrypto_reconcile_*` + `zcrypto_trade_backfill_*` moved to ops at spec `00054`'s 2026-07-16 cutover, and the NAS's stale pre-cutover writer textfiles were **deleted at cutover** (verified: exactly one publisher per series) | **live** since iter-094. No cadvisor (DSM's kernel lacks the cgroup hierarchy it walks — it SIGSEGVs and takes down all of Alloy). Ansible-managed since T0056: the `nas` role renders `alloy-secrets.env` from `group_vars/observed/vault.yml` — no out-of-band secrets file. |
| **zcrypto-ops** (Debian i7) | **yes — live since 2026-07-16** | `node_*`, `ops_archive_pull_*`, `ops_panel_*`, `ops_verify_replay_*`, `ops_verified_replay_*` (all four textfile families existed since OPS-3/OPS-4 with **nothing scraping them** until this deploy), plus `zcrypto_reconcile_*` + `zcrypto_trade_backfill_*` (the sole publisher since the cutover), plus container logs | spec `00054` Task 1 built the role; **Task 3 (attended deploy + verify) ran 2026-07-16** — verified live, **405 active series**. Renders its secrets from the vault (`group_vars/observed/`) — Ansible-managed, like the whole fleet since T0056. No cadvisor here either, but **by choice** (series budget), not by DSM constraint. |
| **zcrypto** (capture primary + engine) | **yes — live since 2026-07-19 (iter-105, spec `00057` D5)** | `node_*` host metrics (`host="zcrypto"` via `external_labels`), Alloy's own container logs via the host journal (capture + engine ship their own logs directly since spec `00068` — see [[T0042]]), prune-unit journal lines | **host-level AND app-level telemetry both live** (spec `00069`, iter-117): capture's additive counters + the engine `/metrics` shipped and deployed — engine at rollout Step 7, 2026-07-26 (`zcrypto changed=2 failed=0`, `127.0.0.1:9102` serving 8 `zcrypto_` series + the six admitted `process_*` families) — completing spec `00043`'s original app-metrics scope. The docker-socket residual this row used to carry is **closed**: the fleet retired `discovery.docker`/`loki.source.docker` entirely at spec `00068`, so Alloy no longer talks to the socket at all — see [[T0042]] (archived). |
| **zcrypto-red** (capture secondary) | **yes — live since 2026-07-19 (iter-105)** | same shape, capture only (`host="zcrypto-red"`) | app-level counters shipped and deployed alongside the primary (spec `00069`); the docker-socket residual closed the same way — see [[T0042]] (archived). |
| **workstation** | no, and none planned | — | its two leftover systemd payloads were retired at iter-103 (spec `00056` D6), not instrumented. |

**The host-label asymmetry is resolved — the whole fleet now labels uniformly.** All four hosts' metrics carry an explicit `host` label via `external_labels` on their `prometheus.remote_write`: `host="ops"`, `host="zcrypto"` / `host="zcrypto-red"` (since iter-105), and, since spec `00069` D7 (rollout Step 2, verified live 2026-07-26: `infra/nas/config.alloy` sets `external_labels = { host = "nas" }`), `host="nas"`. For most of this topic's life the NAS was the one holdout: the `host` label its config set was on the *Loki logs* pipeline only, never on metrics — a gap found the hard way in spec `00054`, when an alert selecting `node_load1{host="ops"}` matched nothing — and it was deliberately left that way, because adding a label to an existing live series changes its identity in Prometheus, and the `Reconciler · residual gap increased` rule pages for **permanent data loss** off `increase()`/`resets()`. The owner took that series-identity risk knowingly at the spec-`00069` rollout window (a window where a false permanent-loss page was cheap to dismiss), re-keying the exactly two `{host=""}` selectors that depended on the NAS's absence-matcher (`node_load1{host=""}` in `NAS · load high`, `count(up{host=""})` in the Alloy-dark NAS rule) to `{host="nas"}` in the same change (`infra/grafana/alerts.yaml`) — verified 2026-07-26: zero `host=""` matchers remain anywhere in that file. The consequences, now settled:

- `node_load1{host="nas"}` selects the NAS; `node_load1{host="ops"}` selects ops — no host any longer resolves via an absent-label matcher.
- Both hosts' Alloy still bind the same `--server.http.listen-addr=127.0.0.1:12345`, so **`instance` still collides** — `host` is the fleet's only per-host discriminator, not `instance`.
- Benign, recorded 2026-07-19 (explicit drop, no action): every fleet Alloy logs a `remotecfg` "noop client" line at startup — the unused remote-config service's normal idle message, not an error; do not chase it.

## Done so far

- **(human sub-item — DONE 2026-07-11, this branch)** The seven `grafana_*` credentials were fetched from the provisioned Grafana Cloud instance and vaulted into `infra/ansible/group_vars/capture_host/vault.yml` via an in-process scripted append (`grafana_prom_url/user/token`, `grafana_loki_url/user/token`, `grafana_sa_token`; a single access-policy token with `metrics:write`+`logs:write` covers both writes). The append verified the six pre-existing vault keys are byte-identical and never emitted a value to the transcript. **(Path note, spec `00054`: those credentials have since MOVED — `grafana_prom_*` / `grafana_loki_*` to `group_vars/observed/vault.yml`, scoped to the hosts that ship telemetry; `grafana_sa_token` to `group_vars/all/vault.yml`, since it is workstation tooling and belongs to no host group. They were mis-scoped here from the start: no capture host runs Alloy, which is this topic's remaining work. Ciphertext unchanged — moved verbatim, never decrypted.)** This clears the topic's only human gate — the remaining Tasks 1–4 are build + attended deploy, no further account action needed.
- **(iter-094, spec/plan `00049`)** The NAS observability slice landed as part of Role B's build (see [[T0003]]): a Grafana Alloy stack on the NAS (`infra/nas/compose.yaml` + `infra/nas/config.alloy`) ships host metrics (load/netdev/disk-free), the Role B gate metric, and the `archive-pull` container's logs to the already-provisioned Grafana Cloud instance — deployed + verified live (`samples_failed 0`, `loki sent 7`, healthcheck up). Container/cadvisor metrics are **not** collected on the NAS (Synology cgroup incompatibility). The same increment also created this topic's shared, not-yet-existing infra ahead of schedule: the **single canonical dashboard** (`infra/grafana/zcrypto-dashboard.json`), the **alert rules** (`infra/grafana/alerts.yaml`: gate, NAS host, ERROR-logs), and the push tooling (`infra/scripts/grafana-push.sh`) — designed so this topic's VPS work later adds its rows to the same file. These are committed **and now provisioned live** on the Grafana Cloud instance (see the next bullet); the VPS-side Alloy/exporters and app `/metrics` endpoints remain unbuilt.
- **(iter-094, this branch — DONE, verified live 2026-07-13)** The dashboard + alert rules were provisioned on the live Grafana Cloud instance (`zcrypto2026.grafana.net`) via `infra/scripts/grafana-push.sh`: the single dashboard `zcrypto — Gate + NAS` (uid `zcrypto-main`) landed in the `zcrypto` folder, all **7 alert rules** provisioned (`Gate · not MET` / `mismatch in the last day` / `journal pull lag high` / `exporter stale` + `NAS · /volume1 free space low` / `load high` / `archive-pull ERROR logs`), each routing to the `email` contact point (present on the instance). Confirmed by a read-only API query (dashboard uid + rule titles + contact point). This closes the topic's live-provisioning sub-item; the end-to-end **alerting drill** (fire a test alert to confirm the email path reaches the inbox) is tracked under [[T0003]].
- **(2026-07-26, spec/plan `00069`, iter-117)** The remaining core — the app-level `/metrics` exporters — is built AND deployed fleet-wide: `cli/obs/metrics.py` + per-daemon taps (capture, engine, poller) ship via each host's Alloy to Grafana Cloud, completing spec `00043`'s original scope (now fully discharged, spec `00069` D6). Engine verified at rollout Step 7 (2026-07-26 13:34:39–13:35:22 UTC, `zcrypto changed=2 failed=0`; container `StartedAt` 13:35:16.873Z, `RestartCount 0`; `127.0.0.1:9102` serving 8 `zcrypto_` series + the six admitted `process_*` families; restart-safe seeding verified against live data — `zcrypto_engine_cycle_completed_at_seconds` decoded to the 12:00 cycle's completion read from the newest journal artifact, not process start). **Process-family diff (Step 8, 2026-07-26 14:17 UTC), settling the plan's own "may publish a different subset" hedge:** all four hosts publish nine `process_*` families on Alloy's own self-scrape; exactly six ship (the admitted set); the three dropped by the keep-list are `process_network_receive_bytes_total`, `process_network_transmit_bytes_total`, `process_virtual_memory_max_bytes` — verified end-to-end (the shipped `__name__` set is exactly the six admitted names, no leaked extras). **Series accounting:** measured increment = 135 new series (capture_app 82 + engine_app 16 + liquidations_app 13 + Alloy self-scrape `process_*` 24) against D9's ≤150 budget (~138 predicted) — ~10% headroom. Per-host TOTAL active series (context, not the budget): nas 144, ops 308, zcrypto 134, zcrypto-red 108, plus two anomalous `host` label values `primary`/`secondary` at 1 series each — a second, non-uniform `host=` labelling convention somewhere in the fleet, harmless for the budget and recorded here as an observation, not yet root-caused. **The untriaged "retry NAS cadvisor" item closes as an explicit posture rejection** (spec `00069` D4): technically credible (the DSM SIGSEGV class is fixable per `.tmp/cadvisor-at-synology.md`'s three tips), but disqualified because cadvisor needs the docker API to resolve container names — re-adding a socket consumer one iteration after spec `00068` removed them all fleet-wide.

- **(2026-08-05, spec/plan `00084`, iter-127 — the dedicated dashboards package, BUILT and reviewed; not yet rolled out)** The package the owner deferred on 2026-07-16 is done, and with it every remaining sub-item below except the one that moved to [[T0018]]. **Four boards** replace the single `zcrypto — Gate + NAS`: `Fleet health`, `Data integrity`, `Engine`, and a reworked `Logs` — sentence-case, untagged, split by who is asking rather than by where the metric comes from. The gap they close was measured first: the old board charted **18** families while the rules fired on **40**, so ~32 families could page an operator with no graph anywhere — including the entire continuity verify-replay sweep (11 families, four of them critical) and all 13 capture families. **The alert layer went 58 → 63 rules**: two capture liveness rules, two engine liveness rules (nothing watched engine cycles at all), and an engine ERROR-log rule (an engine ERROR paged nobody, on the host holding the live trade key); three label collapses ungrouped; six hostname-leaking titles harmonised to the display vocabulary. **Every rule now carries `__dashboardUid__` + `__panelId__`**, so a page lands on the panel plotting that rule's own expression. Two Slack notification templates carry the log line and map hostnames to role names. The **monotonic-counter recent-delta pairing** landed as a binding panel rule, not a one-off. The **JSON-log render fix** landed dashboard-side as query-time `| json` — the 2026-08-02 grooming's suspected cause was wrong and is corrected in the entry below. All the 2026-07-21 untriaged additions folded in (tags, titles, the stale per-node charts, both Slack reworks). Durable rather than one-time: `tests/test_dashboards_cover_metrics.py` asserts every alerting family is charted and admitted where its rule selects, and the bridgehead keep-regex was widened so its own collector alert can fire for it. Fourteen adversarial reviews; every guard proven to trip against a constructed defect.

## Resolution

**Resolved 2026-08-06 — the rollout the `ripe_when` waited on is executed and verified by outcome** (branch `feat/t0020-rollout`, spec `00084` plan Tasks 13–14; build merged as PR #268).

- **Live on the Grafana instance**: 4 boards (`zcrypto-fleet`, `zcrypto-integrity`, `zcrypto-engine`, `zcrypto-logs`), 63 rules with 63 dashboard/runbook pointers, both notification templates read back byte-identical. Templates live-fired into `#zcrypto` and read back: host vocabulary renders (`Capture primary`, never the hostname), the log line arrives fenced, `.PanelURL` carries a time range. The error branch was re-keyed off `.Annotations.Error` after the live fire proved `.Labels.ref_id` is never set.
- **The deprecated `zcrypto-main` board is gone durably**: deleted by hand in the UI (404 read back), and its JSON removed from the repo in the same iteration — `grafana-push.sh` ships every `*-dashboard.json` by uid, so the file's survival would have silently recreated the board at the next push. No family was drawn only by it (guard-verified).
- **The full converge chain ran**: secondary capture bake (3 rotation hours, every abort signal clear, prune `deleted=402`), ops converge (the carried [[T0044]] gauge live at 73 on the 15:12 tick), bridgehead converge (the collector alert now spans 5 hosts), engine converge inside the 00→04 gap (`cycle-04.json` completed 04:01:40.93Z; both gauge fixes proved themselves live). Digests recorded in `fleet-pins.md` row by row.
- **Measured series budget at closeout**: 819 active series — ops 330, zcrypto 175, nas 144, zcrypto-red 129, zaccess 39, plus 2 label-only strays — against spec `00043`'s <1k target; the `00069` per-host baseline (2026-07-26) sums to 694 before the 2 strays counted here, so like-for-like growth is ~123 series, of which this iteration added one family on one host. The unexplained remainder is flagged in the closeout, not attributed.
- Follow-ups carry their own registered topics: [[T0128]] (the engine ERROR rule has no dead canary), [[T0129]] (silence bars re-derived on a full 30-day base), [[T0130]] (the engine journal's constant `code_version`).
