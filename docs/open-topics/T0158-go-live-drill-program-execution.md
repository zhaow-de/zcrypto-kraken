---
status: partial
ripe_when: 'the order-path tier: rung 1 is live'
---

# The go-live drill program — executing spec 00105

## Context — what

Spec `00105` (merged in PR #350, 2026-08-29) defines the drill program that [[T0049]] set out to build: twenty failure scenarios in three tiers, a seven-part PROCEDURE shape per drill with derived bounds, the ops drill log the master plan's Stage-6b artifact list names, one resilience alert, and the Grafana-dark procedure. A spec carries no status, so this topic is the program's git-tracked residual holder: what of `00105` has landed, what is executed, what is parked for rung 1, and what a run discovered that the spec did not foresee.

## Why this matters

The go/no-go clause in `docs/research/00.master-plan.md` §12 requires *ops drills passed (kill-switch, WS-loss, restart-reduce-only)*; the engine has never been armed, so every order-path drill has harness evidence at most, and the telemetry-tier paths have been proven only by whichever incidents happened to fire them. Without a topic, a drill that surfaces a defect mid-window has nowhere to register the remainder except a report nobody re-reads.

## Findings so far

- **The spec's own inventory** (`00105` D3/D4): the telemetry tier — C (ingest plane dark), C′ (Grafana watchdog), I (watermark), J′ (the `/fail` route), K (Alloy kill), O (timer death), P+R (the secondary), Q (phone push), plus the proven tier F/H/J/L/M/N/T with N due for re-verification because [[T0048]]'s fix changed its path after its proof; the order-path tier — A1, A2, B, D, E, E′, F2, G — written now, run at rung 1.
- **Nine of the ten telemetry drills need no human at the keyboard, and that nine COUNTS SOMETHING ELSE than the TEN entries now in `## Done so far`**: this one counts drill **letters** with any autonomous portion (every letter but P), that one counts log **entries** written. The two will not reconcile by membership and are not meant to — C contributes one letter and two entries, and Q sits in the letter count despite needing a person for its phone half. Under the owner's 2026-08-29 authorization (all Alloy actions; any container/unit/timer start–stop the loop can revert itself; only a converge forbidden), J′, K, O, I, both halves of C, C′'s staleness route, N and R run autonomously with the Kraken maintenance feed read immediately before each; P (its restore is a converge), C′'s `/fail` route (a converge) and Q's phone reading need a person.
- **A push from a branch is checked against what `develop` can resolve.** `infra/scripts/grafana-push.sh`'s header forbids pushing summaries or panel descriptions citing repo paths an unmerged branch has and `develop` lacks. Pushing after the merge is therefore the default because it needs no check; a branch push is legitimate exactly when the diff introduces no such citation — which is established by reading the diff, never assumed.
- **Drill I's throwaway healthchecks check must be created with `channels` naming the Slack integration** — a check made through the API inherits no notification integrations ([[T0085]]'s finding), so an unchannelled check breaches in silence and would record a false FAIL of the dead-man domain.
- **The four engine enhancements the order-path tier needs** — `00106` flatten ([[T0159]]), a `rest-hold` plan mode, cancel-on-stop (ruled only after drill G measures the venue's behaviour), re-cancel-on-reconnect — are registered as build-sequence items in [[T0018]].

## Done so far

- **The telemetry tier is executed against the live fleet on 2026-08-31 — all ten drills, every one `pass`.** J′, K, O, I, Q-logs, C-ops, C-secondary, C′ (staleness route), N and R are induced, reverted, recorded and verified **by value** before the next began, with the Kraken maintenance feed re-read immediately before each. Nothing `fail`, nothing `blocked`. Entries in `docs/reference/drill-log.md`; each drill's findings on its own section; proof status in `drills-telemetry.md`'s proven-tier table, where R's row also records that **P remains unproven** — a different scenario sharing R's page, whose restore is a converge. **R's entry was deliberately the last written**, after the reconciler booked its hour at 22:12:28Z: `residual_gap` flat across the tick, with the secondary's `trade_deficit_rows` the only counter that moved (+439), attributed to hour 20 from the ledger's own `hour` field rather than from the tick — five records, `trades_added` 0 on every one, and zero records for hour 19.
- **Four results the spec did not foresee, each landed on an operating surface rather than a report.** Drill O answered `ops-node.md`'s open gap — the dead-man alone catches a dead panel timer, so **no staleness rule is owed**. Drill C had to run in both halves: logs replay only where something writes to the journal during the outage, so recovery is a property of the host's workload and not of Alloy. Drill R showed the silence rules **cannot see a stopped daemon** — the dead-man is its sole detector. And a Loki-sourced dead-man sends **no resolved notice at all**, because its `logs` receiver is minted `disableResolveMessage: true` (the labels are not the cause — corrected 2026-09-02, see below), so a clear must be read from the rule state and never from channel silence.
- **The artifacts landed and are guarded.** Both runbook pages; `docs/reference/drill-log.md` with a shape test whose two assertions were each proven discriminating through `mutate-probe.sh`; `zcrypto-engine-dark-with-exposure` and its `engine.md` section; `observability.md#grafana-cloud-dark` filled from drill C's measured half beside a derived half; the index rows; and the master plan's Stage-6b artifact line naming the drill log.
- **The branch is cut and the plan is written.** `feat/00105-go-live-drill-program` carries `docs/plans/00105-go-live-drill-program.md` against spec `00105` — the task-by-task build of the two drill runbooks, the drill log with its shape test and mutation probe, `zcrypto-engine-dark-with-exposure`, `observability.md#grafana-cloud-dark`, the runbook index rows and the master plan's artifact line, plus the executed telemetry tier and the closeout.
- **The Grafana push and its first-sample read are done, on 2026-09-01.** `grafana-push.sh` upserted the file's 84 rules at **11:35:24-27Z** with a clean orphan report — 84 live against 84 in the file, which corroborates that report independently — landing `zcrypto-engine-dark-with-exposure` live; the read-back at **11:44:20Z**, nine minutes later and so a post-push sample rather than a stale one, gives `$A = 0` and `$B = 1`. **Both are real values, not the fallback**: the bare inner expressions return series on each node — `count(last_over_time(zcrypto_exec_position{host="zcrypto"}[24h]))` reads 12 for `$A`, and `min(up{job="engine_app",host="zcrypto"})` returns a series of its own for `$B` — where `or on() vector(0)` would have produced the same `0` out of a query that had failed. Twelve symbol-labelled series all at 0, exactly the 2026-08-31 shape; the rule reads `state: inactive, health: ok`. The push ran from `fix/nas-deadman-resolved-notice` rather than from merged `develop`, and the constraint above was checked against the **whole** pushed set rather than this one rule: the branch's `infra/grafana/` differs from `develop` in exactly two strings, both expressions, so nothing pushed cites a path `develop` lacks.

- **C′'s `/fail` route and drill Q are both DISCHARGED, 2026-09-01, in one induction** (`C′-fail` in `drill-log.md`). The route restores only by a second converge, which is why it waited for an attended window; it was induced at 21:32:26Z and reverted at 21:39:43Z, total inverted window **7 min 17 s**, recovery confirmed by value (`hc_check_up` 1, `max(hc_checks_down_total)` 0). **The bound it produced: 11 s from failing probe to the operator's phone — and 62 s more before `hc_check_up` moved.** That gap is the healthchecks.io → Alloy scrape lag, so a responder watching Grafana sees green for a full minute after the page has fired; the page is timed from the device, never from the metric. **Q is closed by the same induction**, with the reading it exists for: arrival 23:36:04 local, channel on *all messages*, **DND off at that moment** — the at-the-time value that `K`, `I` and `Q-logs` each record as not captured, because no one was at the device when those ran.


- **The resolved-notice rollout is DROPPED, and the mechanism it rested on is disproved — drill N re-induction, 2026-09-02.** The pilot fired at 21:24:40Z and cleared ~21:28Z with both arms already `{}`, and **no resolved notice arrived**. The labels were never the mechanism: `zcrypto-hcio-watchdog` has the same `or on() vector(N)` shape, is equally unlabelled, and resolved to the same channel in ~1 min — it differs only in receiver. The real cause is that the `logs` contact point is minted `disableResolveMessage: true`, deliberately since T0047, so **no Loki-sourced rule can send a resolved notice at all**. The seven-sibling roll would have changed nothing and is cancelled; `alerts.yaml`'s comment beside the pilot is rewritten to say so. What survives as the operating instruction — **a clear is read from rule state, never from channel silence** — is now in `drills-telemetry.md`'s standing rules. Any real fix is a ROUTING change (move the Loki dead-men to `metrics`, or mint a third receiver), not an expression edit.
- **Drill P is RUN, 2026-09-04, riding the capture rollout's secondary converge — recorded `partial` in `drill-log.md` pending the reconciler's book of hours 08/09, then `pass`.** Induced 08:56:36Z (R's stop, then a nonexistent image reference in the rendered compose; the unit looped 125 times on `Restart=always`); `zcrypto-capture-red` paged by staleness at its own bound — hc.io down 09:16:33Z, `hc_check_up` 0 and `hc_checks_down_total` 1 by 09:17:30Z, inside the 21 min cap re-quoted from the check; `zcrypto-alloy-dark-capture-secondary` quiet; `zcrypto-hcio-watchdog` followed at 09:22:40Z; the four primary-whole signals green on every 60 s tick. Restored at 09:27:12Z by the owner's second secondary converge — the command written down before induction — 30 min 36 s of hold, past the cap by the operator's keystroke; the primary was whole, so the overrun cost redundancy only. One lesson for the next executor: this drill belongs BEFORE the rollout's first secondary converge, so that converge is its restore; run inside the bake it resets the bake clock (here: gate 12:00Z → 13:00Z, primary 12:00Z → 16:00Z).

## Suggested next steps

**Every remaining sub-item is human-gated — this topic has no autonomous residual left.** The 2026-08-29 authorization recorded in `## Findings so far` lists **N** and **R** among the drills the loop may induce itself; that grant is about the *mechanism*, and blast radius overrides it here. Read the sub-item, never the grant, before inducing anything.

- **(human)** The order-path tier at rung 1 — E, E′, G, F2, A1, A2, D, B — after `00106` and `rest-hold` are built and converged; G's measurement rules cancel-on-stop; F2's result decides re-cancel-on-reconnect ([[T0018]]). **B measures decision-to-flat and is this topic's, not [[T0159]]'s** — that topic's execution half closes on the earlier baseline press against the current pin, so nothing in this tier gates it (ruled 2026-09-01).
