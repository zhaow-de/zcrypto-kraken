---
status: partial
ripe_when: **FIRED — the human convened the session on 2026-08-11, and the build is now active rather than waiting.** The gate was re-measured at convening through the canonical evaluator's export: `zcrypto_gate_status` 1, `zcrypto_gate_streak_days` **30**, `zcrypto_gate_mismatch_total` 0, the series 38 s old (fresh, not frozen). What remains is sequenced, not triggered: five specs (`00088`→`00092`, table under `## Suggested next steps`), each startable when its predecessor is merged **and converged** — with two human-money gates before `00090` can run (D3(ii) sleeve funding, D3(iii) probe sign-off) and the D3(vi) go/no-go after `00091`. The live order path carries the Fable review floor per `spec-plan-locations.md`.
  **Re-measure at convening time with the CANONICAL gate, not with a file count** — `zcrypto engine report`, or the exported `zcrypto_gate_status` / `zcrypto_gate_streak_days` / `zcrypto_gate_mismatch_total`. `cli/engine/concordance.py::evaluate_gate` (spec `00039`) makes a day clean iff all six cycles are present **and** each `completed_at` falls in `[boundary, boundary+30min]` **and** each carries no validation failure, no hash mismatch, and a **passing compare**. Replay/compare and hash verification run in a LATER pass, so a cycle that fails concordance still leaves a normal `cycle-HH.json` and no sidecar — it reads clean to a file count while `evaluate_gate` resets the streak to 0. Counting files is a cheap liveness cross-check and nothing more
---

# Phase-6 build sequence and its cross-iteration constraints

## Context — what

The Phase-6 kickoff (spec `docs/specs/00039-phase6-kickoff-design.md`, iter-079, attended 2026-07-10) fixed a build roadmap spanning several iterations: the **shadow engine** (`cli/engine/` — Nautilus `TradingNode`, REST-fed signals, input-snapshot + intended-order journal, concordance reporter; iteration number open — the 00038 trial took iter-080 and the T0019 fixed-weight trial took iter-081), then **VPS deployment** (engine ansible role + compose + key delivery; iter-082), then the **6b executor** behind the Stage-6a concordance gate. A committed spec is not a durable home for deferred actions (per `open-topics.md` — prose is not registration), so the sequence and its cross-iteration constraints live here.

## Why this matters

Three constraints bind *future* iterations and would otherwise be invisible at pick time:

1. **Cadence-agnostic engine — the trigger FIRED (iter-080): the 00038 trial ADOPTED.** The deployable-system candidate is now **registry record 44** (iter-081; supersedes records 43 and 33) — the cross-frequency combination on the **4h union calendar** with **fixed ⅓ sleeve weights** (T0019's adopted simplification; governed noc Sharpe 1.5609; three sleeves: daily benchmark w·l3 intraday-held, A1-lf weekly v0.12 offset-mean, equal-weight A2-4h ensemble; cap 20/10; governor at daily cadence — no adaptive-weight state to build, journal, or reconcile). The engine builds against record 44 via the **committed builder landed in iter-082** (`cli/portfolio/crossfreq_system.py`: `build_crossfreq_system` verified path + bit-identical `build_crossfreq_system_fast`, frozen-figure regression tests; runbook updated). The daily→4h expansion contract matters here: close-time-shifted boundaries per `expand_daily_positions`' pinned contract (the iter-080 look-ahead finding).
2. **§8 hardening before the key lands (deployment iteration)** — §12's host blocker requires the §8 hardening checklist (firewall, fail2ban, unattended security upgrades, key IP-allowlisting) verified on the VPS **before** the ansible-vault key delivery step runs. A named gate in the deployment plan, not an afterthought.
3. **Store seeding — DONE (iter-083, 2026-07-10)**: the workstation store seeded inside the REST window (all 20 series passed seam QA; overlaps 620 daily / 116 4h bars; 7,040 bars appended, 0 divergent) and stays warm under the soak service. **iter-084's VPS store options**: re-seed via REST while the window lasts (the 4h window now covers from ≈ 2026-03-12, receding daily), **rsync the warm workstation store** (the standing fallback), or the quarterly OHLCVT dump. **VPS disk sizing input**: the journal grows ≈ 0.3 GB/month (append-only, no pruning in Phase 6 — an explicit drop, decisions log `[iter-083]`).
4. **6b executor scope** — order submission + order state machine + reconciliation loop + kill-switch, **plus** §12's weekly tracking-error report in full (fills, fees, rollover accrual, slippage vs the cost model), **cost-model recalibration from real fills**, tiny-live sleeve funding (D3(ii) second half, human money action), and the **D3(iii) T2 tax-probe set** (≥ 1 margin long + ≥ 1 margin short, each held across ≥ 2 rollover events, one closed, one settled) scheduled within the first two 6b weeks so the tax verification (T2 pass or T3 fallback) closes before any ramp past 25 %. T0005's Blockpit T1 check rides alongside.

## Findings so far

**(2026-08-08) The Stage-6a gate is MET — measured, and corroborated by the canonical evaluator.** 28 consecutive clean complete-UTC days on the engine journal, 2026-07-11 → 2026-08-07: every day 6 `cycle-HH.json` and zero `failed-cycle-*`. The bar is ≥14, so it is cleared at 2× with no break. Independently confirmed at review from the NAS gate export — `zcrypto_gate_status 1`, `zcrypto_gate_streak_days 29`, `zcrypto_gate_mismatch_total 0` (2026-08-09 03:24Z; 29 as of 08-09 ⇒ 28 as of 08-07).

**That corroboration is load-bearing, because a file count is NOT the gate.** The count agreed here only because `mismatch_total` is 0. `evaluate_gate` additionally requires each cycle's `completed_at` inside `[boundary, boundary+30min]`, no validation failure, no hash mismatch, and a passing compare — and replay/compare runs in a later pass, so a concordance break leaves a perfectly normal `cycle-HH.json` with no sidecar. Read the gate through `zcrypto engine report` or the exported gauges; see the `ripe_when` above.

Read the journal the way it was read, because a wrong path returns a plausible zero rather than an error: day-dirs are `journal/<YYYY-MM-DD>/` holding `cycle-HH.json` + `orders.jsonl` + `snapshots/`. A `find -mindepth 3 -maxdepth 3 -type d` walks into `snapshots/cycle-HH/` instead and reports `ok=0` for all 176 of them, exiting 0 — which reads as a totally failed gate rather than a bad probe.

**This unblocks the gate but does not make the topic ripe** — the convening half is untouched, and it is the half that matters for a live-order path.


- Spec 00039 (kickoff decisions 1–8, the pre-registered go-live criteria incl. the pinned Stage-6a concordance semantics, and the adapter-verification protocol); decisions log `[iter-079]`.
- The go/no-go gate and every ramp step are **D3(vi) human decisions**, conditional on no gate breaches and the DD ladder untouched or correctly handled — never automatic calendar promotions.
- The governor enters live at its true carried state (×0.5).

## Done so far

- **iter-140 (spec/plan `00090`)**: the **third of the five-spec sequence** (`00088`→`00092`, table below) and the **fourth spec to land in execution order** — `00094` was slotted ahead of it by the owner's 2026-08-14 sequencing ruling and is not one of the five — and the first of the five with monetary blast radius: the rung-1 order path. `cli/engine/executor.py` is the **one** module through which an order can reach the venue, and two properties are structural rather than conventional: the gate is **taken, never held** (`_submit` evaluates it itself as its first act and accepts no verdict parameter, so no caller can carry a permission token while the arm file, kill file or venue change underneath it), and the exec-ledger row **precedes** the venue call (a failed write-ahead refuses the submission, so a fill cannot exist without a record of the order behind it). On top of that: TTL- and dedup-bounded probe plans consumed from the state dir; the maker-first ladder (join the touch post-only, ≤5 reprices, a 15-minute time-box, then ≤3 price-bounded limit IOCs — never a market order) with quantity conserved across every order of an intent; ambiguity as a first-class terminal that halts the plan's remainder rather than retrying; `reduce_only` classification judged against the **ledger**, not the adopted order's own flags, plus a startup pass that cancels every resting opener; the first four automatic kill trips (unknown-order fill, per-order overfill, per-intent cross-order overfill, post-terminal position divergence); six new metric families with their Alloy admission; the Engine board's execution and venue rows with the head panel rewritten; and the attended probe procedure as a runbook section with human gates G1–G6. **Landed and green — 3666 tests passing, 0 skipped. DEPLOYED 2026-08-19 21:27:23Z and verified DISARMED**: the engine re-pin rode the secondary's capture bake as its canary gate, `exec_armed` renders `false`, no arm file exists, no order has ever been submitted, and the probe has not run — arming stays the probe checklist's separate, owner-worded act. Full detail: `docs/iterations-history-phase6.md`'s 2026-08-18 entry; decisions: `docs/research/14.phase6-decisions.md`'s `[iter-140]` entries. [[T0138]] and [[T0140]] resolve with it; [[T0121]]'s counter sub-item ships.
- **iter-139 (spec/plan `00094` + spec `00095`)**: the third spec to LAND in execution order, though **not one of the five-spec sequence itself** — slotted ahead of `00090` per the owner's [[T0137]] sequencing ruling — the `/BTC` widening. The engine's pipeline re-keys from base keys to full symbols (`BASKET` as the single source of truth, quote-aware store paths, symbol-keyed `INSTRUMENT_IDS`/`VenueState`, per-symbol `COSTMIN` with an explicit denomination, a pure uncalled `fx_eur_notional`), the cycle journal moves to `SCHEMA_VERSION = 2` with `{1, 2}` loadable, and the gate replay becomes mixed-window-aware so the ratified streak survives the deploy boundary by construction. The traded basket widens from ten legs to twelve — `ETH/BTC` and `SOL/BTC` addressable *and* tradeable, held at structurally-zero targets — **without the model changing**: `CrossfreqSystemConfig.assets` stays the ten EUR bases and `cli/portfolio/` is byte-identical across the branch. The deployable is re-ratified as registry record **47** (owner's attended adopt, spec `00095`), superseding record 44 on a measured identity criterion with max absolute difference `0.0`. Nothing here submits an order — `00090` is still where the first call site exists. **Landed, green, and DEPLOYED 2026-08-16**: all four surfaces converged in the load-bearing order (NAS + ops first, then the secondary-capture canary and the primary's Alloy config, engine last at 16:31:29Z), one converge carrying both the `00094` and `00089` payloads. The NAS gate returned byte-identical (status 1 / streak 35 / mismatch 0), so the ratified streak survived the schema bump. The deploy owed two steps no converge performs — hand-staging the widened legs and seeding them, since the store delivery is only-when-absent and `zcrypto engine seed` is workstation-only; both are now imperatives in `capture-deploys.md`. The first schema-2 cycle then verified clean by value 2026-08-16 20:01:43Z — twelve symbol-keyed targets, no phantom rebalance, and the ratified gate streak advancing 35 → 37 through the boundary; [[T0139]] resolved.
- **iter-135 (spec/plan `00088`)**: the first of the five-spec sequence — the execution safety envelope. Two-key arming interlock (`exec_armed` config + a host arm file, both required), a latching kill switch that overrides both, a fail-closed venue-status gate reading Kraken's public `SystemStatus` at process start and after each cycle completes, a restart-triggered reduce-only hold cleared only by a human, a per-cycle execution ledger structurally invisible to the Stage-6a concordance glob, six `zcrypto_exec_*` gauges with a dashboard row and three alert rules, and `zcrypto engine exec-status`. Still submits nothing — `00090` is where a call site first exists. Full detail and the divergences from the plan: `docs/iterations-history-phase6.md`'s 2026-08-11 entry; decisions: `docs/research/14.phase6-decisions.md`'s `[iter-135]` entries.
- **The 6b session brief is assembled (2026-07-23, pre-departure)**: `docs/research/14.phase6b-session-brief.md` — gate state at parking time, the record-44 build contract with its cost anchors, the ten-item decision queue the memo's `queue #N` references point at, the already-ruled items, and the post-return deferred set. Assembled while the week's context was fresh so the **post-return** session (owner away 2026-07-29 → 2026-08-20) opens on measured state. Two things were measured rather than carried over while writing it: the journal runs **6 cycles/day** (so `soak-check`'s floor of 30 scored *bars* is five days, not a month), and **`soak-check` now runs rather than refusing** — L=65, all self-tests ok, 0 of 6 metrics outside band — which settles that [[T0064]]'s gate-day slot produces a report. What that report is worth is unchanged: structural conformance, not out-of-sample evidence.

- **iter-084 (spec/plan `00042`, merged)**: the engine on the VPS — constraint 2 honored (the §8-hardening gate ran as asserts *before* any secret rendered, and caught a real defect: the gate's own `\b` regex was Jinja-escaped to backspace, fixed + made self-diagnosing); the `engine` ansible role (secrets `0600 root:root` via `engine.env`, digest-pinned one-image/two-services compose, resource budget capture-first, rrsync read-only journal channel); the dead-man's switch (healthchecks.io `zcrypto-engine-shadow`, armed by the first cycle's ping); fail-fast `engine run` + supervision watchdog; `--journal-dir` overrides; the store delivered from the warm workstation copy (constraint 3's fallback path — read-verified on the host). **The Stage-6a gate clock started at the first VPS cycle: 2026-07-11 00:00 UTC, completed 00:01:39, verified replay bit-exact (0.00e+00), arming ping received.** Daily gate ops automated (workstation user timer 06:30 UTC: pull → verified replay of UTC-yesterday → report; attended first run clean). Both soaks run in parallel until the 6a exit review. *\[Superseded 2026-07-18 (iter-103, spec `00056` D6): the workstation soak (`zcrypto-engine-shadow.service`) and the gateops timer were retired — the VPS soak is the only one running, and the daily verified-replay now runs on the ops node (spec `00051` OPS-3 timers).\]*

- **iter-083 (spec/plan `00041`, merged — PR #105)**: the shadow node end-to-end — live price store + seeder (seeded 2026-07-10, seam QA clean), the cycle core (settle-verify both grids, union-aligned snapshots, failed-cycle sidecars, aware-UTC), the Nautilus node wrapper (restart-safe timer arithmetic), the `zcrypto engine` CLI (seed/run/cycle/replay/report), and the **workstation soak running** (systemd user service, linger enabled; first live cycle 2026-07-10 20:00 UTC journaled, fast AND verified replays bit-exact, dev evidence only — the gate clock starts on the VPS).

- **iter-082 (spec/plan `00040`, merged)**: the record-44 builder (verified + bit-identical fast path, frozen-figure regression tests, newest-row contract) and the `cli/engine` concordance core (journal contract with no-peek invariants, replay, compare, the ratified 4h gate evaluator); runbook record-44 section. Constraint 1's builder scope is delivered — the node work remains.

## Suggested next steps

**Convened 2026-08-11.** The gate was re-measured at convening through the canonical evaluator's own export rather than a file count — `zcrypto_gate_status` 1, `zcrypto_gate_streak_days` **30**, `zcrypto_gate_mismatch_total` 0, the series 38 s old at read — clearing the ≥14 bar with the export proven live rather than frozen.

**The build is decomposed risk-first into five specs**, recorded here rather than only in the spec that decided it, so each is visible at pick time. The ordering principle: containment is built and deployed before the capability it contains, and the two specs carrying monetary blast radius both sit behind machinery proven while a mistake still cost nothing.

**The table is EXECUTION order, not serial order.** `00094` was specced after `00093` but runs before `00090` — the owner's 2026-08-14 ruling on [[T0137]] put the `/BTC` widening ahead of the first real-money rung, because `00090`'s state machine, sizing and fill ingestion all consume the symbol keying `00094` changes, and building that path twice on the live-trade path is the waste. Read the rows top to bottom regardless of their numbers.

| Spec | Subject | Blast radius | State |
| --- | --- | --- | --- |
| `00088` | the execution safety envelope — two-key arming, kill-switch, venue gate, reduce-only-on-restart | none (shadow, submits nothing) | landed (iter-135) |
| `00089` | venue truth — instrument map, venue-constraint sizing, the `held` read, realized-state artifact | none (read-only) | landed (iter-138), **deployed 2026-08-16** — its owed converge rode `00094`'s |
| `00094` | the /BTC widening — symbol-keyed PAIR_KEYS/INSTRUMENT_IDS/VenueState, EUR→BTC FX sizing, XBT costmin, AND the deployable re-ratification on the twelve-leg basket | none directly (no order path), but it **re-ratifies what is traded** | landed (iter-139), **deployed 2026-08-16** |
| `00090` | the rung-1 order path — maker-first state machine, submission, fill/fee ingestion | **real money, ~€10–30** | landed (iter-140), **deployed 2026-08-19 — and DISARMED**: `exec_armed` renders `false`, no arm file exists, and no order has ever been submitted. Arming is the probe checklist's separate, owner-worded act on probe day |
| `00091` | the weekly tracking-error report and cost recalibration from real fills | none (read-only) | not started |
| `00092` | rung-3 accumulation — [[T0119]]'s `target − held`, skip-or-carry, the full loop | **real money, ~€1,000** | not started |

The deferrals the sequence creates, registered so they are owed work rather than assumptions. **Five of the original nine are discharged at iter-140** and are rewritten below as what landed rather than deleted, so a reader can see the obligation was met instead of dropped; the rest carry a named owner.

- **Automatic kill-switch trips — the reconciliation family DISCHARGED (`00090` D11, iter-140); drawdown and tracking-error still owed.** They were deliberately absent from `00088` because fill anomalies and reconciliation divergence are unobservable until `00089`/`00090`, and a guard whose defect cannot be constructed is unproven by this project's own rule. Four now ship, each seen to trip on a constructed divergence: a fill for an order the ledger has no row for, an order filling past its submitted quantity, an intent's orders filling past its target between them, and the Cache position against the intent's own fills at every terminal. The unknown-order trip is **strategy-scoped deliberately** — the owner's sanctioned hand-placed settle is an account-external fill on no order of ours and structurally cannot reach it, which a reviewer verified against the installed nautilus source rather than by reading our own wiring (since iter-143 the engine also subscribes the EXTERNAL topic; the disposition filter counts such events and routes none to the trip — the conclusion stands, with the mechanism now a stated filter rather than pure non-delivery). **Drawdown trips stay with `00092` and tracking-error trips with `00091`**, where their inputs are built.
- **Reconciliation-based clearing of the restart hold** is a later *narrowing* of `00088`'s human-only clear, never a widening: adding "and reconciliation agrees" is legitimate; removing the human act is not. Still owed, and `00090` narrowed in the same direction rather than touching it — its D10 startup pass cancels every resting opener while the hold is present, so the hold now forbids more, not less.
- **The armed-too-long alert rule is phase-appropriate, not final.** It fires on `zcrypto_exec_armed` held at 1 for over 6 h, which is correct while arming is episodic (`00088`–`00091`) and fires forever once `00092` arms the engine continuously. It must be replaced at `00092`, most likely by an armed-while-no-recent-cycle condition — not silenced. **Owner confirmed `00092` at iter-140**: `00090` adds no alert rules at all, on the reasoning that the execution families are attended-window instruments (arming stays episodic until `00092`, so nothing fires unattended) and the two states that outlive a window — a kill trip and an over-long arming — already page.
- **A venue-divergence alert is deliberately absent from `00088` too.** `zcrypto_exec_venue_ok` reading not-ok while the capture daemon's own venue reading says online can only happen when the engine's REST read path itself is broken while the venue is genuinely online — a case `zcrypto-capture-venue-not-online` cannot see, since it reads a different series (`zcrypto_capture_venue_status_total`). It is owed once the first order-submission call site exists, because only then does "the engine cannot trade" carry operational meaning; before that, alerting on the divergence would just double-page every real venue outage. It lands at the same moment as the armed-too-long rule's replacement above, so the alert story is reworked as a set rather than piecemeal. **Owner confirmed `00092` at iter-140** — the submission call site now exists, but arming is still episodic, so "the engine cannot trade" acquires continuous operational meaning only when the loop does.
- **The gate-evaluation-cadence revisit — DISCHARGED as a ruling (`00090` D4, iter-140).** `00088`'s gate was evaluated at process start and cycle completion only, so its published state could be up to four hours stale, with the heartbeat gauge making that visible rather than silent. The revisit's answer is: **every executor evaluation publishes to the `_ExecGauges`**, submission-time evaluations included, so during a probe window the published gate state is seconds fresh — while **between** cycles the `00088` cadence stands unchanged. No dedicated timer was added: `00088` already weighed and rejected one inside the live engine for observability alone, and nothing in `00090` changes that calculus.
- **The two items `00090`'s probe checklist owed — DISCHARGED (iter-140), and they are now operating-surface text, not a registration.** Both live in `infra/runbooks/engine.md`'s `engine-probe-window` section, Disarm phase: converge `exec_armed` back to `false` the **same day** the window closes (between the probe and that converge, arming is effectively one key — config rests true and only the host file was removed), and treat any restore of the engine state directory as re-arming until proven otherwise. The restore step is written **per file by name** rather than as a blanket "delete whatever you find": delete `armed` and `probe-plan.json` and only those two; a restored `kill` file is a **finding to read and work through its own section**, never something to erase, because no code path anywhere clears it and it carries the timestamp and reason that tripped it; `restart-hold` is left, since every engine start writes it anyway. The two-key claim's asymmetry itself is unrepaired and stays deliberate — `00088` D2's design — so the checklist operationalizes it rather than closing it.
- **Proving the gate *unavoidable* — DISCHARGED (`00090` D4, iter-140), and the candidate shape this bullet named was deliberately superseded.** `00088` proved the gate refuses correctly but nothing forced a submission path to consult it. The shape named here — a submitting client constructible only from a fresh verdict — was rejected on the grounds that a verdict object, however fresh at construction, is still a **value a caller can hold** while the world changes. What shipped instead: all venue-mutating calls live in exactly one module, and its submit function **takes the gate and evaluates inside**, accepting no verdict parameter, so there is no holdable token to go stale. Pinned two ways rather than asserted: a repo-wide text walk fails if any module outside `cli/engine/executor.py` so much as mentions `submit_order`/`cancel_order`/`order_factory`, and a mutation probe deleting the evaluate call was **KILLED** — the test observed a concrete order tuple reach the stub client where `client.submitted == []` was required.
- **Proving the ledger *unavoidable* — DISCHARGED (`00090` D5, iter-140), with both named gaps ruled rather than patched.** The ledger write is now a **precondition** of submission: the intent's row lands in `exec-<HH>.json` before any venue call, and a failing write **refuses the submission** — at that site the `00088`-era swallow inverts, because a fill with no forensic row is exactly the state this spec exists to make impossible. `write_exec_record` gained merge semantics so the sink's per-cycle verdict write can never drop existing `submitted` rows, and the schema bumped to 2 with a reader that validates key shape against the declared version. The one-shot `zcrypto engine cycle` gap is **closed by construction, not by patching the sink**: submissions exist only inside the node's executor, and a one-shot cycle has no node, no client and no executor — so the gap remains intent-side only, exactly as `00088` designed it. The monitoring gap is discharged by ordering plus proof, scoped honestly: the sink writes the ledger **before** touching any gauge, so between probe windows a persistently failing `write_exec_record` starves `zcrypto_exec_last_evaluation_timestamp_seconds` and the existing staleness rule pages — proven by constructing the raising writer, reading the frozen gauge, and asserting against the threshold read out of `alerts.yaml` rather than remembered. Inside a window the heartbeat stays fresh regardless, and there the cover is the write-ahead refusal itself, loudly, at the attended operator.
- **The `zcrypto_venue_*` panel gap — DISCHARGED (`00090` D12, iter-140)** *(inserted 2026-08-13)*. The Engine board gained a **venue row** for the four `00089` families alongside the new execution row, and both venue rules (`zcrypto-venue-concordance-failed`, `zcrypto-venue-snapshot-stale`) now carry `__dashboardUid__`/`__panelId__`, restoring iter-127's a-firing-alert-points-at-its-panel invariant. `tests/test_dashboards_cover_metrics.py` went green **by charting, never by suppression** — no `NOT_CHARTED` entry was added for any new family, and the test was deliberately left RED across the metrics-task boundary rather than muted while the panels were built.

**The deferrals `00090` itself carries forward (iter-140), each with a named owner rather than an assumption** — recorded here so they are visible at pick time and not only inside a spec's Out-of-scope list:

- **`00092`** — cycle-driven submission, [[T0119]]'s `target − held`, skip-or-carry, continuous arming, §10's account-wide stale-WS cancel-all-and-freeze watchdog (`00090`'s per-intent quote-silence halt is its rung-1 scope, claimed by name), per-instrument `InstrumentStatus` consultation (`00090`'s 30 s quote-freshness precondition and halt-on-reject are the rung-1 cover), plus the two alert-set items above; and the €500–1,000 tiny-live sleeve funding — the master plan's D3(ii) second half, which `00090`'s G1 discharges **at probe scale only**.
- **`00091`** — the weekly tracking-error report, cost recalibration from real fills, and automated Kraken ledger-export ingestion. Rung 1's rollover rows are read by hand from the owner's ledger export during the attended window; the standing reader is `00091`'s.
- **[[T0120]]** — the discrepancy-polling intervals, consciously **re-deferred to `00092`** at iter-140 rather than guessed: `00090`'s reconciliation is post-terminal and startup-time, not a standing poll loop, so it defines no continuous call rate to budget against.
- **`00092`** — the position-reconciliation kill trip can **false-fire on a same-symbol late fill arriving across intents**, and rung 1 excludes it by plan shape rather than by code. `_reconcile_terminal` holds the Cache to `position_before + this intent's own fills`, and `position_before` is snapshotted from the venue truth at *intent start*: a fill belonging to an **earlier** intent on the same symbol that lands after that snapshot is in `actual` but not in `expected`, so a difference above one lot step latches the kill switch on a position that was never actually wrong. Nothing in `probeplan.py` forbids the shape — it accepts a duplicate symbol without complaint; what excludes it today is that rung-1 plans carry at most one intent per symbol and run a close only once its opener is terminal with nothing in flight. **Ripe when a plan, or a cycle's emitted intent set, can carry two intents on the same symbol** — check it against the intent list the executor consumes, which `00092`'s cycle-driven submission widens by construction. The candidate fixes are re-reading the position at the terminal instead of carrying the intent-start baseline, or netting the earlier intents' post-snapshot fills into `expected`; neither is a rung-1 change, because narrowing the trip is exactly what the Task-8 ruling refused to do while positions move.
- **[[T0049]]** — the go-live drill matrix and the phone-reachable halt drill. `00090` deliberately did **not** mechanize the probe sequence into a script: it is at most two attended runs old, and freezing an unproven sequence into tooling is how scripts encode the wrong one. Mechanization follows once the drill matrix picks it up.
- **[[T0005]]** — Blockpit T1 depot mechanics, riding the G5 verdict window, human-gated as always.
- **The T3 Blockpit fallback is a *conditional* deferral with its trigger named**: built only if G5's T2 verdict fails, and its topic registration is a **step of the probe checklist's fail branch**, executed in the same session as the verdict — never a remembered promise.

The items below are the build's content, distributed across those five specs; every bullet carries whether it is autonomous or a decision.

**The build (constraint 4's scope, extended 2026-08-02 with the four pieces the placeability audit found missing — the capability map showed each exists in Nautilus or nowhere, with no build item):**

- **(autonomous)** The asset→`InstrumentId` map — none exists; every submission needs it.
- **(autonomous)** Venue-constraint sizing: `ordermin`/`costmin`/lot/tick handling on every intended order, reading the Cache's live instrument definitions — the layer [[T0119]]'s accumulation deltas pass through.
- **(autonomous)** The restart→reduce-only policy as code, not just runbook prose — §12's gate drills it, nothing implements it yet.
- **(autonomous)** Fill/fee/rollover ingestion from the executions WS into the journal — the tracking-error report's real-fill leg and [[T0090]]'s recalibration input both read from it.
- **(autonomous)** Order submission + the order state machine — the executor's core. **Includes the venue-state check at cycle time** *(the [[T0105]] ruling, 2026-08-06)*: before placing orders, read the last observed Kraken `status` — a cycle landing during `maintenance`/`cancel_only` defers or degrades instead of firing orders into a venue that will reject them. This is the surviving remnant of the pre-drain idea, right-sized by measurement: the first real event delivered `effective_time=None` on every transition (zero advance notice, both hosts), so there is nothing to key a *pre*-drain on — the status message itself, ~60 s ahead of feed silence, is the earliest signal that exists, and the natural place to consume it is the executor's own cycle entry, not a standing drain mechanism.
- **(autonomous)** The reconciliation loop: exchange state vs journalled intent, and what it does on divergence.
- **(autonomous)** The kill-switch, and the conditions that trip it.
- **(autonomous)** **Emit order/position/PnL as first-class `/metrics` families as the state machine is built — not bolted on afterward.** Gauges for position/PnL state, counters for orders/fills, inside the active-series budget, and admit every new family through the Alloy keep-lists **in the same change** (both directions of the T0051 trap: published-but-unadmitted, and admitted-but-unpublished). Inherited from [[T0095]] at its 2026-07-28 resolution, which also recorded the standing owner ruling that trading-data egress to Grafana Cloud is **accepted, live trading included** — no redaction gate. **The dashboard side is now waiting, built.** [[T0020]]'s iteration shipped an `Engine` board (uid `zcrypto-engine`, spec `00084`) deliberately titled for the component rather than for intent, precisely so these families join it as new panels without a rename. Two instructions bind that work and belong here, where the families are made: **name them so intent-versus-execution is unambiguous at `/metrics` level** — the board's existing panels all carry "intended" because today every engine family is intent-side, and a fill counter arriving under an ambiguous name would silently break that distinction on a trading dashboard; and **do not rename the existing intent families to match** — they are live series, and a rename changes series identity under `increase()`. The board's head text panel currently names `data/engine-journal` as the sole source of truth for realized state; that panel is what these families retire. *(**[[T0124]]'s sleeve-occupancy gauge did NOT need to wait for this** — the engine already serves `/metrics` with its own instrument set, so `zcrypto_engine_sleeve_gross` / `zcrypto_engine_active_sleeves` shipped standalone 2026-08-02, commit `193b2043`. They are already in the capture keep-list, so this iteration's families join an engine block that has them; do not re-add.)*
- **(autonomous)** §12's weekly tracking-error report **in full**: fills, fees, rollover accrual, slippage vs the cost model.
- **(autonomous)** Cost-model recalibration from real fills — feeds [[T0090]]'s re-quote.

**The human touchpoints (never automatic):**

- **(decision — D3(vi))** The go/no-go call, and every ramp step: conditional on no gate breaches and the DD ladder untouched or correctly handled. Never a calendar promotion.
- **(decision + money — D3(ii) second half)** Tiny-live sleeve funding.
- **(decision — D3(iii))** Schedule the T2 tax-probe set **within the first two 6b weeks**: ≥1 margin long + ≥1 margin short, each held across ≥2 rollover events, one closed and one settled — so the tax verification (T2 pass, or T3 fallback) closes **before any ramp past 25 %**. [[T0005]]'s Blockpit T1 check rides alongside.

**Entering state, not to be re-derived:** the governor enters live at its true carried state (×0.5); the build targets registry **record 44** via the committed builder (constraint 1) — **superseded 2026-08-16 by record 47**, which re-ratifies the same model on the twelve-leg basket at measured-identical metrics (max abs diff `0.0`); the entering state is unchanged in value, only its record id moved. ~~The 6a exit review also decides retiring the workstation soak~~ *(already retired — iter-103, spec `00056` D6)*.
