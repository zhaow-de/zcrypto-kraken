---
status: partial
ripe_when: 'per sub-item. Cancel sweep and adopt pass: `grep nautilus-trader pyproject.toml` shows a version carrying neither `rc` nor `dev`, OR an attended engine start on the live account is planned with an order resting at the venue that the starting process did not place, on one of BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR, ETH/BTC. Unpark spec 00111: `grep nautilus-trader pyproject.toml` shows a version carrying neither `rc` nor `dev`'
---

# T0160 — nautilus-trader nightly bump, and the flatten contract checks it must not skip

## Context — what

The repo pinned `nautilus-trader===2.0.0rc4.dev20260825` (`pyproject.toml`, index `nautechsystems`) when this topic was registered. On 2026-08-31 a scan of `nautechsystems/nautilus_trader` counted **154 commits merged since that nightly**. Three of them touch surfaces `cli/engine/flatten.py` depends on, and one of those is a latent behaviour change that would make the flatten command refuse to run rather than fail loudly.

The bump itself is trivial. This topic exists because the **checks that must ride with it** are not, and they are not derivable from the diff — they come from knowing which upstream changes intersect this repo's read path.

**It now holds a second subject, which the bump does not own.** The same adapter's order-report read drops an order whose pair its instrument cache cannot resolve — silently, with a successful empty return — and that blinds the engine's kill-switch cancel sweep **and its startup adopt pass** on five basket legs. The repair is parked pending the v2 release rather than absent, and it is registered as a sub-item below because a deferral whose only home is a PR body or an unmerged branch's prose is untracked.

**These are the surfaces corrected so far** — a list of what was checked, never a claim that nothing else claims otherwise, because every review round so far has found the scope of the previous one too narrow: the flatten command's reads, verdict, `--help` and README row and its runbook procedure; the kill-switch trip, the startup adopt pass, both of its row sweeps and the external-event filter, in code; the probe window's restart step, covering an order still resting at the start and one that closed while the engine was down alike, and the order-path drills that turn on that pass (A1, A2, F2, G), including which pair their plan may name; the go/no-go harness's open-order counts at both ends of a run — probe 6's post-run count and probe 2's read of what was already there — and their runbook steps; and the external-events counter's help text and dashboard panel. **Point-in-time records deliberately keep their original wording** — `docs/reference/adapter-verification/`, archived topics, and committed specs and plans. What remains here is the repair itself.

## Why this matters

`zcrypto engine flatten` submits market orders against the live account. Its reads are deliberately defensive: `_required(obj, field, what)` raises `FlattenUnreachable` when a named field is absent or `None`, and that abort is correct **before** the first write because aborting is free there. That same strictness turns an upstream `Some/None` change into a refusal to flatten.

A bump that lands green on the unit suite therefore proves very little about this command: the whole reason the async defect below survived ten tasks and ~1900 green tests is that the test doubles were written to match our code rather than the venue.

## The bump is not cheap — it disarms trading until an attended live-money pass re-earns it

**This is the constraint that governs sequencing, and it is mechanical, not advisory.**

`cli/engine/order-semantics-verified.json` lists the nautilus versions whose attended ~EUR 0.20 order-semantics pass has actually happened. **Two independent guards read that one file:**

- `cli/engine/execgate.py` refuses the gate at runtime when the RUNNING interpreter's `nautilus_trader` is absent from the list.
- `infra/ansible/roles/engine/tasks/main.yml` refuses a converge rendering `exec_armed=true` on a version absent from the list.

The file's own note is explicit: *"The pin is FROZEN at this string until the engine is armed on it"*, and *"Never add a version without the `docs/reference/adapter-verification/<version>.md` record that carries its PASS."*

So bumping off `2.0.0rc4.dev20260825` **cannot** be a lockfile edit followed by a converge. Until a new attended six-probe pass runs against the new version on the live account and its record is committed, the engine cannot arm — and a converge that renders `exec_armed=true` is refused by the role. \[\[T0085\]\] holds that pass as one of its three legs and records the last one (2026-08-26, PASS on all six probes, EUR 0.16).

**Consequence for planning:** the bump belongs *with* its order-semantics pass, not before it, and neither belongs in a converge window shared with unrelated residuals.

**Why the bump was live work rather than a watch.** `904862a8` is this repo's own merge of the `00106` flatten branch (PR #364), 2026-09-01 — not an upstream nautilus PR — and its landing put the flatten fix these checks lean on into the running image before the trading library under it moved.

## Findings so far

Measured 2026-08-31 against the pinned version, in the `feat/00106-engine-flatten` worktree.

**The three upstream commits that intersect this repo:**

- **`Preserve Kraken Spot request correlation after timeouts`** — "Retain request state until definitive evidence or shutdown", "Send submit and batch compensating cancels without rejection", "Reset timeout cancellation state across reconnects", "Document unknown outcomes and recovery behavior". **That reading was wrong, and the bump measured it.** The claim here was that this sits squarely on the flatten write path, as the "did my order go out?" case the incident journal exists to record, and that it was the bump's main prize. Every changed code path in the commit is the Kraken **WebSocket** order-dispatch path; `cli/engine/flatten.py` submits over REST through `KrakenSpotHttpClient` (`cli/engine/command.py:1779` builds it and hands it straight to `run_flatten`), and the engine runs `use_ws_trade=False`. It is a non-intersection for this repo at that setting, and the prize it was called is one this repo cannot collect until WS submission is adopted — which is its own change with its own evidence, not a side effect of a bump. The wrong version of this sentence pointed the next reader at the highest-stakes file in the tree to change it for a path it does not take.
- **`Refine model side enums`** — "Make core side enums fully specified and remove duplicate types", "**Use `Option` for missing sides across model and adapter boundaries**", "Preserve legacy `NO_*` aliases in Python, serde, and the C ABI". The middle bullet is the hazard; the third is the reason it may be a non-issue.
- **`Standardize adapter task lifecycles`** — may move the async surface that `cli/engine/flatten.py` was corrected against (see below).

**The pinned version's behaviour, measured, so the bump has a baseline to differ from:**

- Every `KrakenSpotHttpClient.request_*` raises `RuntimeError: no running event loop` when called outside a loop; inside one it returns a `Future` whose await yields the value.
- `inspect.iscoroutinefunction` returns **`False`** for all seven of `request_instruments`, `request_book_snapshot`, `request_position_status_reports`, `request_account_state`, `request_order_status_reports`, `submit_order`, `cancel_all_orders`. It is not authoritative over these pyo3-backed methods — **do not use it to judge whether the bump changed async-ness; call the method and look at what comes back.**
- `OrderBook.bids` and `OrderBook.asks` are `method_descriptor` — methods, not attributes.

### The order-report read's instrument lookup, and the four engine-path readings around it

Measured 2026-09-03 against the pinned wheel, on a real `LiveNode` and against a loopback venue. **These readings were taken on the parked flatten-cache branch and are not re-checkable from `develop` alone** — the two live arms need the trade credential, and the wheel ships as one stripped compiled extension (`nautilus_trader/_libnautilus.cpython-314-x86_64-linux-gnu.so`), so no source read on this machine settles them either.

**The mechanism.** The Kraken spot HTTP client's instrument cache is keyed on the instrument id and scanned by `raw_symbol`, which is Kraken's `AssetPairs` **key** (`XXBTZEUR`); an open order is looked up by its own `descr.pair`, which is the **altname** (`XBTEUR`). The comparison is raw equality inside an `if let Some(...)` with no `else`, so an unresolved row is dropped with no warning and the call returns a successful, short list. **Five of the twelve `BASKET` legs are spelled both ways — BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR, ETH/BTC** — and that set is recomputable from repo state alone, from `cli/engine/store.py`'s `PAIR_KEYS` against `cli/backfill/read.py`'s `dump_pair_name` (`tests/test_engine_flatten.py::test_the_blind_legs_are_the_two_way_spelled_basket_legs` is that computation).

The four readings that decide what a repair may and may not assume:

1. **There is no local door, and both candidates are PROVEN CLOSED** — positively established, not merely unfound. No execution-client object exists in Python to hook: the factory's public surface is exactly `['name']`, a depth-4 walk of a built node found nothing exposing `cache_instrument`, and interposition was refused four ways. And the node's own instrument path is closed by a controlled A/B on a real `LiveNode` against **two loopback venues, one per client**: an instrument present in the data client's listing and absent from the exec client's own gives `Received 0 order(s)`, while the control arm gives `external=1, open=1`. Timing was ruled out by a 70–400 ms spread.
2. **The exec client's cache is populated KEY-ONLY, not cold.** It fetches its own `/0/public/AssetPairs` and logs `Loaded 2856 Spot instruments`. So the hazard is **spelling**, not population — and the stacked worry that "an empty adopted set is indistinguishable from a spelling miss" can be struck: the set is never empty for want of population.
3. **Flatten's twin shape is INVERTED for the engine, and this is the most expensive thing here to assume.** A **distinct-id** twin — the shape flatten's own cache priming *requires* — is parsed and then dropped by the execution manager (`WARN: 1 orders skipped (instrument not in cache)`, Cache empty). A **same-id alias** reconciles fully. **A plan that says "do for the engine what flatten does" ships an inert fix.**
4. **An altname-spelled open POSITION aborts startup loudly** — `RuntimeError: Failed to get mass status from KRAKEN`, exit 1 — rather than dropping silently. The order read is the asymmetric one; the position read fails closed.

**And the executions subscription is sent with `snap_orders:false, snap_trades:false`** — measured on this build. So "the WS execution stream delivers changes, not a snapshot" is no longer an inference corroborated by `_adopt_resting_orders` existing at all: a pre-existing resting order receives no WS event and therefore never self-heals into the Cache by any route other than the reconciliation read. That closes the one link in the argument below that was previously reasoning rather than measurement.

## Done so far

### The bump's own contract checks

Do these **on the bump branch, before merging it** — each one names what to run and what result means "safe".

**DISCHARGED on the `2.0.0rc6.dev20260915` bump.** The bullets below are kept as written, because what each one asked for is the reason its answer means anything; this block is what they returned.

| # | Bullet | Outcome |
| -- | -- | -- |
| 1 | Bump and lock, full suite | Done. Three real bugs found by RUNNING the suite, not by reading changelogs: the new `Strategy.set_external_order_instrument_ids` (sealed on the observer), the `external_order_claims` → `external_order_instrument_ids` rename that quietly defeated the structural ban, and `OrderSide.NO_ORDER_SIDE` resolving to bare `None`. |
| 2 | Side enum vs `position_side` | **Safe, and closed without a venue read.** `PositionSide.NO_POSITION_SIDE` did go to bare `None`, but `PositionSide.FLAT` survives as a real member that `str()`s to `"FLAT"`, and `PositionStatusReport` refuses a `None` side outright (`TypeError: 'None' is not an instance of 'PositionSide'`). The hazard result is unconstructible at this version, so `_required` cannot trip on it and `run_flatten` cannot exit 3 this way. |
| 3 | Async surface re-measure | **Unchanged.** Outside a loop `request_instruments()` raises `RuntimeError: no running event loop`; inside one it answers a `Future`, awaitable. `iscoroutinefunction` still answers `False` for all seven, so it still must not be used to judge this. `Standardize adapter task lifecycles` did not move it. |
| 4 | `OrderBook.bids` / `.asks` kind | **Unchanged** — both still `method_descriptor` and callable, so `read_book_price`'s `book.bids()` call does not invert. |
| 5 | Re-run the env-gated flatten contract pin | **Was RED, and found the bump's one real intersection** — see the `TradeVolume` row below. Fixed by giving the awaitable probe an uncredentialed client; the assertions are unchanged. What it proved: the awaitable contract still holds and the public listing path still works. What it did NOT prove: the credentialed listing path, which is the one flatten takes. |
| 6 | In-flight REST query on an affected leg | **Half discharged.** The attended pass read the resting SOL/EUR order through startup reconciliation under the engine's own spelling (`SOL/EUR.KRAKEN`, `status=ACCEPTED`, reported by probe 6 as `other 1`), so the happy path answers. SOL/EUR is **not** one of the five two-way-spelled legs, so it cannot exercise the blindness; that half needs a resting order on BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR or ETH/BTC and is parked with the sub-item below. |
| 7 | Scan the remaining commit headers | **Done: 459 upstream commits across rc4..rc6**, triaged against this repo's call sites. Nine candidate intersections were read in full; five reach us and three survived adversarial verification. The new intersections are recorded immediately below. |

**The `2.0.0rc6.dev20260918` bump, 2026-09-18 — taken for this repo's own upstream fix rather than for the nightly.** `nautechsystems/nautilus_trader` #5005, *Fall back to public Kraken fees when TradeVolume fails*, merged upstream 2026-09-18T13:17:43Z as `93d0fc1386d6`, is this project's answer to the `794dbe4f7454` intersection the previous bump found and left OPEN on `docs/reference/adapter-verification/2.0.0rc6.dev20260915.md`: a credentialed `request_instruments` resolved maker/taker fees through an authenticated `POST /0/private/TradeVolume` whose failure propagated and aborted the entire listing, which is `cli/engine/flatten.py:294`'s first read and therefore an exit 3 instead of a flatten. The fallback is `crates/adapters/kraken/src/http/spot/client.rs` +18/−3 with its python binding, so a TradeVolume failure now yields the public fee schedule instead of taking the listing down. **That the wheel carries it is established, not assumed:** the `nightly` branch tip IS `93d0fc1386d6`, the upstream `build` workflow run on that branch carried `sha=93d0fc1386d6` from 14:18:59Z to 18:30:54Z, and the index's `cp314` manylinux wheel for `2.0.0rc6.dev20260918` answers `last-modified: 2026-09-18T18:29:26Z`, inside that run — the wheel is one stripped extension, so no source read on this machine settles it and the build's own sha is the evidence. **What the bump does NOT do is discharge that OPEN item:** the reading that discharges it is the read-only dry run against the version the engine is actually running, and the fallback's own behaviour needs a credentialed client whose TradeVolume call fails, which this bump exercises nowhere.

Checks 1 through 5 and 7 were re-run on this wheel and returned what the previous bump's table returned, with one difference in method worth keeping: **2, 3 and 4 are no longer hand probes, and they are pinned in two different files.** `tests/test_nautilus_interface_pin.py` pins check 2 — the side enums and the `None`-side refusal — and reads `62 passed`. Check 3 and check 4 are `tests/test_engine_flatten.py`'s: `test_every_client_call_the_red_button_makes_needs_a_running_loop` asserts `iscoroutinefunction` False for all seven `request_*` and that each raises outside a loop, and the stub-kind guard `test_no_stub_in_the_red_button_suite_offers_a_name_its_real_library_type_lacks` holds the `OrderBook.bids`/`.asks` kind. **Check 3's other half and check 5 are the same test**, `test_a_client_call_inside_a_loop_answers_with_an_awaitable_the_module_must_await`, gated on `ZCRYPTO_LIVE_VENUE_TESTS` and therefore skipped by the default suite: run here with the opt-in set it passes, so what check 5 proved on this wheel is the awaitable contract and the public listing path, and what it did NOT prove is the credentialed listing path — the one `cli/engine/flatten.py` takes, and the same limit the previous bump recorded. Check 1's full local suite reads `8378 passed, 35 skipped`. **Check 6 is NOT discharged and has lost its fixture:** the SOL/EUR order this project left resting for it is gone — probe 2 and probe 6 of this version's pass both read `open orders 0` — so it waits on a fresh resting order on one of the five two-way-spelled legs. **The attended ~EUR 0.20 order-semantics pass ran the same day, so the freeze rule was never broken: PASS on all six probes, 2026-09-18, EUR 0.16, recorded in `docs/reference/adapter-verification/2.0.0rc6.dev20260918.md`** and the version added to `cli/engine/order-semantics-verified.json`, which is the act that clears both arming guards. Four owed checks are registered in that record rather than here: the 20:00Z boundary cycle, the healthy-boot `unmatched` baseline, the terminal-event minting rate, and the credentialed listing read together with the fallback's own behaviour — which this version was taken for and which the pass cannot report either way, the fallback being silent by construction, so its discharge owes a listing whose TradeVolume call is MADE to fail. The engine read `zcrypto_exec_gate_level 0` when the bump was taken, so the bump cost no live capability.

**What check 7's scan of dev20260915..dev20260918 found — 58 upstream commits, two intersections beyond our own:**

- **`6f14ed18bf9f` *Keep working orders when replacing a reconciliation lifecycle* (#5003) — it narrows this topic's own second subject, and from a different cause.** `process_mass_status_for_reconciliation` cleared every order report for an instrument when the position lifecycle could not be reconstructed from the fills and was replaced with a synthetic fill, which also dropped orders still working at the venue with no fills: a cold start with such a position reported `open=0` for that instrument and those orders were never adopted, and the periodic open-order check does not create orders the cache has not seen. That is the same symptom the `## Suggested next steps` section carries — a resting order absent from `cache.orders_open(venue=_VENUE)`, so neither adopted by `executor._adopt_resting_orders` nor cancelled by `executor._cancel_resting` — reached by a second route. It does **not** close the spelling gap, which is `descr.pair`'s altname against `raw_symbol`'s AssetPairs key and has nothing to do with lifecycle reconstruction; what it removes is one further cause of the same silent `open=0`, so the repair below is still owed and its reading is owed against this wheel rather than the one it was taken on.
- **`7ee58a3def96` *Gate report task follow-up queries during shutdown grace* (#4982) — on the LiveNode path this engine runs, at teardown only.** The run loop guarded the start of continuous reconciliation against `ShuttingDown` but its three report-task completion arms gated only on task presence, so a task completing inside the shutdown grace window could start a new targeted-report or fill-report query. Completed results are now applied during grace and only the follow-up queries suppressed. The engine builds a `LiveNode` per cycle and tears it down, so this is its path; nothing under `cli/` reads those follow-up queries' results, and the change makes a teardown quieter rather than different.
- **Non-intersections, recorded so the next scan need not re-derive them:** `563b7f5c9b0a` *Fix Betfair and engine snapshot reconciliation* does touch `crates/adapters/kraken/src/execution/spot.rs`, but its hunk captures `ts_init` before the venue reads instead of after, so a mass status's `ts_init` now precedes the reports it carries — an ordering fix on the startup-reconciliation path with no outcome this repo reads. `3628bc9e9a87` *Rename OrderBookDepth10 to OrderBookDepth*, `633f34da4530` *Unify NautilusDataType on the model enum* and `a2032f9f4f7f` *Refactor Parquet catalogs and Arrow encoding* name types this tree never imports — `git grep` finds no `OrderBookDepth10`, `OrderBookDepth`, `NautilusDataType` or `ParquetDataCatalog` under `cli/` or `tests/`. The cache-query refactors `0cf5114f1fb1`, `fa3fbaf8bfef` and `700bbd290e50` keep both surfaces the four blind-read consumers use — `cache.orders_open(venue=...)` for the adopt pass and the cancel sweep, `cache.order(...)` for the two row sweeps — which the green suite exercises through its own doubles.

**New intersections this scan found:**

- **`794dbe4f7454` *Fix Kraken Spot instrument fees* — the one that matters, and it is on the red button.** `request_instruments` now resolves maker/taker fees from the account's own tier through an authenticated `POST /0/private/TradeVolume`, issued whenever the client carries credentials; the helper short-circuits only on an absent credential, and otherwise its error propagates and aborts the whole listing. `cli/engine/flatten.py:294` is exactly such a call, inside `read_listing`, whose `except` at 295–298 turns any failure into `FlattenUnreachable` and exit 3. So flatten's first read gains three dependencies it did not have: a private endpoint and the key scope it needs, a new authenticated JSON request shape, and a fee entry for every pair the listing returns — a missing one is a parse error that takes down the ten EUR pairs flatten actually needs along with everything else. The same dependency lands on `infra/scripts/kraken-fixture-mint.py:430` and `infra/scripts/e1b-order-visibility-probe.py:114`. The fee VALUES reach nothing here — nothing tracked reads `maker_fee`/`taker_fee`, the sizing and cost floor use `ordermin`/`costmin`/`lot_step`/`tick_size` only, and `parse_fill_report` is untouched. Registered as an OPEN owed check on `docs/reference/adapter-verification/2.0.0rc6.dev20260915.md`, discharged by the read-only dry run, which waits on the converge that puts THIS version on the host — the row it writes names the version the process was running. **The bump records the finding; the red button's disposition is [[T0159]]'s**, including whether anything should gate a press before that dry run — deliberately not decided here, because a change to the command is spec `00111`'s parked scope.
- **`4cd751e4fcd8` *Fix reconciliation missing-order generation*.** `generate_missing_orders` stops being a partly-ignored flag and becomes the gate on synthetic position adjustment at startup — the adjustment that aligns the Cache's position with the venue, which `cli/engine/venuestate.py:121` reads as the frozen `VenueState` the cycle sizes off. The default is still `True` and the engine's behaviour is identical, but this is the one exec-engine default the engine leans on without pinning, unlike the five `cli/engine/node.py:431-437` states. Pinned in this branch.
- **Non-intersections, recorded so the next scan need not re-derive them:** `824241ba60e9` *Standardize adapter credential handling* is a `SecretString` redaction refactor with no behaviour change at the Python boundary; `9dd7f1a1a631` *Add Kraken configurable REST retry count* adds a `max_retries` field defaulting to 3, which is the value the previous `None` already resolved to; `68a1cbc9e118` *Standardize adapter client source layout* moves private items inside the Kraken data client only; `91163e6e106b` *Standardize adapter decimal precision* touches no Kraken and no shared crate; `9280fdaefe28` *Fix RiskEngine sell-balance scale handling* corrects a rescale in a path this engine takes but changes no outcome at this repo's precisions; `f1e28dadccce` is a Rust visibility change with a byte-identical body.

**What this did to the trigger.** The bump arm is gone: it named a fleet-pins revision the bump's own delivery satisfies, and a trigger the landed work satisfied is not a trigger. The cancel-sweep arm lost its first clause for the opposite reason — it read `grep nautilus-trader pyproject.toml` *no longer reads* `2.0.0rc4.dev20260825`, which the bump made true and which nothing can ever make false again, so it would have fired on every evaluation from here to the end of the topic. That clause is replaced by the same neither-`rc`-nor-`dev` check the spec `00111` arm already carries, which is where both remaining sub-items actually wait: the owner ruled on 2026-09-15 that neither is picked up until nautilus-trader v2 reaches official release. The attended-start alternative stays as written, because an attended window with a resting order on one of the five legs is still the cheaper way to collect the reading if one is planned first.

- **Bump and lock.** Edit the pin in `pyproject.toml` to the chosen nightly, `uv lock`, `uv sync`. Per `CLAUDE.md` a `uv.lock` / `pyproject.toml` change reaches everything, so this branch takes the **full local suite**, not a reachable subset.
- **Check the side-enum change against `position_side`, first and before anything else.** Run a real `request_position_status_reports` against the account (reads only) with at least one open margin position and at least one flat/closed row, and print `repr(row.position_side)` for every row. **Safe result:** flat rows still carry a `FLAT`-like sentinel that `str()`s to `"FLAT"`. **Hazard result:** any row reports `position_side is None` — then `_required(row, "position_side", …)` raises `FlattenUnreachable` and `run_flatten` exits 3, i.e. the command **refuses to flatten an account** instead of skipping that row. If the hazard result appears, fix `cli/engine/flatten.py`'s flat-row handling in the same PR; do not merge the bump alone.
- **Re-measure the async surface** exactly as recorded under *Findings so far*: call `request_instruments()` outside a loop (expect a raise) and inside one (expect an awaitable). If `Standardize adapter task lifecycles` changed either, `cli/engine/flatten.py`'s `Recorder.call` and the `asyncio.run` boundary in `cli/engine/command.py` need revisiting together.
- **Re-measure `OrderBook.bids` / `.asks` kind.** `getattr(OrderBook, "bids")` must still be callable; `read_book_price` calls `book.bids()`. If they became properties, that call inverts.
- **Re-run the flatten contract pin** added with the async correction (the env-gated one) and say in the PR what it proved and what it did not.
- **Read the Kraken correlation fix's own tests** to learn what "unknown outcome" the adapter now reports, and check whether `cli/engine/flatten.py`'s `post_write_failure` / exit-2 path should record it distinctly rather than as a generic transport failure.
- **Measure the in-flight REST query on an affected leg** — iter-168 left it unmeasured. With a resting limit order on a two-way-spelled leg (the SOL/EUR order the owner left resting, or one placed by hand far from the market and cancelled afterwards), run the engine's order-status read (`request_order_status_reports` for that order, reads only) and print `repr(row.instrument_id)`, the row's status and the wall-clock round trip. **Safe result:** the row returns under the engine's spelling inside one tick. **Hazard result:** no row, a row under the other spelling, or a timeout — then the leg's in-flight read is blind and the bump stops here until the adapter answers it.
- **Scan the remaining ~150 commit headers for adapter- and model-level changes** not covered above (`gh api "repos/nautechsystems/nautilus_trader/commits?since=<pinned nightly date>T00:00:00Z&per_page=100" --paginate --jq '.[] | .commit.message | split("\n")[0]'`), and record any new intersection in this file before closing it.

## Suggested next steps

### The kill switch's cancel sweep and the startup adopt pass are blind on five legs — only their repair is left here

**Not a bump-branch check, and not a documentation item.** The false claims have been corrected as `## Context — what` lists; what is registered here is the capability the engine does not have.

- **The engine cannot cancel a resting order it did not place and that was already resting when it started, on a two-way-spelled leg.** `cli/engine/node.py`'s `_exec_engine_config()` sets `reconciliation=True` and `filter_unclaimed_external_orders=False`, and that second line exists precisely so a previous process's order, or one placed by hand before this process started, reaches the Cache instead of being filtered out of it. An order placed by another party *during* the run is not this gap: the discharged `unmatched` baseline in `docs/reference/adapter-verification/2.0.0rc4.dev20260825.md` records +12 events within one process from BTC/EUR orders placed beside it, so the WS path is not blind. It is necessary and not sufficient: such an order reaches the Cache **only** through startup reconciliation, which obtains order state through the order-report read the mechanism above describes — and with the executions subscription carrying `snap_orders:false`, no WS event ever heals the gap afterwards. Two consumers read that Cache through `self._client.cache.orders_open(venue=_VENUE)`, counted against `develop`: `executor._adopt_resting_orders`, the startup adoption pass, and `executor._cancel_resting`, whose own docstring opens *"A trip must leave NOTHING working at the venue"*. `_cancel_resting` issues `cancel_order` **per order** off that list, so an order the list omits is **never requested at all** — a capability gap, not a failed request, and no retry of the trip closes it. **This is the opposite failure to flatten's**, whose account-wide `cancel_all_orders` names no pair and does reach such an order; conflating the two produces the wrong operator instruction and the wrong repair. **The repair owes a venue-side open-order read at trip time, independent of the Cache** — not a wider Cache query, which reads the same populated set — and it must not be modelled on flatten's cache priming: reading 3 above, a distinct-id twin is parsed and then dropped by the execution manager, so "do what flatten does" ships an inert fix.

- **The startup adopt pass is blind to the same order, and that is a SECOND capability gap rather than a restatement of the first.** `executor._adopt_resting_orders` classifies whatever that same `orders_open` list returns, so on the five legs a previous process's resting order is not adopted, not cancelled, and its preserved ledger row is not re-attached; `_reconcile_adopted_rows` looks that row's order up with `cache.order(...)`, gets `None`, and the `continue` that follows logs nothing — so the row is neither reconciled against the venue's figure nor given its terminal state. **The lookup answers `None` whatever state that order is in**, which widens the gap past the resting case in two ways an operator reads separately. A row whose order filled, was canceled or expired *during the downtime* is the ordinary closed-while-down repair, and on these legs it does not happen: no terminal state, no `reconciled` event, no WARNING, and no latch on either divergence. And `_reconcile_finished_rows`, the third consumer, sweeps the rows this engine had already CLOSED for a withdrawn fill — the same `cache.order(...)`, the same silent `continue` — so the withdrawn-fill latch cannot arm there either. That is four consumers of the blind read in `executor.py`, not two. A later fill on a still-resting order then reaches `_on_external_event` with no `_attached` entry to match: counted `unmatched`, logged, and acted on nowhere — no row write, no execution counters, and **no overfill trip**, which is the one kill latch that fill would otherwise arm at boot. The operator consequences are that **a restart is not a way to clear such an order** and that **a quiet startup is not evidence a preserved row is current** — an unchanged row, a silent log and an absent kill file are what both a blind leg and a clean start look like — which the probe window's restart step and the order-path drills now say. **The trip's repair does not cover this one**: a venue-side read at trip time comes too late for a pass that runs at startup and must classify before it cancels, so a repair taking only the trip leaves the restart path where it is. The repair here owes a venue-side read of CLOSED orders as well as open ones, since two of the four consumers are row sweeps rather than open-order sweeps.

**Ripe when either holds, for both consumers together:** `grep nautilus-trader pyproject.toml` shows a version carrying neither `rc` nor `dev` — the owner ruled on 2026-09-15 that neither sub-item is picked up before nautilus-trader v2 reaches official release; OR an attended engine start on the live account is planned with an order resting at the venue that the starting process did not place, on one of BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR, ETH/BTC.

**What to run when ripe.** Start the engine against the live account with reconciliation live and at least one order already resting at the venue that the starting process did not place, then look for that order's `client_order_id` in `cache.orders_open(venue=_VENUE)`. A start with no such order cannot separate the two outcomes and proves nothing. **Rest it on a two-way-spelled pair** — BTC/EUR, ETH/EUR, XRP/EUR, LTC/EUR or ETH/BTC — never on SOL/EUR or another coinciding leg: a coinciding pair resolves either way, so a present order there would read as health while the two-way legs stayed invisible, which is the degenerate control this reading exists to avoid. **Safe result:** the order is present, the adopt pass logs a line naming it, and a trip cancels it. **Hazard result:** it is absent, the pass logs nothing about it, and the repair belongs on this repo's side rather than in an upstream report. The reading describes the build it is taken on and nothing else: the four under *Findings so far* were taken on `2.0.0rc4.dev20260825` and are owed again against the moved pin. They are re-taken with the repair, on the same attended live-account start, against whatever the pin reads then.

### Unpark spec `00111`, whose measurements are pinned to a wheel that churns

**Not a bump check — the thing the bump releases.** `docs/specs/00111-adapter-blind-reads-design.md` and its plan are complete, reviewed and parked as a draft PR, because every number in them was measured against `2.0.0rc4.dev20260825` and upstream moves about a dozen commits a day. The disclosure of what those defects cost an operator merged separately; what parks is the repair.

- **Unpark it, and re-measure before trusting any of it.** The branch's readings — the instrument-cache miss, the twin's identity rules, the exec client's doors, the spelling class — were all taken on the pinned wheel and are re-checkable only against the one that replaces it. Re-run them first; a number that survives is evidence, a number that is merely re-read is not. Then rebase onto `develop` and **reconcile this file again** against whatever it then carries: the last reconciliation dropped a two-armed trigger to one arm and needed a second reader to catch it.

**Ripe when:** `grep nautilus-trader pyproject.toml` shows a version carrying neither `rc` nor `dev`.
