# Open topics

Topics worth follow-up are parked here, one file per topic. See `.claude/rules/open-topics.md` for the convention.

<!-- mdformat-toc start --slug=github --maxlevel=3 --minlevel=2 -->

- [Research and development](#research-and-development)
  - [Open](#open)
  - [Partially done](#partially-done)
  - [Resolved](#resolved)
- [Live trading preparation](#live-trading-preparation)
  - [Open](#open-1)
  - [Partially done](#partially-done-1)
  - [Resolved](#resolved-1)

<!-- mdformat-toc end -->

## Research and development<a name="research-and-development"></a>

### Open<a name="open"></a>

- [T0022 — B1 intraday seasonality family](T0022-b1-intraday-seasonality-family.md) — split from T0016 when its prerequisites fired; the conditioning-overlay class was attributed out (trials 45+46 REJECT) (ripe when: a genuinely new B1 hypothesis — not a conditioning overlay on the A2 book — is pre-registered, or the human expands/closes the shared B budget).

- [T0025 — full symbol & corporate-action ledger](T0025-full-corporate-action-ledger.md) — extend iter-002's alias ledger to a full point-in-time record (redenominations, migrations, delistings) for survivorship. **The universe-refresh leg is BLOCKED by \[[T0093]\]**: `_refresh_universe` fails closed because `ohlc-full` ends 2026-03-31, and a rebuild against it would select eleven names instead of twelve; a live-tailed volume source must exist first, and quarterly dump ingestion cannot supply one (ripe when: a universe pair has a corporate action, or before a live-trading universe refresh — the refresh leg additionally needs T0093 discharged).

- [T0045 — CRC re-derivation needs raw-string price/qty](T0045-crc-rederivation-needs-raw-string-price-qty.md) — the archive stores book price/qty as Float64, but Kraken's CRC needs the raw wire strings (trailing zeros load-bearing), so byte-exact CRC re-attestation isn't derivable from the archive; OPS-3's continuity-replay (spec 00051) covers structural coherence instead (ripe when: a raw-string capture-schema change is on the table, gated on a capture re-pin).

### Partially done<a name="partially-done"></a>

- [T0023 — B2 derivatives-positioning data sourcing](T0023-b2-derivatives-data-sourcing.md) — funding substrate delivered (iter-090); liquidations LIVE via the Coinalyze poller (iter-097; Binance futures WS geo-fenced from our egresses); **the OI substrate is DELIVERED 2026-07-24 (PR #201)** — tooling, the full 2020→present backfill (10 series, 5,010,882 rows, `basket_sha256 e9f7344c…`), promotion to canonical `data/derivatives-oi/`, the hub push and the catalog entry are all done, so it is `data fetch`-able on ops. Measured coverage that **changes the B2 panel**: only BTC reaches 2020-09-01, every other symbol starts 2021-12-01, so a balanced funding+OI panel starts 2021-12-01 and **OI, not funding, is the binding leg**. **Only the B2 harness remains** as autonomous pre-work (ripe when: B2 is picked for an iteration).

- [T0016 — Bucket-B/C alpha families](T0016-bucket-b-c-alpha-families.md) — the §5 queue umbrella, now partial: B1 split out to T0022 (iter-086); remainder = B2/B3/B4, C1–C3 (budgets B=25 shared/C=10) with per-family prerequisites in the frontmatter.

- [T0044 — correcting the reconcile ledger resets the counters](T0044-reconcile-ledger-correction-resets-counters.md) — the reconcile counters are summed from an append-only ledger, so a correction/rebuild reads as a reset; the two `increase()` rules are guarded with `resets()==0` so it can't false-page, and the correction **runbook is now written** (`infra/nas/README.md`); still open: a correction marker + the O(ledger) rotation design (ripe when: the ledger needs another correction, or the ~17-min Atom cycle slows).

- [T0024 — universe spread-cap criterion](T0024-universe-spread-cap-criterion.md) — **partially done 2026-07-22** (iter-115, spec `00067`): the criterion exists and is calibrated — `cli/universe/rules.py` takes an opt-in `spreads` map with a **10 bps/side cap** priced on T0014's mean effective-spread-at-size at the €1,400 max-size position the volume floor already uses (so both criteria are commensurable). The cap is derived from the fee stack (~25 % of the tier-1 round-trip maker fee) and **binds on nothing today**: replaying the committed entries leaves selection unchanged 12 → 12, DOT worst at 6.55 bps/side with ~35 % headroom on the mean (its p99 is 10.87, above the cap — the mean is what the cost model charges). A *rebuild* is a different question and would now select eleven, for a stale-dataset reason → T0093 — tuning it to bite on today's universe was rejected as fitting the rule to the data it judges. **It can only screen 10 of the 12 selected symbols**: capture is EUR-quoted only, so `ETH/BTC` and `SOL/BTC` carry `spread_bps: null` and are not rejected (→ T0092). Remainder = the canonical artifact still says `pending-capture` until the next rebuild, which needs a live AssetPairs fetch and rides T0025's pre-live refresh (ripe when: the next universe rebuild).

- [T0093 — the universe rebuild reads an OHLC set that stops months short](T0093-universe-rebuild-reads-a-stale-ohlc-set.md) — **partially done 2026-07-22**: `_refresh_universe` reads `data/ohlc-full`, reconstructed from the OHLCVT dumps and ending **2026-03-31**, so its trailing-30-day median silently becomes 2026-03-02…03-31 — it answers "was this liquid four months ago?" while claiming to answer "can we trade this now?". Measured: a rebuild today selects **eleven**, dropping AVAX/EUR at 132,274.82 vs the 150,000 floor, with `escalate` staying `false` so nothing flags it. The `data/ohlc` set the artifact cites was **deliberately retired** (iter-103, spec `00056` D4), superseded bit-identically **on prices over the overlap** — daily volume differs up to ~7 %, and the floor reads `volume × vwap` — and not at all in the recency direction. It was not overlooked: `point-in-time-universe.md` declared the question "resolved / dropped" in 2026-07-11 on the false premise that the REST and full-history 30-day windows held identical recent data; they share **zero** rows. That bullet is corrected here. **Done:** a fail-closed per-symbol staleness guard (verified against the live set: raises at 113 days), plus provenance that records the set actually read (`ohlc_dataset_dir`) and its stalest frontier (`ohlc_stalest_daily_bar`); the empty-hash hole split out as \[[T0094]\] and **resolved 2026-07-23** (a set with no usable manifest now fails closed), so provenance is fully hardened. **Remainder:** the data gap — and note **ingesting dumps cannot discharge it**, since they are quarterly and a just-closed quarter still leaves the frontier weeks stale. A universe rebuild needs a live-tailed source, which is now registered as a blocker on \[[T0025]\] and noted in \[[T0065]\]. **The DATA half is discharged 2026-07-23**: the `ohlc-reach` set is the live-tailed source the trigger named — stalest daily bar **1 day** old against the 7-day budget, where `ohlc-full` fails closed at 114. The guard was vindicated, not relaxed. Remainder = the WIRING decision only, deliberately parked: repointing `_refresh_universe` changes which dataset defines the tradeable universe, so it rides \[[T0025]\]'s pre-live refresh with the owner present; until then a rebuild keeps failing closed, which is the safe state.

- [T0092 — the BTC-quoted universe legs have no L2 capture](T0092-btc-quoted-universe-legs-have-no-l2-capture.md) — **partially done 2026-07-23**: the owner ruled to CAPTURE (unbackfillable data — the start date is the whole cost of waiting), and `capture_pairs` went 10 → 12. A 15-agent pre-flight audit corrected this topic's own premise: the one-quote-dir observation is **not** a latent assumption anywhere (every walker keys on `BASE/QUOTE`, verified by probes on real two-quote trees) — the real blocker was the panel's notional ladder, quote-denominated but EUR-labelled with no pair filter — on a BTC-quoted pair the @100 rung asks 100 BTC ≈ EUR 5.9 M, ~10×/25× the two legs' entire daily volume, so every `fill_bps_*` column would be null (the review corrected an earlier wrong rationale here by measurement); now scoped to EUR (`PANEL_QUOTE`) with an explicit non-EUR `--pair` refused. Two hazards were killed by measurement, not argument (a 15-min WS probe: ETH/BTC 8.15 upd/s, longest silence 8.1 s; SOL/BTC 7.06 upd/s, 3.0 s — so the zero-update-book-hour CRITICAL and the ~2.3 s exit-bar threshold are clear by 443× and 17×), and trade-id density was verified dense for both legs. Converge order inverts to **primary-first**. Remainder = extend the spread calibration to the new legs and re-key `SPREAD_CALIBRATION` base → full symbol, then the docs rewrite (ripe when: ≥2 weeks of captured book, ≈2026-08-06 — but the iteration it opens must build the quote-aware notional ladder FIRST, since every `fill_bps_*` is null on BTC-quoted pairs and that is what the calibration reads).

- [T0065 — data-pipeline maturity for strategy research](T0065-data-pipeline-maturity-for-strategy-research.md) — umbrella synthesis (2026-07-18 clarification session): the core (frozen hash-pinned drift-gated canonical, validated reconstruction) is strong and the current 1d/4h strategy is not data-constrained; OPS-6 closed the durability + catalog legs (iter-103), leaving reach (fine-cadence ceiling: trades→bars producer unbuilt + Q2/Q3 dumps un-ingested), validation (\[[T0064]\] — **resolved 2026-08-03**: accepted, and the gate's claim narrowed to execution-not-edge, so this leg is closed rather than pending), and execution-reproducibility (no committed backtest command, uncommitted hash recipes). Holds the data-model taxonomy + the not-yet-tracked dedicated rounds (ripe when: worked in dedicated rounds; the reach round is ripe now, and so is the execution-reproducibility round — a committed backtest command + committed dataset-hash recipes, since research runs from gitignored scratchpad scripts and record 1's hash is not reproducible from committed code alone; the validation round was T0064's and is **resolved 2026-08-03** — the human gate was exercised: accepted, with the go/no-go's claim narrowed to execution-not-edge). **REACH leg partially done 2026-07-23**: `zcrypto data rebuild ohlc-reach` (`cli/ohlc/reach.py`) carries the live `ohlc-full` forward from Kraken's REST window instead of waiting on the dumps — **daily and 4h are now whole to the present** (2013-09-10 → 2026-07-22 / 2026-07-23T12:00, zero irregular steps after the seam across all 20 continuous series), closing the 2026-03-31 → 07-08 hole at both grids with no dump. The 1h grid lands **detached** (REST's ~30-day reach no longer meets the canonical tail) — written under `<interval>.detached.parquet` because those bars EXPIRE, but under a name no `ohlc-full` reader globs so they cannot be spliced across the gap. **Deadline**: promoting 1h needs the Q2 ingest AND a re-run before ~2026-07-30, and that window may be empty; 15m for 07-01 → 07-08 is already Q3-only (~October).

### Resolved<a name="resolved"></a>

- [T0002 — universe liquidity-floor calibration & quote-currency volume](archive/T0002-universe-liquidity-floor-calibration.md) — resolved in iter-007: EUR floor lowered €1M→€150k (footprint-based) + `quote_volume_in_eur` FX-normalizes the BTC-quoted legs → the full 12-name basket, `escalate=False`; findings recorded in master-plan §3.

- [T0001 — full-history OHLCVT backfill](archive/T0001-ohlcvt-full-history-backfill.md) — resolved in iter-008: built `cli/backfill/` (1-minute dumps → canonical 1h/4h/1d, base-authoritative merge); the full-history dataset (12 pairs, BTC 2013→2026) reconstructs OHLC bit-identical to the v0 REST (100% match).

- [T0006 — harness numeric-param type guards](archive/T0006-validation-numeric-param-type-guards.md) — resolved in iter-021: `isinstance(int|float)` guards on `cli/validation/` float params → `ValidationError` (not `TypeError`) on a non-numeric type; closed per defined scope (dsr `n_trials`/`n_obs` deliberately isfinite-only).

- [T0004 — full tick history + tick-derived bar reconciliation](archive/T0004-tick-history-reconciliation.md) — resolved in iter-044 (human-confirmed exit-bar): `cli/tick/` reconciles the full universe + full history at 100% coverage, 99.4–100% within 1% (the early-illiquid residual accepted), plus the true tick-weighted VWAP; 15m/tick-storage folded into the Bucket-B queue.

- [T0007 — dynamic-composition inverse-vol basket (full-history B2 variant)](archive/T0007-dynamic-composition-basket.md) — resolved in iter-044: built the look-ahead-free `dynamic_inverse_vol_basket` (2→10 majors over the full 2013→2026 union calendar) and answered finding-1 — the full-history basket is statistically indistinguishable from single-asset BTC (Sharpe ~1.1 both), so the fixed-window "basket loses" was a window artifact; a co-equal viable base for A1.

- [T0010 — full-history dynamic benchmark B3/B4](archive/T0010-dynamic-benchmark-b3-b4.md) — resolved in iter-055: self-gated dynamic B3/B4 built with full QA; B3+vt point-beats gated-B1 net-of-cost (1.245/1.278 vs 1.047/1.074, n.s., higher drawdown) → the deployable-bar choice escalated to T0009; the Phase-3 basket-cost carry-forward reconciled.

- [T0013 — trial-registry variant field](archive/T0013-registry-variant-field.md) — resolved in iter-056: schema_version 3 adds an optional first-class `variant` (hash-covered, omit-when-None); v2+v3 files co-load with the chain intact; budget counter untouched; adversarial review APPROVED (forge/torn-tail/concurrency probes).

- [T0015 — registry per-schema-version key-set validation](archive/T0015-registry-key-set-validation.md) — resolved in iter-062: exact key-set per schema version (15 base keys, v3 ± `variant`); surplus or missing key = corruption; the chain-consistent "variannt" forge now fails on this check alone; adversarial review PASS.

- [T0009 — Phase-4/5 validation-protocol decisions](archive/T0009-validation-protocol-decisions.md) — resolved in iter-072 (attended review, 2026-07-09): all six legs decided — benchmark-relative + stub-excluded worst-slice, k≥230 decisive window, net-of-cost SPA, DSR 0.95, A1-lf weekly v0.12 admitted (trial 34), A-family resumption; folded into `a1_kill_bar`.

- [T0017 — holdout window ratification](archive/T0017-holdout-window-ratification.md) — resolved in iter-073 (attended): window ratified (out-of-time 2026-04-01 → freeze), the look executed same night (degenerate gate-off window, reading **EQUALS**), ledger created, budget → 0.

- [T0011 — A2 refinements](archive/T0011-a2-refinements.md) — resolved in iter-080: the 2026 probe, the 4h arms (three adopts), the cadence sweep (family closed 40/40), and finally the cross-frequency P1 trial — **trial 43 ADOPT** (governed 1.5366, all ratified legs) — the combination supersedes record 33 as the deployable candidate (Phase-6 scope → T0018).

- [T0019 — P1 fixed-weight combination variant](archive/T0019-p1-fixed-weight-variant.md) — resolved in iter-081 (same session): the pre-registered fixed-⅓ trial — **trial 44 ADOPT** (1.5609, SPA grid max p 0.0060, all legs) — supersedes trial 43 as the deployable candidate and deletes the adaptive-weight mechanism from the Phase-6 engine's scope.

- [T0012 — 15m bars + tick storage for Bucket-B](archive/T0012-15m-tick-storage-bucket-b.md) — resolved in iter-085: `data/ohlc-15m/` built from the 1m dumps via `cli/backfill` (basket 0fed24a6…), tick-reconciled bit-exact + count-proven seam; tick catalog explicitly dropped (re-opens under C2/C1).

- [T0063 — deployable docs name the superseded record 33, not trial 44](archive/T0063-deployable-docs-name-superseded-record-33.md) — resolved 2026-07-19: dated inline supersession pointers at every stale site (Phase-5 closeout ×2, spec 00039 ×2, the runbook body's `ba47e37e` pin) plus a one-lookup "Current deployable" line atop the runbook (record 44 · `build_crossfreq_system_fast` · `dataset_hash 45275ebe`); historical narratives marked in place, never rewritten — the registry stays the source of truth.

- [T0066 — the panel's monotone watermark can permanently capture an un-healed hour](archive/T0066-panel-remint-race-monotone-watermark.md) — resolved 2026-07-19: `cli/panel/materialize.py` gained an explicit book-settle watermark (option a) — an hour is deferred until `now - hour >= 7h` (the reconciler's H+6h max mint + a pull buffer), counting it `hours_unsettled` and holding the monotone watermark off the un-healed primary (`--settle-hours`; spec 00052 D6 corrected in place). One-time ops-side audit found **0** existing un-healed panel hours (all 10 drill mints were healed-derived by a ~10-min margin); the trade-bar materializer's settle-binding moved to T0065's reach round.

- [T0078 — the trade-backfill run summary omits fetch failures](archive/T0078-trade-backfill-summary-omits-fetch-failures.md) — **resolved 2026-07-21** (commit `8a7a436` + review fixes): a run-level `trades_fetch_failed` bucket now totals the ids in gaps whose fetch raised, threaded into BOTH summary sites (the logger line and the CLI's own echo — the topic named only the first), with README's "every outcome bucket" guarantee corrected in the same change. Both pins mutation-verified; the review caught that the logger site itself was initially unpinned. A second residual of the same class — rows fetched for an hour whose MINT fails — was split out as T0087 rather than absorbed.

- [T0071 — capture-era data-hygiene map](archive/T0071-capture-era-data-hygiene-map.md) — **resolved 2026-07-21, by measured refutation of its own premise**: the map (`docs/reference/capture-era-data-hygiene-map.md`) finds the desync-era archive was never contaminated — the 810/468-vs-100 measurement was the *in-memory* book only, the archive stores wire messages verbatim (T0008: "No data was ever lost… the archive is sound"), and the panel replays through the *fixed* book. Content caveats are era-independent (ranks beyond 10 are venue-unverified in every era, corroborated by the 2,190/2,190 CRC verify-replay); the era-dependent findings are structural (launch-day mess, heal pocks, the hash-invisible T0036 truncation class — completeness lives in `continuity.py`, never manifests). The "post-07-13 only" fallback is superseded in the *opposite* direction: T0014/T0024 read the full window. The pre-review draft had inherited the false premise and issued an era-bounded EXCISE — killed by the pre-push review against T0008 and the capture code.

- [T0086 — workspace-transport script](archive/T0086-workspace-transport-script.md) — **resolved 2026-07-21**: `infra/scripts/workspace-transport.sh` moves the whole working environment between workstation and ops — git state incl. unpushed branches and tags via bundle, the memo, `~/.claude/` (plugins set+versions), `~/.claude.json`, scratchpad, `.superpowers` — and mirrors the destination's branch set, deleting destination-only branches by sha only when their commits are contained in a source local branch. **Its first real run failed twice and the failures were the deliverable**: host inference matched the DNS label instead of `hostname -s`, and a host-key failure was reported as a dirty destination repo because ssh exits 255 and the check conflated connection-failure with dirty-repo (reproduced before fixing). Owner then verified both directions; a four-lens adversarial review (54 agents, findings refutation-tested) found **13 further defects** in the fixed version — two found independently by all four lenses — all fixed: a `grep -qx` membership test that misclassified regex-metacharacter branch names *and* SIGPIPE'd to 141 under `pipefail`, a clean-repo gate blind to linked worktrees (which this repo hands subagents by rule), deletion ordered after the fetch so a stale `fix` branch permanently wedged `fix/…`, `--all --contains` trusting a stale `origin/*` ref as proof a commit was recoverable, `[[ -r /dev/tty ]]` failing to detect no-terminal, and the memo + `~/.claude/` being destroyed with no preview. Post-review ordering rehearsed end-to-end against engineered hazards.

- [T0014 — captured-spread cost calibration](archive/T0014-captured-spread-cost-calibration.md) — **resolved 2026-07-22** (iter-114, spec `00066`): the cost model's missing spread term now exists — `cli/costs/spread.py` with a per-pair table calibrated from our own `l2-panel` (10 pairs × 315 hours, ~11.2 M one-second rows). The load-bearing finding: the **median top-of-book spread is unusable for BTC/EUR** (tick-quantised at €0.10, at one tick 42–58 % of the seconds on complete UTC days, so the median swings ~15 × — mean÷median 11.2× vs 0.9–1.3× elsewhere), so the model charges the mean **effective spread at size**. A session term was measured and rejected on **materiality** (1.01–1.10× across the ten pairs, beside a 40–80 bps fee). **No verdict moves** at the realistic ×1.403 stack (governed 1.3645, measured) — though the monotonicity argument that first carried that claim was false and was withdrawn, so coverage is swept points only. Two residuals split out: [T0090] (the modeled 0.006/side is maker + a 20 bps headroom this calibration shows is 5–10× too large — and whether the model over- or under-charges turned on the maker-vs-taker decision, **ruled 2026-08-03: maker-first with a taker fallback**) and [T0091] (the window is 13.1 days against Phase 2's "≥2 weeks" exit-bar row). T0071's falsification probe was run and is **inconclusive** — a placebo at seven non-event dates gives comparable ranges, so it has no discriminating power.

- [T0087 — mint-failed rows land in no summary bucket](archive/T0087-mint-failed-rows-land-in-no-summary-bucket.md) — **resolved 2026-07-28** on a shared carrier with T0043: rows the REST served for an hour whose mint then raised are counted as `mint_failed`, in both summary surfaces. The design question is decided and written beside the counter — it is reported but never subtracted from the post-mint accounting check, because a fetched-but-unminted row is retryable and an explained absence would silence the sweep's strongest check on exactly the failure it exists for.

- [T0043 — a genuinely lost trades file is invisible when its book sibling survives](archive/T0043-lost-trades-file-with-surviving-book-is-invisible.md) — **resolved 2026-08-02** on a ruling taken against measurement. The data-loss half closed by the REST backfill (iter-100) and the attribution half by `hours_repaired_after_loss` (2026-07-28); the exporter is confirmed live in Cloud reading **0**, so no repair-after-loss event has occurred since it began exporting. `is_total_loss` deliberately keeps its current classification: the asymmetry the topic was opened for (false positive loud, false negative silent AND permanent) was inverted by spec `00053` — a hole is now provable against Kraken's dense `trade_id` tape and repaired within a day, guarded by a 2-day dead-man and an exit-code rule. Inverting the book witness would page on 10 of 6,420 book hours today (0.156 %), all on thin pairs. Residual accepted as a conscious drop: a loss beyond Kraken's REST retention (deep — spec `00053` D5) is unrepairable, though still re-detected daily as `trades_unrecoverable`; what it lacks is escalation, not visibility.

- [T0098 — reach-round review residuals](archive/T0098-reach-round-review-residuals.md) — **resolved 2026-08-02** (spec `00080`), all three of \[[T0065]\]'s deferred reach-round findings, with **two of this topic's own premises corrected by measurement**. (1) A missing `manifest.json` is **not** a partial-run signature — false for 2 of the 7 rebuild targets (`snapshots` writes only `kraken-refdata-<stamp>.json`; `universe` writes only `point-in-time-universe.json` and merely *reads* the OHLC manifest), so the proposed manifest-based cleanup would have deleted **completed** output; a failed builder now `rmtree`s its whole just-minted sibling instead, which needs no signature, catches `BaseException` so Ctrl-C cleans up like an error, and is sound because the exists-guard fires before `mkdir`. (2) **`PAIR_KEYS` moved to `cli/ohlc/fetch.py`** with the store re-exporting it, so no downstream import changed and the half-initialised-package trap dissolves rather than being documented. (3) Deleting both seam copies outright is **not** a pure refactor — the two guard blocks' exception types and message texts measurably diverge and are test-pinned on both sides — so `cli/ohlc/seam.py` holds only what was identical (`MIN_SEAM_OVERLAP`, `drop_in_progress`, and `seam_overlap`, the seam definition itself), with the guard policies left local behind cross-pointers, mutation-proved to route both callers through the shared definition. Behaviour-neutral by proof: suite green with no assertion edits, and `engine soak-check` byte-identical before and after. Hard-kill strands are a conscious drop — the extended `sibling already exists` message names the remedy.

## Live trading preparation<a name="live-trading-preparation"></a>

### Open<a name="open-1"></a>

- [T0005 — Blockpit T1 tax check](T0005-blockpit-t1-tax-check.md) — connect the Kraken depot read-only, verify import scope + historical labeling, write the T1 memo; human-gated on the Blockpit/depot authorize step (ripe when: Stage 6b starts — the T1 connection rides alongside the §11 T2 probe window, per spec 00039).

- [T0028 — NAS pull re-hashes the whole archive](T0028-nas-pull-incremental-verify.md) — Role A's `verify_tree` sha256-re-verifies the entire (unbounded) archive every hourly cycle → O(archive) per cycle; verify only rsync-transferred files instead (ripe when: the verify sweep approaches the pull interval, ~250–600 GB in — now ~1–2 years out, since the real growth is 0.48 GB/day, not the 10 GB/day the estimate assumed; see T0032).

- [T0049 — go-live drill matrix + day-2 operations runbook](T0049-go-live-drill-matrix-day2-runbook.md) — the exit-bar alerting drill is passed (owner sign-off 2026-07-16, live incident); before live orders, deliberately drill the full failure-scenario matrix (dead-men, watermark, engine `/fail`, Loki path, Alloy death, Grafana outage → healthchecks independence) and extend `infra/runbooks/README.md` (the runbook now exists there, with the section protocol recorded in the topic) (ripe when: anytime before the final go-live — human-manually picked up; drills still execute in attended maintenance windows; flipped 2026-07-21).

- [T0085 — arm64 image re-enable](T0085-arm64-image-reenable-before-golive.md) — multi-platform builds were disabled in capture-image.yml for dev speed; re-enable + verify one multi-arch build before go-live, not on go-live day (ripe when: before the final go-live / 6b live step).

- [T0113 — the snapshot-register re-confirmation is unregistered](T0113-snapshot-register-reconfirmation-is-unregistered.md) — the master plan marks fees/fee tiers, borrow-rollover rates, pair lists, MiCA status, tax rules and data pricing as externally owned and requires re-confirmation "at Phase 0 and again at go-live"; Phase 0 did it (`01.1.kraken-snapshot-register.md`, `Fetched at: 2026-07-07`) and nothing registers the second sweep — prose is not registration. Each fact is third-party-owned and moves without notice, and a stale one is silent: the go/no-go would be taken against July figures with nothing objecting. Carried in T0049's runbook as a MONTHLY routine, not a single pre-live step (ripe when: monthly from 2026-08-07 — the register's own `Fetched at:` stamp versus today — and unconditionally before the go/no-go).

- [T0115 — `dispose()` aborts a never-run node](T0115-nautilus-dispose-aborts-a-never-run-node.md) — nautilus 1.230.0's `TradingNode.dispose()` takes the `loop.close()` branch on a node that was built but never run, while a `logging` thread and an `iou-sqp` io_uring poller are still alive — a native SIGABRT that killed the whole pytest process in CI (3 of 4 runs, against 27 clean ones before). Spec `00078` **contained** it by fencing both node-build tests into a child interpreter (proven: abort/SIGSEGV/SIGKILL in the child all yield a normal red test, never exit 134), but the race itself is untouched. It cannot reach production — a node that was actually run takes `loop.stop()` instead — and it never reproduced outside CI (ripe when: any native abort recurs, now observable rather than fatal thanks to the fence, or `nautilus_trader` is bumped past 1.230.0).

- [T0119 — the delta formula: target − held](T0119-delta-formula-target-minus-held.md) — intended orders are computed against previously journaled intent, not venue holdings; invisible in shadow, load-bearing with fills. `target − actually held` makes unplaced deltas accumulate and self-place on crossing `ordermin` — rung 3's mechanism, and the fix for the intent-vs-holdings drift defect.

- [T0123 — how a newly validated sleeve enters the live book](T0123-sleeve-promotion-path.md) — no document defines promotion: new record via Phase-5 assembly, builder-contract bump, shadow re-gate scope, ramp re-entry. Undefined, research and the live book never reconnect — or promotion improvises around the gates that made record 44 trustworthy (ripe when: a post-deploy family reaches an ADOPT verdict — the first real candidate).

- [T0126 — vault password rotation decision](T0126-vault-password-rotation-decision.md) — rotate or consciously accept after iter-125's red-phase exposure (the vault password reached local subagent transcript(s) and scrollback; nothing committed — containment evidence and the PGP-key caveat in the topic); one attended answer disposes of it (ripe when: now).

### Partially done<a name="partially-done-1"></a>

- [T0018 — Phase-6 build sequence](T0018-phase6-build-sequence.md) — the kickoff roadmap with its cross-iteration constraints; builder + concordance core (iter-082), the shadow node + workstation soak (iter-083), and the VPS deployment (iter-084, gate clock ticking since 2026-07-11 00:00 UTC) all landed, and the **session brief is assembled** (2026-07-23, `docs/research/14.phase6b-session-brief.md` — gate state, the record-44 build contract with its cost anchors, the ten-item decision queue, and the post-return deferred set, so the post-return session opens on measured state); remainder = the 6b executor (ripe when: the Stage-6a gate is met — ≥ 14 consecutive clean complete-UTC days — and the human convenes the 6b session).

- [T0020 — Grafana Cloud observability](T0020-grafana-cloud-observability.md) — dashboard, alert rules, push script + provisioning all live (iter-094); host-level Alloy ships from **all four hosts** since iter-105 (NAS, ops, both capture); capture-host alert rules landed 2026-07-19; the app `/metrics` exporters (engine + capture additive counters) shipped and deployed fleet-wide 2026-07-26 (spec `00069`/iter-117), completing spec `00043`'s original scope; the untriaged "retry NAS cadvisor" item closed as an explicit posture rejection (spec `00069` D4); remaining = the dedicated fleet-dashboards package, now ripe under either trigger reading since app metrics are live too (ripe when: the owner convenes it; the 2026-07-21 untriaged grooming additions fold in when it runs).

- [T0027 — unattended auto-reboot policy](T0027-unattended-reboot-policy.md) — **the flip is LIVE 2026-07-26** (spec `00071`): both capture VPSes are attended, ops keeps auto, and `node_reboot_required` + four alert rules make a pending reboot loud; remaining = confirming order-state reconciliation survives a mid-order-submission reboot before live 6b, and the attended-reboot skill when the skill family is built.

- [T0037 — hour rotation trusts an untrusted timestamp](T0037-rotation-trusts-an-untrusted-timestamp.md) — `append()` rotated the hour from Kraken's own `timestamp` field, so one event stamped ≤`MAX_TS_AHEAD` ahead in the last minutes of an hour published the live hour as a **committed, complete** segment and dropped every genuine row after it as late (permanent, restart-surviving post-T0036); **fixed by cross-stream quorum** — a shared `HourOracle` acts on a boundary only once 2 witnesses (another stream, or the 5-min-handicapped wall clock) confirm it, holding (never dropping) rows for an unconfirmed hour, so no window size or wall-clock veto is needed and no stream is ever darkened; remainder = two accepted residuals, each ripe only **if ever observed** (two streams bogus into the same hour within one window; a clock leading >5 min plus a bogus stamp).

- [T0103 — the reconciler books unfilled silence as healed](T0103-reconciler-books-unfilled-silence-as-healed.md) — **counters fixed and deployed; all three alert defects closed** (the CRITICAL page evaluates over 24 h, the healable rate is denominated in the unit its summary claims, and the permanent-loss summary is live and verified byte-identical). The 2,228.581124 s of pre-fix over-statement is now a `correction` note in the ledger, carrying no counter field so the Prometheus series stays continuous — proven counter-neutral before the write. The cross-hour straddle is **closed as a consciously accepted limitation** at 0.045%, recorded at the clamp in `settle.py`. One item remains: re-deriving the healable-gap threshold, ripe 2026-08-27 with a spread check, reminder scheduled.

- [T0105 — upstream silence: the paging half, and reacting to the venue's own status](T0105-silence-second-half-paging-and-venue-status.md) — **the venue half is armed** (2026-07-29): `zcrypto-capture-venue-not-online` watches `system != "online"` with a runbook procedure, so the event pages instead of waiting to be noticed; the decision it feeds — whether a pre-drain is worth building — still waits on a real value and its `effective_time`. The paging half is dated: `zcrypto_capture_seconds_since_last_book_message` began reaching Cloud 2026-07-29, so a week of both hosts' history including a weekend trough exists from 2026-08-05 (ripe then; a scheduled reminder carries the checklist).

- [T0121 — three of §10's four whole-book limits have no caller](T0121-section10-whole-book-limits-unwired.md) — **wired 2026-08-02 (iter-119)** at the ruled order `caps → gross → net band → margin floor → × governor`, the governor left **outermost** because it is a pure returns overlay and clamping after it would re-scale its own effect. Verdict-neutral and proven non-vacuously so: frozen figures unmoved, **0 of 137** cycles cap-bound (the *capped* book the limits see runs 10.74 % median gross against a 150 % soft cap — the familiar ~5.4 % is the post-governor book), and choking `soft_cap` to 0.01 fails every cycle at the predicted per-cycle factor. The wiring exposed a worse defect than the topic named — three out-of-builder recomputations still stopped at `apply_position_caps`, so the first binding limit would have raised `stage identity broken` and dropped `reconcile_ok`, **a false alarm firing precisely when the risk layer works**; all book-rebuilding sites now share one helper, while the three quantities that mirror the pre-limits `cap_breach_bars` were deliberately left. Remainder = a limit-breach counter, which joins T0018's order/PnL families rather than minting its own (ripe when: those families are built).

- [T0122 — §12's Ongoing register is prose-only](T0122-ongoing-register-kill-criteria.md) — **DD kill floor ratified 2026-08-02: 20 % governed drawdown** (1.47× record 44's registered maxDD of 0.1357, five points past the ladder's terminal-with-re-arm 15 % rung, so reaching it means the governor went flat and the book lost five more). The underperformance criterion **changed statistic rather than gaining a number**, and the reason is measured: a kill on live Sharpe can never fire — ±2.15 95 % half-width after a full year, 8.2–11.6 years to separate the T0124 live-state baseline from zero — so it is the **paired live-minus-simulation** difference, whose bound is derivable only from 6b's first real fills. Monthly revalidation-as-trials and the quarterly decay test are registered with their triggers; they move into T0049's runbook at its build, since a section with no operator-facing signal would be the backlog that runbook bans (ripe when: 6b yields paired cycles).

- [T0120 — live-order config gaps](T0120-live-order-config-gaps.md) — **`spot_positions_quote_currency` set to `"ZEUR"` 2026-08-02, and the value is the finding**: the field is compared literally against the instrument's quote-currency `code`, and of Kraken's 1,592 spot instruments **546 carry `ZEUR` and 0 carry `EUR`** — the instrument *ID* renders `ADA/EUR.KRAKEN` (a symbol rename) while the `Currency` keeps the legacy `Z`, so the plausible `"EUR"` matches nothing and reports an empty book exactly as the `"USDT"` default did. `default_leverage` is **deliberately left unset** (a ruling): `None` means cash, the node already runs MARGIN, and a global default would silently lever every ordinary rebalance — rung 1's T2 probe passes leverage per order instead. Remainder = the discrepancy-polling intervals, not guessed because their values are a share of a REST budget only the executor's call pattern defines (ripe when: that pattern exists).

- [T0111 — mechanize capture-deploys](T0111-mechanize-capture-deploys.md) — **waves 1+2 landed (specs `00082`/`00083`, iters 124/125)**: thirteen converge guards (wave 1), then `converge.sh` as the documented preview-first typed-confirm converge path, `zcrypto-panel-regenerate`, `ops-postverify.sh`, the `vault-pass.sh` ancestry refusal, the engine canary-parity mirror, the journal-following window floor, and the skip-tags/liquidations/mutate-probe/git-mv gap closures (wave 2); remainder = the attended ops-host drills for `converge.sh` + `zcrypto-panel-regenerate` (ripe when: an attended maintenance window).

- [T0090 — the record's cost basis is directionally unresolved](T0090-deployable-record-quoted-below-realistic-cost.md) — **RULED 2026-08-03: maker-first with a taker fallback**, on the reasoning that resting post-only orders are free until filled, the plan's own thesis is maker-first, and T0119's `target − held` accumulation *is* the mechanism that absorbs an unfilled delta. Re-quoted on the committed builder (instrument validated against all three registered anchors first): headline **1.6304 maker → 1.5609 registered → 1.3641 taker**; conditional **0.9072 → 0.7509 → 0.5066**. **The execution decision matters ~2.7× more for the live book than the headline shows** (headline −16.3 %, conditional −44.2 %) — the one-sleeve book at ⅙ gross carries more turnover per unit of return, so cost bites hardest in exactly the state the live book occupies. The cost model is split into `fee_per_side` + `spread_per_side` (`936db6c3`), byte-neutral by sha256 over raw IEEE-754 output bytes. **T0090's own "cheap re-read of the 43-vs-44 ordering" is impossible**: trial 43's construction was never committed and its scratchpad runner is gone, so the ADOPT criterion that made 44 the deployable can never be re-examined at a different cost basis — the T0065 failure class, now reaching the deployable record. Also surfaced: T0116's "0.5–0.75" band conflates estimation uncertainty with the cost range, two unrelated uncertainties sharing one interval. **The band collision it surfaced is RULED and §12 amended 2026-08-03**: the gate's band now states its two uncertainties as **separate terms** — the statistical CI and the cost basis, only the second of which resolves (at rung 1) — with the failure diagnosis split, since a breach attributable to the estimate means the backtest is wrong ⇒ Phase 5 while one attributable to the cost basis is an execution finding. Remainder = the maker-first executor itself (rides T0018), the realized maker/taker blend, and relaxing `spread_per_side`'s `> 0` validator (zero spread is legitimate on a split term) — ripe when rung 1 produces real fills.

### Resolved<a name="resolved-1"></a>

- [T0040 — alert on docker-socket-proxy denials and non-routine calls](archive/T0040-docker-socket-proxy-denial-alert.md) — closed unbuilt: the proxy it would have watched was removed the same day, so the denial stream it depended on no longer exists; the security residual that replaces it is [T0042](archive/T0042-alloy-holds-root-equivalent-docker-access.md).

- [T0046 — sparse-symbol liquidation hours never finalize](archive/T0046-sparse-symbol-liquidation-hours-never-finalize.md) — resolved via wall-clock finalization: `SegmentWriter.finalize_completed_hours(cutoff)` (additive) closes an open or crash-leftover hour once it is provably older than the caller's cutoff; the Coinalyze poller calls it with a 31h lag (safely past its 30h catch-up window and Coinalyze's own retention), so sparse-symbol manifests now appear at most ~31h late instead of never — ships to the running poller with the next ops-node image re-pin.

- [T0000 — Phase 0 human account actions & live-account confirmations](archive/T0000-phase0-account-actions.md) — resolved in iter-023 (Phase-2 close-out): account actions + live confirmations done 2026-07-07; the deferred July-9 fee-schedule fold-in landed in iter-017 (`cli/costs/`).

- [T0029 — NAS CPU has no AVX; polars crashes](archive/T0029-nas-cpu-no-avx-polars.md) — resolved — determinism measured, Role B bit-identical on the NAS (iter-094).

- [T0030 — NAS Alloy uid-key exposure](archive/T0030-nas-alloy-uid-key-exposure.md) — resolved (iter-094, same PR): Alloy runs as the dedicated non-key-owning user `zcrypto-dummy` (uid 1031, gid 1000), verified live — ships metrics + logs, `gate.prom` readable, and the `0600` pull keys denied through `/host/root`.

- [T0036 — a restart silently truncates the hour it lands in](archive/T0036-segment-writer-restart-clobber.md) — resolved 2026-07-14: committed-final invariant + atomic parts + validated recovery + cross-stream rotation quorum (T0037), deployed with a validated 1 s-downtime migration; post-deploy verified (hour-04 finals begin at :00:00, 0 desyncs, CRC-clean splice, no new truncation).

- [T0031 — re-pin NAS capture image after merge](archive/T0031-nas-image-repin-after-merge.md) — resolved 2026-07-13: the develop-built `-compat` image was published after the Role B merge and the NAS compose re-pinned to it (`sha256:ec180cde…`), replacing the branch-only digest; pull + verify green on the new image.

- [T0041 — archive-pull failures do not page](archive/T0041-archive-pull-failures-do-not-page.md) — resolved 2026-07-14: every error path now goes through `logging` so it carries the `level` label the alert selects on (`_abort` logged rather than printed — closing a regression the label-based rule itself introduced; uncaught exceptions logged via a new `run()` entry point; `pull-entrypoint.sh` emitting the Python log shape), plus a new dead-man rule that fires when no successful pull is seen for 3h — the only rule that catches silence.

- [T0034 — grafana-push safety](archive/T0034-grafana-push-safety.md) — resolved: `grafana-push.sh` now reads every pushed rule back and fails on a wrong datasourceUid (the health=ok trap), and reports/prunes orphaned rules (dry-run default, exact-membership so a renamed rule is caught); proven live (12 == 12, injected orphan flagged).

- [T0047 — Slack incoming-webhook notifications](archive/T0047-slack-webhook-notifications.md) — resolved 2026-07-15: Slack is the sole notification target for Grafana (as-code guarded upsert, proven by a read-back-verified test fire, email integration deleted after the proving call) and healthchecks.io (all 9 checks pinned to #zcrypto).

- [T0038 — NAS mirror accumulates stale part files](archive/T0038-nas-mirror-accumulates-stale-parts.md) — resolved: prune-after-verified deletes each verified hour's `<HH>.part*.parquet` on the NAS (strict numeric names, hardened unlink), the reader side closed by `canonical_segments`; deploy verified 2026-07-15 — the ~13.5k-part backlog drained to just the in-progress hour's parts.

- [T0003 — D2 forward-capture pipeline (VPS daemon → NAS archive)](archive/T0003-d2-capture-pipeline.md) — **resolved 2026-07-16 (iter-099): the Phase-1 capture exit bar is MET** — 7.29 days, worst stream 0.0624 % of the \<0.1 % gap bar, zero missing hours, VPS 3738 = NAS 3738 finals, hashes 3738/3738 + 3718/3718, alerting drill signed off; measured from segment-timestamp continuity, evidence in `docs/research/02.phase1-capture-exit-bar-report.md`. Role A (iter-093), Role B (iter-094), Role C as the second-Linode redundant capture (iter-096) all landed; REST trade-backfill split out to [T0050](archive/T0050-rest-trade-backfill.md).

- [T0051 — Alloy keep-regex missing the trade-backfill prefix](archive/T0051-alloy-keep-regex-missing-trade-backfill.md) — opened and resolved inside the same PR (iter-100): the NAS keep-regex is a `keep` action, so the new `zcrypto_trade_backfill_*` metrics would have been silently dropped — not undashboarded, but nonexistent, with a decorative dead-man on top; fixed at source and verified by parsing the emitted names against the regex.

- [T0026 — reconnect trade-snapshot overwrite](archive/T0026-reconnect-trade-snapshot-overwrite.md) — resolved 2026-07-16: the CAUSE was fixed by T0036's committed-final invariant (a snapshot's late rows are dropped, never rewriting a finalized hour) and the EFFECT was measured and repaired by iter-100's REST backfill — the "plausible but unquantified" loss turned out to be 17,362 trades archive-wide, all recovered.

- [T0050 — REST trade-backfill for the capture daemon](archive/T0050-rest-trade-backfill.md) — **resolved 2026-07-16 (iter-100)**: the last unshipped §8 daemon clause, shipped as an offline pass that never touches the daemon. 194 gaps / 17,362 missing (3.60%) / 10,986 duplicates → **all zero**, verified on the live archive with the raw mirrors byte-identical; closed the reconciler's correlated-loss blind spot for trades (the largest gap predates the secondary, so no mirror could ever have healed it).

- [T0054 — reconcile skips backfill-minted hours](archive/T0054-reconcile-skips-backfill-minted-hours.md) — resolved 2026-07-16 the day it was raised: the `already_minted` skip is per-`kind` (proven — minting a trades hour leaves `already_minted(..., 'book', h)` False), so a trades backfill can never shadow a book heal; skipping backfill-minted *trades* hours is correct, since REST is a strictly better witness than the secondary mirror.

- [T0052 — trade-backfill observability](archive/T0052-trade-backfill-observability.md) — resolved 2026-07-16: two rules, because two things break — a staleness dead-man on `last_success` (noData=Alerting: a vanished series IS the failure) and a non-zero `exit_code` rule (noData=OK: the dead-man already owns "stopped running"), plus a dashboard row and an explicit compose textfile var. The exit-code rule became load-bearing the moment T0053 removed the retry.

- [T0053 — the daily gate degraded to hourly-forever](archive/T0053-trade-backfill-daily-gate-degrades.md) — resolved 2026-07-16 (owner's decision): stamp the UTC day unconditionally — in fact *before* the run, so even a crash consumes it, making the O(archive) cost bound absolute — and let T0052's alerts carry the failure. Also fixed the cause at source: `_MIN_INTERVAL_SECONDS` 1.5 → 3.0, since 1.5s was demonstrably refused on the live run. Residual split to T0055.

- [T0055 — KRAKEN_ALTNAME could drift from the capture universe](archive/T0055-kraken-altname-map-can-drift.md) — resolved 2026-07-16 by DELETING the map: `cli/backfill/read.py::dump_pair_name` already derived the same altname (verified against all 10 values and against the live REST for both irregular pairs), so the dict was a second copy of a fact. An 11th pair now derives automatically; the drift surface shrinks from per-pair to per-aliased-asset (the 2-entry `_ALIAS` table is still hardcoded, so a *newly aliased* asset could still derive wrong — narrowed, not closed).

- [T0056 — the NAS runs whatever was last hand-copied to it](archive/T0056-nas-hand-deploy-drift.md) — resolved 2026-07-17: the NAS is Ansible-managed (owner-ratified — quirks become code, not prompt-knowledge); the maiden converge verified by outcome — TZ guard PASSED on live UTC evidence, 8 changed incl. both digest pins via the rendered `.env` (the `:latest` traps dead in the deployed state), deployed entrypoint sha == repo sha; drift *detection* is moot because hand-deploys no longer exist.

- [T0057 — the ledger-correction backup lived inside custody](archive/T0057-ledger-correction-backup-in-custody.md) — resolved 2026-07-17: the `.bak`'s evidence committed (`infra/nas/ledger-correction-20260714-link-eur.md`), the file sha-verified (`1da63f1cce679d6e`) on both hosts then removed from both (trees identical at 1174 files), and the T0044 runbook now writes backups outside the replicated tree.

- [T0058 — the ops mirror was two hops from source](archive/T0058-ops-mirror-is-two-hops-from-source.md) — resolved 2026-07-17: the owner-ratified NFS pivot is live (RO export + automount, fail-closed `.pull-status` gate through the mount, cadence ×2, the rsync channel retired with keys removed on both sides); the post-NFS 1.7–2.2 h sawtooth is the custody hop the owner ruled unavoidable, so the 3 h alert margin is structural again (≥ 0.8 h worst-phase); the readdir/filesystem probe split to T0062.

- [T0059 — `Persistent=true` + `--date yesterday` skipped multi-day outages](archive/T0059-persistent-timer-replay-coverage-gap.md) — resolved 2026-07-17 by the watermark catch-up loop: the verified-replay replays every unverified day from the persisted watermark through yesterday (hardened against zero-byte/garbage/unpersisted-seed/empty-journal-day/future dates, each execution-reproduced), making the `Persistent=true` interaction harmless by construction; first organic run 2026-07-18 05:23 UTC.

- [T0060 — the ops node's logs are unusable and 99.9% noise](archive/T0060-ops-log-pipeline-unusable-and-noisy.md) — resolved 2026-07-17: all four defects closed and deploy-verified. Pipeline half (iter-101, spec 00054): parse stages match via a compose-service-label-first scheme, the `docker run --rm` jobs are named + dropped from Docker discovery + shipped via the unit journal, 10 alert rules provisioned live. Source half (iter-102, spec 00055 / PR #143): per-coin bucket watermarks skip already-persisted buckets before the writer; the fixed image (`@sha256:495c6d15…`) deployed to ops and verified by outcome — `dropping replayed event` 122/15 min → **0** over 3 cycles, `primed 10/10`, `submitted=9/8 skipped≈2020` proving continued live ingestion, `restarts=0`.

- [T0062 — is the NFS export's filesystem btrfs or ext4?](archive/T0062-nfs-export-readdir-consistency-check.md) — resolved 2026-07-17 the day it was split: `/volume1` is btrfs (stable readdir cookies), so NFSv3 READDIR against the ops mount is safe; probed read-only at the next NAS touch, exactly as the ripe trigger prescribed.

- [T0061 — the live trade key was group-scoped to capture_host](archive/T0061-trade-key-group-scope.md) — resolved 2026-07-17 (owner present): the live Kraken credential moved to a new `group_vars/engine_host/` scope, so the capture-only secondary no longer resolves it — `zcrypto-red` went `key==True` → `key==False` by boolean probe, ciphertext byte-identical, and the engine play dry-runs `changed=0`. Membership is the boundary now, not play targeting.

- [T0039 — the reconciler's `--min-gap-seconds` needed cross-host validation](archive/T0039-min-gap-seconds-needs-cross-host-validation.md) — **resolved 2026-07-17 (iter-096)**: 30 s validated on both legs — the 66 h / 217-window cross-host soak (max 12.08 s, a hand-classified AVAX coalescing artifact a 10 s threshold would have spliced), then Task 13's live drill, where the real 25-min outage was detected and healed exactly (spliced=10, zero phantom splices, hour-16 no-op held, CRC verify-replay 2,190/2,190) after the zero-backlog `--mint` flip.

- [T0033 — home ops node (real-CPU compute tier)](archive/T0033-home-ops-node-compute-tier.md) — **resolved 2026-07-18 (iter-103, OPS-6 Loop)**: OPS-1…6 complete — the ops node is the fleet's compute tier and a research node. OPS-6 landed the custody/hot/private topology + the `zcrypto data` exchange + the hardened `hot/` write channel, seeded the NAS, and proved research runnable on ops (62 regression tests ran-not-skip). The identity regularization it surfaced is the spec-00057 follow-on (T0067/T0068).

- [T0067 — fleet users/groups: ops migration](archive/T0067-fleet-users-groups-ops-migration.md) — **resolved 2026-07-18 (iter-104)**: the ops node's whole data path moved `deploy → zcrypto-data` (the m2m user runs the containers, owns the data, and serves the four now-Ansible-provisioned `rrsync -ro` pull channels), the NAS repointed its four sources to `zcrypto-data@`, the hot-out authoring path (`zhaow` → `zcrypto-data` → NAS) is wired via the setgid `zcrypto-hot` group, and `deploy → zcrypto-deploy` was renamed (uid 1001 kept; converge idempotent). Two corrections found live: `zcrypto-data` needs a real `/bin/bash` shell (nologin structurally blocks the rrsync forced commands; the keys stay jailed by `command=`/`restrict`), and the 5 admin-plane role refs retargeted to `zcrypto-deploy`. Gate-export slowness in the NAS loop parked as [T0069](archive/T0069-gate-export-cpu-cost.md); the capture/engine hosts are the separate [T0068](archive/T0068-fleet-users-groups-capture-engine-migration.md).

- [T0068 — fleet users/groups: capture/engine hosts](archive/T0068-fleet-users-groups-capture-engine-migration.md) — **resolved 2026-07-19 (iter-105)**: both capture hosts on the `zcrypto-*` model, **gap-free** — `kraken-capture → zcrypto-data` (uid 999) and `kraken-engine → zcrypto-engine` (uid 997) renamed keeping uid/gid, so the numeric `--user` compose refs stayed byte-identical and neither container restarted; the capture + journal pull-export keys moved to `zcrypto-data` (Ansible-provisioned, `exclusive:false`), the NAS repointed capture/capture-red/journal to `zcrypto-data@`; `deploy → zcrypto-deploy`; first-time `zcrypto-alloy` shipped on both hosts (D5 residual recorded in [T0042](archive/T0042-alloy-holds-root-equivalent-docker-access.md)); the trade key stayed `0600 root:root`, denied to `zcrypto-engine` throughout (D4). Two fixes found live: the devsec os_hardening shell-removal would nologin-trap `zcrypto-data` (fixed via `os_ignore_users`), and the engine role kept stale `deploy` journal tasks (removed). Followed in-branch by explicit `container_name` on every compose service (drop the `-1` suffix) and the fleet-wide `docker_deploy_user` default fix (prevents a future ops converge resurrecting a dead `deploy`).

- [T0070 — post-migration host cruft](archive/T0070-post-migration-host-cruft.md) — **resolved 2026-07-19**: removed the vestigial `kraken-capture` account (uid 988, nologin) + its empty `/var/lib/zcrypto-capture` home from the ops node (verified uid-988 owned nothing else, no config/systemd refs; the base-role capture-host guard prevents recreation). The NAS `capture-reconciled` "777" question resolved by observation — it is **775** (`zcrypto-data:zcrypto`, no world-write), the container's needed group-write, so no change. The cutover `.bak` backups were already confirmed absent.

- [T0072 — soak-check realized governor/cap metrics](archive/T0072-soak-check-realized-governor-cap-metrics.md) — **resolved 2026-07-20** (iter-108, spec 00059): governor-engagement and cap-breach are now real realized-vs-backtest gating comparisons (fingerprint 5 -> 7), recovered by one timestamp-keyed rebuild on the journal's own hash-verified snapshots — alignment proven window-wide at worst |diff| = 0.0 over 54 production cycles. Two rules came out of the live run: a band spanning a metric's full attainable domain is `n/a` not "consistent" (D8), and a window statistic is judged against same-length windows never a global mean (D9). Remaining scope in T0073.

- [T0074 — gate-cache environment fingerprint](archive/T0074-gate-cache-environment-fingerprint.md) — **resolved 2026-07-20**: `replay_fingerprint` now digests the execution environment (full numpy version + Python major.minor) alongside the ten replay modules, config and replay path, closing the last silent-stale-PASS route (a `uv.lock` bump with journal and source bytes unchanged). Granularity is asymmetric on purpose; polars is deliberately excluded because `snapshot_content_hash` hashes DECODED values, so a decode change fails loudly via `HashMismatchError` rather than silently.

- [T0077 — enabling `gate-export --cache` removes the only continuous integrity check on the gate's evidence](archive/T0077-gate-cache-skips-the-only-continuous-integrity-check.md) — **resolved 2026-07-20** (spec 00062, iter-112): bounded rotating re-verification landed, so the whole journal's parquet bytes are re-verified ~daily even with the cache warm (real journal: warm replays 1 of 57 with 56 hits, gate metrics identical to no-cache; a 24h sweep re-verified 57/57, worst hour ≈54 s on the Atom vs ~510 s for a full replay). The hourly full replay is not just waste, it is the ONLY place the journal's parquet bytes are re-hashed (`concordance.py:116`); the pull deliberately delegates to it (`--no-verify`), rsync has no `--checksum`, and `evidence_fingerprint` digests the content_hash RECORDED IN THE RECORD rather than a fresh read — so a cache hit serves a PASS for a snapshot altered in place, silently, with every green signal still green. No live defect (the flag is not deployed). The alert rule is committed AND pushed live (read-back verified), the cache-file siting is decided (spec 00062 D9 — ephemeral, never on a shared path, since the two hosts run different polars runtimes and neither is fingerprinted), and the owner judged in-place parquet alteration worth defending against, so daily rotation stands. No residual.

- [T0076 — the concordance gate's arithmetic guarantees are unpinned](archive/T0076-gate-arithmetic-guarantees-are-unpinned.md) — **resolved 2026-07-20** (PR #162 + spec 00063 — all nine guarantees now fail when broken, each proven by mutation; test-only, `git diff -- cli/` empty): a 62-mutation isolated audit of `evaluate_gate`/`replay_cycle` found 9 triaged gaps. The sharpest, reproduced independently twice: `_GATE_STREAK_DAYS 14 → 13` leaves **20/20 green** — nothing asserts that one day fewer FAILS the gate, and the constant is referenced by no test at all, so a one-character edit opens the live-trading gate a day early in silence. Second: a dead engine's frozen journal would keep `gate_met=True` instead of resetting, and the test that LOOKS like it covers this checks something else. No live defect — the code is correct today; the guarantees are what is unpinned. Also unpinned: the compare tolerance across five orders of magnitude, and the daily no-peek guarantee against a SAME-LENGTH peek — the one case whose failure mode is silent lookahead contamination rather than a mis-set threshold. Companion to [T0075] The keystone was the same-length peek, whose failure mode was silent lookahead contamination of the verdict rather than a mis-set threshold. Two findings decomposed further than their wording suggested — the day-cutoff WINDOW as distinct from its anchor, and a test whose NAME promised a guarantee it did not provide — both caught only by looking again after "done".

- [T0075 — the gate-evidence cache's safety guarantees are unpinned](archive/T0075-gate-evidence-guarantees-are-unpinned.md) — **resolved 2026-07-20** (spec 00064 + 00065): all 19 gaps pinned and mutation-verified under bytecode control. The re-audit first corrected the count from 8 to 19 — not via the stale-bytecode the topic predicted (zero false detections) but via TRIAGE ERROR: an equivalent-mutant ruling is a universal claim, and one non-distinguishing input is not evidence for it. The whole-branch review re-derived coverage independently, writing its own 19 mutations from the audit's definitions rather than reading the mapping; each was killed by exactly the test claiming it. D9 was ruled by the owner and implemented rather than deferred, and the residual it exposed was split into [T0080] rather than buried in an archived file. Every one of the 20+ defects found across the work was an artifact asserting something untrue — never a computation returning a wrong number.

- [T0080 — replay_fingerprint misses the re-export layers](archive/T0080-replay-fingerprint-misses-the-re-export-layers.md) — **resolved 2026-07-20** (spec 00065): coverage is now DERIVED from the transitive `cli.*` import closure, not enumerated. Measured first — the hand-list covered 12 of 61 executing modules (~20%), so three rounds of "add one more module" were converging on nothing. Adversarial verification then caught the first implementation shipping the same hole one level up: ancestor `__init__.py` files execute on every replay and went unhashed, exploitable at a byte-identical fingerprint with 64 tests green. The durable fix is not the closure but the superset test that enumerates nothing and so cannot go stale — it FAILED on the first implementation, which is what makes it the right pin. Now: 61 covered, 60 executed, 0 uncovered.

- [T0082 — spec 00050/00048 parked-items review](archive/T0082-spec-00050-00048-parked-items-review.md) — **resolved 2026-07-21**: all 15 parked items dispositioned one line each — nine drops with the delivered/tracked evidence named (secondary Alloy shipped iter-105, REST backfill via T0050, verify-replay on ops, dead-men live, reconciler edge cases pinned by 58 tests, the reconciler itself already on ops since OPS-5, remaining residuals tracked on T0008/T0032/T0069), five spec-rationale drops, and ONE revival: the multi-provider residual became [T0088] — the only item whose trigger ("revisit before scaling capital") is live today. Nothing in either spec is silently parked any more.

- [T0079 — nothing alerts on an Alloy that stopped shipping](archive/T0079-nothing-alerts-on-an-alloy-that-stopped-shipping.md) — **resolved 2026-07-21** (attended push window): the four per-host `Fleet · Alloy dark` dead-man rules are live — `count(up{host=...}) or on() vector(0)` below 1 for 10m (the NAS matcher was originally the deliberate `{host=""}` label-absent form, re-keyed to `host="nas"` at spec `00069` D7, 2026-07-22/23) — plus `up` pinned in the capture keep-list (mutation-verified). Live-verified by outcome: one eval cycle after the push all four read Normal/ok, which is only reachable when every selector resolves; the T0048 incident class stays owned by the SD-wedged rule, stated inline. First application of the auto-exec landing rule: authored branch-ready by the loop, completed + PR'd in the attended window.

- [T0083 — healthchecks retag + mutual watchdog](archive/T0083-healthchecks-retag-and-mutual-watchdog.md) — **resolved 2026-07-21** (attended window, all four steps drilled): nine checks retagged node + application (catching `archive-pull` mis-tagged `panel` and `engine-shadow` untagged); the `zcrypto-grafana-watchdog` check + a 5-min ops timer that pings only while Grafana answers and `/fail`s on probe failure; the ops Alloy scraping hc.io's project endpoint (read-only key vaulted) behind rule `zcrypto-hcio-watchdog`, whose `or on() vector(999)` fallback makes scrape silence page rather than read green. Both directions were drilled live and self-resolved — the Grafana rule fired on a throwaway down check, and the real pinger with a swapped probe target drove the check DOWN and back UP (2 m 7 s) in Slack.

- [T0088 — both capture hosts sit on one provider](archive/T0088-both-capture-hosts-single-provider.md) — **resolved 2026-07-23 (owner ruling, taken ahead of the 6b gate)**: the acceptance ruled forward in full — architecture as-is: hot standby declined (split-brain duplicate orders with one trade key and no fencing; key posture 00057 D4; minutes saved against a 4-hour cadence), warm standby assessed feasible (the engine fetches OHLC from REST and mounts no capture data; already the NautilusTrader TradingNode) but declined as over-engineering for the bounded engine-loss cost; diversification and the account-level de-correlation dropped with the rest — the correlated-loss residual is consciously accepted at current stakes.

- [T0091 — the spread calibration is 13.1 days; Phase 2's exit bar says ≥2 weeks](archive/T0091-spread-calibration-two-week-restamp.md) — **resolved 2026-07-23**: the panel reached 353 h/pair, the committed query was re-run, and the table + all three provenance constants + the reference doc + the Phase-2 closeout were restamped together on a **14.68-day** window (`2026-07-08T13:47:33Z … 2026-07-23T05:59:59Z`, ≥1,260,309 rows/pair) — Phase 2's last carried-forward exit-bar row now reads DISCHARGED. Constants moved **within** the recorded 1–6 % day-level SE (largest: LINK @€100 +5.0 %, DOT @€100 −2.9 %), so arithmetic not regime; both instrument checks re-ran clean (mean÷median: BTC 1.63× still the only elevated pair, no new tick-quantisation crossing; session band worst 1.13×, so a session term stays unwarranted).

- [T0094 — a universe rebuild can record an empty dataset hash](archive/T0094-universe-provenance-can-record-an-empty-dataset-hash.md) — **resolved 2026-07-23**: `_refresh_universe` now fails closed with `DataSyncError` when `ohlc-full/manifest.json` is absent, instead of writing `ohlc_dataset_hash: ""`. `backfill_basket` always writes a manifest, so its absence means a broken or half-written set — and an empty string is the wrong shape for "unknown": it reads as a value and compares EQUAL across two different broken builds. Chosen over *omit the key* because both leave no identity, but omitting still publishes an artifact from a set just discovered broken. Guard fires before any write (pinned), blast radius measured at exactly two fixtures, mutation-verified.

- [T0042 — Alloy held root-equivalent Docker access](archive/T0042-alloy-holds-root-equivalent-docker-access.md) — **resolved 2026-07-26** (specs `00068`/`00069`): the docker-socket dependency is eliminated fleet-wide, not merely bounded — verified by `docker inspect` on all four hosts (no socket mount anywhere); apps ship their own logs directly (`--ship-logs`) and container CPU/mem comes from process self-metrics (D4), never cadvisor. The engine-egress concern resolves the same way — no docker-log discovery remains to carry its logs through Alloy at all. The Tecnativa proxy-timeout upstream filing is dropped as moot (no host runs that proxy).

- [T0048 — Alloy's docker tailer dies on container recreation](archive/T0048-alloy-docker-tailer-dies-on-container-recreate.md) — **resolved 2026-07-26**: the `discovery.docker`/`loki.source.docker` mechanism all three defects lived in is retired fleet-wide (spec `00068`), verified on all four hosts' live configs; none of the three defects can recur since the vulnerable component no longer runs anywhere. The topic's own archive gate — the `zcrypto-alloy-docker-sd-wedged` rule — was deleted at the spec `00069` rollout's final prune (Step 8, 2026-07-26), confirmed by a re-run reporting no orphaned rules.

- [T0089 — logrotate copytruncate wedges docker's log readers](archive/T0089-logrotate-copytruncate-wedges-docker-log-readers.md) — **resolved 2026-07-26**: the `base` role's daily `copytruncate` truncated files dockerd held open, leaving six long-lived containers unable to ship logs while `docker logs` hung. The repo fix + fleet-wide policy purge (2026-07-21) stopped any NEW wedge, but that was prevention, not recovery — only container recreation clears an existing one, and the byte-offset explanation was measured and falsified. All six were cleared through their registered vehicles (spec 00068 D7's rollout; the engine last, at spec 00069 Step 7 on 2026-07-26), and closure was measured in Loki — all six confirmed shipping post-recreation, not inferred from the restarts.

- [T0069 — gate-export CPU cost](archive/T0069-gate-export-cpu-cost.md) — **resolved 2026-07-26**: the ~8 min/cycle cost that motivated this topic is measured gone — the journal DOUBLED (96→194 MB) while `gate_export_duration` fell to **33.6 s**, spec `00060`'s incremental scoring decoupling cost from journal size. The parked relocation-to-ops was consciously **dropped** on that evidence (its value today is 0.9%→0.14% of the pull interval, for an attended cross-host move). The re-decision surfaced the real risk instead: the lag alert's threshold was never derived and sat ~0.9 pull periods above a ceiling set by the ENGINE's 4h cadence, so one slow cycle would have paged during the absence — re-derived to 21,600 s across BOTH consumers (the Grafana rule and `--lag-fail-seconds`, whose default gates the hc.io dead-man).

- [T0021 — VPS journal retention](archive/T0021-vps-journal-retention.md) — **resolved 2026-07-26**: a daily 01:23 UTC prune bounds the VPS engine journal at 60 days (spec `00070`, deployed + verified; a no-op until ~2026-09-10, when the oldest day first ages out). "Prune after verified pull" resolved to margin-plus-monitoring rather than a NAS→VPS handshake, which would have opened a write path into the trade-key host; the load-bearing guard turned out to be a keep-newest-N floor protecting order-delta derivation, not disk.

- [T0100 — one-off timer metrics have no transport on the capture hosts](archive/T0100-capture-host-oneoff-metrics-have-no-transport.md) — **resolved 2026-07-26**: the textfile collector is back on the capture hosts via the existing `/host/root` mount, with absent/unreadable/stale rules; it had already cost \[[T0021]\] all observability, silently.

- [T0096 — internal terms on user-facing surfaces](archive/T0096-internal-terms-on-user-facing-surfaces.md) — **resolved 2026-07-26**: the rule is `operator-facing-text.md`, enforced by a test that checks every non-docstring literal and the rendered `--help`; 48 leak sites across eight surfaces, and review tripled the first pass by showing the detector's own blind spots.

- [T0081 — /zcrypto-bump-alloy skill](archive/T0081-zcrypto-bump-alloy-skill.md) — **resolved 2026-07-27**: the skill's first real run bumped Alloy v1.17.1→v1.18.0 across all four hosts with no protected service touched, and corrected four things the written procedure got wrong (a missing `sudo`, a recreate that trips the ops ERROR rule, and two verification checks that read as failures on a healthy host).

- [T0035 — capture crashes when a WS reconnect is rejected](archive/T0035-capture-crashes-on-ws-reconnect-503.md) — **resolved 2026-07-27 by drill**: a sandbox on the ops node hit a real Kraken 503 unprompted and an induced blackout, exercising both arms of the handler — backoff 1→60s capped, the every-10 ERROR, `restarts=0` through 15 attempts, clean recovery, and the spanning hour hash-verifying against its manifest.

- [T0008 — book-desync recovery + the desync ROOT CAUSE](archive/T0008-desync-recovery-robustness.md) — **resolved 2026-07-27**: root cause fixed 07-13, the stuck-shape alert drill-validated 07-27, and recovery is now a bounded retry ladder escalating once to a full reconnect — measured against real Kraken at 3 retries, 1 escalation, 58.3 s to escalate, and no second escalation over the following 4 min.

- [T0101 — the reconciler saw primary silence the daemon did not book](archive/T0101-primary-silence-the-daemon-does-not-book.md) — **resolved 2026-07-27 by spec `00073`**: the cause was a Kraken `1012 (service restart)` reaching both hosts 19.65 ms apart after ~209 s of silence on a socket that stayed open and whose keepalive completed ≥11 ping/pong round trips — so every liveness signal read healthy and the dead-man pinged green through a total 12-pair blackout. The daemon now books a per-pair book-staleness window stamped at `last_seen` (threshold 30 s, ~2.4× the binding pair's measured p99), records the venue `status` channel it used to discard, and floors the post-`1012` backoff at 5 s. This topic's own diagnosis (unbooked *reconnect* silence, ~39 s/pair) and its "no data loss — verified" claim were both **wrong** and are corrected in place; 2,437.15 s of L2 is permanently absent and a CRITICAL page had already fired at 09:18:35Z. Remainders split to \[[T0103]\], \[[T0104]\], \[[T0105]\].

- [T0099 — Dependabot's group splits pre-commit from uv](archive/T0099-dependabot-consolidation-splits-ecosystems.md) — **resolved 2026-07-27 by consciously dropping the group** (owner ruling): `.github/dependabot.yml` is back to per-ecosystem PRs on the pre-group schedules, making the is-the-group-honored question moot; the harmful half (a drifting ruff mirror pin) was already fixed by running ruff from the lock.

- [T0095 — engine trading telemetry](archive/T0095-engine-trading-telemetry.md) — **resolved 2026-07-28 by redistribution**: the standing egress ruling (order/position/PnL detail to Grafana Cloud is accepted, live trading included — no redaction gate) archives here as its durable home, while emitting the metrics moved to \[[T0018]\] as a named 6b-executor bullet (it cannot be built until the order state machine exists to instrument) and charting them moved to \[[T0020]\] (presentation, blocked on the executor). Both landing sites carry the Alloy keep-list requirement.

- [T0102 — `req_id`-correlated resubscribe](archive/T0102-reqid-correlated-resubscribe.md) — **resolved 2026-07-28, both halves**: both frames carry a `req_id` and the `subscribe` waits for the `unsubscribe` ack, closing the overtake race; rejections of either frame are counted and logged where a failed attempt previously left no trace. `resubscribe_book` never awaits (rung 1 runs on the task that drives `stream()`, so awaiting there would deadlock), and a lost ack times out into the old ordering rather than stranding the pair.

- [T0084 — /zcrypto-captures-rollout skill](archive/T0084-zcrypto-captures-rollout-skill.md) — **resolved 2026-07-28**: the skill ran a real capture-image rollout end to end (secondary 07-27T23:58:41Z → primary 07-28T08:04:29Z, an 8 h 06 m event-coverage bake), which was its one remaining step — the run *is* the validation. It surfaced six defects only an execution could find, all landed in `SKILL.md` in the same change: pre-staging ungated at the step that needs it, Phase 5 written as post-primary only (so the pins file was wrong about a live host for 8 h), no operand for the Cloud read-back, the alloy container's real name, no parquet reader on the capture hosts, and the NAS pull being a container rather than a systemd unit. Phase 5 verified clean: 0 missing / 0 truncated hours, manifests `failed=0`, worst stream 0.0114% against the 0.1% bar.

- [T0104 — the panel fabricates a frozen book across a gap](archive/T0104-panel-fabricates-a-frozen-book-across-a-gap.md) — **resolved 2026-07-28**: `stale_seconds` now marks every row the book was frozen for, and the tree was regenerated to `schema_version: 2` so the already-written rows carry it too. Verified by outcome — 4,840 files all 20-column with zero at the old width, BTC-quoted subtrees gone from both copies, and the 2026-07-27 hour-07 blackout marked across all 10 in-scope pairs at 199–208 s, ADA/EUR's 207.93 s matching the reconciler's independently measured 208.566668 s to within a second. The ordering trap it hit — the converge re-enables the panel timer — is recorded in `capture-deploys.md`.

- [T0106 — the rollout's log-shipping freshness signal reads red when idle](archive/T0106-logship-freshness-reads-red-when-idle.md) — **resolved 2026-07-29**: a separate `zcrypto_logship_last_cycle_timestamp_seconds` now answers "is the worker alive?", the rollout abort row reads it, and the old gauge is demoted to corroboration. The bake proved it rather than arguing it — on a green host the two gauges read 0 s and 39 min stale at the same instant.

- [T0107 — the deferred capture-image payload](archive/T0107-deferred-capture-image-payload.md) — **resolved 2026-07-29**: rolled as `sha256:99faf165…` after a 6 h 43 m bake with a strong-form prune. All five previously-undelivered series now arrive in Cloud, including spec `00073`'s blackout measurement, which had been emitted and dropped since it was built.

- [T0109 — every `config.alloy` change is silently skipped by ordinary capture converges](archive/T0109-alloy-config-changes-never-reach-the-capture-hosts.md) — **resolved 2026-07-29**: two layers, a digest gate that stopped the config reaching the host and a single-file bind mount that stopped it reaching the process. Both already documented in spec `00071`, whose ruling never reached the operator-facing rules — so it was re-derived in production during the converge fixing the layer above it. All three tiers now mount the config directory with a change-triggered reload, guarded by a CI test. Spec `00073`'s blackout measurement, emitted and dropped since the day it was built, finally reaches Cloud.

- [T0108 — loss outside a fleet-dark window reaches no counter](archive/T0108-loss-outside-a-fleet-dark-window-reaches-no-counter.md) — **resolved 2026-07-29**: measured with a 400 h detect-only sweep, covering the ~15 days both mirrors overlap with post-fix records, so the split was a pure ledger read. One unwitnessed event in ~15 days, **6.821701 s** reaching no counter — a footnote, not a defect, and far too small to justify moving a thin-market false positive into the CRITICAL permanent-loss page. Decided (a): leave it ledger-only, and the residual counter's HELP now says it is a floor on permanent loss rather than the whole of it.

- [T0110 — nothing compares a host's running Alloy config to the repo](archive/T0110-no-check-compares-a-hosts-alloy-config-to-the-repo.md) — **resolved 2026-07-29**: a converge-time assert in the capture and ops roles, deliberately outside the digest gate that would otherwise skip it on exactly the converge causing the drift. Compares the deployed file's sha256 against the repo's, both via `stat` — the first attempt used the file lookup, which strips the trailing newline and would have failed on every host forever. Proven in both directions against the live host.

- [T0097 — the archive verification instruments have measurement defects](archive/T0097-archive-verification-instruments-have-measurement-defects.md) — **resolved 2026-07-30** (spec `00076`): the silence threshold is now fitted from a week-of-data fit rather than guessed (`p99.99 × 10`, `10 × median` refuted at 2,061.7 s phantom on a clean week), the thin-stream false GREEN is closed by refusal (`UNMEASURED`) rather than better measurement, the head/tail test is boundary-spanning so a genesis hour is annotated not booked while the real T0036 signature still books, and the head/tail fix stands. Its fourth leg — windowing `archive verify-replay` — was **reverted the same evening** (a window cuts chain-anchoring: 1,870/2,218 hours `anchored=False`); superseded by spec `00077`, with the runtime cost split to [T0114](archive/T0114-verify-replay-rescans-the-whole-archive-every-night.md). Acceptance: ETH/BTC 0.1320% → 0.0411%, SOL/BTC 0.1332% → 0.0410%, fleet truncations 72 → 60 (genesis false-RED only), fleet total 0.6131% → 0.4644% (verdict stays FAIL honestly — real EUR-stream gap, not fiction). One residual split before archiving: [T0112 — the derived threshold self-inflates on repeat outages](archive/T0112-quantile-threshold-self-inflates-on-repeat-outages.md).

- [T0032 — capture dies silently when the disk fills](archive/T0032-capture-disk-watermark-silent-death.md) — **resolved 2026-07-30**: a `DiskWatermark` breach used to stop all row writes while the dead-man kept pinging green over lost, unbackfillable L2. The ping is now withheld on breach AND on an unmeasurable probe (so it pages either way), the breach window is booked into the exit-bar gap accounting, T0003's 20×-wrong ~10 GB/day was corrected to a measured 0.48 GB/day, and the 14-day prune ring buffer is proven on BOTH hosts — primary 2026-07-23 (`deleted=560`), secondary 2026-07-29 (`deleted=120`, then `deleted=952` on 07-30) — with the prune frontier landing on the cutoff day and the capture tree flat at 6.2 GiB. The disk-fill deadline is dissolved for capture growth. Its one residual (an `ENOSPC` during an unmeasurable probe booking no gap) was **refuted, not deferred**: the probe withholds the ping so it pages, and the exit bar is measured from the archive rather than from the daemon's in-process counter, so the hole is booked.

- [T0114 — verify-replay rescans the whole archive every night](archive/T0114-verify-replay-rescans-the-whole-archive-every-night.md) — **resolved 2026-08-01 by spec `00078`**: the nightly sweep still certifies the whole archive but replays only what changed, so runtime is `(new hours) + (fixed budget)` forever and the journal output is flat — dissolving both dated cliffs (journald `RateLimitBurst` ~2026-08-14, runtime meeting the 24 h cadence ~2026-09-11). The checkpoint caches **raw per-hour facts only** and the chain verdict is recomputed every run, because caching the fold's output would make anchoring irrevocable; identity is the sha256 of the bytes actually replayed at a hand-bumped `VERIFIER_VERSION`, cached failures are never trusted, and a nightly 25-hour sampled audit of cache-trusted rows is the guard that can actually fail. All three sub-items close: checkpointing built, windowing decided against permanently (a window cuts the anchor chain, and the runtime motive is gone), and the duration/hour-count metrics shipped as the run's census with a backlog-stuck alert. Deploy is an attended tail carried in the iteration entry and the memo.

- [T0112 — the derived threshold self-inflates on repeat outages](archive/T0112-quantile-threshold-self-inflates-on-repeat-outages.md) — **resolved 2026-08-02 by spec `00079`**, ahead of its trigger (which was drifting away as `n` grows with capture) because the reachable path — a narrow `--since` window on a slow stream — still exists. `continuity.py` now refuses a stream whose spacing tail steepens more than 10× across a decade of quantiles (`p99.99/p99.9` **and** `p99.9/p99`, denominators floored at 0.5 s), which is the same degeneracy `MIN_POOL` guards arriving from repeat outages instead of from a small sample. The founding defect flips — the carved k=2 fixture derived `thresh_s = 2004.0`, booked 0.0 s and PASSED over 401.4 s of genuinely missing data, and now reports `UNMEASURED` and FAILs — while the acceptance run against the full production archive is line-for-line identical to the shipped script (12/12 streams still measured, zero refusals; real streams measure 1.05–1.96 against the cut, contamination 88.8–200). Both sub-items close: the second shipped the `tail` depth column as **transparency only**, because this iteration measured that depth is provably not a detector (identical at clean and contaminated n=11,389) and pinned that equality as a test. All three options the topic suggested are explicitly disposed of, and the D6 residual (blindness at k ≥ ~0.01·n, exactly k=115 for n=11,389) is a recorded conscious drop, not a deferral — reaching it needs the window to be 77–97 % outage, and the truncated-hours count is **not** a backstop since it reads `secs > thresh` and the same contaminated threshold blinds it too.

- [T0117 — the ~5 % gross decomposition](archive/T0117-gross-decomposition-measurement.md) — **resolved 2026-08-02 by spec `00081`** over 136 journaled cycles: the framing was a false binary and neither branch was the answer. The chain, medians of per-cycle values: A2 sleeve gross **32.224 %** → ÷3 by the fixed ⅓ combination weights → **10.741 %** → position caps bind on **0 of 136** cycles → **×0.5** governor (constant) → **5.371 %** final gross. The governor *is* derating, but that is only a factor of 2 of a factor of ~6; the missing factor is that two of the three sleeves hold exactly zero, which the topic never listed and which is now [T0124](archive/T0124-deployable-runs-as-a-one-sleeve-book.md). **No residual is left over** — the "naive 15–20 %" was a per-sleeve vol target mistaken for a book gross target, and A2 alone runs above it — and the spec's own D3 hypothesis (sleeve **cancellation** as the likely mechanism) is **refuted**: the cancellation ratio is 1.000, because there is nothing to cancel against — B's and A1's gross are float-exactly `0.0` in all 136 cycles. **5.371 %** is handed to T0116 as the expected-gross parameter for rungs 2–3.

- [T0118 — accumulation replay → the honest tracking band](archive/T0118-accumulation-replay-band-derivation.md) — **resolved 2026-08-02 by spec `00081`** over the same 136 cycles: the drift floor Kraken's minimums impose, per cycle in bps of NAV, is €500 → median 147.0 / p95 209.2; **€1,000 → 51.2 / 115.7**; €2,500 → 11.6 / 30.9; €5,000 → 6.2 / 14.2; €10,000 → 4.3 / 8.9. So the pre-registered 10 bps-of-NAV weekly band is **5.1× unmeetable at the median and 11.6× at p95** at rung 3's €1,000, and only becomes meetable around €5,000 / €10,000 — the far end of the ramp, not the tiny-live size §12 specifies. No weekly p95 is reported and none should be quoted: four ISO weeks cannot support a percentile. Numbers rest on venue minimums stamped **`fetched_at 2026-07-07T03:29:00+00:00`** ([T0113](T0113-snapshot-register-reconfirmation-is-unregistered.md)), and the floor is derived from *shadow* targets — both caveats carried into T0116, where the gate is written.

- [T0116 — Stage 6b sub-phasing amendment](archive/T0116-stage6b-subphasing-amendment.md) — **resolved 2026-08-02: the ladder is ratified and landed in §12.** Stage 6b's and the go/no-go's lines are **rewritten in place** in `00.master-plan.md` (822 → 3,245 chars) rather than corrected by an appended note, so the plan reads correctly cold; the superseded single-step form is stated as measured-unexecutable (0 of 801 journaled intended orders clear Kraken's minimums at €500 quarter-size) rather than deleted silently. §12 now carries the three rungs with entry/exit criteria (T2 probe → time-boxed concentrated, **its tracking error explicitly excluded from the gate** → full weights at €1,000 under `delta = target − actually held`), the go/no-go reading **the measured band at the funded NAV** instead of the unmeetable 10 bps — with a re-derivation trigger that fires on a governor lift or a sleeve re-arming, not only on execution changes, because a stale band is too *wide* — performance judged against the **conditional** expectation as a band *(whose two uncertainties — statistical CI and cost basis — §12 was amended again on 2026-08-03 to state separately, never merged)*, and T0124's composition divergence stated knowingly with sleeve occupancy a rung-3 precondition. Concrete figures deliberately stay OUT of §12 (a number in the north star is quoted for years); they live in the archived topic and in T0117/T0118. **Ratified ≠ executed**: T0119's formula remains open; T0121's limits are wired (2026-08-02) and **T0124's gauge is deployed and verified by value (2026-08-03, `active_sleeves` = 1)**, so the rung-3 precondition is discharged.

- [T0124 — the deployable is operating as a one-sleeve book](archive/T0124-deployable-runs-as-a-one-sleeve-book.md) — **resolved 2026-08-03: the gauge is live and verified BY VALUE.** After the engine converge to `c7ed09020fe1` (04:45:41Z, canary-baked as capture on `zcrypto-red` first — there is no engine secondary, so that bake IS the engine's gate) and the first cycle under it (`cycle-08.json` completed 08:01:40.845Z), `zcrypto_engine_active_sleeves` reads **exactly 1** on the host and in Grafana Cloud, with `sleeve_gross` B=0, A1=0, A2=0.10002169357501955 — the one-sleeve state confirmed live, matching all 136 journaled shadow cycles. Reading "the series appeared" as success would have repeated the very failure the gauge exists to catch, so the value is the evidence. The silence between converge and first cycle was correct by construction: a fresh Gauge's `0.0` would *claim* "no sleeves active" rather than be absent. The owner's PROCEED-KNOWINGLY ruling stands with its three conditions now all discharged — baseline-as-band and the divergence statement in T0116, and this gauge as the rung-3 precondition. Two operational findings kept: the Alloy keep-list lives in the **capture** role, so an engine-only converge would have published these families unadmitted and left the by-value check nothing to read; and the primary's capture was deliberately not re-pinned, since the payload is engine-side and restarting live capture would spend risk on the unbackfillable path for nothing.

- [T0064 — the deployed strategy has no out-of-time holdout evidence](archive/T0064-deployed-strategy-has-no-holdout-evidence.md) — **resolved 2026-08-03: ACCEPT, and the gate's claim narrowed to match.** §12 now states that passing the go/no-go certifies **execution** and explicitly does **not** certify **edge**, and names both limitations it inherits — this one and T0125's unrebuildable adoption criterion. Mitigation was not rejected as costly but **measured as unavailable**: the only window that exists (2026-04-01 → 2026-08-03, 124 days = 0.34 y) returns Sharpe ±3.7–5.0 and would need 3.5–11.6 years to separate from zero, so a ratified budget expansion would repeat the degenerate-window failure that wasted the first look. The decision-support read ran at full window (L=140, 23.17 days, self-tests ok): 1 of 7 outside band, 2 of 7 indeterminate, realized net −0.4559 % — both disagreements tracing to already-registered conditions (T0018's carried ×0.5 governor, T0124's one-sleeve book), and the instrument itself is explicit that this is conformance, not edge. **The read had to be repaired first**: against the configured store it silently scored 54 bars ending 2026-07-19, dropping 87 of 141 cycles, because iter-103 retired the soak that kept that store warm — and the verdict differed (0 of 6 outside band, with `governor_engagement` having no discriminating power at all). `soak-check` now names what bounded the window and warns when it was the store.

- [T0073 — soak-check secondary null + options](archive/T0073-soak-check-secondary-null-and-options.md) — **resolved 2026-08-03.** The secondary block-bootstrap null, `--null`/`--path` and the severity reconciliation landed at iter-111 (spec `00061`), with `--null windows` reproducing prior verdicts exactly so the second construction is strictly additive. The one remainder — spec `00058`'s report-shape item 9, "regime context" — is **DROPPED on a measurement**: any regime split halves the window, and splitting the 23.17-day realized window (L=140) into halves of 71 and 68 destroys the *only* discrimination the instrument makes (`governor_engagement` reads inconsistent at full window, **n/a — no power** on both halves), while the two halves — **the same regime** by every system-internal measure — already disagree with each other (`gross`/`net` consistent vs indeterminate; realized net +0.0302 % vs −0.5514 %). So a regime section could not separate a regime effect from sampling variation, and would invite a reader to see structure in noise. Its honest residue already ships as no-variance disclosures plus T0124's sleeve-occupancy gauge. The drop is recorded in spec `00058` with the measurement, so the spec no longer reads as owing a section.

- [T0125 — the deployable's ADOPT criterion rests on an unrebuildable trial](archive/T0125-deployable-adoption-criterion-is-unreproducible.md) — **resolved 2026-08-03: the gate is RE-GROUNDED on a reproducible basis, and that basis is now measured end to end.** Record 44's verdict was ADOPT *vs incumbent trial 43*, and trial 43 is gone for good — never committed, its runner a vanished scratchpad. But it was never the only support: record 44's **benchmark-relative** kill bar is reproducible, and every leg has been re-derived from committed code with the instrument validated first — SPA (all seven cells, **bit-identical**), DSR + `var_trials` (bit-identical; the registry had `dsr` rounded), `per_period_sharpe_4h` (bit-identical), worst-slice-relative, `cap_breach_bars`/`governor_engaged_bars` exact, and Sharpe 1.5609 against a benchmark reproduced at 1.2128/1.2447 (margin **+0.348 / +0.314**). **Why this is legitimate where rebuilding trial 43 is not**: every primitive is committed and the benchmark's construction was specified *in advance* — a first attempt with the wrong construction missed by 0.06 and was discarded, not adjusted. The re-derivation is **committed** (`cli/portfolio/record44_legs.py` + tests), since landing it in another scratchpad would have been self-defeating. Two details spec `00038` never pinned — the SPA headline cell `(30, 42)` and the worst-slice series/window — are now written there. The forward guard (`dbe27e0b`) makes recurrence impossible. **What stays lost is the counterfactual**: the 43-vs-44 ordering can never be re-read, including where the registry discloses it reverses at ×2.0 — the re-grounding makes the gate not depend on it.
