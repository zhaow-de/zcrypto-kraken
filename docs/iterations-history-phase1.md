# Iterations history — Phase 1 (Data Foundation)

Per-iteration changelog for Phase 1. Appended at each iteration's close-out; see `.claude/rules/iterations-history.md`.

## 2026-07-07 — iter-004: OHLC ingestion → canonical Parquet (Phase 1 · v0)

- **Added `cli/ohlc/`** — a public-REST OHLC → canonical **polars** Parquet ingestion pipeline (the project's first research-core data dep, `polars>=1.42.1`): `fetch.py` (public `OHLC` GET, raises `OHLCError` at every failure boundary incl. a missing/`last`-only result and unparseable string decimals), `dataset.py` (`to_frame` — typed schema: UTC `ts`, Float64 OHLCV/vwap, Int64 count, sorted+deduped, raises on NaN/non-monotonic; `write/read_parquet`; deterministic `dataset_hash`), `ingest.py` (`ingest_basket` — writes `data/ohlc/{symbol}/{interval}.parquet` + `manifest.json`, returns a content-hashed manifest), `errors.py`.
- **Generated the v0 dataset** — 12 candidate symbols (resolved live via `cli.snapshot`) × 3 decision-cadence intervals (1d/4h/1h) = 36 series × 721 rows (Kraken's REST cap ≈ 2y daily / 120d 4h / 30d 1h), under gitignored `data/ohlc/`. Cataloged in `docs/reference/data-catalog.md` (symbol×interval → rows/span/sha256 + fetched_at).
- **18 unit tests** on a saved trimmed `OHLC` fixture + monkeypatched opener (no live-network tests): error-array + boundary raises, decimal-parse, sort/dedupe, NaN raise, Parquet round-trip, `dataset_hash` determinism + sensitivity, manifest persistence.
- **Parked (→ open-topic T0001):** full-history backfill from Kraken's downloadable OHLCVT ZIP archive — the ~2y REST window is insufficient for §9's 2019→2026 walk-forward regimes; this v0 dataset is what the backfill later extends. **Known minor:** `dataset_hash` stability depends on the polars version's float formatting — a polars bump may require regenerating the catalog hashes.
- Independent whole-branch review (Sonnet): initial verdict CHANGES REQUIRED (the catalog documented a `manifest.json` the pipeline didn't write; two error boundaries leaked non-`OHLCError` exceptions) — all fixed and re-review APPROVED, verified by execution.

## 2026-07-07 — iter-005: rule-driven universe finalization (Phase 1)

- **Added `cli/universe/`** — the §3 mechanical universe rule, the first iteration to *consume* two prior iterations' outputs (iter-002's margin/leverage snapshot + iter-004's daily OHLC): `volume.py` (`median_quote_volume` — 30d median of volume×vwap), `rules.py` (`finalize_universe` — margin ∧ leverage≥2× ∧ median-quote-volume≥floor; BTC/ETH-EUR mandatory; `escalate` on <8/>15 names), `build.py` (`build_universe_file`/`render_markdown` — the point-in-time file with per-symbol criteria + provenance + `spread_cap: pending-capture`), `errors.py`.
- **Generated the point-in-time universe file** `docs/universe/point-in-time-universe.md` — and its first real run **fired the §3 escalation**: at the (loop-chosen, tunable) €1M/day EUR-volume floor only **6 names** pass (BTC/ETH/SOL/XRP/ADA/LTC-EUR); LINK/DOGE/DOT/AVAX have genuinely thin Kraken-EUR liquidity (<€1M/day median), and the BTC-quoted RV legs (ETH/BTC, SOL/BTC) fail on a quote-currency unit mismatch (BTC-denominated volume vs a EUR floor). This divergence from §3's recommended 10–12 basket is a human decision → parked in **open-topic T0002** (calibrate the floor / route thin alts via deeper USD pairs / accept a smaller basket / make the volume floor quote-currency-aware).
- **13 unit tests** (synthetic inputs, no live network): median-volume + too-few-rows raise, leverage/volume-floor drops, mandatory-kept-though-failing + flagged, escalate boundaries, reasons populated, build determinism.
- **Deferred (non-blocking, → T0002):** relabel cross-quote volume reasons as "quote-currency mismatch — deferred"; quote-currency-aware / FX-normalized volume; the spread criterion still awaits the L2 capture daemon. A `MAX_NAMES`-boundary test is a noted test-coverage gap.
- Independent whole-branch review (Sonnet): APPROVED — rule engine correct + tested; the BTC-leg mismatch adjudicated as a narrow, non-silent limitation legitimately deferred to T0002 (escalate=True guarantees human review before production).

## 2026-07-07 — iter-006: OHLC dataset QA report (Phase 1)

- **Added `cli/ohlc/qa.py`** — read-only QA over the canonical OHLC Parquet dataset: `detect_gaps` (missing candles on the interval grid), `wick_outliers` ((high−low)/close > 0.20), `qa_series` (rows, coverage%, gaps, wick outliers, monotonic-ts, nonneg-volume), `qa_dataset` (walks the nested `{base}/{quote}/{interval}.parquet` tree — symbols contain `/`), `render_markdown`. Pure (`as_of` injected; no clock/network); stdlib + polars.
- **Generated the QA report** `docs/research/02.phase1-ohlc-qa-report.md` over the v0 dataset — **clean: 100% grid coverage, 0 gaps across all 36 series** (12 symbols × 1d/4h/1h); 87 wick-outlier flags, all on daily altcoin candles (a benign heuristic flag, not a failure).
- **10 unit tests** on synthetic frames (no live-dataset reads): planted gap → `missing==1`, planted extreme candle flagged, coverage math, monotonic/nonneg flags, whole-dataset walk + summary, `as_of` present/omitted.
- **Deferred (follow-up Phase-1 iterations):** empty-interval reconstruction (fill no-trade candles — the gaps this surfaces), Binance-Vision cross-venue reconciliation, the symbol & corporate-action ledger.
- Independent whole-branch review (Sonnet): APPROVED — verified by regenerating the report from the live dataset (byte-identical), confirming the nested-layout walk matches the `manifest.json` symbol convention, and checking every correctness detail (missing-candle count, coverage math, div-by-zero→inf-not-crash, empty-frame guards).

## 2026-07-07 — iter-007: universe floor calibration + quote-currency-aware volume (Phase 1 · T0002)

- **Resolved open-topic T0002** — iter-005's §3 escalation (only 6 of 12 names cleared the €1M/day EUR floor). Grounded in a live **EUR-vs-USD-vs-USDT** depth + margin-parity + FX/tax analysis (multi-agent workflow, adversarially verified): USD Kraken books are 2–8.5× deeper than EUR for all 10 bases, but that depth does **not** bind at ~$10k (a full per-name position is <1% of even the thinnest EUR book) and EUR gives up **zero** margin capability (EUR=USD exact `leverage_buy`/`leverage_sell` parity on all 10 bases), while USD adds conversion + continuous cash-leg EUR/USD risk + per-fill §23-EStG tax joins + cross-margin FX double-counting. USDT ruled out entirely (thinnest venue, MiCA-delisted for EEA, AVAX/USDT non-marginable, LTC/DOT-USDT capped at 3×).
- **`cli/universe/` changes** (commit `9495e2d`): lowered `DEFAULT_MIN_MEDIAN_QUOTE_VOLUME` €1M → **€150k**/day (footprint-based — a full max-size position ≈1% of median daily EUR volume, our microstructure-impact floor); added `quote_volume_in_eur(daily, *, fx_daily=None, window=30)`, which FX-normalizes a BTC-quoted leg's `volume × vwap` to EUR via the BTC/EUR daily close (inner-join on `ts`, then **sort before `.tail`** so the latest window is always selected — a join-ordering guard added in review, since polars does not guarantee join output order). 6 new tests (144 total).
- **Regenerated `docs/universe/point-in-time-universe.md`** — now **12 of 12** selected, `escalate=False`: the four previously-thin EUR alts (LINK/DOGE/DOT/AVAX) clear €150k, and the two BTC-quoted RV legs FX-normalize to €579,964 (ETH/BTC) and €233,595 (SOL/BTC), ending the unit mismatch — the full §3 target (10 EUR + 2 BTC RV). `data/universe/point-in-time-universe.json` regenerated (gitignored; new sha256 recorded in the doc's provenance).
- **Master-plan §3** — inserted a "Quote leg, liquidity depth, and the floor — measured (Phase 1)" subsection + a floor-decision paragraph: the measured EUR/USD/USDT depth, the margin parity, the friction rationale for EUR, the size-dependence (depth doesn't bind at our footprint), and USD-routing deferred as a pre-registered **scaling trigger**. Supersedes the earlier unquantified EUR-preference note.
- Independent review of the code commit (Sonnet): APPROVED — the FX-normalization correctness and the `.sort("ts")` join-ordering guard both verified (the reviewer reproduced the failure mode empirically).
- **Decisions logged** to `.tmp/decisions.md` (iter-007). **Open-topic sweep:** T0002 → resolved + archived; T0001 (OHLCVT backfill) and T0000 (residual Phase-2 cost-model fold-in) remain open.

## 2026-07-07 — iter-008: full-history OHLCVT backfill (Phase 1 · T0001)

- **Added `cli/backfill/`** — reconstructs canonical OHLC bars from Kraken's downloadable **OHLCVT 1-minute dumps** (on the NAS): `read.py` (`read_minute_rows` — merges the base dump + quarterly-update zips), `aggregate.py` (`aggregate_minutes` — 1-minute → 1h/4h/1d: OHLC first/max/min/last, volume+trades summed, **vwap = Σ close·vol / Σ vol**, a proxy since the dumps carry no vwap), `backfill.py` (`backfill_basket` — canonical Parquet + manifest, reusing `cli.ohlc.dataset`), `reconcile.py` (vs the v0 REST), `errors.py`; `ohlcvt_source_dir` added to `cli.config` / `zcrypto.toml`. **27 tests** on synthetic ZIP fixtures.
- **Generated the full-history dataset** — `data/ohlc-full/` (gitignored): the 12-name universe × 1h/4h/1d, **BTC/EUR daily 2013-09-10 → 2026-03-31** (4581 rows). The 1-minute bars are full-history (only *native* 4h is short, 2024+), so **all cadences are reconstructed from 1-minute**, giving full-history 1h/4h/1d + a real vwap. Cataloged in `docs/reference/data-catalog-full.md`.
- **Validated** (`docs/research/02.phase1-ohlcvt-backfill-reconciliation.md`) — reconstructed OHLC is **bit-identical to the v0 REST** (100% exact match over 623 daily + 137 4h overlap rows per pair); vwap proxy within ~0.05% of REST's true vwap. Honest caveats: daily volume within ~7% max on the worst bar (aggregation/revision artifact; 4h volume exact); 1h not independently reconciled (v0's REST 1h window post-dates the 2026-03-31 dump end). QA: 7807 no-trade gaps, 90.5% min coverage (expected for thin markets / early history).
- **Merge policy** — found by an instrument-check smoke test on the real archive: the base dump and 2023-2025 quarterly updates overlap and disagree on volume/trades (the base is more complete) → the base dump is authoritative for its range and quarterlies extend only past its last ts (Q1-2026, exactly contiguous).
- **Design/plan:** `docs/specs/00005-ohlcvt-backfill-design.md`, `docs/plans/00005-ohlcvt-backfill.md`. **Resolves open-topic T0001** (→ archived). **Deferred:** finer cadences (1m/5m/15m), full-exchange breadth, empty-interval reconstruction, Binance-Vision cross-check, the corporate-action ledger, and pointing the universe/backtests at the new dataset hash (a Phase-2 decision).
- Independent whole-branch review (Sonnet): APPROVED — after two review-driven hardening fixes (numeric de-dup + `BackfillError` at every boundary; the base-authoritative merge policy), both verified live; the 100% OHLC reconciliation vs REST corroborates the reconstruction.

## 2026-07-07 — iter-009: full-history dataset QA report (Phase 1)

- **Generated `docs/research/02.phase1-ohlc-full-qa-report.md`** — ran `cli.ohlc.qa` (iter-006) over the new full-history dataset `data/ohlc-full/` (iter-008): 36 series (12 pairs × 1h/4h/1d), integrity clean (monotonic ts + non-negative volume on every series). Coverage is high — the 7807 gaps are **no-trade intervals** (Kraken omits them; reported, not filled), concentrated in 1h for thinner/older pairs (worst: LTC/EUR/60 at 90.5%; most series 97–100%).
- **Gap characterization for §9** — per-year bar density: BTC/EUR 1h is **100% dense 2014–2025** (2013:18% partial Sept listing; 2026:25% data-to-March); DOT/EUR 1d 100% 2021–2025 (2020:37% partial listing). So the full-history intraday carries reliable density across the major walk-forward regimes (2014+); thin-alt early 1h is sparser — flagged for regime-aware validation.
- Manufactured package (the R&D open-topics backlog was empty after T0001/T0002 resolved) — "deepen data QA" per the loop's empty-backlog rule. No code change (reuses iter-006's `cli.ohlc.qa`); the report + gap characterization are the deliverable. **Deferred follow-ups** (empty-interval reconstruction, Binance-Vision cross-check, the symbol & corporate-action ledger) unchanged.

## 2026-07-07 — iter-010: symbol & corporate-action ledger + discontinuity audit (Phase 1)

- **Added `cli.ohlc.qa.price_discontinuities`** — flags bar-over-bar close moves beyond 3× (or below 1/3×): candidate corporate actions (splits, redenominations) or data errors. 5 tests (176 total).
- **Committed `docs/reference/symbol-corporate-action-ledger.md`** — the §3-required ledger for the 12-name universe: Kraken aliases (XBT=BTC, XDG=DOGE + the dump altnames `ETHXBT`/`SOLXBT`/…), the corporate-action check, and quote-book migrations (the USDT EEA delisting — not applicable to our EUR/BTC legs).
- **Discontinuity audit** over the full-history dataset (`data/ohlc-full/`, daily): **only 2 flags across all 12 symbols, both genuine market events, not corporate actions** — ETH/EUR 2015-08-08 (ratio 0.25, ETH's chaotic launch weeks) and DOGE/EUR 2021-01-28 (ratio 4.89, the Jan-2021 WSB/Elon pump). **DOT/EUR shows no discontinuity** — confirming DOT's Aug-2020 1:100 redenomination is *not* in our history (Kraken listed DOT post-redenomination). Conclusion: the full-history dataset is **clean of corporate-action price artifacts** for the universe; no price adjustments needed for backtesting.
- Manufactured Phase-1 package (§3 hard-dependency; R&D open-topics backlog empty). **Deferred data-foundation follow-ups** remaining: empty-interval reconstruction, Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED — the function + tests verified; the ledger's audit findings independently re-run (2 flags, DOT clean) and the alias table cross-checked against `cli.backfill` / `cli.snapshot`.

## 2026-07-08 — iter-011: empty-interval reconstruction — fill_gaps (Phase 1)

- **Added `cli.ohlc.reconstruct.fill_gaps`** — empty-interval reconstruction as an **opt-in on-read transform**: inserts a synthetic bar per missing grid point (OHLC = vwap = the prior forward-filled close; volume = 0, count = 0), returning a contiguous frame (`detect_gaps` → `[]` after). 6 tests (182 total).
- **Design (distrust-the-instrument):** the fill is a reusable utility, **not** baked into a canonical dataset — a synthetic zero-volume flat candle looks like a real bar and can bias features, so the fabrication stays explicit + opt-in at use time. A filled dataset variant is generatable on-demand if a strategy needs contiguous bars.
- Resolves the "empty-interval reconstruction" deferred data-foundation follow-up (iter-004/006). **Remaining Phase-1 follow-up:** the Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED.

## 2026-07-08 — iter-012: Binance cross-venue cross-check (Phase 1)

- **Added `cli/xcheck/`** — an independent-venue sanity check on the full-history dataset: `binance.py` (`binance_pair_name` maps the canonical `BASE/QUOTE` to Binance's concatenated symbol; `fetch_binance_klines` GETs daily klines from `data-api.binance.vision` via stdlib `urllib`; `binance_daily_closes` → `{ts, close}`; `crosscheck_series` reports overlap rows + close/return Pearson correlation + max abs relative diff; `crosscheck_dataset` walks the dataset root), `errors.py` (`XCheckError`). **19 tests** (201 total) on synthetic frames + mocked `urllib` (no live network in the suite).
- **Generated `docs/research/02.phase1-binance-crosscheck-report.md`** — Kraken vs Binance daily closes over the recent overlap window (902 rows/pair): **close correlation 1.0000 on every one of the 12 universe series**, return correlation ≥ 0.998, max abs relative diff ≤ 4.8% on the worst single bar (DOT/EUR 2025-10-10 — a venue-specific daily-close timing artifact, not a data error). The two venues corroborate each other; the full-history dataset is venue-consistent.
- **Scope (disclosed in the report + docstrings):** the cross-check covers the most recent **≤ 1000 daily candles** per pair — Binance's single-page kline limit. Binance EUR history reaches only ~2020 anyway; a full-overlap check via `startTime` pagination is a deferred follow-up.
- **Distrust-the-instrument:** the review independently verified the correlation metric is genuine — it returns −1.0 on anti-correlated inputs and ≈0.60 on independent random walks, so corr≈1.0 on the real data is a real finding, not a formula that always returns ~1.
- Resolves the last deferred Phase-1 data-foundation follow-up (the Binance-Vision cross-check). Manufactured Phase-1 package (R&D open-topics backlog empty).
- Independent whole-branch review (Sonnet): APPROVED — after review-driven fixes (mocked fetch-error-path tests, the recent-window scope caveat in report + docstrings, and skipping malformed symbols in `crosscheck_dataset`), all verified.

## 2026-07-08 — iter-038: T0003 D2 capture pipeline — built + deployed LIVE (Phase 1)

- **The Phase-1 exit-bar capture pipeline is live.** Designed interactively (spec/plan `00027`) then built unattended and deployed to the provisioned Linode `zcrypto.zhaow.me` (Debian 13, 2 vCPU / 4 GB / 80 GB). New tree `infra/ansible/` (portable IaC) + `cli/capture/` (the daemon) + `infra/docker/` + GHCR CI.
- **Host hardened** (`infra/ansible/`, executed against the real host, no self-lockout): `deploy` sudo user, SSH → **10022** (root + password off), dev-sec.io `os_hardening`+`ssh_hardening` (CIS-L1), **nftables** default-drop inbound (10022 + ICMP; manages only its own table so Docker NAT survives — a bug caught + fixed when the first ruleset's `flush ruleset` + `forward drop` broke container egress), fail2ban, **chrony NTS**, unattended-upgrades (04:00 reboot), Docker Engine + compose.
- **Capture daemon LIVE** (`cli/capture/`): thin Kraken WS v2 client (book **depth-100** + trades, 10 EUR majors), per-pair L2 book with **CRC32 checksum validation** (verified vs Kraken's documented example `3310070434` + **3874 live checksums, zero mismatches**; parses with `Decimal` since the checksum depends on wire-format trailing zeros), hourly **zstd-Parquet + sha256 manifests**, gap accounting, disk watermark. Containerized (on-host build tonight; GHCR public-visibility deferred) and run via compose (`restart: unless-stopped` + docker-enabled → survives the 04:00 reboot): **19 % CPU / 74 MiB / 0 errors, all 10 pairs flowing**. The **≥7-day exit-bar clock started 2026-07-08 ~13:39 UTC**.
- **Liveness monitoring**: a healthchecks.io dead-man's-switch check provisioned + wired (`HEALTHCHECK_URL`, status **up**) → emails on a missed ping; status badge added to the README.
- **Secrets** (two-layer, self-contained public repo): sops+GPG-encrypted ansible-vault password; vault-encrypted deploy + pull-only SSH keys, hostname, and healthchecks key. The GPG key is passphrase-protected (no unattended decrypt), so tonight's runs used a transient vault password from a `0600` temp file (shredded at closeout); nothing plaintext is committed and the committed sops→GPG mechanism is what future runs use.
- **Built by three parallel subagents** (daemon / hardening roles / docker layer) + controller-orchestrated real-host execution with verify-before-tighten rails; the daemon self-validated on the real Kraken feed.
- **T0003 → `partial`** (index synced). Remainder (follow-ups): the workstation pull/NAS-sync + alerting drill, the full monitoring role, the ≥7-day clean run, GHCR public-visibility, the `ansible-lint` pre-commit hook, and REST trade-backfill. Spec/plan `docs/{specs,plans}/00027-t0003-capture-pipeline*`.

## 2026-07-08 — iter-039: T0004 tick-derived bar reconciliation + true VWAP (Phase 1)

- **Built `cli/tick/`** (spec/plan `00028`) — the tick→bar machinery, TDD, 25 synthetic-fixture tests: `read.py` (`read_trades_csv` — Kraken trades CSV from a path or a ZIP member, header/headerless auto-detect by sniffing whether line-1's first field is numeric; extra trailing columns ignored positionally; `TickError` on malformed/NaN, never a silent misparse), `aggregate.py` (`ticks_to_bars` — `group_by_dynamic` left-closed epoch-aligned OHLCV + `count` + **true tick-weighted `vwap` = Σ(price·volume)/Σ(volume)**), `reconcile.py` (`reconcile` — per-interval O/H/L/C match vs canonical OHLCVT, strict + loose bands, worst-mismatch surfacing; `KRAKEN_TICKER_MAP` + `csv_pair_to_canonical` `XBTEUR→(BTC,EUR)`). No CLI subcommand (a scratchpad run drives the sample, per spec).
- **Sample reconciliation cleared the exit bar at 100%** (`docs/research/02.phase1-tick-reconciliation-report.md`): all 10 EUR majors, Q1-2026, **~7.98 M ticks** → **100.000 % O/H/L/C match within a 1e-6 relative tolerance on every one of the 30 pair×interval cells (1h/4h/1d), at 100.0000 % coverage** (27,889 canonical bars, 0 not-covered — the AVAX 1h shortfall is real no-trade hours the canonical also omits, not a blind spot). Master-plan tolerance bar is ≥99.5 %.
- **Distrust-the-instrument:** 100 % OHLC match is the *expected* result (both paths derive from the same Kraken trades) and is provably not aliasing — the **VWAP genuinely differs** (true tick VWAP vs the stored close-weighted proxy: ~1 bp median for liquid pairs, tails to ~200 bps on the illiquid ones like AVAX), and the planted-mismatch unit test confirms `reconcile` returns <100 % on real mismatches. The VWAP gap is the "treatment engaged" evidence and a real finding — the tick VWAP is a material refinement over the proxy, most on the thin pairs.
- **Data-schema discovery** (corrects the spec's single-schema assumption): the quarterly ZIPs are 7-field-with-header (`Price,Volume,Timestamp,Type,OrderType,Misc,TradeID`); the **complete** dataset is headerless **3-field `Timestamp,Price,Volume`** (different order, no side). The reader handles the quarterly and raises cleanly on the 3-field layout — reading the complete dataset needs a second recognized schema (the full-history batch remainder).
- **Fix found via the sample run** (not the unit tests): `read_trades_csv` assumed the ZIP path was a `Path` (`.name`); passing a `str` `AttributeError`'d. Widened to accept `str | Path` (coerced) + added a regression test.
- **T0004 → `partial`** (index synced). Remainder: the 3-column complete-dataset reader → the full-universe/full-history reconciliation batch (→ then `resolved`); 15m bars + tick storage/catalog when Phase-4 microstructure needs them. Report is a top-level `docs/` doc (self-formatted, not on the mdformat allowlist). Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-042: complete-dataset tick reader + full-history BTC/EUR reconciliation (Phase 1, T0004)

- **`cli/tick/read.py` now reads the complete-dataset schema** — the full-history dump (`Kraken_Trading_History.zip:TimeAndSales_Combined/*.csv`) is headerless **3-field `Timestamp,Price,Volume`** (no side), vs the quarterly ZIPs' `Price,Volume,Timestamp,Type[,…]`. `_detect_schema` picks the layout by the first data row's field count, disambiguating a 3-field row by whether field 0 is a plausible Unix timestamp (`≥ 1e9`) — so a genuinely-short 4-field row (small first field) still errors rather than being silently reinterpreted. Complete-layout rows get `side` = null (unused by the O/H/L/C/VWAP math). TDD (2 new tests; the existing malformed-row tests stay green via the disambiguation).
- **Full-history BTC/EUR reconciliation** (`docs/research/02.phase1-tick-reconciliation-report.md`): 102.4 M ticks (2013-09-10 → 2025-12-31) → 106,626 hourly bars vs the canonical OHLCVT, **100.0000 % coverage**. Match rate **99.94 % within 1 %, 97.23 % within 10 bp**, but **77 % at 1e-6**.
- **Distrust-the-instrument — the strict miss is a real data property, not a bug.** Same code gives 100 % at 1e-6 on Q1-2026; coverage is 100 %; the **median close reldiff is 0.000 bps in every year** — the centre is exact. The ~23 % strict miss is sub-10-bp cross-source storage-precision noise (the historical OHLCVT reconstructed from 1-minute dumps vs the raw tick export carry different rounding). Only **68 hourly bars (0.064 %) genuinely diverge > 1 %**, isolated to 2013–2015 early-illiquid BTC (+ 1 lone 2024 bar) — flag-candidates only if early-2013 bars ever become load-bearing.
- **Exit-bar reading:** the ≥99.5 % tolerance test is met at a 1 % band (99.94 %) but is precision-noise-limited at 1e-6; the canonical dataset faithfully reproduces 12+ years of tick-derived bars to within ~10 bp. **T0004 stays `partial`** — the per-pair full-**universe** full-history batch (each ~GB member) remains the deferred remainder; this iteration proves the reader + a representative 12-year BTC/EUR run. Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-043: full-universe full-history tick reconciliation (Phase 1, T0004)

- **Ran the full-universe full-history reconciliation** — all 10 EUR majors, complete dataset, 2013→2025 (`docs/research/02.phase1-tick-reconciliation-report.md`): **660,343 hourly bars, 100.0000 % coverage** (0 not-covered). The iter-042 BTC finding **generalizes** — **9 of 10 pairs match ≥ 99.7 % within 1 %**; 1e-6 is precision-noise-limited (77–91 %); total **1088 bars (0.165 %) diverge > 1 %**, all in each pair's early-illiquid history. Doc-only iteration (no code — the iter-042 reader + reconcile handle everything); numbers verified from the reviewed reconcile path.
- **Distrust-the-instrument on the weakest pair.** LTC/EUR is the low outlier (99.37 % within 1 %, 611 > 1 % bars — 3–4× the next pair); rather than hand-wave it, verified the per-year distribution: **606 of 611 fall in 2013–2017** (220 in 2014, 211 in 2015), near-zero after 2018 — LTC was very thinly traded early, so its divergence is concentrated in sparse history while modern LTC reconciles near-exactly. Consistent with the universe-wide early-illiquid pattern, not an anomaly.
- **T0004 stays `partial` — reconciliation complete, one judgment left.** The heavy batch work is done and characterized; what remains is a **human exit-bar tolerance judgment** (accept the ~1 % band / ≥99.7 % for 9-of-10 with the characterized early-illiquid residual → flip to `resolved`, or tighten/flag the pre-2018 sparse years) — deliberately not autonomously resolved because it touches the Phase-1 exit-bar standard. The optional 15m-bars / tick-storage enhancement remains deferred to Phase-4 microstructure need. Engineering/data iteration — not logged in `.tmp/decisions.md`.

______________________________________________________________________

**Continuation — post-close Phase-1 backlog (capture-pipeline hardening), appended while Phase 6 is active.**

______________________________________________________________________

## 2026-07-14 — iter-095: capture writer crash-safety — T0036/T0037 + T0032/T0035 slices

- **T0036 (the headline): a restart silently truncated the hour it landed in.** `SegmentWriter` kept its part bookkeeping in process memory — a restarted process renumbered from `part0000`, overwrote the pre-restart parts, merged only its own list, and re-manifested the truncated final (hash-invisible). Measured on the live archive: **70 truncated pair-hours, ~1,748 pair-minutes of unbackfillable L2 destroyed** — every restart in the system's history. `GapMonitor` undercounted the damage ~50× (it scores WS downtime, not the clobbered hour). Fix (five adversarial rounds, 10 reproduced criticals, every one found by *execution*): parts written atomically (tmp+fsync+rename); `close()` flushes but never publishes; finalization commits via `<HH>.parquet.merging` → sidecar → unlink parts → rename, so recovery is mechanical and never guesses; anything unreadable is quarantined (`.corrupt`), never deleted; a single-instance `flock`; `<HH>.parquet` on disk now always means **committed and complete**.
- **T0037: hour rotation trusted Kraken's own `timestamp`** — one bogus stamp finalized the live hour early, and post-T0036 the truncation became permanent. Fix (judge panel over three executed designs): **cross-stream corroboration** — a boundary-crossing event is *held* (spilled to parts at `flush_rows`) until a second witness confirms the hour: another stream, or the 5-min-handicapped wall clock (permissive only, never a veto). Witnesses are **clamped at the wall clock** so an unconfirmed stamp can never corroborate (kills both poisoning shapes), the stream witness measures the clock's *rate* not value (a constant offset cancels — a lagging clock cannot darken a quiet pair), `MAX_TS_ABSURD` (1 day) rejects coherently-wrong streams, and never-confirmed held rows spill as `.held####.parquet` — quarantined, redeemable, unable to fabricate an hour. Two bounded residuals documented in the topic, ripe only if observed.
- **T0032 slices:** the disk-watermark breach window now books into `GapMonitor.gap_seconds` (a dedicated path — deliberately *not* `start_gap`, whose idempotency would let a concurrent desync gap swallow it); the watermark loop survives a backward clock step (clamp, never raise) and any probe exception; the per-pair `end_gap` got the same clamp. Remaining: retention design, probe-outage blind spot (registered).
- **T0035:** the production 2026-07-13 crash (Kraken WS restart → reconnect handshake 503 → `InvalidStatus` escaped `stream()`'s `ConnectionClosed`-only handler → daemon died) — reconnect now catches `WebSocketException`/`OSError`/`TimeoutError`, backs off identically, ERROR-logs every 10 consecutive failures; `CancelledError` still propagates.
- **Deploy migration validated by execution before touching production:** the runbook was adversarially rehearsed on a replica built with the *old* writer (10 defects found, incl. a wrong `SEG` that made verification a silent no-op and a clobbering `mv` that ate 5,000 rows); the corrected `migrate_stop_hour.py` then dry-ran against a 2.7 GB copy of the real production tree — 840k pre-stop rows preserved exactly, demoted=10 refused=0, idempotent.
- **Exit-bar instrumentation:** built and committed the segment-timestamp-continuity gap measurement (`infra/scripts/continuity.py` — per T0003 the daemon's counters can't see restart damage; this script is the exit-bar instrument and the T0036 post-deploy check, run against a pulled copy of the archive). Baseline on the real archive since 2026-07-09: worst stream **0.0890%** of the <0.1% bar — the two truncation events (07-11 reboot 83 s, 07-13 crash 270 s) consumed 86% of the budget; the old code's mid-hour deploy would have failed the bar outright.
- **New topics:** [[T0038]] (NAS mirror accumulates stale parts — 7,824 measured; a `**/*.parquet` glob double-counts, first consumer at risk is T0014) opened; T0036→partial (deploy remains), T0035→partial, T0037 core closed with documented residuals, T0032 updated.
- Suite: **1,395 passed** at branch tip; every fix TDD'd (failing test first); subagent-reviewed per commit.

______________________________________________________________________

**Continuation — post-close backlog entries begin here (the phase closed with its report; later data-foundation iterations keep appending below per `iterations-history.md`).**

______________________________________________________________________

## 2026-07-15 — iter-098: the L2 primitive panel (spec 00052, OPS-4)

- **The panel exists, verified, replicating, accruing hourly**: 1-second-grid wide primitives (spread/spread_bps/mid/microprice/imbalance, effective-spread-at-size for €{100,1k,10k}, depth at K∈{1,5,10}, updates/s) materialized from the canonical reconciled-first depth-100 capture — 1,740 hours × 10 pairs, zero errors, zero unanchored, manifests verified, NAS replica hash-verified (`checked=1740 ok=1740 failed=0`). First-look spread table (median bps): BTC 0.18 → DOT 5.33, economically ordered and coherent with iter-079's live observation. [[T0014]]'s calibration is now a one-query start.
- **A design-premise falsified by first contact with real data** (decision `[iter-098]` correction, phase-1 log): only ~4% of hours open with a snapshot (Kraken snapshots arrive on *subscribe*), invalidating the self-contained-hour design in both the panel AND the merged OPS-3 verifier. Fixed by **state-threading** (OrderBook state carried across contiguous hours; `<HH>.state.json` sidecars for O(1) watermark resume; "unanchored" as an honest-gap category) and **chain-anchored** verify semantics. A second critical caught in review: final-fractional-second messages must drain into the carried book or every successor starts stale (locked by a regression test).
- **Infra**: hourly ops-node timer pair (recurring NAS→ops pull — closing the replay-staleness gap — then watermarked materialize), `PANEL_*` NAS replication channel, dead-men `zcrypto-archive-pull`/`zcrypto-panel`, guards from review (dark-channel not-clean reporting, timer state tracking the var, `--allow-holes`, generation-mismatch refusal).
- **Catalog discipline**: `docs/reference/data-catalog-full.md` gains a "Live-accruing operational datasets" section (this panel + the T0023 liquidations buckets) so the research loop's pick-time surfaces and the dataset inventory cannot drift; codified as a closeout rule alongside this entry.

## 2026-07-16 — iter-099: the capture-pipeline exit bar — verified, and T0003 closed

- **The Phase-1 D2 exit bar is MET** (report: `docs/research/02.phase1-capture-exit-bar-report.md`). Measured over **175 h (7.29 days)** from **segment-timestamp continuity**, never the daemon's own counters (T0003's blind spot: `GapMonitor` is in-process, resets on restart, and scored the 07-13 crash at ~5.5 s against an actual 270 s): worst single stream **0.0624 %** vs the \<0.1 % bar, **zero missing hours** across all 10 pairs, VPS **3738 = NAS 3738** finals (zero segment loss), hashes **3738/3738** (NAS hourly) + **3718/3718** (independent ops `verify_tree`). With the alerting drill signed off the same day, every leg of the bar is discharged and [[T0003]] is **resolved + archived**.
- **A red guardrail investigated, not waved through.** The instrument reported 20 truncated hours where it says "MUST be 0 after the fix". Enumerating them showed exactly **two fleet-wide events** — 07-11 04:00 (83 s, the kernel reboot) and 07-13 07:00 (270 s, the WS-503 crash + clobber) — both pre-dating their fixes (iter-095) and **no truncation after 07-13**. Corroboration that the instrument measures reality: the two events sum to 353 s against a measured 361–393 s per pair, the 8–40 s remainder being intra-hour silence that is *larger on thinner pairs* exactly as market structure predicts; and iter-095's independent run recorded 0.0890 % for the *same* 353 s over a shorter denominator — the bar passed because nothing changed, not because something did.
- **Clock ruling** (owner-ratified, decision `[iter-099]`): the 07-13 clobber is a within-budget **gap**, not a corruption event, so it does not restart the clean-run clock — unlike the 07-08 desync, which produced checksum-**invalid** segments. Missing data is visible, bounded, and priced into the \<0.1 % budget; wrong data is none of those.
- **ansible-lint wired** as a `local` hook running the already-locked `uv run ansible-lint` (one version, tracked by dependabot's `uv` ecosystem; the upstream hook repo would add a second, drifting pin), scoped `^infra/`, config in `.ansible-lint`. `name[casing]` is **skipped by decision** (the repo's lowercase idiom; satisfying it would require renaming handlers plus every `notify:` in lockstep — a desync silently stops a handler firing). Infra now passes ansible-lint's **production** profile (was `min`).
- **The hook paid for itself on day one** — 4 real findings in the `engine` §8 gate: 3 × `command-instead-of-module` were false positives on read-only probes (per-task `noqa` with reasons, so genuine violations still fail), and 1 × `risky-shell-pipe` was a **silent-mask bug on the firewall gate guarding the live trade key** — `nft list ruleset | grep -q .` takes its rc from grep, so a failing `nft` that emitted output still passed the gate. **The obvious fix was wrong and was caught before shipping**: `set -o pipefail` makes `grep -q` exit early → `nft` dies of SIGPIPE → **rc 141 on a healthy host** (measured), which would have failed the gate closed and blocked every engine deploy. Fixed by dropping the pipe entirely (rc is then unambiguously nft's) and asserting non-emptiness explicitly.
- **`getent` deprecation fixed** — 4 bare `getent_passwd[...]` sites (capture ×2 and, unlisted in T0003, **ops ×2**) now read `ansible_facts['getent_passwd']`, matching the pattern already used in the engine role. All three roles verified in check mode against real hosts, each run reaching the changed task: ops `ok=15 changed=0 skipped=0`, capture-on-secondary `ok=17 changed=0`, engine §8 gate on the live primary `ok=30 changed=0 failed=0`. (Branch review caught the first cut citing `ok=9` — a run whose `when: ops_image_digest is defined` block had **skipped** the changed lines; ansible prints a skipped task's header too, so the header was not the proof it looked like.)
- **New topic:** [[T0050]] (REST trade-backfill — the last unshipped §8 daemon clause) split out of T0003 at its close, written self-contained because archived topics are never re-read; it is not an exit-bar blocker, and [[T0026]]'s snapshot-overwrite remedy needs the same REST `/Trades` machinery. The report also records honestly that the bar's "all hashes match" leg is strong for books but gives **false assurance for trade segments** (T0026: a reconnect snapshot can overwrite a finalized trade hour *and regenerate its manifest*).

## 2026-07-16 — iter-100: REST trade-backfill — a provably complete trade stream (spec 00053, T0050)

- **The trade stream was 3.60 % incomplete, and now it is provably complete.** Spec `00053` shipped `cli/trades/` + `zcrypto archive backfill-trades`: detect `trade_id` gaps from the archive itself, fetch only the missing ids from Kraken's public REST, union via the reconciler's existing `union_trades`, mint healed hours into the reconciled overlay. Verified on the live archive: **194 gaps / 17,362 missing / 10,986 duplicates → all zero**, 391/391 minted hours verify against their manifests, and the raw mirrors are **byte-identical** to their pre-mint baseline (1908 trade finals, aggregate sha `6093eede…`; books untouched at 1910). Worst-hit were the thin alts — AVAX 13.2 %, DOT 13.0 %, DOGE 9.8 % — precisely the names where each print carries the most information, and the pairs [[T0014]]/[[T0024]] calibrate against.
- **It rests on a property verified, not assumed.** Kraken's `trade_id` is **dense** per pair — probed across a liquid pair, a thin pair, the crash window, and 18-month-old data; every 1000-row page spans exactly 1000 ids. So a hole in the sequence IS missing data, provable with no network call. That gives trades what [[T0045]] denies books: **provable completeness AND a repair path**, because REST is an independent third witness. It closes the reconciler's own stated blind spot — *"when both streams are dark there is no witness to heal with, so the loss is permanent"* — and not academically: the largest gap (**974 BTC trades, 2026-07-08 18:59:57 → 19:50:42**) predates the secondary host entirely, so no mirror could ever have healed it.
- **Two corrections to the record.** [[T0003]]'s archived note said the 07-08 desync left "trades unaffected" — it did not; the two largest gaps in the whole archive fall inside that window, and nothing in the stack could see it (GapMonitor is in-process, the manifests verify because the rows are *absent* not corrupt, and no secondary existed to compare against). And [[T0026]]'s "net loss is PLAUSIBLE but unquantified" is now measured **and repaired** — that topic is resolved, its cause having been fixed independently by [[T0036]]'s committed-final invariant and its effect by this pass.
- **The spec was wrong twice, and measurement caught both.** D5 asserted a "nanoseconds vs seconds trap" that does not exist — and the conversion it *mandated* was itself an **infinite loop** against the live venue (1000 rows sharing one second yield an identical `since`), triggered precisely by the volatility bursts this feature heals → corrected to D5a (pass the cursor raw + a `trade_id`-progress guard). D6 demanded REST rows be "indistinguishable" from WS rows; the venue forbids it (REST's float runs ~+1 µs above the WS microsecond; ULP is 0.238 µs, so it is an offset not noise; truncation recovers WS 2/4, rounding 0/4) → corrected to D6a. Both residuals are registered, not buried.
- **Review found what tests could not.** Five rounds of mutation-tested review caught: the infinite loop; an assert mathematically incapable of firing (which I had insisted on keeping); **two tests that could not fail**; **two fabricated verification claims**; and — worst — an orchestration that **reported recovery it never performed** (rows for a wholly-missing hour were fetched, counted, and dropped, so the operator read `recovered=8, unrecoverable=0` over an unchanged archive). Root cause was the plan: spec **D9** mandates a post-mint invariant re-check and the plan implemented it zero times. Once added, D9 immediately caught a bug in the very fix that implemented it. Counters now derive from `union_trades`' own result — every fetched row lands in exactly one printed bucket (recovered / deferred / unrecoverable / cross-hour), and `--detect-only` reports what was **found** separately from what was **healed**.
- **Live-run findings**: Kraken rate-limited the client at its 1.5 s spacing (`EGeneral:Too many requests`) — one gap of 34 trades, isolated as designed and recovered exactly by the idempotent re-run, which proved the `replace=True` retry path on real data. Registered in [[T0053]] with the measured evidence.
- **New topics:** [[T0052]] (the metrics reach Grafana but nothing watches them — no panel, no alert, no dead-man), [[T0053]] (a permanent error means the stamp is never written, degrading the daily gate to hourly-forever; names the live trigger: `KRAKEN_ALTNAME` matches `capture_pairs` today but nothing ties them), [[T0054]] (reconcile's `already_minted` skip vs backfill-minted hours — check per-`kind` before the [[T0039]]-gated `--mint` flip, since a shadowed **book** heal is the one outcome that would cost unbackfillable data). [[T0051]] opened and resolved inside the PR (the Alloy keep-regex would have silently dropped the new metrics — not undashboarded, *nonexistent*). [[T0033]]'s OPS-5 now relocates the reconciler **and** this backfill as one unit (owner directive).
- Suite **1677 passed** (from 1641); every new test mutation-verified able to fail.

______________________________________________________________________

## 2026-07-16 — iter-101: OPS-5 Offload — the overlay writer moves to the ops node (spec/plan `00054`)

- **The move.** The reconciler and the trade-backfill relocated from the NAS's 4-core no-AVX Atom to the ops node's 24-thread i7, **as one unit** — they share the entrypoint, the `capture-reconciled` overlay and `union_trades`, so splitting them would put two writers on one tree with an rsync between them. The NAS kept custody, Role A's pull/prune, its own Alloy, and `gate-export` (D3/D6). **No `cli/` changes on the whole branch** — spec `00053` D4 had deliberately built the backfill where the reconciler already lived so this would be a pure relocation.
- **Motivating measurement:** the NAS's "hourly" loop had drifted to a **103-minute** period (≈43 min of compute per cycle) with `zcrypto_reconcile_source_lag_seconds = 4072`. The archive only grows, so the period grew with it.
- **The flow inverted.** Ops became the overlay's producer, so the NAS acquires it via a new `RECONCILED_SOURCE` channel of exactly the `PANEL_SOURCE` shape — **the NAS pulls, it never receives a push**, preserving spec `00051` D10's pull-only transport. Proven confined before use: the key lists the overlay, cannot escape the pinned root (`/etc/passwd` → `…/capture-reconciled/etc`), and cannot write (`sending to read-only server is not allowed`). Hash-verified with the default `--verify`, **not** the journal channel's `--no-verify`: `verify_tree` walks `*.parquet` only, so the unsidecar'd ledger rides along unchecked rather than failing the pull.
- **Observability landed FIRST (D1), and that ordering is a safety property.** The ops node had been writing four textfile metric families (`ops_archive_pull_*`, `ops_panel_*`, `ops_verify_replay_*`, `ops_verified_replay_*`) every hour **for weeks with nothing scraping them**. Alloy now ships them plus host metrics and container logs; the `zcrypto-ops` alert group watches them — five metric rules at this landing, **ten by branch end** once [[T0060]]'s log rules joined, all provisioned live and read back — `noDataState` split per rule (dead-man → `Alerting`, exit-code → `OK`). Active series **405** against the <1k budget, of which `node_cpu_seconds_total` alone is 192 (24 threads × 8 modes) — the trim candidate if it ever tightens.
- **Alloy runs as a dedicated non-key-owning uid.** `zcrypto-alloy` (uid 999), not `deploy` (1001) — verified live that it **cannot** read `/home/deploy/.ssh/sync_nas_archive` through the `/host/root:ro` mount. This mirrors the NAS's uid-1031 choice rather than merely resembling its shape; the first implementation ran as `deploy` and would have had a direct, no-escalation read of a live private key while the config comment claimed a "smaller blast radius". Spec D7's text, which caused that, was corrected in place. The docker-socket residual ([[T0042]]) is unchanged and stated as defence-in-depth only.
- **Vault re-scoping.** The six Alloy credentials had been mis-scoped to `group_vars/capture_host/` since spec `00043` — no capture host runs Alloy ([[T0020]]), so they sat in a group that never used them while the host that needed them could not see them. They moved to a new `observed` group (`capture_host` + `ops_host`), and `grafana_sa_token` + `slack_webhook_url` to `group_vars/all/` (owner's call — workstation tooling, read by `grafana-push.sh` by path, not via inventory). Ciphertext moved verbatim; nothing was decrypted. Verified by boolean probe: `zcrypto-ops` resolves the Alloy vars and resolves **neither** trade-key var. The ops secrets file is now rendered from the vault with `no_log` — the hand-placed out-of-band file is gone (that pattern exists only because the NAS is not Ansible-managed).
- **Verified by outcome (D10), not exit code.** Overlay byte-identical across hosts after a full cycle in the new direction (1175 files, list sha `0b684ce3bf0b3774`); **exactly one publisher** for each of `zcrypto_reconcile_*` / `zcrypto_trade_backfill_*` (the NAS's stale textfiles were deleted at cutover, or they would have frozen and paged forever from a host that no longer does the work — the rules read bare instant vectors with no instance aggregation); raw mirrors unharmed (`checked=3998 ok=3998 failed=0`, `1002/1002`); ops-side invariant `gaps=0 trades_missing=0 duplicate_rows_found=0 recovered=0 unrecoverable=0`; `residual_gap_total` **2662 unchanged** through the publisher change — the feared false permanent-loss page did not fire. Reconciler still **detect-only** ([[T0039]] untouched).
- **D9:** the ops verified-replay stopped replaying the whole journal daily — a cost that grew with the archive forever. As first landed it was scoped `--date yesterday` (UTC, host-side; yesterday, not today — the timer fires 05:23 UTC, when today's journal holds at most the 00:00/04:00 cycles); the multi-day-outage hole that scoping opened was registered as [[T0059]] and closed on this same branch by the watermark catch-up loop (continuation below).
- **Six defects were found by doing, not by review** — every one traceable to plan/spec text: Alloy running as the key-owning uid; `group_add: ["docker"]` by name (Docker resolves named groups against the *container's* `/etc/group`, and the Alloy image has only `alloy:x:473:`, so the container would never have started); an alert selecting `node_load1{host="ops"}` when no metric carried a host label at all (the label was on the Loki path only); the NAS's `load high` rule, which our own change would have made misfire against a 24-thread box (now `node_load1{host=""}` — an empty matcher selects series where the label is absent); `config.alloy` left in `templates/` after switching to `copy:`, which only ever searches `files/` (no syntax check can catch that — only a converge); and `"$SOURCE{{ ops_capture_subdir }}/"` rendering `$SOURCEcapture-segments`, an unbound variable that under `set -u` kills the script on its **first rsync** — invisible to the `bash -n` the plan mandated, and combined with the NAS strip in the same commit it would have been a fleet-wide Role C outage. The reconciler also could not have appended its ledger at all: the tree carried the NAS's uid 1000 while the container runs as ops's `deploy` 1001, and `ledger_append` opens the ledger in append mode **in detect-only mode too**.
- **New topics:** [[T0056]] (the NAS is the only non-Ansible host — `infra/nas/` changes are inert until hand-copied, and nothing detects the drift: the trade-backfill sat undeployed for days so its daily gate had **never run once**, and `compose.yaml`'s `:latest` placeholder destroyed the `-compat` digest pin when copied verbatim, killing the AVX-less Atom with `Illegal instruction` for ~90 s), [[T0057]] (the [[T0044]] runbook's `.bak` lives inside the replicated overlay: it broke the new channel on a permission mismatch and is the only surviving record that an append-only ledger was edited — it has 12 lines to the live ledger's 11, the missing one a LINK/EUR `total_loss`), [[T0058]] (ops is two hops from source, so `source_lag` **rose** 4072→6465s and the 3h alert margin shrank from ~1.9h to ~1.2h — D10 predicted a fall, conflating loop-period with mirror lag), [[T0059]] (`Persistent=true` + `--date yesterday` verifies one day after a multi-day outage and still reports healthy). [[T0044]]'s growth leg is relieved but the topic **stays open** on its correction-marker sub-item (D8 — relieving pressure is not fixing the defect).
- **Measured 2026-07-17:** the NAS's post-cutover loop settled at **~67 min** (from the 103-min before-picture — near its 60-min floor, so the loop-period win was real; `gate-export`'s ~6 min/cycle and T0028's O(archive) `verify_tree` stay on the NAS by D3). The attribution question the spec's risk note flagged resolved against the move's other promise: with the compute gone, `source_lag` kept **rising** (4072 → 6465 → 7928 s) — the residual term was the mirror hop itself, which drove the T0058 pivot below.
- **(2026-07-17) The log pipeline became usable ([[T0060]], stays partial).** The ops logs reached Loki but were ~99.9 % noise with dead parse stages: the parse scheme now keys on the compose service label first (real `level` labels live), the `docker run --rm` ephemeral jobs are named and dropped from Docker discovery, and the timer jobs' logs ship via the unit journal — which also carries the host scripts' own gate-decision lines no container log ever had. The `zcrypto-ops` alert group grew to **ten rules, provisioned live and read back**: ERROR logs, two green-when-blind canaries (`log pipeline dead`, `journal transport dead`, both noData=Alerting), and exit-code rules for all four timer jobs. The remainder — the poller stops re-submitting at source via a per-pair watermark (owner-ratified 2026-07-17) — is the next PR's spec, so the topic stays `partial`.
- **(2026-07-17) [[T0059]] resolved: the verified-replay catches up via a watermark.** The loop persists the last successfully-replayed date and replays every day from the watermark through yesterday, so `Persistent=true` is now harmless by construction — a missed timer's single catch-up run covers every missed day. Hardened against zero-byte, garbage, unpersisted-seed, empty-journal-day, and future-date states — **every failure mode execution-reproduced then fixed** (a zero-byte watermark had parsed as TOMORROW: a permanent silent-skip that fed the dead-man forever). Deployed to ops (converged); first organic timer run 2026-07-18 05:23 UTC.
- **(2026-07-17) The NAS came under Ansible ([[T0056]] resolved).** The owner ratified the model — quirks become code, not prompt-knowledge — and the maiden converge ran 2026-07-17: the TZ guard **PASSED on live UTC evidence** (the owner flipped DSM; the guard's fail-first behavior was proven in `--check` the day the TZ was still CEST), 8 changed including **both digest pins via the rendered `.env`** (the `:latest` traps dead in the deployed state, not just the repo), deployed entrypoint sha == repo sha, and the apply tasks ran (`up -d` + both restart tasks). `run.sh` loads the NAS deploy key into its throwaway agent (fixed after review). Drift detection is moot: hand-deploys no longer exist.
- **(2026-07-17) The in-custody `.bak` became committed evidence ([[T0057]] resolved).** `infra/nas/ledger-correction-20260714-link-eur.md` records the [[T0044]] correction; the `.bak` was sha-verified (`1da63f1cce679d6e`) on BOTH hosts, then removed from both — trees identical at **1174 files** after — and the runbook now writes its backups outside the replicated tree, so no future correction re-creates the failure shape.
- **(2026-07-17) The T0058 pivot: ops reads custody over NFS ([[T0058]] resolved).** The owner ratified four consensus points after the lag series and the final review's **two-hop gate blindness** (the cutover gate keyed on the NAS→ops rsync, a transport that succeeds against a frozen mirror — falsifying the entry above's "delays healing, does not produce wrong verdicts"): (1) the NAS exports `/volume1/ZhaoCrypto` **read-only** to ops (rule live, hand-added by the owner, recorded in `infra/external-systems.md`); (2) the 3-tree rsync retires — canonical trees read through the automounted `ro,nfsvers=3,nolock,soft,timeo=100,retrans=3,noatime,nosuid,nodev` mount (write-probe refused for user AND root; the write-back direction unchanged); (3) the reconcile gate consumes the NAS-written `.pull-status` (`capture_ok=1 secondary_ok=1` live) **through the mount**, fail-closed and octal-safe; (4) the writer cadence doubles (`:12`/`:42`, armed and firing).
- **(2026-07-17) The measured after-state.** Post-NFS `source_lag` sawtooths **6136/6746/7938 s** (1.7–2.2 h): the NAS-hop + finalization is now the ONLY term, so 2.2 h is a **hard phase bound**, not a drifting average — against the 3 h alert that is ≥ 0.8 h margin worst-phase, ~1.2–1.4 h typical (vs ~48 min worst at the rsync two-hop's 7928 s), and it is **structural** again per the owner's ruling that the custody hop is unavoidable ("we have distributed systems, therefore the hops are unavoidable"). The exporter stamps cycle START. NFS cycle cost 7 m 24 s–9 m 17 s per writer cycle — the `verify_manifest` re-hash of the 48 h window now rides the wire, recorded beside the [[T0028]] reference on [[T0058]]. `residual_gap` held **2661.8** throughout — no false verdicts, ever.
- **(2026-07-17) The old channel is retired for real.** The `sync_nas_archive` key pair + `nas_known_hosts` removed on ops, the rrsync `authorized_keys` entry removed on the NAS, and ansible re-verified with a ping AFTER the removal. The one un-run T0058 next step — the `/volume1` btrfs-vs-ext4 readdir probe — split to [[T0062]] rather than buried in the archive.
- **(2026-07-17) New topic [[T0061]]**: the live Kraken trade key is group-scoped to `capture_host`, so the capture-only secondary resolves it (only the engine play's targeting keeps it off `zcrypto-red`); the fix is a small ciphertext move but touches the live trading credential — human-gated, parked for go-live hardening ([[T0049]]).
- **(2026-07-17) What the review machinery earned this branch:** the final review found the two-hop gate blindness that drove the T0058 pivot, and the octal/leading-zero gate arithmetic that failed the gate OPEN (`ts_epoch=0999999999` read as invalid octal aborted the `if/elif` chain; every arithmetic site now forces base-10). Across five review waves, **~40 confirmed findings — every one either fixed or registered**, none dropped.
