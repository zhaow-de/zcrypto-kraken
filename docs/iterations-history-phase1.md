# Iterations history — Phase 1 (Data Foundation)

Per-iteration changelog for Phase 1. Appended at each iteration's close-out; see `.claude/rules/iterations-history.md`.

## 2026-07-07 — iter-004: OHLC ingestion → canonical Parquet (Phase 1 · v0)

- **Added `cli/ohlc/`** — a public-REST OHLC → canonical **polars** Parquet ingestion pipeline (the project's first research-core data dep, `polars>=1.42.1`): `fetch.py` (public `OHLC` GET, raises `OHLCError` at every failure boundary incl. a missing/`last`-only result and unparseable string decimals), `dataset.py` (`to_frame` — typed schema: UTC `ts`, Float64 OHLCV/vwap, Int64 count, sorted+deduped, raises on NaN/non-monotonic; `write/read_parquet`; deterministic `dataset_hash`), `ingest.py` (`ingest_basket` — writes `data/ohlc/{symbol}/{interval}.parquet` + `manifest.json`, returns a content-hashed manifest), `errors.py`.
- **Generated the v0 dataset** — 12 candidate symbols (resolved live via `cli.snapshot`) × 3 decision-cadence intervals (1d/4h/1h) = 36 series × 721 rows (Kraken's REST cap ≈ 2y daily / 120d 4h / 30d 1h), under gitignored `data/ohlc/`. Cataloged in `docs/reference/data-catalog.md` (symbol×interval → rows/span/sha256 + fetched_at).
- **18 unit tests** on a saved trimmed `OHLC` fixture + monkeypatched opener (no live-network tests): error-array + boundary raises, decimal-parse, sort/dedupe, NaN raise, Parquet round-trip, `dataset_hash` determinism + sensitivity, manifest persistence.
- **Parked (→ open-topic T0001):** full-history backfill from Kraken's downloadable OHLCVT ZIP archive — the ~2y REST window is insufficient for §9's 2019→2026 walk-forward regimes; this v0 dataset is what the backfill later extends. **Known minor:** `dataset_hash` stability depends on the polars version's float formatting — a polars bump may require regenerating the catalog hashes.
- Independent whole-branch review (Sonnet): initial verdict CHANGES REQUIRED (the catalog documented a `manifest.json` the pipeline didn't write; two error boundaries leaked non-`OHLCError` exceptions) — all fixed and re-review APPROVED, verified by execution.

## 2026-07-07 — iter-005: rule-driven universe finalization (Phase 1)

- **Added `cli/universe/`** — the §3 mechanical universe rule, the first iteration to *consume* two prior iterations' outputs (iter-002's margin/leverage snapshot + iter-004's daily OHLC): `volume.py` (`median_quote_volume` — 30d median of volume×vwap), `rules.py` (`finalize_universe` — margin ∧ leverage≥2× ∧ median-quote-volume≥floor; BTC/ETH-EUR mandatory; `escalate` on <8/>15 names), `build.py` (`build_universe_file`/`render_markdown` — the point-in-time file with per-symbol criteria + provenance + `spread_cap: pending-capture`), `errors.py`.
- **Generated the point-in-time universe file** `docs/universe/point-in-time-universe.md` — and its first real run **fired the §3 escalation**: at the (loop-chosen, tunable) €1M/day EUR-volume floor only **6 names** pass (BTC/ETH/SOL/XRP/ADA/LTC-EUR); LINK/DOGE/DOT/AVAX have genuinely thin Kraken-EUR liquidity (<€1M/day median), and the BTC-quoted RV legs (ETH/BTC, SOL/BTC) fail on a quote-currency unit mismatch (BTC-denominated volume vs a EUR floor). This divergence from §3's recommended 10–12 basket is a human decision → parked in **open-topic T0002** (calibrate the floor / route thin alts via deeper USD pairs / accept a smaller basket / make the volume floor quote-currency-aware).
- **13 unit tests** (synthetic inputs, no live network): median-volume + too-few-rows raise, leverage/volume-floor drops, mandatory-kept-though-failing + flagged, escalate boundaries, reasons populated, build determinism.
- **Deferred (non-blocking, → T0002):** relabel cross-quote volume reasons as "quote-currency mismatch — deferred"; quote-currency-aware / FX-normalized volume; the spread criterion still awaits the L2 capture daemon. A `MAX_NAMES`-boundary test is a noted test-coverage gap.
- Independent whole-branch review (Sonnet): APPROVED — rule engine correct + tested; the BTC-leg mismatch adjudicated as a narrow, non-silent limitation legitimately deferred to T0002 (escalate=True guarantees human review before production).

## 2026-07-07 — iter-006: OHLC dataset QA report (Phase 1)

- **Added `cli/ohlc/qa.py`** — read-only QA over the canonical OHLC Parquet dataset: `detect_gaps` (missing candles on the interval grid), `wick_outliers` ((high−low)/close > 0.20), `qa_series` (rows, coverage%, gaps, wick outliers, monotonic-ts, nonneg-volume), `qa_dataset` (walks the nested `{base}/{quote}/{interval}.parquet` tree — symbols contain `/`), `render_markdown`. Pure (`as_of` injected; no clock/network); stdlib + polars.
- **Generated the QA report** `docs/research/02.phase1-ohlc-qa-report.md` over the v0 dataset — **clean: 100% grid coverage, 0 gaps across all 36 series** (12 symbols × 1d/4h/1h); 87 wick-outlier flags, all on daily altcoin candles (a benign heuristic flag, not a failure).
- **10 unit tests** on synthetic frames (no live-dataset reads): planted gap → `missing==1`, planted extreme candle flagged, coverage math, monotonic/nonneg flags, whole-dataset walk + summary, `as_of` present/omitted.
- **Deferred (follow-up Phase-1 iterations):** empty-interval reconstruction (fill no-trade candles — the gaps this surfaces), Binance-Vision cross-venue reconciliation, the symbol & corporate-action ledger.
- Independent whole-branch review (Sonnet): APPROVED — verified by regenerating the report from the live dataset (byte-identical), confirming the nested-layout walk matches the `manifest.json` symbol convention, and checking every correctness detail (missing-candle count, coverage math, div-by-zero→inf-not-crash, empty-frame guards).

## 2026-07-07 — iter-007: universe floor calibration + quote-currency-aware volume (Phase 1 · T0002)

- **Resolved open-topic T0002** — iter-005's §3 escalation (only 6 of 12 names cleared the €1M/day EUR floor). Grounded in a live **EUR-vs-USD-vs-USDT** depth + margin-parity + FX/tax analysis (multi-agent workflow, adversarially verified): USD Kraken books are 2–8.5× deeper than EUR for all 10 bases, but that depth does **not** bind at ~$10k (a full per-name position is <1% of even the thinnest EUR book) and EUR gives up **zero** margin capability (EUR=USD exact `leverage_buy`/`leverage_sell` parity on all 10 bases), while USD adds conversion + continuous cash-leg EUR/USD risk + per-fill §23-EStG tax joins + cross-margin FX double-counting. USDT ruled out entirely (thinnest venue, MiCA-delisted for EEA, AVAX/USDT non-marginable, LTC/DOT-USDT capped at 3×).
- **`cli/universe/` changes** (commit `9495e2d`): lowered `DEFAULT_MIN_MEDIAN_QUOTE_VOLUME` €1M → **€150k**/day (footprint-based — a full max-size position ≈1% of median daily EUR volume, our microstructure-impact floor); added `quote_volume_in_eur(daily, *, fx_daily=None, window=30)`, which FX-normalizes a BTC-quoted leg's `volume × vwap` to EUR via the BTC/EUR daily close (inner-join on `ts`, then **sort before `.tail`** so the latest window is always selected — a join-ordering guard added in review, since polars does not guarantee join output order). 6 new tests (144 total).
- **Regenerated `docs/universe/point-in-time-universe.md`** — now **12 of 12** selected, `escalate=False`: the four previously-thin EUR alts (LINK/DOGE/DOT/AVAX) clear €150k, and the two BTC-quoted RV legs FX-normalize to €579,964 (ETH/BTC) and €233,595 (SOL/BTC), ending the unit mismatch — the full §3 target (10 EUR + 2 BTC RV). `data/universe/point-in-time-universe.json` regenerated (gitignored; new sha256 recorded in the doc's provenance).
- **Master-plan §3** — inserted a "Quote leg, liquidity depth, and the floor — measured (Phase 1)" subsection + a floor-decision paragraph: the measured EUR/USD/USDT depth, the margin parity, the friction rationale for EUR, the size-dependence (depth doesn't bind at our footprint), and USD-routing deferred as a pre-registered **scaling trigger**. Supersedes the earlier unquantified EUR-preference note.
- Independent review of the code commit (Sonnet): APPROVED — the FX-normalization correctness and the `.sort("ts")` join-ordering guard both verified (the reviewer reproduced the failure mode empirically).
- **Decisions logged** to `.tmp/decisions.md` (iter-007). **Open-topic sweep:** T0002 → resolved + archived; T0001 (OHLCVT backfill) and T0000 (residual Phase-2 cost-model fold-in) remain open.

## 2026-07-07 — iter-008: full-history OHLCVT backfill (Phase 1 · T0001)

- **Added `cli/backfill/`** — reconstructs canonical OHLC bars from Kraken's downloadable **OHLCVT 1-minute dumps** (on the NAS): `read.py` (`read_minute_rows` — merges the base dump + quarterly-update zips), `aggregate.py` (`aggregate_minutes` — 1-minute → 1h/4h/1d: OHLC first/max/min/last, volume+trades summed, **vwap = Σ close·vol / Σ vol**, a proxy since the dumps carry no vwap), `backfill.py` (`backfill_basket` — canonical Parquet + manifest, reusing `cli.ohlc.dataset`), `reconcile.py` (vs the v0 REST), `errors.py`; `ohlcvt_source_dir` added to `cli.config` / `zcrypto.toml`. **27 tests** on synthetic ZIP fixtures.
- **Generated the full-history dataset** — `data/ohlc-full/` (gitignored): the 12-name universe × 1h/4h/1d, **BTC/EUR daily 2013-09-10 → 2026-03-31** (4581 rows). The 1-minute bars are full-history (only *native* 4h is short, 2024+), so **all cadences are reconstructed from 1-minute**, giving full-history 1h/4h/1d + a real vwap. Cataloged in `docs/reference/data-catalog-full.md`.
- **Validated** (`docs/research/02.phase1-ohlcvt-backfill-reconciliation.md`) — reconstructed OHLC is **bit-identical to the v0 REST** (100% exact match over 623 daily + 137 4h overlap rows per pair); vwap proxy within ~0.05% of REST's true vwap. Honest caveats: daily volume within ~7% max on the worst bar (aggregation/revision artifact; 4h volume exact); 1h not independently reconciled (v0's REST 1h window post-dates the 2026-03-31 dump end). QA: 7807 no-trade gaps, 90.5% min coverage (expected for thin markets / early history).
- **Merge policy** — found by an instrument-check smoke test on the real archive: the base dump and 2023-2025 quarterly updates overlap and disagree on volume/trades (the base is more complete) → the base dump is authoritative for its range and quarterlies extend only past its last ts (Q1-2026, exactly contiguous).
- **Design/plan:** `docs/specs/00005-ohlcvt-backfill-design.md`, `docs/plans/00005-ohlcvt-backfill.md`. **Resolves open-topic T0001** (→ archived). **Deferred:** finer cadences (1m/5m/15m), full-exchange breadth, empty-interval reconstruction, Binance-Vision cross-check, the corporate-action ledger, and pointing the universe/backtests at the new dataset hash (a Phase-2 decision).
- Independent whole-branch review (Sonnet): APPROVED — after two review-driven hardening fixes (numeric de-dup + `BackfillError` at every boundary; the base-authoritative merge policy), both verified live; the 100% OHLC reconciliation vs REST corroborates the reconstruction.

## 2026-07-07 — iter-009: full-history dataset QA report (Phase 1)

- **Generated `docs/research/02.phase1-ohlc-full-qa-report.md`** — ran `cli.ohlc.qa` (iter-006) over the new full-history dataset `data/ohlc-full/` (iter-008): 36 series (12 pairs × 1h/4h/1d), integrity clean (monotonic ts + non-negative volume on every series). Coverage is high — the 7807 gaps are **no-trade intervals** (Kraken omits them; reported, not filled), concentrated in 1h for thinner/older pairs (worst: LTC/EUR/60 at 90.5%; most series 97–100%).
- **Gap characterization for §9** — per-year bar density: BTC/EUR 1h is **100% dense 2014–2025** (2013:18% partial Sept listing; 2026:25% data-to-March); DOT/EUR 1d 100% 2021–2025 (2020:37% partial listing). So the full-history intraday carries reliable density across the major walk-forward regimes (2014+); thin-alt early 1h is sparser — flagged for regime-aware validation.
- Manufactured package (the R&D open-topics backlog was empty after T0001/T0002 resolved) — "deepen data QA" per the loop's empty-backlog rule. No code change (reuses iter-006's `cli.ohlc.qa`); the report + gap characterization are the deliverable. **Deferred follow-ups** (empty-interval reconstruction, Binance-Vision cross-check, the symbol & corporate-action ledger) unchanged.

## 2026-07-07 — iter-010: symbol & corporate-action ledger + discontinuity audit (Phase 1)

- **Added `cli.ohlc.qa.price_discontinuities`** — flags bar-over-bar close moves beyond 3× (or below 1/3×): candidate corporate actions (splits, redenominations) or data errors. 5 tests (176 total).
- **Committed `docs/reference/symbol-corporate-action-ledger.md`** — the §3-required ledger for the 12-name universe: Kraken aliases (XBT=BTC, XDG=DOGE + the dump altnames `ETHXBT`/`SOLXBT`/…), the corporate-action check, and quote-book migrations (the USDT EEA delisting — not applicable to our EUR/BTC legs).
- **Discontinuity audit** over the full-history dataset (`data/ohlc-full/`, daily): **only 2 flags across all 12 symbols, both genuine market events, not corporate actions** — ETH/EUR 2015-08-08 (ratio 0.25, ETH's chaotic launch weeks) and DOGE/EUR 2021-01-28 (ratio 4.89, the Jan-2021 WSB/Elon pump). **DOT/EUR shows no discontinuity** — confirming DOT's Aug-2020 1:100 redenomination is *not* in our history (Kraken listed DOT post-redenomination). Conclusion: the full-history dataset is **clean of corporate-action price artifacts** for the universe; no price adjustments needed for backtesting.
- Manufactured Phase-1 package (§3 hard-dependency; R&D open-topics backlog empty). **Deferred data-foundation follow-ups** remaining: empty-interval reconstruction, Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED — the function + tests verified; the ledger's audit findings independently re-run (2 flags, DOT clean) and the alias table cross-checked against `cli.backfill` / `cli.snapshot`.

## 2026-07-08 — iter-011: empty-interval reconstruction — fill_gaps (Phase 1)

- **Added `cli.ohlc.reconstruct.fill_gaps`** — empty-interval reconstruction as an **opt-in on-read transform**: inserts a synthetic bar per missing grid point (OHLC = vwap = the prior forward-filled close; volume = 0, count = 0), returning a contiguous frame (`detect_gaps` → `[]` after). 6 tests (182 total).
- **Design (distrust-the-instrument):** the fill is a reusable utility, **not** baked into a canonical dataset — a synthetic zero-volume flat candle looks like a real bar and can bias features, so the fabrication stays explicit + opt-in at use time. A filled dataset variant is generatable on-demand if a strategy needs contiguous bars.
- Resolves the "empty-interval reconstruction" deferred data-foundation follow-up (iter-004/006). **Remaining Phase-1 follow-up:** the Binance-Vision cross-check.
- Independent whole-branch review (Sonnet): APPROVED.

## 2026-07-08 — iter-012: Binance cross-venue cross-check (Phase 1)

- **Added `cli/xcheck/`** — an independent-venue sanity check on the full-history dataset: `binance.py` (`binance_pair_name` maps the canonical `BASE/QUOTE` to Binance's concatenated symbol; `fetch_binance_klines` GETs daily klines from `data-api.binance.vision` via stdlib `urllib`; `binance_daily_closes` → `{ts, close}`; `crosscheck_series` reports overlap rows + close/return Pearson correlation + max abs relative diff; `crosscheck_dataset` walks the dataset root), `errors.py` (`XCheckError`). **19 tests** (201 total) on synthetic frames + mocked `urllib` (no live network in the suite).
- **Generated `docs/research/02.phase1-binance-crosscheck-report.md`** — Kraken vs Binance daily closes over the recent overlap window (902 rows/pair): **close correlation 1.0000 on every one of the 12 universe series**, return correlation ≥ 0.998, max abs relative diff ≤ 4.8% on the worst single bar (DOT/EUR 2025-10-10 — a venue-specific daily-close timing artifact, not a data error). The two venues corroborate each other; the full-history dataset is venue-consistent.
- **Scope (disclosed in the report + docstrings):** the cross-check covers the most recent **≤ 1000 daily candles** per pair — Binance's single-page kline limit. Binance EUR history reaches only ~2020 anyway; a full-overlap check via `startTime` pagination is a deferred follow-up.
- **Distrust-the-instrument:** the review independently verified the correlation metric is genuine — it returns −1.0 on anti-correlated inputs and ≈0.60 on independent random walks, so corr≈1.0 on the real data is a real finding, not a formula that always returns ~1.
- Resolves the last deferred Phase-1 data-foundation follow-up (the Binance-Vision cross-check). Manufactured Phase-1 package (R&D open-topics backlog empty).
- Independent whole-branch review (Sonnet): APPROVED — after review-driven fixes (mocked fetch-error-path tests, the recent-window scope caveat in report + docstrings, and skipping malformed symbols in `crosscheck_dataset`), all verified.

## 2026-07-08 — iter-038: T0003 D2 capture pipeline — built + deployed LIVE (Phase 1)

- **The Phase-1 exit-bar capture pipeline is live.** Designed interactively (spec/plan `00027`) then built unattended and deployed to the provisioned Linode `zcrypto.zhaow.me` (Debian 13, 2 vCPU / 4 GB / 80 GB). New tree `infra/ansible/` (portable IaC) + `cli/capture/` (the daemon) + `infra/docker/` + GHCR CI.
- **Host hardened** (`infra/ansible/`, executed against the real host, no self-lockout): `deploy` sudo user, SSH → **10022** (root + password off), dev-sec.io `os_hardening`+`ssh_hardening` (CIS-L1), **nftables** default-drop inbound (10022 + ICMP; manages only its own table so Docker NAT survives — a bug caught + fixed when the first ruleset's `flush ruleset` + `forward drop` broke container egress), fail2ban, **chrony NTS**, unattended-upgrades (04:00 reboot), Docker Engine + compose.
- **Capture daemon LIVE** (`cli/capture/`): thin Kraken WS v2 client (book **depth-100** + trades, 10 EUR majors), per-pair L2 book with **CRC32 checksum validation** (verified vs Kraken's documented example `3310070434` + **3874 live checksums, zero mismatches**; parses with `Decimal` since the checksum depends on wire-format trailing zeros), hourly **zstd-Parquet + sha256 manifests**, gap accounting, disk watermark. Containerized (on-host build tonight; GHCR public-visibility deferred) and run via compose (`restart: unless-stopped` + docker-enabled → survives the 04:00 reboot): **19 % CPU / 74 MiB / 0 errors, all 10 pairs flowing**. The **≥7-day exit-bar clock started 2026-07-08 ~13:39 UTC**.
- **Liveness monitoring**: a healthchecks.io dead-man's-switch check provisioned + wired (`HEALTHCHECK_URL`, status **up**) → emails on a missed ping; status badge added to the README.
- **Secrets** (two-layer, self-contained public repo): sops+GPG-encrypted ansible-vault password; vault-encrypted deploy + pull-only SSH keys, hostname, and healthchecks key. The GPG key is passphrase-protected (no unattended decrypt), so tonight's runs used a transient vault password from a `0600` temp file (shredded at closeout); nothing plaintext is committed and the committed sops→GPG mechanism is what future runs use.
- **Built by three parallel subagents** (daemon / hardening roles / docker layer) + controller-orchestrated real-host execution with verify-before-tighten rails; the daemon self-validated on the real Kraken feed.
- **T0003 → `partial`** (index synced). Remainder (follow-ups): the workstation pull/NAS-sync + alerting drill, the full monitoring role, the ≥7-day clean run, GHCR public-visibility, the `ansible-lint` pre-commit hook, and REST trade-backfill. Spec/plan `docs/{specs,plans}/00027-t0003-capture-pipeline*`.

## 2026-07-08 — iter-039: T0004 tick-derived bar reconciliation + true VWAP (Phase 1)

- **Built `cli/tick/`** (spec/plan `00028`) — the tick→bar machinery, TDD, 25 synthetic-fixture tests: `read.py` (`read_trades_csv` — Kraken trades CSV from a path or a ZIP member, header/headerless auto-detect by sniffing whether line-1's first field is numeric; extra trailing columns ignored positionally; `TickError` on malformed/NaN, never a silent misparse), `aggregate.py` (`ticks_to_bars` — `group_by_dynamic` left-closed epoch-aligned OHLCV + `count` + **true tick-weighted `vwap` = Σ(price·volume)/Σ(volume)**), `reconcile.py` (`reconcile` — per-interval O/H/L/C match vs canonical OHLCVT, strict + loose bands, worst-mismatch surfacing; `KRAKEN_TICKER_MAP` + `csv_pair_to_canonical` `XBTEUR→(BTC,EUR)`). No CLI subcommand (a scratchpad run drives the sample, per spec).
- **Sample reconciliation cleared the exit bar at 100%** (`docs/research/02.phase1-tick-reconciliation-report.md`): all 10 EUR majors, Q1-2026, **~7.98 M ticks** → **100.000 % O/H/L/C match within a 1e-6 relative tolerance on every one of the 30 pair×interval cells (1h/4h/1d), at 100.0000 % coverage** (27,889 canonical bars, 0 not-covered — the AVAX 1h shortfall is real no-trade hours the canonical also omits, not a blind spot). Master-plan tolerance bar is ≥99.5 %.
- **Distrust-the-instrument:** 100 % OHLC match is the *expected* result (both paths derive from the same Kraken trades) and is provably not aliasing — the **VWAP genuinely differs** (true tick VWAP vs the stored close-weighted proxy: ~1 bp median for liquid pairs, tails to ~200 bps on the illiquid ones like AVAX), and the planted-mismatch unit test confirms `reconcile` returns <100 % on real mismatches. The VWAP gap is the "treatment engaged" evidence and a real finding — the tick VWAP is a material refinement over the proxy, most on the thin pairs.
- **Data-schema discovery** (corrects the spec's single-schema assumption): the quarterly ZIPs are 7-field-with-header (`Price,Volume,Timestamp,Type,OrderType,Misc,TradeID`); the **complete** dataset is headerless **3-field `Timestamp,Price,Volume`** (different order, no side). The reader handles the quarterly and raises cleanly on the 3-field layout — reading the complete dataset needs a second recognized schema (the full-history batch remainder).
- **Fix found via the sample run** (not the unit tests): `read_trades_csv` assumed the ZIP path was a `Path` (`.name`); passing a `str` `AttributeError`'d. Widened to accept `str | Path` (coerced) + added a regression test.
- **T0004 → `partial`** (index synced). Remainder: the 3-column complete-dataset reader → the full-universe/full-history reconciliation batch (→ then `resolved`); 15m bars + tick storage/catalog when Phase-4 microstructure needs them. Report is a top-level `docs/` doc (self-formatted, not on the mdformat allowlist). Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-042: complete-dataset tick reader + full-history BTC/EUR reconciliation (Phase 1, T0004)

- **`cli/tick/read.py` now reads the complete-dataset schema** — the full-history dump (`Kraken_Trading_History.zip:TimeAndSales_Combined/*.csv`) is headerless **3-field `Timestamp,Price,Volume`** (no side), vs the quarterly ZIPs' `Price,Volume,Timestamp,Type[,…]`. `_detect_schema` picks the layout by the first data row's field count, disambiguating a 3-field row by whether field 0 is a plausible Unix timestamp (`≥ 1e9`) — so a genuinely-short 4-field row (small first field) still errors rather than being silently reinterpreted. Complete-layout rows get `side` = null (unused by the O/H/L/C/VWAP math). TDD (2 new tests; the existing malformed-row tests stay green via the disambiguation).
- **Full-history BTC/EUR reconciliation** (`docs/research/02.phase1-tick-reconciliation-report.md`): 102.4 M ticks (2013-09-10 → 2025-12-31) → 106,626 hourly bars vs the canonical OHLCVT, **100.0000 % coverage**. Match rate **99.94 % within 1 %, 97.23 % within 10 bp**, but **77 % at 1e-6**.
- **Distrust-the-instrument — the strict miss is a real data property, not a bug.** Same code gives 100 % at 1e-6 on Q1-2026; coverage is 100 %; the **median close reldiff is 0.000 bps in every year** — the centre is exact. The ~23 % strict miss is sub-10-bp cross-source storage-precision noise (the historical OHLCVT reconstructed from 1-minute dumps vs the raw tick export carry different rounding). Only **68 hourly bars (0.064 %) genuinely diverge > 1 %**, isolated to 2013–2015 early-illiquid BTC (+ 1 lone 2024 bar) — flag-candidates only if early-2013 bars ever become load-bearing.
- **Exit-bar reading:** the ≥99.5 % tolerance test is met at a 1 % band (99.94 %) but is precision-noise-limited at 1e-6; the canonical dataset faithfully reproduces 12+ years of tick-derived bars to within ~10 bp. **T0004 stays `partial`** — the per-pair full-**universe** full-history batch (each ~GB member) remains the deferred remainder; this iteration proves the reader + a representative 12-year BTC/EUR run. Engineering/data iteration — not logged in `.tmp/decisions.md`.

## 2026-07-08 — iter-043: full-universe full-history tick reconciliation (Phase 1, T0004)

- **Ran the full-universe full-history reconciliation** — all 10 EUR majors, complete dataset, 2013→2025 (`docs/research/02.phase1-tick-reconciliation-report.md`): **660,343 hourly bars, 100.0000 % coverage** (0 not-covered). The iter-042 BTC finding **generalizes** — **9 of 10 pairs match ≥ 99.7 % within 1 %**; 1e-6 is precision-noise-limited (77–91 %); total **1088 bars (0.165 %) diverge > 1 %**, all in each pair's early-illiquid history. Doc-only iteration (no code — the iter-042 reader + reconcile handle everything); numbers verified from the reviewed reconcile path.
- **Distrust-the-instrument on the weakest pair.** LTC/EUR is the low outlier (99.37 % within 1 %, 611 > 1 % bars — 3–4× the next pair); rather than hand-wave it, verified the per-year distribution: **606 of 611 fall in 2013–2017** (220 in 2014, 211 in 2015), near-zero after 2018 — LTC was very thinly traded early, so its divergence is concentrated in sparse history while modern LTC reconciles near-exactly. Consistent with the universe-wide early-illiquid pattern, not an anomaly.
- **T0004 stays `partial` — reconciliation complete, one judgment left.** The heavy batch work is done and characterized; what remains is a **human exit-bar tolerance judgment** (accept the ~1 % band / ≥99.7 % for 9-of-10 with the characterized early-illiquid residual → flip to `resolved`, or tighten/flag the pre-2018 sparse years) — deliberately not autonomously resolved because it touches the Phase-1 exit-bar standard. The optional 15m-bars / tick-storage enhancement remains deferred to Phase-4 microstructure need. Engineering/data iteration — not logged in `.tmp/decisions.md`.

______________________________________________________________________

**Continuation — post-close Phase-1 backlog (capture-pipeline hardening), appended while Phase 6 is active.**

______________________________________________________________________

## 2026-07-14 — iter-095: capture writer crash-safety — T0036/T0037 + T0032/T0035 slices

- **T0036 (the headline): a restart silently truncated the hour it landed in.** `SegmentWriter` kept its part bookkeeping in process memory — a restarted process renumbered from `part0000`, overwrote the pre-restart parts, merged only its own list, and re-manifested the truncated final (hash-invisible). Measured on the live archive: **70 truncated pair-hours, ~1,748 pair-minutes of unbackfillable L2 destroyed** — every restart in the system's history. `GapMonitor` undercounted the damage ~50× (it scores WS downtime, not the clobbered hour). Fix (five adversarial rounds, 10 reproduced criticals, every one found by *execution*): parts written atomically (tmp+fsync+rename); `close()` flushes but never publishes; finalization commits via `<HH>.parquet.merging` → sidecar → unlink parts → rename, so recovery is mechanical and never guesses; anything unreadable is quarantined (`.corrupt`), never deleted; a single-instance `flock`; `<HH>.parquet` on disk now always means **committed and complete**.
- **T0037: hour rotation trusted Kraken's own `timestamp`** — one bogus stamp finalized the live hour early, and post-T0036 the truncation became permanent. Fix (judge panel over three executed designs): **cross-stream corroboration** — a boundary-crossing event is *held* (spilled to parts at `flush_rows`) until a second witness confirms the hour: another stream, or the 5-min-handicapped wall clock (permissive only, never a veto). Witnesses are **clamped at the wall clock** so an unconfirmed stamp can never corroborate (kills both poisoning shapes), the stream witness measures the clock's *rate* not value (a constant offset cancels — a lagging clock cannot darken a quiet pair), `MAX_TS_ABSURD` (1 day) rejects coherently-wrong streams, and never-confirmed held rows spill as `.held####.parquet` — quarantined, redeemable, unable to fabricate an hour. Two bounded residuals documented in the topic, ripe only if observed.
- **T0032 slices:** the disk-watermark breach window now books into `GapMonitor.gap_seconds` (a dedicated path — deliberately *not* `start_gap`, whose idempotency would let a concurrent desync gap swallow it); the watermark loop survives a backward clock step (clamp, never raise) and any probe exception; the per-pair `end_gap` got the same clamp. Remaining: retention design, probe-outage blind spot (registered).
- **T0035:** the production 2026-07-13 crash (Kraken WS restart → reconnect handshake 503 → `InvalidStatus` escaped `stream()`'s `ConnectionClosed`-only handler → daemon died) — reconnect now catches `WebSocketException`/`OSError`/`TimeoutError`, backs off identically, ERROR-logs every 10 consecutive failures; `CancelledError` still propagates.
- **Deploy migration validated by execution before touching production:** the runbook was adversarially rehearsed on a replica built with the *old* writer (10 defects found, incl. a wrong `SEG` that made verification a silent no-op and a clobbering `mv` that ate 5,000 rows); the corrected `migrate_stop_hour.py` then dry-ran against a 2.7 GB copy of the real production tree — 840k pre-stop rows preserved exactly, demoted=10 refused=0, idempotent.
- **Exit-bar instrumentation:** built and committed the segment-timestamp-continuity gap measurement (`infra/scripts/continuity.py` — per T0003 the daemon's counters can't see restart damage; this script is the exit-bar instrument and the T0036 post-deploy check, run against a pulled copy of the archive). Baseline on the real archive since 2026-07-09: worst stream **0.0890%** of the <0.1% bar — the two truncation events (07-11 reboot 83 s, 07-13 crash 270 s) consumed 86% of the budget; the old code's mid-hour deploy would have failed the bar outright.
- **New topics:** [[T0038]] (NAS mirror accumulates stale parts — 7,824 measured; a `**/*.parquet` glob double-counts, first consumer at risk is T0014) opened; T0036→partial (deploy remains), T0035→partial, T0037 core closed with documented residuals, T0032 updated.
- Suite: **1,395 passed** at branch tip; every fix TDD'd (failing test first); subagent-reviewed per commit.

______________________________________________________________________

**Continuation — post-close backlog entries begin here (the phase closed with its report; later data-foundation iterations keep appending below per `iterations-history.md`).**

______________________________________________________________________

## 2026-07-15 — iter-098: the L2 primitive panel (spec 00052, OPS-4)

- **The panel exists, verified, replicating, accruing hourly**: 1-second-grid wide primitives (spread/spread_bps/mid/microprice/imbalance, effective-spread-at-size for €{100,1k,10k}, depth at K∈{1,5,10}, updates/s) materialized from the canonical reconciled-first depth-100 capture — 1,740 hours × 10 pairs, zero errors, zero unanchored, manifests verified, NAS replica hash-verified (`checked=1740 ok=1740 failed=0`). First-look spread table (median bps): BTC 0.18 → DOT 5.33, economically ordered and coherent with iter-079's live observation. [[T0014]]'s calibration is now a one-query start.
- **A design-premise falsified by first contact with real data** (decision `[iter-098]` correction, phase-1 log): only ~4% of hours open with a snapshot (Kraken snapshots arrive on *subscribe*), invalidating the self-contained-hour design in both the panel AND the merged OPS-3 verifier. Fixed by **state-threading** (OrderBook state carried across contiguous hours; `<HH>.state.json` sidecars for O(1) watermark resume; "unanchored" as an honest-gap category) and **chain-anchored** verify semantics. A second critical caught in review: final-fractional-second messages must drain into the carried book or every successor starts stale (locked by a regression test).
- **Infra**: hourly ops-node timer pair (recurring NAS→ops pull — closing the replay-staleness gap — then watermarked materialize), `PANEL_*` NAS replication channel, dead-men `zcrypto-archive-pull`/`zcrypto-panel`, guards from review (dark-channel not-clean reporting, timer state tracking the var, `--allow-holes`, generation-mismatch refusal).
- **Catalog discipline**: `docs/reference/data-catalog-full.md` gains a "Live-accruing operational datasets" section (this panel + the T0023 liquidations buckets) so the research loop's pick-time surfaces and the dataset inventory cannot drift; codified as a closeout rule alongside this entry.

## 2026-07-16 — iter-099: the capture-pipeline exit bar — verified, and T0003 closed

- **The Phase-1 D2 exit bar is MET** (report: `docs/research/02.phase1-capture-exit-bar-report.md`). Measured over **175 h (7.29 days)** from **segment-timestamp continuity**, never the daemon's own counters (T0003's blind spot: `GapMonitor` is in-process, resets on restart, and scored the 07-13 crash at ~5.5 s against an actual 270 s): worst single stream **0.0624 %** vs the \<0.1 % bar, **zero missing hours** across all 10 pairs, VPS **3738 = NAS 3738** finals (zero segment loss), hashes **3738/3738** (NAS hourly) + **3718/3718** (independent ops `verify_tree`). With the alerting drill signed off the same day, every leg of the bar is discharged and [[T0003]] is **resolved + archived**.
- **A red guardrail investigated, not waved through.** The instrument reported 20 truncated hours where it says "MUST be 0 after the fix". Enumerating them showed exactly **two fleet-wide events** — 07-11 04:00 (83 s, the kernel reboot) and 07-13 07:00 (270 s, the WS-503 crash + clobber) — both pre-dating their fixes (iter-095) and **no truncation after 07-13**. Corroboration that the instrument measures reality: the two events sum to 353 s against a measured 361–393 s per pair, the 8–40 s remainder being intra-hour silence that is *larger on thinner pairs* exactly as market structure predicts; and iter-095's independent run recorded 0.0890 % for the *same* 353 s over a shorter denominator — the bar passed because nothing changed, not because something did.
- **Clock ruling** (owner-ratified, decision `[iter-099]`): the 07-13 clobber is a within-budget **gap**, not a corruption event, so it does not restart the clean-run clock — unlike the 07-08 desync, which produced checksum-**invalid** segments. Missing data is visible, bounded, and priced into the \<0.1 % budget; wrong data is none of those.
- **ansible-lint wired** as a `local` hook running the already-locked `uv run ansible-lint` (one version, tracked by dependabot's `uv` ecosystem; the upstream hook repo would add a second, drifting pin), scoped `^infra/`, config in `.ansible-lint`. `name[casing]` is **skipped by decision** (the repo's lowercase idiom; satisfying it would require renaming handlers plus every `notify:` in lockstep — a desync silently stops a handler firing). Infra now passes ansible-lint's **production** profile (was `min`).
- **The hook paid for itself on day one** — 4 real findings in the `engine` §8 gate: 3 × `command-instead-of-module` were false positives on read-only probes (per-task `noqa` with reasons, so genuine violations still fail), and 1 × `risky-shell-pipe` was a **silent-mask bug on the firewall gate guarding the live trade key** — `nft list ruleset | grep -q .` takes its rc from grep, so a failing `nft` that emitted output still passed the gate. **The obvious fix was wrong and was caught before shipping**: `set -o pipefail` makes `grep -q` exit early → `nft` dies of SIGPIPE → **rc 141 on a healthy host** (measured), which would have failed the gate closed and blocked every engine deploy. Fixed by dropping the pipe entirely (rc is then unambiguously nft's) and asserting non-emptiness explicitly.
- **`getent` deprecation fixed** — 4 bare `getent_passwd[...]` sites (capture ×2 and, unlisted in T0003, **ops ×2**) now read `ansible_facts['getent_passwd']`, matching the pattern already used in the engine role. All three roles verified in check mode against real hosts, each run reaching the changed task: ops `ok=15 changed=0 skipped=0`, capture-on-secondary `ok=17 changed=0`, engine §8 gate on the live primary `ok=30 changed=0 failed=0`. (Branch review caught the first cut citing `ok=9` — a run whose `when: ops_image_digest is defined` block had **skipped** the changed lines; ansible prints a skipped task's header too, so the header was not the proof it looked like.)
- **New topic:** [[T0050]] (REST trade-backfill — the last unshipped §8 daemon clause) split out of T0003 at its close, written self-contained because archived topics are never re-read; it is not an exit-bar blocker, and [[T0026]]'s snapshot-overwrite remedy needs the same REST `/Trades` machinery. The report also records honestly that the bar's "all hashes match" leg is strong for books but gives **false assurance for trade segments** (T0026: a reconnect snapshot can overwrite a finalized trade hour *and regenerate its manifest*).

## 2026-07-16 — iter-100: REST trade-backfill — a provably complete trade stream (spec 00053, T0050)

- **The trade stream was 3.60 % incomplete, and now it is provably complete.** Spec `00053` shipped `cli/trades/` + `zcrypto archive backfill-trades`: detect `trade_id` gaps from the archive itself, fetch only the missing ids from Kraken's public REST, union via the reconciler's existing `union_trades`, mint healed hours into the reconciled overlay. Verified on the live archive: **194 gaps / 17,362 missing / 10,986 duplicates → all zero**, 391/391 minted hours verify against their manifests, and the raw mirrors are **byte-identical** to their pre-mint baseline (1908 trade finals, aggregate sha `6093eede…`; books untouched at 1910). Worst-hit were the thin alts — AVAX 13.2 %, DOT 13.0 %, DOGE 9.8 % — precisely the names where each print carries the most information, and the pairs [[T0014]]/[[T0024]] calibrate against.
- **It rests on a property verified, not assumed.** Kraken's `trade_id` is **dense** per pair — probed across a liquid pair, a thin pair, the crash window, and 18-month-old data; every 1000-row page spans exactly 1000 ids. So a hole in the sequence IS missing data, provable with no network call. That gives trades what [[T0045]] denies books: **provable completeness AND a repair path**, because REST is an independent third witness. It closes the reconciler's own stated blind spot — *"when both streams are dark there is no witness to heal with, so the loss is permanent"* — and not academically: the largest gap (**974 BTC trades, 2026-07-08 18:59:57 → 19:50:42**) predates the secondary host entirely, so no mirror could ever have healed it.
- **Two corrections to the record.** [[T0003]]'s archived note said the 07-08 desync left "trades unaffected" — it did not; the two largest gaps in the whole archive fall inside that window, and nothing in the stack could see it (GapMonitor is in-process, the manifests verify because the rows are *absent* not corrupt, and no secondary existed to compare against). And [[T0026]]'s "net loss is PLAUSIBLE but unquantified" is now measured **and repaired** — that topic is resolved, its cause having been fixed independently by [[T0036]]'s committed-final invariant and its effect by this pass.
- **The spec was wrong twice, and measurement caught both.** D5 asserted a "nanoseconds vs seconds trap" that does not exist — and the conversion it *mandated* was itself an **infinite loop** against the live venue (1000 rows sharing one second yield an identical `since`), triggered precisely by the volatility bursts this feature heals → corrected to D5a (pass the cursor raw + a `trade_id`-progress guard). D6 demanded REST rows be "indistinguishable" from WS rows; the venue forbids it (REST's float runs ~+1 µs above the WS microsecond; ULP is 0.238 µs, so it is an offset not noise; truncation recovers WS 2/4, rounding 0/4) → corrected to D6a. Both residuals are registered, not buried.
- **Review found what tests could not.** Five rounds of mutation-tested review caught: the infinite loop; an assert mathematically incapable of firing (which I had insisted on keeping); **two tests that could not fail**; **two fabricated verification claims**; and — worst — an orchestration that **reported recovery it never performed** (rows for a wholly-missing hour were fetched, counted, and dropped, so the operator read `recovered=8, unrecoverable=0` over an unchanged archive). Root cause was the plan: spec **D9** mandates a post-mint invariant re-check and the plan implemented it zero times. Once added, D9 immediately caught a bug in the very fix that implemented it. Counters now derive from `union_trades`' own result — every fetched row lands in exactly one printed bucket (recovered / deferred / unrecoverable / cross-hour), and `--detect-only` reports what was **found** separately from what was **healed**.
- **Live-run findings**: Kraken rate-limited the client at its 1.5 s spacing (`EGeneral:Too many requests`) — one gap of 34 trades, isolated as designed and recovered exactly by the idempotent re-run, which proved the `replace=True` retry path on real data. Registered in [[T0053]] with the measured evidence.
- **New topics:** [[T0052]] (the metrics reach Grafana but nothing watches them — no panel, no alert, no dead-man), [[T0053]] (a permanent error means the stamp is never written, degrading the daily gate to hourly-forever; names the live trigger: `KRAKEN_ALTNAME` matches `capture_pairs` today but nothing ties them), [[T0054]] (reconcile's `already_minted` skip vs backfill-minted hours — check per-`kind` before the [[T0039]]-gated `--mint` flip, since a shadowed **book** heal is the one outcome that would cost unbackfillable data). [[T0051]] opened and resolved inside the PR (the Alloy keep-regex would have silently dropped the new metrics — not undashboarded, *nonexistent*). [[T0033]]'s OPS-5 now relocates the reconciler **and** this backfill as one unit (owner directive).
- Suite **1677 passed** (from 1641); every new test mutation-verified able to fail.

______________________________________________________________________

## 2026-07-16 — iter-101: OPS-5 Offload — the overlay writer moves to the ops node (spec/plan `00054`)

- **The move.** The reconciler and the trade-backfill relocated from the NAS's 4-core no-AVX Atom to the ops node's 24-thread i7, **as one unit** — they share the entrypoint, the `capture-reconciled` overlay and `union_trades`, so splitting them would put two writers on one tree with an rsync between them. The NAS kept custody, Role A's pull/prune, its own Alloy, and `gate-export` (D3/D6). **No `cli/` changes on the whole branch** — spec `00053` D4 had deliberately built the backfill where the reconciler already lived so this would be a pure relocation.
- **Motivating measurement:** the NAS's "hourly" loop had drifted to a **103-minute** period (≈43 min of compute per cycle) with `zcrypto_reconcile_source_lag_seconds = 4072`. The archive only grows, so the period grew with it.
- **The flow inverted.** Ops became the overlay's producer, so the NAS acquires it via a new `RECONCILED_SOURCE` channel of exactly the `PANEL_SOURCE` shape — **the NAS pulls, it never receives a push**, preserving spec `00051` D10's pull-only transport. Proven confined before use: the key lists the overlay, cannot escape the pinned root (`/etc/passwd` → `…/capture-reconciled/etc`), and cannot write (`sending to read-only server is not allowed`). Hash-verified with the default `--verify`, **not** the journal channel's `--no-verify`: `verify_tree` walks `*.parquet` only, so the unsidecar'd ledger rides along unchecked rather than failing the pull.
- **Observability landed FIRST (D1), and that ordering is a safety property.** The ops node had been writing four textfile metric families (`ops_archive_pull_*`, `ops_panel_*`, `ops_verify_replay_*`, `ops_verified_replay_*`) every hour **for weeks with nothing scraping them**. Alloy now ships them plus host metrics and container logs; the `zcrypto-ops` alert group watches them — five metric rules at this landing, **ten by branch end** once [[T0060]]'s log rules joined, all provisioned live and read back — `noDataState` split per rule (dead-man → `Alerting`, exit-code → `OK`). Active series **405** against the <1k budget, of which `node_cpu_seconds_total` alone is 192 (24 threads × 8 modes) — the trim candidate if it ever tightens.
- **Alloy runs as a dedicated non-key-owning uid.** `zcrypto-alloy` (uid 999), not `deploy` (1001) — verified live that it **cannot** read `/home/deploy/.ssh/sync_nas_archive` through the `/host/root:ro` mount. This mirrors the NAS's uid-1031 choice rather than merely resembling its shape; the first implementation ran as `deploy` and would have had a direct, no-escalation read of a live private key while the config comment claimed a "smaller blast radius". Spec D7's text, which caused that, was corrected in place. The docker-socket residual ([[T0042]]) is unchanged and stated as defence-in-depth only.
- **Vault re-scoping.** The six Alloy credentials had been mis-scoped to `group_vars/capture_host/` since spec `00043` — no capture host runs Alloy ([[T0020]]), so they sat in a group that never used them while the host that needed them could not see them. They moved to a new `observed` group (`capture_host` + `ops_host`), and `grafana_sa_token` + `slack_webhook_url` to `group_vars/all/` (owner's call — workstation tooling, read by `grafana-push.sh` by path, not via inventory). Ciphertext moved verbatim; nothing was decrypted. Verified by boolean probe: `zcrypto-ops` resolves the Alloy vars and resolves **neither** trade-key var. The ops secrets file is now rendered from the vault with `no_log` — the hand-placed out-of-band file is gone (that pattern exists only because the NAS is not Ansible-managed).
- **Verified by outcome (D10), not exit code.** Overlay byte-identical across hosts after a full cycle in the new direction (1175 files, list sha `0b684ce3bf0b3774`); **exactly one publisher** for each of `zcrypto_reconcile_*` / `zcrypto_trade_backfill_*` (the NAS's stale textfiles were deleted at cutover, or they would have frozen and paged forever from a host that no longer does the work — the rules read bare instant vectors with no instance aggregation); raw mirrors unharmed (`checked=3998 ok=3998 failed=0`, `1002/1002`); ops-side invariant `gaps=0 trades_missing=0 duplicate_rows_found=0 recovered=0 unrecoverable=0`; `residual_gap_total` **2662 unchanged** through the publisher change — the feared false permanent-loss page did not fire. Reconciler still **detect-only** ([[T0039]] untouched).
- **D9:** the ops verified-replay stopped replaying the whole journal daily — a cost that grew with the archive forever. As first landed it was scoped `--date yesterday` (UTC, host-side; yesterday, not today — the timer fires 05:23 UTC, when today's journal holds at most the 00:00/04:00 cycles); the multi-day-outage hole that scoping opened was registered as [[T0059]] and closed on this same branch by the watermark catch-up loop (continuation below).
- **Six defects were found by doing, not by review** — every one traceable to plan/spec text: Alloy running as the key-owning uid; `group_add: ["docker"]` by name (Docker resolves named groups against the *container's* `/etc/group`, and the Alloy image has only `alloy:x:473:`, so the container would never have started); an alert selecting `node_load1{host="ops"}` when no metric carried a host label at all (the label was on the Loki path only); the NAS's `load high` rule, which our own change would have made misfire against a 24-thread box (now `node_load1{host=""}` — an empty matcher selects series where the label is absent); `config.alloy` left in `templates/` after switching to `copy:`, which only ever searches `files/` (no syntax check can catch that — only a converge); and `"$SOURCE{{ ops_capture_subdir }}/"` rendering `$SOURCEcapture-segments`, an unbound variable that under `set -u` kills the script on its **first rsync** — invisible to the `bash -n` the plan mandated, and combined with the NAS strip in the same commit it would have been a fleet-wide Role C outage. The reconciler also could not have appended its ledger at all: the tree carried the NAS's uid 1000 while the container runs as ops's `deploy` 1001, and `ledger_append` opens the ledger in append mode **in detect-only mode too**.
- **New topics:** [[T0056]] (the NAS is the only non-Ansible host — `infra/nas/` changes are inert until hand-copied, and nothing detects the drift: the trade-backfill sat undeployed for days so its daily gate had **never run once**, and `compose.yaml`'s `:latest` placeholder destroyed the `-compat` digest pin when copied verbatim, killing the AVX-less Atom with `Illegal instruction` for ~90 s), [[T0057]] (the [[T0044]] runbook's `.bak` lives inside the replicated overlay: it broke the new channel on a permission mismatch and is the only surviving record that an append-only ledger was edited — it has 12 lines to the live ledger's 11, the missing one a LINK/EUR `total_loss`), [[T0058]] (ops is two hops from source, so `source_lag` **rose** 4072→6465s and the 3h alert margin shrank from ~1.9h to ~1.2h — D10 predicted a fall, conflating loop-period with mirror lag), [[T0059]] (`Persistent=true` + `--date yesterday` verifies one day after a multi-day outage and still reports healthy). [[T0044]]'s growth leg is relieved but the topic **stays open** on its correction-marker sub-item (D8 — relieving pressure is not fixing the defect).
- **Measured 2026-07-17:** the NAS's post-cutover loop settled at **~67 min** (from the 103-min before-picture — near its 60-min floor, so the loop-period win was real; `gate-export`'s ~6 min/cycle and T0028's O(archive) `verify_tree` stay on the NAS by D3). The attribution question the spec's risk note flagged resolved against the move's other promise: with the compute gone, `source_lag` kept **rising** (4072 → 6465 → 7928 s) — the residual term was the mirror hop itself, which drove the T0058 pivot below.
- **(2026-07-17) The log pipeline became usable ([[T0060]], stays partial).** The ops logs reached Loki but were ~99.9 % noise with dead parse stages: the parse scheme now keys on the compose service label first (real `level` labels live), the `docker run --rm` ephemeral jobs are named and dropped from Docker discovery, and the timer jobs' logs ship via the unit journal — which also carries the host scripts' own gate-decision lines no container log ever had. The `zcrypto-ops` alert group grew to **ten rules, provisioned live and read back**: ERROR logs, two green-when-blind canaries (`log pipeline dead`, `journal transport dead`, both noData=Alerting), and exit-code rules for all four timer jobs. The remainder — the poller stops re-submitting at source via a per-pair watermark (owner-ratified 2026-07-17) — is the next PR's spec, so the topic stays `partial`.
- **(2026-07-17) [[T0059]] resolved: the verified-replay catches up via a watermark.** The loop persists the last successfully-replayed date and replays every day from the watermark through yesterday, so `Persistent=true` is now harmless by construction — a missed timer's single catch-up run covers every missed day. Hardened against zero-byte, garbage, unpersisted-seed, empty-journal-day, and future-date states — **every failure mode execution-reproduced then fixed** (a zero-byte watermark had parsed as TOMORROW: a permanent silent-skip that fed the dead-man forever). Deployed to ops (converged); first organic timer run 2026-07-18 05:23 UTC.
- **(2026-07-17) The NAS came under Ansible ([[T0056]] resolved).** The owner ratified the model — quirks become code, not prompt-knowledge — and the maiden converge ran 2026-07-17: the TZ guard **PASSED on live UTC evidence** (the owner flipped DSM; the guard's fail-first behavior was proven in `--check` the day the TZ was still CEST), 8 changed including **both digest pins via the rendered `.env`** (the `:latest` traps dead in the deployed state, not just the repo), deployed entrypoint sha == repo sha, and the apply tasks ran (`up -d` + both restart tasks). `run.sh` loads the NAS deploy key into its throwaway agent (fixed after review). Drift detection is moot: hand-deploys no longer exist.
- **(2026-07-17) The in-custody `.bak` became committed evidence ([[T0057]] resolved).** `infra/nas/ledger-correction-20260714-link-eur.md` records the [[T0044]] correction; the `.bak` was sha-verified (`1da63f1cce679d6e`) on BOTH hosts, then removed from both — trees identical at **1174 files** after — and the runbook now writes its backups outside the replicated tree, so no future correction re-creates the failure shape.
- **(2026-07-17) The T0058 pivot: ops reads custody over NFS ([[T0058]] resolved).** The owner ratified four consensus points after the lag series and the final review's **two-hop gate blindness** (the cutover gate keyed on the NAS→ops rsync, a transport that succeeds against a frozen mirror — falsifying the entry above's "delays healing, does not produce wrong verdicts"): (1) the NAS exports `/volume1/ZhaoCrypto` **read-only** to ops (rule live, hand-added by the owner, recorded in `infra/external-systems.md`); (2) the 3-tree rsync retires — canonical trees read through the automounted `ro,nfsvers=3,nolock,soft,timeo=100,retrans=3,noatime,nosuid,nodev` mount (write-probe refused for user AND root; the write-back direction unchanged); (3) the reconcile gate consumes the NAS-written `.pull-status` (`capture_ok=1 secondary_ok=1` live) **through the mount**, fail-closed and octal-safe; (4) the writer cadence doubles (`:12`/`:42`, armed and firing).
- **(2026-07-17) The measured after-state.** Post-NFS `source_lag` sawtooths **6136/6746/7938 s** (1.7–2.2 h): the NAS-hop + finalization is now the ONLY term, so 2.2 h is a **hard phase bound**, not a drifting average — against the 3 h alert that is ≥ 0.8 h margin worst-phase, ~1.2–1.4 h typical (vs ~48 min worst at the rsync two-hop's 7928 s), and it is **structural** again per the owner's ruling that the custody hop is unavoidable ("we have distributed systems, therefore the hops are unavoidable"). The exporter stamps cycle START. NFS cycle cost 7 m 24 s–9 m 17 s per writer cycle — the `verify_manifest` re-hash of the 48 h window now rides the wire, recorded beside the [[T0028]] reference on [[T0058]]. `residual_gap` held **2661.8** throughout — no false verdicts, ever.
- **(2026-07-17) The old channel is retired for real.** The `sync_nas_archive` key pair + `nas_known_hosts` removed on ops, the rrsync `authorized_keys` entry removed on the NAS, and ansible re-verified with a ping AFTER the removal. The one un-run T0058 next step — the `/volume1` btrfs-vs-ext4 readdir probe — split to [[T0062]] rather than buried in the archive.
- **(2026-07-17) New topic [[T0061]]**: the live Kraken trade key is group-scoped to `capture_host`, so the capture-only secondary resolves it (only the engine play's targeting keeps it off `zcrypto-red`); the fix is a small ciphertext move but touches the live trading credential — human-gated, parked for go-live hardening ([[T0049]]).
- **(2026-07-17) What the review machinery earned this branch:** the final review found the two-hop gate blindness that drove the T0058 pivot, and the octal/leading-zero gate arithmetic that failed the gate OPEN (`ts_epoch=0999999999` read as invalid octal aborted the `if/elif` chain; every arithmetic site now forces base-10). Across five review waves, **~40 confirmed findings — every one either fixed or registered**, none dropped.

## 2026-07-17 — iter-102: the liquidations poller stops re-submitting at source (spec 00055, T0060)

- Per-coin bucket watermarks (`prime_bucket_watermarks` + `poll_cycle(watermarks=…)` in `cli/liquidations/coinalyze.py`): primed from the segment tree at startup, advanced in-memory on submit; already-persisted buckets are skipped before the writer, eliminating the ~15,800/h `dropping replayed event` WARNINGs on ops. The 30 h fetch window, writer dedup, and late-event floor are untouched — surviving dedup drops are now genuine anomalies.
- Cycle log truth: `poll cycle: submitted=N skipped_at_watermark=M closed bucket(s)` (was a bare submitted count annotated "re-submissions are dropped by dedup/floor"); startup logs `primed bucket watermarks for N/10 coin(s)`.
- Spec 00055 D5 answered empirically: no restart-duplication — dedup keys reseed from open-hour parts (T0026); pinned by a new regression test in `tests/test_capture_segment_writer.py`.
- No CLI/config surface change; deploy to ops is a follow-up tracked in T0060 (ripe when a capture image containing the fix exists).

## 2026-07-17 — iter-096: Role C redundant capture close-out — soak pin, --mint flip, live drill (spec 00050, 2 of 2)

- **[[T0039]] pinned and closed: `--min-gap-seconds = 30 s`, from 66 h of two-host soak.** 217 primary-silence windows the secondary witnessed (p50/p90/p99 = 3.26 / 6.10 / 6.54 s, max 12.08 s), and the max hand-classified per the harness's outlier rule: AVAX/EUR silent for 12.08 s while its **nine sibling pairs flowed on the same host** (97,104 AVAX rows that hour) — a per-connection coalescing artifact, not a gap, so at a 10 s threshold the reconciler would have spliced secondary rows into an hour the primary never lost. 30 s clears both measurements (2.03× the single-host max 14.78 s, 2.48× the cross-host max) and stays 2.8× below the smallest real outage on record (83 s), so it costs no detection power.
- **The `--mint` flip, gated and zero-backlog (`dc71d48`).** A named, defaulted-off knob (`ops_reconcile_mint`, default `false` in the role) enabled durably in `host_vars` with the evidence beside it — never a silent default change. The template renders `--mint` / `--detect-only` explicitly either way, and the `| bool` cast is load-bearing: a CLI override arrives as the **string** `"false"`, which is truthy — without the cast, the override meant to disable minting would enable it (all four bool/string cases render-verified). Verified safe to apply: the soak ledger held **zero** `would_mint` entries (11 total: 10 `trade_deficit` — a QA signal, never a mint — and 1 `both_streams_silent`) and `healable_gap_seconds_total = 0`, so the flip applied no backlog — the first mint ever would be the drill's gap, deliberately created and fully observed.
- **The live two-leg drill (Task 13).** **Leg A (primary kill):** stop 15:01:15Z with the restore pre-armed via `systemd-run` (fired 15:26:14Z); the check lifecycle observed end-to-end (up→grace ~15:11, grace→DOWN ~15:21, recovery ~15:27) and the page cycle **owner-confirmed on the production Slack path** ("zcrypto-capture is DOWN" 15:20–21Z, "…is UP" 15:28Z — email is retired per [[T0047]], so this is the stronger form of the plan's "alert email arrives" assertion); the secondary positively verified covering the window (~20 files/90 s) after the pre-check's 71 s mtime-proxy false-alarm (flush cadence, not staleness). Evidence-based plan deviation: restore at 25 min, not the plan's 15 — the deployed check is timeout 600 s + grace 600 s, so a 15-min outage can never page; the plan's own numbers could not have fired its own assertion. **Leg B (secondary kill):** the pre-check **gated** this time (Leg A's first pre-check printed FAIL but did not gate the stop — three unchained ssh calls; Leg B's script &&-chains everything off its pre-check): primary-liveness guard fired 16:01:11 (48 files/90 s alive), stop 16:05:04Z → restore 16:30:04Z, DOWN page owner-confirmed 16:24Z, UP 16:31Z, container up with 0 error lines and 34 files/300 s flowing — canonical unaffected throughout. Engine untouched across both legs (verified active before and after).
- **The first mint (17:12Z cycle, finished 17:21:05Z).** `spliced=10 union=6 failures=0` — **15,509.5 pair-seconds healed**, one secondary block per pair (BTC/EUR provenance: 3 blocks, the single secondary block [15:01:15.230 → 15:27:05.979] = 356,200 rows); manifest `15.parquet` verifies. The honesty check: overlay 730,269 rows / max_gap **0.8 s** vs raw-primary 374,069 rows / max_gap **1550.8 s**, and 374,069 + 356,200 = 730,269 — row-exact. Trades: +1,278 primary-deficit rows over 6 pairs, `deduped=0`. Residual frozen at **2661.78874** — the pre-existing permanent loss neither grew nor was "healed" by the drill.
- **The hour-16 no-op — the phantom-splice negative control (18:12Z cycle).** `spliced` stayed **10**, not 11: no book mint for hour 16 and no hour-16 book overlay file — correct, the primary was healthy all of hour 16; healed frozen at 15,509.529138, residual frozen at 2661.78874.
- **Drill symmetry — the redundant-capture thesis demonstrated live.** Trade-deficit accounting: primary deficit **1,278** rows (the Leg A window, added to canonical from the secondary) vs secondary deficit **1,875** rows (the Leg B window, `trades_added=0`) — a secondary outage causes **zero canonical impact**, only a recorded mirror deficit; a primary outage is healed from the secondary. "A redundant stream that can die silently is not redundancy — leg B proves it can't."
- **The T0003 gap-rate alert's first live firing, threshold math exact.** "Reconciler · primary gap rate high (degrading host)" fired ~17:51Z on the drill gap (owner Slack 19:52 CEST): `healable` jumped 0 → 15,509.5 at 17:21Z, far above the 600 s threshold, and `for: 30m` fired it ~17:51Z; it auto-resolves ~17:21Z 2026-07-18 as the 24 h window slides. No silence applied, deliberately. Leg B's independence confirmed by design — `healable` counts secondary-witnessed **primary** gaps only.
- **CRC verify-replay: the spliced hour is provably coherent (20:54:20Z, ~3 h 30 m run).** 2,190 hours replayed, **2,190 ok, 0 failed, exit 0** — including hour 15 on all pairs (e.g. XRP/EUR 15:00 `anchored=True ordered=True checksum=True replay=True rows=427120`): state convergence across both splice boundaries proven with venue checksums.
- **Found-and-fixed during the drills: the writer cycle's dead-man had silently starved (`d13335f`).** The T0058 rewrite dropped the `zcrypto-archive-pull` healthcheck ping (owner-found: last ping 11:21Z, DOWN since ~14:21Z) — Grafana's dead-man stayed green on fresh metrics while the deliberately-independent healthchecks.io domain starved. Restored with semantics updated for what the unit now is: ping on rc=0 **including gate-skips** (a skip means the writer is alive and correctly refusing stale input; the upstream problem pages through its own channels), and the 16:12Z cycle's first ping flipped the check UP at 16:21Z, owner-confirmed live. Bonus catch: a Jinja `{# #}` comment inside a bash continuation chain renders as broken-but-valid shell under plain Jinja (`trim_blocks` masks it in Ansible) — production verified unaffected, comment relocated, both render configs now tested in the harness.
- **Closeout sync:** [[T0039]] **resolved + archived** (both legs validated — no phantom splice across the soak's healthy windows, a real 25-min outage healed exactly and CRC-clean); the Task-14 stale cross-ref adjudications ([[T0003]]'s reboot-slot and Role-C text, [[T0032]]'s D8 retention pointer, the [[T0008]] splice cross-ref, [[T0027]]'s fleet windows, [[T0028]]'s re-derived sweep-cliff horizon — ~8–9 months to the 250 GB onset, ~1.7 y to the 600 GB cliff, at the ~0.96 GB/day both-mirror growth) landed in `9d652d4`.

## 2026-07-18 — iter-103: OPS-6 Loop — the hot-cluster dataset exchange, research runnable on ops (spec 00056)

- **The topology (D1).** Datasets are partitioned by sync-behaviour × mutability into three clusters: **custody** (the unbackfillable capture archive + raw dumps on the NAS, read in place over NFS, never transmitted by this tool), **hot** (the ~140 MB compiled working set every research node fetches/pushes), **private** (per-host engine store/journal, never synced). The catalog was rewritten by this taxonomy (D8, `docs/reference/data-catalog-full.md`).
- **The `zcrypto data` command group (D2/D3).** `fetch` additively mirrors the NAS `hot/` hub into the local data root (manifest-verified); `push` sends this node's authored sets to the hub; `rebuild` re-freezes/refreshes a set into a minted sibling and pushes it. Config collapsed to one top-level `[zcrypto].nfs_mount_dir` (default `/mnt/zhao-crypto`, one committed value aligned across workstation + ops) — the hot source and the custody OHLCVT path both derive from it (overnight feedback 4b, replacing the separate `hot_dir`/`ohlcvt_source_dir` keys); `[zcrypto.data]` carries only `push_dest` + `authored_sets`.
- **The hot/ hub — the FIRST and only write path into custody, hardened + jailed.** The NAS `hot/` (2775 setgid) receives the workstation push over rsync-over-ssh, jailed by a vendored `rrsync` (rsync 3.4.1's upstream python3 jailer, byte-identical to the fleet's own `/usr/bin/rrsync`). Containment is **four layers**, all holding against a stolen key running its own client: `restrict`; the `command=""` path jail; `-munge` (symlink-target munging); and `-no-del -no-overwrite` (strip `--delete*`/`--remove*`, force `--ignore-existing`) — append-only is enforced **server-side**, not by the client. The ops→NAS pull is a **new dedicated 4th D9 channel** (`sync_hot -ro`): the three existing roots are exact-subtree pinned, none a parent of `hot-out` — a raw `rsync --archive --ignore-existing --chmod=D0775,F0664` in `pull-entrypoint.sh`, not the `zcrypto archive pull` wrapper (which lacks `--ignore-existing` and would fail on the hot sets' `manifest.json`/no-sidecars).
- **Validated live on the real NAS (rsync 3.1.2):** push works; `--delete` refused server-side (*"option --delete has been disabled"*); a content-changed re-push is untransmittable; `--force` dir-replace neutralised by the forced `--ignore-existing` (measured — no rrsync patch needed).
- **Seed + acceptance (D4).** First `data push` seeded all six authored sets (74 files, 0 skipped) into a NAS `hot/` that held none of them. Then the sharpest acceptance: on ops (provisioned as a research node — `uv` + a clone at `/home/zhaow/Projects/zcrypto-kraken` as `zhaow`), `zcrypto data fetch` mirrored `hot/` over NFS and the two data-dependent regression suites **RAN (not skipped) and passed — 62 tests in ~4 min** — research is runnable on ops.
- **The acceptance caught a real bug.** `data fetch --verify` crashed on the real manifests: `_verify_new_files` assumed `series` was a list of `{path, sha256}` dicts, but the hot sets nest it (OHLC `series[symbol][grid].sha256`, funding `series[symbol].sha256`; the holdout carries only a manifest-level `manifest_sha256`; universe/snapshots ship none). Rewritten to **hash-membership** — attest each fetched parquet's `dataset_hash` against every per-parquet `sha256` in the (nested) manifest; format-agnostic, fail-closed on an unattested hash, skip-with-warning when a set exposes no per-parquet sha. Adversarially reviewed; validated on the real seeded data.
- **Dead-config + payload removals (D5/D6).** Config: `backup_dir` + two never-wired fetch fields removed. The workstation's two leftover `--user` payloads retired — `zcrypto-engine-shadow.service` (Phase-6a soak, superseded by the live VPS engine, T0018) + `zcrypto-engine-gateops.timer` (VPS-journal pull + verified-replay) disabled/removed with their `~/.ssh/zcrypto-sync_ed25519` key — and the dead sets deleted: `data/ohlc` (v0 REST, superseded by `ohlc-full`, reconstructs bit-identical per T0001; `docs/reference/data-catalog.md` kept as its historical record) + `data/engine-journal-vps` (the now-retired gateops pull copy).
- **Decisions-log continuity (D7).** The per-phase decision logs became git-tracked, one file per phase (`docs/research/<serial>.phase<N>-decisions.md`), appended live per iteration exactly like this changelog — no draining, no continuation files — so a decision made on either host survives the multi-host loop.
- **A whole-fleet users/groups regularization split off (spec 00057, [[T0067]]/[[T0068]]).** Standing up the write channel surfaced that the ops data path runs as `deploy` (the sudo user) with hand-installed m2m keys, and the fleet mixes `kraken-*`/`zcrypto-*`/`deploy` naming. The ratified target — uniform `zcrypto-deploy`/`zcrypto-data`/`zcrypto-engine`/`zcrypto-alloy`/`zhaow`, with the trade key isolated at the compose/root layer (so the dedicated engine user is defense-in-depth, not required) — is captured in spec 00057 on a dedicated branch (`feat/fleet-users-groups`): its ops phase is [[T0067]], the capture/engine phase [[T0068]]. The NAS renames (`zcrypto`→`zcrypto-data`, `zcrypto-dummy`→`zcrypto-alloy`, uid 1000 kept) were applied manually, and this branch's nas role was adapted (`owner: zcrypto-data`). The ops identity migration + the hot-out **authoring** writability (zhaow→hot-out) are that follow-on's work; OPS-6's ratified flow (workstation authors, ops consumes) is complete without them.

## 2026-07-18 — iter-104: fleet users/groups (ops phase) — deploy leaves the data path (spec 00057)

- **`zcrypto-data` m2m user + `zcrypto-hot` exchange group (ops role).** A dedicated machine-to-machine user (`zcrypto-data`, uid auto-assigned, no sudo, no password) now runs every data container (`--user`), owns the data trees, and serves the NAS pulls — `deploy` (the passwordless-sudo user) leaves the data path entirely. The setgid `zcrypto-hot` group bridges `zcrypto-data` (owns + serves hot-out) and `zhaow` (authors into it). New vars `ops_data_user`/`ops_hot_group`/`ops_research_user` (`roles/ops/defaults`); the user/group/membership tasks live in the role's unconditional preamble, so the accounts exist before any ownership/run-as change.
- **`zcrypto-data`'s shell is `/bin/bash`, NOT `nologin` — a design correction the Task-4 pre-flight caught before the cutover.** sshd runs a `command="rrsync -ro …"` forced command through the account's *login shell*, so `nologin` swallows it (`"account not available"`) and every pull would fail. The four keys stay fully jailed by `command=`/`restrict` (one read-only subtree each) and there is no password, so no interactive login is reachable despite the real shell — the "no interactive login" intent by the mechanism that actually enforces it (the same pattern the fleet's `deploy` already uses). Spec 00057 D2 + the plan + the code comments were corrected so [[T0068]] gives any pull-serving user a real shell too.
- **The four NAS pull channels became Ansible-provisioned on `zcrypto-data`** (they were hand-installed on `deploy`). Committed pubs `infra/ansible/files/sync_{liquidations,panel,reconciled,hot}_ed25519.pub`; the ops role installs each as an `rrsync -ro` forced command pinned to its own subtree (`exclusive: false` — dual-authorized with `deploy` across the cutover, then `deploy`'s copies dropped).
- **Data ownership + container run-as → `zcrypto-data` (attended).** The container-user derivation and every data-plane tree moved deploy → zcrypto-data: `ops_data_dir`, `textfile`, `capture-reconciled` (recurse), and `hot-out` (now `zcrypto-data:zcrypto-hot 2775`, setgid). The pre-existing `liquidations`/`l2-panel` trees (container-produced, never role-managed — a gap the plan missed) were migrated by a **one-time host `chown`** in the attended window, not a permanent recurse task (nothing re-imports foreign uids; a rebuilt host starts empty). `ops_compose_dir` deliberately stays admin-plane. Window: poller stop 18:57:01Z → converge → chown → start 18:59:47Z (~2m46s, well within the poller's ~30 h Coinalyze catch-up); an hourly parquet then wrote as `zcrypto-data` with **no `EACCES`**.
- **NAS repointed `deploy@ → zcrypto-data@` (attended).** `host_vars/nas/vars.yml`'s four ops sources → `zcrypto-data@192.168.100.6:`; the archive-pull container pulled all four cleanly (`checked=830/2450/407, failed=0`; hot a no-op, outbox empty); `deploy`'s four hand-installed `rrsync -ro` keys dropped (authorized_keys 5 → 1). The capture/journal sources are untouched (still `deploy@`, T0068).
- **hot-out authoring handoff verified end-to-end.** `zhaow` authors into hot-out (group-write via `zcrypto-hot` + setgid inheritance — the child dir came out `zhaow:zcrypto-hot drwxrwsr-x`), `zcrypto-data` reads/serves it, the NAS pulls it into `hot/` — the OPS-6-deferred *authoring* direction, now live.
- **`deploy → zcrypto-deploy` rename (attended, root break-glass).** `usermod -l`/`-d -m` + `groupmod -n` + sudoers move (uid 1001 kept; the interactive login key moved with the home, so ssh/Ansible reconnect unchanged). `bootstrap.yml`'s ops play provisions `zcrypto-deploy` (authorized_key `exclusive: true` — one key, D1); `host_vars/zcrypto-ops` `ansible_user: zcrypto-deploy`; the 5 admin-plane `owner: deploy` role refs retargeted (a Task-3 review finding — without it the first post-rename converge fails user-lookup). Verified: `ssh hp` lands as `zcrypto-deploy` + passwordless sudo, and `site.yml --tags ops --check` is **`changed=0 failed=0`**.
- **Docs + follow-up.** `infra/ops/README.md` rewritten (users/groups model, role-provisioned channels, `zcrypto-data@` sources, containers run as `zcrypto-data`, admin `zcrypto-deploy`) and `infra/nas/README.md` updated; the stale alloy-security comment referencing the retired `sync_nas_archive` key corrected (verified absent from the renamed admin home). [[T0069]] parked: `zcrypto engine gate-export` is CPU-bound ~8 min per hourly NAS archive-pull cycle on a 96 MB journal (in-budget, super-linear-looking; it briefly masked the ops pulls during the Task-4 cutover — initially mis-read as an infinite loop, corrected once it terminated). [[T0067]] resolved + archived.

## 2026-07-19 — iter-105: fleet users/groups (capture/engine phase) + container names (spec 00057, [[T0068]])

- **Capture/engine account renames — gap-free, no container restart.** `kraken-capture → zcrypto-data` (uid 999) on both capture hosts and `kraken-engine → zcrypto-engine` (uid 997) on the primary, via `usermod -l`/`groupmod -n` keeping the uid/gid. The compose `--user` is numeric (getent-derived), so it stayed byte-identical across the rename and **neither the capture daemon nor the engine was recreated** — the unbackfillable L2 stream never gapped (verified live: capture Up 46h primary / engine Up 8d at cutover, `StartedAt` unchanged). The base role now creates `zcrypto-data` with the Debian `/usr/local/sbin/rrsync-shell` wrapper in place of `kraken-capture`.
- **The three pull-export keys moved to `zcrypto-data`, Ansible-provisioned.** The capture, capture-red, and engine-journal forced-command keys were hand-installed on `deploy`; the capture/engine roles now install each as an `rrsync -ro`/journal forced command on `zcrypto-data` (`exclusive: false` — dual-authorized across the cutover, `deploy`'s copies dropped at the host). `zcrypto-data` joins the `zcrypto-engine` group to traverse the `0750` journal dirs, which became group-readable for the pull.
- **NAS repointed `deploy@ → zcrypto-data@` for capture + journal (attended).** `host_vars/nas/vars.yml`'s `nas_capture_source`/`nas_capture_red_source`/`nas_journal_source` → `zcrypto-data@`; verified live — capture pull `ok=5276`, capture-red `ok=2260`, journal ok, `gate-export` ran on the pulled journal and refreshed `gate.prom`. With the ops channels (iter-104) this puts **every** fleet pull channel on `zcrypto-data@`, off the admin user.
- **`deploy → zcrypto-deploy` on both capture hosts (attended, root break-glass).** `usermod -l`/`-d -m` + `groupmod -n` + sudoers move (uid kept; the login key moved with the home, so ssh/Ansible reconnect unchanged). The virgin `bootstrap.yml` capture play provisions `zcrypto-deploy` (authorized_key `exclusive: true`, D1). The `AllowUsers` superset (`hardening_ssh_allow_users`) admits `zcrypto-data` + `zcrypto-deploy` (+ transitional `deploy root`), set BEFORE the NAS repoint or the pull is denied at sshd; the §8 engine gate asserts the live `sshd -T` posture carries both `zcrypto-data` AND `zcrypto-deploy` before the trade key renders (fail-closed — a `--check` can't satisfy it, so the primary was validated by the clean real apply).
- **First-time `zcrypto-alloy` telemetry stack on both capture hosts (spec 00057 D5).** A dedicated non-login, non-key-owning `zcrypto-alloy` uid/gid runs Alloy in its OWN compose project (an Alloy redeploy never restarts capture); admin-plane files stay `root:root`, alloy-data/secrets are `zcrypto-alloy`. Divergences from the ops copy: `cpus 0.5` (a 1–2 vCPU VPS, safer than ops' 2.0), the journal reader scoped to the prune unit (avoids double-ingesting the attached capture stdout), and the `host` label from `constants.hostname`. The **accepted docker-socket residual** (root-equivalent Docker API, now on the trade-key host) is recorded in [[T0042]] — the boundary re-decision is deferred to the go-live readiness review.
- **Two defects found live and fixed.** (1) devsec `os_hardening`'s "Remove shell for linux system accounts" would reset uid-999 `zcrypto-data`'s wrapper shell to `nologin` and break the pull — exempted via `os_ignore_users: [zcrypto-data, vagrant, kitchen]` in `group_vars/capture_host` (caught by the live `--check` at the secondary cutover). (2) The engine role kept the OLD `user: deploy` journal-key + deploy-in-group tasks — Task 2 ADDED the `zcrypto-data` tasks but did not REMOVE the `deploy` ones (the capture role correctly replaced); masked because the cutover ran AS `deploy`, it surfaced as `getpwnam(): deploy` on the later container_name converge (run as `zcrypto-deploy`) and aborted before recreating the engine. Removed; re-converge `ok=227 changed=0`.
- **Explicit `container_name` on every compose service (drop the docker `-1` suffix).** 7 services renamed: capture → `zcrypto-capture`, engine → `zcrypto-engine`, ops liquidations → `zcrypto-ops-liquidations`, NAS archive-pull → `zcrypto-archive-pull`, all three Alloy → `grafana-alloy`. The NAS `config.alloy` container-log relabel switched from the docker-NAME regex (whose `zcrypto-archive-` prefix-strip would collapse `zcrypto-archive-pull → pull` and silently break the archive-pull error alert + dead-man) to the compose-SERVICE-label scheme, matching capture/ops. Applied fleet-wide (NAS → ops → secondary → primary), each container recreated (docker treats `container_name` as identity), Alloy restarted per [[T0048]]; two brief capture resubscribe blips, not gap-loss.
- **`docker_deploy_user` consolidated to a correct fleet default.** The docker role default was `deploy` (overridden only on the capture hosts); ops had no override, so a future ops converge would have `ansible.builtin.user`-CREATED a dead `deploy` account in the docker group. Default → `zcrypto-deploy` (correct fleet-wide since every managed host runs its admin as `zcrypto-deploy`), `ansible_user` consolidated into `group_vars/capture_host`, the per-host overrides dropped. Verified: secondary `--check --tags docker` idempotent (`changed=0`), ops clean (no `deploy` user; docker group = only `zcrypto-deploy`).
- **Docs + closeout.** `infra/README.md` (`zcrypto-deploy@10022`; virgin bootstrap creates `zcrypto-deploy`), `infra/nas/README.md` (capture/journal sources `zcrypto-data@`, the stale `deploy@` contrast note fixed), `infra/external-systems.md` (NAS `/bin/sh` + forced-command jail since DSM rejects custom login shells; shared folder `group zcrypto`), and `.claude/rules/fleet-deploys.md` (day-to-day user `zcrypto-deploy`) updated. [[T0068]] resolved + archived; [[T0042]] annotated with the D5 residual and its now-fired capture-host trigger.

## 2026-07-19 — iter-106: panel settle-watermark — an un-healed hour can't be permanently captured ([[T0066]])

- **`cli/panel/materialize.py` settle gate (spec `00052` D6 correction).** The materializer deferred an hour only by the monotone per-pair watermark; D6 claimed "no settle margin is needed: a canonical final is settled by construction," conflating *settled* (final + hash-verified) with *heal-complete*. The reconciler mints healed book hours at H+2h…H+6h, so at the post-T0058 NFS-lag trough (1.7–2.2h vs the H+2h mint floor) an hourly pass could consume the un-healed primary and — the watermark being monotone with no re-mint invalidation — capture it permanently, silently degrading the exact dataset [[T0014]]/[[T0024]] are about to read. Fix: `materialize` now defers an hour until `now - hour >= PANEL_SETTLE` (7h = the H+6h max mint + a 1h pull/visibility buffer), counting a not-yet-settled hour `hours_unsettled` and holding the watermark off it. `--settle-hours` on the CLI (default 7); injectable `now` for tests. Chose option (a) [explicit watermark] over (b) [ledger-driven invalidation] — the ~5h freshness cost has no current consumer.
- **One-time audit: 0 un-healed panel hours.** A read-only ops-side comparison of every `book`/`minted` reconcile-ledger record's `at` time vs the panel hour's mtime (10 book mints — the 2026-07-17T15:00 drill across all 10 pairs) found every panel hour written 17:22–17:23Z, ~10 min AFTER the 17:12:16Z mint — all healed-derived. Nothing to re-derive; the pre-fix tree is clean. No committed audit tool (YAGNI — the forward fix structurally prevents recurrence).
- **Closeout.** Spec `00052` D6 corrected in place; the trade-bar materializer's settle-binding moved to [[T0065]]'s reach round (trades are heal-complete only after the ≤~28h daily REST backfill — a chasm, not a race); [[T0066]] resolved + archived. **Deployed to ops 2026-07-19** — re-pinned the ops jobs to a develop-HEAD image (`@sha256:e7440257…`, T0066 verified inside) via `site.yml --tags ops` (render-only, `changed=3`, no restart/gap); the panel timer's next run confirmed the gate live: `hours_unsettled=10` (the 10 pairs' most-recent <7h hours now deferred instead of consumed from the un-healed primary).

## 2026-07-22 — iter-115: the universe's spread cap, retired from placeholder ([[T0024]], spec `00067`)

- **`spread_cap: "pending-capture"` now has data behind it.** Selection filtered on margin + leverage + median quote volume only, so a thin-book pair could clear the €150k/day floor and still be untradeable at our sizing. `cli/universe/rules.py` gains an opt-in `spreads` map + `max_spread_bps`, and `build_universe_file` emits a structured `spread_cap` record in place of the placeholder string.
- **It reuses [[T0014]]'s calibration rather than re-deriving** — "one derivation, two consumers", as the topic asked. That also inherits T0014's measurement that the **median top-of-book spread is unusable for BTC/EUR**, so the criterion is priced on the mean **effective spread at size**, at the **€1,400 max-size position the volume floor is already calibrated against** — the two criteria are now commensurable.
- **The cap is 10 bps/side, derived from the fee stack, and it binds on nothing.** A round trip crossing twice at the cap costs 25 % of the tier-1 round-trip maker fee (a chosen convention, not a derivation — at the cap spread is 20 % of the round trip, not yet dominant); at €1,400 the whole universe passes, DOT worst at 6.55 bps/side (16.4 % of that fee, ~35 % headroom on the mean). That 6.55 is log-interpolated between the calibration's €1k and €10k anchors, not measured at €1,400, and the headroom is a mean statement: DOT's p99 is 10.87 with 2.54 % of seconds above the cap. **Selection is unchanged for the replayed inputs, 12 → 12, `escalate` unchanged** — determined by replaying the stored universe entries through `finalize_universe` with and without the criterion. The replay is faithful (without the cap it reproduces `selected`, `escalate` *and every* `reasons` list exactly), so the cap is the only variable; but the inputs come from that same file, so it proves the criterion changes nothing **given those inputs**, not that a fresh rebuild would select the same twelve. A rebuild was then measured and **would not**: on the live `data/ohlc-full` AVAX/EUR falls to 132,274.82 EUR/day against the 150,000 floor, so eleven names — that set's last bar is 2026-03-31 and the artifact cites an OHLC dir that no longer exists (→ T0093, unrelated to the cap). Tuning the cap to bite on the current universe was considered and rejected: a threshold chosen to exclude today's worst member is a post-hoc rank, not a criterion.
- **The finding that outlasts the criterion: the cap can only screen 10 of the 12 selected symbols.** The capture daemon subscribes to **EUR-quoted pairs only** (verified — every base in the archive has exactly one quote dir, `EUR`), so `ETH/BTC` and `SOL/BTC` have no L2 at all. They are recorded `spread_bps: null` and **not** rejected — absence of evidence is not evidence of a wide spread — but the null is deliberate, so a filter covering 10/12 cannot read as universe-wide. `SOL/BTC` sits at 233,595 EUR/day, barely over the volume floor: the blind spot is exactly where the criterion was most wanted. Registered as [[T0092]] (capturing those legs touches a live unbackfillable pipeline, so it is a deliberate decision, not a drive-by).
- **Three self-inflicted defects caught before the review returned, by auditing my own claims.** (1) `_refresh_universe` — the *only* production caller — never passed `spreads=`, so the criterion would have shipped inert; now wired, with the cap record. (2) `render_markdown`'s structured-cap branch required four keys no producer set: **dead code, shipped untested**; now produced and covered. (3) The per-symbol markdown table had **no spread column at all**, so the `null` whose entire justification is "visible in the artifact every reader consults" was visible only in the JSON; the table now carries a `Spread (bps/side)` column, rendering `not screened` for a symbol the cap could not screen and a neutral `—` on the placeholder path, where the criterion never ran at all.
- **Opt-in by construction.** `finalize_universe(..., spreads=None)` leaves the **selection outcome** unchanged — *not* byte-identical: every entry gains a `spread_bps` key, null on that path (pinned by `test_omitting_spreads_screens_nothing_but_still_records_the_gap`; the original test compared `finalize_universe(p, v)` against `spreads=None`, which is the default — the same call twice, unable to fail — and was deleted). Every existing caller and all nine pre-existing rules tests are untouched. **16 new tests in total** across the three files (rules +9, build +6, rebuild +1; none removed — measured as a set difference, not by subtracting totals).
- **Closeout.** [[T0024]] → `partial`: the criterion, its calibration and its measured effect are delivered; the **canonical universe artifact still carries the placeholder** until the next rebuild, which needs a live `AssetPairs` fetch and a canonical-set write — outside this run's boundary, and it rides [[T0025]]'s pre-live refresh anyway. Decisions in `02.phase1-decisions.md` (universe selection is Phase-1 data-foundation subject matter, spec `00003`).

## 2026-08-11 — iter-136: the universe refresh's volume source — reach reaches the BTC-quoted legs, the universe becomes a resolved stamped-set series ([[T0093]], spec `00093`)

- **Three corrections to the topic's own numbers, measured against a live clock (2026-08-11) before any code changed.** A rebuild refuses TODAY on every candidate source, not just `ohlc-full`: `data/ohlc-full` is 133 days stale, the promoted `hot/ohlc-reach` 20 days, `data/ohlc-holdout-2026-07-10` 33 days — all past the 7-day budget, so a fresh reach round is a precondition of the refresh, not an alternative to it. The topic's "a rebuild today selects eleven, dropping AVAX/EUR" figure is a **stale-window artifact**: it reproduces exactly (132,274.82 vs the 150,000 floor) only over `ohlc-full`'s frozen March window, because "trailing 30 days" is the last 30 *rows* of a set that stopped moving in March; on every fresh window AVAX clears comfortably (208,265.93 on the reach window, 267,840.43 on the holdout), and the thinnest passer under a fresh window is DOT/EUR at 194,771.98. So the ruling this iteration answers was never "eleven names or twelve" — reach is structurally EUR-only, so no number of re-runs could ever produce `ETH/BTC` or `SOL/BTC`, and the real question was what supplies those two legs.
- **The reach round became quote-aware, so it can mint the BTC-quoted legs.** `cli/ohlc/reach.py::_canonical_symbols` discovers full `BASE/QUOTE` symbols from the canonical tree instead of globbing `*/EUR/{interval}.parquet`, and `reach_round`/`_write_manifest` split the symbol to build read/write paths (previously hardcoded to `"EUR"`, which would have silently collapsed both legs of a two-quote base onto the EUR path). `ETH/BTC` and `SOL/BTC` were verified feasible before implementation, not assumed: `XETHXXBT`/`SOLXBT` are live pair keys in the AssetPairs snapshot, and `ohlc-full` already carries both series, so REST's window overlaps the seam. One trap surfaced: the venue spells the quote `XBT`, we spell it `BTC` — a first feasibility check for a `/BTC` suffix came back empty and looked exactly like "these legs do not exist" until the mapping was read as a translation, not a copy.
- **`PAIR_KEYS` is re-keyed by full symbol (`cli/ohlc/fetch.py`), and the engine keeps a DERIVED EUR-only view.** The base-keyed map (`"ETH": "XETHZEUR"`) cannot express two quotes for one base. A cold-review round caught that an earlier draft's claim — that the engine's `cli/engine/cycle.py` held a *different*, unaffected `PAIR_KEYS` — was false: `cli/engine/store.py` imports and re-exports the same map, and re-keying it unqualified would have silently widened the live engine's basket from ten EUR legs to twelve, produced store paths like `root/ETH/BTC/EUR/1440.parquet`, and broken the test pinning the BTC legs as excluded. The false claim traced to a grep piped through `head` that dropped the `store.py` import line — the truncated-sweep failure this project has a standing rule against. Fixed before landing: `cli/engine/store.py` now builds `{base: key for symbol, key in PAIR_KEYS.items() if symbol.endswith("/EUR")}`, one source of truth, the engine's ten-EUR-leg basket pinned unchanged by test.
- **`_refresh_universe` now refuses a source narrower than the candidate set.** `escalate` compares the *selected* set against band bounds and cannot see that the *source* was narrower — a source missing one candidate leg would previously have produced a smaller universe with `escalate` still `False`, the exact silent shrink this topic exists to prevent, arriving through a different door. The guard checks every `CANDIDATE_SYMBOLS` member's `1440.parquet` exists under the resolved source root before reading any frame, raising `DataSyncError` naming the missing legs instead of letting an untyped `FileNotFoundError` escape from inside polars.
- **Both the OHLC source and the universe artifact resolve newest-wins, symmetrically.** `resolve_ohlc_source` (consumed by `_refresh_universe` alone; the reach builder keeps its dump-derived `_require_ohlc_full` anchor, since a reach round must never consume its own freshly-minted, still-empty sibling) picks the newest `ohlc-reach-<stamp>` directory over the fixed `ohlc-full`, admitting only exact 8-digit stamps. `resolve_universe_path` (`cli/capture/command.py`) does the same for the canonical artifact: newest `universe-<stamp>/point-in-time-universe.json` wins, falling back to the legacy `universe/` set only when no stamped set exists. Both resolvers are loud on an incomplete newer set — a stamped directory present without its artifact logs ERROR naming it, rather than silently degrading to an older set (the same silent-shrink class the D4 guard closes, at the resolution layer instead of the content layer). The `--pairs` help text and the matching README Usage row, which named the fixed legacy path, now describe the resolution rule instead.
- **The universe artifact stops being one mutable file and becomes a series of immutable stamped sets.** The additive-only transport (`rsync --archive --ignore-existing`, deliberately never `--delete`) cannot express a second version of a fixed filename — a hand-promoted local copy would leave the hub saying `pending-capture` forever, silently, with every fetching host reading stale. `rebuild_sets` already minted `universe-<stamp>/` siblings, previously discarded by the hand copy; `push_hot`'s existing `extra_sets` parameter already publishes them additively. Regression-pinned rather than newly wired (`command.py` already passed `extra_sets` on develop): a stamped set reaches the hub under its own name without touching the legacy `universe/` set, a repeat push of the same stamp is a no-op, and pushing a new stamp names only that stamp's files in rsync's own itemised output. A suffixed stray (`universe-<stamp>.bak`) is proven not to outrank a real dated set — the same defect class already pinned for `ohlc-reach-<stamp>.bak`.
- **The false `_canonical_symbols` docstring is corrected.** It asserted the BTC-quoted legs were ones "capture holds but the dumps do not" — measurably false, `ohlc-full` carries both `ETH/BTC` and `SOL/BTC` dailies to 2026-03-31. The comment made a *wiring* limit (the reach round's hardcoded EUR glob) read as a *data* limit, which is how the topic came to be framed as a source question rather than a glob question; caught during planning, corrected in the same branch that fixes the glob.
- **Six commits, all Fable-reviewed, TDD throughout; two review rounds reworked the spec before implementation started** — a cold review found 3 blocking issues (the false engine-`PAIR_KEYS` claim above; nothing had actually repointed `_refresh_universe`, which would have left the attended sitting refusing at its rebuild step; the plan's resolver test asserted the silent degradation the spec's own verification forbids), and a second round fixed eleven further findings before Task 1 opened.
- **Closeout.** [[T0093]] stays `partial` — the wiring landed here, but no fresh reach round has minted the BTC legs and no `universe-<stamp>/` set has published; that is the **attended sitting** (`zcrypto data rebuild ohlc-reach` → `zcrypto data rebuild universe` → publish), sequenced after merge as one sitting so the canonical universe is rebuilt once, not twice. [[T0024]]'s `pending-capture` remainder is **not** discharged by this branch either — it clears only when that same rebuild run actually executes, post-merge. **No dataset-catalog change is owed by this branch**: the reach round's *reach* (twelve legs instead of ten) only changes the shape of a dataset once a fresh round is actually minted, which is that same attended sitting, not this code. Decisions in `02.phase1-decisions.md` (universe selection is Phase-1 data-foundation subject matter, spec `00093`).

## 2026-08-13 — iter-137: the attended universe refresh — the volume signal finally reads a live window ([[T0093]], [[T0024]], spec `00093`)

- **The sitting iter-136 unblocked, run end to end.** `zcrypto data rebuild ohlc-reach` minted all twelve legs into `data/ohlc-reach-20260813` — every daily series `continuous` against the canonical tail with 586 overlap bars, 134 bars appended and `gap_bars: 0`, reaching 2026-08-12. `ETH/BTC` and `SOL/BTC` came back indistinguishable from the ten EUR legs, which was the open question the whole iteration existed to answer: the spec's feasibility argument (live pair keys `XETHXXBT`/`SOLXBT`, `ohlc-full` already carrying both dailies so REST's window overlaps the seam) held in practice, not just on paper. **Step 1 was run `--no-push`, a deliberate deviation from the plan's prescribed sequence**, so the fresh reach set is LOCAL ONLY — the hub still carries the promoted unstamped `ohlc-reach/` frozen at 2026-07-22, and nothing depends on the gap because the universe rebuild reads the local sibling directly. The reason is that the reach round writes a `detached` status and *succeeds* rather than aborting, while the hub's transport is `--ignore-existing` and never deletes: the default would have published a possibly-detached set through a one-way door before anyone could inspect it. Publishing it later is a plain `rebuild ohlc-reach --push` if wanted. `zcrypto data rebuild universe` then selected on that fresh window and published `universe-20260813/` additively to the hub; the legacy `universe/` directory is untouched there, and `resolve_universe_path` selects the stamped set in production. The artifact's provenance reads `ohlc_dataset_dir: ohlc-reach-20260813`, `ohlc_stalest_daily_bar: 2026-08-12` — one day old against the 7-day budget, where the previous source had been frozen at 2026-03-31 for 135 days.
- **ELEVEN of twelve selected, and the exclusion is the mirror image of the defect being fixed.** DOT/EUR fell below the 150,000 floor at 146,957.37, down from 194,771.98 on the window ending 2026-07-22 — roughly a 25% liquidity decline over three weeks, measured correctly. The original defect ran the other way: recomputing over the frozen `ohlc-full` window had dropped **AVAX/EUR** at 132,274.82, which spec `00093` refuted as a stale-window artifact and which clears at 183,593.90 here. Two symbols, two causes, and only one of them a measurement fault — worth stating plainly, because "a rebuild selects eleven" was the sentence that motivated this topic and it is now true again for an entirely different and legitimate reason. `escalate` stayed `False` (11 within `MIN_NAMES` 8 – `MAX_NAMES` 15). This is also the presence-versus-outcome distinction the source guard was built around, exercised for real: the guard checks that the SOURCE carries every candidate, and a thin leg then flows through to selection and is excluded on merit with its reason recorded.
- **[[T0024]] is resolved by the same run.** The artifact carries a computed `spread_cap` record — `max_spread_bps` 10.0, `reference_notional_eur` 1400.0, `unevaluated_count: 0` — in place of the `pending-capture` placeholder every prior generation held, and all twelve symbols carry a numeric `spread_bps`, the BTC-quoted legs included at 1.178 and 1.842. Those legs read `null` while L2 capture was EUR-only, and since 2026-08-08 a null on either is the failure signal rather than the expected state. The cap **binds on nothing this run** (widest DOT/EUR 4.930 bps): a measured non-binding, not an untested criterion.
- **The intraday reach series cannot seam at this staleness, and say so by filename.** All twelve symbols' 240 and 60 minute series came back `detached` and are written as `240.detached.parquet` / `60.detached.parquet`, so a consumer globbing `240.parquet` finds nothing rather than a series with an 85- or 2,500-bar hole. The cause is structural and measured, not a defect this iteration introduced: Kraken's REST returns ~720 bars at every interval, which reaches back 719 days at 1440 but only ~120 days at 240 and ~30 at 60 — shorter than the 135-day canonical gap. Only the daily series bridged it, and the universe depends on the daily series alone. The operational consequence is a cadence requirement: a reach round must run inside those reach-backs to keep the intraday legs seamable, which is why the hub's 2026-07-22 set has a continuous 240 and today's does not.
- **The provenance guard was SPLIT rather than repointed.** `tests/test_universe_provenance.py` pinned the universe doc's basket hash directly against the v0 catalog table, on the assumption that the committed universe is selected from that table's series. This sitting deliberately made that false, so a single assertion tying the two could only have stayed green by freezing the universe on a retired dataset. It now pins two separate properties: the catalog's twelve rows must still reproduce the v0 basket hash against a literal — `data/ohlc` is gone from disk and from the NAS, so the table plus that constant are jointly the only surviving record of it — and, separately, the universe doc's cited hash must reproduce from the manifest of whichever set its own provenance names, skipped when that gitignored set is absent. The second property follows the artifact wherever the source moves next. Both halves were mutation-proven rather than assumed.
- **The legacy `universe/` fallback is retired in the same iteration**, its trigger having fired with the sitting. `resolve_universe_path` no longer returns the unstamped artifact — that directory cannot be updated through the additive transport, so it is frozen at 2026-07-07, and returning it would present a six-week-old basket as the current one. Absence of a stamped set is now fatal, naming both recoveries. **No host fetch was required, and that is measured rather than assumed**: the Dockerfile entrypoint expands `CAPTURE_PAIRS` into explicit `--pairs` flags and the Ansible compose always sets it, while the command reaches the resolver only when `pairs` is falsy — so the fleet never calls it, and the capture container mounts only its segments dir. Both couplings the remainder flagged were honoured rather than discovered late: `universe` stays in `authored_sets` and the directory stays on disk, because `push_hot` raises on a named set that is missing. The retired test was replaced, not deleted — the new one builds a READABLE legacy set and asserts the resolver raises anyway, the only shape that distinguishes retirement from a missing file, and it is mutation-proven against a restored fallback. Two surfaces moved with it: `UNIVERSE_RELATIVE_PATH` is removed as the change's own leftover, and the `--pairs` help text and the README Usage row — which both promised the fallback in operator-facing words — now say the command fails instead, in the same commit per `readme-usage.md`.
- **Closeout.** [[T0024]] and [[T0093]] both `resolved` and archived — T0093 with both halves discharged, the data gap by the sitting and the repo-side remainder by the retirement above. Decisions in `02.phase1-decisions.md`.

______________________________________________________________________

**Continuation — 2026-08-28: the corporate-action ledger's detection moves onto the operating surface.**

______________________________________________________________________

## 2026-08-28 — iter-151: T0025's trigger is retired rather than waited on, and building its record disproved a claim the ledger carried

- **The topic was parked on an event nobody was watching for.** [[T0025]]'s trigger — a universe pair undergoing a redenomination, quote-book migration or delisting — had no watcher: `/zcrypto-refdata-sweep` already rendered every fact needed to see two of the three, and its step 3 asked a human to "diff the rendered tables against the committed ones". A twelve-row table read by eye is exactly where a `NOT FOUND`, a `status` that stopped saying `online`, or a renamed altname gets scrolled past. Step 3 now **refuses** instead: `sweep_refusals` returns one reason per finding, each naming the pair *and* the observed value, because the operator's next act is deciding whether a corporate action happened and "something changed" cannot start that. All reasons are returned rather than the first — a batch delisting is announced as a batch.
- **The venue announces an asset delisting 93–116 days ahead, and nothing read that feed.** `scan_delistings` filters the same `scheduled-maintenances.json` the converge rules already fetch for scheduling ([[T0145]]'s lesson, applied one topic over) down to entries naming a selected asset. Same finding as the `AssetPairs` check, a quarter earlier. It searches the entry's **name, its components, and its update bodies** — the live feed's "Delisting assets for UAE clients" names its seven assets only in the body, no ticker in the title and no components at all. Bodies match **case-sensitively**: they are English prose, where `IGNORECASE` would fire `LINK` on "the link below" — a constructed counterexample, not one the live feed has yet produced. The lead time is a property of the entry **class**: the same filter catches funding-rail discontinuations, and the live "Arbitrum Nova Deposits and Withdrawals Discontinued" was published five minutes before its effective time, for a change already a day old.
- **Neither check fetches, deliberately.** The feed is the caller's to supply. A module that fetched would put a live venue endpoint inside the suite, where it is a flake source and skips silently if the venue blocks the runner — and a silent skip on the routine that gates the go/no-go reads as coverage.
- **Both are proven the only way that counts: a constructed defect on real data, with an unmutated control that must stay silent.** The archived 2026-08-04 snapshot (12 selected pairs) refuses nothing; delisting BTC/EUR, flipping SOL/EUR to `cancel_only`, and renaming DOGE's altname each produce exactly one refusal naming that pair. The live 50-entry feed against 13 selected assets — the Kraken spellings `XBT` and `XDG` included — produces no hits, which is correct, and a real entry renamed to name SOL produces exactly one. The clean case is a test of its own: Kraken delists constantly and almost none of it is ours, so a scan that flags everything is one nobody reads.
- **The point-in-time record was never event-gated at all**, and had been waiting on a trigger for no reason. `docs/reference/symbol-corporate-action-ledger.md` now carries per-pair first and last bar for all twelve pairs, with the first labelled a **coverage floor** rather than a listing date and the last flagged as the quarterly freeze rather than evidence a pair still trades. The table also states plainly that it *cannot* show the failure it guards — a delisted pair leaves the dump entirely — so detection is named as the sweep's job rather than implied to be the dump's.
- **Building it disproved a claim the ledger had been carrying.** It said the DOT 1:100 redenomination was "not in our price history". `DOT/EUR`'s first daily bar, 2020-08-18, reads `open=29.0000 high=45.0000 low=2.5100 close=2.6095`, and at 60 m the whole transition sits inside the **first hourly bar** (`17:00 open=29.000 high=45.000 close=2.750`) with every later hour trading 2.5–3.4. **Why nothing caught it is the transferable part**: `price_discontinuities` tests bar-over-bar *closes*, so a corporate action completing within the first bar of a listing leaves no ratio to flag **at any bar size**. The audit is not buggy; it is structurally blind to that class. Every newly admitted pair now gets one hand read of bar one.
- **What is measured there, and what is not — the review's sharpest catch.** The first draft called the bar-one prints "pre-redenomination (old DOT)" and said a reader gets "a price in a different unit". Every figure was right and the attribution was an inference presented as measurement: a 1:100 old-unit price would be **~261 EUR** (2.6095 × 100), while the observed 29–45 is **11.1×–17.2×**, matching no clean denomination. Worse, "different unit" arms a repair — rescaling by 1/100 turns 29.00 into **0.29**, an order of magnitude below the true price and worse than the artifact. The bullet now separates the measurement from the mechanism and says **never rescale**: exclude or distrust bar one, which is the safe handling under every explanation.
- **One trigger leg is watched by nothing, and says so in three places.** A quote-book migration is reported by no endpoint, so there is nothing to compare. It is named an accepted gap in `cli/snapshot/register.py`, in the sweep's step 3, and in the topic's resolution — a decision with its reason, not an oversight to be rediscovered.
- **Review also caught a step that could not run and a false-clean it invited.** The sweep's step 3 referenced `snap` from step 1's fenced block with a shell command between them; each block is its own interpreter, so it raised `NameError`. The repair that invites — rehydrating "the snapshot" by hand — can load an older archived file, and a clean verdict against last month's data is a false clean on the one routine that gates the go/no-go. Step 3 now rehydrates from the newest `data/snapshots` file and prints its `fetched_at`, so the operator sees which snapshot was judged.
## 2026-08-29 — iter-153: T0037's residuals get detectors, and the guard class gets a guard (spec `00103`, [[T0037]] partial)

- **The parked residuals could never have fired their own trigger.** T0037 held three accepted residuals behind `ripe_when: either accepted residual is OBSERVED in production`. Measured: nothing observed either. `rows_held_total` has a structural baseline (73/day/host — holding is the normal path at every boundary), `rows_quarantined_total` watches the never-confirmed path that neither residual takes, the late-event drop is a bare `logger.warning` the rollout skill documents as healthy, and `node_timex_offset_seconds` was not shipped at all. The topic was parked on a condition its own instrumentation could not deliver.
- **Residual (b) is invisible to every clock-referenced counter, and that is provable.** An earliness measurement is taken with the same leading clock that caused the truncation, so the clock subtracts its own lead back out — a 30-min-leading host measures its truncation as −26 min. An earlier design split the counter into two bands and assigned the upper one to (b); that band has no reachable state, because `append()` holds anything above the confirmed hour and `observe()` clamps every witness at `now + MAX_TS_AHEAD`. One counter, and the clock-skew signal is (b)'s sole detector.
- **The clock is read on the host, not in the container.** Reading `node_timex_*` inside Alloy needs `adjtimex`, which Docker's default seccomp gates behind `CAP_SYS_TIME` — a capability that permits SETTING the clock. Granting it to the metrics shipper on hosts whose correctness argument is that the clock cannot be trusted would widen what can move the clock in order to watch it. A host-side chrony timer writes into the existing textfile mount instead: no capability, no compose change, and the seccomp question three reviews split on never has to be answered.
- **A stale `.prom` reads healthy forever**, so `zcrypto-capture-clock-exporter-stale` watches the exporter's own mtime. Recorded rather than closed: a DELETED file still evades both rules, because the series vanishes rather than stales and `max by (host)` simply drops the dimension.
- **Substring selectors over hand-edited config, found in wave after wave.** `"needle" in <file text>` matches a comment, a `fail_msg` or a suffixed value. One instance was blind on the LIVE TRADE PATH: `test_engine_execgate.py` asserted a record path was in the engine role's text, and a `fail_msg` naming that path satisfied it while the real `lookup()` could point anywhere. Every hand sweep missed live cases, so the fix is an AST guard with its own tripwire — and three review rounds then found the guard itself evadable three more times, including by the exemption added to fix the previous round. Residual gaps are recorded in the guard's own docstring.
- **Two false justifications, both mine, both caught by review.** A `config-selector-ok` marker claiming the next line was the real assertion (it pinned a different file), and a grandfather rationale claiming rendered output has no human-written comments (a j2 template's comments survive the render). A suppression with a wrong reason is worse than none, because it reads as considered.
- **T0037 stays `partial`.** Every detector exists only in the repo and has emitted nothing in production. Archiving would delete the only written record of the residuals while their signals are unproven; it closes after the attended rollout and the by-value reads.

## 2026-08-28 — iter-152: the NAS pull's verify cost, bounded and observable (spec `00102`, [[T0028]] resolved)

- **The skip test is rsync's own itemization, so knowing what changed costs no probe at all.** `_run_rsync` runs with `--out-format='%i %n'` and returns an `RsyncOutcome` naming the `*.parquet` files it actually received: a line is a transfer iff its itemize flags begin `>f` **and** its path ends `.parquet`, which excludes directory lines, deletions, and — the one that would have made every cycle look like a full transfer — the attribute-only touch this pull's own `--chmod=D0775,F0664` produces on every run. The alternative was a checkpoint store probing each sidecar or `stat`ing each file (spec `00078`'s pattern, in this same package); it was declined because those probes are themselves O(total files) per cycle, shrinking the constant while keeping the growth term this work exists to remove.
- **Only the HASH is narrowed. The walk is not, and that is the load-bearing invariant.** `verify_tree` still `rglob`s and counts every final, because `newest_ts` feeds the pull-lag figure `infra/nas/pull-entrypoint.sh` calls its dead-man signal: narrowing the traversal to transferred files would make `newest_ts` `None` on a cycle where nothing arrived — the freshness signal going blank exactly when nothing is arriving, the condition it exists to detect — and would narrow `prune_stale_parts` along with it. The guard is a fixture where the defect and the fix differ rather than a degenerate one: four finals with one transferred, asserting `checked=4, hashed=1` and `newest_ts` at the tree's newest hour rather than the transferred hour — a whole-sweep implementation reports `hashed=4`, one that narrows the walk instead of the hash reports `checked=1` with `newest_ts` at the transferred hour, so the fixture separates the fix from both defects at once. A narrowed scope handed no rotation slice raises instead of defaulting silently.
- **The rotating 1/24 slice is keyed on the loop's own cycle counter, never the clock.** A final belongs to `sha256(<root-relative posix name>)[:8] % 24` and is re-hashed in the cycle whose rotation index matches — no cursor, no persisted state — and it is the only detector for bytes that change without rsync itemizing them. The obvious key, `now.hour % 24` (the shape spec `00062` uses), is wrong for *this* loop: the period is `interval + work` and therefore drifts, so the sampled hour walks, and whenever the drifted period divides 24 h a fixed subset of slices is never visited at all. The counter lives in the entrypoint's memory, so the every-24-cycles guarantee is a claim about an uninterrupted run; a restart merely revisits the low slices sooner.
- **The cost is published per channel as three gauges** — `zcrypto_archive_pull_verify_seconds`, `zcrypto_archive_pull_files_hashed` and `zcrypto_archive_pull_files_walked`, written through new `--textfile`/`--channel` options into **one file per channel**, because the five verified pulls share a single textfile directory and would otherwise clobber each other's series. `files_walked` is the denominator that makes `files_hashed` readable; the pair is what will distinguish "the cut worked" from "the archive stopped growing". A channel that stops running deliberately leaves its last values in place rather than vanishing, so `node_textfile_mtime_seconds` — admitted to `infra/nas/config.alloy`'s keep regex in the same edit as the new family, and pinned in `NAS_REQUIRED` so deleting either entry fails the suite instead of going dark — is what makes a stopped channel read as an ageing mtime rather than as a gap that looks like a zero. Two panels on the data-integrity dashboard chart the family, and the `pull complete` line gains `verify_s=`/`hashed=` as the record of last resort when the process is killed before it can publish, with `failed=%d` kept verbatim in place because the dead-man rule matches that token.
- **One image serves both legs, because the scope is a deployed setting rather than a build.** `--hash-scope full|incremental` defaults to `full`, so every workstation and ops caller behaves exactly as before; the NAS value is `nas_archive_pull_hash_scope` in `infra/ansible/host_vars/nas/vars.yml`, rendered into `ARCHIVE_PULL_HASH_SCOPE`, with a role assert refusing any value the CLI would answer with exit 2. That turns the sequence into two converges of one image: leg A converges the new digest with the hash still whole and reads the baseline by value, leg B is a config-only converge on the running digest that flips the setting — and leg B's rollback is the same one-line flip, reviewed like any other committed file. `--slice` is required with `incremental` and validated to `[0, 23]`: a narrowed hash with no safety net is a usage error, never a silent default.
- **What is NOT yet true: no converge has run, no baseline exists, and nothing has been measured on the NAS.** `nas_capture_image` is deliberately unchanged — the image carrying this code does not exist until CI builds it from the merge — so what landed is the instrument and the switch, not a cut cost. The "75,806 parquets re-hashed in one cycle" this work is built on remains what it always was, a single observation from a pull log rather than an instrumented measurement, and every horizon estimate on the topic stays unreliable until the metric reports one. The two imperatives that deploy needs at execution time — both mistakes made today from the rule as it stood — are live on the deploy skills rather than on `.claude/rules/fleet-deploys.md`, whose condensation moved the per-tier mechanics out and left it one NAS line, the `-compat` invariant. `.claude/skills/zcrypto-rollout-image/SKILL.md`'s `## NAS converges` carries the NAS-specific one: there is no `nas_alloy_digest` — the pin is `nas_alloy_image` — and the role deploys `infra/nas/config.alloy` unconditionally but restarts Alloy only under `-e nas_apply_compose=true`, without which a converge renders files and restarts nothing. The `## Shared converge mechanics` block duplicated into that skill and `zcrypto-bump-alloy` carries the generic one: a newly admitted metric family's first scrape is verified by VALUE, where `(no series)` reads FAIL.
- **Closeout.** [[T0028]] → `partial`, its remaining next-steps naming both legs and the coupling that forbids splitting leg A: the entrypoint is bind-mounted and re-read on any container restart, and an image predating this work answers `--hash-scope` with exit 2 — which the loop books as `capture_ok=0` and the ops writer reads as a not-clean capture pull, fail-closed until the dead-man pages. So the image pin and the new entrypoint land in one applied converge, and a render-only converge of this entrypoint against the old image is never run. No decisions-log entry is owed: this is pipeline engineering, not subject-matter research.

- **Measured on both legs, not modelled.** Leg A ran `full` under the new image for the baseline; leg B flipped one config value. Per channel `files_hashed` → `verify_s`: capture 28,171→1,175 / 169.978→13.583 s; capture_red 25,141→1,052 / 221.819→9.085 s; panel 14,028→590 / 13.402→3.989 s; liquidations 8,701→346 / 4.825→2.117 s; reconciled 456→20 / 0.499→0.147 s. **410.5 s → 28.9 s per cycle, 93.0%**, `failed=0` throughout, published gauges agreeing with the log lines to the millisecond. `files_walked` grew on four channels and shrank on none — the walk stayed whole, which is the invariant the cut could have broken. Every channel landed within 3.96–4.39% of the 1/24 = 4.167% slice target. Full re-verification wraps in ~28–30 h at the measured cadence (change-counting over 6 h, not a timed wrap).
- **Deployed on an interim image built from the feature branch, by explicit exception.** The two-leg measurement needed the new CLI on the NAS before the branch merged; the user approved it as a one-off and it is not a pattern to repeat. `docs/reference/fleet-pins.md`'s archive-pull row carries the constraint and the condition that retires it.
- **The branch disproved one of the spec's own deploy assumptions.** Spec 00102 reasoned that keeping `cli/archive/` outside the gate-export replay-fingerprint closure kept both converges out of a cold replay. The premise was true and the conclusion did not follow: the gate cache lives in the container's ephemeral `/tmp`, so EVERY converge that recreates the container replays cold — 3534 s and 3565 s measured on the two legs, against a 3505 s recorded cold export. Corrected in the spec, the plan, `fleet-pins.md`, `vars.yml` and the rollout skill. The `zcrypto-gate-verify` dead-man tolerated it (green throughout), so it is a cost to budget, not a page.

## 2026-09-02 — iter-163: the past-dated detector counted a benign restart, and its alert could not see it either way (spec `00109`, [[T0037]] still partial)

- **The ruling this supersedes named the harm correctly, and the code never implemented it.** Spec `00103` D5 specified a stamp *fabricating a committed final for a past hour*; what shipped was `self._oracle is not None and hour < _hour_start(_utcnow())`, which never tests whether the hour holds data and is therefore strictly wider than the condition it was written for. A restart satisfies the extra width whenever a stream had no event in the current hour before the stop: `close()` deliberately never finalizes the open hour, so `_recover` seeds no floor above it and the first replayed print re-opens it. Measured on `zcrypto-red` four seconds after that host's 2026-09-01 16:15:21Z capture re-pin — `first stamp opened a past hour pair=SOL/BTC kind=trades hour=2026-09-01 15:00:00+00:00`, with `15.part0000.parquet` already on disk for that hour and the replayed prints recognised from it and dropped. That is the T0026 reconnect replay the rollout skill documents as healthy, counted as a fabrication. D5's immunity claim — "no interaction with drains, replays or future stamps" — holds mid-stream, where the floor is `_current_hour`; it fails at the first event, where the floor is `_floor` and can sit an hour back. `00103` is not edited: a spec is immutable once written and a dated addendum would break any hash pinned to it, so `00109` supersedes D5 in its own file.
- **The predicate now asks D5's question — was this hour ever captured — and reads the disk to answer it.** `_enter_hour` counts only when the opened past hour holds no `.part` files. That is a **new, earlier read** than the one `_open_hour` makes three lines later to seed its de-dup set, not a reordering of it, and the ordering is load-bearing: `_open_hour`'s read runs *after* `_redeem_held` has renamed the hour's `.held` spills into ordinary parts, so sharing one read would make a `.held`-only hour look captured and blind the detector to the exact case the alert summary names. `.held` is deliberately not capture evidence — those are rows the oracle never confirmed, and an hour holding only them is about to be certified from rows nothing corroborated. A finalized hour has no parts either, and that does not widen the predicate: `_commit` unlinks them, but `_recover` seeds `_floor` at the newest committed final plus an hour, so such an hour's stamp is refused as a late event before `_enter_hour` is ever reached. The predicate is deliberately NOT widened to also stat `<HH>.parquet` — that conjunct is unreachable code guarding a door the floor already closes, and it would mask a future `_recover` regression instead of letting it fail.
- **Four new guards, and the existing positive re-pointed rather than merely kept green.** `test_t0037_restart_reopening_a_captured_hour_counts_nothing` builds the incident — oracle-bearing writer, mid-hour crash with parts on disk, first stamp one hour back — and asserts the branch really runs (`_current_hour` moves back) and counts 0; the whole existing T0026 replay family misses that branch by one constructor argument, using the oracle-less writer where production passes the oracle. `test_t0037_a_held_only_past_hour_still_counts` is that same fixture with the hour's `.part` swapped for a `.held` and must read 1 — the only test a predicate widened to accept any parquet as capture evidence fails, while a plain swap of the marker is killed by the negative test alone. `test_t0037_a_finalized_past_hour_never_reaches_the_counter` pins the floor argument the `.part`-only test rests on, asserting the late-event guard refuses the stamp and the counting branch never runs. `test_t0037_a_never_captured_hour_beside_unswept_parts_still_counts` pins the granularity no other fixture did: a glob dropping the `<HH>` prefix, or one walking the stream root the way `_sweep` legitimately does, sees an unswept crash hour's parts and reads 0 on the fabrication that shares its day. The pre-existing positive's target hour was already empty on disk — which is exactly the new discriminator — so it stays a true positive for the right reason, and its docstring now names the test that inverts it. `-k t0037` collects 30.
- **One rule could not report the event it exists for, and the reason is permanent rather than a timing artifact.** The counter's only step is bound to process start; scrapes are 60 s apart, so its reset to 0 is never sampled, every in-window sample is already 1, and `increase(...[6h])` returns 0 forever. The rule's own comment argued that shape as its justification — the counter advancing "at most once per stream per process" was offered as the reason `increase() > 0` was "the whole detection". The alert and panel 110's series now read `max by (host) (zcrypto_capture_ts_past_dated_hour_total{...})`, the form `zcrypto-capture-venue-not-online` already uses for its own process-lifetime counter, with the relative range cut from 21600 s to 600 s. Both costs are written onto the surfaces rather than left to be discovered: the rule is a **latch** no window clears — only the capture process restarting, or the host falling silent, which pages on its own — and `max by (host)` yields one instance per host, so a further step moves the measured value without changing a label and Grafana raises no second page. The runbook therefore records the standing value in the ops-journal entry the page produces and rules any later value above it a new event.
- **The by-value proof exists, is copied here because it is unrepeatable, and its second half is owed.** Taken 2026-09-01T19:2xZ with `infra/scripts/grafana-query.py`, before any re-pin: `max by (host) (zcrypto_capture_ts_past_dated_hour_total{host=~"zcrypto|zcrypto-red"})` returns `{host=zcrypto-red} = 1` where `increase(...[6h])` over that same series returns 0 — the whole of the proof that the replacement expression sees the event the old form cannot — with the silent control on the same host, `max by (host) (zcrypto_capture_hour_finalized_early_total{...})`, reading 0. The reading cannot be retaken: the re-pin the push is gated on restarts capture and clears the counter, so afterwards both hosts read 0 by construction and the discriminating value is gone. **Owed, and deliberately not written here**: both hosts' values under the absolute-value form after both re-pin — the both-read-0 confirmation and the two-host control. They do not exist yet, and a 0 predicted "by construction" and stamped with today's date would be a control asserted rather than taken. It is registered in [[T0037]]'s next steps, where the operator who takes the reading records it with its date.
- **The sibling counter was examined for the same defect and deliberately left on `increase()`.** `_count_if_early` has a second call site — the first statement of `_finalize_hour`, on `_enter_hour`'s ordinary rotation branch — so `zcrypto_capture_hour_finalized_early_total` steps at every genuine boundary on every one of the 24 writers whenever the clock lags at all, which the runbook sizes at up to ~576 counts a day per machine and makes the operator's own discriminator. That is precisely the counter `increase()` *can* read. Converting it would trade a self-clearing warning for a latch an operator can only clear by restarting live capture on the unbackfillable path, and would falsify the six-hour window its magnitude test and two of its runbook steps are built on. Its own residual is named and accepted rather than closed: a first-ever early close on a freshly-started process has its reset unscraped for the same reason, and that is one count of a signal whose detection is the recurring series.
- **`zcrypto-capture-rows-quarantined` is exposed to the same class and is consciously DROPPED here, not parked.** The reason is per increment **site**, not a claim about the counter as a whole. Its `close()` spills happen in a dying process with a 60 s scrape and no `stop_grace_period` override, so those increments are usually never published at all — and a value that was never scraped cannot be read by any expression, absolute or windowed. Its other site, `_hold`'s `flush_rows` cap, is on the live path, is scraped normally, and is not start-correlated, so `increase()` already reads it correctly. Absolute value therefore buys nothing at either site, which is what keeps it out of scope rather than any claim that the whole counter is invisible. Its real fix is a different decision — persist the count across restart, or widen the shutdown grace so the last scrape lands — and it is dropped rather than registered, so no reader mistakes this bullet for queued work.
- **The operator surfaces asserted a baseline the fleet had already broken.** `capture.md` said the baseline was "a hard zero over the fleet's whole life" and that the alert "has never fired"; the phone-read `summary` said "any step at all is a real event"; the runbook index tagged the entry "Hard-zero baseline"; panel 110's description repeated the hard zero. All of it was false while `zcrypto-red` read 1. The entry now carries that dated reading and what it is *not*, and names the four shapes a count can have: capture not running for the hour; a non-graceful stop that lost an unflushed buffer (there is no timed flush — `DEFAULT_FLUSH_ROWS = 5_000` — so a thin stream's whole hour can sit in RAM, and an OOM kill, panic or power loss takes it where `close()` would have written it out as an ordinary `.part`); an hour holding only a `.held` spill, whose evidence `_redeem_held` renames away silently within one rotation; and the genuine bogus stamp. Shapes 1, 2 and 4 leave the identical trace on the paged host's disk, so the peer comparison is promoted from a tie-breaker to the mandatory branch of the read, and what separates the fabrication from the two benign shapes is whether a stop, reboot or crash on this host's record accounts for the gap. `grep -rni "hard.zero" infra/` fell from six hits in four files to one — the sentence that now negates it.
- **The file's own generalisation about this failure class was wrong, and is corrected beside the rules it governs.** `alerts.yaml` had already diagnosed the class for `venue-not-online` and then generalised it to lazily-materialising labels only. It missed two more: an eagerly-published counter on a host **new to the metric** — today's case, the counter having shipped 2026-08-29 and reached `zcrypto-red` only at the re-pin, so its first-ever sample already carried the step — and a **start-correlated** step, whose reset lands seconds after start where 60 s scrapes never see it. `increase()` reads a counter only where a 0 sample precedes the step *inside* the window, and that needs all three conditions at once.
- **Nothing here is deployed, and the landing order now sits on the surface that performs the push.** The repo carries the narrowed predicate and the absolute-value rule; no host runs an image containing the predicate, and nothing from this branch has been pushed to Grafana. The order is a constraint rather than a step: land the predicate, re-pin **both** capture hosts to an image carrying it — verified **by digest**, never by reading the counter — and only then push. A value gate would pass inside the bake gap the canary rule forces between the two re-pins: only the secondary carries a non-zero counter, so "both read 0" is satisfied while the primary still runs the wide predicate, and one ordinary restart of it — a converge, a reboot, an OOM — would latch a CRITICAL on the capture pair, the one place a false page is most expensive. `.claude/rules/fleet-deploys.md`'s alert-rule lifecycle now carries that bullet: a check computable from repo state alone (each capture row's build revision in `fleet-pins.md`, then `git show <revision>:cli/capture/segment_writer.py` grepped for the predicate), a freshness qualifier because a hand compose re-pin re-trues no pins row and a stale row would pass for a host that would fail, an escape hatch naming by subject the single commit whose revert takes the rule, its unit, its horizon, its latch sentence and its panel series back together, and its own retirement condition. Checked at closeout: both capture rows read revision `eb6a503a`, which does not carry the predicate — the gate is closed, as intended.
- **A second, unrelated defect landed on the same branch: the Alloy config the roles left behind.** The pre-`conf/` layout left a `config.alloy` one directory **above** the deployed one on every Alloy host. Nothing mounts it — the container binds `conf/`, not its parent — so it is inert, and it is not harmless: measured 2026-09-01 on all four hosts, a stale file at the path an operator would *guess* answers a wrong-path guess with plausible data instead of an error, and it had already produced a confidently-wrong "three metric families are dark" report against a host whose config was in fact current — disproved by value afterwards, all three returning series at `offset 6h`, hours before the converge in question. Removed in the capture, ops and NAS roles rather than by hand: a hand `rm` is neither idempotent nor recorded, and a freshly provisioned host would grow one again. **The first placement would have removed nothing** — on capture and ops it landed inside the `<role>_alloy_digest is defined` block, which every converge carrying no digest skips, which is every ordinary one and precisely the three hosts holding the orphan. Hoisted to top level beside the deployed-config drift check, which sits outside the same gate for the same recorded reason, with the explaining comment moved to travel with the task.
- **Closeout.** [[T0037]] stays `partial`: its three residuals are unchanged and remain detection-based, and only the past-dated detector's predicate is repaired here. Its `ripe_when` is rewritten from `00103`'s order — deploy, read by value, push — to `00109` D7's, and its `## Done so far` no longer claims the detectors have emitted nothing in production. No decisions-log entry is owed: this is instrumentation and deploy engineering, not subject-matter research.

______________________________________________________________________

## 2026-09-03 — iter-167: the fourth venue outage gets its provenance row (spec-less)

- **`docs/reference/capture-era-data-hygiene-map.md` gains the 2026-09-03 row, and the retention clock is why it could not wait.** `infra/runbooks/ops.md`'s `undetermined` triage ends "if the venue announced it, write the event's row in this map — that map, not this counter, is the durable answer to 'was this hour ours?'". The venue announced it 41.4 h ahead and the hour booked `undetermined` at the 09:12Z tick, so the row was owed; `zcrypto_capture_venue_status_total` holds 14 days, Loki reaches back only to 2026-08-20, and both hosts' counters zero at the next capture restart. The 2026-07-27 row records what happens otherwise — it "went unrecorded here until 2026-08-21 … nothing prompted a row".
- **Every figure is read from its source.** Fleet data-gap span `07:01:01.946946 → 07:04:00.668272` (the ledger record's widest `stream_windows` extent), beginning 1.95 s after the published start and inside the 1.1–4.3 s the earlier episodes recorded; **2,134.703242 s** of `both_streams_silent` across twelve streams, 177.892 s each; verdict `undetermined`; `continuity.py` agreeing at 2,134.7 gap-seconds with **0 truncated and 0 missing hours** on the primary's and the secondary's pulled trees alike, which give that total to 0.1 s while their per-pair row counts differ — cross-host agreement rather than one tree read twice.
- **Three claims in the first draft were wrong in the same way, and the review that caught them is the reason the row is trustworthy**: figures sourced from the capture daemon's log wearing the archive's vocabulary. The header's end overstated the dark window by 28.3 s; the ~28 s difference was explained as a booking threshold, which the ledger disproves (the per-pair `stream_windows` seconds sum to `residual_seconds` byte-exactly, so nothing is trimmed); and the single-window shape was attributed to 2026-08-06, which books two. The refusal reads the fleet-dark INTERSECTION, not the per-stream windows — `cli/archive/command.py` builds one and passes it — and 2026-08-06 carries one window on eleven of its twelve streams while still having two in the intersection, which is why it falls out for want of interior updates instead. The matching shape is **2026-07-27's**, two rows above.
- **One difference is recorded as unexplained rather than given a cause it cannot have.** The daemon's `gap end … seconds=` lines read 205.503–206.033 s per stream against the archive's ~178 s. Receipt-versus-payload stamping is the right direction, but the daemon documents its close-side imprecision as bounded by one 5 s tick, so it does not account for ~28 s. An honest open question in the durable record beats a mechanism that cannot produce the number.
