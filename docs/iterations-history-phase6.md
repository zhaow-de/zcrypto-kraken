# Iterations history — Phase 6 (Execution Engine, Paper Trading & Go-Live)

Per-iteration changelog for Phase 6. Appended at each iteration's close-out; see `.claude/rules/prose.md`.

## 2026-07-10 — iter-079: Phase-6 kickoff — adapter verified, key ceremony, go-live pre-registered

- **Phase-6 interactive kickoff executed** (spec `docs/specs/00039-phase6-kickoff-design.md`, attended): all §12 blocking items decided — **engine = NautilusTrader made fully functional** (human's direction; thin-custom stays strictly a fallback, auto-trigger restricted to §7's order-semantics/reconciliation failure class), **host = D2 VPS** (engine as its own resource-limited compose service; §8 hardening verified before the key lands = an iter-081 gate), **trade key activated now** (D3(ii) first half), **D3(iii) committed** (the 6b sleeve trade plan includes ≥1 margin long + ≥1 margin short across ≥2 rollovers, one closed one settled, within the first two 6b weeks), and **go-live criteria pre-registered** (6a concordance gate with journaled-input replay semantics; 6b tracking band; §12's model-consistency criterion with the return-to-Phase-5 hatch; ramp as per-step D3(vi) human decisions, >25 % gated on T2-pass-or-T3-fallback). Decisions log `[iter-079]` (8 entries).
- **Adversarial spec verification before commit** (3-agent pass vs master plan / repo rules / release source) caught pre-registration-poisoning issues, decisively: **the adapter exposes no AddOrder `validate` param**, so the ratified validate-only sweep was unexecutable — substituted a zero-fill sweep pre-run with the human's sign-off; the go-live gate had dropped §12's model-consistency criterion — restored.
- **Key ceremony done** (guided): least-privilege trade key (query + orders + WebSocket; no withdrawal/export ever), IP-allowlisted (VPS + workstation as a verification-window-only exception), stored ansible-vault-encrypted in `group_vars/capture_host/vault.yml`, verified by an authenticated Balance read.
- **§7 adapter verification: PASS on all six probes** (memo `docs/reference/adapter-verification/1.230.0.md`, mdformat-allowlisted): auth/account, reconciliation, WS data (10/10 pairs first-tick at 2.0 s), zero-fill order semantics ×4 incl. **margin short at leverage 2 accepted-rested-canceled**, the ~€10 real round-trip (fill → reconcile → close → flat; total verification cost €0.1618), post-run reconciliation clean. **NautilusTrader 1.230.0 confirmed as the Phase-6 engine**, exact-pinned with a version-bump tripwire test (`tests/test_nautilus_adapter.py`, suite 956 green).
- **Build findings recorded for the shadow-engine build (renumbered to iter-081 — the 00038 trial took iter-080)**: XBT→BTC normalization (`<BASE>/EUR.KRAKEN` IDs); crossing post-only surfaces as accept-then-venue-cancel (never `OrderRejected`); `spot_account_type=MARGIN` reports TradeBalance equity, not wallet balances; adapter nonces outpace millisecond-nonce sidecar scripts; observed taker fee exactly **0.80 %/side** (vs modeled 0.6 %) → appended to T0014's findings for the calibration pass.
- **Topics**: T0018 opened (Phase-6 build sequence + cross-iteration constraints — cadence-agnostic engine keyed to the 00038 verdict, §8-hardening-before-key gate, full 6b-executor scope); T0005 consciously re-deferred to Stage 6b (trigger refreshed). Key-allowlist closure (workstation IP removal) enforced via the iter-079 PR checklist.

## 2026-07-10 — iter-082: the record-44 builder + concordance core (the Phase-6 engine's computational heart)

- **The deployable system is now committed, tested code** (spec `docs/specs/00040-crossfreq-builder-design.md`, attended design + three-agent adversarial spec verification that caught, pre-implementation: the exact raw-vs-ffilled **feed map** (the B sleeve consumes raw prices — Nones drive basket disqualification), the **newest-row contract** (without synthetic boundary/dummy-close appends the forming interval's B/A1 targets silently zero — the row the engine trades), and the **no-peek gap** (replay on a contaminated snapshot reproduces the same wrong target, so the journal needs hard boundary invariants). Decisions log `[iter-082]` (gate restated to 4h semantics, human-ratified; two-iteration scope; `zcrypto engine` naming; vectorize-now).
- **`build_crossfreq_system`** (`cli/portfolio/crossfreq_system.py`): the verified path — record 44's full construction with frozen-figure regression tests pinning every registered figure (Sharpe 1.5609/1.5583, maxDD 13.57 %/18.66 %, cap-breach 1,318, governor-engaged 7,302, five sleeve anchors, dataset-extent guard first: "canonical dataset drifted — STOP"). Emits one extra **newest row** (the forming 4h interval's target) under a three-part no-peek/no-drop invariant test. Build time 111.5 s full history.
- **`build_crossfreq_system_fast`**: **bit-identical** (dataclass-equal), **~58×** (1.9 s vs 111.5 s) — reproduces CPython's exact rational arithmetic for the hot rolling statistics (big-integer window sums finished by the stdlib's own correctly-rounded square root) and vectorizes only order-insensitive arithmetic. Review verified the math against the CPython source, proved the −0.0 accumulator invariant, and ran 3,000+ adversarial bit-identity probes incl. subnormals and None-bearing grids. numpy promoted to a direct dependency.
- **`cli/engine/` (library package)**: the journal contract (schema v1; byte-pinned snapshot content hashes — int64 LE epoch-seconds + float64 LE with None→NaN, one shared helper; **no-peek boundary invariants enforced on metadata AND reconciled against the data actually read** — the review's daily-grid catch), `replay_cycle` (hash-verify → boundary-check → build → newest row located by `cycle_ts`), `compare_targets` (1e-6 absolute, structural fail on asset-set mismatch), `evaluate_gate` (the ratified 4h gate: complete-UTC-days universe, 6 cycles/day, 30-min inclusive deadlines, ≥ 14 consecutive clean days, duplicate-cycle guard). 59 synthetic tests, no dataset dependency.
- **Docs**: runbook gained the record-44 deployable-system section (construction, figures, reproduction, the multiplier-transition P&L disclosure, the equivalence-evidence discipline); record-33 spec marked historical; CLAUDE.md's stale "no CLI subcommands" parenthetical fixed. Suite: **1,064 passed** (from 957). Next: iter-083 — the node (TradingNode, scheduler, price store + REST append before the ≈ Jul 25 window in T0018, journal writing, `zcrypto engine` CLI).

## 2026-07-10 — iter-083: the shadow node — store, cycle, node, CLI; the workstation soak is LIVE

- **The shadow engine runs** (spec `docs/specs/00041-shadow-node-design.md` + its execution amendments, plan `00041`; four TDD subagent tasks, each adversarially reviewed with fix loops): the **live price store** (`cli/engine/store.py` — seeder with two distinct seam guards incl. the window-shortfall error naming the OHLCVT fallback, in-progress-candle drop rule, poisoned-tail repair via re-seed; PAIR_KEYS transcribed + independently re-verified against the snapshot register), the **cycle core** (`cli/engine/cycle.py` — settle-verify across BOTH grids after the review caught the midnight-boundary gap, raw-series staleness before union-alignment, journal-relative snapshot paths with the shared hash helper, intended orders vs the last successful targets, success records vs failed-cycle sidecars, aware-UTC pinned), the **node wrapper** (`cli/engine/node.py` — restart-inside-a-passable-window startup arithmetic, alert-chain-scheduled-before-cycle, the iter-079-verified adapter shape with reconciliation explicit and the exec client behind `exec_enabled`), and the **`zcrypto engine` CLI** (seed/run/cycle/replay/report; report = replay-on-demand with honest error classification; `zcrypto --help` stays nautilus-free via a lazy PEP-562 re-export). Suite **1,150 passed**.
- **The attended soak started, all evidence clean**: `engine seed` filled the April→July gap inside the REST window (20/20 series seam-QA pass, overlaps 620 daily / 116 4h bars, 7,040 bars appended, 0 divergent — the ≈Jul-25 window race is over); the **first live cycle (2026-07-10 20:00 UTC) journaled in ~8 s** (20 snapshots, 8 intended orders ≈ €34 gross on the €1,000 shadow book — the low-exposure regime as expected); **fast AND verified-path replays both bit-exact (worst |Δ| 0.00e+00)** — the oracle agrees with the live cycle on live data; `engine report` scores the incomplete day correctly (streak 0, no failures). The systemd user service is **active** (linger enabled); the node detected the already-journaled 20:00 boundary and scheduled 00:00 UTC as its first autonomous cycle. **Dev evidence only — the pre-registered gate clock starts on the VPS (iter-084).**
- **Fold-ins ridden by this branch**: the dotenv credential path eliminated (`.env`, `.env.sample`, dead `cli/secrets.py` + tests, the gitignore line — the gpg/sops/ansible-vault path is the sole secrets mechanism); the legacy `runs/` placeholder purged (engine dirs live under `data/`, gitignored by construction); README `## Configuration` promoted to top level; CLAUDE.md's CLI-subcommand parenthetical and suite-size line refreshed.
- **Topics**: T0018 updated — store seeding DONE with the VPS options enumerated (REST re-seed / rsync the warm store / OHLCVT dump), journal disk sizing (~0.3 GB/month, pruning explicitly dropped for Phase 6) as an iter-084 input, and the tz-caveat corrected to the pinned aware-UTC convention. Next attended session: **iter-084 — VPS deployment** (ansible `engine` role + compose, §8-hardening gate before the key, `exec_enabled` on, the Stage-6a clock starts).

## 2026-07-11 — iter-084: the engine on the VPS — the Stage-6a gate clock is ticking

- **The shadow engine runs 24/7 on the D2 VPS** (spec `docs/specs/00042-vps-deployment-design.md`, revised by a three-agent adversarial pass that pre-caught: the sync channel that didn't exist, a world-readable trade key, the silent-zombie node, host over-commit; plan `00042`; two TDD subagent tasks + the attended deployment): one image serves both services (compose entrypoint override; workflow path triggers extended to `cli/**`), deployed by digest beside the untouched capture stack.
- **Code additions** (`cli/engine/`): the dead-man's switch (`HEALTHCHECK_URL` ping after record/sidecar; a propagating exception deliberately pings nothing — silence alerts), fail-fast `engine run` (`ZCRYPTO_REQUIRE_CONFIG`, store presence) + a supervision watchdog (`timeout_connection + timeout_reconciliation + 30 s`, force-exit on a never-running trader — a bad key becomes a visible crash loop, never a silent zombie), and `--journal-dir` overrides on replay/report.
- **The `engine` ansible role**: the §8-hardening gate as hard asserts BEFORE any secret renders — and it earned its keep by failing honestly: the `allowusers` assert could never match because assert-expression Jinja unicode-escapes `\b` to backspace; the live host was compliant all along. Fixed (`\\b`), made self-diagnosing (fail_msg prints the effective posture lines), plus first-run check-mode guards. Secrets land only in `engine.env` (`0600 root:root`, `no_log`, `diff: false`); the warm workstation store delivered only-when-absent + read-verified; resource budget capture-first (engine cpus 0.75/mem 1g/shares 512); rrsync read-only forced-command journal channel on deploy's authorized_keys.
- **Deployment evidence**: full-site `--check --diff` with both digests showed zero capture drift (capture healthcheck up throughout); converge ok=31/changed=13/failed=0 at 22:19 UTC; first-start verification green (effective-config line `exec_enabled=True`, exec client authenticated + **reconciliation succeeded** — equity 99.84 EUR, the two iter-079 probe fills recognized; egress 172.105.64.43 proven against the key's IP allowlist by the successful private-WS auth; no watchdog exit).
- **The Stage-6a gate clock started: first VPS cycle 2026-07-11 00:00 UTC, completed 00:01:39** (99 s into the 30-min window), **verified-path replay bit-exact (worst |Δ| 0.00e+00)**, the `zcrypto-engine-shadow` healthcheck armed by that cycle's ping (n_pings 1, timestamp matching `completed_at` to the second). Attended first pull over the rrsync channel clean (after adding the load-bearing `IdentitiesOnly=yes`); daily gate-ops user timer installed (06:30 UTC: pull → verified replay of UTC-yesterday → report; attended run `Result=success`). Both soaks continue until the 6a exit review.
- **Ridden by this branch**: the Grafana Cloud observability spec + plan authored and parked (spec/plan `00043`, T0020 — user-directed scope incl. the legacy container-log logrotate removal; three-agent adversarial pass folded 14 findings); `.ansible/` gitignored.
- **Topics**: T0018 synced (Done-so-far gains iter-084 with the gate-clock evidence; remainder trimmed to the 6b executor; `ripe_when` → the gate being met); **T0021 registered** (VPS journal retention, ~0.35 GiB/month measured, `ripe_when` = the disk watermark via T0020's alert); T0020 registered earlier tonight.

## 2026-07-12 — iter-093: the three-tier topology — Role A (the always-on NAS pull/archive) is LIVE

- **New topology (spec + plan `00048`)**: the durability path moves off the intermittently-online workstation to a **three tiers** — VPS producer (24×7) → always-on **NAS ops node** (Synology, Docker) → intermittent workstation. Failure-domain reasoning: the VPS is uncorrelated with the home ISP; the NAS+workstation share it (≤14 h outage) but the VPS 7-day ring buffer covers that with ~12× margin. Three roles, built incrementally: **A** pull/archive (this iter), **B** gate-verify replay, **C** redundant capture. Human gates (scope A+B+C, RAID-5 durability, reboot-window move, keep-Grafana, uid/gid 1000, plain-POSIX perms) were cleared one-by-one.
- **Role A code** (`cli/archive/`): `zcrypto archive pull SOURCE DEST` — rsync-over-ssh then a manifest hash-verify sweep (`verify_tree` walks `*.parquet`, skips in-progress `.part*`, checks each against its `.sha256` sidecar; a malformed/missing sidecar counts *failed*, never crashes) + a `pull_lag_seconds` dead-man (age of the newest verified segment). **Distinct exit codes**: 2 = transport failure (incl. a missing `ARCHIVE_SSH_KEY`) — a partial pull is never authoritative; 1 = hash mismatch; 0 = all verified. `--no-verify` for sidecar-less sources (the engine journal). Fail-closed ssh: `StrictHostKeyChecking=yes` + a pre-seeded read-only `known_hosts`, `IdentitiesOnly=yes`, `CheckHostIP=no`; `--chmod=D0775,F0664` forces the mandated perms (plain-POSIX share = no ACL inheritance).
- **NAS stack** (`infra/nas/`): a standalone `archive-pull` container under Container Manager (`restart: unless-stopped`, no systemd / no NAS-OS config), `user: "1000:1000"`, an in-container scheduler (`pull-entrypoint.sh`, SIGTERM-clean, hourly default) that runs the capture pull always and gates the journal pull on `JOURNAL_SOURCE` (**capture-only** in Increment 1 — the journal has no sidecars and needs a distinct key). Plus an idempotent plain-POSIX `normalize-archive-perms.sh` (0775/0664, group `zcrypto`).
- **Two-variant image ([[T0029]])**: the NAS Atom C3538 (Goldmont) has **no AVX**, so the standard `polars` wheel crashes `Illegal instruction`. Resolved with a `POLARS_RUNTIME` build arg + a 2-entry CI matrix (`.github/workflows/capture-image.yml`): the **default** multi-arch/AVX `polars-runtime-32` image for the VPS is **runtime-untouched**; an amd64 **`-compat`** build swaps to `polars-runtime-compat` (same polars 1.42.1 — `polars-lts-cpu` is deprecated, `rtcompat` is the maintainer's replacement). The shared image also gained `rsync`+`openssh-client` and a fixed **uid-1000** default user (ssh's `getpwuid` exits 255 otherwise) — both inert on the VPS (its compose overrides `user:`; capture/engine don't rsync).
- **VPS (`capture` role)**: a read-only `rrsync -ro` forced-command channel for the segment pull on a **separate** `sync_capture` key (vaulted private, public in group_vars); rsync installed; `deploy` added to the `kraken-capture` group; unattended-upgrades reboot window **04:00 → 02:00 UTC**. Converged with `--check` showing zero capture drift.
- **Deploy evidence (attended)**: NAS container Up on the `-compat` digest; **first pull `checked=1962 ok=1962 failed=0`**, 2.0 GB archived to `/volume1/ZhaoCrypto` owned `1000:1000` at `0664/0775`, host key pinned (`[zcrypto.zhaow.me]:10022`), `lag_s≈6203 s` traced to a real finalized+verified hour-14 segment. Dry-run first proved the rrsync channel end-to-end (jail enforced, 4131 files).
- **Reviews**: whole-branch (Opus 4.8) → READY TO MERGE, then its 3 substantive Minors folded in (exit-2 on missing key, fail-closed ssh, doc accuracy) and re-reviewed (Sonnet 5) → APPROVED.
- **Topics**: **T0028 registered** (Role A re-hashes the whole unbounded archive each cycle → O(archive), `ripe_when` ≈ 250–600 GB); **T0029** partial (AVX resolved; residual = Role B cross-runtime replay determinism); **T0003 synced** (Role A supersedes the workstation-pull half; remainder = alerting drill + ≥7-day clean-run + Roles B/C).

## 2026-07-12 — iter-094: the three-tier topology — Role B (NAS gate-verify + telemetry) is LIVE

- **Determinism resolved by measurement (T0029).** Before building, the pivotal residual — can the NAS's no-AVX `polars-runtime-compat` reproduce the VPS's AVX-computed gate verdict? — was measured against the 8 VPS-journaled cycles on the **real Atom NAS**: the **fast path (the gate path) is bit-identical cross-runtime, `worst |diff| 0.00e+00` on 8/8**, matching the AVX control; the verified path carries inherent ~`1e-18` same-runtime float noise and is too slow on the Atom, so it is dropped from the NAS loop. The exact-rational big-integer builder is a genuine cross-runtime *portability* property. Role B scores the gate on the fast path.
- **`zcrypto engine gate-export`** (`cli/engine/command.py`): the machine-readable sibling of `report`. Extracts report's evidence loop into a shared `_evaluate_journal`, then emits an atomic Prometheus textfile (`zcrypto_gate_status/streak_days/journal_pull_lag_seconds/mismatch_total/export_timestamp_seconds`) + an **independent healthchecks.io dead-man** ping. `clean` is based on gate *health* — `streak > 0 or last_failure is None` (captures every break reason incl. missing/late cycles, doesn't false-page pre-MET or on a recovered gate), not on a counted subset. Exit 0 on a finding; non-zero only on an operational failure.
- **Journal pull** (extends Role A): the entrypoint selects the ssh key **per call** (`CAPTURE_SSH_KEY` vs `JOURNAL_SSH_KEY`, no `command.py` change), reusing the existing VPS `sync` rrsync channel; the `gate-export` step runs after each journal pull (`JOURNAL_SOURCE`-gated).
- **NAS Alloy telemetry stack** (`infra/nas/config.alloy` + two Container-Manager compose services): host metrics via the unix exporter (**load / netdev / disk-free**) + the gate `.prom` via the textfile collector + archive-pull logs → Loki, all `remote_write`/`loki.write` to Grafana Cloud; a GET-only `docker-socket-proxy` fronts Docker.
- **One canonical dashboard** (`infra/grafana/zcrypto-dashboard.json`, uid `zcrypto-main`) + alert rules (`infra/grafana/alerts.yaml`, correct `ProvisionedAlertRule`/`expression` schema) + `infra/scripts/grafana-push.sh` (per-rule provisioning API) — namespaced so T0020 later adds the VPS rows to the same file.
- **Retired the workstation gate-ops timer** (`infra/systemd/zcrypto-engine-gateops.*` deleted; README section retired with disable steps).
- **Subagent-driven execution + adversarial review caught 11 real bugs** across per-task, a 3-reviewer whole-branch workflow, and its fix wave — e.g. sidecar-failures invisible to `mismatch_total`; `env()`→`sys.env()` (Alloy boot crash); Alloy silently root (host-secret exposure); the Loki keep-regex that couldn't match the compose-prefixed container name; the dead-man missing missing/late gate breaks; the alert-rule `expression` field + wrong provisioning endpoint (alerts never firing).
- **Attended live deploy** on the Atom NAS surfaced **6 more Synology-only bugs** (all in the compose/config, none in the Role B code): cadvisor SIGSEGVs on DSM's cgroup-less kernel (removed); `NanoCPUs`/`cpu_shares` unsupported (dropped); `/var/lib/docker` is `/volume1/@docker` (mount dropped); Alloy uid 473 isn't a DSM user so the ACL denies it write (a real DSM user is required — the live fix ran as uid 1000, later hardened to the dedicated `zcrypto-dummy` uid 1031:1000 under T0030); the DSM ACL isn't honored in-container so `textfile`/`alloy-data` need real POSIX `chmod 0775`; the socket-proxy needs `NETWORKS: 1` for Loki discovery.
- **Live verification (end-to-end):** `prometheus_remote_storage_samples_failed_total 0` + `samples_pending 0` (host + gate metrics accepted by Grafana Cloud); `loki_write_sent_entries_total 7`, `dropped 0`; healthchecks.io `zcrypto-gate-verify: up`; `gate.prom` `streak_days 2`, `status 0` (not-MET), **`mismatch_total 0`** — the NAS's compat replay agrees with the VPS in production. All 3 containers `running rc=0`. The NAS runs the branch `-compat` image (`@sha256:dfda2580…`); re-pin to the develop-built digest after merge.
- **Topics**: **T0029 → resolved** (both the AVX two-variant AND the determinism residual, now measured-closed); **T0003 synced** (Role B landed; remainder = ≥7-day clean-run + alerting drill + Role C); **T0020 synced** (the NAS observability slice — Alloy + the shared dashboard/alerts + the push script + the creds path — landed, and the dashboard/alerts **push ran live**: the single dashboard + all 7 alert rules + the `email` contact point provisioned on `zcrypto2026.grafana.net`, verified by API query; remainder = the VPS `obs` role + app `/metrics` + the capture exporter flip); **T0021 synced** (the disk-watch mechanism moved from the retired gate-ops read to the NAS Alloy disk-free alert); **T0030 → resolved** (Alloy hardened to run as the dedicated non-key-owning `zcrypto-dummy` uid 1031:1000, verified live: it ships metrics + logs but can't read the 0600 rrsync pull keys via `/host/root`). Container/cadvisor metrics are not collected on the NAS (DSM cgroup incompatibility).

## 2026-07-15 — iter-097: the ops node joins the fleet (spec 00051, OPS-1…3)

- **OPS-1 — provisioned + converged**: new `ops_host` ansible group + `ops` role; the home i7-13700 (Debian 13, reinstalled from Ubuntu for fleet homogeneity) converged with `base`/`chrony`/`docker` only (home-LAN box — no hardening/firewall/fail2ban, owner's call; SSH stays port 22 with a minimal deploy-user bootstrap play). Hostname collision fixed (`zcrypto-red` → `zcrypto-ops`); fleet reboot-window assert generalized to `capture_host + ops_host` (21:25/22:25/02:25 pairwise distinct); charter enforced in inventory comments + site-play pre_task: **no trade key, never `engine_host`, pull-only**.
- **OPS-2 — liquidations, with a mid-flight pivot**: built the Binance `forceOrder` WS recorder (`zcrypto liquidations`, `LIQ_SCHEMA`, SegmentWriter reuse) — then discovered **Binance geo-fences futures market-streams from every egress we own** (home DE + Linode; spot/vision/REST all flow — proven with three independent clients). Recorder **shelved in place**; replaced by the Coinalyze poller `zcrypto liquidations-poll` (`LIQ_AGG_SCHEMA`, 1-min buckets, `convert_to_usd`, closed-bucket discipline `t+60<=now-120` — load-bearing because SegmentWriter dedup keeps the first row; overlap safety = dedup for the open hour + the writer's late-event floor for finalized hours, locked by a byte-identity regression test). Deployed live: first cycle back-filled 1878 buckets, 10/10 coins, 244 finals+manifests; dead-man `zcrypto-liquidations` green. NAS replication over a dedicated `sync_liquidations` rrsync `-ro` channel (ops port 22, per-call `LIQUIDATIONS_SSH_PORT`), optional-channel pattern in `infra/nas/{compose.yaml,pull-entrypoint.sh}`.
- **OPS-3 — the first-ever replay oracles off the Atom**: `zcrypto archive verify-replay` (canonical reconciled-first book continuity: snapshot-anchored / ts-ordered / checksum-present / structurally-replayable per hour — deliberately NO CRC re-derivation, deferred to [[T0045]] with the raw-string schema change) + two daily systemd timers (03:41 continuity-replay, 05:23 `engine replay --path verified`) emitting textfile metrics + dead-men (25 h/6 h). The ops node seeded from the NAS over a new pull-only channel (DSM lacks `rrsync` — Debian's python3 rrsync shipped to the NAS and invoked explicitly): capture-segments 3.5G, red 424M, reconciled, journal. First runs of both replays started detached.
- **Process notes**: three adversarial review waves + a final cross-cutting review (findings: a parser crash-loop on `Infinity` timestamps, a missing single-instance lock, the finalized-hour floor-guard trace, an ops-README pivot drift — all fixed); T0045 (CRC re-derivation), T0046 (sparse-hour finalization), T0047 (Slack webhooks — Grafana wiring remains) registered en route; the canary T+24 h Slack-reminder practice codified in `fleet-deploys.md`.

## 2026-07-20 — iter-107: `zcrypto engine soak-check` — the realized-OOS-vs-backtest report ([[T0064]] stand-in)

- **New read-only `zcrypto engine soak-check`** (spec/plan `00058`, core in `cli/engine/soak.py`): the stand-in for the never-run out-of-time holdout — it places the deployed strategy's realized shadow-soak behaviour against its backtest expectation. It is **decision-support, never validation, never the concordance gate** (that stays `engine report`), and it **consumes no holdout budget** (reads only the journal, the live store, and the frozen `data/ohlc-full`; never touches `data/ohlc-holdout-*`). Built subagent-driven over 7 TDD tasks (implementers Sonnet 5, reviewers Opus 4.8), each task adversarially reviewed with a fix-and-reverify loop, plus a final whole-branch review.
- **Behavioural-fingerprint design.** At L≈84 4h cycles a Sharpe/return statistic can't discriminate, so the headline is **position-derived structural metrics** judged against their own overlapping-window backtest null on the frozen canonical: each metric's realized window-mean vs the distribution of historical window-means (two-sided band, `--band 0.90` → `[p5,p95]` inner `[p10,p90]`; too-good flags too; effective-n and band-width disclosed; a multiplicity line counts outliers-vs-chance).
- **The off-by-one keystone.** Forward 4h returns are **timestamp-keyed** on the store (`start=closes[T−4h]`, `end=closes[T]`, both pinned to the journal's hash-verified 240-snapshot `last_ts == cycle_ts−4h`); a `_chain_consistent` cross-check (`end(T)==start(T+4h)`) VOIDs on a fragmented scored sequence. A null-close in the store skips the cycle rather than crashing.
- **The governor-cost (D4) bias cancels by construction.** The non-gating P&L null is recast onto the live cost convention (`net_live = governed_net + mult·fee·Σ|Δcapped| − fee·Σ|Δfinal_targets|`), derived from the builder's returned quantities alone — `capped` reconstructed from `sleeve_positions` via `apply_position_caps(⅓·ΣB/A1/A2)`, never `final_targets/multipliers` (the `mult==0` trap). A `reconcile_ok` invariant (`mult·capped==final_targets` to 1e-9) VOIDs a bad reconstruction.
- **Distrust-the-instrument VOID gates + honesty lock.** Every run runs three self-tests that VOID on a ran-and-failed check: the frozen build reproduces registry **record 44's exact integer diagnostics** (`governor_engaged_bars=7302`, `cap_breach_bars=1318`, read from the registry not hardcoded), the journaled `final_targets` reproduce under `replay_cycle` (1e-6), and the null reconciles. A near-zero-exposure window → INDETERMINATE (the record-33 `[0,0]` echo). Every report prints the bare-fact banner (trial 44 has zero OOS holdout evidence; paper is its only OOS test) and the rendered text is **vocabulary-locked** (never validated/passed/confirmed/proven — note "Provenance" contains "proven", so the section is "REALIZED-SERIES WINDOW", and self-test prose like "identity check passed" is never echoed to the text, only to the non-locked JSON). Refusal is **exit 0**; only a broken-instrument VOID is exit 1.
- **Verified end-to-end on the real journal (2026-07-20):** all three self-tests `ok` (the frozen build reproduced record 44 live), longest clean run L=14 (2.17 days — the cutover/restarts fragmented the 44-cycle journal), `chain_ok` True, and it **honestly refused** — "NO VERDICT — L=14 < floor=30" — at exit 0. The 5-metric table + P&L line populate once ≥30 consecutive clean cycles accumulate (approaching the ~07-25 gate).
- **Scope decisions (reversible, surfaced to the owner) + follow-ups.** v1 **gates on 5 position metrics** (gross/net exposure, active fraction, turnover, HHI) — the journal carries `final_targets` (=`mult×capped`) but not the per-bar multiplier or a pre-cap book, so **governor-engagement + cap-breach** (undetectable/ambiguous from the traded weights) are reported as clearly-labeled **backtest context**, not realized comparisons: registered [[T0072]] (recover them via a build on the latest journal snapshot). The spec's **secondary block-bootstrap null** + `--null`/`--path` options + regime-context section are built-but-unwired (the primitive `block_bootstrap_null` exists + is tested, docstring-flagged): registered [[T0073]]. Turnover + P&L use **interior bars** (drop the first full-entry bar symmetrically on both sides) instead of the spec's flat-start-per-window normalization.
- **Closeout.** [[T0064]] flipped **partial** — the OOS-consistency instrument now exists and self-proves on live data; the recording at the gate + the human go-live judgment remain (its `ripe_when` stands). README `## Usage` gained the `soak-check` entry. PR #154.

## 2026-07-20 — iter-108: soak-check gates the realized governor/cap fingerprint ([[T0072]], spec 00059)

- **The two remaining structural metrics became real realized-vs-backtest comparisons.** `zcrypto engine soak-check` gated on 5 metrics computable from the journaled `final_targets`; governor-engagement and cap-breach needed strategy *internals* the journal doesn't store (the per-bar multiplier; a pre-cap book). The unlock: each cycle record's 240 snapshot carries the **full hash-verified price history**, so ONE `build_crossfreq_system_fast` rebuild on the **latest** record yields multipliers and sleeve positions for every scored cycle — no live-store dependency, the journal is its own evidence.
- **The alignment keystone, proven on production data.** The row for a decision at cycle `T` is the index where `h4_ts[k] == T − 4h` (resolved from a `{ts: index}` map, never positionally). It is proven window-wide by a `final_targets` identity against the journaled values, which **VOIDs** on any shift — measured on the real 55-cycle journal at **worst |diff| = 0.0** across 54 cycles × 10 assets (bit-exact), with per-bar cap-breach agreeing with the builder's own diagnostic at **1318 == 1318**.
- **`n/a` now covers BOTH degeneracies (spec D8, found only by running for real).** The first live 7-metric run returned `governor_engagement` with band `[0.0, 1.0]` — the entire attainable range of a rate, so nothing could ever fall outside it — yet verdict "consistent" *and* counted in the multiplicity denominator. `metric_verdict` already escaped a **zero**-width band; a **full**-width band is the same failure inverted. An optional `domain` marks it `n/a` when the band covers **both** ends (a one-sided touch keeps its power, so `active_frac`'s real `[0.0074, 1.0]` stays discriminating), and the panel self-corrects to "0 of 6".
- **A correction the instrument made to its operator (spec D9).** Mid-iteration the constant realized multiplier (exactly 0.5 on all 54 cycles) was read against the null's *global* engagement rate (0.266) and flagged as a likely divergence. Judged correctly — against the distribution of same-length 9-day windows — the backtest reaches 100% engagement often enough that p95 = 1.0, so nine-of-nine engaged days is unremarkable. **Not a divergence.** Comparing a window statistic to a global mean is precisely the error the windowed null exists to prevent; it was caught before any verdict was registered.
- **Honesty hardening.** The vocabulary lock is now **structural** rather than conventional: free-form text reaching the rendered report (exception-derived reasons, void reasons) is scrubbed at the render boundary while the JSON keeps the raw diagnostic. A disclosure claiming a "partial-day downward bias" was **removed as false** — the multiplier is constant within a day by construction, so a partial day carries identical engagement information. And because `gross`/`net`/`active_frac`/`hhi` are all deterministic functions of one weight vector with `turnover` its first difference, the report now names that cluster (genuinely independent dimensions ≈ 2–3, not 6) and the footer states plainly that these are **structural-conformance checks — does the live book look like the backtest book — not evidence of edge**.
- **No regression, verified against a saved pre-change baseline:** all five original metrics byte-identical, provenance identical (L=54, span 8.83 d, `chain_ok`), `void_reasons: []`, exit 0. Constant realized series are judged, not suppressed — `n/a` stays reserved for an undiscriminating *instrument*, never a constant *observation*.
- **Ops-node enablement (first autonomous run here).** `data/engine-store` is absent by design on ops and is **not** a fetchable hot-hub dataset; `zcrypto engine seed` generates it from canonical + a Kraken **public** REST gap-fill (20/20 series seam-QA clean, 7700 bars appended, 0 divergent tail rows). The journal lives at the pull mirrors (`/mnt/zhao-crypto/engine-journal`), not `data/`. With both in place the soak-check produces real verdicts on ops.
- **Closeout.** [[T0072]] resolved + archived. [[T0073]] (secondary block-bootstrap null + `--null`/`--path`/regime context) remains open. No trial registered and no budget spent — this iteration builds instrument, not evidence.

## 2026-07-20 — iter-109: gate-export profiled — the super-linear hypothesis refuted, the real cause found ([[T0069]])

- **Measured, not assumed.** [[T0069]] suspected `zcrypto engine gate-export` of super-linear (repeated full-journal) work because ~8.5 min of CPU for a 96 MB journal "is far more than a linear scan should cost". Scaling the ops journal mirror 1→7 days shows per-cycle cost **flat** — 1.667 → 1.603 s/cycle across 6→39 cycles, slightly *decreasing* as fixed startup amortizes — and every step matches linear (6→12 cycles: ×1.97 measured vs ×2.00 linear, ×4.00 quadratic). **There is no O(n²) bug.**
- **But the root cause is worse than a slow scan, and better news for the fix.** `cProfile` attributes **81%** of the run to `build_crossfreq_system_fast`, called **once per journaled cycle** via `replay_cycle` (39 calls, 120.6 s cumulative) — each one a full ~28k-bar, 10-asset, 12-year strategy rebuild. Every hourly run re-verifies the *entire* append-only, hash-verified journal from scratch, redoing work the previous run already did. Snapshot hashing is a distant second at 14%.
- **Linear is not safe when the input grows without bound.** At the NAS Atom's ~10.9 s/cycle (derived from the topic's own 8.5 min / ~47-cycle datapoint; the Atom is ~6.8× slower than the ops i7), the topic's own "~20 min wall" trigger projects to fire **≈2026-07-29**, 50% of the 3600 s pull interval **≈2026-08-07**, and 100% **≈2026-09-04**. This **corrects the 2026-07-19 trigger check** that recorded both thresholds as "comfortably unfired" — true then, ~9 days out on the projection.
- **The remedy choice is now evidence-backed rather than a guess.** Incremental scoring (cache the verified prefix keyed on the journal's own content hashes; replay only new cycles) attacks the actual cause and bounds the hourly cost at roughly ONE cycle's rebuild (~11 s) **regardless of journal growth** — a ~47× cut that stays flat. Relocation to ops (spec `00054` D6's deferred option) buys a constant ~6.8× and decouples the single-threaded pull loop, but postpones rather than bounds. **Recommendation: both, incremental first.** [[T0069]] → `partial`, its `ripe_when` rewritten from a threshold to "ripe NOW".
- **No code changed.** This iteration is measurement + a corrected remedy decision; incremental scoring caches *gate evidence*, so it gets its own TDD iteration (cache-hit-equals-fresh-replay, tampered-journal misses the cache, streak arithmetic unchanged). The relocation is attended and stays parked for a maintenance window.

## 2026-07-20 — iter-110: gate-export incremental scoring — the cost stops growing with the journal ([[T0069]], spec 00060)

- **The structural fix for iter-109's finding.** `gate-export` re-replayed every journaled cycle every hour, each replay a full ~28k-bar, 12-year rebuild (81% of runtime). It now caches per-cycle outcomes and replays only what is new: **opt-in `--cache PATH`**, with no flag meaning today's behaviour byte-for-byte and `report` never cached (it is a human-facing *verification* tool and must genuinely re-verify).
- **Verified on real production evidence, not fixtures.** On the ops journal mirror (39 cycles): no-cache 63.11 s / cold 62.77 s (39 replayed) / **warm 0.30 s (0 replayed, 39 hits)** — and **the gate metrics were identical across all three runs**, which is the load-bearing property: a cache hit must be indistinguishable from a fresh replay. Cold matches no-cache, so enabling it costs nothing. Steady state (one new cycle/hour) is ~2 s here and ~13 s on the Atom vs ~510 s today; the point is that the cost is now **flat** rather than climbing toward the 3600 s pull interval — precisely, the *expensive* per-cycle rebuild is bounded to what's new, while `_journal_artifacts` and the evidence fingerprint still scan/hash every record each run, a small O(n) constant (the 0.30 s warm run over 39 cycles is that term) that stays seconds even at the projected 332-cycle horizon.
- **The safety property is the whole design, and the review found it broken twice before merge.** The cache invalidates wholesale whenever anything that could change a verdict changes. The first implementation hashed four modules; tracing `replay_cycle`'s actual call graph found two **live** gaps — `cli/risk/governor.py` (whose multipliers fold straight into `final_targets`) and `cli/engine/journal.py` (whose `validate_record`/`snapshot_content_hash` decide pass vs `validation_failed` vs `HashMismatchError`) — plus four latent verified-route modules and the unhashed replay *path*. Separately, the per-cycle key digested only `content_hash`, so tampering `n_bars` would make the real replay fail validation while the cache served the stale **pass**. Both closed; each fix pinned by a test the reviewer confirmed fails when the fix is reverted.
- **Deliberately over-sensitive.** A comment-only edit to any of the ten covered modules invalidates the whole cache and costs one full rebuild. Over-invalidation is cheap; under-invalidation silently corrupts the evidence that decides whether the strategy trades real money.
- **Fail open, never fail trusting.** Any cache problem — absent, unreadable, truncated, wrong schema, fingerprint mismatch — degrades to a full replay and rewrites; it never aborts and never serves a partially-trusted entry. The exception nets were narrowed to `(OSError, ValueError, KeyError, TypeError)` so a future refactor's `AttributeError` surfaces in tests instead of silently disabling the cache forever.
- **Observability so a dead cache can't look healthy:** `zcrypto_gate_cache_replayed_total`, `_hits_total`, `_invalidated` emit **always** (zero when caching is off), so a fingerprint that changes every run shows up as `hits 0 / replayed n` rather than as a working optimisation.
- **A counting trap avoided:** `JournalCounts` is now derived from each resulting `CycleOutcome` in one place rather than incremented inside the replay branches — otherwise cached cycles would stop being counted and a report would state "0 mismatches" because it never counted them, while the gate itself still looked right.
- **Closeout.** [[T0069]] updated (incremental scoring landed; the **attended** relocation and enabling `--cache` in the deployment remain, both needing a maintenance window — the code ships inert). [[T0074]] registered: the fingerprint covers the replay *code* but not the execution *environment*, so a numpy/Python bump could serve stale outcomes if the drift exceeds the 1e-6 `compare_targets` budget.

## 2026-07-20 — iter-111: soak-check's second null — a verdict that depends on how the null was built is no longer reported as a finding ([[T0073]], spec 00061)

- **The descoped half of spec `00058` D3, now wired.** `soak-check` judged every metric against a single **windowed** null (all overlapping L-bar windows). Overlapping windows share observations, so that construction understates sampling variance — a known, one-directional bias with no second opinion. The **stationary block bootstrap** (Politis–Romano) resamples independent paths and carries a different, largely uncorrelated bias. Both now run by default (`--null both`), and each metric's two verdicts are reconciled by an explicit severity rule: identical ⇒ that label; adjacent ⇒ the **milder**, disclosed; opposite extremes ⇒ **`indeterminate (instrument-fragile)`**; exactly one `n/a` ⇒ the discriminating null's label, disclosed. `--path [fast|verified]` also landed, so a suspicious result can be re-read on the oracle builder without editing code.
- **The second null earned its place on its first real run.** On the ops journal mirror (54 cycles), `gross` and `net` come back **windowed `consistent`, bootstrap `weakly-consistent`** — an adjacent disagreement, reconciled to the milder and disclosed verbatim. The magnitude is the point: on `gross` the windowed band is `[0.0005, 0.2904]` (width 0.2899) against the bootstrap's `[0.0411, 0.1699]` (width 0.1288), so **the primary null's band is 2.25× wider**, and the same live value sits at the 35.6th percentile under one construction and the 6.5th under the other. The overlapping-window variance understatement is no longer a theoretical caveat in a docstring — it is measured, on this journal, and it moves a verdict.
- **Structural honesty, not a heuristic.** The gating verdicts and the panel count the **reconciled** label; the numeric columns stay the primary's (D2), so existing readings remain comparable. The bootstrap is seeded and the seed is deliberately **not** a CLI option — a user-tunable seed on gate-adjacent evidence invites shopping for a friendlier null.
- **Eleven defects were found before merge, and every one was the same class: the instrument describing its output falsely, never miscomputing it.** The table merged two columns into `consistentweakly-consistent`; the summary line counted raw verdicts while the table rendered reconciled ones, so a row could read `inconsistent` beside "0 of 2 outside band"; the JSON asserted the windowed null was the reference even under `--null block-bootstrap`; a disclosure could claim "no discriminating power" next to a rendered `inconsistent`; a docstring promised a `SoakError` never raised; the README claimed "a disagreement is never rendered as two identical strings", which is false on the adjacent branch. Three were caught only by **reading rendered output**, and the sharpest only by a reviewer **enumerating reconciliation branches the live data never exercises**.
- **The two that mattered most.** (i) `reconcile_verdicts` validated its vocabulary *below* the equality and `n/a` short-circuits, so `("probably-fine", "n/a")` returned `probably-fine` **as a rendered verdict** — the guard existed and the defect walked around it; validation is now an unconditional **precondition**, independent of branch order. (ii) The table rendered only `verdict` (reconciled) and `secondary` (raw), which **collide on 3 of D1's 5 branches**: `primary='inconsistent', secondary='weakly-consistent'` displayed as `weakly-consistent | weakly-consistent`, a suppressed `inconsistent` invisible to the reader, with the primary's label appearing nowhere. The table now carries **three** columns (`verdict | primary | secondary`), which makes concealment structurally impossible rather than depending on the labels happening to differ. The same shape was then found and fixed on the P&L line.
- **A test that fails pre-fix is not the same as a test that pins the claim.** Mutation testing on the fix wave's own tests found the central claim — *which column reports which null construction* — pinned by nothing: swapping `primary`/`secondary`, or attributing a verdict to a construction **that never ran**, left 128/128 green because the assertions were order-agnostic membership checks. Beware also that `"inconsistent"` contains `"consistent"` as a substring, so the obvious assertion passes against the broken output. The column→construction attribution is now pinned **positionally**, verified by applying each mutation and watching it fail.
- **Regression guard.** `--null windows` reproduces the pre-change verdicts, numerics, panel line and disclosures **exactly** (confirmed by a side-by-side `develop`-vs-`HEAD` run on the real journal), so the new construction is strictly additive. `--null both` also never raises where `--null windows` degrades — which holds only because `effective_n` is computed once and shared across both nulls, now documented at the sharing site as load-bearing.
- **Closeout.** [[T0073]] → `partial`: the dual null, the reconciliation rule, `--null`/`--path` and their tests landed; the report's **regime context** section (spec `00058` report-shape item 9) stays deferred, since `00058` named it without defining what it should contain and inventing a definition here would be worse than leaving it open. Its `ripe_when:` was rewritten accordingly — the original trigger has now fired and been consumed. Spec `00061` D2's wording was refined from "one new column" to the three-column form, with the reason recorded. No trial registered and no budget spent — this iteration builds instrument, not evidence.

## 2026-07-20 — iter-112: the gate cache no longer trusts a cycle forever ([[T0077]], spec 00062)

- **The finding this fixes was not a bug report — it was a design gap nobody had modelled.** `gate-export --cache` (iter-110) skips `replay_cycle` on a fingerprint hit, and that replay is the **only** place the journal's parquet bytes are re-hashed (`concordance.py:116`). The NAS pull delegates to it explicitly (`--no-verify`: *"no .sha256 sidecars, Role B verifies it via replay"*), rsync has no `--checksum`, and `evidence_fingerprint` digests the `content_hash` **recorded in the record** rather than a fresh read. Enabling `--cache` would therefore have converted *"every parquet re-verified ~24×/day"* into *"verified once, then trusted forever"* — and a snapshot altered in place after its first replay would be served as a **PASS**, silently, with `mismatch_total` at 0 and the streak still climbing. Spec `00060` D2 had scoped its fingerprint to "everything a replay's verdict depends on **from the journal side**", so the parquet-bytes axis was never argued; the design's governing asymmetry (*over-invalidation is safe, under-invalidation silently corrupts*) was never applied to the axis that actually breaks.
- **This also reframes iter-109.** That iteration characterised the per-cycle full rebuild as pure waste — *"redoing work the previous run already did."* It is **also the continuous integrity monitor**. Any optimisation that removes redundancy must replace the monitoring the redundancy was silently providing; that is the transferable lesson, not the specific fix.
- **The mitigation: bounded rotating re-verification, always on when the cache is active** — not a flag, because a safety property that can be switched off will be. Each run force-replays a deterministic slice of otherwise cache-eligible cycles. Keyed on `sha256(cycle_ts) % 24` so a cycle's slice is a **fixed property of the cycle** — an index-keyed slice would move as the journal grows, making coverage neither uniform nor provable — and `sha256` rather than the builtin `hash()`, which is not guaranteed stable across processes. The current slice is `now.hour % 24`: stateless, no cursor to persist, corrupt, or reset.
- **A forced re-verification failure moves the gate** (D4): the same `CycleOutcome(mismatch=True)` any replay produces, into `JournalCounts`/`evaluate_gate` unchanged, counted as `replayed` and never `from_cache`. Detecting corruption must change the verdict, not merely log.
- **Verified on the real journal against the MODIFIED predicate** (57 cycles) — iter-110's "identical gate metrics" evidence covers the old eligibility rule and does not carry over: no-cache 94.52 s / cold 93.70 s / warm 2.45 s / warm-again 2.48 s, all exit 0, **gate metrics identical across all four**. Warm replays **1** with 56 hits — the counters, not the runtime, are what prove rotation engaged, since a fast warm run is exactly what a silently-disabled rotation would also produce. A 24-hour sweep re-verified **57/57 cycles, each in exactly one slice**; worst hour 5 replays ≈ 54 s on the NAS Atom, ~1.5% of the 3600 s interval against ~510 s for a full replay.
- **The guarantee was attacked, not merely tested.** Review confirmed eligibility depends only on `cycle_ts` and the run clock, reading nothing from the cache file — so an attacker with full control of that file can suppress the staleness metric but not the rotation replay, and a failed `save_cache` cannot stop it either.
- **Two latent traps closed on the way, both of the run's recurring shape.** The new metrics' *values* were unpinned end-to-end: hardcoding `oldest_verification_age` to `0.0` passed 72/72 until a test pinned the exact value — and that number is D5's own declared backstop, the sole way a stopped rotation is distinguishable from a healthy cache, so a constant `0.0` would have read as *"everything verified seconds ago, forever."* Separately, `_ROTATION_SLICES > 24` would have left high slices permanently unreachable (since `now.hour % N` only spans `[0,23]`) — **silently**, the exact failure class the iteration exists to prevent; now an assert.
- **Also corrects [[T0069]] on two counts**, established by reading the deployed loop rather than trusting its own projections: the *"loop can no longer keep up"* cliff **does not exist** (`pull-entrypoint.sh` sleeps *after* the work, so the period is `3600 + work` and the loop drifts rather than falling behind — the same file records it already running at ~103 min), and consequently **relocation is the safer remedy, not the redundant one**, since it preserves the integrity check `--cache` removes. The constraint that actually binds is the 18000 s lag alert, whose own comment concedes the value was copied from a CLI default rather than derived.
- **Closeout.** [[T0077]] → `resolved` and archived, but only after its residuals were actually worked down rather than filed away. It briefly stood at `partial` with three: the >3-day staleness alert rule, the cache-file siting decision, and the owner's threat-model judgement. All three closed in-iteration — the rule was written, committed and **pushed live** (read-back verified); siting was decided as spec `00062` D9 (ephemeral, never on a shared path, since the NAS and ops run different polars runtimes and `replay_fingerprint` digests neither); and the owner confirmed in-place alteration is worth defending against, so daily rotation stands. Worth recording that the plan's Task 3 had pre-written "T0077 → resolved" before any of that was true: closing with live deferred sub-items is exactly what the `open-topics` rule merged earlier the same day forbids, and review caught it. The difference between that and this closure is that the sub-items were *done*, not re-labelled.
- **A guard came out of the deploy, not the design.** The first live push failed with a bare `curl: (22) ... error: 400`: the new rule's uid was 41 characters against Grafana's 40-char column. It bit only on that push because every existing rule updates via `PUT` while a new one is created via `POST`, and the limit applies only on create — so a too-long uid breaks exactly once, on the run that introduces it, and `grafana-push.sh` discards the response body. Since that is discoverable only during an attended push behind a vaulted token and a GPG pinentry prompt, `tests/test_infra_alert_rules.py` now pins uid length and uniqueness, the `noDataState`/`execErrState` enums, the required-field set (including that `condition` names a refId present in `data`), and that datasource UIDs stay templated. The longest surviving uid is exactly 40 — the ceiling was already being brushed, without a guard.
- [[T0069]]'s `--cache` sub-item is unblocked: enabling the flag no longer trades away the integrity check. It remains an attended deploy. No trial registered and no budget spent — instrument, not evidence.

## 2026-07-20 — iter-113: the gate's arithmetic guarantees now fail when broken ([[T0076]], spec 00063)

- **Test-only, and that is the finding.** A 62-mutation audit of `evaluate_gate` / `replay_cycle` / `compare_targets` found **no live defect** — the gate has been computing the right thing throughout. What it found was nine guarantees the code honours and nothing enforces. Two were pinned in PR #162 (the streak threshold, the dead-engine reset); this iteration pins the rest. `git diff -- cli/` is empty across the whole branch.
- **The keystone: the daily no-peek guarantee reopened on a *same-length* peek.** `concordance.py:118` is `len(ts) != n_bars or ts[0] != first_ts or ts[-1] != last_ts`, and the existing peek fixture tripped **two** terms at once — so deleting any single conjunct left the suite green. Swap the trailing settled bar for the in-progress candle at equal length and only `last_ts` differs: absent that check the builder runs on **lookahead data while the targets still compare equal**. Of the nine findings this is the only one whose failure mode was silent contamination of the verdict rather than a mis-set threshold or a loud error. Review confirmed it must use the *daily* grid — on h4 the metadata-equality backstop masks the mutation.
- **The three conjuncts are provably independent**, not three spellings of one test: deleting each in turn fails *exactly* its own case and leaves the other two green. That cross-check is the difference between three pins and one pin with extra names.
- **Two findings decomposed further than their own wording suggested**, and both were caught only by looking again after "done" — which is the transferable lesson, not the individual fixes. Finding 7 read "the day-cutoff anchor **and window**"; the anchor landed first, and deleting `+ _FRESHNESS_WINDOW` from the day cutoff still left all 30 tests green, because the anchor test's `now=20:35` sits past both the true 20:30 cutoff and a window-dropped 20:00 one. `now=20:15` discriminates. (The two `_FRESHNESS_WINDOW` usages are not both bare — the per-cycle bound was already pinned; only the day cutoff was uncovered.) Finding 2's remainder turned out to be a **name**: `test_gate_export_stale_journal_pings_fail` read as though it covered the dead-engine reset while exercising only `--lag-fail-seconds`, and it sat green beside an entirely unpinned reset. **A name that implies a guarantee it does not provide is worse than a missing test, because it stops anyone looking.**
- **Every pin is mutation-proven, and the method is now the deliverable.** For each: apply the mutation, confirm the edit **on disk** (the original text can be a substring of the replacement, silently no-opping), purge `__pycache__`, run under `PYTHONDONTWRITEBYTECODE=1`, watch it fail, restore. That substring trap fired for real during the final review — the reviewer's own mutation was rejected by its assert, and without it a survivor would have been reported as killed.
- **A do-not-clean-up, recorded deliberately:** the metadata-equality guard is a *provably equivalent* mutant — deleting it leaves all 31 tests passing — so the next reader will be tempted to remove it as dead code. It is the backstop that makes the keystone work on the h4 grid. Spec `00063` D8 says so; review verified the equivalence rather than taking it on faith.
- **The asymmetry worth carrying forward:** every mutation that opened the gate EARLIER survived the audit; every one that opened it later was caught or errs harmlessly stricter. The existing tests were written by someone checking the gate was not too strict. Nobody checked it was not too loose.
- **Closeout.** [[T0076]] → `resolved` and archived — all nine findings pinned, the finding-2 remainder fixed in the same branch rather than deferred, and no live sub-item left. [[T0075]] (the gate-cache guarantees) remains open and is the next package; its own audit needs re-running under bytecode control first, since its 44 "detected" verdicts were produced without it and stale bytecode makes a mutant look detected when the test never saw it. No trial registered and no budget spent — instrument, not evidence.

## 2026-07-20 — gate-cache evidence guarantees pinned, and fingerprint coverage made measurable ([[T0075]], [[T0080]])

- **All 19 [[T0075]] gate-cache guarantees are pinned** (spec `00064`), each mutation-verified under bytecode control. No live defect anywhere — `gate_cache.py` invalidates correctly today; what was missing was any test that notices when it stops. The whole-branch review re-derived coverage independently, writing its own 19 mutations from the audit's mutant definitions rather than reading the task mapping, and every one was killed by exactly the test claiming it.
- **The scope was 19, not the recorded 8 — and the reason was not the one predicted.** [[T0075]] expected its count understated because Audit A ran without bytecode control. Re-audited: **zero false detections**, so all 44 of Audit A's clean verdicts stand. The undercount came from **triage error** — `pair`/`grid` were ruled equivalent because the entry sort key masks the tamper, which holds only for an *order-changing* tamper; an order-preserving rename (`ETH`→`FTH`) defeats it and yields a stale PASS. **An equivalent-mutant ruling is a universal claim, and one non-distinguishing input is not evidence for it.** Why bytecode bit the concordance audit but not this one: Audit A's mutations were mostly line *deletions*, which change file size and invalidate the `.pyc` regardless of mtime.
- **The sharpest pin:** an unparseable `cycle-*.json` scored as a clean pass flips a 14-day journal from `gate_met=False` to `gate_met=True` with `last_failure=None` — a corrupt file becoming a PASS on the artifact authorising real-money trading. Asserted at journal level, since the per-cycle classification passing proves nothing about the gate the operator reads.
- **`replay_fingerprint`'s coverage was a false invariant, closed in two steps.** Spec `00060` D3 called `_REPLAY_CODE_PATHS` "the modules that determine a replay's result". It never was. Spec `00064` D9 added `command.py` + `dataset.py` (`_snapshot_reader` reads every price series a replay sees; switching `close`→`open` changed every verdict at a **byte-identical** fingerprint). Spec `00065` then replaced enumeration entirely with the transitive `cli.*` import closure — measured first: the hand-list covered **12 of 61** executing modules (~20%), so three rounds of "add one more module" were converging on nothing.
- **Adversarial verification caught the closure shipping the same hole one level up.** `_resolve_module` mapped a dotted name to its leaf file only, but importing `cli.engine.command` *executes* `cli/__init__.py` and `cli/engine/__init__.py` first. **Seven** modules ran unhashed on every replay (four ancestor `__init__.py` files plus three `cli/ohlc` submodules reachable only through one of them) — exploitable identically, at a byte-identical fingerprint with all 64 tests green. Fixed (`00065` D10); ancestors are now resolved explicitly.
- **The durable artifact is one test, not the closure.** `test_closure_covers_every_module_the_replay_roots_actually_execute` (`00065` D11) imports the roots in a clean subprocess, reads `sys.modules`, and asserts the closure is a **superset of what actually ran**. It enumerates nothing, so it cannot go stale — and it **failed on the first implementation**, which is what makes it the right pin. Now: 61 covered, 60 executed, **0 executed-but-uncovered**. A subprocess is required; pytest has already imported far more of `cli` than a replay does, so an in-process read passes by over-counting.
- **The pattern this iteration actually taught.** Over twenty defects were found across the work and **every single one was an artifact asserting something untrue** — a spec decision, a test comment, a commit subject, a docstring — never a computation returning a wrong number. **Four separately claimed coverage was complete when it was not**: spec `00060` D3 itself, the D9 commit, spec `00065`'s own goal statement, and `replay_fingerprint`'s docstring. Several originated in the orchestrator's specs and dispatch prompts and were propagated into contexts that falsified them. Prose asserting completeness is unfalsifiable and invites every later reader to assume a check exists; D11 is the first thing here that checks it automatically. Spec `00060` D3 was amended at its root to point at that test, with the instruction that if the two disagree, believe the test.
- **Fix-the-instance, twice more.** Finding 13's pin caught the guard it had been told about; its sibling in the same expression (dropping `not o.mismatch` from `replayed_ok`) survived all 65 tests until pinned. And the D7 label correction landed in the code and spec but left the [[T0075]] bullet *instructing a future worker to restore the wrong label* — a correction that leaves the instruction to undo it is not a correction.
- **Closeout.** [[T0075]] → `resolved` and archived (all 19 pinned; D9 ruled by the owner and implemented rather than deferred; the residual split into [[T0080]] rather than buried). [[T0080]] → `resolved` and archived (option 2 implemented, ancestor gap found and closed). Ratified spec `00060` D3 amended. **Operational note:** the fingerprint change invalidates the whole cache by design — the fleet pays **one** cold rebuild (~627 s vs 55 s warm) on the next NAS pull, once for both changes since they ship together. No trial registered and no budget spent — instrument, not evidence.

## 2026-07-22 — iter-116: the app ships its own logs, and docker.sock leaves the fleet ([[T0089]], [[T0042]], spec 00068)

- **The CLI ships its own logs.** New `cli/logging/ship.py`: an opt-in `--ship-logs` sink that POSTs the existing `JsonLineFormatter` line straight to Grafana Cloud Loki with labels `{host, container, level}` — `level` from `record.levelname`, so **no regex exists anywhere in this path**, at ingest or at query. stdlib only (urllib/threading/collections), no new dependency on the capture image. Env: `ZCRYPTO_LOKI_URL`/`_USERNAME`/`_PASSWORD` + `ZCRYPTO_LOG_HOST`/`_SERVICE`; the flag with any of them unset **or empty** is a hard startup error naming every missing one.
- **stdout is untouched and remains the local ground truth.** The switch is additive: plain-text console exactly as before, docker's json-file driver still capturing it under the existing caps. A crash loses at most the in-memory ring; the last lines survive locally.
- **Bounded, non-blocking, self-announcing.** `emit()` is a deque append under an O(1) lock — all network I/O lives in a daemon worker. Ring 4096, drop **oldest** and count; batches ≤500 or 1 s; one in-flight batch; backoff 1 s → 30 s; exit flush with a 2 s hard deadline. A dead Loki costs one bounded ring and nothing else. **Non-429 4xx drops the batch rather than retrying** — after an outage longer than Loki's out-of-order window the held batch's honest timestamps are rejected forever, so retry-forever would wedge shipping permanently and silently.
- **docker.sock is mounted into nothing, fleet-wide.** All four Alloys lose the socket bind-mount, the docker-group `group_add`, and their `discovery.docker`/`loki.source.docker` components. Alloy's own logs now reach Loki through the **journald log driver** + each host's journal pipeline (a first-time build on the NAS). The T0042 socket residual closes; the T0089 wedge class becomes unrepresentable, because no *foreign* process reads docker's log files any more.
- **The parse stage survives where it is still fed.** Deleted on the capture hosts only; **kept on ops and the NAS**, where the oneshot units and the `archive-pull` wrapper still ship Python-shaped lines through the journal. The honest principle is `direct-ship path regex-free; journal path keeps its one ingest parse` — an earlier draft said "delete it everywhere" and would have permanently blinded `NAS · archive-pull ERROR logs`.
- **Label continuity, so no rule needed re-keying.** `ZCRYPTO_LOG_HOST` renders today's values: `{{ base_hostname }}` on the capture hosts, the literal `ops`/`nas` elsewhere (those Alloys apply static relabels, and the ops rules hardcode `host="ops"`).
- **Alerting changed where the transport changed.** `zcrypto-ops-log-pipeline-dead` was re-scoped to `{container="alloy"}`: the poller now direct-ships lines carrying `host="ops"` and a level, which would have pinned that critical dead-man **green forever** — worse than NoData, because NoData is visible. Two rules added: `zcrypto-ops-poller-log-dead` (nothing watched the new direct-ship path) and `zcrypto-ops-unit-parse-dead` (restores T0060 level-regression coverage at 26 h). `zcrypto-alloy-docker-sd-wedged` retires with its subject, and its `prometheus_sd_*` series leave every keep-list — a keep-list entry for a series that cannot exist is the T0051 admitted-but-unpublished trap.
- **Rollout has NOT happened.** Everything above is repo state. Spec D7's attended steps (vault token in place, per-host converges, the canary bake, the post-gate engine converge, per-step positive-trace verification in Loki, and the `grafana-push.sh` step — which prunes orphaned rules, so it must wait until ops and NAS are actually converged) are the remainder, and the T0089/T0042/T0020 topic updates land with them.
- **Credential.** `logship_loki_token` vaulted in `group_vars/observed/vault.yml` — Grafana Cloud access policy `zcrypto-app-logship`, scope `logs:write` only, distinct from Alloy's token so the app's push can be revoked without blinding fleet telemetry. Only the token is new; the push URL and username are stack properties, reused from the existing `grafana_loki_*`.

## 2026-07-26 — iter-117: every daemon serves `/metrics`, and the rollout completes ([[T0020]], [[T0089]], [[T0042]], [[T0048]], spec 00069)

- **Four daemons expose a Prometheus endpoint.** `cli/obs/metrics.py` builds a registry carrying six `process_*` families and no `python_gc_*`/`python_info`, started only when `ZCRYPTO_METRICS_PORT` is set — absent means exporter-off, so the workstation soak and any ad-hoc run stay silent. The server is loopback-published (`127.0.0.1:<port>:<port>`), and a bind failure logs one ERROR and returns rather than raising: a broken exporter must never take a capture or trade daemon down with it.
- **Per-daemon taps, each with its own hazard closed.** Capture (9101) publishes additive counters plus the disk-watermark gauge, reading snapshot copies so a mid-scrape mutation cannot tear; engine (9102) publishes cycle gauges seeded from the newest journal artifact at startup, so a restart does not fake a fresh cycle; the ops liquidations poller (9103) and the logship sink expose their own counters, absent rather than zero when no handler is configured — absence is honest, zero is a claim.
- **The file-based metrics approach is retired**, and cadvisor is not adopted: the NAS SIGSEGVs, and admitting the whole container-metrics surface for four hosts was rejected on posture and series budget. Alloy's own self-scrape carries the same six `process_*` names instead.
- **Rollout executed and verified, Steps 0–8** (Steps 0–6 on 07-22/23; Step 7 the engine on 07-26 after the Stage-6a gate; Step 8 the same day). The engine step was an image upgrade, not a flag flip — the running image predated `cli.obs` entirely (`ModuleNotFoundError: No module named 'cli.obs'`) — and it was deliberately held until the gate measured `streak_days 15`, `status 1`, `mismatch_total 0`.
- **Restart-safe seeding proved on live data, not only in tests**: after the engine converge, `zcrypto_engine_cycle_completed_at_seconds` decoded to 12:01:48 UTC — the preceding 12:00 cycle's completion — rather than process start.
- **The "may publish a different subset" hedge is now measured.** All four hosts publish **nine** `process_*` families on Alloy's self-endpoint; exactly **six** ship. The three the keep-list drops are `process_network_receive_bytes_total`, `process_network_transmit_bytes_total`, `process_virtual_memory_max_bytes`; the shipped `__name__` set is exactly the admitted six, extras leaked: none.
- **Series accounting: 135 new against D9's ≤150 budget** (`capture_app` 82, `engine_app` 16, `liquidations_app` 13, Alloy self-scrape `process_*` 24), versus the ~138 predicted. The budget governs the increment, not per-host totals — a per-host total of 308 on the ops node is not a breach, and the increment was measured directly rather than derived as (total − base), because the sets do not nest (00068 retired the docker-sd metrics).
- **Step 8 retired the last orphan rule.** `zcrypto-alloy-docker-sd-wedged` deleted via `GRAFANA_PRUNE=1` after a dry run confirmed it was the only one; the re-run reports no orphans. That was [[T0048]]'s archive gate.
- **[[T0089]] closed on its own criterion** — all six wedged containers confirmed shipping post-recreation *in Loki*, not inferred from the restarts; the engine was the sixth, cleared by this rollout's Step 7. One measurement trap is recorded there: the engine's post-recreation lines are stamped 13:35:18/13:35:20 while the converge *returned* at 13:35:22, so cutting the query at the deploy command's finish time reads as a failure — the container's own `StartedAt` (13:35:16.873) is the correct marker.
- **[[T0042]] and [[T0048]] closed and archived**; [[T0020]]'s host-label-asymmetry section was rewritten in place (past tense) now that the NAS carries `host="nas"` like the other three hosts, and the archived [[T0079]]'s index clause with it.
- **Operational finding, recorded rather than fixed here:** `primary` and `secondary` appear as `host` label values carrying one series each alongside `zcrypto`/`zcrypto-red` — a second labelling convention somewhere in the fleet. Harmless for the budget; it means `host=` is not uniform.

## 2026-07-26 — the engine journal stops growing forever, and the guard that matters is not about disk ([[T0021]], spec 00070)

- **A daily 01:23 UTC oneshot bounds the VPS engine journal at a 60-day tail** — `zcrypto-engine-journal-prune`, installed by the `engine` role with `ProtectSystem=strict` and `ReadWritePaths` limited to the journal dir (plus, since spec `00071`, the textfile dir it publishes into), so a bug in its own globs *structurally* cannot reach the trade key or the capture spool. Timer enabled, service left `static` (enabling it too would prune on every boot).
- **Retention was never a capacity problem, and saying so out loud changed the design.** 13 MB/day against 51 GB free is ~11 years of headroom. With no deadline forcing a cheap choice, the design could pay any amount of caution — which is how the real hazard got found instead of a `find -mtime +14 -delete` shipping.
- **The load-bearing guard protects order generation, not disk.** `cli/engine/cycle.py` derives each cycle's orders as a delta against the most recent journaled cycle; with no prior record `prev_targets is None`, every delta becomes the full target, and the engine emits orders to rebuild the entire book ("the shadow book starts flat"). An age-only prune reaches that state whenever the engine stops for longer than the retention window — every day ages out at once. So deletion requires **two** conditions: older than the cutoff **and** not among the newest N day-dirs. They coincide in healthy operation and come apart exactly when the floor is the only thing preventing a spurious whole-book rebuild. Recorded caveat: the floor counts day-*dirs*, so 60 consecutive days of all-failed cycles could still strand the lookup — not guarded, because any guard would key on the records the outage is failing to produce.
- **"Prune after verified pull" resolved to margin-plus-monitoring, not a handshake.** The architecture is pull-only (`rrsync -ro`, spec `00051` D10), so a NAS→VPS acknowledgement would mean opening a write-capable path *into the trade-key host*. Rejected: disk hygiene is not worth weakening that boundary. A day instead survives ~1,440 hourly pull opportunities under a 6 h lag alert — and the precondition was **verified rather than argued**: the oldest day on the host (`2026-07-11`) measured 127 files / 12,553,096 bytes on the VPS and byte-identically on the NAS.
- **The first review found the tests green and hollow.** Fixtures planted enough recent days that the keep-newest floor satisfied every assertion on its own — so the age cutoff could be *deleted outright* and all 21 tests still passed, as could either regex anchor and `-type d`. Four cases were added and the suite is now mutation-checked: all five mutations fail it. The lesson is the general one — a passing suite is evidence only about the fixtures it was given.
- **Observability was got wrong here, and is corrected in spec `00071` ([[T0100]]).** The reasoning recorded at the time — app metrics come from `/metrics` endpoints Alloy scrapes, a oneshot has no process to scrape, these hosts run no textfile collector, therefore publish nothing — was true at every step and did not follow. *The correct response to "no reader exists" is to add the reader.* The fallback claimed instead was false too: the journal keep-regex admitted only `zcrypto-capture-prune.service`, so the log line never reached Loki and the prune spent its first day observable through **nothing**. Superseded text follows, kept only because the reasoning is instructive: App metrics are now scraped from live `/metrics` endpoints; a oneshot that runs for a second has nothing to scrape. The fleet's mechanism for ephemeral timers is the textfile collector, which the ops node runs and the capture/engine hosts deliberately do not. Stated honestly rather than papered over: **nothing detects a timer that silently stops** — the spool-disk alert covers this filesystem but sits ~9 years away at 13 MB/day, so it backstops disk exhaustion, not the timer. Accepted because over-deletion is the dangerous failure and the keep-newest floor, not an alert, is what guards it. That alert's summary now names both prune timers.
- **Retention is 60 days.** The owner raised it from 14 during implementation, *between* the two converges — the first deploy ran at 14. The design is value-independent — the cutoff and the floor read the same parameter — so only numbers moved: steady state ~780 MB (1.5% of free space), ~1,440 pull opportunities of margin. A test now pins the deployed default against the spec, so the next drive-by edit has to change the ruling too.
- **Deployed attended and verified by outcome**, converged at 14 and re-converged at 60: `changed=4` then `changed=1`, with compose and the engine systemd unit `ok` in both, so no restart handler ever fired — confirmed by the container's `StartedAt` being byte-identical across both converges with `RestartCount` 0. The deployed command was read back from systemd's own resolved `argv[]`, not just the unit file.
- **The prune was proven on real data before the value changed**, which is the more informative record. At 14 a host `--dry-run` returned exactly the predicted `deleted=1 kept=15 cutoff="2026-07-12 UTC"` — one eligible day, the day sitting exactly *on* the cutoff correctly spared. At 60 it returns `deleted=0 kept=16 cutoff="2026-05-27 UTC"`, so **the timer is a verified no-op until ~2026-09-10**, when the oldest day first ages out.

## 2026-07-26 — the capture VPSes stop rebooting themselves, and one-off timers get a metrics path ([[T0027]], [[T0100]], spec 00071)

- **`Automatic-Reboot "false"` on both capture VPSes.** Patches still auto-install; the reboot is a human act. `zcrypto-ops` still reads `"true"` at 02:25 — untouched, because the role default preserves today's behaviour and only `group_vars/capture_host` overrides it. Three lines, and one footgun worth recording: a bare YAML `false` renders through Jinja as Python's `False`, emitting `Automatic-Reboot "False"` — accepted by apt as not-true *by accident*, invisible to lint, to `--check --diff`, and to any on-host grep phrased as "is it false". Both values are quoted strings and a test proves the unquoted form breaks. Nothing in `tests/` covered `roles/base` before this.
- **The recorded guideline's transport did not exist.** T0027's 2026-07-23 design called for the detector to write to "the same `integrations/unix` transport that already carries the NAS `gate.prom` — no regex, no new plumbing". The two hosts it targets ran **no textfile collector**, and `integrations/unix` is static-mode Grafana Agent vocabulary this flow-mode fleet does not use. Building the transport became its own defect, [[T0100]].
- **The collector had been dropped for a contingent reason that expired.** Spec `00069` moved long-lived services to `/metrics` endpoints, correctly; the capture-host Alloy shed its textfile collector alongside, stating why: *"there is no gate/replay/archive timer on them."* A one-off runs for a second and has no process to scrape, so textfile is the right transport for that class — which is exactly why the ops node kept its collector. The premise died the day a one-off timer landed. The rule is now written into the config: long-lived → `/metrics`, one-off → `.prom`.
- **It had already cost [[T0021]] everything.** That prune shipped publishing nothing, reasoning that no collector would read it — true, and the wrong conclusion; the fix is to add the reader. The fallback claimed in its place was false too: the journal keep-regex admitted only `zcrypto-capture-prune.service`, so its log line never reached Loki. For its first day the prune was observable through **nothing**. Both halves fixed; its metrics now reach Grafana Cloud.
- **No compose edit and no volume change** — the material simplification. Alloy already bind-mounts `/:/host/root:ro`, and the collector's `directory` is just a path it globs, unrelated to `rootfs_path`. But it *does* need a container recreate, which the spec originally got wrong: `config.alloy` is a **single-file** bind mount, so `copy:` writes a new inode the running container never sees, and no role in the fleet restarts Alloy — deliberately. The converge lands the config; a human recreates.
- **Three ways a `.prom` fails, and each defeats the rule that catches the others.** *Stale* — the collector serves the last values forever and `node_textfile_scrape_error` does not fire (it covers malformed input only). *Unreadable* — it does. *Absent entirely* — neither can, because a staleness rule cannot fire on a series that does not exist and `noDataState: OK` renders that silence green. So one rule became four: per-publisher `node_textfile_mtime_seconds` (1 h for the 15-minute probe, 26 h for the daily prune — one shared window would have let the attended-reboot net sit dead for a day), `scrape_error`, and `count(node_reboot_required) < 2`.
- **The review caught the defect reproducing itself one layer down, before deploy.** `mktemp` creates `0600` and `mv` *preserves* it, so the retro-fixed prune published root-only while Alloy reads as non-root — measured `600` against the new detector's `644`, because the `chmod` was written into the new script, with a comment explaining exactly why, and not into the one being retro-fixed. It would have shipped inert, and the staleness rule could not have surfaced it: the collector skips an unreadable file *before* stamping its mtime, so there was no series to go stale. The allow-list was also admitting three of the prune's four series; `oldest_day_age_seconds` was being dropped at remote-write — the exact trap the spec claims to close. It is now derived from what the script actually emits, not a hand-kept list.
- **Four tests passed for the wrong reason**, all found by the same review. The atomicity test asserted only that no temp file was left behind — which a truncate-in-place rewrite satisfies trivially, so it passed *because* the safety mechanism was removed; now asserted by inode. The producer/reader test passed with `"textfile"` removed from `set_collectors`. The timer-enable test split on the last of three occurrences and never inspected the task it named. The `.timer` file had no content coverage at all.
- **Ordering was load-bearing, not cosmetic.** T0027's own verification step — touch and remove the flag — would have rebooted the live capture + engine primary had it run while `Automatic-Reboot` was still `"true"`, since a present `/run/reboot-required` is precisely what unattended-upgrades acts on. The flip landed and was verified first, and the precondition was re-checked on-host in the same command that touched the flag.
- **Deployed attended in three scoped converges, verified by outcome.** `--tags base` (`changed=1` per host, one line), `--tags capture` with the running digests (`changed=6`), `--tags engine` (`changed=2`) — capture `StartedAt` and engine `StartedAt` byte-identical across all three, `RestartCount` 0. Then read back from Grafana Cloud rather than inferred: `zcrypto_engine_journal_prune_kept_days{zcrypto}=16`, `oldest_day_age_seconds=1377233`, `node_reboot_required` 0 on both hosts flipping to 1 and back under a real flag touch, `node_textfile_mtime_seconds` carrying the `file` label all three staleness rules key on, and `node_textfile_scrape_error` 0 on all four hosts.

## 2026-07-26 — the fault signals nothing was watching ([[T0008]], [[T0100]])

- **Six alert rules, for metrics that had been live and unwatched.** [[T0008]]'s own trigger was the starting point — spec `00069` shipped `zcrypto_capture_book_desynced` and `zcrypto_capture_resubscribes_total` on both hosts and `alerts.yaml` queried neither — but writing the guard test first turned up four more, and the worst of them was not on anyone's list.
- **A breached disk watermark discards data, and only a dead-man knew.** `cli/capture/command.py`'s book and trade handlers `continue`/`return` past the writers while `watermark.breached` holds, so incoming L2 and trades are **dropped**, unbackfillable. It also withholds the healthcheck ping — so hc.io pages *"this host went quiet"*, not *"capture is discarding"*, and nothing could tell those apart. Now `critical` at 2 min.
- **`min_over_time`, not the `max_over_time` the topic proposed.** `max` fires on any desync inside the window including the transient the daemon self-heals 234/234 times; `min` requires the pair desynced for the *whole* window — the stuck shape, which is what "past its single resubscribe" means. Recovery is one fire-and-forget resubscribe fired only on the `not was_desynced` transition, so a failed attempt is never retried.
- **Measurement changed a decision, which is the point of taking it first.** `zcrypto_capture_reconnects_total` looked like an obvious fourth rule until it measured **32–35 per host per week** — baseline, not a fault; a naive threshold would have paged ~5×/day. Excluded with the number written down. [[T0035]]'s trigger is a counter *reset* correlated with a `process_start_time_seconds` jump, which needs the correlation rather than a count.
- **Review caught a rule that would have misfired during the absence.** `increase()` extrapolates to the window edges, so `increase(counter[1d]) > 1` returns ~1.0007 for a **single** increment at any nonzero sample offset — one genuine self-healed venue desync would have paged, asserted a re-elevating rate that was not happening, and stayed firing a full day. Reproduced numerically; threshold is 1.5. The same alarm fatigue this file already killed once by deleting the `Gate · not MET` rule.
- **The guard is derived from the keep-regex, not hand-listed** — 46 admitted series, 38 excluded each with a written reason, 8 requiring a rule. A hand-list cannot catch the *next* unwatched metric, which is precisely the mechanism that let these sit for two months. Matching is word-boundary: `node_load1` is a strict prefix of `node_load15` and both are admitted, so a substring test let one satisfy the other.
- **One rule exists to keep the others honest.** `node_scrape_collector_success` = 0 means a collector errored and its entire family goes *absent* — and NoData maps to `OK` on this fleet, so a failed `filesystem` collector silently disarms the disk-low rule and a failed `textfile` collector disarms every one-off timer alert.

## 2026-07-26 — internal vocabulary off the operator's screen, enforced by a test ([[T0096]])

- **The rule is `.claude/rules/operator-facing-text.md`**: `Phase <N>`, `T<NNNN>`, `iter-<NNN>` and `spec <NNNNN>` never reach a surface visible **without opening the repo**. Source comments, docstrings, `docs/` and commit messages stay untouched, where those tokens are load-bearing.
- **A test, not a sweep.** A sweep has to be re-run by whoever remembers it exists; `tests/test_internal_terms_not_operator_visible.py` runs on every PR. That also settles the topic's open question about a pre-commit guard: a hook would add noise for a check the suite already performs.
- **Two rounds, and the second was three times the first.** The initial pass found **23** leaks against an estimated 14. Adversarial review then showed the *detector* had blind spots, and widening it found **25 more** — 48 sites, 62 token instances, across eight surfaces. The method was the work; the sweep was mechanical.
- **The three blind spots are each a false-all-clear waiting to happen.** *Indirection*: a message built into a variable and echoed later is invisible to any call-site walk, and chasing it statically is dataflow analysis — so the detector checks every **non-docstring literal** instead, which costs four findings and no false positives. *Line wrapping*: Rich breaks the `--help` column at ~46 chars, so `spec 00052` split across a line was invisible to a regex expecting the space. *A too-greedy path carve-out*: `spec 00054/T0058` is two tokens joined by a slash, not a path — and that exact string was in this repo, in a `Description=` that only failed on its **other** token.
- **Grafana alert summaries are the strongest case in the rule, not an exception to it.** The reasoning that excuses log lines — the reader has the repo open — is exactly inverted for an alert read on a phone in Slack. Ten summaries carried trailing tokens appended to otherwise actionable text; the tokens went, the instructions stayed. Pushed live and read back: 48 rules, zero carrying vocabulary.
- **Surfaces the rule named but nothing checked** are now checked: Prometheus metric HELP text, Grafana panel descriptions, compose `${VAR:?message}` errors, and `infra/` shell as well as Python — a textfile exporter's `# HELP` lines are printed by `printf`, and widening the scanned *directory* without widening the *file types* is how the trophy example (`spec 00054/T0058`, the very string used to demonstrate the path-carve-out fix) survived two sweeps.
- **Several leaks were added the same evening**, by the T0021 and T0027 iterations, while this topic sat in the queue. That is the argument for enforcement over a one-time sweep, in one line.
- **Two boundaries decided rather than left implicit**: log lines out of scope (verified, not assumed — no `logger.*` literal carries vocabulary today); a token inside a file path is an operand, not a reference, so it stays.
- **Caught in my own work twice.** A slice-then-replace edit deleted the CLI test function and the follow-up `.replace()` silently no-op'd, because I omitted the `assert old in s` that `agent-ops.md` requires — the suite dropping from 51 failures to 21 with no corresponding fix is what exposed it. Later, a regex meant to tidy `" ."` glued a sentence to `.claude/rules/...`; a targeted re-grep for that shape found the one instance.

## 2026-07-27 — Alloy v1.17.1 → v1.18.0 across the fleet, and what the run taught the skill ([[T0081]])

- **All four Alloy containers bumped in canary order** (ops → NAS → capture secondary → capture primary), `4f6ddc56ffdc` → `491b0578c049`, every one landing with `RestartCount` 0. The `/zcrypto-bump-alloy` skill's first real run, which is what this topic was waiting on.
- **No protected service was touched, and that was verified rather than assumed.** Passing each host's *currently-running* capture digest kept the capture-compose render idempotent — it never entered the changed set, so the `restart capture service` handler could not fire. On the primary, `--skip-tags engine` kept the engine play out entirely: the live trade engine's `StartedAt` is byte-identical across the whole run, and both capture daemons kept writing parquet throughout (17 and 22 files in the 3 minutes after their bumps).
- **Config risk was measured, not assumed.** v1.18.0 ships two breaking changes, both `otelcol.*`; the fleet uses only `loki.*` and `prometheus.*`. All three `config.alloy` files were then dry-started against the new binary with `alloy validate` before any host was touched — worth doing regardless, since the capture config had been changed hours earlier that same evening.
- **The run corrected four things in the written procedure**, which is the entire argument for validating a deploy skill by running it:
  1. **`docker compose up -d` needs `sudo`** — `alloy-secrets.env` is `0600 zcrypto-alloy`. The unprivileged form fails *before* touching the container, so the old one keeps running and no dark window opens; but the skill's command omitted it.
  2. **Every ops recreate trips `Ops · ERROR logs`** — the *outgoing* container logs two `service=remotecfg … err="noop client"` errors on shutdown, and that rule is host-wide with `for: 0s` over 15 m. Telling it from a real fault needs a specific check: the lines are stamped ~200 ms *before* the new container's `StartedAt`, and the new container's logs are clean.
  3. **Alloy goes quiet after startup**, so the prescribed `{host=…, container="alloy"}` freshness check reads empty on a healthy host bumped 30 minutes earlier. The liveness proof has to accept any container's line — caught by refusing to treat an empty filtered query as an absent event, and finding the positive trace (`liquidations` lines still flowing, `loki_sent_entries` climbing).
  4. **A fresh container reports `samples_total=0`** until its first 60 s scrape lands; reading too eagerly looks like a dead shipper.
- **Verified per host before moving on**: new digest with zero restarts; `failed_total` 0 and `samples_total` climbing on `127.0.0.1:12345`; `up` present in Cloud; six `process_*` families. Host-specific — the ops liquidations poller still scraped, and the NAS's next `archive-pull` cycle logging `checked=9215 ok=9215 failed=0` after the apply bounced it. End state: **48 alert rules, none firing.**
- **The bump also carried the previous iteration's cosmetic tail.** Every converge landed the T0096 `Description=` rewordings for that host's timers — exactly the "lands at each role's next natural converge" prediction, arriving within the hour rather than someday.

## 2026-07-27 — two drills, and a real incident that arrived on its own ([[T0035]], [[T0008]], [[T0049]], [[T0101]])

- **[[T0035]] resolved by drill, after two half-verifications that never tested the handler.** The code fix was landed and regression-tested; what had never happened was the handler for a *rejected* connect attempt executing in production — zero `attempt [2-9]` lines across the full Loki retention, because every observed reconnect succeeded first try. A throwaway capture on the ops node (same pinned digest, real Kraken, isolated dir, no Loki creds, no dead-man URL) closed that.
- **Kraken supplied the fault unprompted.** The sandbox's *very first* connect was rejected with `server rejected WebSocket connection: HTTP 503` — this topic's originating fault, arriving within seconds without being induced. That says the 503 is more common than production logs suggest; production has simply always succeeded on the retry. A `docker network disconnect` then supplied the `OSError` arm, so both halves of `except (WebSocketException, OSError, TimeoutError)` were exercised.
- **What the sustained blackout proved**, none of it previously observed: backoff `1, 2, 4, 8, 16, 32, 60, 60, 60, 60` (exponential, capped); the `_RECONNECT_ERROR_EVERY` line at attempt 10; **`restarts=0` across ~6 minutes and 15 attempts** — the actual claim; clean resubscribe-and-resume on reconnect; and **data consistency across the fault**, with the spanning hour's book *and* trades `.parquet` both hash-verifying against their manifests.
- **[[T0008]]'s alert leg validated, and deliberately scored as not closing the topic.** Injecting `zcrypto_capture_book_desynced{pair="DRILL"} 1` through the textfile transport built the day before drove the full path: Cloud → `Pending` → `Alerting` → Slack naming the pair → resolved on removal, with all 24 real pair instances staying `Normal` throughout. But no desync occurred, so `resubscribe_book` never ran — the safety net is proven, the defect behind it is not. Recorded as a partial rather than counted as a validation that did not happen.
- **A drill's latency is not the real latency.** The injected series alerted in 6.4 min against the summary's stated ~20; `min_over_time` aggregates only present samples, so a brand-new series satisfies it immediately while a real desync flips an *existing* series and carries a full window of zeros. The summary is right for the real case. Recorded in T0049, because the same trap applies to any alert-path injection.
- **A real incident arrived mid-session and is the better teacher.** *Reconciler · primary gap rate high* fired at 09:48Z — **41 minutes before the drills**, so unrelated. The primary needed **2,318 s (~39 min)** of healing in 6 h while its own `zcrypto_capture_gap_seconds_total` read **zero**. Windows of 1 h / 6 h / 24 h / 7 d measured 0 / 2318 / 2313 / 2312 s: one event, already over, not a trend. `healed_gap_seconds_total` matched it, so the secondary minted every second — **no data loss**, the redundant-capture design working silently.
- **The disagreement is the finding, and it under-reports.** The daemon booked no gap for a period the reconciler measured at 39 minutes; the arithmetic (~39 s per pair per reconnect across 5 reconnects and 12 pairs) points at reconnect silence never being booked, though that is unverified against the ledger. Registered as [[T0101]] rather than absorbed, because everything keyed on the daemon's gap counter — including the operator's sense of how healthy the primary is — reads clean through a 39-minute hole.
- **[[T0049]] gains the half it was missing.** Its matrix rehearsed *inducing* faults; it had no entry for reconstructing one already over and silently absorbed. Added as a scenario with the sequence this incident actually required — bound it across widening windows before calling it a trend, establish loss-or-no-loss *first*, cross-check the two producers against each other, and rule out your own same-day changes by timestamp (without which this would have been misattributed to the Alloy bump, the cheapest wrong answer available). Both drill methods are recorded there for reuse.

## 2026-07-27 — desync recovery survives its first attempt failing ([[T0008]], spec 00072)

- **Recovery is now a bounded ladder**: the existing transition resubscribe, then 3 retries at 5/10/20 s, then exactly one escalation to a full reconnect, then stop. Built on the owner's ruling to do (a)+(c) as one design rather than two — each rung clears strictly more state than the last, which is the whole reason escalation is meaningful.
- **Retries are driven by desync STATE, never by protocol responses**, and that is the load-bearing choice. A snapshot that fails its own checksum produces no error frame at all — Kraken is satisfied, the protocol succeeded, the book is still wrong. Only "still desynced N seconds later" can see it, which is also why `req_id` correlation is not a substitute (registered as [[T0102]] for its separate observability value).
- **The parameters are unfitted, and the spec says so rather than implying otherwise.** `zcrypto_capture_resubscribes_total` and `_gap_seconds_total` both read **0 on both hosts** — the 2026-07-13 root-cause fix took desyncs to zero, so there is no heal-latency distribution to fit. They come from the two constraints that are known: a grace generous against any snapshot round-trip, and a total ladder short enough that the dead-man it withholds **for all 12 pairs** stays inside its grace.
- **The dead-man is the real budget.** While any pair is desynced, `is_healthy()` is False and the ping is gated on it, so the entire ladder runs with that host's hc.io watchdog dark for every pair. That is why the ladder is measured in seconds — a "patient" ladder silently trades one stuck pair for a fleet-wide dead-man outage.
- **Drilled against real Kraken, both halves.** Hold 45 s: rung 1 fired **once** (guard held, no storm), retries at +23.4 / +5.002 / +10.004 s, pair healed before escalation. Hold 600 s: escalation at **+20.002 s**, `force_reconnect` reached `stream()`'s existing `ConnectionClosed` path, and over the following ~4 minutes with the pair still held desynced there was **no second escalation**. `retries=3, escalations=1, restarts=0`; desync→escalation **58.3 s** against the predicted ~55 s.
- **The terminal state was the part worth drilling.** A cycling ladder turns one stuck pair into a reconnect loop, and every reconnect drops all 12 pairs — strictly worse than the defect. No unit test establishes that in a live process.
- **Two drill runs failed first, both in the instrument rather than the ladder.** Faking only the handler's return value left the book reporting healthy, so `was_desynced` read False on every forced failure and the transition guard re-fired rung 1 twice in 0.5 s — the exact storm it exists to prevent — while the state-reading recovery loop never engaged. Setting `book.desynced` as well, but on a book whose data was genuinely valid, let the next update recompute a good CRC and heal it in milliseconds, before the 20 s grace could elapse; the daemon stayed healthy and kept writing parquet, which is precisely what made it look like a pass. The knob's unit became a **duration**, since a pair is stuck for a length of time and neither a count nor a flag expresses that.
- **The generalisable lesson, recorded in [[T0049]]:** a fault injector that does not move the state the system actually keys on does not simulate the fault — it simulates a different one, and does so convincingly enough to read as a pass.
- **Whole-branch review then found two high-severity defects, both in the driver loop rather than the ladder's arithmetic, and neither reachable by either drill.** *The escalation bound leaked, and the escalation itself was the leak*: `note_recovered` cleared the escalation record, so "one per pair per hour" bound only a pair that stayed *continuously* desynced — while the likeliest healer of an escalated pair is that escalation's own reconnect, which forces a fresh snapshot for everything. Escalate → reconnect → heal → record erased → escalate again ~55 s later. Simulated against the real ladder: a pair desyncing every 10 min escalated **6×/hour against the intended 1**, a flapping pair **51×/hour** — **72** and **~610** reconnects/hour fleet-wide across 12 pairs, against 12. Those are state-machine ceilings, not a steady state: the ladder has no clock, so the reconnect's own downtime goes uncharged. The 6:1 over-run is the finding, and at ~39 s of silence per pair per reconnect ([[T0101]]'s arithmetic from one incident, not a fitted number) it is the "strictly worse than the defect" outcome D5 warns about. Neither drill could have caught it: drill A never escalated, so there was no record to erase, and drill B held the pair desynced throughout — the one shape where the old code was correct.
- **The second: one pair's exception starved eleven.** The `try` wrapped the whole sweep and `books` is insertion-ordered, so a raising pair deterministically starved every pair after it — this topic's own defect reintroduced for 11 pairs by 1. Compounding it, `note_attempt` was skipped when the call raised, so the pair retried every tick forever *without* ever reaching rung 3 (57 retries in 5 min, 0 escalations). The trigger is self-inflicted: pair A's `force_reconnect` closes the socket pair B then sends into.
- **Mutation testing was the sharpest finding, and it took three passes to actually land.** Deleting `note_desync`, `note_recovered`, or the `recovery` argument entirely — a ladder nobody arms — passed the whole suite. The new suite's own docstring claimed to cover that seam while no test called `_handle_book_message`: the [[T0035]] / [[T0100]] trap one level down, inside the branch built to escape it.
- **Calling the real handler did not cover the real caller.** A second review re-ran the mutations and found `recovery` could still be dropped from `_consume`'s call with everything green — the tests passed the ladder in explicitly, so none of them ever executed the production call site, and both functions defaulted the parameter to `None` so the drop was silent rather than a `TypeError`. Fixed on both axes: the argument is now required, and a test drives `_consume` itself so that `TypeError` is reached by the suite. Five mutations, each verified dead by re-running it.
- **The mutation harness itself was the third defect, and it faked two passes.** `git archive HEAD` sandboxes are useless here: the editable install's `.pth` puts the repo on `sys.path`, so `pytest` inside a sandbox imports the **repo's** `cli` and `tests` — every "survived" and "killed" verdict measured unmutated code. Caught only because a mutation that provably raises `TypeError` when invoked directly still reported green under pytest. Mutations now run in-repo against a committed tree, restored with `git checkout --` after each.
- **Incomplete wiring broke six existing tests** — the scheduler referenced a `recovery` that was never constructed. Caught by running the capture suites rather than only the new ones, which is the whole argument for not scoping a test run to the files you touched.
- **Merged unrolled, deliberately.** Capture-daemon code on the unbackfillable path reaches the fleet only through an image build, the ≥24 h secondary canary bake, then the primary re-pin; the deploy is its own attended step.

## 2026-07-27 — a stream that is connected, subscribed, and silent ([[T0101]], spec 00073)

- **The 2026-07-27 blackout was a Kraken WebSocket service restart, and the daemon was structurally unable to see it.** Both hosts lost all 12 book streams 07:01:04 → ~07:04:35 UTC — every pair's last message within **0.454658 s** of every other — then received an application-layer `1012 (service restart)` close **19.65 ms apart** (`07:04:49.352183069Z` primary, `07:04:49.332529441Z` secondary), and the primary's next connect was answered `HTTP 503`. Exactly two such frames exist in 19.3 days of retention (07-13 and 07-27, both Mondays, 14 days apart) with **07-20 clean as a negative control** — a signature worth acting on, not a schedule.
- **Every liveness signal answered correctly while the data was absent.** The socket stayed open (a 1012 cannot arrive on a dead one), the `websockets` keepalive completed **≥11 ping/pong round trips** at its 20 s/20 s defaults, no gap window existed — so the healthchecks.io dead-man **pinged green through a total 12-pair blackout on both hosts**. `gap_seconds_total` read `0.0` on all 12 pairs across 97.57 h of uptime containing 33 reconnects, and neither container log held a single `gap start` line in its whole 4-day life.
- **T0101's own diagnosis was wrong, and correcting it was the useful part.** It read the event as unbooked *reconnect* silence at "~39 s per pair per reconnect". Measured reconnect cost is **single-digit seconds** — 0.002–2.82 s close-frame-to-first-book-event, 2.276–6.204 s close-to-first-processed-message; the two methods disagree on the interval but not on the order of magnitude — 2,318/12 ≈ 193 s is **one** silence window per pair. The defect was never reconnect silence but **connected-but-silent upstream** silence, which no reconnect accounting would have found. The falsified 39 s figure is withdrawn from spec `00072` D5 and from `desync_recovery.py`, where the escalation-bound argument survives *a fortiori* at the smaller number.
- **Its "Not data loss — and that is verified" was also false, and the verification was circular** — it checked the healed counter against the healable counter, both summed from the same ledger. Measured against the parquet: **2,437.147792 s** of L2 permanently absent across 12 pairs, against **82.955463 s** actually healed of the 2,311.536587 s booked. A CRITICAL page had already fired at **09:18:35Z**, thirty minutes *before* the warning the topic was written from.
- **Three changes shipped.** A per-pair book staleness watchdog stamped at `last_seen` rather than at detection (stamping at `now` would discard the threshold from every outage — the same under-reporting direction as the defect); the venue's `status` channel recorded instead of dropped unlogged; and a `1012` flooring the first reconnect delay at 5 s per Kraken's guidance, after the primary's 1.0 s attempt earned that `HTTP 503`.
- **The threshold is derived, not guessed.** Worst *natural* intra-hour book spacing fleet-wide is **12.196 s** (ETH/BTC, the thinnest leg; largest natural hourly max **12.299 s** over 104 segments), so 30 s is ~2.4× the binding pair's p99 — and it equals the reconciler's `--min-gap-seconds`, so the two producers finally measure the same thing rather than merely adjacent things.
- **Silence gets its OWN window in `GapMonitor`**, for the same reason the disk watermark has one: `start_gap` is idempotent per pair, so routing silence through it would let a concurrent `checksum_resync` gap swallow it, and whichever closed first would book the other's duration. The two faults genuinely co-occur.
- **The dead-man is deliberately NOT gated on silence yet**, and the consequence is stated rather than glossed: a repeat of 07-27 is now correctly counted and still pages nobody. `is_healthy()` gates the ping for all 12 pairs on both hosts, so a threshold fitted to ~4 days of thin-leg data would trade a metric gap for a liveness outage. A test asserts the current behaviour so reversing it cannot happen by accident. Registered as [[T0105]].
- **Mutation caught what review did not: a watchdog nobody starts passed all 22 tests.** Every property test drives the loop body directly, so all of them hold on a loop `_run` never schedules — [[T0100]]'s defect in another costume, and the second time this class has survived a branch built to escape it. The guard parses `_run`'s AST rather than grepping the module, because a grep for `_staleness_loop(` matches the definition and would pass on the exact mutation it exists to catch.
- **Two repo comments were rewritten because they argued against the fix.** `tests/test_infra_alert_rules.py` excluded `gap_seconds_total` on the grounds that it "measured zero across all 24 series" and that an open gap is "covered twice over — the dead-man and the desync rule". The zero **was** the blind spot, and neither covering mechanism saw it.
- **The new gauge was added to the Alloy keep-regex in the same change.** It is an allow-list with no wildcard, so a published-but-unadmitted series is dropped at remote-write and looks identical to a producer that never ran — the [[T0051]] trap, avoided here only because the exclusion-list staleness test failed loudly first.
- **Three topics split out rather than left inside an archived file**: [[T0103]] (the reconciler books unfilled silence as healed, and double-counts 2,187.03 s into both a "covered" and a "nobody covered" counter), [[T0104]] (the panel emits a frozen book across the hole — 212 rows with **2 distinct `mid`** against 8 across the hour's other zero-update seconds, already materialized into the research feature set), and [[T0105]].
- **One thing deliberately left unexcluded**: both hosts resolve `ws.kraken.com` to the *identical* Cloudflare anycast set, so host independence is established but **network-path independence is not** — and `HTTP 503` is what a CDN edge returns when it cannot reach origin. It changes no decision here, since the watchdog fires either way, but it bears on whether a mirror on a different provider would have covered the loss.

## 2026-07-27 — the corpus learns to refine itself (spec 00074, T0099, T0084 fallout)

- **`/zcrypto-refine-rules` exists and its first run is this entry's iteration**: harvest → joint graduation → staleness sweep → condense → lossless verify, user-invoked only, with the watermark a git trailer prose cannot fake. The cold spec+plan review it introduced ran three times today and returned FIX FIRST or LOSSY each time — every verdict correct.
- **Graduation: 12 memory items walked jointly** — 7 graduated (4 one-liners into `agent-ops`, the model-attribution mechanism into `commit-messages`, the memo-authority + registered-but-unqueued lesson into `open-topics`, the write-time deferral sweep as a mandatory `open-pr` step), 4 dropped as duplicates verified at equal-or-greater strength, 1 stays. **The case study passed**: both seeded fixtures traversed the full pipeline and took joint dispositions — one as the general drift-is-not-license form, one as mechanization (P6) rather than more prose.
- **The staleness sweep (10 read-only checkers, 15 findings) was all real**: a census-shaped CLAUDE.md bullet three commands stale, `docs/universe/` missing from the docs map, dead and circular cross-references in three skills, a skill whose own mandated loop its allowlist forbade, a release skill asserting opposite facts about develop's protection in two steps (settled by measurement: PR-protected, step 17 wins), and a changelog template whose `v` its own extraction awk required.
- **The D-number enforcement gap closed and immediately paid**: the rule always claimed `spec` D-numbers as guarded vocabulary; the test had no pattern for them. Adding it flagged four live leaks (soak header, soak note, backfill error message, one alert summary) — each reworded to keep the semantics with the token in an adjacent comment, and the reworded summary pushed to Grafana Cloud and read back the same evening.
- **Dependabot de-grouped on the owner's ruling** — per-ecosystem PRs on the pre-group schedules recovered from git history — resolving [[T0099]] by construction: the is-the-group-honored question is moot for a config that declares no group.
- **`fleet-deploys.md` went 6,261 → 4,664 bytes in one day**: rollout mechanics to their single home in the `zcrypto-rollout-image` skill, the Reboots section (host posture, not deploy law) to `fleet.md` — with the owner catching what the sweep missed: "the reverse of the image-rollout order" was false; reboots use the **same** canary order.
- **The P7 gate returned LOSSY twice and was right twice.** The T0084 shrink had deleted a bullet that also served engine converges; the round's own diff carried three losses — a routing sentence pointing reboots at the file they left, a graduated clause that **inverted its own lesson's ground truth** (naming the artifact class the lesson had measured stale), and a lesson half that had landed nowhere. Every changed line, not only removals — that scope is why the second and third were catchable at all.
- **The skill corrected itself mid-run** (the T0081 pattern): sequential decision gates — fact-collection may run ahead, but the next step's findings are held until the previous step's dispositions close; presenting two open sets collided them on the user's desk.
- **Net always-loaded: 33,693 → 34,963 bytes (+1,270, presented for the owner's OK at close — growth is never assumed approved)** — seven graduated lessons plus the P7 repairs cost more than the Reboots relocation recovered, and the round reports the measured sign rather than the intended one. The memory inbox closed at one item.

## 2026-07-28 — the instruments that measured the blackout, corrected (T0104, T0102, T0103, T0087, T0043)

- **Two of the 2026-07-27 blackout's three remediation children shipped** — T0103 and T0104; T0105 stays deliberately deferred — alongside [[T0102]], which is a T0008 child rather than a blackout one. The theme across all of them is the one T0101 itself ended on: every instrument that looked at that event reported it wrongly, and each in the reassuring direction.
- **The panel's dense grid now says when a book is frozen** (`stale_seconds`, spec `00052` generation 2): the 1-second grid emits a row per second whether or not data exists, so an archive hole produces carried-forward rows of a stale book that `updates == 0` alone cannot distinguish from a quiet second — and the hour still emits its full 3,600 either way. Measured on the real 07:00 hour: **174** rows above the 30 s threshold, 3,425 below, and **1 null** — the anchor sidecar predates the column, and polars drops nulls from *both* sides of a `> 30` filter, so the null is the hazard worth publishing. The column addition is a **generation** change: without the `SCHEMA_VERSION` bump a mixed 19/20-column tree raises `SchemaError` on any multi-hour scan, even one touching only old columns, and `_check_generation` would have waved it through because none of its four compared keys changed. The re-materialization is an attended sequencing requirement — deploy and regenerate in one window, or the hourly timer aborts and its dead-man pages meanwhile.
- **The capture daemon correlates its resubscribes by `req_id`**: all four reply categories now reach `note_reply` — the two acks it discarded outright, and the two errors it only printed — so a rejected resubscribe is correlated and counted rather than merely logged. That is T0102's observability half and its prevention half (the `subscribe` waits on the `unsubscribe_ack`), since a resubscribe that silently failed is indistinguishable from one that silently succeeded until the next gap.
- **The reconciler's counters measure the splice rather than the window that admitted it.** `secondary_covers` admits a gap on one secondary `update` row anywhere inside it; one row admits a window, it does not fill one. The blackout hour claimed **2,311.536587 s** healed against a measured **82.955463 s** — 3.59% — and LINK/EUR booked 209.493793 s on 6 update rows inside a spliced block spanning 1.638743 s — the updates themselves span 5 ms. `measure_residual` now re-runs the window arithmetic over the frame actually minted, `healed_seconds` becomes claimed − unfilled, and the unfilled remainder reaches the provenance sidecar, which had been writing a literal `residual_gaps: []` on every minted hour in the archive.
- **The fix had to avoid replacing a heal over-count with a loss over-count**, since `both_streams_silent` already books the fleet-dark intersection as window × stream count and 2,187.027326 of that event's stream-seconds sat in a "we covered it" and a "nobody covered it" counter simultaneously. Each side now subtracts what the **ledger records** the other already booked — never a fresh recomputation, which review measured wrong in both directions: a pair whose mirrors land after the fleet decision had its whole loss deleted from a total nobody added it to, and an unreadable segment suppresses the detector for a cycle, so a pair minting meanwhile booked first and the fleet record booked the same seconds again. Attribution is order-independent, and both orders are tests. `residual_seconds` also joined the per-(pair, kind, hour) dedup, because a `would_mint` → `minted` flip carries the same measured residual on both records and the second step reads to the CRITICAL page as a fresh permanent-loss event.
- **The correction was validated on a known answer before its own verdicts were read.** The 2026-07-17 drill hour — primary deliberately stopped, secondary genuinely up — measures 15,509.529138 s healed against 15,509.529138 s claimed, **0.0000% fiction**: the stricter measure does not slander a genuine heal. The blackout hour's 82.955463 s then agrees with T0103's figure **to the microsecond** — re-derived by a second route over the minted parquet rather than independently corroborated: both are the same window arithmetic at the same threshold, so the agreement confirms the implementation, not the premise. The topic's own lifetime-fiction figure was wrong (2,252.718188 s reconciled with neither measurement beside it); measured, it is **2,228.581124 s, 12.5053%** of the ledger's 17,821.065725 s, and the file was rewritten in place.
- **A suspected symmetric defect turned out not to exist, and was dropped with the measurement as the reason**: all 18 `trade_deficit` records enumerated, every one a *secondary* deficit (`trades_added = 0` in all 18, 1,926 rows). The canonical trade stream lost nothing, so their `residual_seconds: 0.0` is correct. Permanent trade loss cannot appear in such a record at all — rows absent from both mirrors are invisible to a cross-host union, and `backfill-trades` already measures them as `unrecoverable`.
- **The trade backfill gained the two buckets a real outcome could fall between.** `mint_failed` counts rows the REST served for an hour whose mint then raised — previously in no printed bucket at all — and it is reported but **never subtracted** from the post-mint accounting check, because a fetched-but-unminted row is retryable and an explained absence would silence the sweep's strongest check on exactly the failure it exists for. `hours_repaired_after_loss` counts settled hours minted with no prior trades final while a book final **spanning** the same hour existed: the signature of a genuinely lost segment that the REST backfill would otherwise repair silently.
- **That signature's first cut over-claimed, and our own operations produce the false positive.** A book final's existence proves only that one event landed *somewhere* in the hour — a capture restart at :45 leaves one too, and every image converge restarts capture. The witness now reads the final's first and last `ts` and requires it to span the hour within 60 s at each edge; an unreadable or empty final witnesses nothing, since the question is whether the stream can be *proven* connected.
- **Attended tails, left registered rather than merged behind**: the panel re-materialization (T0104), the reconciler's two alert rules plus a third this work made stale (the CRITICAL page still sends the operator to look for a `both_streams_silent` record it no longer needs), the ledger's existing fiction (all three T0103), and the export of `hours_repaired_after_loss` (T0043). The capture image roll carrying T0102 is tracked in the memo's live rollout block rather than in a topic — the same shape T0008's deploy took, and the reason the memo is the in-flight authority. The ledger correction's shape is now decided — an appended note record carrying **no** counter field, because any record that lowers a monotone counter is a decrease, and Prometheus reads a decrease as a reset and reports the post-reset value as fresh healing: louder than the fiction it corrects.
- **Two process findings, both agreed with the owner rather than argued away.** First: three of these components met `spec-plan-locations.md`'s **substantive** bar — a schema-generation bump with a clock threaded across hour boundaries, a counter-semantics rewrite across four modules carrying an exactly-once attribution invariant, and an async ack-correlation state machine on the unbackfillable capture path — and all three ran the *trivial* path, so none got a committed spec, a committed plan, or the cold spec+plan review at the Fable floor that path prescribes for capture and canonical data. The evidence is this branch's own history: three defects fixed before merge were introduced by earlier commits on it. Not backfilled, deliberately — a spec written afterwards documents what was built instead of shaping it. Second: the `Reviewed-by:` trailer is missing on nine non-merge commits, three of them code. The reviews did happen; the durable evidence did not land, and the closeout-commit fallback cannot reach three of them because they land after it. Recorded in the PR body as a one-off on the owner's ruling rather than fixed by rewriting nine commits mid-history.
- **A four-dimension review before merge found five code defects the per-commit rounds had not** — archive correctness, panel/capture correctness, documentation truthfulness, and adversarial deploy readiness, run in parallel against the whole diff. The sharpest was the per-stream booking introduced *by this iteration*: it computed each stream's window from whichever mirrors were readable that cycle, so a stream whose second mirror arrived a cycle late had its entire single-mirror silence booked as permanent loss — 2,860 s over-booked against the old code's 465 s, unbounded where the old error was bounded by the intersection width, and reachable by the repo's own mandated primary-first pair-add order. The lesson is not that reviews find things; it is that **a review scoped to one commit cannot see a defect whose inputs come from the deploy environment**, and only the adversarial deploy dimension asked what the fleet would actually hand the code.

## 2026-07-29 — the config that never reached the process, and the triggers nobody was watching (T0107, T0109, T0110, T0106, T0108, T0103, T0105)

- **The deferred capture payload rolled** as `sha256:99faf165…`, secondary 00:52:53Z and primary 07:36:13Z after a 6 h 43 m bake with a strong-form prune (`deleted=210`). Both payload items were verified from the image's own surface rather than its tag. `continuity.py` on the pulled copy: 0 missing, 0 truncated, worst stream 0.0123% against the 0.1% bar — the mid-hour restart produced no truncated hour, because the stop→start window was 29 s.
- **Spec `00073`'s measurement half had been emitted and dropped since the day it was built.** Both capture hosts published `seconds_since_last_book_message` and `venue_status_total` to a keep-regex that never admitted them, so the fleet's answer to the 2026-07-27 blackout could not be alerted on or graphed. All five previously-undelivered series now arrive.
- **A `config.alloy` edit reached the host and not the process, on all three tiers.** A single-file bind mount binds the inode; Ansible's `copy` writes atomically and replaces it, so a running container reads a file no longer in the host tree — indefinitely, while every converge reports `changed`. Measured on `zcrypto-ops` right after a clean converge: host and container `sha256` **and** inode both differing. Fixed structurally — all three tiers mount the config *directory*, with a change-triggered `reload alloy` handler and a CI guard against regressing to a single-file mount.
- **That trap was already documented, and that is the actual finding.** Spec `00071` records the same mechanism in nearly the same words and chose attended-recreate, explicitly warning against a handler. The ruling never reached the operator-facing rules, so it was re-derived in production during the converge that was fixing the layer above it. **A decision recorded only in a spec is a decision nobody executing will read.**
- **Every ordinary check reported success while the container read a stale inode** — Ansible said `changed`, the host `grep` found the term, Alloy's reload genuinely succeeded, the exporter emitted every series. Each was accurate and each measured the wrong side of the mount. Only comparing host-vs-container `sha256` exposed it, so that comparison is now the verification.
- **The detection gap became its own converge-time assert (T0110)**, deliberately *outside* `when: <tier>_alloy_digest is defined` — inside, it would be skipped by exactly the converge that causes the drift. Its first implementation compared against `lookup('ansible.builtin.file', …) | hash('sha256')`; the file lookup **strips the trailing newline**, so it failed on a host byte-identical to the repo and would have failed on every converge forever. A permanently-red assert gets deleted, not investigated — the both-directions verification is what caught it.
- **A liveness gauge that means what it says** (T0106): the ship-success timestamp stamps only on a successful *non-empty* push, so it reads stale whenever logging is merely quiet, which is what healthy looks like. Measured on a green host during the bake: `last_success` 39 minutes stale while the new `last_cycle_timestamp_seconds` read 0 s. The rollout's abort row now reads the honest one and the old gauge is marked explicitly *not* an abort signal.
- **The unwitnessed-loss exposure was measured rather than argued** (T0108): a 400 h detect-only sweep over the full mirror overlap, using post-fix records so the split was a pure ledger read — one event in ~15 days, **6.821701 s** reaching no counter. Decided (a), leave it ledger-only; the residual counter's HELP now says it is a floor on permanent loss rather than the whole of it. That also settled a figure this topic had reversed itself on twice: 9.746002 s and 6.821701 s were each correct for their own code generation.
- **The pre-fix over-statement is recorded, not erased** (T0103). Appending a record that *lowered* `healed_gap_seconds_total` would read to Prometheus as a counter reset and make `increase()` report the post-reset value as fresh healing — louder than the fiction it corrects. Instead a `state: "correction"` note carrying no counter field, proven counter-neutral against the live ledger before the write and confirmed unchanged by the next cycle's re-derivation.
- **Triggers that nobody was watching became triggers that fire.** Two topics held items gated on events months away with no mechanism to notice them. The venue-status half is now a Grafana alert on `system != "online"` — a *negative* match, because the payload shape for a real outage is unobserved and an enumeration would silently miss whatever Kraken actually sends. Its first form used `increase()`, which cannot fire on a series that is born at 1 and never steps: it would have sat green through the very event it exists for.
- **`infra/runbooks/README.md` establishes the day-2 pattern** the drill matrix (T0049) will fill: one `##` section per trigger, anchored on what the operator already holds — the alert `uid` from Slack or the slug from a code comment — four fixed parts, two section kinds (ALERT, KNOWN LIMITATION), an explicitly *checkable* retirement condition per section, and a stated boundary that it is not a backlog. T0049's own next-step, which proposed a second runbook elsewhere, now points here.
- **A structural bound was retracted after a reviewer's counter-example.** The cross-hour straddle was described as bounded by `min_gap_seconds`; H−1 books a stream's tail only when the *fleet*-dark window clears the threshold, and that is bounded above by the stream's own, so a thin pair straddles far wider. The real bound is the measured silence distribution — which, unlike the structural claim, degrades correctly when a thin pair joins the universe. Closed as a consciously accepted limitation at 0.045%, recorded at the clamp where whoever edits it will meet it.
- **Seven review rounds, none clean, and several found defects in the previous round's fixes** — a production abort under `set -u`, a false-green alert built while fixing another, a real dropped series on ops, an alert that could not fire, a runbook query returning nothing, and repeatedly tests asserting a property one level shallower than the one that mattered. The durable lesson: **a guard is unproven until the bug it names is constructed and seen to fail it.** Reading the assertion is not enough.

______________________________________________________________________

## 2026-07-30 — the zaccess internet access portal (spec/plan 00075)

- **A Linode bridgehead now fronts three home services with the home network accepting nothing inbound.** WireGuard `zaccess0` (bridgehead ↔ ops, `AllowedIPs /32` each way so a compromised VPS gets no LAN route), `systemd-socket-proxyd` relays (no NAT, no IP forwarding on either host), and a Caddy mTLS edge. **G1** (`ssh -p 20022 zhaow@zaccess.zhaow.me` → lands on `zcrypto-ops`) and **G3** (`https://nas.zaccess.zhaow.me` → the DSM console, through mTLS → tunnel → relay with full NAS-cert verification) are live and verified end-to-end. **G2** (agentboard web terminal onto the `zcrypto` tmux) shipped **spike-gated** by design — `access_ops_agentboard_live` defaults false so no converge can bring an unauthenticated terminal onto the weeks-long autonomous session on its own — and then went **live in this same iteration** once the owner ran the D13 non-destructiveness spike and flipped the flag; the browser terminal, and multi-client resize across a MacBook browser, the ops terminal, and an Ubuntu terminal, were all confirmed end-to-end.
- **The bridgehead is the fleet's fifth Ansible host, in one playbook** (`access_host`, joins `observed`), reusing the audited `base`/`hardening`/`firewall`/`fail2ban`/`chrony` roles; two new roles (`access`, `access_ops`) carry the payload. No Docker on the edge — Caddy and Alloy are native apt-pinned packages (the first non-containerized Alloy on the fleet, `host="zaccess"`), WireGuard is in-kernel, the relays are systemd. The `firewall` role gained an `extra-ports` seam proven byte-identical for the capture hosts by a golden-render test; the `hardening` role's stale-drop-in removal was parameterized.
- **The mTLS edge is the sole control (D2) and is proven fail-closed**: `require_and_verify` + an internal-CA `trust_pool` + a `verifier leaf` that pins the exact client leaf and rejects even the empty set. The negative test (no client cert) dies at the TLS handshake with no HTTP status; the positive test returns 200. Revocation is a leaf PEM's absence: the drill issued a throwaway leaf through the real vault-CA pipeline, saw it accepted, deleted it, converged, and saw it refused while the MacBook leaf still worked — proving the host-side orphan (the copy has no `--delete`) is inert because the re-rendered Caddyfile no longer lists it. Production Let's Encrypt certs for both vhosts via HTTP-01; renewal is Caddy-intrinsic and watched by a cert-expiry alert over both edge certs and the NAS's Sectigo cert (all three `zaccess_tls_not_after_seconds` targets in Cloud).
- **Observability from Phase 2, both-ended**: native Alloy ships node metrics; a probe timer on each end writes WireGuard handshake age and TLS `notAfter`, so the tunnel and the NAS cert are watched from bridgehead and ops alike. Alerts pushed and confirmed on the stack: `zcrypto-alloy-dark-zaccess`, `zaccess-disk-high`, `zaccess-tunnel-stale`, `zaccess-cert-expiring`. No hc.io dead-man for the bridgehead — Grafana Cloud nodata already covers "the bridgehead died", and it stays the capture/ops failure domain (D16).
- **Six real defects surfaced only at converge, none catchable from the repo, each fixed and re-reviewed** — the value of executing against live hosts: the minimal bridgehead has no `gpg` (→ `deb822_repository` for both apt repos), `deb822` doesn't refresh the cache (→ `update_cache`), Caddy (uid 997) couldn't traverse a `0750` pinned-leaves dir so the gate sat on the deb default `:80` (→ explicit `file` task + reload notify; `directory_mode` alone only fires on creation), an ansible-`trim_blocks` interaction collapsed the `verifier leaf` block (→ no Jinja whitespace-control), the edge-cert probe silently skipped under `pipefail` because `s_client` exits non-zero against a `require_and_verify` endpoint (→ `|| true`, let the emptiness guard decide), and the NAS presents a leaf-only chain for a `*.zhaow.pro` cert — not `*.zhaow.me` (a global find/replace had corrupted that one measured value) — so D7 validation needed the Sectigo intermediate pinned as the upstream trust anchor.
- **Two operational disciplines recorded** (`fleet.md`): `zaccess` converges need a single-identity SSH agent because `ssh_hardening` sets `MaxAuthTries 2` and `run.sh` offers the zaccess key last; and a Route53 subdomain-delegation trap — the apex address records must live in the delegated child zone, not beside the parent's `NS` record, which shadows them (this was the live symptom that blocked Phase 1 until fixed).
- **The tmux `window-size manual` pin crashes tmux 3.5a, and it cost two live-session losses during go-live.** D13 had proposed pinning `window-size manual` as a resize-conflict mitigation; on tmux 3.5a the server aborts on the next `new-session`, so agentboard creating its session killed the whole tmux server — twice, taking the autonomous `zcrypto` session with it. The managed `~/.tmux.conf` now carries only `set -g mouse on` + `set -g history-limit 10000` with a comment banning the pin, and `tests/test_infra_tmux_conf_no_window_size_manual.py` fails any regression. New hard rules landed in `fleet.md`: never run a test agentboard on the ops default tmux socket, never blanket `pkill -f agentboard` (it kills the live systemd unit), never leave a test agentboard's `PORT` unset (it defaults to 4040 and collides with the live one).
- **agentboard is node-version-independent, and the ops sshd was hardened for its new exposure.** The unit sources nvm and execs `agentboard` at start time rather than a converge-resolved absolute path, so a node upgrade via nvm needs only `systemctl restart` — no codebase change, no re-converge. A whole-branch cold review (Fable 5) also caught that the G1 relay had exposed ops `:22` to the internet while the box kept its home-LAN password-auth default; a `PasswordAuthentication no` drop-in (validated `sshd -t`, reloaded not restarted) closed it. A companion script (`infra/scripts/zaccess-extract-client-cert.sh`) extracts the vaulted client `.p12` + passphrase to `0600` files for browser import.

## 2026-07-30 — refine-rules round 2 (guidance corpus refinement)

- **Five lessons graduated from the memory inbox into always-loaded rules** on per-item owner rulings: guard-proving (a guard is unproven until its named defect is constructed and seen to trip it, failure mode read) and the stale-`.pyc` mutation-probe hazard into `agent-ops.md`; stage-by-explicit-path-one-kind into `commit-messages.md`; operational-rulings-land-on-the-operating-surface into `spec-plan-locations.md`; closeout-is-the-branch's-end into `iterations-history.md`. One candidate (exposure-change re-derives hardening) dropped on the owner's word.
- **Two new refinement principles**: P9 — a prohibition names a live temptation (a `Never` against something absent from the environment is template residue); P10 — a mechanized case needs no ambient prose (once a mechanism refuses at the point of use, the rule shrinks to a pointer, keeping only the wrong-recovery clause a guard cannot enforce).
- **The `staged-kind` pre-commit hook** (`infra/scripts/staged-kind-check.sh`) mechanizes the staging rule it shipped beside — claude-kind files never share a commit with another kind; proven against a constructed mixed staging at script level and through pre-commit, with claude-only/feat-mix/empty stagings passing, and independently re-proven by the cold review in a scratch repo.
- **Guard-class sweep over every prohibition in the corpus** (95 rule + 83 skill items): zero dead guards in the rules; three in the skills (the `glab` ban — not installed, GitHub-only remote, citing a rule that never made the claim; `cd src` residue; a GitLab comparison), all deleted. Eight guarded-rule shrinks landed where a mechanism already refuses (the two `config.alloy` drift-assert bullets, merge-pr's UNKNOWN triplication, dependabot's `main` bullet against verified branch protection, among others); nearly everything else earned a keep because the guard fires late/lossy or cannot block the tempting wrong recovery.
- **Staleness fixes**: the protected `config.alloy` bullets' consequence narrative (the drift assert now fails loudly — the silent exit-0 story predated PR #227), the panel out-of-scope-subtrees bullet had denied the tree scan `1668c610` added in the same commit as the sentence, the Coveralls caveat described a failure `fail-on-error: false` now prevents, CLAUDE.md's registration clause overstated an import form, and the fleet docs' image-repo header under-enumerated twice in one review cycle — it now states the invariant (match the pin row to the service, never to the repo; four services pin `zcrypto-capture` independently).
- **[[T0111]] registered and memo-queued** — mechanize `fleet-deploys.md`: 15 buildable guards in two waves (role asserts, then converge/panel-regenerate/postverify scripts) plus two M-sized asserts needing a design pass; each guard's landing PR shrinks its rule line in the same change, so the heaviest always-loaded rule shrinks as the guards land, never before.
- **Verification**: two cold reviews (Fable 5 on the round diff, Opus 4.8 on the extension), each finding real defects that were fixed and re-verified — including two of the round's own single-home relocation claims refuted with evidence; modal floor 76 → 78 with each removed `never` itemized; net always-loaded growth +846 bytes accepted by the owner.

## 2026-07-30 — the verification instruments, re-fitted (spec 00076, T0097 resolved)

- **The silence threshold is now fitted from real data, not guessed.** A week-of-data fit (340M rows) refuted T0097's own leading candidate — `10 × median` collapses to the 5 s floor because book updates burst same-millisecond, booking 2,061.7 s of phantom gap on a clean ETH/BTC week; the incumbent `p99.99 × 10` is the fitted winner, and `MIN_POOL = 5002` is pinned by a test that measures polars' nearest-interpolation behaviour rather than trusting a comment.
- **The hour-boundary head/tail test is now a single cross-hour timeline model** — `stream_timeline(segs, *, genesis_hour)` measures spacing across the hour boundary rather than treating each hour file independently, and annotates rather than books a stream's genesis hour — dissolving the false truncation this class of defect produced while the real T0036 restart-clobber signature still books above threshold.
- **The thin-stream false GREEN is closed by refusal, not by better measurement.** Any stream sparse enough to self-calibrate into blindness is now exactly a stream `MIN_POOL` declines to score (`UNMEASURED`). Proven against the real pre-fix module on the same fixture: OLD `thresh_s=2040.0, gap_s=4.0, 0.0556% PASS` vs NEW `n=2332, UNMEASURED, FAIL`.
- **The daily `archive verify-replay` is now windowed** (`ops_verify_replay_window_days`, 7 days) — the CLI's `--since`/`--pair` already existed and only the runner omitted them; proven with a synthetic bad hour that trips `checksum_present`: exits 1 unwindowed, 0 windowed. *(Reverted the same evening — see the next entry. The synthetic proof held; real data did not: a window cuts the chain-anchoring predecessor set, so 1,870 of 2,218 hours reported `anchored=False`. The variable no longer exists.)*
- **Acceptance run** (full 12-stream mirror, before/after merge-base `eef44196`): ETH/BTC 0.1320% → 0.0411% (genesis 535.7 s head no longer booked, now under the 0.1% bar), SOL/BTC 0.1332% → 0.0410% (genesis 535.3 s); truncations fleet-wide 72 → 60 (only the genesis false-RED removed — every EUR stream 7→6, both BTC-quoted 1→0), BTC/EUR's six surviving crossings (3276.0/1988.1/1890.3/270.7/126.7/83.4 s) the real restart-clobber events — the 2026-07-08 deploy-day sequence (3276.0 / 1988.1 / 1890.3 s) and the July-13 crash's 270.7 s signature; real outages preserved exactly (ETH/BTC's 256.2 s booked intra silence containing the 210.7 s event identical to the pre-implementation fit; BTC/EUR still books its 1550.8 s event inside 1805.9 s); fleet total 0.6131% → 0.4644%, verdict stays `*** FAIL ***` honestly — the EUR streams carry ~0.49% of REAL gap, and the change removed 30,370.1 s of fiction (12 genesis heads + the retired fixed 1s tail allowance), not real loss.
- **Closeout.** [[T0097]] → resolved and archived; one residual split before archiving, [[T0112]] (the derived threshold self-inflates on repeat outages — `MIN_POOL` protects against exactly one outage-scale interval, `n ≥ 10000k − 5000` for k of them; no production stream within three orders of magnitude of the k=2 regime — BTC/EUR 20,491 intervals of margin, LINK/EUR 10,508, ETH/BTC 2,197, SOL/BTC 898). `.claude/rules/fleet-deploys.md`'s genesis-carve-out sentence retired on the same branch (protected-file edit, owner sign-off) now that the instrument annotates the genesis hour itself. No trial registered and no budget spent — this iteration builds instrument, not evidence.

## 2026-07-31 — verify-replay pages on new breakage, not on a stale exit code — the second attempt (spec 00077)

- **The first attempt fixed the wrong layer.** Spec `00076` D7 windowed the daily `archive verify-replay` sweep with `--since` to stop one historical bad hour from paging its CRITICAL exit-code rule forever; it shipped 2026-07-30 and was reverted the same evening after failing 1,870 of 2,218 hours (all `anchored=False`) on real data, because a window cuts the chain-anchoring predecessor set — a fact `verify_replay`'s own docstring already stated before D7 was written. The real defect was never the sweep's *scope*, only what the alert *meant*: `ops_verify_replay_exit_code > 0` cannot distinguish a known, already-triaged bad hour from a genuinely new one, so any fix that left that meaning in place — including a windowing fix that had worked — would still have paged forever on the first bad hour it ever saw.
- **Three separate paging channels existed on that one finding, not one, and only the metric rule was obvious.** The healthchecks.io dead-man's ping was gated on `rc`, so once any hour ever failed `rc` stayed 1 forever and the ping — meant to prove the sweep was alive at all — was withheld forever; and the CLI logged every failed hour at ERROR on every run, which the ops ERROR-logs catch-all rule paged on nightly regardless of the exit-code rule's fate. Both of those channels would have kept paging forever even if D7's windowing had worked, since neither reacts to the sweep's scope — each needed its own, independent fix.
- **What shipped, across all three channels.** A failed hour now logs at WARNING instead of ERROR (a finding about data is not a fault in the program); the runner publishes `ops_verify_replay_failed_hours` (carried forward, never a sentinel, when a run breaks), `ops_verify_replay_hours_total`, and `ops_verify_replay_run_ok` on every run; the dead-man ping and `last_success_timestamp` now gate on `run_ok` — the ping on `run_ok` alone, and `last_success_timestamp` on `rc == 0` **and** `run_ok == 1`, since a sweep that never ran cannot have been clean; and the single `Ops · verify-replay non-zero exit` rule is replaced by two — `Ops · verify-replay NEW hours stopped replaying` (`delta(ops_verify_replay_failed_hours[25h]) > 0`) and `Ops · verify-replay run broken` (`ops_verify_replay_run_ok < 1`, `for: 15m`). Because the WARNING-level change touches `cli/`, this deploys as an image rebuild plus an ops re-pin rather than a template-only edit, which puts the liquidations-compose re-pin question (the role never restarts that compose on an `ops_image_digest` move) on the attended deploy tail.
- **A fourth adjacent fact was caught before shipping, not after.** Deleting a rule from `alerts.yaml` does not retire it — `grafana-push.sh` upserts and never deletes, and its own comment says a rule removed from the file "keeps evaluating and emailing forever." Retiring the old exit-code rule therefore needs the attended `GRAFANA_PRUNE=1` step; without it this iteration would have shipped with the very rule it exists to retire still paging.
- **Every task produced at least one guard that could not fail — eight in total, every one caught by review rather than by the suite, and two of them minted by the fix for a previous one.** Task 1's WARNING-level test passed only because two unrelated CLI-invoking tests ran earlier in the same file (`caplog` misses a logger that becomes non-propagating mid-test, and the CLI's root callback only flips `propagate=False` on its first invocation) — fixed by attaching `caplog.handler` to the logger directly, removing the ordering dependency. Task 2's ping-gate assertion matched the literal string `run_ok` in the surrounding comment rather than in the gate itself, so deleting the gate entirely still left all 11 template tests green — fixed by asserting the exact gate line. Task 3's `run-broken` alert summary read "unlike the rule above," a pointer that resolves to nothing on a phone in Slack with no sibling alert in view — rewritten to stand alone. The final review added three more: the parse test hardcoded its own input, so a CLI-side field rename shipped green; no committed test executed the carry-forward block, so collapsing it to a `0` sentinel passed all eleven; and the `last_success` gate's `run_ok` half was uncovered, so dropping it passed all twelve. Then the loop turned on itself twice. Replacing the hardcoded input with a whole-file substring check re-opened the same hole one level up — the old literal left in a comment satisfied it — and replacing *that* with an executable `CliRunner` check silently dropped the `hours=` field coverage the textual version had, so renaming that field passed all 28 tests while `hours_total` would have gone empty in production. The pattern is one assertion shape — a match window wider than the thing under test, satisfied by surrounding prose, or a branch nothing executes — and the sharpest lesson is that a fix for it is itself a prime site for the next instance. Every one of the seven *guards* was found by constructing the defect and reading which assertion fired; the eighth, Task 3's alert summary, had no assertion to fire and was caught only by reading the text cold as an operator receives it.
- **Deploy is attended and not run by this branch.** The plan's ordered tail (build → pull the digest on the host → record it in `fleet-pins.md` → converge `--limit zcrypto-ops` → decide the liquidations roll explicitly → push rules → **verify at the first tick, reading `failed_hours`'s value** → only then `GRAFANA_PRUNE=1` → confirm the old uid 404s) was the owner's checklist; **it ran 2026-07-31/08-01 and the outcome is recorded in `docs/reference/fleet-pins.md`**, including the pre-flight find that ops' Alloy compose was pinning the ops image digest as `grafana/alloy`. The prune is last on purpose: it is irreversible, and `delta()` is blind to breakage already present in a series' first sample, so pruning before the replacement is proven would retire the only rule that could have caught a bad hour born in that window.

## 2026-08-01 — verify-replay certifies the whole archive by replaying almost none of it (spec 00078, T0114 resolved)

- **`zcrypto archive verify-replay` takes `--state-dir`** and serves unchanged hours from an ops-local checkpoint, so the nightly canonical sweep's runtime tracks the day's new hours rather than the whole archive; a cached *failure* is never trusted, and three conditions exit 2 with the summary withheld so the night pages rather than reading green (spec `00078`).
- **The checkpoint stores raw per-hour facts only and the chain verdict is recomputed every run** — caching the verdict would make anchoring irrevocable, since an hour cached anchored stays true forever after its predecessor is later rewritten into a failure.
- **Environment drift is caught by the run's sampled audit of cache-trusted hours, not by an invalidation key on the library version** — a monthly dependency bump would otherwise leave a multi-night drain permanently mid-drain with `pending` never reaching zero; the audit deliberately never samples pending rows, which are known-stale by construction.
- **The backlog rule's `for:` must strictly exceed `max(24 h, its delta window)`**, and a test reads the window from the rule's own selector rather than from literals; the runbook entry's first step is to read `run_ok`, since a persistently broken run also carries `pending` forward unchanged.
- [[T0114]] archived: **windowing is decided against permanently, not deferred** — `--since`/`--pair` stay ad-hoc tools and refuse to combine with `--state-dir`, because a window cuts the chain-anchoring predecessor set.
## 2026-08-02 — the exit-bar instrument refuses a contaminated tail instead of trusting it (spec 00079, T0112 resolved)

- **`infra/scripts/continuity.py` refuses a stream whose tail steepens more than 10× across a decade of quantiles** — `UNMEASURED` plus exit-bar FAIL, exactly like its under-bound refusal — because a contaminated threshold's output *is* the lie and printing it with a caveat would print the lie (spec `00079`).
- **The `tail` depth column is transparency, and is pinned as NOT a contamination detector**: promoting it into a gate means first deleting the test that disproves it.
- **A refused stream prints `unmeasured: `**, the same word the table shows in those rows' `thresh_s` cell, so the reason maps onto its rows when a contaminated stream sits beside measured ones — the case where it is least guessable.
- The script is run ad hoc from a repo checkout against a **pulled** copy of the archive, never the live dir; it ships in no image and is driven by no timer, so there is nothing to converge.
- [[T0112]] archived, with the blind regime at very high outage fractions recorded as a **conscious drop** at the gate in the source as well as in the topic — no successor topic exists, and the truncated-hours count is explicitly not a backstop.
## 2026-08-02 — reach-review residuals closed, with two of the topic's own premises refuted (spec 00080, T0098 resolved)

- **On any builder failure `rebuild_sets` deletes the whole just-minted sibling**, uniformly for all seven rebuild targets and resting on no per-target signature — the manifest-presence signature [[T0098]] proposed is false for two targets and would have deleted their completed output. The handler catches `BaseException`, so an operator's Ctrl-C cleans up exactly like a builder error.
- **The `sibling already exists` refusal now names both possibilities** — today's completed sibling, or the leavings of a run killed mid-build — and tells the operator to inspect and remove it; that message is the whole mitigation for a hard kill, which skips every handler.
- **`PAIR_KEYS` has one binding, in `cli/ohlc/fetch.py`**, so importing `cli.ohlc.reach` no longer loads `cli.engine` — the half-initialised-package trap is dissolved rather than documented.
- **`cli/ohlc/seam.py` is the seam definition's single home; the two guards' policies deliberately stay separate** — unifying them would change operator-visible test-pinned messages or drop the close-values diagnostic, a behaviour change wearing a refactor's clothes. Each guard carries a docstring cross-pointer to its sibling.
- **The next attended engine converge carries this refactor**, and the converge session must know it does (spec `00080` D6): the instrument that reads the soak at the go/no-go sits on the changed import path.
## 2026-08-02 — the two Stage-6b feeder measurements, answered (spec 00081, T0117 + T0118 resolved)

- **`zcrypto engine decompose` and `zcrypto engine accum-replay` ship** as read-only `--journal-dir` commands over a journal replica; `--json` sanitizes non-finite floats to `null` with `allow_nan=False` as the self-check, because NaN is legitimate here (a flat cycle's `0/0`) and a bare `NaN` token most non-Python parsers reject.
- **Both name and count a record that fails to replay rather than skipping it**, and abort on one that fails to parse — no aggregate is ever computed over a quietly smaller window; the two identity guards fail with distinct text so the failure modes are distinguishable at the point of reading.
- **Read venue minimums from the snapshot's `universe` block with the `quote == "EUR"` filter** — never from `raw.assetpairs` (whose wsnames are `XDG/EUR`, `XBT/EUR`, so an `<ASSET>/EUR` match silently loses assets) and never keyed by base alone (which overwrites a EUR floor with a BTC number read as euros). Every drift figure prints the snapshot's `fetched_at` beside it.
- **The measurement's own finding is registered as [[T0124]] rather than folded away**: the book is structurally a one-sleeve book at ⅙ of A2's gross, and no metric states how many sleeves contribute, so a re-arming would be as silent as the onset. The gross chain, the drift floors and the band's re-derivation trigger live on the archived topics and on [[T0116]].
- **The accumulation policy is simulated, not implemented** — building it is [[T0119]]'s work inside [[T0018]]'s iteration.
## 2026-08-02 — the §10 whole-book limits get a caller, and two pre-live settings get their real values (iter-119, T0120 + T0121 + T0122 partial)

- **Both target builders run §10's whole-book limits** through one shared helper at the ruled order `combined → position caps → gross leverage → net exposure → margin floor → × governor`, the governor outermost because it is a pure returns overlay. Every book-rebuilding site outside the builder calls that helper, so a binding limit no longer raises `stage identity broken` at the moment the risk layer works.
- **Order among the three whole-book limits is deliberately unpinned** — all three are proportional scalings, so asserting an order-sensitivity property none holds would manufacture a test that passes for the wrong reason. The three cap-breach mirrors deliberately keep reading the pre-limit book, since `cap_breach_bars` is computed before the limits run.
- **`spot_positions_quote_currency` is `"ZEUR"`**: the adapter compares it literally against the instrument's quote-currency code, which keeps Kraken's legacy `Z` prefix even where the instrument ID renders `ADA/EUR.KRAKEN` — the plausible `"EUR"` matches nothing and reports an empty book indistinguishable from a flat one. Pinned against the measured instrument histogram so a later correction back to `"EUR"` fails.
- **`default_leverage` stays unset as a ruling, not an omission** — `None` means cash, the node already runs MARGIN, and a global default would silently lever every ordinary rebalance of a book §10 caps at gross; rung 1's probe passes leverage per order.
- **The kill criteria gain a ratified DD floor (20 % governed drawdown — retire, not de-lever), and the live-Sharpe criterion becomes the paired per-cycle live-minus-simulation difference**, so no master-plan amendment is owed. Recurring routines register on their topic, never in `infra/runbooks/README.md`, whose scope is procedures for a signal that fires at an operator; the remainder went to [[T0121]] (archived) and [[T0120]].
## 2026-08-03 — iter-119 shipped: the sleeve gauge is live and reads 1 (T0124 resolved)

- **The engine serves `zcrypto_engine_active_sleeves` and `zcrypto_engine_sleeve_gross`, and `zcrypto-engine-sleeve-count-changed` is live** — the one-sleeve state [[T0124]] measured is observable rather than inferred from journaled shadow cycles.
- **Verify each payload from the pulled image's own surface, never from the tag**, and read a new series BY VALUE on both the host `/metrics` and Grafana Cloud — the provisioning API accepts a wrong datasource uid happily and still reports `health=ok`.
- **Both sleeve families publish nothing until the engine's first cycle**, by construction: the labelled family stays silent until `.labels()` and the unlabelled one registers lazily, because a fresh Gauge's `0.0` would *claim* no sleeves active. The silence is not breakage.
- **An engine converge that must publish a new metric family runs `--tags capture,engine` with capture's digest held at its current value** — the Alloy keep-list lives in the capture role, so `--tags engine` alone leaves the new families unadmitted; holding the digest keeps live capture from restarting.
- **Read an RSS bake against the host's own history, never against a cold baseline** taken a minute after start — a two-point comparison would have rolled back a healthy image — and re-read the converge gate immediately before the run, since a backgrounded converge can sit queued for hours.
## 2026-08-03 — the deployable's cost basis, ruled and re-quoted (iter-120, T0090 partial)

- **The execution style is ruled maker-first with a taker fallback** (owner, 2026-08-03): resting post-only orders are free until filled, so attempting maker costs only latency, and `target − held` already absorbs an unfilled delta — the realized blend is measured at rung 1, not assumed. Maker-only-never-cross and taker-only were both rejected; the reasoning is on [[T0090]].
- **The cost model splits into `fee_per_side` + `spread_per_side`, representational only** — the defaults keep the pre-measurement 20 bps spread so every registered figure reproduces, and re-pricing to the measured spread is a separate deliberate step at rung 1. Only the sum is ever charged, so each guard pins that its field *feeds the sum*.
- **§12's go/no-go band now states two separate terms — the statistical CI and the cost basis** — with the failure diagnosis split: a divergence attributable to the estimate means the backtest is wrong and returns to Phase 5, while one attributable to cost is an execution finding that re-prices the basis without invalidating the backtest. The cost term resolves at rung 1; the statistical one never does.
- **Prove neutrality past the pinned tolerance**: the regressions pass under a 1-ulp shift, so the proof is sha256 over the raw IEEE-754 bytes of the builder's outputs before and after.
- **Record 44's ADOPT criterion can never be re-examined at a different cost basis** — trial 43's construction was never committed and its runner is gone. `spread_per_side`'s `> 0` validator stays until rung 1 produces real fills (zero spread is legitimate on a split term); both are carried on [[T0090]].
## 2026-08-03 — the registry can no longer record a trial nobody can rebuild (T0125 partial)

- **`TrialRegistry.append` refuses a record whose `run_ref` is absent or names no existing repo-relative path**, with its own error for a `scratchpad` marker; absolute paths and `..` escapes are rejected, because provenance means a path *inside* the repo. No git, no subprocess — it runs on every append.
- **`tests/test_trial_registry_provenance.py` is where *committed* is checked** — every `run_ref` must name a git-tracked path — and its legacy exemption is asserted in **both** directions, so widening the frozenset fails a test rather than quietly decorating a pass.
- **The stored-record load path stays lenient on purpose**: `append` re-validates every stored record under lock, so a strict loader would have bricked all future appends to the live registry. A dedicated test pins the split so nobody tightens it later.
- **Both layers canonicalize the path they extract.** The registry is append-only, so a committed file named in a non-canonical spelling would otherwise pass the append guard and fail the provenance test forever, with weakening the test the only remedy.
- **The signature change carries no `!` marker, deliberately** — at `version = 0.0.0` with `major_version_zero = false` it would mint v1.0.0 and announce a stable public API this repo has never released.
## 2026-08-03 — the go/no-go now says what it certifies, and a named-but-undefined report section is dropped (iter-121, T0064 + T0073 resolved)

- **§12 states that passing the go/no-go certifies EXECUTION and explicitly not EDGE** (owner ruling, 2026-08-03), and names the two limitations the decision inherits — no out-of-time validation of returns, and [[T0125]]'s adoption criterion, then believed unrebuildable (the 2026-08-21 recovery entry below re-trued that). Mitigation was not rejected as costly: the only window that exists provably cannot discriminate, and accepting forecloses nothing.
- **`zcrypto engine soak-check` now states what bounded its window** — `store`, `clock` or `journal`, classified structurally from the gate that skipped the trailing cycles — and a store-bound window warns with both bounds and the cycle cost before the fingerprint table. It deliberately does not refuse: a partial pulled store is legitimate, and the failure was silence.
- **The default `--store-dir` deliberately still points at the cold workstation store** — the configured store is the engine's own, correct on the VPS — so a workstation run is store-bound by construction and now says so; the remedy is to pass a pulled VPS store.
- **Spec `00058`'s named regime-context section is DROPPED, with the measurement recorded in the spec itself** so it no longer reads as owing one: conditioning destroys the only discrimination the instrument makes, and the regimes do not alternate, so a split leaves one cell empty at any window length. The honest residue already ships — the report states regime state without conditioning any verdict on it.
- [[T0064]] and [[T0073]] archived.
## 2026-08-03 — the deployable stops resting on a comparison nobody can rebuild (iter-122, T0125 resolved)

- **§12 states the go/no-go rests on record 44's benchmark-relative legs** — SPA, DSR, worst-slice-relative, cost-stress — which are independent of trial 43, then unrebuildable, so the gate no longer depends on a comparison nobody could rebuild.
- **`cli/portfolio/record44_legs.py` re-derives the kill bar from committed code**, with tests and runnable by anyone: a topic that exists *because* a scratchpad vanished cannot honestly deposit its answer in another one.
- **Reconstructing trial 43 was explicitly rejected** — it then had neither committed code nor a specification to reproduce against, so a rebuild could only be tuned until it matched, which proves nothing; its original bytes were later recovered (the 2026-08-21 entry below), and this entry is deliberately left as the point-in-time record.
- **A spec whose sha256 is stored as a registry record's `spec_hash` is immutable** — appending to it breaks the pin that verifies the ratified record and nothing in the gate catches it; a recovered convention's durable home is committed, runnable code (`spec-plan-locations.md`).
- **Two conventions record 44 rests on are recovered rather than chosen**, and live in that module: the SPA grid's headline cell is stored as `spa_p_full`/`spa_p_decisive` (which is why no `spa_grid_b30_s42` key exists), and the benchmark-relative worst-slice test runs on the governed net over full history, year taken from each bar's close stamp.
## 2026-08-03 — refine-rules round 3: eight lessons placed, and the round catches its own weakening (iter-123)

- **A lesson lands where someone hits the wall**, not on the always-loaded corpus by default: the vault token recipe went into `infra/scripts/grafana-push.sh`'s own env documentation, the tmux warning into `fleet.md`, the backgrounded-converge lesson into a role assert — and an instance of a rule already present is folded into that bullet rather than added beside it.
- **A registry record's `spec_hash` must equal the sha256 of some committed spec**, with the two legacy records frozen and the exemption asserted in both directions — the recurrence a narrated provenance note had failed to prevent.
- **The spec/plan ceremony's trivial branch widened to design-settled, and the model floor rides with it explicitly**: Opus for any review, Fable where the change touches the unbackfillable capture path, the live trade path or canonical data. An escape hatch widened without noticing what rode on it is the characteristic failure of a refinement round.
- **The ruling that licenses the design-settled path must PREDATE the branch**, and the unattended skill says so at the step where a loop would otherwise author the deciding topic and claim design-settled in the same pass.
- **Skipping the spec/plan and skipping the iterations-history entry now turn on different tests** — design settled for the first, trivial in substance for the second — because the old bundled branch would have licensed dropping entries for multi-file work.
## 2026-08-03 — converge discipline becomes refusable: thirteen guards, mutate-probe, and capture-deploys rewritten to mechanism pointers (iter-124, T0111 partial)

- **The capture/engine/ops/docker roles, `site.yml` and `bootstrap.yml` now REFUSE the converge mistakes they used to only warn about** (spec `00082`): digest-resident preflights on all three tiers, the un-tagged-primary refusal, the engine inter-cycle window re-asserted at task execution time (a launch-time check does not survive a delayed converge), pair-add order, canary digest parity, fleet-pins recording, the liquidations repin decision, the panel-timer hold, the `daemon.json` change-ack and the re-bootstrap refusal.
- **Every bypass is `-e <name>_override="<reason>"`** — empty, boolean-like and very short values refused, the accepted reason echoed into the play log so bypass and justification land in the same artifact; the conscious-choice flags follow the same refuse-by-default shape (spec `00082` D1).
- **`infra/scripts/mutate-probe.sh` is the one enforced mutation path** — dirty-tree refusal, `git archive` sandbox seeding, mandatory failing control, no-op-sed abort, `cmp`-verified pristine restore, signal-safe cleanup — replacing the four `agent-ops.md` bullets it collapses.
- **`.claude/hooks/git-mv-guard.sh` warns the moment `git mv` forms the staged-rename-with-unstaged-edits trap**, which repo-side pre-commit structurally cannot see because it stashes unstaged changes first.
- **`fleet-deploys.md` reads as mechanism pointers, not incident narratives** — the narratives now live in the asserts' fail messages, delivered at the moment of the mistake. Wave-2 and the two guard-tightening candidates went to [[T0111]], delivered in the two entries below.
## 2026-08-04 — the converge path itself becomes refusable: wave-2 scripts, the engine parity mirror, and the gap closures (iter-125, T0111 partial)

- **`infra/ansible/scripts/converge.sh` is the documented converge path** (spec `00083` D1): it requires `--limit` (a bare `site.yml` still runs every play), always runs and displays the `--check --diff` preview first and refuses on a failed one, then takes a **byte-exact typed confirm of the `--limit` value**, read from `/dev/tty` only — so a pipe or heredoc cannot drive it.
- **`zcrypto-panel-regenerate` runs the panel delete-and-rebuild as one refusing flow**: timer stopped first, the window sized from the input tree and refused when the ETA crosses the auto-reboot, the healthchecks.io pause taken as a typed gate before anything is deleted, the rebuild run **inside** the materialize unit so a stray timer tick collides instead of double-writing, and the NAS-deletion + un-pause checklist printed for the halves the host cannot reach.
- **`infra/scripts/ops-postverify.sh` runs the ops verify-by-outcome checklist as one command**, where `(no series)` and query errors read FAIL and never a clean bill, with value extraction anchored to series lines so a PromQL containing `== ` cannot mint a phantom PASS from the query echo.
- **`vault-pass.sh` refuses the `ansible-inventory` ancestries that decrypt the whole vault to stdout** — `--host`, `--list` and `--graph --vars` — by walking `/proc`, and the refusal names the safe alternatives.
- **The engine role refuses an engine re-pin whose digest the secondary is not running as capture** (there is no engine secondary — that bake IS the engine's gate), sharing the override and fail-closed-on-unreachable semantics with the capture assert; the inter-cycle window floor follows the boundary cycle's own completion + 5 min, falling back to the conservative fixed floor on any read failure. [[T0111]]'s remainder was the attended ops-host drills, run in the entry below.
## 2026-08-04 — a mandated routine that nothing ran, and the stale source it was reading (iter-126, T0113 resolved)

- **The Kraken reference-data re-confirmation lives in four places** — the `/zcrypto-refdata-sweep` skill (procedure), a scheduled `#zcrypto` reminder (trigger), the `refdata-sweep-due` runbook section (response), and the register relocated to `docs/reference/`. **The reminder is the weak link by construction**: a scheduled message fires once and its absence is silent, so re-arming the next one is step 4 of the runbook procedure.
- **A changed `raw_sha256` is not a changed fact** — Kraken's universe churns underneath the basket nearly every sweep, and a routine that alarms on the hash is ignored by its third run.
- **The register captures what the master plan's ⏱ list names**: the volume-tiered fee ladders, `margin_call`/`margin_stop`, position limits, and per-asset `margin_rate`, which *is* the borrow-rollover rate and lives on the **asset** — read off the pair it is `None` for every row and reads as absent data rather than as a bug.
- **Both borrow sides render**, because a short sells the borrowed base while **a long buys with borrowed quote currency** — rendering only the base side leaves the book's long leg unpriced while the table looks complete.
- **`docs/reference/kraken-fee-schedule.md` is the fee source of truth**, read from the logged-in account: Kraken's public `AssetPairs` still serves a superseded schedule, so the register's fee columns are a **drift detector on the endpoint**. The sweep's attended half re-reads the fee tab and records "not re-read" when the owner is unavailable — never inherited.
## 2026-08-05 — the two drills that closed the mechanization topic, and the render bug only a real converge could find (iter-125 closeout, T0111 resolved)

- **`converge.sh`'s typed-confirm gate refuses an agent-driven attempt in every shape** — agent-harness commands carry pipes and no controlling terminal, foreground or backgrounded — so a converge lands only when the operator runs it in a plain terminal.
- **Read a rebuild's generation by value, from the following timer tick's skip count**, not from the unit's exit status: a mismatched generation would have refused and a corrupt tree would have rewritten rather than skipped, and "the unit exited 0" shows none of that.
- **A completed `Type=oneshot` unit retains the PREVIOUS run's `Result`** — anchor a green to `ExecMainExitTimestamp` and the journal's own `Starting`/`Finished` pair, never to `is-active`, which exits 3 while `activating`.
- **`tests/test_infra_shell_templates_render.py` walks every `roles/*/templates/*.sh.j2` through Ansible's own Templar plus `bash -n`** and fails on an unregistered template — a template test that does not use the renderer the fleet uses proves nothing. Write a shell length check as offset expansion: `${#var}`'s brace-hash pair is a Jinja comment tag, and Ansible renders with `trim_blocks=True` where a bare `jinja2.Environment()` does not.
- [[T0111]] archived with its accepted residuals recorded as measured, fail-safe and consciously unfixed — none is a live deferral.
## 2026-08-05 — the presentation layer: 18 charted families against 40 that could page you (iter-127, T0020 built)

- **Four Grafana boards — `Fleet health`, `Data integrity`, `Engine` and a reworked `Logs` — chart every metric family an alert can fire on**, and every rule carries `__dashboardUid__`/`__panelId__` so a page lands on the panel plotting that rule's own expression rather than one merely showing the same family.
- **Coverage is FAMILY-level, never rule-level, and a firing alert must point at the panel that shows it** (owner ruling). `tests/test_dashboards_cover_metrics.py` makes the first half mechanical and derives its expectations from the topology pinned in its own constants — deriving them from the keep-regexes it audits made a whole host drop silently out of the derived set.
- **`container="engine"` was matched by no rule at all**, so an ERROR line from the component that moves money paged nobody; the engine now has liveness and ERROR-log rules of its own, and a panel must not paint absence green on a signal whose rule pages critical on no-data.
- **An alert summary never interpolates `$labels.host`** — Grafana bakes annotations at evaluation time, before any template runs — and a notification template's `eq .Labels.x ""` branch is only reachable under `missingkey=zero`, so execute a template against a Grafana-shaped payload rather than reasoning about it.
- [[T0020]] archived; the rollout's follow-ups went to [[T0128]], [[T0129]] and [[T0130]], all since resolved. **Record the active-series budget per host, never as a total**, so a later reading is comparable like for like against spec `00043`'s target.
## 2026-08-06 — the panel regeneration told an operator to delete something it never named (iter-128, T0111 follow-up)

- **`zcrypto-panel-regenerate`'s closing checklist names the host, path and command for the NAS-side deletion**, and states the narrow trigger: only a grid-directory rename or a pair leaving the archive/quote scope orphans a path — a schema, ladder or level-count change forces a regeneration but rewrites identical paths and orphans nothing.
- **Never delete NAS-only paths by diffing the two trees.** The materialize sweep skips every hour at or below its watermark, so an hour a rebuild failed to anchor is present *only* on the NAS and indistinguishable by path from a stale orphan — deleting it destroys the last copy, and the pull runs outward from ops so nothing restores it.
- **Capture a run's completion line anchored to that run, before restarting a `Persistent=true` timer** whose queued catch-up tick will otherwise supply plausible, reassuring, wrong numbers.
- **Pin `LC_ALL=C` on `comm` as well as on both `sort`s** — `ssh` forwards `LANG`, and under a UTF-8 locale glibc case-folding makes `comm` reject C-sorted input as unsorted, at the moment the operator decides whether to delete.
- **`fleet-deploys.md` no longer promises an unconditional NAS deletion**: it is the always-loaded text an agent reads while *planning* a regeneration, so the stale promise would have kept reproducing the expectation the script change exists to kill. The spec, plan and archived topic deliberately keep the old wording — they record what was true when written.
## 2026-08-06 — the alert stack's first live event, and the decision it unblocked (iter-129, resolves T0105)

- **The layered silence and venue alert set proved itself on a real Kraken maintenance window**: the fleet-silence rule caught the blackout, the per-pair rule named every stuck stream, the venue rule attributed the cause in the same minute, and both silence rules auto-resolved when the feed returned — no human action needed, and the venue rule's "do not restart or converge on this signal alone" held.
- **~15 minutes of L2 across all 12 pairs is permanently gone, and this entry originally said the opposite.** The residual counter read minutes after recovery could carry no answer yet — the reconciler runs twice-hourly and `SETTLE_HOURS = 2` made the hour ineligible for ~2 h. **An unprocessed interval is not a verified-empty one.** No fleet component failed: the venue emitted nothing, so neither host missed anything that existed.
- **The pre-drain on venue maintenance is consciously dropped** (owner ruling, decisions log): Kraken published no `effective_time` on any transition, so nothing exists to key it on. The right-sized remnant — the executor checking venue state at cycle entry before placing orders — is an input to [[T0018]]'s order-submission item.
- **`ALERTS{alertstate="firing"}` is structurally empty for Grafana-managed rules**, so its `(no series)` reads as "nothing firing" regardless of reality; `grafana-query.py`'s docstring names the API endpoint that actually answers the question.
- [[T0105]] archived. The counter-presence alert's silence is time-boxed on **that rule uid only**, with `system` matched to the states actually read, and deleted at each host's own next capture restart (`infra/runbooks/capture.md`).
## 2026-08-07 — the permanent-loss page was right, and the entry above it was wrong (iter-130)

- **`Reconciler · residual gap increased` fired correctly on the 2026-08-06 venue halt**, and the ledger names the cause exactly: ~15 minutes of L2 book data across all 12 pairs is permanently gone. The opening boundary tracks the venue's own status line to the second; the closing one does not — book flow resumed at the ladder's `cancel_only` step, well before `online`.
- **An unprocessed interval is not a verified-empty one, and freshness is not coverage.** Before reading a derived counter as evidence of absence, compute when its producer could first have covered the interval — `SETTLE_HOURS = 2` (`cli/archive/settle.py`) means hour H is first bookable at H+2 h, at the next `:12`/`:42` tick. `zcrypto_reconcile_last_success_timestamp_seconds` is run liveness, not a coverage watermark, and answers reassuringly in exactly the situation that misleads you.
- **`infra/runbooks/README.md` carries that arithmetic in place of the counter read, and states the loss** — correcting the records one authored while leaving the record one *operates by* makes a correction look complete while the failure mode stays armed.
- **The residual counter under-reports by design**: `fleet_dark_windows` books each stream's own window only when it contains a fleet-wide intersection, and `unwitnessed` is ledgered for visibility and counted nowhere, since counting it beside `both_streams_silent` would double-book the same seconds. The counter is monotone and can never be walked back, so it omits seconds it cannot attribute — a caution, not a reassurance.
- **Correct a durable record by rewriting the narrative in place, and sweep for the CLAIM rather than the phrase written** — the original sentences read first, and more confidently, than any note beneath them. `agent-ops.md` already said so, so no new rule is owed.
## 2026-08-08 — the notional ladder learns about quotes, and the calibration covers all twelve legs (iter-131, spec 00085, resolves T0092)

- **The panel's fill ladder is per-quote** (`NOTIONALS_BY_QUOTE`): EUR pairs keep their EUR rungs and BTC-quoted pairs use the BTC quantities worth those amounts at a pinned FX reference, so a grid point's column name means the same thing on every pair and values stay comparable across quotes. **The reference is pinned to the TREE, not to a calibration window** — the manifest holds the derived rungs, so changing it makes the generation check refuse every existing tree at the next sweep.
- **The whole panel tree was regenerated and both `/BTC` subtrees exist for the first time**, with their `fill_bps_*` columns populated where all six were previously null. The regeneration orphaned nothing — the first real exercise of the checklist rule that a ladder bump rewrites identical paths.
- **`SPREAD_CALIBRATION` is keyed by full symbol** over one window shared by every row: a base-keyed table returned the EUR row for `"ETH"` while raising for `"ETH/BTC"`, so the re-key converts a silently wrong value into a loud failure.
- **One window's calibration is a point estimate, not a constant.** A permanent control re-runs the committed script over the OLD window and holds the superseded values as literals, so it cannot follow the restamp it exists to verify.
- **A test reads the calibration window's two timestamps and trips on a span below Phase 2's "≥2 weeks of captured spreads" row** — every other assertion is blind to span, so a regressed exit bar would have shipped green. A venue maintenance gap inside the window is disclosed in `captured-spread-calibration.md` rather than trimmed.
## 2026-08-08 — the engine's ERROR rule gets a dead canary, and the measurement nearly went the wrong way (iter-132, resolves T0128)

- **`zcrypto-engine-log-dead` is live**: a fires-on-silence dead-man for the trading engine's log plane, mirroring the capture rule's shape. The `Engine · ERROR logs` rule is presence-fired, so it is silent in two states indistinguishable from outside — the engine is healthy, or its shipping path regressed and no line arrives at all.
- **The engine is a BURST emitter — it emits at each 4-hourly boundary and nothing between — and that decides the window**: the canary window is the measured cycle period plus the fleet's own 2 h margin, the same shape its ops timer canaries use. Anything at or under the period false-fires on every healthy inter-cycle gap.
- **`count_over_time` emits no sample for a step whose range window is empty**, so on a burst emitter it reads back as a continuous emitter; `or on() vector(0)` is what makes the zero steps visible. `alerts.yaml` carries that corrected explanation — the first one recorded, that Loki forces a minimum step spacing, is false.
- **The `Logs` panel plots the engine's dead-man beside its siblings on a shared linear axis** where the engine sits near the floor, so its description says to read the threshold line at 1, not the height.
- **Verify a pushed rule BY VALUE** — its own query returning a number inside the measured band — and confirm it is evaluating (`state`, `health`, `lastError`): presence alone cannot distinguish a healthy rule from one reading 0 through a wrong selector. [[T0128]] archived.
## 2026-08-09 — the dispose race reaches production after all (dependabot round; T0115 → partial)

- **[[T0115]]'s central safety claim was false**: `TradingNode.dispose()` branches on the **loop's state at dispose time**, not on whether the node ran, and `zcrypto engine run` is a synchronous command — so the live engine executes the aborting `loop.close()` path on every shutdown. The option of closing the topic on "unreachable from production" is struck.
- **A grep that counts call sites establishes nothing about which branch a site takes** — quoting a measurement for a proposition it does not address is the failure here, and it is the same shape as the range-query misreading recorded the day before.
- **`infra/scripts/nautilus-dispose-probe.py` is the per-bump dispose probe as runnable code**, not a procedure described in prose — rebuilding it from the description had already picked the wrong import once. **It can escalate, never exonerate**, recorded on the instrument itself so a future clean run is not misread as a fix.
- **The probe reports `NO VERDICT` on any non-signal failure and `INCONCLUSIVE` on a partial run**, and refuses zero rounds outright: an import error from the very bump under test would otherwise manufacture a race finding.
- **`tests/test_nautilus_adapter.py`'s deliberate version pin stopped a MINOR nautilus bump** that an "escalate on major versions" heuristic would have waved through; the pin binds a bump to an attended re-run of the order-semantics probes recorded under `docs/reference/adapter-verification/`.
## 2026-08-09 — dataset provenance becomes what the run read, and the registry gets its first real door (iter-133, spec 00086, closes T0065's execution-reproducibility round)

- **`cli/registry/observed.py` derives dataset identity from the BYTES A RUN READS** — hashing each parquet at read time and applying any window itself, so rows-used cannot drift from rows-recorded — and a new dataset backing a trial needs no provenance code at all: no allowlist entry, no adapter. Two earlier designs derived identity from what manifests *declare*, to guard a door with no production caller.
- **Schema 4 computes `dataset_hash` through the registry's own hashing** with no `append()` argument to supply it, re-validates the finished record before writing, and enforces the block at load behind a `schema_version >= 4` floor past trial 46 — without that floor the next writer templates off a record saying 3 and reproduces the original failure verbatim, loading clean forever.
- **`zcrypto research eval` is the door**, and registration requires a committed subject — the discipline record 44 already followed, now the paved path rather than an unwritten norm.
- **`docs/reference/legacy-dataset-pins.jsonl` carries the epistemics in the referent value** — a null referent where unrecoverable, INFERRED marked inline — so a careless grep cannot return a bare path reading as verified fact.
- **A guard missing its true-positive case can refuse every healthy read with a false diagnosis while CI stays green**: the loader compared file-byte hashes against manifests that vouch frame-content hashes, and only a probe reconstructing that mutation shows it. [[T0065]]'s REACH half remains, its OHLCVT ingest waiting on Kraken publishing the dumps.
## 2026-08-10 — the trade tape becomes a bar dataset, and a clock stops being trusted (iter-134, spec 00087, closes T0065's REACH autonomous half)

- **`zcrypto tick materialize` publishes 15m OHLCV daily finals from the captured trade tape**, read reconciled-first, with coarser intervals derived from the base — below 4h nothing else reaches the present, since REST's window recedes and the OHLCVT dumps are quarterly while the captured tape accrues.
- **Publishing is gated on MEASURED heal-completeness** — `trade_id` contiguity proven from the bytes (`cli/trades/gaps.py::detect`) — not on a wall-clock proxy for "the backfill has run": the healer's designed delay modes break that proxy by an order of magnitude, and with no rewrite path a proxy failure is silent, permanent and indistinguishable from a quiet market.
- **A day missing an hour is not an incomplete day**: the capture writer commits no final for an hour with no events, and zero-print hours are production-measured. `trade_id` contiguity tells a missing hour from a quiet one where file presence never could, and `detect`'s span reaches the nearest *present* segment each side rather than an adjacent hour.
- **Kraken publishes `vwap` truncated to the pair's price precision**, so the REST cross-check asserts the exact half-open tick identity rather than a widened tolerance, and skips *distinctly* on a network failure so downtime never indicts the data.
- **The ops timer, its gauges, the two alert rules and the dashboard panel are live.** `days_gap` counts settled, unpublished days that have fallen outside the re-scan window, and the staleness rule reads `last_publish`, never `last_success`: the unhealed path exits 0 by design, so a stalled healer would freeze the watermark while every surface reported success.
## 2026-08-11 — the execution safety envelope: two keys, a kill switch, a venue gate, and a ledger nothing can submit through yet (iter-135, spec/plan 00088, T0018 partial)

- **The first of [[T0018]]'s risk-first five-spec decomposition lands, none of it able to submit an order**: a two-key arming interlock (the `exec_armed` config key plus a host-side arm file, both required), a latching kill switch that overrides everything, a fail-closed venue-status gate, a restart-triggered reduce-only hold cleared only by a human, a per-cycle execution ledger structurally disjoint from the cycle records, six `zcrypto_exec_*` gauges, a dashboard row, three alert rules with runbook sections, and `zcrypto engine exec-status`.
- **`cli/engine/execgate.py` is authoritative over the plan's snippet, and the asymmetry is deliberate**: the arm file fails closed by reading **absent** on any doubt, while the kill and restart-hold files fail closed by reading **present** — the directions are opposite because absence is what *arms* the first key and what *permits* the other two.
- **`Path.exists()` and `os.path.lexists()` swallow every `OSError` internally**, so an `except OSError` around either is unreachable; a fail-closed presence check uses `os.lstat()` directly and maps `ValueError` (an embedded NUL byte) to present-and-refuse alongside every non-`FileNotFoundError`.
- **`cycle._metrics_sink` is module-global process state**, and an autouse `tests/conftest.py` fixture resets it after every test: a leaked sink fired live outbound connections and stray ledger writes from later tests, silently, because the cycle's own sink guard swallows a raising sink by design.
- **A gauge that is fresh by construction answers nothing** — the venue-snapshot age gauge became `zcrypto_exec_last_evaluation_timestamp_seconds`, which says whether the gate is still being evaluated at all rather than publishing a permanently reassuring number, and backs `zcrypto-engine-exec-not-evaluated`. The venue-divergence alert is deferred on [[T0018]].
## 2026-08-13 — venue truth lands: the instrument map, a frozen Cache snapshot, and the realized-state artifact (iter-138, spec/plan 00089, T0018 partial, T0130 + T0134 resolved)

- **The second of [[T0018]]'s decomposition lands, all of it read-only**: the asset→`InstrumentId` map and the pure `size_order` sizing function, `VenueState` as a frozen Cache snapshot with `runtime_concordance`, the `venue-<HH>.json` artifact beside each cycle record — journaled **first**, before any target computation, and never consulted for targets or orders — the repo-side basket-concordance test, and four `zcrypto_venue_*` gauges with two alert rules, admitted through the Alloy keep-list in the same change.
- **nautilus's Kraken adapter never maps Kraken's `costmin` onto `min_notional`** — it reads `None` on every instrument — and the engine host mounts no refdata snapshot, so `costmin` ships as a committed constant labelled `costmin_source: snapshot-constant` so no reader mistakes it for a live venue read; the runtime concordance check covers only the Cache-supplied constraints (spec `00089` D5a).
- **`tests/test_costmin_drift.py` is that constant's only guard and skips wherever `data/snapshots/` is absent, CI included** — so the `zcrypto-refdata-sweep` skill runs it immediately after minting a fresh snapshot, the one moment the guard can actually fire.
- **A gauge registered eagerly at `0.0` pages on every converge**: seed it from the newest on-disk `status: ok` artifact at startup, guarded so a corrupt or absent one degrades rather than blocking engine start.
- **`_code_version()` composes `<version>+<sha12>` from a build sha threaded through as an image build-arg** — a semver that has never moved cannot identify a build, and CI's OCI revision label is invisible to the process. A `caplog` assertion attaches its handler to the `zcrypto` logger directly, because `configure()` turns root-logger propagation off. [[T0130]] and [[T0134]] archived.
## 2026-08-15 — the twelve-leg pipeline: full-symbol keys, the /BTC legs at structural zero, and record 47 (iter-139, spec/plan 00094 + spec 00095, T0137 resolved, T0018 partial)

- **`cli/engine/store.py::BASKET` is the single source of truth for the traded basket, and the engine's whole pipeline keys on full symbols** — `PAIR_KEYS`, `INSTRUMENT_IDS` and `COSTMIN` derive from it, and the cycle and venue schemas bump so a pre- and a post-deploy record are distinguishable.
- **The strategy did not change**: `CrossfreqSystemConfig.assets` stays the ten EUR bases and `cli/portfolio/` is byte-identical; `_expand_to_basket` emits the `/BTC` legs at exactly `0.0` downstream of the model, because widening the `assets` knob would have had the sleeves compute real weights for legs the holdout evidence never covered (spec `00094` D1; owner rulings in the `[iter-139]` decisions-log entries).
- **Each cycle-record schema replays and compares IN ITS OWN NATIVE KEY SPACE**, so the ratified gate streak survives the deploy boundary — normalizing across the two would have zeroed it (D3).
- **Registry record 47 is the deployable**, appended on the owner's attended adopt, every re-derived leg exactly equal to record 44's, inheriting its holdout position; spec `00095` is its construction doc, **immutable once its sha256 landed in the record**. Recorded so the drift is not read as license: the review floor dropped from Fable to Opus mid-branch on the owner's word, rule unamended, and `efee3da9` (the reviewer's own follow-up) and `8c3c9be5` (the record-47 append at the attended adopt) carry no `Reviewed-by:`.
- **Deployed NAS and ops images FIRST** — old code there raises on the new schema and would zero the ratified streak — **then the engine LAST**, the `/BTC` canonical parquets staged to the host first, since the engine mounts no data root; `zcrypto engine run` now refuses to start on a store missing any basket leg. [[T0139]] and [[T0137]] archived; [[T0138]] took the costmin guard, delivered 2026-08-18 below.
## 2026-08-18 — the rung-1 order path: one chokepoint, a write-ahead row, and an engine that could now place a real order but is not armed to (iter-140, spec/plan 00090, T0018 partial, T0138 + T0140 resolved)

- **The engine can submit a real order and ships DISARMED** — `cli/engine/executor.py` is the one module through which an order reaches the venue, four automatic kill trips latch the kill file and cancel resting orders, and arming stays the probe checklist's separate owner-worded act rather than a converge operand (spec `00090`).
- **`infra/runbooks/README.md` gains `engine-probe-window`** — five phases with human gates — and `zcrypto engine probe-plan --check` validates a plan before it is *renamed* into `<engine_state_dir>/exec/probe-plan.json`, never written in place, since a partially-written file is parsed, refused and deleted.
- **The Engine board gains execution and venue rows and reads as an intent-vs-execution legend**: positions are seeded across an engine restart and realized PnL is not, so a flat zero after a restart is a gap in the record rather than a claim that nothing happened.
- **`tests/test_internal_terms_not_operator_visible.py` now catches lettered decision numbers (`D5a`) on operator surfaces** — its word-boundary pattern could never match a digit followed by a letter, so every lettered form escaped the guard.
- **[[T0120]] stays deferred to spec `00092`** — `00090`'s reconciliation is post-terminal and startup-time, so it defines no continuous call rate to divide a REST budget against; [[T0141]] took the runbook's own split threshold and [[T0142]] the adopted-order routing question, both resolved in entries below.
## 2026-08-18 — the runbook splits by subsystem, and its README becomes a pure index (T0141 resolved)

- **`infra/runbooks/README.md` is a pure index and holds no procedure sections** — every procedure lives in a subsystem file (`capture.md`, `zaccess.md`, `ops.md`, `engine.md`, `reference-data.md`), so a new section has an obvious home or mints a new subsystem file.
- **`tests/test_infra_alert_rules.py` resolves runbook citations across every `*.md` under `infra/runbooks/`**, capturing both halves of a citation and pinning each anchor to exactly one file, and it now reads the index's own sideways rows and the dashboard panel descriptions that cite sections.
- **Alert summaries, panel descriptions, topic files and the root README point at the file that defines their anchor**; four citations inside executed plans are deliberately left as point-in-time records of the alert YAML as authored then.
- **`infra/scripts/grafana-push.sh` must run once after the merge to re-ship the summaries and the Engine board** — the branch's only open action, registered in the archived [[T0141]]'s `## Resolution`; inside the merge-to-push window a summary's link is degraded, not dead.
## 2026-08-20 — refine-rules round 4

- **Round 4's lessons live in `agent-ops.md`, the protected `fleet-deploys.md`, `docs-style.md`, `spec-plan-locations.md`, the `topic-ops` and `iteration-closeout` skills, and `grafana-push.sh`'s header** — read those, never the round's report.
- **`zcrypto-rollout-image` and `zcrypto-bump-alloy` drive converges through `converge.sh`, unwrapped** — a `timeout` around it kills the wrapper while its `ansible-playbook` child keeps converging a production host.
- **`iteration-closeout`'s phase-serial routing is corrected**: a phase's decisions log has ONE serial, and deriving it from the phase's other docs was false for Phases 4 and 5.
- **`operator-facing-text.md` adds Grafana notification templates to its in-scope list, and CLAUDE.md's commit-gate sentence names `ansible-lint`.**
- **The `WP<N>` ban is mechanized** — `test_wp_tokens_stay_out_of_git_tracked_files` walks `git ls-files` with a two-way allowlist, so a carrier that stops matching is a stale entry rather than a silent pass, leaving exactly the recorded exception.
## 2026-08-20 — the rung-1 order path reaches the fleet, and fleet-pins becomes a state file (deploy of iter-140)

- **The rung-1 order path is live on all three tiers, and the engine is verified DISARMED** — the gate reads `config_not_armed,arm_file_absent,restart_hold`, and arming remains the probe checklist's separate owner-worded act.
- **`docs/reference/fleet-pins.md` is a STATE file, not a changelog** — every row is re-trued at each re-pin and each converge's evidence lives in that update's commit message, so `git log --follow` on the file is the deploy chronicle.
- **The canary rule says what a bake is for**: it qualifies the IMAGE as a runtime always and the capture code when it changed, an engine payload's own proof is its next disarmed boundary cycles, and digest asymmetry between the capture hosts is an in-flight state of an OPEN rollout needing a recorded reason and a bound.
- **A single-host capture re-pin costs no data; the constraint is never restarting BOTH capture hosts close together** — a per-host gap is covered continuously by the other, and a healed hour books only whatever its splice leaves unfilled.
- **`data-catalog-full.md` records the `/BTC` store legs' REST-only provenance**, and `infra/grafana/alerts.yaml` names its verify-replay blind spot beside those rules: nothing watches `ops_verify_replay_reused`, so a busted checkpoint reverts every sweep to a full rescan while all three rules read healthy.
## 2026-08-20 — iter-141: the reconciler says WHY the fleet went dark, without changing what it books ([[T0143]] resolved, spec `00096`)

- **`zcrypto_reconcile_residual_gap_seconds_total` books the ABSENCE of data and never fault attribution** — the dark-episode verdict rides beside it as a parallel view and is never subtracted from it (spec `00096` D1).
- **`zcrypto_reconcile_dark_episode_seconds_total{verdict=...}` partitions the booked seconds across three labels, so the metric checks itself**; records written before the discriminator existed count `undetermined`, so the counter never retroactively claims knowledge the system did not have.
- **The discriminator is cross-host agreement on Kraken's own payload timestamps** — two hosts receiving the same message record byte-identical values by construction, and a host that was not receiving cannot manufacture one; the interior evidence brackets the dark windows and says nothing about their interiors.
- **`infra/runbooks/ops.md` gains a section for `zcrypto-reconcile-residual-gap`**, the system's only `severity: critical` reconcile rule, with the verdict as a numbered triage step and the detail moved off the alert summary.
- **[[T0144]] took the uncovered half** — nothing writes Kraken's status messages into the archive — and was resolved as a refutation two entries below.
## 2026-08-20 — the dark-episode verdict reaches the fleet, and two apt pins are retired (deploy of iter-141)

- **The dark-episode verdict is live on `zcrypto-ops`**, its three labels partitioning `residual_gap` exactly, with the historical records counted `undetermined`.
- **The ops role's pins assert reads the CONTAINER NAME, not the entrypoint command** — one shared role default replaces the two independent literals that made `docker inspect` return rc=1 and the assert skip itself silently on every ops converge, guarded by a drift test.
- **zaccess's `alloy` and `caddy` are unpinned and their holds actively cleared; the repo FOLLOWS apt** — they are third-party packages, and the pin had already made the role request a downgrade `apt` refuses, dropping the host from the play and taking the D16 client-cert revocation path with it.
- **Both packages are ABSENT from `fleet-pins.md` rather than restated there as readings** — a reading has no producer and no consumer, so it drifts into a confidently-wrong number the first time anyone runs `apt upgrade`; the reasoning sits at the file and at the role.
## 2026-08-20 — Kraken announced both venue outages, and the repo said otherwise ([[T0144]] resolved as a refutation)

- **Spec `00096`'s complementarity motivation is withdrawn: Kraken announced BOTH venue outages** — the WS `status` channel, the REST `SystemStatus` endpoint and the public status page are three sources, and collapsing them into one phrase is what made the claim false.
- **[[T0144]] is resolved as a measured refutation with its remedy consciously dropped** — the `status` frame carries no venue timestamp, so an archived row would hold LOCAL receipt time, inheriting none of the cross-host verifiability the discriminator rests on.
- **The residual-gap triage reads the alert's `activeAt` or the log's own timestamped frames, never the venue-status counter** — that counter latches until the capture daemon restarts and `increase()` is blind to a first sighting; writing the hygiene-map row is now an imperative, because the map, not the counter, survives retention.
- **A venue-not-online silence is per host and deleted at that host's own next restart** — carried past a restart it blinds the next event entirely, since a restart makes the next event's states first sightings again and the recurrence rule cannot see one.
- **[[T0145]] took the published maintenance calendar and [[T0146]] the spread calibration's second frozen interval — both resolved in the entries below.**
## 2026-08-21 — the spread calibration discloses both of its frozen intervals ([[T0146]] resolved)

- **`captured-spread-calibration.md`'s caveat names BOTH frozen-book intervals and shows their split**, counted the same way on both — rows the panel carried forward rather than the venue's reported status-ladder span, which is not what the caveat is about.
- **The published percentage stands; only its attribution changed** — a figure accidentally right in aggregate and wrong in attribution is the harder kind to catch, which is why the caveat shows the split rather than a single number.
- **The hygiene map gained its 2026-07-27 row**, so a continuity-sensitive analysis over that date is flagged like the other venue episodes rather than silently passing.
- **Unchanged deliberately: the calibration's numbers, its conclusion and its window** — this was a disclosure understating its own exposure, never a reason to distrust the result; re-examining the superseded windows is an explicit drop, their figures having no live consumer.
## 2026-08-21 — the venue publishes its own outage calendar, and the fleet now schedules around it ([[T0145]] resolved)

- **Every fleet-dark episode was a Kraken maintenance published days in advance** on `status.kraken.com/api/v2/scheduled-maintenances.json`, so the 07:01 clustering [[T0101]] parked as an unexplained signature is a calendar nothing in the repo read.
- **`.claude/rules/fleet-deploys.md` bars a converge inside such a window and `docs/reference/fleet.md`'s reboot `Schedule:` line bars a reboot** — both carrying the component filter and the trap that entries appear only a few days ahead, so an empty feed is never evidence the window is clear.
- **Discharged on a retrospective test rather than a manufactured converge** — scheduling a converge purely to execute the rule and rolling it back would have cost two restarts on the unbackfillable path to prove nothing that mattered.
- **A `ripe_when` names an observable event, never an act on unrelated work** — this topic's demanded that a converge be placed using the rule, which would also have kept the guidance off `develop`, unavailable at exactly the moment it is meant to be read.
## 2026-08-21 — the reconcile cycle stops scaling with market volume ([[T0147]] partial, spec `00097`)

- **The ops reconcile cycle no longer scales with market volume** — the timestamp arithmetic is vectorized onto int64-microsecond arrays and settled hours are skipped behind a fingerprint (spec `00097`).
- **The skip-cache fails open, never wrong** — an hour is skipped only on a fingerprint match taken from the same scan the examination used, the overlay is fingerprinted too, anything unreadable or stale reads as no cache at all, and any audit divergence pages and drops the whole cache.
- **`zcrypto_reconcile_cycle_duration_seconds` and `zcrypto_reconcile_hours_skipped` are published and charted**, the skip gauge incremented inside the skip branch rather than derived from the planned count; the `zcrypto-reconcile-cycle-duration` warning rule was staged for a post-merge push, taken in the rollout entry below.
- **A systemd trigger that fires while the unit is still `activating` is dropped, not queued** — a cycle overrunning its own tick silently halves the booking cadence with nothing paging below the 3 h staleness backstop.
- **`infra/nas/README.md`'s ledger-correction procedure carries the obligation to delete the cache in the same act**, and the residual-gap runbook's staleness arithmetic is corrected: the stamp lands at cycle completion.
## 2026-08-21 — the 00097 rollout: 1,636 s → 18 s in production ([[T0147]] resolved)

- **The reconcile cycle's cost fix is live on `zcrypto-ops`** — steady-state cycles skip the settled hours, and the ledger totals are identical across old and new, so the cache changed which hours were examined and never what any hour decided.
- **`zcrypto-reconcile-cycle-duration` was pushed only after the metric's first sample existed and its first evaluation read the live series** — the ordering every rule whose metric ships with an image now follows.
## 2026-08-21 — trial 43's "unrebuildable" runners were recovered, and the transcript that held them was pruned 27 minutes later ([[T0148]])

- **Trial 43 is rebuildable from committed code: `cli/portfolio/record43_book.py` reproduces registry row 43**, the treatment `record44_legs.py` already gives row 44, with `tests/test_record43_book.py` beside it; [[T0125]]'s "unrebuildable" finding, [[T0090]]'s and the master plan's §12 gate-limitations paragraph are re-trued in place.
- **The recovered runners' original bytes survive only in git history** — the module's docstring carries the `git log --all --full-history --diff-filter=A` recipe and the seven sha256s as its authenticity anchor, the frozen directory having left HEAD in the same branch.
- **A Claude session transcript is workstation-local, unbacked and pruned at 30 days** — the source transcript here was pruned minutes after it was read, which is the argument for committing bytes rather than a pointer to them.
- **The 43-vs-44 counterfactual has no crossing to find**: the ordering changes sign repeatedly across the whole measured cost band, so record 44's ADOPT margin is a point convention at the ×1.0 headroom basis rather than a regime — decision-support only, since the go/no-go reads the benchmark-relative basis and nothing re-couples to trial 43.
- **Point-in-time carriers deliberately left with the old wording**: the two 2026-08-03 entries above this one, and spec `00095`'s line, which registry record 47's `spec_hash` pins immutable.
## 2026-08-22 — iter-143: adopted orders' fills become observable, without widening the kill trip ([[T0142]] resolved, spec `00098`)

- **The engine subscribes `events.order.EXTERNAL` and filters by disposition against ledger-vouched rows** — a matched adopted fill appends to its forensic row, moves the fill and fee counters and latches the overfill trip; an unmatched event is counted and logged and reaches no trip, no row and no cancel (spec `00098`).
- **Neither path ever removes rows from `_attached`** — a late fill within the ledgered quantity journals as a detached append and one beyond it trips, because Kraken's ownTrades and openOrders are separate WS channels with no cross-stream ordering.
- **The startup adopt pass reads the cache WIDE and repairs from the reconciled order's own `filled_qty`** — a fill that happened while the process was down reaches no subscriber at all, so a positive delta past a dead-band is journaled as a REPAIR, never as a fill, and a negative delta trips rather than being clamped (D7).
- **`zcrypto_exec_external_events_total{disposition=...}` is a forensic instrument, not a page** — its admission lives in the CAPTURE role's keep-regex, so **the deploy converges `--tags capture,engine`, never `--tags engine` alone**.
- **The dashboard-text guard now scans `legendFormat`**, and `filter_unclaimed_external_orders` and its sibling knob are explicit and pinned by the config-shape test, so an upstream default flip surfaces in the suite rather than in production.
## 2026-08-22 — the one-sleeve era ended, and every number derived under it was re-measured ([[T0149]] resolved)

- **The book runs three sleeves, and the `Engine · sleeve composition changed` rule, its runbook step and the engine panel description stop claiming gross moves in proportion to the sleeve count** — it does not, and the combined book is not the summed sleeve gross ÷ 3, so the read recipe is `sum(abs(zcrypto_engine_target_weight))` directly.
- **The `grafana-push` that summary and panel description owed ran with the 2026-09-01 rule-set push; what nothing yet carries is the read-back confirming `notification_settings.repeat_interval` survived it** — the provisioning API drops unknown fields silently and the push script's read-back checks only datasource UIDs.
- **`repeat_interval: 12h` sits on the rule's own `notification_settings`** rather than in the shared policy tree — an announcement whose runbook opens "nothing is broken" should not page every four hours, and 12 h is a deliberate trade, because a SECOND composition step reuses the same alert instance and the repeat interval IS its detection latency.
- **The alert's re-derivation step carries the ≥3-complete-ISO-week basis, the `accum-replay` command and its flat-start initialisation trap** — the pre-registered 10 bps band is no longer met at any NAV measured, and the archived [[T0149]] holds the re-derived curve and the fixed ⅓ weights question.
- **Placement rises with gross, and `ordermin` binds where `costmin` does not** — the alert and its runbook had never drawn that distinction, and the topic's own speculation that more sleeves would shrink notionals is refuted.
## 2026-08-23 — iter-145: the weekly tracking-error comparison, the ledger reader behind its cost half, and a trip for an engine that has quietly stopped placing (spec `00091`, [[T0018]] and [[T0090]] still partial)

- **`zcrypto engine tracking-report` reports weekly realized-vs-intended drift and PROPOSES a cost blend it never applies, and a kill-switch trip on the SAME drift function fires at the 4-hourly boundary** — so the number a human bands and the number the engine trips on cannot diverge (spec `00091`).
- **It fails closed rather than reporting a number it cannot stand behind** — a week with no fills reads *no data*, an unreconcilable ledger export refuses the cost half while the drift half still prints, and a non-EUR fee currency disables the cost half alone.
- **The trip ships disarmed and is structurally inert**: `tracking_band_bps` is absent, the trip is refused while `exec_armed` is false, and a verdict needs 42 consecutive boundaries journaled at `full` — counting `full` across a window's exec records is a verify-by-outcome step of the probe-window procedure, and `infra/runbooks/engine.md` gains an `engine-tracking-band` section for the tile's states and the arming preconditions.
- **The cycle artifact journals the `closes` it used**, so a week is scored from the journal instead of replayed on the trade path; the deploy is an attended, canary-gated engine converge run `--tags capture,engine` with every reader converged before the writer, and the branch puts `cycle.py` and `journal.py` inside the NAS gate-export's replay closure.
- **The realized numbers are owed at rung 1, not here** — [[T0090]]'s remaining next-step records that split — and the class answer to the trip's finite life, journalling the POSITION beside the `closes`, is [[T0150]].
## 2026-08-23 — T0115's abort was never a `dispose()` race: the Rust logger double-inits; bumped to the version that fixes it ([[T0115]] resolved)

- **`nautilus-trader` is bumped to the version that fixes the abort** — dropping the kernel's `LogGuard` no longer resets the initialized flag, so a second `init_logging()` raises instead of panicking in the Rust logger; [[T0115]] is resolved and every surface that described the old mechanism is corrected where it stands.
- **`zcrypto engine run` re-arms faulthandler immediately after the node is built** — `loop.add_signal_handler` for SIGABRT installs CPython's no-op C handler over faulthandler's, so a native crash dumped nothing; `disable()` before `enable()` is load-bearing, and the dump goes to fd 2, which needs no `fileno()`.
- **`infra/scripts/nautilus-logger-guard-probe.py` replaces `nautilus-dispose-probe.py`** — the old probe spawned pytest-free children, so every child selected uvloop, the one arrangement in which the guard cannot drop, and it was blind on every version.
- **Arming is gated on `cli/engine/order-semantics-verified.json` by two independent guards** — the engine role refuses a converge rendering `exec_armed = true` on a version absent from it, and `cli.engine.execgate` refuses the runtime gate when the RUNNING interpreter's version is absent; the record is shape-checked before membership, because a malformed record must vouch for nothing.
- **`infra/scripts/kraken-order-semantics-probe.py` and `infra/runbooks/order-semantics-verification.md` make the attended ~€0.20 pass committed, repeatable code** — dry-run by default, submission behind `--apply`, cancelling only by order because the live engine shares the account, recorded in `docs/reference/adapter-verification/`; adding a version to the record IS the act that records the re-run.
## 2026-08-23 — every open/partial `ripe_when` becomes a trigger again, and two that had already fired were adjudicated

- **Every open and partial topic's `ripe_when` states the trigger and nothing else** — a status report, a derivation or a history there costs more than it informs at the index review that reads it; what those fields carried moved into the topic bodies first, checked claim by claim.
- **A trigger is only as good as the datasource it is read from** — the measurable ones were read live rather than reasoned about, and one is counting from a series restart rather than accruing continuously, which the topic now says.
- **A fired trigger is live work: [[T0018]] and [[T0016]] were consciously re-deferred on the owner's ruling with the reason written down** — T0018's remainder is sequenced rather than triggered, so it carries no `ripe_when` at all, and T0016's ripe legs wait on attention, not readiness.
- **Topic frontmatter parses as YAML** — a value opening with a backtick or `**`, or containing `": "`, silently did not; nothing consumes the field programmatically, which is why it had never bitten and exactly why it would have bitten the first tool that tried.
- **A long trigger is not automatically wrong** — [[T0085]]'s stays per-sub-item because it genuinely gates five different things; the standard is one trigger stated directly, never a character budget.
## 2026-08-23 — a periodic check stops being an open topic: T0103 re-homed to the runbook, and the drop its trigger licensed withdrawn

- **The healable-threshold re-derivation is a runbook routine, not an open topic** — `infra/runbooks/ops.md#healable-threshold-rederivation-due` carries the trigger, the post-fix cutoff, the production summing expression and an explicit re-arm step, on [[T0113]]'s precedent that a recurring routine becomes durable where the operator meets it.
- **Count that counter from the ops reconcile ledger, never from Grafana Cloud** — Cloud's retention is far shorter than the selectors written against it, so a long-window query reads zero over a series it has never seen move; the corrected trigger names its source path.
- **`zcrypto-reconcile-healable-gap-rate` divides by live pair count before comparing, so its threshold is 600 s PER PAIR** — an un-divided comparison reverses the conclusion on the same numbers — and the rule finally has a runbook section, found only because re-homing the routine required asking where its signal is documented.
- **[[T0045]] is a conscious DROP on the owner's ruling** — byte-exact CRC re-attestation costs a schema migration on the unbackfillable live capture stream and covers only post-change data; the gap stays documented and a future discrepancy earns a new topic, not a revival.
- **[[T0025]]'s trigger kept only the leg that would make the work live, a selected pair changing identity** — a trigger adjudicated to *no* every time only guarantees the next refresh re-derives the same answer; the topic was retired outright at iter-151.
## 2026-08-23 — the cycle record learns the NAV it priced against, and the drift it feeds stops depending on a live config value

- **`nav` and `held` are journaled per cycle, and `realized_drift` scores each cycle under the NAV that was live for IT** — a `shadow_nav_eur` change no longer re-scores an already-closed, healthy week against a new denominator.
- **Both producers of `CycleStages` carry it** — the executor's trip path and `tracking-report`'s replay path — because fixing one surface alone leaves the human banding at one NAV while the engine trips at another.
- **`infra/runbooks/engine.md` still REQUIRES the band disarmed across any NAV change until the engine converges onto this code, and is re-trued in that deploy** — the arithmetic replaces the habit only once the habit's premise is gone.
- **`held` is journaled and deliberately NOT read** — a week scored shortly after the deploy spans cycles written before it, so a scorer taking journaled positions for some cycles and fill-accumulated ones for others mixes two position sources inside one week; [[T0150]] stays `partial` on a retention clock, and the three fill-history guards stay live until then.
- **The weekly report's FLOOR half stays on the caller's live scalar deliberately** — realized drift is past tense and must not be re-denominated while the floor holds NAV constant by design, so a verdict across a NAV change is advisory; `evidence_fingerprint` covers neither new field and a test pins that.
## 2026-08-24 — the manifest zoo is removed rather than read

- **Every dataset manifest is normalised at the WRITER: `series` is keyed by the parquet's path relative to the dataset root**, so no consumer ever derives a key — which is what flattens the four shapes into one and makes path-bound verification possible at all (spec `00099`).
- **A manifest declares its own `identity` digest, and the digest's ordering is by series key** — without the declaration a caller would still need per-set knowledge, the special case one layer up; the converter refuses unless every recomputed series hash matches what the legacy manifest attested.
- **[[T0133]]'s parked consumer is discharged in production**: every hub set supplies path bindings, so two series swapped inside a manifest-attested set are refused at fetch and at read, where both were invisible before.
- **The external freeze is excluded by construction, carrying no `schema_version`** — loosening the contract to fit a set this repo does not write is what killed the first attempt; the committed sidecar is the normalised form of its attestation.
- **A guard asserting only the fields its converter preserves passes against the defect it names** — that shape erased part of `data/ohlc-reach-20260813`'s seam record; derivable fields were reconstructed and labelled, and what is genuinely gone is named in that set's own rows.
## 2026-08-24 — refine-rules round 5: the corpus stops telling stories

- **Round 5's lessons live in `agent-ops.md` and `fleet.md`'s nas row** — discriminating fixtures on guard-proving, a grep hit is not a call site, snapshot unversioned data before rewriting, `--amend` folds the staged set into HEAD, and push-only workflows sit outside PR CI's reach.
- **The rollout checklist stopped demanding a freshness read its own abort table strikes through.**
- **The dependabot skill lost a jq expression that hid every pending check behind any concluded one, and a bare `git stash pop` that could dump the user's own pre-existing stash.**
- **The corpus is condensed to the cold-agent standard — every sentence tells a cold agent when to do what and how** — tense-narration of before-states, incident history, rationale past the one-clause why, and mechanics duplicated from self-documenting scripts are out.
- **Two protected-file residuals were flagged for the owner rather than edited**, per `fleet-deploys.md`'s per-edit sign-off.
## 2026-08-25 — the engine crosses to nautilus v2, and the attended pass clears it for arming

- **The engine runs nautilus-trader 2.x, pinned with `===`** — arbitrary equality, because `==` ignores a local version segment and orders `X+local` above `X`, which produced one lockfile handing different platforms different artifacts (spec `00100`).
- **The startup watchdog is deleted, and `cli/__main__.py` logs every `BaseException` but `SystemExit` and `KeyboardInterrupt` across every subcommand** — a Rust panic derives from `BaseException` precisely so `except Exception` misses it, and the engine's core is compiled Rust, so a panic would have died on stderr with no level for the alerting to see.
- **`tests/test_engine_stub_fidelity.py` checks every test double under `tests/test_engine_*.py` against the library type it restates, the module list derived from the directory** — a hand-written restatement of a nautilus contract with nothing verifying it is what let a load-bearing production coercion be a no-op under every fixture.
- **The attended ~€0.20 order-semantics pass PASSED on v2 and both arming guards clear the pinned version**, recorded in `docs/reference/adapter-verification/`, with the pin FROZEN at that string until the engine is armed on it; `infra/scripts/probe-with-vaulted-key.sh` decrypts the trade key straight into the harness's environment and never onto a command line, a file or a shell.
- **The engine converge onto a v2 image was owed at closeout and landed 2026-08-26, two entries below**, gated on the secondary's capture bake at the v2 pin ([[T0085]]); the record's own `## Owed checks not discharged by this pass` section is what `infra/runbooks/engine.md`'s pre-probe step reads and refuses to arm past; [[T0152]] (since resolved), [[T0153]] (since resolved) and [[T0154]] (since resolved) carry the observer join, the voided-fill surface and the reconciliation-minted terminal events.
## 2026-08-27 — the engine's idle data socket stops spending a connection budget it shares with capture (iter-146)

- **`ws_idle_timeout_ms=0` on the engine's Kraken data client** — disarmed, nothing is subscribed, so the adapter's idle timer measured the absence of data it was never sent and reconnected against a per-IP budget shared with L2 capture and the REST order path; the heartbeat still catches a dead peer (spec `00101`).
- **The value is pinned as the literal, because `None` means the adapter's default** — an interface pin measures both readings on the pinned wheel, and the builder test was proven to refuse `None`.
- **`infra/runbooks/engine.md` says `systemctl stop`, and says why `docker stop zcrypto-engine` is wrong in the same sentence** — the unit is an attached `docker compose up` with `Restart=always`, so systemd restarts it and the retry storm resumes.
- **What this does not fix is recorded where it will be read** — the two-socket outage loop has no adapter knob and Rust-side socket lines never reach Loki; spec `00101` D7, the runbook section and archived [[T0155]]'s body each carry it.
- **The converge was owed**: [[T0155]] closed at the next attended, canary-gated engine converge — engine-only tags, gated on the new image having baked as capture on the secondary first.
## 2026-08-27 — iter-147: the external-order observer's two halves are joined against a real venue order, and the alert that step owed is declined ([[T0152]] resolved)

- **The external-order observer's publish and delivery legs are joined against a real venue order** — one owner-placed order moved `zcrypto_exec_external_events_total{disposition="unmatched"}`, and what the events WERE is read back by event class from the executor's own log line in Loki, since the counter carries only a `disposition` label.
- **A reading of 0 on that counter cannot distinguish a wired observer with nothing to report from one never called** — both label sets are registered eagerly at engine start, which is why only a window containing a known external order could ever have decided it.
- **No alert is owed on that family: the decision is DECLINED, not left owed** — the reasoning and the one change that would make a rule viable sit in `tests/test_infra_alert_rules.py`'s `NOT_A_FAULT_SIGNAL` entry, where the next person to ask reads it; what is declined is paging, not watching.
- **The first disarmed boot carrying live orders or positions is a reading still owed**, owned by `infra/runbooks/engine.md`'s pre-probe step; the `matched` disposition's one-off reading BY VALUE is registered in [[T0018]] and echoed in its index bullet, which is what a pre-probe blocker sweep actually reads.
- **The v2 engine converge landed BEFORE the attended OS/apt upgrade round rather than after it, as [[T0085]] had sequenced** — that round will now restart a RUNNING v2 engine on the primary, which makes its inter-cycle-gap constraint load-bearing rather than routine.
## 2026-08-28 — iter-148: the reconcile ledger scan is measured, made cheaper, and given the monitors that will say when it needs renovating ([[T0044]] resolved)

- **The reconcile ledger loader parses line by line off the handle instead of materialising the whole file**; the return type stays a `list` deliberately, because the caller appends to it mid-cycle and queries it per pair-hour, so a generator breaks at the first append.
- **`zcrypto_reconcile_ledger_scan_seconds` publishes the scan's own cost as a REQUIRED parameter, never a defaulted one** — a default would publish a healthy-looking `0` from any caller that forgot it, which is exactly the reading the two rules are thresholded on; `infra/scripts/bench-ledger-scan.py` is the committed instrument behind it, synthesising against a GROWING key space.
- **`zcrypto-reconcile-ledger-scan-slow` and `-critical` both carry `noDataState: OK`, panel 207 plots the cost against the record count, and `infra/runbooks/ops.md` gains `reconcile-ledger-scan-cost` with a `### Retire when` naming its own end condition.**
- **That runbook's operative step says MemAvailable, never MemFree** — the two differ by about tenfold on the ops host, so an operator following it under a page would have judged a healthy host over-committed; `docs/reference/fleet.md`'s ops row records both figures and which one is the operand.
- **Converge `zcrypto-ops` first, THEN `grafana-push.sh`, then verify by VALUE** — a rule pushed ahead of its metric's first record pages a spurious no-data alert, and `(no series)` reads FAIL, never a zero.
## 2026-08-28 — iter-149: the capture silence bars survive their re-derivation, and the claims around them stop being false ([[T0129]] resolved)

- **Both capture silence bars stand at their derived values, and a future re-derivation starts at the parquet archive via `infra/scripts/gap_distribution.py`, not the gauge** — retention caps the gauge and never the phenomenon, which was [[T0129]]'s own defective premise.
- **Every firing either rule has ever had was a published venue maintenance window** — `alerts.yaml`, spec `00084`'s D11 rows, panel 102's description and the operator-visible summary stop calling the cause unexplained, both rules now open by telling the operator to read that calendar, both gained the runbook sections they never had, and `infra/runbooks/capture.md`'s venue-halt step stopped calling those firings not something to respond to separately.
- **`execErrState` goes `OK` on those two rules alone** — a datasource failure there minted a page asserting a silence the query never ran to observe; the residual is that a rule-scoped error on them now pages nothing, and a dedicated datasource-health rule is a conscious drop, since a datasource outage is already over-reported.
- **`infra/grafana/alerts.yaml` is inert until `grafana-push.sh` runs — an upsert, so no prune is owed — and verification is BY VALUE: read `execErrState` back for both uids and confirm each summary carries its `Runbook:` line.**
- **`.claude/rules/spec-plan-locations.md` now ranks every review dispatch by blast radius and bans the open-ended sweep**, and a fix commit takes its own mandatory pre-push review rather than a further round on a closed subject.
## 2026-08-28 — iter-150: the rollout gate closes at gate-close, memory becomes a fleet-wide routine, and every converge writes its own record

- **A rollout's gate is hard-capped at its three events and PASSES at gate-close** — nothing after it is owed to a rollout and the next converge never waits on an open read; the skill's residual paragraph, its T+ reading bands and `fleet-pins.md`'s owed-read clauses are gone.
- **Leak detection is a fleet-wide telemetry routine that runs regardless of converges** — `zcrypto-fleet-memory-headroom` reads RSS against the container limit with literal-byte denominators a test pins to the ansible vars, `zcrypto-fleet-memory-leak` is the bake's own reading method as PromQL gated on process age so a restart's predecessor floor cannot be subtracted, and `zcrypto-fleet-daemon-restarted` pages once per converge or Alloy bump as that action's own record.
- **`converge.sh` appends one JSON line per real pass to `docs/reference/deploy-log.jsonl`** — target, tags, every `-e` operand, revision, dirty flag and rc — so the digest and timestamp a rollback needs are written by the pass that set them; preview-only runs and aborted confirms leave no line, and `fleet-pins.md` stays the state file, re-trued from that line.
- **`capture-deploys.md` is `fleet-deploys.md` and `zcrypto-captures-rollout` is `zcrypto-rollout-image`**, each named for what it covers, with a new test walking every topic link in the open-topics index so the next mechanical rename cannot leave a dead pointer on the surface every review consults.
- **The `grafana-push.sh` those three rules and the retitled panel owed ran with the 2026-09-01 rule-set push — an upsert, so no prune; the verify-by-value read is four hosts returning and the rules reading `Normal`.**
## 2026-08-29 — iter-154: the decompose table keeps the whole-book limits' share (spec-less, [[T0121]] resolved)

- **`zcrypto engine decompose` splits the transition into `capped -> limited` and `limited -> final`**, so a cycle where a §10 whole-book limit bound no longer charges the limits' share to the volatility governor — which matters because that table is what an operator opens to understand `zcrypto_engine_limit_bound_total`.
- **`limited_gross` is a REQUIRED field on `CycleStages`, never defaulted** — a default would let a future construction omit the stage and fall back to a book that is only correct while nothing binds, the same shape as the defect being fixed.
- **A `ripe_when` that waits for the failure before fixing the thing that reports it is not a trigger** — [[T0121]]'s first arm waited on a counter that may never move, and its second was not a condition anyone evaluates.
## 2026-08-29 — iter-155: the sleeve-promotion path is ruled, then re-ruled when the code disproved it (spec-less, [[T0123]] partial)

- **§12 rules how a superseding family re-enters the gates, scoped by WHAT THE CHANGE TOUCHES rather than by a duration** — always a new Phase-5 record, a builder-contract bump, Stage-6a concordance re-earned and a head-to-head win over the incumbent; class A adds nothing, B adds rung 1, C adds rungs 1–2, and the class is pre-registered before the evidence so it cannot become a post-hoc argument for the least work.
- **Parity re-earns on the candidate's OWN journal, produced disarmed beside the live record, at the full unshortened gate window** — replaying the new contract against the incumbent's journal cannot work, because `replay_stages` raises on exactly the difference that IS the promotion.
- **The go/no-go band SPLITS**: the floor half is measurable from shadow cycles and quoted in the cut-over decision, and the realized half accumulates after cut-over on the new record's own fills, because Stage 6a logs intended orders only and a shadow book returns `insufficient-data` forever.
- **The decisions log records the partial overturn where the earlier rejection stands**, rather than appending a correction beneath it — §12 already re-derives the band whenever the target series moves, so band evidence never transferred across NAVs.
- **The remainder is one capability, deliberately undesigned** — the candidate runnable beside the live record and scorable under its own contract; [[T0123]] stays `partial`, its `ripe_when` re-cut to the ADOPT verdict the registry actually mints.
## 2026-08-30 — iter-156: every alert gets a runbook, and a daily pass that reads the fleet — with a guard that took six rounds to stop lying (spec 00104, [[T0157]] partial)

- **Every alert rule resolves to a runbook section in one shape** — *What you are seeing* / *What it means* / *What to do* / *Retire when* — reached from the rule's own `Runbook:` annotation, with a test that goes red on any rule lacking a resolving link (spec `00104`).
- **`infra/scripts/ops_daily.py` reads alert state and history, Loki, the dead-men two ways, the fleet verdict and the window's deploys** — exit 2, a source it could not read, outranks exit 1, because a source that cannot be reached is a finding about that source and never a silent gap.
- **A command is autonomous only when it matches an ENUMERATED SHAPE** — a literal head, the flags it may carry and how many operands of which class — because no flag allowlist can catch an argument that is a file by position; `awk`, `sed`, `xargs`, `tee` and `dd` are absent entirely, and a corpus test refuses any autonomous verdict on a runbook command carrying a destructive token while going red if a narrowing makes the pass refuse its own diagnostics.
- **`prepared` means prepare it and stop — including *not yet enumerated*** — add the shape on a fix branch, never widen the table in the moment to get the pass moving.
- **At closeout the instrument had not run against the live stack and the runbook summaries were unpushed** — both need a vaulted credential and an outbound call; the first real pass was [[T0157]]'s remaining sub-item and the spec's own acceptance test (since resolved).
## 2026-08-30 — iter-157: the signals that were silently absent — reminders, log levels, and the descriptions no test can reach (spec 00107)

- **`ops_daily.py` computes reminder due-ness every day from state it can reach — the snapshot register's `## Re-confirmation log` and the healable counter — rather than from a scheduled Slack ping**, which the MCP can schedule but offers no way to list or verify, so an arming that failed to exist is undetectable from this side; an OWED line reports and never blocks, and only an unreadable source is exit 2.
- **The three operating surfaces that named Slack as the trigger now name the report's `## Reminders` section** — `reference-data.md`, `ops.md#healable-threshold-rederivation-due` and the daily-ops skill's step.
- **The `dropping late event` / `dropping replayed event` sites log at INFO** — they record the expected consequence of a normal reconnect, and the scraped reconnect counter is the handle rather than a log-line count; `zcrypto-rollout-image` now tells an operator those post-start lines are RESTART replay, so a reconnect counter reading 0 there is correct rather than a failed benign explanation that would have rolled back a healthy host.
- **`check_descriptions` asserts every healthchecks.io description carries a resolving `Runbook: infra/runbooks/<file>#<anchor>` citation and no internal token** — the one operator-visible surface no repo test can reach, since the descriptions are hand-written in a SaaS; a finding is a report line and a journal clause, never the exit code, because the check detects and cannot repair.
- **The daily-ops skill gains the remedy as a step: rewrite the failing description in healthchecks.io with the admin key, sending the description field alone so the schedule and grace period are untouched, and read it back**; the log demotion itself reaches the hosts only with the attended capture-image rollout [[T0037]]'s `ripe_when` already owes.
## 2026-08-31 — iter-158: ten autonomous inductions against production, and what the fleet said back (spec 00105, [[T0158]])

- **The telemetry drill tier is executable and recorded** — `infra/runbooks/drills-telemetry.md` and `drills-order-path.md`, `docs/reference/drill-log.md` with a heading/order shape test, the critical rule `zcrypto-engine-dark-with-exposure` with its `engine.md` section, and the `grafana-cloud-dark` procedure; drill P's restore is a converge, so it stays attended and unproven, as the proven-tier table records (spec `00105`).
- **A run's measurements live once in `drill-log.md`; only what changes how a drill is RUN stays in the runbook section** — the section's own standing rule, applied to the run-record blocks that had accumulated in it.
- **`zcrypto-capture-all-streams-silent` cannot see a stopped daemon** — a stopped daemon does not publish a large value, it stops publishing, the series vanishes and `noDataState: OK` reads the absence as healthy; the healthchecks.io dead-man is the sole detector of that failure, and the two cover opposite ones.
- **Confirm a clear from the RULE STATE, never from channel silence** — a dead-man whose fire path is an unlabelled `or on() vector(0)` has its firing instance deleted rather than resolved on recovery, so no resolved notice is sent at all.
- **Six method traps, each fixed on the operating surface**: the rule-state endpoint keys on TITLE, not uid; a firing rule's `activeAt` is firing-onset, so the `Pending` read is taken while pending; the history endpoint takes epoch SECONDS; rule titles must match WHOLE; a check's bound anchors on its `last_ping`, not a timer's `LastTriggerUSec`; and restoring an `OnCalendar` timer fires a run immediately regardless of `Persistent=`.
## 2026-08-31 — iter-159: the red button — one command that flattens the whole account, and the defect ~1900 green tests could not see (spec 00106, [[T0159]] partial)

- **`zcrypto engine flatten` cancels every resting order, closes every margin position reduce-only and sells every non-EUR spot balance in two passes — and its PLAIN INVOCATION IS A DRY RUN that sends nothing**; only `--execute` writes, behind a `FLATTEN` typed at the controlling terminal, and there is no `--dry-run` flag, because a flag meaning "do nothing" is the one that gets forgotten (spec `00106`).
- **The exit code is the whole verdict and never reads a leg's answer** — 0 flat, 1 refused with nothing sent, 2 something still open or the account-wide cancel failed, 3 the venue unreachable BEFORE the first write; a read that fails after the first write is 2, never 3 and never 0, because the account may already have moved.
- **`/usr/local/sbin/zcrypto-flatten` is the operator's only interface** — it writes the kill file, stops the engine unit, waits for inactive, then runs a one-off container with `--entrypoint zcrypto` placed BEFORE the image, without which the words are appended to capture's entrypoint script and the container starts CAPTURE on a host whose engine was just stopped with positions open; the daily pass's classifier is asserted to refuse it under every wrapping the fleet spells.
- **One predicate serves both the send decision and the residual verdict**, so a balance skipped as dust can never also be reported as a residual; dust is SPOT-ONLY, because a sub-`ordermin` margin remainder left open is exposure.
- **No call has ever been made against the live account** — the read shapes are unconfirmed until the attended read-only dry run runs THROUGH the wrapper and is recorded in `docs/reference/adapter-verification/`, and reduce-only MARKET on margin is unproven until drill B passes; [[T0159]] stays `partial`, and [[T0160]] carries the nautilus bump whose side-enum change would make the command refuse to flatten.
## 2026-09-01 — iter-160: `rest-hold` — an order that stays, and the two rulings that came from the running code (spec 00108, [[T0018]] partial)

- A plan may declare a third mode, `rest-hold` — an order priced passive of the touch that rests for a stated span; **`offset_pct` is a PERCENT, `5.0` is five percent**, and the neighbouring cancel-mode constant is a fraction, so `probe-plan --check` echoes both fields back in words on the intent line an operator reads before anything is placed.
- On `rest-hold` a cancel the engine did not request is terminal — the intent finishes and nothing is resubmitted — so an operator trained to cancel by hand in Kraken's web UI ends the intent rather than provoking a replacement (spec `00108`).
- `zcrypto_exec_resting_order_age_seconds{mode}` states in its own HELP text that it is the engine's belief rather than venue truth and that nothing publishes while the engine is down; no alert rule is built on it, and spec `00108` D7 records at the guard why one cannot be — an orphaned order reaches an operator through the engine's ERROR-log rule instead.
- `infra/runbooks/engine-procedures.md` carries the `rest-hold` plan shape beside Drill A's, with its units in the sentence next to it and the three terminal outcomes to read back in the ledger.
- The mode ships disarmed and reaches the engine only on an attended converge — the capture role first, since the metric family must be admitted to the remote-write allowlist before it is published; `infra/runbooks/drills-order-path.md`'s order-path sections stay `blocked` until rung 1, and [[T0158]] holds that tier's execution.
## 2026-09-01 — iter-161: the dead-man that could fire but never clear (spec-less, [[T0158]] partial)

- `zcrypto-nas-archive-pull-stalled` aggregates without `by (host)`, so its healthy and firing arms carry the same label set and Grafana can resolve the instance it raised; a dead-man whose arms differ pages and then goes quiet with no resolved notice at all.
- Panel 103's threshold expression moves with the rule in the same commit — `tests/test_dashboards_cover_metrics.py` pairs panel against rule on exact string equality, so editing one alone drops the pair rather than failing on it.
- The comment beside the rule names the siblings that genuinely share the shape, so a rollout scoped from it cannot hunt for a wrong count and "fix" rules that need nothing.
- The pilot proves instance identity only; whether a resolved notice is emitted is read from a fire-and-clear, and [[T0158]] carries that reading with both outcomes written in advance — a YAML comment is prose, not registration.
- `zcrypto-engine-dark-with-exposure` is live on the Grafana stack, discharging the push [[T0158]] owed on a trigger that had already fired.

______________________________________________________________________
## 2026-09-01 — iter-162: the spec+plan review becomes a skill, and the four runs that shaped it become a report (spec-less)

- `zcrypto-plan-review` is the gate between a committed plan and its execution — `/zcrypto-plan-review <spec> <plan> [T<NNNN>]` for a human, or after `superpowers:writing-plans` for the model; a round is contract pin, two lens reviewers, a mechanical max-severity union, then a fixer, and the exit is the consequence statement rather than a counter.
- The skill verifies by running code every premise the pair asserts about the tree before any reviewer reads, routes a fleet-host premise to the main loop, refuses to amend a spec whose sha256 a `docs/reference/trial-registry.jsonl` record names, and sends any mechanism a fix introduces to a scoped re-review whose findings gate like a union.
- `docs/research/90.spec-plan-review-protocol.md` is the durable record of the four review-strategy runs and reads without the repo; every experiment branch, tag and worktree behind it is purged, and the loop ledgers stay as the raw evidence.
- `spec-plan-locations.md` delegates the substantive flow's review to the skill and keeps the blast-radius paragraph for the pre-push review alone; `zcrypto-auto-exec` names the skill in its cycle and its orchestrated list.

______________________________________________________________________
## 2026-09-03 — iter-164: the daily pass reported PASS on the value it exists to catch (spec-less)

- Every fleet check the daily pass reads carries the bound of the rule that owns it, so a `PASS` line means the value is inside that bound rather than that a series came back — `up` reads 0, not absent, when Alloy is running and the app it scrapes is dead.
- The pass renders the alerts that fired and cleared overnight as their own section, carries them into the journal paragraph, and moves the verdict off all-clear for a day containing a critical firing.
- It reports one bullet per firing INSTANCE rather than per rule, so an event on both capture hosts names both — which is what `capture.md`'s per-host prescribed silence needs.
- Its history filter admits every Alerting transition reason but `Error`, and its instance filter default-denies, because admitting an unmeasured reason costs one report line while dropping a firing costs a silent all-clear over a page.

______________________________________________________________________
## 2026-09-03 — iter-165: the venue-halt prose the fourth Kraken maintenance falsified (spec-less)

- `ops.md`'s `undetermined` triage requires an `activeAt` inside the hour under triage and names the recurrence rule beside it: the venue-latch rule cannot fall until the capture daemon restarts, so an older stamp proves nothing.
- `capture.md` states which capture silence rule a halt of a given length can raise, so which rule fired measures the outage's LENGTH and the quiet per-pair rule bounds it rather than exposing a blind spot.
- The prescribed silence now matches `system` as well as uid, host and window — the rule groups by that label precisely because it decides the response, and matchers are conjunctive — widened as the ladder climbs and never ahead of it.
- The venue's maintenance publication lead is a FLOOR on every surface that carries it, with the consequence unconditional: an empty feed is never evidence the window is clear.
- The venue-status guard asserts the `effective_time` field its docstring names, captured at the logger because `zcrypto` loggers do not propagate and `caplog` sees nothing; `alerts.yaml`'s comments moved with the runbooks and owe no Grafana push, since `grafana-push.sh` parses the file through `yaml.safe_load` and comments never reach the API.

______________________________________________________________________
## 2026-09-03 — iter-166: four Grafana surfaces the venue halt disproved (spec-less)

- Panel 55 reads "last evaluated reading" rather than "cached": `zcrypto_exec_venue_ok` is written only when the gate is evaluated, so the tile can be hours old in either direction — and its description says a stale tile is never a stale gate, because every submission evaluates the gate itself first.
- The all-streams-silent summary points at the cumulative gap counter and the log's gap lines as the sharpest bound available while the reconciler cannot yet answer, and restores the three signals that stay green through total upstream silence — the socket connected, the keepalive completing, the dead-man pinging — which is the fault class's signature.
- Panel 202 says on its own face that a zero during a live blackout means "not yet booked", not "nothing lost".
- Panels 102 and 108 drop the counts that rotted: the ladder counter counts FRAMES and is born at whatever its first scrape catches, so reading it as an event count over-reads.
- None of it reaches an operator until `grafana-push.sh` runs — that script upserts every rule and every dashboard in one pass, with no dashboards-only mode, so repo and live stack diverge for exactly as long as the push waits.
## 2026-09-03 — refine-rules round 6: the oral rulings become written invariants, and the sessions become a protocol

- A month of oral rulings became written invariants in `.claude/rules/`: `ripe_when` holds the condition alone, reviewed is not approved and the trailer's referent is the commits the reviewer read, premises are verified at write time, necessity is the first gate for a new sentence, and a topic opens on the coordinator's word.
- `docs/reference/multi-agent-protocol.md` and the thin `zcrypto-main` skill define the session setup — one coordinator that runs no payload work and holds PR, topic, memory and memo-token authority, payload sessions assigned from a gitignored coordination table, and an hourly tick that pokes a stalled session rather than interrogating it.
- A session records *it was mine*, a rule consciously not followed, or feedback on a rule in an append-only lesson registry that rides its own branch and the refine round harvests.
- The plan-review loop takes the smallest correct fix and treats three or more mechanisms as a design change, re-pins on a decision-changing spec amendment, caps rounds at three with the budget moved to breadth, and runs the floor's executability pass and blind gate in parallel.
- `infra/scripts/amend-reviewed-by.sh` lands one trailer on one commit, HEAD or not, and refuses to orphan a hash `deploy-log.jsonl` or `fleet-pins.md` records; the Fable floor is scoped to the review that gates a change, and tiers are by family at the newest version.
## 2026-09-03 — T0162: the eight Loki dead-men can announce their own clear (spec-less)

- The eight dead-man rules page on the `metrics` receiver, which announces the clear an operator is waiting for; the four ERROR-log rules stay on `logs`, minted `disableResolveMessage: true` because a burst ages out of its own window before a resolve ping would land.
- `alerts.yaml` records beside `nas-archive-pull-stalled` that drill N disproved the label-mismatch theory, so re-deriving the cause from git history reaches the wrong answer; the receiver pin is the only lever, since `disableResolveMessage` is a property of the contact point rather than of a rule.
- `test_a_rule_that_fires_on_absence_can_notify_its_clear` refuses a rule that fires on absence while pinned to a receiver `grafana-push.sh` mints with that flag, reading the receiver set from the script and separating dead-men from burst rules structurally rather than by a list of uids, so a ninth dead-man is caught.
- The change is as-code: it reached an operator only when `grafana-push.sh` ran, and that push, the induction that verified it and the `drills-telemetry.md` flip it owed were [[T0162]]'s — the 2026-09-05 entry below records them live.
## 2026-09-03 — refine-rules round 7: the registry's first harvest

- `agent-ops.md` gains *a cause that fits is not a cause that was checked*, with its measured shapes as examples, and *announcing a step is not taking it* — read your own last declared next step against what actually happened at the top of every turn.
- `branch-workflow.md`'s PR gate leads with the multi-session delegation, so a payload session reads the PR word as the coordinator's rather than as its own.
- `docs/reference/multi-agent-protocol.md` states that the coordinator is unpoked — the tick resumes a stalled payload turn, nothing resumes main's — and fixes the carried-record `branch` convention.
- `topic-ops` and the plan-review fixer say a unique anchor pins where text goes, never what it lands inside; `iterations-history.md` says an operator-facing sentence about an as-code alerting change flips with the attended push, not the merge.
- Refine-round whole-branch reads route to a Fable reviewer, on the owner's ruling.
## 2026-09-04 — iter-168: the emergency exit's reads are blind on a spelling class, and every surface that promised otherwise now says so

- Every operator-visible claim that `flatten`'s verdict, the kill-switch trip's sweep, the startup adopt pass or the go/no-go harness's pre-run count is complete now discloses that the adapter's order read is blind on the two-way-spelled basket legs; `tests/test_engine_flatten.py` computes that leg set from the basket, so the frozen tuple cannot drift.
- The three failure shapes stay distinct because their remedies differ: `flatten`'s cancel is account-wide and complete, so only its verdict is blind and the remedy is a re-press PAIRED with Kraken's own page as the non-adapter witness, never a re-run alone; the trip, the adopt pass and both row sweeps work per order from a blind Cache, so a retry does not help and a restart is not a remedy.
- The sentence stating that the cancel itself is account-wide is true and deliberately untouched, verified byte-identical to base.
- This ships the disclosure, not the repair: [[T0160]] carries the engine-path half — where flatten's twin shape inverts, so a repair written as "do what flatten does" ships inert — and the engine's in-flight REST query on an affected leg stays unmeasured, bounded by the ambiguous-strand path that halts the window rather than moving money.
- The runbook, README and code prose are live on merge; panel 61's description is as-code and waits on `grafana-push.sh`, and the harness's pre-run banner and the external-events metric HELP text reach an operator at the next engine converge.
## 2026-09-04 — refdata sweep #2: the basket holds, the alts' borrow falls to the band floor, and the account outruns the endpoint

- The monthly re-confirmation ran and the candidate basket is UNCHANGED — every leg online and margin-enabled, with leverage bands, `ordermin`, `costmin` and the alias ledger identical cell-by-cell against a fresh `AssetPairs`/`Assets` fetch, no refusals and no announced delisting naming a selected asset; the verdict is read from the rendered tables, never from `raw_sha256`, which moves monthly for churn that touches nothing depended on.
- The endpoint's short-side borrow rate fell to the floor of `kraken-fee-schedule.md`'s band on the alts, staying inside both that band and `cli/costs/margin.py`'s per-base bands — the fee schedule stands and nothing downstream is re-quoted.
- The account has moved to a fee ladder the public endpoint has not, which is what the register's fee columns exist to detect; the register records which of its parts are measured and which are inference, since the venue's Fee tab names no schedule version.
- Three pre-existing register defects were folded on the coordinator's word, each named in its commit as not this sweep's finding.
## 2026-09-04 — the flatten dry run gets a procedure, so the row it discharges has an author (spec-less)

- **`T0160`'s bump sub-item reads a `docs/reference/adapter-verification/` row recording `zcrypto engine flatten` on the current pin, and nothing on any operating surface told anyone to write one.** The imperative existed, in full, in `T0159`'s second human step — and `spec-plan-locations.md` makes the same point of a spec or decisions-log entry: a ruling recorded only there is invisible at execution time. The runbook carried the consequence without the instruction; its second flatten limit already told a reader the five account reads are unproven until such a row exists, but that paragraph addresses someone closing the book within minutes and could not sensibly tell them to author a verification record.
- **The dry run now has its own short procedure in `engine-procedures.md`'s flatten section**, beside the press rather than inside it: after the converge that carries the wrapper, engine running, a non-EUR spot balance the command can sell, the wrapper run read-only, and the result discharged into that version's adapter-verification file beside its probe table — the shape drill G's extra reading already uses. It names the clause the row satisfies, so a later sweep reads the row as a live operand rather than a stale record to tidy away.
- **Both ends are pointers rather than copies.** The limits paragraph links to the procedure so an operator mid-emergency can find who produces the row without being told to produce it; `T0159`'s step points at the procedure instead of restating it. The runbooks index gains the new anchor too — it was the only one of 114 missing from a page that declares itself the place a reader starts, which is this branch's own subject reappearing one level up. Nothing else in those files moved, and the row itself is not written here — writing the instruction and discharging it are different acts, and the discharge belongs to the attended dry run.

## 2026-09-04 — the attended flatten pass gets a fixture minter, and every number in it is the venue's own (spec-less)

- `infra/scripts/kraken-fixture-mint.py` mints the three ingredients an attended flatten pass needs — a resting order the command's own client did not place, a margin position and a non-EUR spot balance — and stops: the dry run is the default, `--execute` plus a typed word is the gate, and it has no cancel path at all, since the order-semantics harness's `on_stop` cancel is right for a probe and fatal for a fixture.
- `infra/scripts/mint-with-vaulted-key.sh` supplies its trade key as a SECOND wrapper with its own hardcoded target rather than a selector on the probe's, because that grant's `--probes:*` wildcard covers any tail and a smuggled selector would ride an already-granted command line; it is deliberately not allowlisted, so every run costs a fresh decision.
- It refuses to mint on a two-way-spelled basket leg twice over — from the list imported from `cli/engine/flatten.py` before anything is read, and from the venue row's own `altname` against its key at run time — and each refusal names which fired, so a disagreement reads as a finding about the list rather than as a duplicate.
- Every floor and both steps come from the raw `AssetPairs` row at run time and a missing one refuses: the adapter's instrument object never maps `costmin` into `min_notional` for any pair, and a floor read as `0.0` always clears on the one path where clearing wrongly spends money. The resting leg is sized at the price it will rest at, not at the best bid, because `costmin` is a floor on what the order is worth.
- Every account read is made in the MODE the reader it mints for uses and both test stubs answer the mode — a stub that answers the same whatever it is asked is a fixture the defect cannot move; [[T0163]] holds the client-order-id length the attended run must record, and the dry-run procedure's recording step instructs the position read that has never returned one.
## 2026-09-04 — E1b: which open-orders shapes return the resting rows (spec-less)

- `infra/scripts/e1b-order-visibility-probe.py` walks the eight open-orders call shapes — `open_only` × `instrument_id` × cache empty/populated — on ONE client with the cache populated once between the arms, so the variable that moves is the one E1 moved.
- The empty-cache arm is byte-for-byte what `zcrypto engine flatten` gets and the table marks that row; tests pin the client constructor to two positional arguments and no keywords, and pin the marked row to that exact combination, so the shapes cannot be renumbered out from under the mark.
- Read-only is guarded rather than asserted — the sweep runs against a stub that raises on any write method, a second check confirms no write name appears in the script's text, credentials come from the environment and the refusal names the variables, and the live-venue test is gated on an explicit opt-in variable so a skip is a decision.
- `docs/reference/fleet.md` records how an ad-hoc read reaches the engine host's trade key: inside the engine image with `--env-file /opt/zcrypto-engine/engine.env` and an `--entrypoint` override whose value belongs to what is being run, image read from the RUNNING container rather than from a pins row, driven from the workstation with the script on stdin so nothing lands on the host. Without the override the launcher execs `zcrypto capture` and the read never happens; `probe-with-vaulted-key.sh` is controller-side and is not the way to supply those credentials.
- Any such read shares the trade key with the still-running engine, so one engine order or cancel may be rejected around it: the safe window is the engine play's own — 30 min after a boundary to 10 min before the next — and `fleet.md` carries it alongside the run shape, true of any such read and not only this probe.
## 2026-09-04 — refine-rules round 8: the prose principle, the registry inbox, and the review loop's stopping rule

- A session's lessons go to its own gitignored inbox, `.local/agent-lessons/<session>.jsonl` in the main checkout; the committed ledger, its union merge and its pre-commit hook are gone, and the refine round harvests every inbox in its own PR.
- The memo and the coordination table live under `.local/`, kept and gitignored, with the read-guard hook on the new path; `.tmp/` stays disposable.
- One rule, `prose.md`, replaces the three prose rules with one principle — a durable file holds state and decisions, an event goes to git — and `infra/scripts/prose-tripwire.py` holds every threshold: a flagged block passes the necessity gate at review or is cut, and T0164 runs the global cleanup.
- Before any review the author self-checks the diff against the three classes that dominated findings; the third round carrying a Critical or Important on one subject ends iteration on it; a scoped re-review of a fix diff runs on Opus or the author's tier; a loop that reaches that round gets a transcript retro from main before the session's next assignment.
- CLAUDE.md's working discipline is `general.md`, with one cut and seven sharpenings from the point-by-point challenge; the count principle, the tick's pairwise compare, the staleness sweep's fixes and the coordinator skill's rename to `zcrypto-main-session-init` are the rest.

## 2026-09-05 — T0162: the Loki dead-men announce their clears

- The eight Loki-sourced dead-men page on the `metrics` receiver, so an operator sees the resolved notice in `#zcrypto` instead of reading rule state after a page; the four ERROR-log rules stay on `logs`, where a burst ages out unannounced. Drill N's re-induction is the proof, in the drill log.

## 2026-09-05 — T0165–T0167: the claims the prose cleanup found asserted in prose become tests, a rule and a daily read

- `infra/grafana/alerts.yaml` gains `Ops · verify-replay stale`, for the case its three sibling rules cannot see — the nightly canonical-archive sweep not running for 48 hours, while their gauges hold their last value; its section in `infra/runbooks/ops.md` reads the timer and the unit, and the sweep's freshness panel carries its bar. It is live: pushed on the owner's word ahead of the merge and read back by value.
- The daily pass reports every alert rule Grafana could not evaluate under "Rules not evaluating" and exits non-zero on one, so a rule broken by a later edit is read the next morning instead of paging never.
- The unwatched-metric guard's candidates include the sweep's whole textfile: a series added to the runner is watched by a rule or excluded with a reason in `tests/test_infra_alert_rules.py`, or the guard fails.
- The alert comments no longer enumerate blind spots: each of T0167's four is a rule, a test, the daily read, or a drop recorded in the topic.
- The engine executor and flatten tests assert what their docstrings claimed (T0165), and three of the alert-rule guard's stated limits are assertions (T0166); each item's disposition, including the owner's recorded drops, is in the archived topics.

## 2026-09-05 — T0170: the CLI's paper trading is measured against the no-testnet substitution, and the MCP server fronting it is dropped

- `docs/research/91.kraken-cli-paper-trading-assessment.md` settles whether Kraken's official CLI reopens §12's "no spot testnet — shadow plus tiny-live replaces it": it does not, so shadow mode and the 6b rungs stay the evidence path and no gate reads a paper number. Spot paper refuses every short, prices every order size identically, ignores the venue minimums 6b exists to work around, and defaults below the cheapest round trip the venue allows. Cite the report rather than re-deriving it when the substitution is questioned again.
- Reach the CLI through the shell with `-o json`, never through an `mcp__kraken__*` tool: the MCP rewrites tool calls to argv and re-enters the CLI's own dispatch, so it adds nothing, and it exposes strictly less — the recording, replay, session-start and streaming commands are absent from its catalog. Its dangerous-tool gate is a flag the calling model sets on itself, where a shell invocation passes the harness permission prompt.
- `fleet-deploys.md`'s invariants now carry the boundary: `kraken-cli` is workstation-only and is never installed or invoked on a fleet host — not the engine node, not the capture pair, not ops — so no role, compose file, image or unit may install it. Its account reads run from the workstation against the API, which needs no host presence.
- The refdata sweep reads the account's live fee tier itself: step 7 runs authenticated `kraken volume` for the taker and maker rate, the 30-day volume and the next tier's threshold, and the attended half narrows to the full ladder's shape and AoP qualification. `kraken-fee-schedule.md` no longer tells its reader that nothing automated can refresh those numbers, and both surfaces name the API read as preferred over the public `AssetPairs` fee columns.
- Removing the MCP registration is the owner's edit in user-scope config rather than a repo change; \[[T0170]\] tracks it, along with the reconciliation reader ripe at go-live and the microstructure cross-checks ripe at the next alpha search.
