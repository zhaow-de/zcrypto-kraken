# Rendered by the `ops` Ansible role — do not hand-edit on the host.
# Viewer-scoped service-account token, deliberately NOT `grafana_sa_token`: that one is the push
# credential and carries a wider blast radius than a call to /api/search needs.
ZCRYPTO_GRAFANA_KEEPALIVE_TOKEN={{ grafana_ro_token }}
