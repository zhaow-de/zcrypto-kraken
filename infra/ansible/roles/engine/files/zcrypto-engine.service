# Installed by the `engine` Ansible role at /etc/systemd/system/zcrypto-engine.service —
# do not hand-edit on the host, edit this file and re-converge instead.
[Unit]
Description=zcrypto engine daemon (Docker Compose)
Requires=docker.service
After=docker.service network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/zcrypto-engine
ExecStartPre=/usr/bin/docker compose -f /opt/zcrypto-engine/compose.yaml pull
ExecStart=/usr/bin/docker compose -f /opt/zcrypto-engine/compose.yaml up
ExecStop=/usr/bin/docker compose -f /opt/zcrypto-engine/compose.yaml down
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
